# Changelog
> Older entries live in [CHANGELOG_ARCHIVE.md](CHANGELOG_ARCHIVE.md) -- this file keeps the most recent ~10.

## 7.226.3 -- 2026-08-24 -- Producer Dry-Run Regression (T-1154)

- T-1154: adds a real public-CLI regression proving `qq --dry-run` and
  `ee --dry-run` leave existing producer namespaces byte-identical.
- T-1154: the control covers staging, settled/superseded directories,
  producer epochs and existing kitchen bytes; the focused test and all 117
  unit tests pass.

## 7.226.2 -- 2026-08-24 -- Cyrillic Shortcut Routing (T-1153)

- T-1153: routes whole-message shortcuts through one shared Unicode
  codepoint resolver before CLI dispatch; the six Cyrillic twins now enter
  byte-identical canonical Latin paths.
- T-1153: pins `сс` to `cc` (continue), never Latin `ss` (stop), and derives
  the six-twin/nine-without count mechanically from the canonical table.
- T-1153: makes `cc` the uniform safety-valve continuation key for goal and
  converge intent; bare `saipen goal` remains create/pivot only.
- T-1153: adds route-echo boundary coverage, 46 routing tests and validator
  red controls; 116 unit tests, all routing probes and 271 audit mutations pass.

## 7.226.1 -- 2026-08-24 -- Producer Translation and Wiki Rebind (T-1152)

- T-1152: force-refreshed and independently audited all 32 locale READMEs
  against the current normalized English source; the canonical digest remains
  `2a33e364c3c12e8b1b9b2caf41b05db3ee27f17161336579ae85ee59da34fe56`.
- T-1152: repaired the Turkish scenario-fixture Markdown link and removed its
  leaked English fragment; refreshed the DED rendering.
- T-1152: rebound the nine-page saiwiki package to current Core truth, including
  256 contiguous conformance rows, 15 shortcut routes and the exact `cc`
  safety-valve continuation contract.
- T-1152: published strict current producer packages SAIT-022 and W-039; both
  producer collection gates and the converge gate pass.

## 7.226.0 -- 2026-08-20 -- V7 Producer Parallelism Hardening (T-1100)

Safe parallel execution of isolated SubSaipen PRODUCER roles (saitranslate /
saiwiki) WITHOUT Concurrent Mode and WITHOUT a second canonical Core writer.
Core stays the sole main-tree writer; `saipen crew` stays serial.

- T-1100: dependency-aware READY packages carry `base_source_head`,
  `base_source_tree_fingerprint`, `role_revision`, `read_set` (path->hash),
  `write_set` (target->before hash), deterministic `package_identity` and
  whole-tree fingerprint for provenance.
- T-1100: integration classification -- identical global source identity =
  CURRENT fast path; changed identity revalidates read_set + write_set by
  CONTENT HASH (never mtime) -> COMPATIBLE_DRIFT (usable) or STALE (reprepare).
- T-1100: explicit conflict model derives `A.write ∩ B.write`,
  `A.write ∩ B.read`, `B.write ∩ A.read` and exposes the exact reason.
- T-1100: atomic prepare publication -- payloads/manifests/metadata land in a
  non-READY staging generation; READY appears only via a final `os.replace`
  switch; a crash leaves only incomplete staging, never a false READY.
- T-1100: producer-LOCAL lock (`ProducerLock`) serializes same-producer writers
  and grants NO authority over the canonical main tree; cross-producer
  (saitranslate + saiwiki) runs concurrently. Core integration still requires
  the canonical `project_writer_lock`.
- T-1100: per-namespace producer epoch -- a stale worker whose epoch was
  superseded by a takeover cannot publish READY.
- T-1100: idempotent package identity -- identical prepare reuses the READY
  package instead of duplicating OUTBOX/package records.
- T-1100: hard capability boundary (`assert_producer_capability`,
  `guard_core_mutation`) refuses Core STATE/BOARD/LOG mutation, integration,
  collect/disposition, commit/tag/push, ship -- CAPABILITY_DENIED, zero writes.
- T-1100: multi-package integration plan shows READY packages, base identities,
  CURRENT/COMPATIBLE_DRIFT/STALE, read/write conflicts, deterministic order and
  which package must regenerate; no auto-rebase of stale packages.
- T-1100: conformance suite `tools/test_v7_producer_parallelism.py` exercises
  matrix A..N (concurrent ee+qq, same-producer serialization, crash safety,
  stale epoch, idempotency, compatible drift, STALE on read-dep change, same
  output collision, unrelated vs declared input drift, capability denial,
  restart recovery, serialized integration) -- 14/14 pass.

## 7.225.0 -- 2026-08-19 -- Performance Wave

- T-1008: pending recovery cost is governed by unresolved work rather than settled-history length.
- T-1020: crew history capture is one-pass.
- T-1021: roster projection is linear.


## 7.224.4 -- 2026-08-14 -- hostile integrity sweep: prose is never mechanical evidence (T-1003)

- T-1003: every expected crew blocker is now a structured closed-code REFUSE (HOME_REQUIRED, CREW_BLOCKED) -- never a traceback; the blocked circuit names stage/role/reason and an inspect-required next action.
- T-1003: Core closure exemption comes only from an exact closed blocker-class token (HELD/FUTURE_GATE/PERMANENT_WARNING_OWNER/WAIT_USER_CONFIRMATION/ACTIVE); free prose in a description or blocker detail is inert.
- T-1003: `parse_board` is the ONE Core ticket parser (validator consumes its model); a duplicated known field is a parse error, never first-wins or last-wins.
- T-1003: the shared-contract SOURCE is a closed mandatory inventory -- a broken/incomplete saipen_home refuses INVALID_SOURCE_HOME with zero writes and zero obsolete deletion.
- T-1003: the inherited shared tree is exact -- unexpected descendants (TEMPLATE/EVIL.md) make the contract NOT current, are reported by path, and are never auto-deleted.
- T-1003: saiwiki has ONE current ticket namespace (W-), an explicit read-only LEGACY boundary for pre-current history, and the validator enforces the same expected role/prefix as worker health; PROTOCOL.md's documented prefixes match the executable registry.
- T-1003: producer OUTBOX packages carry an explicit `legacy: true` boundary -- pre-provenance history stays readable, immutable and non-collectable without poisoning a strict current package.
- T-1003: collection dedup uses only structured witnesses (MANIFEST last_collect + the journaled collect event); a package_identity SHA in free prose is not evidence, and autonomous collected hypotheses insert at the end of Core TODO without preempting existing work.
- T-1003: one shared `validate_sub_state` backs worker health and the validator; a schema-invalid worker state reports INVALID, never CURRENT.
- T-1003: crew release truth comes from one canonical release-engine verdict (UNKNOWN/AMBIGUOUS are not PASS); post-ship certification requires the working tree to equal the shipped commit exactly.
- T-1003 second hostile root pass: ONE PHYSICAL BOARD RECORD == ONE TICKET IDENTITY -- a second `- [ ] T-###` opener embedded in a ticket line is a parse error (the live T-473/T-576 and T-407/T-406 merges are split back into independent records); `parse_board` rejects them and the validator goes red on the reproduced corruption.
- T-1003: the sub STATE/BOARD pair is one coherent machine -- one shared `validate_sub_lifecycle` binds DONE/task-none, task == the single DOING ticket, role-valid ticket prefixes and truthful BLOCKED blockers; the real saiwiki DONE/W-031 mismatch is INVALID in the validator and in worker health.
- T-1003: ordinary tickets reaching SHIP under an active crew epoch DEFER_FOR_CREW instead of deadlocking -- a committed structured crew-defer receipt (crew_epoch, ticket, exact reviewed scope identity + per-path hashes, source identity) closes the ticket locally with zero git writes and Core returns to the planner.
- T-1003: the terminal crew release surface is DERIVED from committed defer receipts (never git status, never prose); the SHIP is executable through the existing release executor via a mechanically-owned carrier, and terminal authorization is one explicit `crew_ready_for_terminal_ship` predicate requiring SC-0..SC-10 all present and satisfied (missing evidence != PASS).
- T-1003: CURRENT != FRESH FOR THIS CREW EPOCH -- role evidence certifies a new SC stage only when a committed `crew_run` receipt binds the package identity to epoch + role + exact source + role_revision; producer integration is a structured S0->S1 edge, never a rewritten package provenance.
- T-1003: the crew epoch derives from the structured COMMITTED converge_intent operation receipt (not LOG prose); the final gate binds the committed finalize_crew operation receipt and a published `crew_release_evidence` record; release continuation identity comes from committed release receipts, not a `ship vX` substring in LOG text.
- T-1003: `release_verdict` calls ONE real read-only verifier -- a receipt claiming REMOTE_VERIFIED is not remote verification (branch tip + peeled tag + version relation queried independently; remote unavailable is UNKNOWN); receipt selection fails closed on malformed siblings and competing terminal receipts are AMBIGUOUS; no-publish crew closures verify LOCAL closure truth with zero Git requirements.
- T-1003: WAIT_ROLE:<role> is a structured crew-owned blocker -- the planner routes to the owning built-in role and disposes the ticket on real evidence; `.saipen*` prefix names like `.saipenicious.py` are NOT excluded from post-ship certification (exact `.saipen/` boundary); crew plan `ok`/`crew_complete`/`action_required` are separated so a valid nonterminal plan is not a CLI failure; every serial SC role runs IN THE CURRENT AGENT.

## 7.224.3 -- 2026-08-14 -- remove dead SubSaipen helpers (T-1000)

- T-1000: `_outbox_blocks` and `_hash_or_empty` were definition-only after the strict OUTBOX and lifecycle rewrites; a repo-wide reference scan found zero uses and no dynamic contract, so both were deleted. SAICREW, Ruff and the full scenario suite stay green.

## 7.224.2 -- 2026-08-14 -- durable sub-sync receipt lineage selection (T-1001)

- T-1001: the canonical sub-sync ownership receipt is now the one whose OWN `created_at` is newest, never the `operation.json` filesystem mtime -- a copy or touch can push an older committed inventory's mtime forward and feed the wrong obsolete reconciliation. Receipts sharing the newest committed second with different inventories are ambiguous and fail closed (sync refuses, zero writes); a receipt with a broken durable timestamp is never selected and a fresh sync self-heals. Six hostile lineage controls plus the obsolete-delete and crash-resume controls pass.

## 7.224.1 -- 2026-08-14 -- sub-sync ownership receipt bound into the SAICREW finalizer CAS (T-1002)

- T-1002: the crew snapshot now binds the exact selected sub-sync ownership receipt (path + bytes) into the finalizer's CAS alongside the epoch and release receipts. Deleting or mutating that receipt after the green snapshot and before APPLY returns `STALE_STATE` with zero Core writes; the positive finalizer is unchanged.

## 7.224.0 -- 2026-08-14 -- SAICREW fixed-point orchestration and SubSaipen trust completion (T-999)

- T-999: `sc` / `saipen crew` is now a mechanically resumable serial circuit over the full built-in crew. Its durable `converge_target: crew` state is derived from live role, OUTBOX, shared-contract, release and Core evidence instead of a mutable stage counter.
- T-999: shared-contract sync now has receipt-bound ownership, safe regular-file inventory, exact obsolete-path reconciliation and source/read CAS. Spawn establishes that contract first; collect treats a truly empty targeted OUTBOX as a truthful no-op while refusing malformed or non-ready evidence.
- T-999: crew snapshots and finalization close every evidence race found in REVIEW. File/tree/source identities, role packages, pending operations and release receipts are captured before the snapshot barrier and rechecked at APPLY; stale proof writes zero Core bytes.
- T-999: journal recovery now covers directory and missing-file dependencies plus the crash boundary where a target already carries its after-hash. The hostile matrix carries 138 SAICREW controls and the full scenario suite passes.

## 7.223.16 -- 2026-08-13 -- false-success repair (T-998): ship pending v7.223.15 content + untracked-scope fix

- T-998: _surface_dirty now includes untracked non-ignored files (git ls-files --others) so a scope whose only change is a new file can never round into NEEDS_CLOSURE and skip its content commit (v7.223.15 false-success). Regression test 14 proves an untracked-only scope now creates content commit A.
- T-998: ship the pending content v7.223.15 omitted: KNOWLEDGE/HABITS-browser-hang.md, the executor deletion-scope support, the T-996 scope record.

## 7.223.15 -- 2026-08-13 -- track the cross-agent browser-hang habit note (T-996)

- T-996: track KNOWLEDGE/HABITS-browser-hang.md so a fresh clone carries the habit note validate.py already accounts for in shipped-doc coverage.

## 7.223.14 -- 2026-08-13 -- orphan cleanup (T-997): remove tracked zero-ref SAIUI command doc; board ## DONE scrub under RFC 1.2 soft cap

- T-997: delete the tracked orphan .saipen/SAIUI_CORE_INTEGRATION_COMMAND_V2.md (zero references) via the release executor scope deletion path.
- T-996/T-997 wave: board scrub pruned 11 stale ## DONE tickets, kept the T-549 barrier anchor.

## 7.223.13 -- 2026-08-13 -- release trust repair (T-994): one recovery-visible release operation, exact reviewed scope, no-publish matching ship.md, canonical first-publish WAIT, project-bound immutable plan

- T-994: release executor is one SAIOPS recovery operation (release-<hex>) under .saipen/recovery/ops/; every external fact is classified expected-BEFORE / expected-AFTER / CONFLICT and recovery never repeats a side effect (crash A->B->tag edges proven).
- T-994: release scope = the exact reviewed ticket scope recorded at REVIEW->SHIP (`saipen scope`) + mechanical release metadata; a real source change now ships into a fresh clone.
- T-994: no-publish matches ship.md exactly (zero staging/commit/tag/push, local validation, truthful skipped-publish event, digest, SHIP->DONE, works git-less).
- T-994: first publish is a journaled canonical WAIT with confirmation evidence, never chat memory.
- T-994: every public refusal returns a code from errors.CODES; internal stage failures collapse to RELEASE_FAILED.
- T-995: release receipt write failure surfaces through the public result (RELEASE_FAILED) with zero later remote stages.

## 7.223.12 -- 2026-08-12 -- one canonical release executor for `saipen ship` and `saipen push`

`saipen ship` and `saipen push` now dispatch to ONE release executor (`saipen_engine/release.py`). A frozen `ReleasePlan` owns the decision: release version, branch, commit message, tag, the exact release metadata surface (VERSION + README + CHANGELOG + all 32 mechanically mirrored locale badges), foreign pre-existing staging detection, and the pre-ship index snapshot. `plan_release` builds the plan with ZERO writes and validates version parity; `execute_release` is the only place bytes change: exact staging -> `--gate ship` -> `git diff --cached --check` -> commit -> current-branch push -> tag push, in that order. Foreign pre-existing staged paths refuse the plan before any write (never committed, pushed or tagged) and stay byte-identical. Dry-runs are zero-write and, after invocation-name normalization, `ship` and `push` produce structurally identical plans. Scenario controls prove plan parity, dry-run no-mutation, foreign-staging refusal + preservation. (T-635)

## 7.223.11 -- 2026-08-12 -- claim-to-proof VI: real critic dogfood + foundation seal

The foundation wave (T-992 provenance chain, T-991 role freshness, T-639 audit isolation, T-638 pre-apply integrity) is sealed with a REAL cold role=critic Improve lifecycle run through the public CLI path only: admit -> submit (NO_FINDINGS run) -> complete (bound bar) -> verify PASS -> cycle-complete -> clean/archived, with zero raw evidence edits. The EVIDENCE_ADVERSARY lens joins SAICRITIC: adversarially falsify a green claim's proof linkage (stale fingerprint, wrong seat, duplicate identity, missing gate receipt, wrong source, malformed-but-parseable ledger) -- a gate that stays green is a finding, never a PASS. Every T-638/T-992/T-991/T-639 claim was adversarially falsified and every gate went red as required; NO foundational P0/P1 reproduced. claim_to_proof_VI.md documents the five-dimensional repaired claims (UNIT/COMPOSITION/CANONICAL/GATE/PROVENANCE). One ordinary post-fix sequential release (this one) is the stability seal; its exact commit is recorded for the T-442 v8 gate. Full gates ran fresh TWICE. (T-625)

## 7.223.10 -- 2026-08-12 -- Improve pre-apply semantic integrity + false-evidence cleanup

Every Improve lifecycle mutator now validates BASE then PROPOSED state before ANY byte is written. `load_valid_manifest` reads, validates and snapshots a cycle manifest in ONE pass (no first-read/second-read ambiguity) with an allowed-status gate; abort/complete/archive validate their own proposed manifest before journaling, and `create_cycle` refuses an invalid created_at/project_identity with ZERO writes (no directory left behind). `write_sweep_entry` validates the existing SWEEP ledger and the owning report via the bound bar before appending, and validates the proposed ledger -- a malformed SWEEP or malformed COMPLETE report is never extended. `validate_bound_report` is the ONE shared proof bar (structural + roster-bound provenance + source freshness), wired into resume, append, complete_report, verify_cycle, complete_cycle and the validator's ACTIVE strict scan; a report's agent is compared to the ROSTER seat (its owning directory), never to itself. `create_report` and `prepare_audit_seat` DERIVE protocol fingerprint/version from the installed SAIPEN -- a caller-supplied digest is refused on mismatch, so fabricated provenance can never be created, resumed, completed, verified or sealed. Resume refuses forged metadata with INVALID_REPORT/STALE_REPORT. `cycle_aborted` has one legal meaning (ARCHIVED + canonical `draft-preserved`); ACTIVE/COMPLETE+aborted, duplicates and unknown values are invalid. Canonical CLEAN pruned 9 stale DONE tickets (keeping the needs-chain) so the board is back under the soft cap. The warn-ownership harness proves isolation by asserting the WARN slug SET delta, never just returncode. Ticket-ID allocation reads STRUCTURED records only (BOARD ticket lines + LOG `[T-###]` field) -- prose mentions of T-NNN can no longer poison the allocator. Hostile controls cover the full mutator matrix. (T-638)

## 7.223.9 -- 2026-08-12 -- audit harness warn-slug isolation

The `audit_checks.py` warn-slug ownership probe was failing for a condition it created: the probe's own synthetic owning ticket pushed the board over the soft cap, so `board-soft-cap` became another aged unowned WARN and the run failed on harness contamination rather than on the slug under test. The probe's green leg now explicitly asserts that filing the owning ticket creates no other failing warn slug -- the isolation guarantee is proven, not incidental. The probe red/green pair isolates exactly the aged unowned slug: the red leg fails with "no live BOARD ticket names it", the identical slug with a live naming ticket passes, and the probe's own action cannot introduce a second failure. (T-639)

## 7.223.8 -- 2026-08-12 -- SubSaipen role freshness fails closed: UNKNOWN is never FRESH

`sub_collect` no longer treats an unverifiable role revision as current. `_role_status` is a tri-state verdict: `current` (readable charter, matching revision), `stale` (readable charter, different revision), or `unavailable` (missing home, missing charter, or a read/hash failure). `_role_current` now returns True ONLY for `current` -- a missing home, missing charter, or exception is UNAVAILABLE, never silently fresh. `sub_collect` refuses ready evidence whose role revision is superseded (STALE) or unverifiable (UNAVAILABLE) with a structured `PACKAGE_INCOMPLETE` reason. Empty role revisions were already refused by the completeness check. The blanket `except Exception: pass` in the freshness path is gone. Controls cover the verifiable-pass, superseded-stale, and unverifiable-unavailable cases. (T-991)

## 7.223.7 -- 2026-08-12 -- Improve provenance chain integrity: manifest-owned fingerprint, strict shared validator, install-only version

The external provenance audit found the shipped T-624 header-writer fix incomplete: the provenance CONSUMER still trusted fabricated values. This release finishes the chain. `installed_protocol_fingerprint()` derives from the ONE canonical inventory in `saipen/MANIFEST.json` (every required `saipen/` owned doc plus all phase docs, so IMPROVE.md and SAICRITIC.md are covered), hashes FRAMED records (relative path + byte length + raw bytes) in deterministic order, and REFUSES a missing required owned document instead of hashing whatever exists. `validate_strict_provenance()` is the one shared strict validator: required scalars non-empty, no CR/LF/control injection, no unknown header fields, and agent/seat, project/manifest-identity, saipen_version/installed-version and fingerprint/installed-fingerprint must MATCH -- wired into prepare_audit_seat, create_report, and the validator's ACTIVE strict report scan (archived/complete history keeps its historical identity). `saipen_version` comes ONLY from the SAIPEN install, never the target project's VERSION (a foreign 1.2.3 can no longer launder into report identity). The strict writer derives agent from the seat and project from the owning manifest; the caller supplies observations, not identity facts Python can derive. `SAIPEN_RUNTIME` is stripped/bounded/control-free. Validator freshness no longer swallows exceptions (UNKNOWN is never FRESH). IMPROVE.md's abort contract now matches the writer's at-same-path preservation. Hostile controls cover fingerprint mutation/missing-file/cross-directory identity, every strict provenance shape, and the foreign-VERSION case. (T-992)

## 7.223.6 -- 2026-08-12 -- Improve evidence provenance truth: derived fingerprint, truthful runtime, honest context

`saipen improve` no longer fabricates report identity. The protocol fingerprint is derived by hashing the installed protocol's normative documents (CORE, MAINTENANCE, BOOT, STYLE, INDEX + every phase doc) -- a report binds to the exact protocol bytes the audit ran against, and editing any protocol file changes the fingerprint of every subsequently prepared report. The model/runtime is no longer a guessed `deepseek-reasoner` constant: it reads the `SAIPEN_RUNTIME` environment variable when present and falls back to the truthful neutral value `unknown`. Context begins as `partial` instead of claiming `complete`, because a freshly prepared assignment has not proven completeness. Red controls prove the CLI-prepared header carries the derived fingerprint, never a guessed model constant, and that mutating a protocol document changes the fingerprint; strict and legacy cycles still validate. (T-624)

## 7.223.5 -- 2026-08-12 -- CI/local lint parity: one canonical pinned ruff surface

`harness.md` and `.github/workflows/validate.yml` now run the SAME canonical lint command (`python -m ruff check tools/ tests/`) against the SAME pinned ruff version (`ruff==0.16.0`) -- CI no longer installs an unpinned ruff against VERIFY's pin-gate-dependencies rule, and the local doc no longer documents a narrower `tools/` surface than CI enforces. `tools/validate.py` gains a lint-parity check that reads both files and FAILs on a surface divergence, a version drift, or a missing pin. `harness.md` states that a host without Ruff must report lint as missing evidence, never as green. Red controls prove the parity check can go red on surface drift, version drift, and an unpinned CI install. (T-628)

## 7.223.4 -- 2026-08-12 -- Improve submit input normalization: structured refusal instead of tracebacks

`saipen improve submit` now validates the findings payload shape before any access: a top-level array, scalar, null, or string refusals with a structured `VALIDATION_FAILED`, and `run_text` must be a non-empty string (non-string, empty, whitespace-only, and missing values all refuse the same way). Previously these crashed with a raw `AttributeError` traceback, and an object missing `run_text` returned an untyped exit code. Unsupported surplus positional arguments are rejected where the submit grammar owns exactly four. The expected-vs-programming-error distinction from T-623 is preserved: shape errors are structured refusals, genuine programming errors still propagate. Hostile controls cover every malformed shape plus a valid append and the surplus-arg refusal. (T-629)

## 7.223.3 -- 2026-08-12 -- brochure adoption: translated presentation brochures join the README

The user-requested brochure set produced in E-3115 is adopted without rewriting history. `BROCHURE_EN.md`, `BROCHURE_RU.md`, `BROCHURE_ET.md`, and `BROCHURE_JA.md` join the existing `BROCHURE_DED.md` at the repository root; the README Documentation rows on `README.md`, `README.ded.md`, `README.ee.md`, and `README.ja.md` link all five. The validator already declared the set in `ROOT_ALLOWED` and `EXEMPT`. Content-only change, no code touched. (T-636)

## 7.223.2 -- 2026-08-12 -- scheduler cleanup sweeps killed-run temp files

`bootstrap/schedule.ps1 remove` and both global uninstallers now also remove the runId-scoped temp artifacts a hard-killed background run leaks under `%LOCALAPPDATA%\saipen`: the `source-<runId>.zip` archive, the `source-<runId>` snapshot directory, and the `inject-<runId>.log` capture. The canonical `inject.log` is never touched. The sweep was previously limited to the fixed-path `scheduled-source-previous` backup, so repeated kills (taskkill, power loss) grew unbounded temp state that nothing cleaned. Hostile controls seed each leftover shape and prove schedule remove plus both uninstallers clear them while the canonical log survives. (T-637)

## 7.223.1 -- 2026-08-11 -- one scheduler, external atomic wrapper, reversible upgrades and complete cleanup

`bootstrap/schedule.ps1` is now the sole scheduler manager; the obsolete `tools/schedule_autoinject.py` implementation is removed after a full consumer sweep. The hidden VBScript wrapper is generated atomically under `%LOCALAPPDATA%\saipen`, encoded as UTF-16 so Unicode clone paths execute intact, and never dirties the repository. Installation migrates the shipped `saipen-autoinject` task, snapshots an existing canonical task and wrapper before replacement, and restores both on failure. Manager removal and both global uninstallers clean `saipen-inject`, `saipen-autoinject`, and the runtime wrapper while failing closed on task-query or delete errors; PowerShell falls back to `schtasks` when ScheduledTasks cmdlets are unavailable. Background runs refuse dirty or racing source, inject one archived HEAD through a persistent reversible runtime snapshot, and never fetch or mutate the development clone. Health binds the current interactive principal, exact task action, indefinite 15-minute trigger, runtime policy, and canonical wrapper bytes; degraded installations cannot run. Release preparation now binds every staged version surface to working bytes before commit and restores the exact prior index tree on failure. Hostile controls cover clean install/remove, Unicode wrapper launch, failed upgrade restoration, query/delete failures, immutable source refusals, scheduler-definition drift, both global uninstallers, release mirror drift, and exact staging rollback. Full scenarios, validator, audit 270/270, Ruff, and diff checks pass. (T-634)

## 7.223.0 -- 2026-08-11 -- evidence-continuity closeout: one unambiguous identity, stale sources never resume, abort is crash-safe, log tail is a real max

The PRE-v8 evidence-integrity closeout (T-630 + T-632 + T-633). A DRAFT Improve seat can no longer resume across a tracked source-identity change: prepare re-captures the current source identity and refuses STALE_REPORT with zero writes, no silent rebase and no provenance rewrite (A1). The active MANIFEST is validated against its own directory identity before any seat allocation, admission, unavailable handling or resume -- an invalid roster returns INVALID_MANIFEST and is never consumed or mutated, and register_seat/create_report are gated the same way (A2). Strict seat fields are exactly-once: a duplicated role/report_path/availability is INVALID_MANIFEST, never a first/last-value pick, and availability:bogus can never resume as ALREADY_ASSIGNED (A3). Strict RUN and finding identity is injective: RUN numbers unique/ascending/contiguous 1..N, canonical IMP-NNN grammar, and unique composite <RUN>/<IMP> identities -- a duplicate composite can never complete, so one disposition can never be deduplicated into a swept verdict (A4). discovery_model joins REQUIRED_HEADER for strict reports with a deliberate legacy boundary for the three historical cycles, and the writer/validator field sets are parity-tested at write time (A5). The public engine root is normalized once on entry, so relative and absolute references to the same project behave identically (A6). Improve abort is crash-safe: ONE journaled manifest write (archived + cycle_aborted) with no raw rename -- draft reports stay byte-identical at their path, and a forced journal failure or crash at any stage leaves no split active-manifest/discarded-report state (T-632). log_tail_event returns the actual maximum E-ID, independent of line or file enumeration order, and sealed segments sort numerically (T-633). Hostile HUNT closeout added the ledger-side duplicate-composite refusal and the create_report manifest gate. Gates: validator PASS, run_scenarios full green (incl. 7 log-tail + A1-A6 + abort crash-safety controls), audit 270/270, ruff clean, floor audited. (T-630, T-632, T-633)

## 7.222.0 -- 2026-08-11 -- the human gate becomes mechanical: blocker is active blocked-state data and a human-decision ticket cannot be made workable

`| blocker:` was advisory: a ticket under `## TODO` carrying a human-decision blocker validated clean, was picked as top_workable, and was claimable by normal AND explicit claim -- so moving one Markdown line converted "human decision required" into "workable TODO" (reproduced live as the unjournaled T-576 drift). T-631 makes the section the status: blocker is present and non-empty exactly under `## BLOCKED`, TODO/DOING/DONE plus blocker and BLOCKED without blocker all FAIL, and defense in depth independently excludes malformed blocker input from the router, normal claim, and explicit claim -- a priority override may override priority, never authorization. `ticket block` requires a reason and `ticket unblock` requires the lifting decision; both own the field atomically, VERIFY history stays in `verify_attempts:` + LOG, and blocking the ACTIVE ticket parks the execution state at DONE/task none with a truthful mid-flight `transition_from` and a routed next action instead of fabricating a session-level block -- a transitional DONE edge accepted only while the active LOG's most recent event for that `## BLOCKED` ticket is a canonical, `[op:]`-provenanced `ticket block via SAIOPS (active)` line. A board the shared parser cannot read whole (a typo'd `| blockr:`, a missing/duplicate heading, a ticket outside the four sections, a checkbox/section disagreement) routes to inspection and refuses every mutation instead of laundering; the Pick Rule now also excludes a live foreign § 1.4 claim; ticket/blocker/verify payloads are reversibly escaped so a pipe or line break cannot inject fields; `board.schema.json` machine-describes the invariant. Gates: run_scenarios green (28 fixtures incl. block-park green/red pair, 185 nitro-integrity, 120 Improve), validator PASS, audit 270/270, Ruff clean. (T-631)


## 7.221.0 -- 2026-08-10 -- the Improve wave becomes mechanical: command family, report schema, sweep linkage, reasoning gates, coverage

The Improve meta-control wave (T-553..T-561) is now mechanically enforced end to end. `saipen improve` + its five routes resolve in CORE section 1.10 with a closed subcommand set, no shortcut key, and `improve clean` never entering the CLEAN phase ([improve-command-family]). Improve routing/status is DERIVED from the cycle manifest + reports + sweep ledger, never carried in STATE.md ([improve-state-purity], T-553). Seat reports are mechanically checkable: a finding without its expected/actual/evidence triple, a class/severity outside the closed set, a complete report over an unmet completion bar, a partial scope claiming full context, and a report carrying BOARD headings all fail ([improve-report], T-555/T-557). The Core sweep is the only path from report to canonical work: a `source_reports` ticket field links tickets to SWEEP.md dispositions, and the [sweep-ticket-link] check refuses an unverified finding producing a ticket, an INVALID/ALREADY_FIXED finding carrying one, an edited-away original finding, an unresolvable source_reports reference, a PROTOCOL_VIOLATION ticket missing the META-IMPROVEMENT recurrence and WEAK-MODEL PRECEDENT reasoning gates, and an ACCIDENTAL_SUCCESS result recorded as PASS (T-556/T-558). `saipen improve clean` is archive-with-provenance: refuses unswept findings, preserves verbatim, provenance must keep resolving ([improve-boundary], T-557/T-559). All 25 spec red controls have a named mechanical check or behavioral probe (KNOWLEDGE/improve_red_controls.md, T-560). Gates: run_scenarios green (68 improve + 171 nitro-integrity behaviors), validator PASS, audit 240/240, floor + parity green. (T-553..T-561)

## 7.220.0 -- 2026-08-09 -- NITRO dogfood IV: gate integrity -- a valid end state is not proof of the required process

The fourth proof level. `finish_ticket` previously wrote `transition_from: SHIP` regardless of the actual phase, so finishing a ticket from SCOUT/BUILD/VERIFY/REVIEW produced a syntactically legal `DONE` whose phase history was fabricated -- final bytes validated, the gates never ran. The permanent invariant: VALID END STATE != PROOF OF REQUIRED PROCESS. Ticket completion now requires the ticket to have actually reached `phase: SHIP`; from any earlier phase `finish_ticket` (`saipen ticket done`) REFUSEs `ILLEGAL_PHASE` with zero canonical bytes written, and `transition_from` records the ACTUAL phase, never a laundered legal-looking source. The public `claim->BUILD->ticket done` false-PASS composition precedent is inverted; gate controls prove finish from SCOUT/BUILD/VERIFY/REVIEW refuses, the full `SHIP -> finish` chain ends validator-green, the safety valve can trip mid-ticket (REVIEW stays, ticket stays DOING, the exact WAIT is set, reauthorization completes legally), and a `[gate-closure]` validator check FAILs a fabricated post-boundary non-SHIP closure (self-establishing boundary, same shape as `[saio]`). The historical skipped-gate closures (T-591/T-594/T-595 closed from VERIFY) are recorded as ACCIDENTAL_SUCCESS with incomplete gate proof and re-verified by current evidence (KNOWLEDGE/claim_to_proof_IV.md); the GATE proof level joins UNIT/COMPOSITION/CANONICAL for SAICRITIC. Improve `complete_cycle` now requires full SWEEP disposition coverage for every finding's exact composite identity -- Core can never freeze the artifact before its own sweep finishes (partial sweep REFUSEs, full coverage COMMITs, completed cycles are immutable, the next cycle is admitted). The Improve semantic verifier is target-aware (`verify_improve(root, targets)`) and is the SAME postcondition class on APPLY and Recovery, so a malformed SWEEP.md fails its own verifier and a ledger that parses with the consumers' grammar passes. `resolve_conflict` serializes under the project writer lock: two simultaneous resolvers yield exactly one settlement. The context compiler budgets STRUCTURALLY -- mandatory sections (recovery, computed next action, protected exact next ticket, STATE essentials, routed phase doc, needs/verify) are never truncated; only optional BOARD orientation and bounded LOG evidence shrink, the board map caps truthfully (`... +K more`), and byte/character/token metrics describe the FINAL emitted surface exactly (proven with Cyrillic and Japanese content). Journal compaction is the bounded SETTLED maintenance operation (COMMITTED + RESOLVED only, tombstone kept, evidence classes never compacted). Gates: run_scenarios full green (171 nitro-integrity + 49 improve behaviors), validator PASS, audit 231/231, floor + parity green, R1..R13 reproductions all NOT REPRODUCED. (T-596, T-600, T-601, T-602)

## 7.219.0 -- 2026-08-09 -- NITRO integrity sweep: transactions are truthful, recovery is conflict-safe

The external NITRO audit reproduced 12 defect hypotheses (R1..R12) against the closed M2-M5 machinery; this release is the integrity pass that makes the deterministic mechanics actually deterministic before any further expansion. The engine's transaction journal now records every target with path, role, before_hash and after_hash (no positional LOG/BOARD/STATE pseudo-semantics -- a MANIFEST is never reported as LOG_WRITTEN). Recovery is conflict-safe: per-target live hash == before applies staged bytes, == after advances, anything else is CONFLICT with evidence preserved; corrupt staged bytes are never committed. Pending-operation preflight runs before every mutation; `saipen recover` exposes pending ops and status/next report real recovery state. PLAN/APPLY are separated around an immutable OperationPlan (one op_id, exact bytes, commit failure always wins over semantic success; WRITER_BUSY/STALE_STATE are structured results). The monolithic state renderer is gone: operations patch only owned STATE fields, so checkpoint/ticket_add/goal preserve phase/task/next_action and unrelated fields. A shared LOG event builder emits mechanical parents and `[op: ...]` provenance markers; the validator's `[saio]` check detects manual structural edits after the first provenanced event. The codec genuinely preserves encoding/BOM/newline; Improve writers are journaled with path ownership, one active cycle, per-seat derived status, full sweep-finding coverage and propagated commit results. Red-control minimum set (50 items from the audit) covered by 44 nitro-integrity behaviors; all 12 reproductions flipped to NOT REPRODUCED; a forced HUNT closed the staged-byte integrity hole; the claim-to-proof matrix for T-578..T-583 is recorded in KNOWLEDGE/NITRO.md. Validator, scenario suite, audit 231/231, floor all green. (T-583, T-584)

## 7.218.0 -- 2026-08-08 -- one name, one execution meaning: `saipen crew` stays sequential

The Crew name collision is resolved (T-571), Option A: `saipen crew` remains the existing strictly sequential circuit, and the future concurrent execution design is renamed Concurrent Mode with its own distinct command name, `saipen concurrent`, recorded in `.saipen/KNOWLEDGE/crew-v8-backlog.md` (T-443 cites the decision). CORE.md section 1.10's `saipen crew` row now states its single execution meaning and that it is never the concurrent design. `tools/validate.py` gains a `[crew-naming]` cross-doc check -- the crew row must keep its single-meaning statement and the gated backlog must carry the distinct command name and the T-571 citation -- so one command can no longer silently carry two execution meanings; two audit red controls prove each half goes red (226/226).

## 7.217.0 -- 2026-08-08 -- optional USERPERSON preference meta-control, off by default

`saipen userperson` is a new optional meta-control: with no `.saipen/USERPERSON.md` it adds nothing to a cold start -- no warning, no boot failure, no placeholder, no onboarding, no cold-start cost. Invoked while absent it runs lightweight onboarding (at most three broad questions, prefer two, or a free-form paste), then writes the canonical profile only after explicit user activation. USERPERSON means default direction, not order: precedence is current explicit request > project/task requirements > SAIPEN > verified evidence > USERPERSON, used only where a valid implementation choice remains unspecified. `add` merges semantically (an equivalent preference is a no-op, never appended history), `remove` drops the matching preference, `reset` requires explicit destructive confirmation, and only explicit user input modifies the profile -- no passive profiling, no mining LOG/chat. When a preference materially influenced a decision the task did not specify, the agent reports `USERPERSON alignment:` once at completion; a deliberate override by stronger constraints is reported as `USERPERSON deviation:`; neither trace appears when USERPERSON had no material effect. SubSaipen handoffs project a relevant subset only (saiui, saitranslate, saiwiki, saihunt) and never dump the whole profile. Implemented before the Improve wave so Improve can classify `USERPERSON_MISS` separately from protocol/project/logic violations. Mechanical core in `tools/userperson.py` (parse/render/merge/remove/validate/projection), the five commands resolve in CORE.md section 1.10, and a present profile is validated structurally. 15 probes plus the `userperson-valid`/`userperson-malformed` fixtures prove the semantics; the shortcut key-count sweep is unchanged (no new key). (T-574)

## 7.216.0 -- 2026-08-08 -- capability-measured platform probes + the active-task/DOING binding

The three runtime files that reached CI for the first time in v7.215.0 (`tools/freshness.py`, `tools/sub_clean.py`, `tools/hardening_controls.json`) were swept for the platform assumptions the manifest-staging paradox let ship untested. `tools/run_scenarios.py`'s tracked-mode-change probe had asserted the source fingerprint moves when `core.fileMode` is true by editing the INDEX (`git update-index --chmod`) while the fingerprint model reads the HEAD-vs-working-tree delta -- it was measuring git's fallback, not the fingerprint, and the fixed probe now records the original mode, chmods on disk, re-stats, asks Git what delta resulted, and SKIPs out loud only where no executable-bit transition can be represented. Symlink and junction capability are now MEASURED probes (`symlinks_available`, `junctions_available`, lazy at first use) wired into every probe that creates such an object; a host that cannot create them gets an explicit SKIP with the capability named, never a crash that hides every later check. The freshness sweep grew nine measured probes: spaces in filenames, Unicode filenames, case-distinct filenames (inode-verified -- a case-insensitive host silently aliases instead of raising, so the naive try/except capability test would have passed vacuously), FIFO objects, a project nested inside another Git repository, no-Git executable bits, and a Windows junction hashed as link identity instead of recursed. `tools/sub_clean.py` closes the reparse boundary: junction/reparse-point directories are never recursed, evidence digests never follow a symlink (a symlink's digest is its link identity), and symlink/reparse recovery evidence is rejected as non-preserved -- an outside file can no longer hash like preserved evidence. `tools/hardening_controls.json` gets mechanical structural checks (list shape, exact keys, unique contiguous IDs, unique names and anchors, canonical repository-relative owners that exist as files). The same Windows run reports 29 role-freshness behaviors executed and 5 SKIPs named by capability; Linux CI reports the complementary set. (T-572)

The v7.215.0 crash exposed a Core state invariant the validator could not see: a checkpoint claimed `STATE.task: T-572` in a ticket-bearing phase while BOARD carried no `## DOING` claim and T-572 sat in `## TODO`, and the tree validated. `tools/validate.py` now binds them -- a ticket-bearing phase (SCOUT, BUILD, VERIFY, REVIEW, SHIP) whose `task:` names a real ticket requires that ticket in `## DOING`, a `task: none` with a self-claimed or unclaimed `## DOING` ticket is the opposite interruption, and a `PHASE`/`RESUME:` `next_action` naming a different ticket than `task:` is a contradiction. A stranger's `## DOING` claim stays legal (`task: none` while another agent holds the ticket), because that is the multi-agent shape the Pick Rule already handles. Two checked-in crash fixtures (`active-task-not-claimed`, `state-behind-board-self-claim`) go red with one focused diagnostic, two audit mutations cover both directions, and a recovery probe performs RFC section 1.5's Recovery on each interruption and proves the rebuilt state validates and that a repeated Recovery is byte-idempotent. (T-573)

## 7.215.0 -- 2026-08-08 -- gate context: a stale producer package stops blocking an unrelated Core ship

The wave's centre is an ownership fix. `tools/validate.py` answered two different questions with one severity, so a stale, malformed or unrefreshed EE/QQ package produced by another model failed the global validator and blocked ordinary Core commits -- seven of ten live FAILs were external producer state. `--gate` now names why the validator is running: bare or `--gate core` is structural conformance; `--gate ship` is everything needed to ship THIS tree, where producer readiness is a visible WARN and blocks nothing; `--gate collect:<producer>` makes exactly that producer hard (malformed, incomplete, stale, wrong `role_revision`, wrong source identity, not ready, or nothing to collect are each a FAIL) while leaving other producers soft; `--gate converge` adds CONVERGE.md stage M's fresh EE and QQ, where missing is also a FAIL. An unknown or misspelled gate exits 2 rather than falling back to the soft default. The validator stays read-only under every gate. CORE.md's collect row, CONVERGE.md stage M and `phases/ship.md` each name the gate they invoke. 14 probes covering all six required red controls, red-tested in BOTH directions -- forcing every gate hard turns five soft rungs red, forcing every gate soft turns three hard rungs red (T-568).

`phases/ship.md` step 6b is reordered around that gate: read the index and working tree, attribute every file, preserve anything the user staged, stage ONLY the reviewed files by explicit path (`git add .` and `git add -A` are forbidden), prove the staged set equals the reviewed scope, THEN run the ship gate, then `git diff --cached --check`, then commit exactly that scope, then push. Before this, the gate ran before any staging existed, so a required runtime file added by the ticket being shipped could never satisfy the tracked-runtime check without an undocumented staging step -- three MANIFEST entries were live in exactly that state, green locally and red in CI. A gate failure now names its rollback: no commit, no push, only this run's staging undone, pre-existing staged state untouched. `[ship-stage-before-gate]` pins stage < gate < commit by position (T-569).

`check_against_schema` claimed to interpret everything `state.schema.json` uses and did not: `minimum` sat on four fields unenforced, so `goal_waves: -1` passed the gate and handed the safety valve a budget below its own floor. `minimum` and `items` are enforced, and `audit_schema_keywords` now FAILs any keyword the schema declares that no enforcer interprets, and any `type:` value outside the four this validator knows -- the class guard, not just the instance (T-565).

The ccc I -> SHIP -> J proof compared raw hash strings, so one commit written at two abbreviation widths read as two commits and the check meant to catch a SHIP that changed nothing concluded it had. `canonical_commit()` resolves every reference through `git rev-parse --verify <ref>^{commit}` and compares full OIDs; the OID pattern widened from `{7,40}` to `{4,64}`, because the old one did not skip a SHA-256 OID -- it matched the first 40 characters and compared a truncated string. Evidence naming a commit the repository lacks now FAILs; a project with no Git at all WARNs rather than failing an unperformable proof. 10 probes including a real `--object-format=sha256` repository (T-566).

`saipen/SKILL.md` still routed readers to bare "RFC" -- "it points into RFC only when a rule question comes up", "before RFC" -- and instructed BOOT twice. Every RFC-trap pattern required the literal string `RFC.md`, so none of it was visible. The loader now says exactly: read BOOT once, BOOT loads STYLE before output, INDEX routes rule questions, CORE answers the exact one, RFC is a compatibility redirect no step sends you to. A semantic rung FAILs a routing verb aimed at RFC in any adapter, SKILL or injector, sentence-scoped so naming RFC as a redirect stays legal (T-549).

Also: the OUTBOX producer/consumer contract closes -- nonempty package text that parses as zero entries FAILs, and every charter's `output_contract` cites canonical PROTOCOL.md § 2 instead of publishing a local field list its own consumer rejects (T-564). `tools/audit_checks.py` gained an optional per-case gate after review found that its deliberate habit of keeping WARN lines would have let every producer control go on passing -- on a warning -- while proving no refusal; a gated case matches FAIL lines only, and an OUTBOX case naming no gate is refused before the suite starts. Re-verified on the final tree: T-562 distribution, T-563 cleanup fail-closed, T-567 Golden Default. audit_checks 221 of 221 with zero not-evidence, up from 211.

## 7.214.0 -- 2026-08-07 -- role freshness is recorded and compared, never trusted

T-542: a subSaipen's findings are only as trustworthy as the charter it ran under, so the charter revision is recorded and compared. At spawn and every adopt, `role_revision` from the built-in charter's metadata block is recorded into the sub's own `STATE.md`; every ready OUTBOX package records the same revision; at adoption, prepare and collect the recorded revision is compared against the current project-local charter after `saipen sub sync`. A mismatch means the package or instance was produced under a superseded role -- the package is `stale`, not collected, and the producer re-runs; a fixer/scout still carrying an old revision revalidates before reuse. `saipen sub sync` refreshes charters and never a live sub's STATE/BOARD/LOG/kitchen, which is exactly what makes the mismatch visible.

`validate.py`: a ready OUTBOX package whose `role_revision` differs from the project-local charter FAILs; a live sub STATE missing it WARNs (legacy grandfather); one carrying an old revision WARNs. `state.schema.json` accepts `role_revision` on sub STATEs. Three probes (`run_role_freshness_probes`: scenario 17 green+red, scenario 19) and one red control; audit_checks 172 of 172. CONFORMANCE row 247.

## 7.213.0 -- 2026-08-07 -- role charters become machine-readable

T-541: every shipped `sai*.md` charter now opens with a fenced YAML metadata block declaring `role_kind` (SCOUT/FIXER/PRODUCER/TOOL), `write_scope`, `trigger`, `collect_policy` (automatic/core-review/explicit), `done_condition`, `freshness_inputs`, `output_contract`, `role_revision` -- the eight keys a tool can read without parsing prose. The two existing charters (saitest = TOOL, saiui = FIXER) gain blocks; four new charters land: saihunt (SCOUT), saipython (FIXER), saiwiki (PRODUCER), saitranslate (PRODUCER). A charter existing does not mean a worker exists -- MANIFEST.md lists only live spawned/adopted instances.

`validate.py` gains [charter-metadata]: every shipped `extensions/subs/sai*.md` must declare all eight keys with `role_kind`/`collect_policy` from the closed sets, and PRODUCER charters must exist for saiwiki and saitranslate. `outbox.schema.json` accepts `source_tree_fingerprint` and `role_revision` (the freshness fields T-542/T-543 bind packages to). PROTOCOL.md § 3.1 requires the block. One new red control; audit_checks 171 of 171. CONFORMANCE row 246.

## 7.212.0 -- 2026-08-07 -- HUNT detects, CLEAN mutates: the ownership split

T-540: two phases both held deletion authority. `hunt.md` carried the proof-of-recovery gate, the 5-file mass-deletion cap and the pre-move reference sweep; `clean.md` carried orphan/trash/kitchen deletion -- two documents, two authority claims, no reader able to tell which scope belonged where, and a sweep that deleted on the spot while the phase meant to own hygiene only ran when asked. Now `HUNT` is detect/classify/ticket/report with no mutation authority beyond BOARD/LOG/STATE bookkeeping: its kitchen scan finds stale files and tickets them for CLEAN, it never removes them. Every deletion, move, rename, prune and relocation happens in `CLEAN` under the gates that moved with the authority.

`validate.py`: [hunt-delete-proof] -> [clean-delete-proof] on clean.md, [move-reference-sweep] repointed to clean.md (the phase that moves now carries the sweep), new [hunt-no-mutation] pin proves the scopes cannot overlap. Four audit cases repointed/rebuilt, each red on its own slug. CONFORMANCE row 222's enforcement updated, row 245 added.

# MASTER AGENT PROMPT — SAIPEN CURRENT STAGE

ROLE

Continue the current SAIPEN project.

This roadmap is direction, not a replacement for canonical STATE/BOARD/LOG.

## FIRST ACTION

Inspect whether Wave 0 is already proven.

If sealed LOG history is still missing or validator is still red:

DO WAVE 0 ONLY.

Do not continue phase compression over a broken evidence graph.

## IMPORTANT CURRENT FACTS

- native Audit Inbox already exists;
- audit/1 and audit/3 were consumed by it;
- audit/2 is active;
- 57 focused inbox tests are green;
- current phase corpus is ~100.3 KB;
- current T-1226 verification is mathematically inconsistent;
- SRC-013 contains premature generic VERIFIED evidence;
- shared producer allocator is absent;
- HUSH remains planned.

## HARD RULES

- restore truth before optimization;
- never fabricate LOG ancestry;
- never delete sealed history;
- never close Source coverage from generic evidence;
- do not manually delete audit/2;
- do not parse audit body as commands;
- active Work outranks audit;
- lowest WORKABLE audit outranks ordinary TODO;
- Improve is last;
- producer claims are not maintainer truth;
- HUSH changes narration only;
- SAIPAL never becomes a Core writer.

## PER-WAVE DISCIPLINE

For exactly one wave:

1. inspect owner contracts;
2. repair/implement smallest coherent scope;
3. add red controls;
4. run focused tests;
5. run validator;
6. run clean-checkout proof where required;
7. checkpoint exact evidence;
8. stop.

Do not silently begin later waves.

## CURRENT SUCCESS PATH

The likely immediate sequence is:

```text
restore sealed LOG truth
→ make core gate green
→ repair SRC-013 coverage truth
→ finish current four phase edits
→ compress remaining phase deltas
→ close SRC-013
→ native inbox consumes audit/2
→ harden lowest-workable audit selection
→ shared producer API
→ HUSH
→ SAIPAL bridge
→ normal backlog
```

# 12 — WAVE 10: RELEASE / BACKLOG / CLEANUP STABILIZATION

## Goal

After audit/2, producer foundation and HUSH are stable, return SAIPEN to boring healthy operation.

Boring is good. Software rarely appreciates drama.

## Release metadata

Resolve current drift:

- VERSION vs CHANGELOG;
- release ledger;
- stale digest;
- tag documentation.

Do not rewrite old tags.

Record truthful release history.

## BOARD cleanup

Current BOARD is well above soft cap.

Move closed-ticket narrative out through normal CLEAN/history semantics.

Do not remove unresolved Work.

## LOG cleanup

After sealed history is restored and graph-valid:

seal active LOG at the normal threshold.

Never repeat the accidental loss of sealed segments.

Add a regression that CLEAN cannot delete canonical sealed LOG history.

## Old W4 zombie tickets

T-1212..T-1221 remain TODO despite much of their work already being incorporated by umbrella audit work.

Reconcile each:

- DONE with evidence;
- MERGED/SUPERSEDED;
- remain TODO only if genuinely unfinished.

Do not allow ordinary `cc` to redo completed Wave 4 work.

## Stale SubSaipen packages

Current warnings show stale/uncollected producer packages.

Use existing collect/discard/reproduce contracts.

Never collect stale packages merely to reduce warning count.

## Existing performance backlog

After Audit Inbox is empty and current audit ecosystem Work closes:

normal Pick Rule resumes existing verified performance/correctness Work.

Do not create endless meta-roadmap priority that starves real engine defects.

## Completion bar

1. release metadata coherent;
2. BOARD manageable;
3. LOG safely sealed;
4. sealed-history retention regression added;
5. old W4 zombies reconciled;
6. stale packages dispositioned normally;
7. ordinary backlog resumes automatically.

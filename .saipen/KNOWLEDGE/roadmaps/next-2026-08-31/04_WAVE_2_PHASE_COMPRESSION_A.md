# 04 — WAVE 2: PHASE COMPRESSION A — FINISH CURRENT LARGE-PHASE WORK

## Goal

Complete the current edits safely without pretending four files can satisfy the whole corpus target.

## W2.1 — Repair T-1226 contract

Do not continue with the impossible verification wording.

Use normal SAIOPS ticket amendment/supersession mechanics.

Recommended interpretation:

`T-1226 = Phase Compression A`

Scope:

- ship
- translate
- markhunt
- clean

Verify local outcomes only.

Suggested safe targets:

- ship <= ~9 KB
- translate <= ~8 KB
- markhunt <= ~9 KB where semantics permit
- clean <= ~8 KB where semantics permit

And:

- all existing red controls green;
- phase-specific semantics preserved;
- no CORE regrowth;
- phase corpus materially reduced toward the final R008 target.

Do NOT claim total <=70 KB at the end of this four-file slice.

## W2.2 — Preserve current useful progress

Current sizes are already lower than HEAD:

- ship ~17.1 → ~12.8 KB
- translate ~13.7 → ~10.9 KB
- markhunt ~10.9 → ~10.2 KB
- clean ~10.7 → ~9.8 KB

Continue from these edits.

Do not restart from HEAD unless current changes fail semantic review.

## W2.3 — High-risk SHIP proof

Before further SHIP deletion, preserve/regress:

- first publish ordering;
- existing-origin behavior;
- no-publish mode;
- tag/branch ordering;
- release metadata;
- push failure;
- post-publish evidence;
- blocked SHIP semantics.

## W2.4 — TRANSLATE proof

Preserve:

- isolation;
- target/source resolution;
- no fabricated locale content;
- cache/staging semantics;
- validation;
- exit behavior.

## W2.5 — MARKHUNT/CLEAN proof

Preserve:

- markhunt finding accounting;
- brakes;
- no-fix semantics where required;
- CLEAN protected-artifact rules;
- history preservation;
- user-confirmation boundaries.

## W2.6 — Close T-1226 only as slice A

T-1226 completion evidence should say:

> largest-phase slice completed; global R008 remains open under Phase Compression B.

Do not disposition SRC-013:R008 yet.

## Completion bar

1. four files hit justified local budgets;
2. focused behavior green;
3. T-1226 verification is no longer mathematically impossible;
4. R008 remains open;
5. next compression Work is created before T-1226 closes.

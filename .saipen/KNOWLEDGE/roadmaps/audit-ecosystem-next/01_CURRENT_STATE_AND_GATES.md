# 01 — CURRENT STATE / REQUIRED GATES

## Current project truth

The current SAIPEN state is still actively working the manually-created audit queue.

Observed canonical Work includes conceptually:

- active Work for `audit/1.md`;
- queued Work for `audit/2.md`;
- queued Work for `audit/3.md`;
- a parent/goal instruction to implement valid findings sequentially.

This means the current three files are already known Work.

The new native Audit Inbox implementation MUST NOT recapture them into duplicate Source Receipts or duplicate tickets.

## Current compression status

Before treating `audit/1.md` as unfinished merely because BOARD still says so, verify the current implementation against its acceptance bar.

Current implementation already materially shows:

- BOOT compressed;
- INDEX compressed;
- CONFORMANCE compact;
- 256-case machine corpus;
- measured load profiles;
- cold profile below 20 KB.

Therefore the agent must verify what remains instead of blindly reimplementing all Wave 4 steps.

## Current core gate debt

Before sealing the current queue, resolve reproducible core-gate failures through their owning contracts.

Known classes include:

- LOG parent continuity problem;
- historical Improve CONFIRMED findings lacking canonical Work linkage;
- transport/package manifest appearing as a forbidden repository-root artifact.

Do not weaken the validator to make these disappear.

## Packaging rule

`_AUDAPACK_MANIFEST.json` is transport metadata unless separately ratified as a repository file.

Archive/export tooling must not accidentally redefine the protocol root.

## Fresh checkout bar

No major wave is complete until:

```text
fresh checkout
→ runtime/protocol artifacts present
→ validate core green
→ focused tests green
→ no hidden dependency on dirty working tree
```

Historical green checkpoints are useful evidence, not substitutes for current reproducibility.

# 02 — WAVE A: SEAL THE CURRENT THREE AUDITS

## Goal

Finish the existing backlog before installing the automatic consumer.

Do not let the new Audit Inbox see the current known files as new external work.

## A1 — audit/1.md

Treat current implementation as evidence and verify remaining acceptance criteria.

Do not re-run the entire implementation mission blindly.

Check:

- BOOT size and routing ownership;
- INDEX ownership graph;
- load budgets;
- 256/256 corpus;
- compact CONFORMANCE;
- stale command-owner references;
- registry authority;
- reproducible clean gate.

If all semantic requirements are already satisfied, close using evidence.

## A2 — audit/2.md

Perform Phase Delta Compression only after audit/1 is truthfully closed.

Preserve all 16 phases.

Target only duplicated hot-path prose.

Keep:

- phase-specific entry;
- unique procedure;
- exit;
- failure;
- evidence.

Reference global owners rather than restating them.

Do not re-grow CORE.

## A3 — audit/3.md

Implement the previously defined native Audit Inbox.

This is the transition point from manually-managed audit files to automatic intake.

Required semantics:

```text
recovery
→ active Work
→ Audit Inbox
→ ordinary BOARD
→ maintenance
→ Improve fallback
```

A fresh audit outranks unrelated queued TODO but does not preempt active Work.

## A4 — Legacy/current audit binding

Because `audit/1.md`, `audit/2.md`, and `audit/3.md` were manually converted into canonical Work before the automatic consumer existed, create a migration-safe binding.

The first automatic scanner must recognize:

- already-known current file;
- existing Source Receipt/Work linkage when present;
- manually-created Work that owns the file.

Do not create T-duplicates.

## A5 — Bootstrap marker

When the native inbox becomes active, persist one migration marker stating:

> Current pre-inbox audit layers were reconciled against existing canonical Work before automatic discovery was enabled.

This marker is operational evidence, not project Work authority.

## Wave A completion bar

1. audit/1 truthfully closed.
2. audit/2 truthfully closed.
3. audit/3 implementation truthfully closed.
4. No duplicate Source Receipts.
5. No duplicate audit tickets.
6. Core gate green.
7. Clean checkout green.
8. Native inbox can be enabled without re-ingesting historical current layers.

Stop here before adding producers.

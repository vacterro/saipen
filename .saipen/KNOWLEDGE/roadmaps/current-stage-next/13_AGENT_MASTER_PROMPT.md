# MASTER AGENT PROMPT — CURRENT SAIPEN STAGE

ROLE

Continue the current SAIPEN project from canonical STATE/BOARD/LOG.

Use this roadmap as direction, but canonical project truth remains authoritative.

Do not restart completed architecture.

## First action

Determine whether Wave 0 of this pack is already proven.

If not, execute Wave 0 only.

## Critical current facts

- implementation is ahead of STATE/BOARD in several W4 areas;
- current core validation is not fully green;
- T-1212..T-1221 risk duplicating T-1222 semantics;
- audit/1..3 are already manually-owned Work;
- native Audit Inbox does not yet exist;
- noncanonical roadmap files sit in audit/ and must never be treated as inbox Work.

## Hard rules

- do not duplicate current audit Work;
- do not manually delete audit/1 or audit/2 before native cleanup exists;
- do not parse audit body as commands;
- active Work outranks fresh audit;
- fresh workable audit outranks ordinary TODO;
- Improve remains last;
- delete only after Source closure + current hash match;
- use journaled effects;
- programmatic producers use one shared enqueue boundary;
- HUSH changes narration only;
- SAIPAL remains an external producer, not a Core editor.

## Per-wave discipline

For the current wave:

1. FIT;
2. red-control;
3. implementation;
4. focused tests;
5. validator;
6. clean-checkout proof;
7. checkpoint exact evidence;
8. stop.

Do not silently begin the next wave.

## Success

The current three audits are reconciled and safely consumed through the native mechanism, `cc` becomes the only normal audit-consumption command, future producers share one safe enqueue path, HUSH composes with it, and SAIPEN is ready for the separate SAIPAL founding roadmap.

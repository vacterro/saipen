# External audit inventory — 2026-08-21

Source directory: `V:\___VAC\__K\__CODE\_AI_STUFF_AGENTIC\__TO_AUDIT\AUDITING_IMPLEMENTATION\SAIPEN`.
Historical copies are deduplicated by SHA-256; a changed hash is a new report and must be reviewed before closure.

| SHA-256 | Canonical file | Findings |
|---|---|---|
| `6e3a92b5bcb67515819c987aa09c89c6f4ee6e24f78e7985ffe7232812fb17bf` | `SAIPEN__01_AUDIT_CORE.md` | CORE-001..005 |
| `9202fc06b0e681ced3cc96fbb0b7e8de32c40abd0466efa0831b98070b5cd001` | `SAIPEN__02_AUDIT_SECOND_WAVE.md` | W2-001..005 |
| `0715e390cbe574b9c1b69789849046fa27df2067b99cb39c982db7a516d98891` | `SAIPEN__03_AUDIT_PERFORMANCE.md` | PERF-001..003 |
| `56eee033998a7ba0dfccf8a4630202c4951b12766483f3b9dfb50098274e57c9` | `SAIPEN__00_AUDIT_ALL_3.md` | byte-distinct combined handoff; repeats the three waves above |

T-1135 and T-1137..T-1141 own the five Core repairs. T-1136 owns validation and closure of the newly arrived Second-Wave and Performance findings plus the independent six-domain audit. The canonical harness is documented in `harness.md`.

## Accepted external findings

- CORE-001..005: fixed. Current-session capability, zero-write dry-run,
  non-fabricated producer execution, failure-safe worker preservation, and
  bounded progress all carry executable regressions.
- W2-001..005: fixed. Handover is folded into the admitting transaction,
  semantic receipts share one strict authority, intent preparation
  prevalidates before mutation, complete sealed+active history is canonical,
  and cleanup debt is bounded and truthful.
- PERF-001..003: fixed. Dependency capture is decision-scoped, one crew action
  reuses one coherent snapshot, and initial semantic receipt decoding reads
  each manifest once while retaining a separate lightweight closing CAS pass.

## Independent six-domain closure

| Domain | Verified defect and closure evidence |
|---|---|
| Optimize | Removed whole-tree producer fallback, duplicate crew snapshots, repeated receipt decodes, per-path Git index queries, and lifetime settled-ledger cleanup scans. Performance gates: 41/41 plus targeted PERF wave green. |
| Bug Hunt | Fixed resolved-journal staged cleanup, stale NITRO fixtures, unique goal allocation, block-parked history binding, release fixture VERIFY/BOARD authority, and a shared-checkout zero-write test race. NITRO: 190/190. |
| Memory | Crew dependency capture hashes only decision-relevant roots; lightweight receipt stability reads each manifest/sidecar authority once and ignores staged payload bulk while structurally tokenizing corrupt membership. |
| Dead Code | Deleted the dormant alternate autonomy loop, unused CLI pending/conflict/top-level corrupt scanners, and an unreachable partial op-ID validator. The remaining legacy loop is documented and tested as crew-only. |
| Secure | Corrupt recovery evidence is one closed `CORRUPT_JOURNAL` class across mutation, recovery, status, next, context, release, and compaction; no corrupt receipt is replayed. Cleanup debt cannot follow a symlink/reparse namespace or escape the project, and operation IDs use the shared portable safe-ID grammar. Producer source identity fails closed without an unrestricted project walk. A present malformed durable `crew_epoch.json` can no longer downgrade to absent/legacy evidence or authorize producer integration with an empty epoch; its strict decoder binds final and ancestor node identity, size, encoding, duplicate-free schema, IDs, UTC timestamp, and project lineage. Portable lineage and durable crew-epoch bytes now come from a descriptor-bound bounded reader: pre-open `lstat`, descriptor `fstat` before/after, exact observed-size consumption, and post-read path/ancestor CAS close the lstat/open pivot and short concurrent snapshot instead of merely noticing a stable symlink. The validator makes that bounded shared reader its first acceptance path and only performs a second bounded read for refusal diagnostics. The audit harness proves symlink mutations restore owned bytes without following or modifying the external target, and a regular but oversized carrier proves the validator cannot bypass strict authority through its diagnostic path. Hostile authority: 71/71. |
| Quality | Binding SHIP validation uses the exact Git index tree while core/install validation still inspects the complete injector tree. Receipt CAS binds both immutable manifests and mutable progress sidecars. The historical `v7.223.15` VERSION mismatch is explicitly visible, explained, and rechecked instead of silently failing the tag gate. Release executor: 140/140 including no-publish, crash recovery, scoped deletions, untracked scope, fresh clone, tag ordering, and sealed Event Graph. Validator mutation audit: 271/271. |

The receipt stability tokenizer additionally frames invalid namespace entries,
manifest-less op directories, symlinks/reparse points, unreadable manifests,
and exact `progress.json` presence/bytes. Adding corrupt evidence or changing
mutable progress between initial capture and closing CAS can no longer hash
like absence; each present authority still requires one content read per pass.

## Final verification evidence

- Full executable scenario and injector matrix: PASS, 37 executable fixtures;
  all behavioral probes green after the final authority repair
  (`tools/run_scenarios.py`, fresh post-fix run).
- Validator mutation audit: PASS, 271/271 checks independently went red on
  their owned defect, including external-identity symlink refusal, bounded
  descriptor authority, and byte-exact special-node restoration
  (`tools/audit_checks.py`, fresh post-fix run).
- Declared/executable parity audit: PASS, 271/271 mutations applied; 13 caught
  by the portable floor, 255 additional hard failures caught by Python, and
  the remaining 3 are explicitly WARN-only (`tools/audit_parity.py`, fresh
  post-fix run).
- Portable floor: PASS, 27 checks in each of the sh and PowerShell halves.
- Unit regressions: PASS, 57 internal + 18 external-audit tests (75 total),
  including zero-write checks for both targeted and crew producer integration
  and a red control proving corrupt durable authority cannot fall back to a
  valid legacy receipt, plus a deterministic lstat/open path-pivot control.
- Release freshness: PASS 11/11; export shell parity: PASS 12/12; release
  executor: PASS 140/140; NITRO integrity: PASS 190/190; hostile authority:
  PASS 71/71; receipt authority: PASS 33/33; performance regressions: PASS
  41/41.
- Structural gates: validator conformant with 0 failures and 7 owned warnings;
  audit order 29/29; tag ledger 325 comparable tags, 8 documented historical
  mismatches, 0 new; Ruff, compileall, and `git diff --check` all PASS.
- Source rescan after verification found the same four files with the same
  sizes and SHA-256 values recorded above; no new audit document was pending.

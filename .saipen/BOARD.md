# Board
## DOING

## DONE
- [x] T-191 [P1] close subSaipen mechanism — PASS.
- [x] T-170 [P1] CANCELLED — subsumed by T-191 (2026-07-27, PASS).

## TODO
- [ ] T-192 [P2] 29 stale locale batch refresh — refresh remaining stale locales to v7.81.0. Validate.py badge drift check now catches them. Run mechanical version bump + content sync for all 29 | verify: `python tools/validate.py` shows 0 stale badges. Subsumes T-186.
- [ ] T-193 [P3] board scrub — Prune verbose DONE narratives to summary lines. Full detail lives in LOG.md + CHANGELOG.md | verify: BOARD.md under reasonable size, no structural info lost
- [ ] T-194 [P0] Self-critique output enforcement — before any response: strip emoji, strip apology words, compress to <=4 lines unless detail asked. Verify: grep output for emoji/apology — zero matches.
- [ ] T-195 [P0] Tag all missing releases — v7.65.0 through v7.82.0 untagged. `git tag v7.XY.0 HEAD~N && git push --tags` for each. Verify: `git describe --tags --abbrev=0` returns v7.82.0.
- [ ] T-196 [P1] No ask-execute drift — never ask "what next?" when work remains. TODO not empty → execute highest. Blocked → state exact reason. Check: write "next: [action]" not "what next?".
- [ ] T-197 [P1] Post-edit diff self-review — after every edit, `git diff --stat` to verify only intended files changed. Catches path mistakes. Enforce before commit.
## DONE
- [x] T-186 [P2] CANCELLED — subsumed by T-192. 29 non-Core locales merged into T-192 batch scope.
- [x] T-183 CLOSED 2026-07-26: the 34 fixtures in `tests/scenarios/` are executable now, or honestly labelled. (a) Completed the four that shipped only the one file their concept concerned -- `blocked-ticket` got its LOG, `dependency-cycle` and `multi-agent-claim-conflict` got STATE+LOG, `resume-after-crash` got BOARD+LOG matching its own `STATE.task` -- so each now fails only on the defect it exists to demonstrate, or passes cleanly. Nothing fabricated: every added file states what that fixture's README already claimed. (b) All 9 executable fixtures declare `expect: pass|fail`; the 25 behavioral ones correctly declare nothing, since there is nothing to run. (c) New `tools/run_scenarios.py` runs each and holds it to its declaration -- and refuses to be a no-op: a fixture with a `.saipen/` but no `expect:` is an error, a behavioral fixture that declares one is an error, and collecting zero fixtures exits non-zero. (d) Wired into `.github/workflows/validate.yml` and added to the runtime manifest so it cannot silently stop shipping. (e) Broken on purpose three ways -- flipped a declaration, removed an `expect:`, corrupted a fixture's real state -- red every time, green again after restoring. Also fixed a fixture that contradicted itself: `multi-agent-claim-conflict` describes an active (<15 min) claim while carrying a fixed past `claim_time`, which by RFC §1.4 reads as stale; unavoidable in a checked-in file, now stated plainly instead of implied.
- [x] T-184 CLOSED 2026-07-26: ran the full `inject.sh` -> `uninstall.sh` round-trip against a sandbox `HOME` (never the real one) -- the last untested path that writes into user config. Steps (a)/(b)/(c)/(e) passed: user content survives, `$1.bak` is byte-identical to the pristine original, the block-refresh path leaves it pristine (first end-to-end confirmation of the T-178 fix through the real script rather than an isolated sed repro), no temp residue, `.uninstalled.bak` written, skill dirs removed. Proved the test can fail before trusting it: temporarily restored the T-178 bug and the check went red, then restored the fix. Step (d) FAILED and found a real defect -- see T-185.
- [x] T-185 CLOSED 2026-07-26 (found by T-184's step (d)): `uninstall.sh` did not reverse `inject.sh` exactly. `$BLOCK` starts with a newline, so every install appended one blank line that the `BEGIN..END` range-delete left behind. Measured compounding: a 2-line `CLAUDE.md` grew to 7 lines over five install/uninstall cycles, one stray line per cycle, unbounded -- while README promises "leaving the rest of your file alone". Fixed with an awk pass that drops exactly ONE blank line immediately before `BEGIN` (the one we added) and preserves any the user had. `uninstall.ps1` never had this: its regex already consumed the surrounding whitespace -- same bash-lags-PowerShell asymmetry as T-178. Verified: byte-identical after five round-trips, and a file with the user's own trailing blank lines survives untouched.
- [x] T-182 CLOSED 2026-07-26 (HUNT over this session's own diff, c3933df..HEAD): v7.72.0's brand-new "a shipped template must not carry a concrete `saipen_home`" check keyed on `sp.parts[0] == "extensions"` alone -- which also matches a *consuming* project using RFC § 1.9's legacy root-level `extensions/subs/`, where a real `saipen_home` is not a defect but precisely what `saipen sub spawn` is required to write. Those projects got a hard FAIL and, through `tools/install_hook.py`'s pre-commit wiring, blocked commits. Third instance of this exact class this session (T-176 found two). Reproduced on a live legacy mock, then gated the check behind the same home fingerprint the distribution self-check uses (`saipen/` + `bootstrap/` + `VERSION` + `README.md`). Verified three ways: legacy project PASSes, the home still checks all 4 library states, re-pollution is still caught. Rest of the diff clean -- RFC insertion reads correctly, CONFORMANCE rows 39/40 well-formed, all README links resolve.
- [x] T-178 CLOSED 2026-07-26 (MARKHUNT triage, user-authorized): `bootstrap/inject.sh` re-run destroyed the user's pristine pre-SAIPEN backup -- `sed -i.bak` overwrote the `.bak` that `backup_file()` had correctly protected. Fixed with the `-i.saipen-strip-tmp` suffix + cleanup that `uninstall.sh` already used; T-177 had fixed three of the four sites and missed this one. Re-ran the original reproduction: pristine backup now survives, no temp left behind.
- [x] T-179 CLOSED 2026-07-26 (MARKHUNT triage): `extensions/subs/saipython/STATE.md` shipped with the author's absolute `saipen_home` and a live timestamp while both siblings carried the placeholder -- a public repo file the injector copies to every user. Reset to template state, and fixed the root cause: `tools/validate.py` only walked `.saipen/extensions/subs/`, never the shipped library it distributes, so nothing could catch it. The walk now covers both (4 states checked, was 1) and a concrete `saipen_home` in a shipped template is now an explicit FAIL. Regression-verified by re-polluting the file.
- [x] T-180 CLOSED 2026-07-26 (MARKHUNT triage): README told users to run the injector into their home config but never mentioned `bootstrap/uninstall.*` (it existed only in SPEC.md/SECURITY.md). Added an explicit undo block plus a plain statement of exactly which files the installer touches, and restored `saipen plan`/`saipen ship` to the trigger list -- both were absent from README entirely while GUIDE.md covered all twelve.
- [x] T-181 CLOSED 2026-07-26 (MARKHUNT triage): `BOARD.md` had no size discipline while `LOG.md` had a cap, sealing and a WARN -- despite BOOT reading both on every cold start. Resolved by picking the cheap half: no sealing machinery (the board is prunable, not append-only -- `phases/clean.md`'s scrub is already the mechanism), but a validator WARN past ~16 KB so growth surfaces instead of waiting to be noticed. RFC § 1.2 states the reasoning. This repo's own board tripped it immediately at 28 KB (16 KB of closed-ticket prose) and was scrubbed in the same pass.
## BLOCKED

# Board
## DOING

## TODO
- [ ] T-168 [P1] saitranslate content refresh, PARTIALLY DONE 2026-07-24 -- 10/32 languages now carry the current drift fixes (saicrew bullet + Codex/FreeBuff platform-list clause): ru, bg, ded, uk, de, fr, es, it, pt, nl. 22 remain untouched, and 5 of those (hi/ja/ko/th/zh) are missing the platform-list clause entirely, not just the saicrew bullet. History: an unsupervised parallel instance (agent: deepseek-v4-flash-free) claimed this ticket 2026-07-24, wrote to all 31 README_XX.md files, then went silent with zero LOG.md entries (stale claim, adopted per RFC §1.4 spirit, see LOG E-710) -- its run left 4 files (bg/ded/ru/uk) with genuine invalid-UTF-8 corruption in the platform-list clause, found via raw-byte validation and repaired (LOG E-711). Remaining 22 languages need the same two-item drift fix -- fabricate nothing (translate.md §2/§3). RUN AS A DEDICATED/PARALLEL saitranslate INSTANCE, not the main agent under limits -- user confirmed 2026-07-24: leave the remaining 22 to a small/cheap model, not this session, token cost not worth it for mechanical work | verify: 32+Дед current and spot-checked against real root docs, byte-valid UTF-8 confirmed for every file touched
- [ ] T-170 [P1] bare-name crew command discovery is unreliable, found live in FastPrompter crew test 2026-07-24, THREE compounding failures observed directly (FreeBuff/DeepSeek + OpenCode/DeepSeek, both weak free-tier models):
  (1) STALE PER-PROJECT COPY -- a project that spawned subs/ before v7.56.0 (crew) has a frozen PROTOCOL.md with no §7; `saipen sub spawn` never re-syncs an already-copied extension when home gains new vocab. Ad-hoc synced in FastPrompter only, not a general fix.
  (2) GLOBAL SKILL NEVER CARRIED extensions/subs/ AT ALL -- confirmed via FreeBuff's own local .freebuff/desktop.db (SQLite, readable: tables threads/messages/queue_items, real-time queryable): agent loaded skill "saipen", read the globally-injected RFC.md/STYLE.md/UI.md from ~/.agents/skills/saipen/, found ZERO mention of subSaipen roles anywhere, and reasoned "saipython" = a "saipen"+"python" portmanteau -- a plausible-sounding wrong guess, not a lookup failure. bootstrap/inject.sh + inject.ps1 copy_skill() never included extensions/subs/ in the distributed bundle. FIXED THIS SESSION: both injector scripts now copy extensions/subs/ (PROTOCOL.md/crew.md/README/TEMPLATE) into every platform's skill folder; tools/validate.py's dist_tokens check extended to catch a regression here.
  (3) RFC §1.10's "check extension vocab before declining" (OpenCode/DeepSeek trace) requires an inferential leap (unknown word -> go find + open the right file) a weak model skipped, declining outright instead of looking.
  | verify: (a) confirm re-running the injector actually fixes a cold FreeBuff/OpenCode session's discovery of "saipython"/"saihunt" (re-test after user approves an injector re-run), (b) design a `saipen sub sync`-style refresh path for already-spawned per-project copies (failure 1), (c) make extension-vocab discovery mechanical not inferential in RFC §1.10 wording (failure 3) -- test against weak/free-tier models specifically, not just strong ones
  | found: a real live saihunt run (OpenCode/DeepSeek-free, once bare-name WAS understood) surfaced two MORE real spec gaps, both fixed this session: (4) `TEMPLATE/BOARD.md` shipped empty with zero example, and PROTOCOL.md §1 only said "same shape as Core" abstractly -- saihunt invented its own ad-hoc shape, reusing OUTBOX's bold-field markdown for its BOARD.md instead of RFC §1.2's checkbox ticket line; fixed with an explicit example in both files. (5) spawn instructions (§7) told an agent to replace `agent:`/`saipen_home:` but never said to also set `updated:` to a real timestamp; §7 now explicitly lists `updated:` too. Corrected the one live instance directly.
  | found (round 2, 2026-07-24): a fresh OpenCode session hit "No .saipen/ at project root, not initialized here" for a real, existing FastPrompter -- root cause traced live: `.saipen/` is gitignored by design, and FreeBuff spawns each session into a *linked git worktree* (confirmed via `git worktree list`), which only receives tracked content -- `.saipen/` never arrives there even though the main worktree has one. Verified directly: `git rev-parse --git-common-dir` from inside the linked worktree returns a real path, `.saipen/` confirmed absent there, present at the main root. Likely the REAL explanation for the earlier FreeBuff "No read access" report too. FIXED: RFC §1.1 and `BOOT.md` now instruct checking `--git-common-dir` for a linked-worktree signal before concluding "not initialized."
  | fixed & DEPLOYED LIVE 2026-07-24: injector re-run authorized by user ("Даю добро на всё") -- `bootstrap/inject.ps1` executed against this machine, verified by diff against `~/.claude/skills/saipen/`, `~/.config/opencode/skills/saipen/`, `~/.agents/skills/saipen/` (FreeBuff's own read path). Failure (2) is now actually closed, not just source-fixed.
  | fixed (b)+(c) 2026-07-24: **`saipen sub sync`** now a real command (PROTOCOL.md §7) -- re-copies the 4 shared reference files from `saipen_home`, NEVER touches any `<name>/` subSaipen's own live STATE/BOARD/LOG/kitchen. **BOOT.md** now instructs checking `.saipen/extensions/*/PROTOCOL.md`/`README.md` for an unrecognized word before guessing or declining. Both deployed live, verified by diff.
  | blocker: LIVE VERIFICATION PAUSED BY USER 2026-07-24 -- FreeBuff itself failing to start sessions ("Could not start a Freebuff session for deepseek/deepseek-v4-flash"), not a SAIPEN-side bug; all source fixes above deployed live regardless. Resume live crew re-test once FreeBuff/OpenCode Zen are reliably reachable -- not moved to ## BLOCKED since nothing is stuck, just deliberately not being worked right now
  | found (round 3, 2026-07-24): a real live trace of another agent parsing bare "saiwiki init" on a brand-new project (user-supplied) surfaced two more real gaps, both fixed this session: (6) `saipen sub spawn`'s first-bootstrap file list never included `crew.md`, even though `saipen sub sync` already described it as one of "those four shared items" -- a fresh project's `saipen crew` command and README's Crew section pointed at a file that would not exist until someone separately ran `sync`. Fixed: spawn's list now matches sync's. (7) the bare-name role-adopt rule (§7) was implicitly scoped to the 3 shipped examples plus already-spawned folders -- generalized to any `sai*`-prefixed name (the naming convention every real subSaipen already follows), auto-spawning it fresh the same one word, without opening bare-word matching to arbitrary unrecognized text. Also documented `<subname> init`/`start` as identical to bare `<subname>` (same "init" reuse as `saipen set`/`saipen init`, RFC §1.7). validate.py PASS
  | acceptance: USER-SET CLOSURE BAR 2026-07-24 -- "наша задача пока ты лично не выводишь PASS всему subsaipen механизму". This ticket, and README/GUIDE's own "in development" framing for saicrew, stay exactly as-is until *this agent* personally runs and verifies the full mechanism end-to-end and can state PASS -- a partial saihunt success is not sufficient to close it
## DONE
- [x] T-177 CLOSED 2026-07-25 (sweep continued into the unreviewed executable surface and the contributor-facing scaffolding): injector re-run authorized by the user and executed -- `bootstrap/inject.ps1` hit 7 targets, verified by diff against the home: `CONFORMANCE.md`/`BOOT.md`/`RFC.md`/`extensions/subs` identical across all four platforms (`.claude`, `opencode`, `.codex`, `.agents`), so T-176's third finding is closed on the live machine and not just in source. Four more real defects: (1) `bootstrap/uninstall.sh`'s `strip_block` **destroyed the backup `inject.sh` had made** -- `sed -i.bak` overwrote `"$1.bak"` (which held the user's ORIGINAL pre-SAIPEN config) with the current SAIPEN-containing content, and the follow-up `rm -f` then deleted it, so uninstalling wiped the exact backup installing existed to create. The `.ps1` twin was already correct; shell-only bug. Fixed with a distinct `.saipen-strip-tmp` suffix, verified live (original survives, block still stripped). (2) `bootstrap/export.*` drops its archive in the project root by design but `.gitignore` didn't know the name -- added `saipen_export_*.tar.gz`/`.zip`. (3) `CONTRIBUTING.md` step 4 told contributors to run **only** `tests/validate.sh`/`.ps1` -- the frozen floor, which says of itself that new checks land only in `tools/validate.py` -- so the canonical validator (schema, E-### graph across sealed segments, runtime manifest, injector wiring, soft cap) went unmentioned in the one place a contributor is told what to run. Rewritten: canonical first, floor as the no-Python fallback. (4) **No CI existed at all** -- the pre-commit hook is opt-in, installed per machine, and bypassable with `--no-verify`, so nothing enforced conformance on a push or PR, in a repo whose own CHANGELOG records version-badge drift shipping three separate times. Added `.github/workflows/validate.yml` running the canonical validator plus the portable floor; both steps executed locally first (exit 0) and the YAML parse-checked
- [x] T-176 CLOSED 2026-07-25 (full-repo HUNT under user /goal, "quality over tokens" -- swept the executable surface first, where bugs are real rather than documentary): 6 real defects, **two of them regressions this session's own last two releases shipped**, and both broke *other people's* projects rather than this one. (1) `tools/validate.py`'s home-repo fingerprint, rewritten by me in v7.65.0, hard-FAILED any ordinary project that happens to keep a `saipen/` folder next to a `VERSION` file and a `README.md` -- with a message about a stray clone that never happened -- and since `tools/install_hook.py` wires the validator into pre-commit, that **blocked commits** in the victim project. Reproduced live, fixed by adding `bootstrap/` as the discriminator (home-only, and untouched by the very corruption the check exists to catch), then verified three ways: consuming project passes, real corruption still caught, home still 13 PASS. (2) v7.66.0 made `no-publish` + `SHIP` legal, but **all three validators** still banned it (`validate.py:172`, `validate.sh:31`, `validate.ps1:34`) -- a git-less project sitting in a now-legal state failed conformance and, again via the hook, couldn't commit. Fixed all three plus `tests/scenarios/no-git-ship-denial/README.md` (which asserted the literal opposite) and CONFORMANCE row 8; verified `no-publish`+SHIP now passes both validators while `read-only`+SHIP still fails. (3) `CONFORMANCE.md` was never distributed to skill copies, though `BOOT.md` (loaded on every cold start) and `phases/validate.md` both cite it -- a dangling pointer on every injected platform, exactly the v7.22.3/v7.25.0 class the dist check exists to catch, which the check itself didn't cover. Added to both injectors and the manifest, and extended `dist_tokens` to per-file granularity; regression-verified by removing it and confirming the FAIL. (4) `SPEC.md` listed 14 of 16 phases (no `markhunt.md`, no `prepare.md`) and knew nothing of `BOOT.md` or the crew launchers -- notably LOG E-675 once claimed SPEC.md was clean, so that earlier check was superficial. (5) `extensions/templates/BOARD.md` (Core) shipped with no shape example, while the subs template got one in v7.58.0 after a real incident (saihunt invented its own shape) -- the fix had been applied asymmetrically. (6) Found while fixing (5), by testing rather than assuming: **neither validator skips HTML comments**, and `validate.sh` scans for the dependency field across the whole file rather than only ticket lines -- so a commented-out example ticket in a template breaks a brand-new board, and the two validators disagree about it. Both templates de-fanged (examples carry no leading dash, no field-name-next-to-id), trap documented inline so it isn't reintroduced. All four combinations (both templates x both validators) verified clean. NOTE: deployed skill copies are now stale (missing `CONFORMANCE.md`) until the injector is re-run -- that touches the user's home config, so it needs their say-so
- [x] T-175 CLOSED 2026-07-25 (third thoughts/ batch, 4 files, live-verified): 6 claims rejected with evidence (goal_mode+mature "infinite loop" -- `add.md`:52 already sets `goal_mode: false` before DONE; ship.md tag recreate -- already fixed in v7.68.0; PLAN proposal-mode dead end -- plan.md already routes bare `saipen` to the first TODO; concurrent-detection ignoring claim_time -- §1.4 already says liveness is read off BOARD alone; HUNT delete-free -- already capped at 5 + never-user-data, §1.1 covers only *mass* deletion; next_action grammar / goal_tickets naming -- both already spelled out, and the consumer is always an LLM by design). 19 real gaps found and fixed. Big ones: VERIFY hysteresis sent the whole session to BLOCKED while `blocked.md`'s own entry condition forbids that when other tickets are workable; MARKHUNT's closure self-test demanded `findings:` == ticket count while the same doc mandates grouping (test could never pass when grouping was followed); my own v7.68.0 BOOT clone-fallback assumed git, which `no-publish` by definition lacks; my own v7.66.0 sealing crash-safety hand-waved "worst case a re-seal" -- a re-seal duplicates E-### across two segments, now guarded by an idempotency check; the `read-only` paradox (mode must be written to STATE, but read-only means writes are unavailable) resolved honestly; secrets were never banned from `.saipen/recovery/` and `.saipen/logs/` -- precisely where Recovery copies STATE *verbatim* and sealing moves lines *verbatim*; a session at `phase: BLOCKED` could escape into autonomous HUNT because a board of only BLOCKED tickets reads as "no open TODO"; `T-###` high-water (clean.md prunes DONE, so "max on board + 1" silently reuses a pruned ID -- now derived from LOG including sealed segments); plain LOG append had no crash-safety rule (added: a truncated *last* line is debris, earlier malformed lines stay history); Recovery broke when no `## DOING` existed (crash between LOG and BOARD writes) or when the last event was session-level. Smaller: BUILD risky-edit no longer reads as a substitute for §1.1's gate; ADD's `PLAN_or_SCOUT` got a real criterion; §1.6 states `-> BLOCKED` is universal (a phase doc's silence isn't a prohibition); from-any-phase commands must checkpoint an active DOING; safety-valve got a legal `WAIT:` category; goal-counter bumps must leave an identifiable DEC line (Recovery is required to count them); missing `python` mapped to real degradation; explicit `saipen ship` doesn't bypass VERIFY/REVIEW; `init.md` got the linked-worktree guard (would otherwise create a second disconnected `.saipen/`); BOOT's "execute immediately" explicitly doesn't override the destructive gate. `tools/validate.py` gained a mechanical soft-cap WARN for the active LOG (the segmentation rule previously had no signal at all) -- verified live by inflating LOG.md to 363 lines, confirming the WARN, then restoring. validate.py PASS
- [x] T-174 CLOSED 2026-07-24 (second user-supplied thoughts/ batch, 10 files, live-verified not trusted): most claims repeated already-fixed/already-rejected ground from a stale snapshot (add.md, README/GUIDE version, init.md, ship.md no-publish, hunt.md delete-free, CONFORMANCE row 38, T-136 -- all re-verified still correct) or re-proposed the ratified-rejected `goal_exit` command (decisions.md, 3 prior rejections). 13 real gaps survived: (1) `validate.md` repaired structure even under `mode: read-only`, a direct RFC §1.3 violation -- added the report-only guard the phase doc was missing. (2) T-171's boundary check only compared `.saipen/` metadata files, not the whole working tree -- a confused sub could corrupt real source code past it undetected; widened to the full tree and connected to a formal `BLOCKED`/`WAIT:` instead of just "mention it in chat." (3) `markhunt.md`'s closure self-test hard-required a git hash, permanently unsatisfiable on a no-git project -- added an explicit `no-git` literal that satisfies the check by construction. (4) `BOOT.md` had no fallback when `saipen_home` is empty/dead -- the one step that could dead-end a cold agent with literally nowhere else to look; added the clone-and-update path directly in BOOT instead of only in lazily-loaded RFC §1.7. (5) `verify.md`'s clean-tree step used `git restore`, which never touches newly-created untracked files from a failed attempt -- added scoped cleanup (never a blanket `git clean`, which would eat unrelated scratch RFC §1.5 already protects). (6) `clean.md`'s "stale TODO" criterion was undefined -- now evidence-based (superseded, or cited files/behavior no longer apply), explicitly never a clock since tickets carry no creation timestamp. (7) RFC §1.5's dirty-tree attribution required git with no fallback -- now reuses Recovery's own no-git mtime heuristic. (8) A parallel `TRANSLATE` instance had no cold-start path back to its own `.saipen/saitranslate/STATE.md` -- BOOT's generic path would hand it the *shared* STATE.md's phase instead; fixed as the mirror image of T-173. Plus 5 cheap ones: ship.md's tag delete-recreate scoped explicitly to a never-yet-pushed local tag (and first-publish trigger widened to "origin exists but empty"); `sk-***` redaction marked illustrative, not exhaustive; `human_note` clearing now requires a LOG line; extension-vs-extension bare-command collision gets an explicit decline-and-ask; `verify:` field execution now gated against obviously destructive patterns, same footing as RFC §1.1. Also corrected BOOT.md's stale "~30-line" self-description (now well past that, honestly) to "compact" across BOOT/SKILL/CONFORMANCE/scenario-README. validate.py PASS
- [x] T-173 CLOSED 2026-07-24 (user-requested fresh Core self-review, not an audit-on-phases): read every phase doc not yet covered this session (scout/build/hunt/init/plan/prepare/translate/clean.md), RFC §1.10 + Part 2 in full, KNOWLEDGE/decisions.md, extensions/security+performance. One real, personally-witnessed gap found: TRANSLATE carries the same class of problem subSaipen carried before T-171 -- "a parallel instance MUST NOT write the shared phase: TRANSLATE" is stated twice (RFC §2.1, `phases/translate.md` §1) but neither said what to do if it's already been violated. It was: the same deepseek-v4-flash-free run this session (E-710) did exactly this. Fixed by generalizing RFC §1.4's stale-claim recovery to whole-STATE phase claims, not just BOARD tickets -- LOG a DEC naming the old agent, inspect what it actually produced, before rebuilding STATE.md. Everything else read (scout/build/hunt/init/plan/prepare/clean.md, decisions.md, extensions/) was internally consistent, nothing else ticketed. validate.py PASS
- [x] T-172 CLOSED 2026-07-24 (user-supplied thoughts/ review, live-verified not trusted): 20 claims across two outside-audit files checked against real files one by one. 16 stale/overstated -- 10 already fixed by past sessions (STYLE haiku line, MARKHUNT closure self-test, ship.md digest, blocked.md/validate.md DONE path, add.md §2.2 sync, GUIDE.md saitranslate path, "14 vs 16 phase split-brain" -- RFC §1.6 and validate.py both confirm 16 live), 3 already covered by existing text once read in full (BOOT/next_action/read-only), plus one accurate-but-already-tracked observation (T-170 status). 4 were real and unfixed: (1) `mode: no-publish` had no path to `DONE` -- `review.md` makes SHIP mandatory with zero exception, `ship.md` blocked the whole phase for no-publish, so a git-less project could never close a ticket; fixed by degrading only the git-dependent steps (RFC §1.3, `ship.md` no-publish branch: local steps run, tag/push/first-publish skipped, STATE -> DONE directly, same treatment as `read-only`/`manual-verify`). (2) CONFORMANCE.md had zero mention of LOG segmentation despite `tools/validate.py` enforcing it live every run -- added row 38. (3) LOG sealing had no specified crash-safe write order -- RFC §1.2 now requires the same temp+rename discipline as STATE/BOARD. (4) Recovery's goal-counter rebuild didn't say to walk back into sealed LOG segments when the pivot line got sealed away -- RFC §1.5 now says so explicitly. validate.py PASS throughout
- [x] T-136 CLOSED (was P3 design-debt, 8 versions): MARKHUNT got a manifest-driven closure self-test in markhunt.md (vectors 5/5, cursor:done, head_end==HEAD, findings==[MARKHUNT] tickets) before DONE + enriched auditable completion line; CONFORMANCE row 37 + stub. No validate.py change (self-enforced, no ceremony)
- [x] T-167 saitranslate carve-out fix: removed 28 flat GUIDE_XX (duplicated hand-maintained guides/)
- [x] T-171 CLOSED 2026-07-24 (user-authorized via /goal): real subSaipen boundary violation found live on FastPrompter -- saiwiki wave 5 wrote fabricated-looking tickets and draft files directly into the *main project's own* `BOARD.md`/`kitchen/`/`LOG.md`, bypassing `kitchen/OUTBOX.md` entirely while its own OUTBOX sat stale at wave 4. Fixed at the source: `extensions/subs/PROTOCOL.md` §4 collect procedure gained a mandatory boundary-check step 0 (verify the main project's own BOARD/kitchen/LOG/STATE show no changes the collecting agent didn't make itself, before trusting any OUTBOX); `TEMPLATE/STATE.md` and `TEMPLATE/BOARD.md` both gained explicit inline warnings at the exact point of temptation, so a future spawn sees the rule before it can be tempted to break it. Enforcement stays procedural (§1 already says so plainly) -- this makes the check mechanical to run, not something `collect` has to remember unprompted. FastPrompter's own damage repaired as an ad-hoc favor (not this Core): verified the 7 WIKI-* tickets had become genuinely accurate, fixed one shape-corrupted LOG.md line (content preserved), removed 8 redundant scratch files after confirming each had a real merged counterpart in docs/wiki/. validate.py PASS
- [x] T-169 CLOSED 2026-07-24 (user-authorized via /goal): saitranslate structural reconciliation, mechanical + verified, zero fabrication. Backed up saitranslate/kitchen/ (3.2MB, mostly untracked -- irreversible without it) to `.saipen/recovery/saitranslate_kitchen_backup_20260724_1230` first. Steps executed: (1) deleted README_en.md, confirmed byte-comparable stale mirror of real root README.md, English needs no translation; (2) renamed `ee/` -> `et/` + all 7 files' `_EE`->`_ET` suffix (correct ISO language code for Estonian; verified both were genuinely Estonian content, subdir just mis-coded), then overwrote the migrated README with flat's fresher v7.55.0 content (subdir's own was stale v7.41.0); (3) migrated 28 more overlapping languages from flat into subdir form the same way (flat verified fresher across every sampled language: v7.55.0 vs subdir's v7.41-42.0); (4) deleted 64 out-of-scope files (RFC_XX.md/STYLE_XX.md per language -- neither lives at repo root, translate.md's scope excludes them); (5) verified independently after the fact (not trusting the script's own report): 0 loose root files, all 32 subdirs match the exact 5-file shape (CODE_OF_CONDUCT/CONTRIBUTING/README/SECURITY/SPEC, uppercase-suffixed), 0 RFC_/STYLE_ files remain anywhere, migrated et/README_ET.md carries the correct v7.55.0 content. Content itself is now one version behind (v7.55 vs current v7.63) -- that refresh is T-168's job, a dedicated/parallel run, not bundled here.
## BLOCKED
- [ ] T-178 [MARKHUNT] [P1] [findings: 1] `bootstrap/inject.sh` silently destroys the user's pristine pre-SAIPEN backup on any re-run that refreshes the block. `add_block()` calls `backup_file "$1"` (which correctly refuses to overwrite an existing `$1.bak`) and then immediately runs `sed -i.bak '/SAIPEN:BEGIN/,/SAIPEN:END/d' "$1"` -- sed's own `-i.bak` overwrites `$1.bak` with the CURRENT, already-SAIPEN-containing file, so the genuine original is gone and unrecoverable. Reproduced live: seeded a pristine `CLAUDE.md` + `.bak`, appended a v1 block, ran exactly line 38's sed, and the `.bak` came back containing `<!-- SAIPEN:BEGIN -->`. `bootstrap/uninstall.sh:6-10` documents this exact hazard in a comment and avoids it (uses `-i.saipen-strip-tmp`), and `bootstrap/inject.ps1`'s `Write-NoBom` is also safe (guarded by `-not (Test-Path "$file.bak")`) -- so T-177 fixed three of the four places and left the installer's bash path. Fix direction: give inject.sh's sed a non-`.bak` suffix and delete that temp, same shape as uninstall.sh already uses | blocker: unvetted audit -- bootstrap/inject.sh:38 vs bootstrap/uninstall.sh:6-10, reproduced 2026-07-26
- [ ] T-179 [MARKHUNT] [P1] [findings: 2] A shipped library template carries one machine's absolute path, and nothing validates that class of file. (a) `extensions/subs/saipython/STATE.md` has `saipen_home` pointing at this author's local clone plus a real `updated: 2026-07-23T21:42:19Z`, while both siblings (`saihunt`, `saiwiki`) correctly carry no `saipen_home` and the `2026-01-01T00:00:00Z` placeholder -- proof it was checkpointed live once and shipped unreset. This is a public repo and the injector copies `extensions/subs/` into every platform's skill folder, so it both publishes the author's directory layout and hands every user a dead `saipen_home` pointer. (b) Root cause: `tools/validate.py` only walks `.saipen/extensions/subs/*/STATE.md` (this project's live instances) and never `extensions/subs/*/STATE.md` (the shipped library that actually gets distributed and copied), so the pollution had no way to be caught. Fix direction: reset the file to template state AND extend the validator's subs walk to cover the library copies | blocker: unvetted audit -- extensions/subs/saipython/STATE.md vs saihunt/saiwiki siblings; tools/validate.py subs_root only resolves .saipen/extensions/subs or legacy root
- [ ] T-180 [MARKHUNT] [P3] [findings: 2] Front-door documentation gaps, both Reddit-facing. (a) `README.md` Quick Start tells the user to run `bootstrap/inject.*`, which writes into `~/.claude/CLAUDE.md`, `~/.config/opencode/AGENTS.md`, `~/.codex/AGENTS.md`, `~/.gemini/GEMINI.md` and several skill folders -- but README never mentions that `bootstrap/uninstall.*` exists. It appears only in `SPEC.md` (architecture reference) and `SECURITY.md`. GUIDE.md documents undoing the optional pre-commit hook but not the global injection. A tool that edits home config with no visible undo is the obvious first complaint from a public audience. (b) README's "Explicit Triggers" bullet lists 8 of the 12 RFC § 1.10 commands; `saipen plan` and `saipen ship` appear nowhere in README at all (grep: 0 hits each), while GUIDE.md covers all 12 | blocker: unvetted audit -- README.md Quick Start (install, no undo) vs SPEC.md + SECURITY.md; README.md trigger-list bullet; grep 'saipen plan'/'saipen ship' README.md = 0
- [ ] T-181 [MARKHUNT] [P2] [findings: 1] `BOARD.md` has no size discipline while `LOG.md` has three, even though `BOOT.md` steps 2 and 3 read both on every cold start. RFC § 1.2 gave `LOG.md` a ~300-line/~64 KB soft cap, sealing into `.saipen/logs/`, and (v7.69.0) a validator WARN, all justified by "slow to read on weak hardware and can overflow a context window" -- an argument that applies verbatim to a board read at the same moment, yet `BOARD.md` got none of it. Live evidence from this very repo, which is also the familiarity-blindness case: `.saipen/BOARD.md` is 23,634 bytes, its `## DONE` section holds 10 closed tickets whose entries run 1,035-2,999 characters each, and every one of those is already duplicated verbatim into `LOG.md` and `CHANGELOG.md`. `phases/clean.md` step 1 already says to prune `[x]` DONE entries older than current active work for exactly this reason, and it hasn't run since (1 `clean -> done` line in the active LOG). So: decide whether the rule is a real cap (then state it in RFC § 1.2 next to LOG's, and WARN on it) or purely CLEAN's job (then say that explicitly), and scrub this repo's own board either way | blocker: unvetted audit -- wc -c .saipen/BOARD.md = 23634; DONE-entry lengths 1035..2999; saipen/BOOT.md reads BOARD every cold start; RFC § 1.2 LOG segmentation has cap+seal+WARN, BOARD has none

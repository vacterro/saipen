# Board
## DOING
## TODO
- [ ] T-572 [P0] Sweep the three runtime files that reached CI for the first time in v7.215.0 for further platform-conditional defects. `tools/freshness.py`, `tools/sub_clean.py` and `tools/hardening_controls.json` were required by the runtime MANIFEST while untracked, so every CI run before v7.215.0 executed a tree that did not contain them -- the SHIP/MANIFEST staging paradox (T-569) was also a CI blind spot, and closing it exposed the first defect immediately: `run_role_freshness_probes` asserted that a tracked mode change moves the source fingerprint by running `git update-index --chmod=+x`, which touches the INDEX, while the delta model reads HEAD vs the WORKING TREE. With `core.fileMode` false the probe read git's index fallback and passed; with it true (Linux, CI) git read the filesystem, saw no change, and the probe failed. It was measuring git's fallback, not the fingerprint. Assume that is not the only one. | verify: every path in the three files that branches on platform, filesystem capability or `core.fileMode` is either exercised by a probe that SKIPs out loud where the capability is absent, or documented as platform-independent with the reason; CI green on Linux and the suite green on Windows, with the SKIP counts named on both so a vacuous pass cannot hide in either; no probe asserts a behavior the host cannot express
- [ ] T-464 [P2] Own the `board-soft-cap` warning and prune the board, on CURRENT evidence. The ticket's original text said `## DONE` was not the cause and that no work on the ticket could clear the condition; both statements are now false, so the PERMANENT WARNING OWNER framing goes with them -- an owner whose own premise expired is a ticket that teaches the next reader to leave the warning alone forever. Measured at v7.215.0: BOARD.md is 84 KB against a ~16 KB soft cap, of which the validator attributes 62 KB to closed-ticket text. CLEAN owns this: archive or prune closed `## DONE` ticket bodies whose evidence already lives in LOG.md and CHANGELOG.md, keeping only the compact retention window Core requires; move the gated Crew design backlog (T-442..T-451) to KNOWLEDGE with ONE gate ticket left on the active board; remeasure. | verify: BOARD.md is below the cold-start soft cap, or this ticket records the exact remaining bytes and why each is unavoidable; every pruned DONE body resolves to its LOG or CHANGELOG evidence; the Crew backlog is reachable from the one gate ticket left on the board; the warning either stops emitting or has a truthful owner naming the measured remainder
- [ ] T-571 [P0] Resolve the Crew name collision BEFORE T-443 unblocks. Today `saipen crew` means a strictly SEQUENTIAL three-window circuit; T-442..T-451 call the future concurrent design Crew Mode. One name for "runs nothing in parallel" and for "multi-agent concurrency" is precisely the ambiguity a weak but compliant model resolves wrongly while believing it followed SAIPEN. Decide one of exactly two: (A) `saipen crew` stays sequential forever and the concurrent mode receives its own explicit command name; or (B) v8 deliberately changes `saipen crew`'s semantics as a breaking major-version migration, with the exact compatibility behaviour written down -- what a v7 invocation does on a v8 install, and what the v8 command does when concurrency is unavailable. Record the decision and its reasoning; implement no concurrency under this ticket. | verify: exactly one meaning per command name across CORE.md 1.10, the Crew backlog and every callout; the decision names which option was taken and why; if B, the compatibility behaviour is stated for both directions; validate.py FAILs a command surface where one name carries both meanings; T-443 cites this decision
- [ ] T-551 [P0] Fix report ownership AND per-seat isolation in the `saipen improve` specification before implementing anything on top of it. The spec writes each seat's report to `<saipen_home>/saipen_improve_<PROJECTNAME>.md`, but `saipen_home` is the SHARED protocol install (CORE.md 1.1, resolved in BOOT.md step 2), not project memory: two projects audited from one install collide on one path, the report never enters the project's own `.saipen/` history, and `saipen improve clean` would archive files inside the protocol tree. Canonical path, Core: `<project_root>/.saipen/improve/<cycle_id>/<seat_id>/saipen_improve_<PROJECTNAME>.md`. SubSaipen: `<sub_home>/improve/<cycle_id>/saipen_improve_<PROJECTNAME>.md`. The requested basename is preserved exactly; what changes is everything above it. `cycle_id` is ONE improve invocation or campaign; `seat_id` is ONE concrete auditing seat or session and NOT merely a model family -- `opencode-01`, `claude-01`, `codex-01`, `saiui`, `saihunt`. Two sessions both running `agent: opencode` MUST NOT resolve to one report unless they are deliberately the same registered seat, which is the collision the model-family reading guarantees. A second run by one seat APPENDS an immutable RUN section; an earlier RUN is never overwritten. Nothing is ever written under `saipen_home`. | needs: T-549 | verify: the report path is defined once, derives from `project_root`/`sub_home`, and no shipped doc names `saipen_home` as a report location; validate.py FAILs two DISTINCT seats resolving to one report path, FAILs a report under `saipen_home`, and FAILs a second run that overwrites rather than appends a RUN section; two sessions sharing a model family but not a seat identity resolve to different reports
- [ ] T-552 [P0] DECIDE, with evidence, whether `saipen improve` is a PHASE at all -- and prefer that it is not. The original ticket assumed a new IMPROVE phase in CORE.md 1.6's enum plus a transition table row. Improve is a meta-control operation like `status` and `stop`, not ordinary project execution, and four properties say so: several seats may audit independently and concurrently; reports are read-only evidence, never canonical state; invoking improve during BUILD or VERIFY must not orphan the DOING ticket; and one global `STATE.phase: IMPROVE` cannot represent several independent audit seats at once. Adding global improve routing fields to STATE turns retrospective evidence collection into project execution state, which is the category error. Preferred design: `saipen improve` checkpoints current work if necessary, does NOT change phase/task/next_action, reloads the current protocol, audits this seat's observable context and writes only this seat's report; `saipen improve status` is read-only and shows each registered report as missing / draft / complete / swept; `saipen improve sweep` is Core-only, reads reports in deterministic order, verifies findings, deduplicates root causes, creates normal T-### TODO tickets and never silently pre-empts an existing DOING ticket; `saipen improve verify` is a bounded delta audit with no recursion; `saipen improve clean` archives report evidence and never enters the CLEAN phase. Normal Core phases are unchanged. A real IMPROVE phase may still be proposed, but only by first proving a failure the meta-control design cannot solve -- a command having a name is not a reason to add phase and state machinery. | needs: T-550, T-551 | verify: the decision is recorded with the failure each property was derived from; if meta-control wins, no phase enum row is added and the phase count stays 16; if a phase is still chosen, the ticket names the exact failure the meta-control design could not solve and validate.py proves the new phase breaks neither INDEX/phase parity nor VALID_TRANSITIONS
- [ ] T-570 [P1] Give an improve cycle a real seat roster, because "every agent reports" cannot otherwise be closed mechanically. `STATE.agent` stores only the LATEST actor, so historical participants cannot be inferred from it; LOG's `[agent:]` is a MAY field, so they cannot be inferred from there either. At cycle creation Core records the seats EXPECTED FOR THIS CYCLE in `.saipen/improve/<cycle_id>/MANIFEST.md`, which owns routing and nothing else: `seat_id`, `role`, `report_path`, `status` from the closed set expected / draft / complete / unavailable / swept. Core is the SOLE manifest writer and seats never edit each other's report. A historical agent whose private or live context cannot be reached is `unavailable`, never silently `complete`; Core MAY audit that agent's observable artifacts itself, and MUST label the scope as observable output only. | needs: T-551 | verify: `reports_expected`/`reports_complete` are derived from this manifest and no independent copy exists; validate.py FAILs a seat marked complete with no report at its declared `report_path`, FAILs an unreachable seat marked complete, FAILs a manifest written by a non-Core seat, and FAILs a status outside the closed set; a Core audit of observable artifacts is labelled as such
- [ ] T-553 [P1] Keep improve routing OUT of `STATE.md` unless routing genuinely requires it -- reversing this ticket's original direction, which was to add flat `improve_cycle`/`improve_status`/`improve_reports_expected`/`improve_reports_complete` fields conditioned like the goal counters. Under T-552's meta-control design there is no global improve phase to route, and independent counters are exactly the drift risk: `reports_expected` and `reports_complete` MUST be DERIVED from the cycle manifest (T-570), never maintained as separate numbers that can disagree with the files they describe. If a field survives that test it lands flat with a closed enum, REQUIRED only while an improve cycle is live and cleared at completion, exactly the way `goal_waves`/`goal_tickets` are conditioned on `execution_intent: goal`. No finding is ever encoded into STATE. | needs: T-552, T-570 | verify: each proposed field is justified by a routing decision that cannot be answered by reading the manifest; the counters are derived, and a scenario proves a manifest edit changes them with no STATE write; validate.py FAILs findings text in STATE; 1.2's required-set sentence is extended in at most one place
- [ ] T-554 [P1] Register the command family in CORE.md §1.10's closed list: `saipen improve`, `saipen improve status`, `saipen improve sweep`, `saipen improve verify`, `saipen improve clean` -- five exact routes, multi-word like the existing `saipen sub <verb>` precedent. No repeated-letter shortcut is assigned: a new key is a ~74-file callout rollout (T-493) and nothing in the request asks for one, so the key count in every locale README/guide/mirror MUST stay unchanged. `saipen improve clean` archives reports only and never enters the CLEAN phase -- the name collision is the trap and the row states the distinction. | needs: T-552 | verify: all five verbs resolve in §1.10 and in validate.py's command surface check; an undeclared `saipen improve <word>` is rejected rather than guessed; the shortcut key count is byte-unchanged across every callout file; `improve clean` cannot transition `phase` to `CLEAN`
- [ ] T-555 [P1] Make a finding mechanically checkable rather than prose: the required header block (agent, role, model_or_runtime, project, saipen_home, source_head, source_tree_fingerprint, saipen_version, protocol_fingerprint, context_scope, context_available, report_status), the `IMP-###` finding schema with its closed `severity`/`class`/`confidence`/`action` vocabularies, and the completion bar for `report_status: complete`. A finding with no observable expected/actual/evidence triple is rejected, not softened. `context_available: complete` is refused when the stated scope is partial. | needs: T-551 | verify: validate.py FAILs a report whose finding lacks expected/actual/evidence (red control 6), FAILs a class/severity outside the closed set, FAILs `report_status: complete` while the completion bar is unmet, and FAILs a full-context claim over a partial `context_scope` (red control 3); `NO_FINDINGS` with a stated scope passes
- [ ] T-556 [P1] Define the Core sweep as the only path from report to canonical work: deterministic report order, numeric IMP order inside a report, the closed disposition set (CONFIRMED \| DUPLICATE \| ALREADY_FIXED \| SUPERSEDED \| LATER_RULE \| NOT_REPRODUCED \| INVALID \| NEEDS_EXTERNAL_EVIDENCE), root-cause deduplication producing ONE ticket with all `source_reports:` references, and the required ticket fields (source_reports, reproduced, invariant, defect, impact, exact_fix_scope, red_control, done_condition). A report's own `confidence: proven` is evidence to inspect, never a ticket authorization. Findings are appended with a disposition, never rewritten. | needs: T-555 | verify: validate.py FAILs a canonical ticket generated from an unverified finding (red control 10), FAILs a ticket with no resolvable `IMP-###` back-reference (red control 20), FAILs an INVALID or ALREADY_FIXED finding that produced a ticket (12, 13), and FAILs an edited original finding; three reports sharing one root cause produce one ticket in the scenario suite (red control 11)
- [ ] T-557 [P1] Enforce the writer boundary and the recursion stop the spec depends on but does not mechanize: during SELF-AUDIT/REPORT no seat may touch Core protocol, main source, canonical BOARD/LOG/STATE except the bookkeeping that registers the audit; a SubSaipen writes only inside its own home; `saipen improve verify` audits ONLY the delta produced by this cycle (fixes, new rules, new tests, final state) and MUST NOT re-enter a full improve cycle; `saipen improve` never silently enters ADD. | needs: T-552, T-556 | verify: red controls 7, 17, 18, 21 and 22 are mechanical -- an auditing sub that writes a main-project file FAILs, a verify pass that reopens unrelated history FAILs, a verify that starts a full cycle FAILs, an improve run whose `next_action` names ADD FAILs, and a report treated as canonical BOARD state FAILs
- [ ] T-558 [P1] Land the two reasoning gates as checked artifacts rather than advice: the META-IMPROVEMENT rule (a protocol-level fix MUST record cross-project recurrence reasoning; a local bug stays local) and the WEAK-MODEL PRECEDENT test (every protocol-level fix MUST record the answer to "could a weak but compliant model still choose wrong while honestly believing it followed SAIPEN?", strengthened in the fixed preference order: state field, transition rule, validator, red scenario, canonical example, prose last). ACCIDENTAL_SUCCESS is a first-class class: a correct result reached without the required verification is never recorded as PASS. | needs: T-556 | verify: validate.py FAILs a protocol-level improve ticket with no recurrence reasoning (red control 15) and none with no weak-model answer (16); a finding whose result was correct but whose verification never ran is classified ACCIDENTAL_SUCCESS, not PASS (red control 5); a fix answered only with prose where a state field or validator was available is flagged
- [ ] T-559 [P1] Define `saipen improve clean` as archive-with-provenance and nothing else: it refuses while any finding is unswept or any disposition is missing, it preserves the original findings verbatim, and every archived report keeps the canonical ticket references that point back at it. Partial or timed-out test evidence can never mark an IMP fixed. | needs: T-556, T-557 | verify: red controls 23, 24 and 25 are mechanical -- clean with an unswept finding is refused naming the finding, an archived report still resolves its ticket provenance, and a timeout/partial run recorded as fix evidence FAILs
- [ ] T-560 [P1] Wire mechanical coverage for all 25 spec red controls into `tools/run_scenarios.py` probes and/or `tools/audit_checks.py` CASES and/or `tools/validate.py` checks, covering at minimum: reload-before-audit (1), previous-conclusions-not-trusted (2), partial-scope-cannot-claim-full (3), later-rule-not-violation (4), accidental-success (5), vague-finding-rejected (6), sub-cannot-modify-main (7), one-report-owner (8), append-preserved (9), no-ticket-before-verification (10), duplicates-one-ticket (11), invalid-no-ticket (12), already-fixed-no-ticket (13), confidence-never-overrides-Core (14), cross-project-recurrence-required (15), weak-model-test-required (16), verify-is-delta-only (17), verify-cannot-recurse (18), original-finding-preserved (19), ticket-links-to-IMP (20), improve-does-not-enter-ADD (21), report-is-not-BOARD-state (22), cleanup-refuses-unswept (23), archive-preserves-provenance (24), partial-evidence-cannot-close (25). | needs: T-553, T-554, T-555, T-556, T-557, T-558, T-559 | verify: every one of the 25 has a named check or probe that demonstrably goes red when its rule is removed; run_scenarios green, audit_checks full-suite green with zero "not evidence" lines
- [ ] T-561 [P1] Ownership consolidation + the full closure gate for this wave. One owner per concept: the command rows in CORE.md §1.10, the audit chain in phases/improve.md, the convergence lifecycle in CONVERGE.md (referenced, never restated), the report/finding schema in one place, SubSaipen boundaries in PROTOCOL.md. Then run the full applicable suite and report real PASS with fresh evidence -- no skipped, timed-out, inspection-only or reused result: validate.py, audit checks, floor parity, scenario suite, phase/command parity, shortcut parity, state schema, forced HUNT, `git diff --check`, `git status --short`. | needs: T-560 | verify: no concept is defined in two documents and the drift check proves it; every gate reports PASS with evidence produced this wave; `git diff --check` clean; BOARD/STATE/LOG/CHANGELOG truthfully describe the new command
- [ ] T-473 [P1] HELD, not scheduled: Rosary A3, the concurrent whole-file clobber guard. Witnessed twice this session -- a parallel session overwrote `BOARD.md` and reverted six `## DONE` moves whose work was already committed and pushed (E-1863), caught an hour later by CONFORMANCE 199 rather than at the moment of damage. § 1.5 already reads each file before writing it, so the guard is cheap: compare the content against what was read immediately before the write and refuse on a difference. It is held deliberately because it touches the checkpoint path every session runs and because T-442..T-451 already own the v8 Crew concurrency design; landing a collision detector ahead of that design risks pre-empting it. | needs: T-443 | verify: a read-then-foreign-write-then-write sequence is refused; an unchanged file writes normally; the guard stays a detector and never becomes a scope or locking scheme
## DONE
- [x] T-562 [P0] Make runtime distribution consume one canonical inventory. Both injectors must copy every required MANIFEST entry into the flattened skill layout, autoinject must digest that same surface, and Aider uninstall must reverse the current BOOT+STYLE managed block instead of the retired RFC form. The installed copy currently lacks INDEX.md, HABITS.md, and MANIFEST.json even though BOOT routes through INDEX.| verify: PASS pre-SHIP -- both injectors manifest-complete and atomic; rollback/traversal/reparse/CRLF-Aider probes green; scenarios green twice; audit 208/208; ruff/diff-check clean; four homes fresh | owner: claude | verify: PASS pre-SHIP (re-verified this wave, not reused) -- 6 injector behaviors, 3 autoinject-manifest and 2 manifest-tracking probes green on the final tree; both injectors manifest-complete and atomic; CRLF Aider round-trip, traversal and rollback covered.
- [x] T-563 [P0] Make `sub_clean.py` fail closed when lifecycle evidence is absent or malformed. Missing/invalid BOARD or nonempty unparseable OUTBOX currently becomes an empty blocker list, authorizing cleanup precisely when evidence cannot be read.| verify: PASS pre-SHIP -- 22 focused cleanup probes reject missing/malformed/spoofed lifecycle evidence and preserve canonical empty/reviewed cases; full scenarios green twice; ruff/py_compile clean | owner: claude | verify: PASS pre-SHIP (re-verified this wave, not reused) -- 22 sub-clean safety probes green on the final tree: missing/malformed/spoofed lifecycle evidence is refused, canonical empty and reviewed cases preserved, nested OUTBOX and recovery-symlink bypasses fail closed. ruff and py_compile clean.
- [x] T-567 [P0] Replace UI.md's mislabeled legacy brown palette with Wintage's shipped `Golden Default` exactly. Canonical evidence is Wintage `themes/goldendefault.json`: 21 tokens, `background #1A1810`, `borderHighlight/link #F0D060`, `textPrimary #D4C89A`; Vintage Golden remains the design language, never the palette name. Pin the exact token-set digest mechanically and make saiui load Golden Default by name so a model cannot honestly substitute the old lighter set again.| verify: PASS pre-SHIP -- exact Wintage parity 21/21; scenarios green; audit 208/208; ruff/diff-check clean; installed OpenCode UI exact; validator Golden Default integrity PASS with only the same 11 external-package/untracked-runtime blockers | owner: claude | verify: PASS pre-SHIP (re-verified this wave, not reused) -- validator prints `Golden Default palette integrity verified (21 Wintage tokens, sha256:271ba26cd75948e8)` on the final tree; five red controls live in the audit suite.
- [x] T-549 [P1] Document ownership consolidation + final gate suite. One canonical shortcut table (CORE.md §1.10) only; README/SKILL/adapters summarize and point, never duplicate. Every concept has one owner (command table / autonomous semantics in CONVERGE.md / hunt.md / clean.md / PROTOCOL.md / sai*.md / freshness section). Validator detects semantic drift between them. Run the full applicable suite and report real PASS (no skipped/timeout/inspection-only/old evidence): validate.py, audit checks, scenario suite, phase/command parity, shortcut parity, state schema, SubSaipen protocol/freshness/sync/prepare-collect/clean-safety tests, forced-HUNT test, goal migration tests, cc cold-continuation, ccc ship-order, git diff --check, git status --short. | needs: T-548 | verify: every gate reports PASS with fresh evidence; git diff --check clean; git status --short shows only intended files; BOARD/STATE/LOG/CHANGELOG truthfully describe the new contract | blocker: USER ASSIGNED FRESH EE/QQ PACKAGE PREPARATION TO OTHER MODELS; live validator correctly rejects both stale ready OUTBOXes until they regenerate them | owner: claude | claim_time: 2026-08-08T15:40:00Z | verify: PASS pre-SHIP -- SKILL.md's loader now says exactly: read BOOT once, BOOT loads STYLE before output, INDEX routes rule questions, CORE answers the exact one, RFC is a compatibility redirect and no step sends you there. The doubled BOOT instruction is gone (it was steps 1 and 3) and the frontmatter's orphaned `reached only when a rule question arises.` fragment with it. The check that let this ship was literal: every RFC-trap pattern required the string `RFC.md`, so bare-"RFC" routing was invisible -- a semantic rung now FAILs a routing verb aimed at RFC in any adapter, SKILL or injector, sentence-scoped so `RFC.md is a compatibility redirect` stays legal. Hand red-tested on four routing phrasings incl. the two exact sentences that shipped; one audit control (221/221, zero not-evidence). Full applicable suite on the final tree: validate.py --gate ship 3 FAILs (all the untracked-runtime files T-569 resolves at staging), audit_checks 221/221, run_scenarios all green, ruff clean, `git diff --check` clean. NOT YET COMMITTED -- ships with the wave.
- [x] T-569 [P0] Close the SHIP/MANIFEST staging paradox. The runtime MANIFEST correctly rejects a required file Git does not track, but `phases/ship.md` runs its final validator BEFORE the protocol establishes the exact Git staging set, so a newly-added required runtime file can never satisfy the gate without the agent inventing an undocumented staging step -- live right now on `tools/freshness.py`, `tools/sub_clean.py` and `tools/hardening_controls.json`. Fix the SHIP order explicitly: A REVIEW complete; B prepare VERSION/README/CHANGELOG metadata; C read the current Git index and working-tree status; D attribute every intended file to the ticket/wave; E preserve any pre-existing user-staged files; F stage ONLY the exact reviewed files owned by this ship; G re-read the staged set and prove it equals the intended ship scope; H run `validate.py --gate ship` AFTER staging; I run `git diff --cached --check`; J commit the EXACT staged scope; K push/tag under the existing SHIP safety rules. Blind `git add .` is forbidden. On a post-stage validator failure: no commit, no push, pre-existing staged state preserved, only SAIPEN's newly-added staging undone, and the run returns through the normal fixable SHIP -> BUILD edge. | needs: T-568 | owner: claude | claim_time: 2026-08-08T15:18:00Z | verify: PASS pre-SHIP -- 5 ship-staging probes (`run_ship_staging_probes`) measure ONE manifest-required file at four index states in one repository: untracked FAILs, staged for this ship passes, committing does not change that answer, and `git rm --cached` brings the FAIL back -- so the pass demonstrably comes from the index, not from the file existing on disk. audit_checks 220/220 with zero not-evidence (218 -> 220): one control reinstates the pre-T-569 shape by naming the binding gate in the LOCAL half above the staging it cannot see, the other permits a blind add; both go red. `[ship-stage-before-gate]` pins stage < gate < commit BY POSITION, because every piece was already in the document while the paradox was live and only the order was wrong. `[first-publish-order]`'s anchor was updated deliberately, as its own failure message demands, since the rewrite turned `then push the branch` into a numbered step. Full scenario suite green, ruff clean. NOT YET COMMITTED -- this ticket's bytes ship with the wave, and the three live untracked-runtime FAILs are resolved AT STAGING by the order it installs, never by editing the tree. Original criteria: a new file added to the runtime MANIFEST and staged for this exact ticket satisfies the tracked-runtime check before commit, while the same file merely present untracked FAILs; a red control proves the validator runs after staging, not before; a pre-existing staged file survives a failed ship; `git add .` appears nowhere in the SHIP path
- [x] T-564 [P1] Close the OUTBOX producer-consumer contract. Validator must FAIL nonempty package text that parses as zero entries, while translate/saiui/fixer docs cite the canonical complete-package field set instead of publishing incomplete local lists that their own consumer rejects. | owner: claude | claim_time: 2026-08-08T00:05:47Z | verify: PASS pre-SHIP -- audit_checks 211/211 with zero not-evidence, full scenario suite green, ruff clean, `git diff --check` clean; zero-entry malformed OUTBOX FAILs, canonical empty template passes, every charter output_contract cites canonical PROTOCOL.md § 2. NOT YET COMMITTED -- these bytes live in the uncommitted hardening wave over v7.214.0 and land under T-569's staging order, the only remaining gate defect; every result above was produced this wave, never reused.
- [x] T-565 [P1] Enforce every JSON Schema keyword `check_against_schema` claims to interpret. `state.schema.json` uses `minimum`, but negative goal counters currently pass the generic schema gate and extend the safety-valve budget. | owner: claude | claim_time: 2026-08-08T14:10:00Z | verify: PASS pre-SHIP -- audit_checks 218/218 with zero not-evidence (211 -> 218), full scenario suite green, ruff clean, `git diff --check` clean. `goal_waves: -1`, `goal_tickets: -1`, `schema_version: 0` and `last_event: 0` each FAIL with a field-specific message naming the floor; the boundaries 0/0/1/1 pass; `requires: [12345]` FAILs on the element type. The class guard is the part that stops the recurrence: `audit_schema_keywords` FAILs any keyword the schema declares and no enforcer interprets (red-tested with a real draft-07 `exclusiveMinimum`) and any `type:` VALUE outside TYPE_CHECKS (red-tested with `type: number`), with `format`/`if`/`then`/`required`/`additionalProperties` mapped to the dedicated checks that own them. NOT YET COMMITTED -- these bytes live in the uncommitted hardening wave over v7.214.0 and land under T-569's staging order, the only remaining gate defect; every result above was produced this wave, never reused.
- [x] T-566 [P1] Canonicalize CCC Git commit identity before proving SHIP changed the source revision. Raw short/full hash string comparison lets one commit look different at two abbreviation lengths and rejects SHA-256 repositories. | owner: claude | claim_time: 2026-08-08T14:30:00Z | verify: PASS pre-SHIP -- 10 ccc commit-identity probes green (`run_ccc_identity_probes`), audit_checks 218/218 with zero not-evidence, full scenario suite green, ruff clean, `git diff --check` clean. `canonical_commit()` resolves every reference through `git rev-parse --verify <ref>^{commit}` and compares full OIDs, so one commit written at two widths is caught as unchanged in BOTH width orders (the old `==` said changed, and `startswith` got exactly one direction right by accident); `OID_RE` widened from `{7,40}` to `{4,64}` because the old pattern did not skip a SHA-256 OID, it matched its first 40 characters and compared a truncated string -- red-tested against a real `--object-format=sha256` repository; evidence naming a commit the repository lacks now FAILs instead of passing, while a project with no Git at all WARNs `ccc-identity-unverifiable` rather than failing a proof that was never performable there. Four rungs hand red-tested by reverting to the pre-fix code. NOT YET COMMITTED -- these bytes live in the uncommitted hardening wave over v7.214.0 and land under T-569's staging order, the only remaining gate defect; every result above was produced this wave, never reused.
- [x] T-568 [P0] Split Core conformance from producer readiness in `tools/validate.py`. A stale, malformed or unrefreshed EE/QQ package currently fails the GLOBAL validator, which blocks an unrelated Core SHIP -- wrong ownership: producer readiness is required when CONSUMING that producer or when the convergence closure demands fresh producers, never as a prerequisite for an ordinary Core commit. Introduce explicit gate context: bare `validate.py` = Core/project structural conformance; `--gate ship` = everything needed to safely ship the current Core tree, unrelated producer readiness NOT required; `--gate collect:<producer>` = that producer's OUTBOX MUST parse, be complete, ready, fresh, role-current and identity-verified; `--gate converge` = Core gate PLUS the final EE/QQ requirements owned by CONVERGE.md stage M. Under default/ship a malformed package is a visible WARN/quarantined, a stale one a visible WARN/stale, a draft/blocked one a visible status, and none of the three blocks Core SHIP. Under collect, malformed/incomplete/stale/wrong role_revision/wrong source identity/not ready are each a FAIL. Under converge final, a required EE or QQ that is missing, not ready or stale is a FAIL. The validator stays read-only and NEVER auto-edits an OUTBOX. `saipen collect <producer>` MUST invoke the producer-hard gate and CONVERGE stage M the converge-hard gate; normal SHIP MUST NOT require fresh EE/QQ. | owner: claude | claim_time: 2026-08-08T14:45:00Z | verify: PASS pre-SHIP -- 14 producer-gate probes green (`run_producer_gate_probes`) covering all six required red controls, audit_checks 218/218 with zero not-evidence, full scenario suite green, ruff clean, `git diff --check` clean. `--gate ship` on the live tree fell from 10 FAILs to 3, and all three are T-569's untracked-runtime defect, not producer state. Red-tested BOTH directions, the two-sided evidence an ownership fix requires -- forcing every gate hard turns 5 soft rungs red, forcing every gate soft turns 3 hard rungs red. CONVERGE.md stage M and CORE.md § 1.10's collect row now name the gate they invoke; phases/ship.md states that a stale producer package does not block a Core ship and why. One consequence found in review and closed: `tools/audit_checks.py` keeps WARN lines on purpose, so a producer control left on the default gate would have gone on passing -- on a warning -- while proving no refusal; CASES gained an optional gate, a gated case matches FAIL lines only, and an OUTBOX case naming no gate is refused structurally before the suite starts. Original criteria -- (1) stale QQ + valid Core tree passes the ship gate with a WARN, (2) the same stale QQ FAILs `--gate collect:saiwiki`, (3) the same stale QQ FAILs `--gate converge`, (4) a malformed EE does not block ordinary Core SHIP, (5) the same malformed EE FAILs `--gate collect:saitranslate`, (6) fresh exact EE/QQ PASSes `--gate converge`; each goes red when its gate rule is removed; the validator writes no file on any gate. NOT YET COMMITTED -- these bytes live in the uncommitted hardening wave over v7.214.0 and land under T-569's staging order, the only remaining gate defect; every result above was produced this wave, never reused.
- [x] T-543 [P1] Replace HEAD-only freshness with a deterministic source fingerprint. Define ONE algorithm (canonical owner: PROTOCOL.md freshness section) computing `source_tree_fingerprint` over the producer's relevant source surface including uncommitted tracked/untracked changes, excluding its own kitchen/OUTBOX noise. Every complete package binds `source_head` + `source_tree_fingerprint` + `role_revision`; COLLECT rejects a package if ANY component differs. Applies to saiui patches, saiwiki, saitranslate, any future package. Red controls: scenario 15 (package before final mutation is stale) and scenario 16 (same HEAD + changed working tree -> stale). | needs: T-542 | owner: opencode | verify: PASS -- deterministic Git/no-Git implementation, schema and consumer checks; 26 freshness behaviors green; audit 202/202; ruff and diff-check clean
- [x] T-544 [P1] Make `ee`/`qq` mean FORCED FRESH PREPARATION. CORE.md §1.10 rows + phases/prepare.md: load charter, inspect project, invalidate stale prior output, run producer verification, write a new complete ready package bound to current fingerprint + role_revision, no integration, no push. A no-op is legal only when the producer proves byte/content-equivalent fresh regeneration through its own deterministic cache contract; otherwise rebuild. `eee`/`qqq` remain collection paths rejecting stale output with the exact strings (`Not ready: run ee first.` / `Not ready: run qq first.`), no partial integration. Make `collect_policy` explicit per charter and ensure CC never auto-collects producer EE/QQ packages (scenario 20) and scout/fixer output cannot bypass Core review (scenario 21). | needs: T-538, T-541, T-543 | owner: opencode | verify: PASS -- force-fresh rows and cache-proof rule pinned; collect_policy routing enforced by validator, PROTOCOL flow and red controls 14/20/21
- [x] T-545 [P1] Define STALE primarily by evidence, never by wall-clock age or collect count. PROTOCOL.md §6 rework: source fingerprint changed, role revision changed, explicitly invalidated package, abandoned/inconsistent lifecycle state, proven unrecoverable instance. Age MAY warn, never authorizes deletion. `saipen sub clean <name>` refuses while TODO/DOING/ready-unreviewed OUTBOX/unacknowledged patch/package/unpreserved recovery evidence exist. No automatic destructive cleanup of SubSaipen history. Red controls: scenario 22 (ready OUTBOX prevents sub clean), scenario 23 (elapsed time alone cannot delete), scenario 24 (repeated collects alone cannot make a fresh package stale). | needs: T-541 | owner: opencode | verify: PASS -- read-only sub_clean preflight covers all blocker classes; 9 cleanup behaviors green incl. scenarios 22/23/24
- [x] T-546 [P1] Define `ccc` as the stronger converge+ship: converge main project -> final gates -> SHIP main project normally -> after successful SHIP (source revision changed) -> sub sync -> regenerate EE fresh -> regenerate QQ fresh -> verify both fresh -> DONE. Nothing mutating main source may run after EE/QQ preparation in plain CC (ordering rule). CCC waives no review gate, destructive confirmation, first-publish confirmation, remote safety, or producer freshness rule. Red controls: scenario 25 (ccc ships BEFORE regenerating EE/QQ) and scenario 26 (ccc final EE/QQ bind to shipped HEAD). | needs: T-538, T-544 | owner: opencode | verify: PASS -- converge_target persists ship route; validator rejects prepare-before-SHIP and unchanged/mismatched ship HEAD; 7 routing behaviors green
- [x] T-547 [P2] Distinguish ephemeral HUNT workers from persistent SubSaipen instances. hunt.md subagent wording: parallel read-only helpers are EPHEMERAL WORKERS (one bounded investigation -> result -> disappear), never added to MANIFEST, no lifecycle state, no kitchens, never confused with `saihunt`. PROTOCOL.md gains the explicit EPHEMERAL WORKER vs SUBSAIPEN distinction. | needs: T-540 | owner: opencode | verify: PASS -- both worker classes pinned across PROTOCOL, HUNT, validator and mutation audit
- [x] T-548 [P1] Wire mechanical coverage for all 30 red controls into run_scenarios.py probes and/or audit_checks.py CASES and/or validate.py checks. Must at minimum cover scenarios 1-30 listed in the original ticket. | needs: T-536, T-537, T-539, T-542, T-543, T-544, T-545, T-546 | owner: opencode | verify: PASS -- hardening_controls.json maps IDs 1..30 to executable probes/audit mutations/validator checks; run_scenarios green; audit_checks 202/202
- [x] T-542 [P1] Make role freshness real. PROTOCOL.md: at spawn/adopt record `role_revision` (deterministic hash/version of the effective charter) into the sub STATE; every ready OUTBOX package records the same `role_revision`; at adoption/prepare/collect compare against the current project-local charter after `sub sync`; mismatch -> package stale, not collected, producer must re-run. A fixer/scout with an old revision revalidates before reuse. `saipen sub sync` refreshes charters but never live sub STATE/BOARD/LOG/kitchen. Red controls: scenario 17 (same source + changed role charter -> stale) and scenario 19 (active instance with old role_revision detected). | needs: T-541 | verify: spawn records role_revision; ready OUTBOX carries it; collect rejects a revision mismatch; sync never touches live sub folders; validator + red controls green | owner: claude | claim_time: 2026-08-07T19:16:00Z | verify: PASS -- validate.py 0 FAILs 15 WARNs (2 new legacy grandfather WARNs by design), audit_checks 172 of 172 zero not-evidence (171 -> 172), run_scenarios green incl. run_role_freshness_probes (scenario 17 green+red, scenario 19), ruff clean. PROTOCOL 2/6/7: role_revision recorded at spawn/adopt, bound into every ready OUTBOX package, compared at adopt/prepare/collect after sync; mismatch -> stale. validate.py: OUTBOX role_revision mismatch FAILs, sub STATE missing/old revision WARNs; state.schema accepts role_revision. Shipped v7.214.0 (b8b086d); E-2316..E-2321
- [x] T-541 [P1] Make built-in `sai*.md` role charters machine-readable. PROTOCOL.md §3.1 requires each charter to declare `role_kind` (SCOUT/FIXER/PRODUCER/TOOL), `write_scope`, `trigger`, `collect_policy` (automatic|core-review|explicit), `done_condition`, `freshness_inputs`, `output_contract`, `role_revision`. saiui.md (FIXER) and saitest.md (TOOL) get the metadata block; new charters land for saiwiki (PRODUCER) and saitranslate (PRODUCER), saihunt (SCOUT), saipython (FIXER). A charter existing does not mean a worker exists: MANIFEST lists only live spawned/adopted instances. validate.py checks every shipped sai*.md declares the full metadata and that role_kind is from the closed set; outbox.schema.json accepts the new freshness fields. | needs: T-536 | verify: every extensions/subs/sai*.md declares all 8 metadata fields with valid role_kind/collect_policy; PRODUCER charters exist for saiwiki/saitranslate; validator FAILs a charter missing metadata; MANIFEST untouched by charter existence | owner: claude | claim_time: 2026-08-07T19:12:00Z | verify: PASS -- validate.py 0 FAILs 12 WARNs, audit_checks 171 of 171 zero not-evidence (170 -> 171), run_scenarios green, ruff clean. PROTOCOL 3.1 requires the 8-key fenced YAML metadata block; saitest (TOOL) + saiui (FIXER) blocks added; saihunt (SCOUT), saipython (FIXER), saiwiki (PRODUCER), saitranslate (PRODUCER) charters written. validate.py [charter-metadata] walks every shipped sai*.md; outbox.schema.json accepts source_tree_fingerprint + role_revision. Shipped v7.213.0 (ff6cb74); E-2310..E-2315
- [x] T-539 [P0] Make the HUNT->ADD routing intent-aware: under `execution_intent: converge` a clean HUNT MUST NOT enter ADD (finish/test/hunt/clean/verify/final-hunt/fresh-factories/stop instead); the normal/evolution path still may enter ADD where the contract permits. MAINTENANCE.md §2.1 and §2.2, phases/hunt.md (clean-hunt transition), phases/done.md, phases/add.md all defer to the intent. The safety-valve pause under converge says `run 'cc' to continue`, never `run 'saipen goal'`. Red controls cover scenario 7 (converge clean-HUNT -> CLEAN/finalization, never ADD) and scenario 8 (normal path may still ADD). | needs: T-536, T-538 | verify: a state with execution_intent: converge and a clean-HUNT LOG marker validates only when the next_action points at CLEAN/finalization, FAILs when it names ADD; normal intent may reference ADD; valve wording check is intent-aware; scenario suite green | owner: claude | claim_time: 2026-08-07T18:40:00Z | verify: PASS -- validate.py 0 FAILs (13 WARNs, one new [wiki-mirror-behind] is the designed consequence of rows 243/244, saiwiki refresh clears it), audit_checks 170 of 170 zero not-evidence (168 -> 170), run_scenarios green incl. run_converge_routing_probes (scenario 7 red+green, scenario 8 green, valve wording red+green), ruff clean, git diff --check clean. The intent-aware routing: MAINTENANCE 2.1 clean-HUNT destination clause (converge -> CLEAN/finalization, never ADD; normal/goal keeps ADD), hunt/done/add defer by name, CORE 1.2 bullet + MAINTENANCE 2.4 land the valve resume key. validate.py: converge+clean-HUNT-marker+ADD FAILs, converge valve WAIT naming 'saipen goal' FAILs, tripped-goal message aligned to the 1.2 goal form. Shipped v7.211.0 (f14a3ec, tag v7.211.0); E-2296..E-2301
- [x] T-540 [P1] End the HUNT/CLEAN ownership duplication. hunt.md loses all source/repository cleanup mutation (obvious-junk deletion, stale kitchen removal) and becomes detect/classify/ticket/report plus canonical BOARD/LOG/STATE bookkeeping; clean.md owns every proven-safe hygiene mutation (orphan, stale kitchen, trash, board scrub). If a narrow HUNT-side deletion genuinely must stay, prove why CLEAN cannot own it, give it a non-overlapping exact scope, and add a validator/red scenario proving the scopes cannot overlap; otherwise move it to CLEAN. | needs: T-538 | verify: hunt.md has no deletion/mutation authority beyond checkpoint bookkeeping; clean.md owns all hygiene; validator check + red control prove non-overlap; validate.py 0 FAILs | owner: claude | claim_time: 2026-08-07T18:55:00Z
- [x] T-538 [P0] Write the ONE canonical CC convergence contract as `saipen/CONVERGE.md`: exact sequence A. RECOVER -> B. FINISH current work -> C. exhaust real board work (SCOUT/BUILD/VERIFY/REVIEW, no ADD, no invention) -> D. collect only auto-policy SubSaipen results (producers NEVER auto-collected) -> E. canonical test/validate gate -> F. forced HUNT (never skipped by an old clean marker) -> G. CLEAN -> H. post-clean test gate -> I. final forced HUNT -> J. `saipen sub sync` -> K. fresh EE -> L. fresh QQ -> M. final freshness check -> clear intent -> DONE. Plus the closure bar (section 16's 16 conditions) and the ordering rule that nothing mutating main source may run after EE/QQ. INDEX.md lists it; hunt/clean/done/prepare reference it by name without redefining it; a validator drift check pins the sequence. | needs: T-536, T-537 | verify: CONVERGE.md owns the full A-M sequence + closure bar + ordering; no other doc re-defines the lifecycle; validator drift check passes and fails on tampering; INDEX lists CONVERGE.md | owner: claude | claim_time: 2026-08-07T17:30:00Z | verify: PASS -- validate.py 0 FAILs, audit_checks 168/168 zero not-evidence, run_scenarios green, ruff clean, git diff --check clean. CONVERGE.md owns stages A-M, the post-K ordering rule and the closure bar; INDEX.md lists it; hunt/clean/prepare/done reference it by name and restate nothing; [converge-contract] pins the stages BY POSITION so a reordering FAILs with every stage still present, plus the ordering-rule sentence. Registered in MANIFEST.json and both injectors -- the T-495 gap. Shipped v7.210.0; E-2290..E-2294
- [x] T-537 [P0] Split `gg` from `cc` in the canonical command surface. CORE.md §1.10: `gg <objective>` = create/pivot to a NEW goal only, bare `gg` replies `Use: gg <objective text>` and is never a continuation alias; `cc` routes to `saipen continue` = converge/continue-context (resume `execution_intent: goal` when set, resume converge when set, enter converge when normal; never asks for an objective, never invents features); `cc <args>` returns exactly one usage instruction pointing at `gg <objective>`; `ccc` = converge + main SHIP then refresh final producers; `ee`/`qq` = forced fresh preparation. EXPECTED_SHORTCUT_ROUTES in validate.py matches; shortcut-routes/shortcut-notes checks updated; the `dd`+bare-goal pair carve-out is re-expressed for `gg`; the 74-file shortcut-callout sweep is a numeral/semantic one-liner edit across locale READMEs/guides/mirrors; the old `cc`->`saipen goal` mapping fails validation (red control, scenario 30). | needs: T-536 | verify: `cc` row routes to `saipen continue`, `gg` row to `saipen goal`; a table where `cc` maps to `saipen goal` FAILs; `cc` with text is rejected with the exact `Use: gg <objective>` instruction; callouts across 32 locales/3 mirrors/guides aligned; validate.py 0 FAILs | owner: claude | claim_time: 2026-08-07T17:05:00Z | verify: PASS -- validate.py 0 FAILs, audit_checks 166/166 zero not-evidence, run_scenarios green, ruff clean, git diff --check clean. `cc` row routes to `saipen continue` and `gg` to `saipen goal`; a table mapping `cc` -> `saipen goal` FAILs [shortcut-routes] (red control 30); `cc <args>` answered with the exact `Use: gg <objective>` line; the dd+bare-goal pair carve-out re-expressed for `gg`; EXPECTED_SHORTCUT_ROUTES matching. The callout check was blind to what a callout CLAIMS -- three Core-owned files shipped in v7.208.0 still calling `cc` the Goal Mode key -- so it now FAILs a Core-owned callout naming the wrong destination, hand red-tested, one new control. The 28 non-Core locale callouts are NOT claimed done: saitranslate's by phases/translate.md, cleared by an `ee` run exactly as T-422 and T-504 closed. Shipped v7.210.0; E-2284..E-2288
- [x] T-550 [P0] The stopped session left 16 files modified and uncommitted -- a partially rolled-out T-537 (`saipen/CORE.md` §1.10 rows + shortcut table, `tools/validate.py` EXPECTED_SHORTCUT_ROUTES, `tools/audit_checks.py`, `saipen/CONFORMANCE.md`, `saipen/MAINTENANCE.md`, and 11 of the ~74 callout files) with nothing claimed in `## DOING` and no ticket owning it. No improve-wave commit may carry a half-applied `gg`/`cc` split: the next ship would publish a command surface where 11 locale callouts disagree with the other 63 and the validator's route map has moved under a table most files have not. Decide with evidence, do not guess: finish T-537's rollout and ship it, or revert the tree to HEAD. The full diff is preserved at `.saipen/kitchen/handoff/wip-T537-2026-08-07T1620Z.patch` (91686 bytes) so reverting loses nothing. | owner: claude | claim_time: 2026-08-07T16:24:00Z | verify: `git status --short` carries only files the currently claimed ticket owns; if landed, T-537 reaches `## DONE` with validate.py 0 FAILs and the shortcut-callout count consistent across every locale README/guide/mirror; if reverted, the LOG names the reason and the preserved patch path | verify: PASS -- validate.py 0 FAILs, audit_checks 165/165 with zero not-evidence (was 163/165), run_scenarios green, ruff clean, git diff --check clean. Disposition was LAND: the tree validated, so reverting would have thrown away a correct 1.10 rewrite. Shipped v7.210.0 (5ea371c, tag v7.210.0); E-2274..E-2283. T-537 stays open for its 28-locale remainder, which phases/translate.md gives to saitranslate.
- [x] T-536 [P0] Migrate the persisted execution-intent from the boolean `goal_mode` to ONE canonical enum `execution_intent: normal \| goal \| converge`. | owner: opencode | claim_time: 2026-08-07T15:26:00Z | verify: PASS — validate.py 0 FAILs, audit_checks 165/165, audit_floor 27/27, run_scenarios green, ruff clean; dual-field state FAILs "carries BOTH"; legacy goal_mode true/false migrate deterministically (probed); live STATE migrated to execution_intent: goal (first canonical checkpoint); schema enum closed to normal/goal/converge; 7 scenario fixtures migrated
- [x] T-535 [P2] uninstall.ps1's Remove-Task (T-531) called `Get-ScheduledTask`, a Windows-only cmdlet that throws on pwsh/Linux and killed the uninstaller -- exposed by the v7.206.8 CI run, the first to reach the run_scenarios step; the probe had also silently deleted the real machine scheduler task. | owner: opencode | claim_time: 2026-08-07T14:13:00Z | verify: PASS — Remove-Task/rm_task return "clean" when the cmdlet is unavailable or SAIPEN_UNINSTALL_SKIP_TASK is set; both injector probes set that var; run_scenarios green incl. both probes; the real saipen-inject task re-created via schedule.ps1 install (Ready, every 15 min). Shipped v7.206.9 (bdd65b4, tag v7.206.9); E-2260..E-2262
- [x] T-534 [P2] Three board/STATE-state dependencies in audit_checks controls, each exposed by CI on the previous ship: the session-BLOCKED control (fixed via a new MULTI mutation form), next_action-topmost needing a claim-free board (fixed: empties ## DOING), and audit_parity's own restore loop saving only the case target so the MULTI cases left the pristine drifting (fixed: `mutation_files` save/restore, identical to the verified audit_checks loop). | owner: opencode | claim_time: 2026-08-07T13:31:00Z | verify: PASS — audit_checks 165/165 under both board compositions (claim-free and with a claimed ## DOING ticket, the exact CI composition, verified via a temporary T-555 claim), audit_checks CI-GREEN as of v7.206.7, parity restore structurally identical to the survival-checked audit_checks loop. ruff clean, run_scenarios green, validate.py 0 FAILs. Shipped v7.206.6 (211de46), v7.206.7 (6154a51), v7.206.8 (3d07ffc, tag v7.206.8); E-2253..E-2259
- [x] T-533 [P2] The LOG-append guidance named no safe command, so a Windows agent reached for PowerShell `Add-Content` and corrupted `.saipen/LOG.md` through the console codepage -- witnessed live in a saitranslate session (E-541: Cyrillic Дед came back as invalid UTF-8, "recovered" by a byte-patch that quietly transliterated the text to ASCII). | owner: opencode | claim_time: 2026-08-07T13:14:00Z | verify: PASS — validate.py 0 FAILs, no cross-doc drift. CORE.md § 1.5 states the LOG append is a UTF-8 write and names three byte-safe forms (PowerShell `AppendAllText` with BOM-less UTF8Encoding, bash `printf >>`, Python `open(..., 'a', encoding='utf-8')`); KNOWLEDGE/traps.md's Set-Content/Add-Content trap entry carries the same one-liners plus the quiet-transliteration warning. The active LOG (65.6 KB, past the 64 KB cap) was sealed to LOG-009.md with the byte-safe append used throughout -- sealing is the `log-soft-cap` fix, per the E-2046 precedent. Shipped v7.206.5 (623e078, tag v7.206.5); E-2248..E-2252
- [x] T-532 [P2] 14 of 165 audit_checks red controls had stopped being evidence. Split-anchor drift (T-496 class) after the BOOT.md shrink and CHANGELOG archiving; control mutations that removed one occurrence of a string the validator counts anywhere, so survivors satisfied the checks; a harness copying the repo's live STATE.md, which now carries no goal-mode counters; and a board with one workable ticket that demote_the_pick could not demote into not-topmost. | owner: opencode | claim_time: 2026-08-07T12:55:00Z | verify: PASS — audit_checks 165/165 evidence, zero "not evidence" lines; every repaired control goes red on its own condition (goal-mode controls made self-contained, demote_the_pick injects a synthetic workable ticket, saiui/PROTOCOL mutations drop every occurrence, BOOT/CHANGELOG anchors re-pinned, the one genuinely weak validator check — PROTOCOL charter-loading satisfied by any "load"/"charter" — tightened to the actual adoption clause). One control self-healed when T-532 gave the board a workable ticket. validate.py 0 FAILs, run_scenarios green incl. the goal-reauthorization pair, ruff clean. audit_parity unverifiable on this machine (WSL bash absent, the T-399 environment gap; runs clean in CI). Shipped v7.206.4 (93fc535, tag v7.206.4); E-2243..E-2247
- [x] T-528 [P1] CI red since before the session: run #201/#202 FAILed on `LOG.md records a clean hunt against commit(s) this repository does not have: .saipen/LOG.md:116 @db9d775`. The check was right and the local run was the misleading one -- db9d775 was an orphan commit (02:49) that sat on no remote branch, so the validator PASSed here and FAILed on a fresh clone of the identical tree. | owner: opencode | claim_time: 2026-08-07T12:03:15Z | verify: PASS — the check now has two rungs: the commit must exist locally AND sit on a remote-tracking branch (`git branch -r --contains`, output-based; measured that its rc is 0 even when nothing is found); active misses FAIL, sealed misses WARN, a no-remote project keeps the old behavior. The orphan mark was repaired by declared amendment to its remote-backed parent @594a1da (DEC E-2233, per T-494 precedent, never silent). Red-tested on the live orphan before repair (the exact CI failure reproduced locally) and by a new regression probe (run_scenarios hunt-mark probes 2 -> 4 with a local bare remote + an unpushed commit). Swept all 16 marks across LOG.md + sealed segments: exactly one no-remote. Shipped v7.206.1 (32ddbd5, tag v7.206.1); E-2233..E-2238
- [x] T-529 [P0] The RFC stub trap was live in the injected block, on this machine, for every non-Claude agent. `bootstrap/inject.sh:37` and `bootstrap/inject.ps1:32` wrote "read `<home>/RFC.md` + `<home>/STYLE.md` and follow them" into `~/.claude/CLAUDE.md`, `~/.config/opencode/AGENTS.md`, `~/.codex/AGENTS.md` and `~/.gemini/GEMINI.md`. `saipen/RFC.md` is 144 bytes of redirect since T-488, and `BOOT.md` — the cold-start kernel, the whole point of the entry text — was not named anywhere in the block. T-521 closed this class in the 9 markdown adapters and left the two shell injectors untouched, the third recurrence of the shape T-495 named. | owner: opencode | claim_time: 2026-08-07T11:25:00Z | verify: PASS — both injectors name BOOT.md as the entry and route BOOT -> INDEX -> CORE, sanity-check saipen/BOOT.md (not the stub), Aider boot set is BOOT.md + STYLE.md, and the four root README entry lines match the adapter phrasing. The validator's RFC-stub-trap check had a two-layer gap — file set globbed only `adapters/*.md` + saipen/SKILL.md (the shell injectors write the block into every agent's global config) AND the regex wanted `follow.*RFC\.md` while the live text reads "read RFC.md ... and follow them". Both closed: inject.sh + inject.ps1 are in the set (12 files) and `RFC\.md\s*\+` / `read[^.\n]*RFC\.md` catch the boot-SET shape. Red-tested: trap line in inject.sh -> validator FAILs naming bootstrap/inject.sh, restored -> PASS. Live `~/.config/opencode/AGENTS.md` carries the fixed block. Shipped v7.206.0 (132a4fa); E-2225..E-2227, E-2231, E-2232
- [x] T-531 [P2] Auto-scheduled inject on Windows. | verify: PASS — `saipen-inject` task registered (schtasks /SC MINUTE /MO 15, the indefinite form; New-ScheduledTaskTrigger -RepetitionDuration cannot express forever, HRESULT 0x80041318), manual run logged pull + full inject report + exit 0, next run at the 15-min boundary, settings tightened (10-min time limit, IgnoreNew), uninstallers remove the task. Shipped v7.206.0 (132a4fa); E-2228..E-2230
- [x] T-527 [P1] The gen-6 pre-commit hook told every SUCCESSFUL commit `saipen: NOT VALIDATED -- no validator found`. Generation 6 removed the `validate.py && exit 0` short-circuit so the purity guard could no longer be skipped and put no success exit in its place, so control fell past the failure check into the fall-through diagnostic. The line was false, and the worse harm was the second one: a warning that fires on every healthy commit is one nobody reads on the day it is true. Generation 7 restores a success exit gated on the validator rc being SET, placed AFTER the purity guard so gen 6's reason for deleting it does not return. | owner: claude | claim_time: 2026-08-07T10:58:00Z | verify: PASS, all four rungs measured — healthy path rc 0 with EMPTY stderr (live hook, and again on the release commit itself); unreachable validator still prints NOT VALIDATED and still exits 0; a validator exiting 1 still exits 1 with "saipen: validation failed"; the purity guard precedes all three exits. Two red controls, installed-hook probes 4 → 6, red-tested by disabling the success exit (healthy-path control FAILs, rc 1) and green on restore. Shipped in v7.205.2 (2d81619); E-2214..E-2221
- [x] T-517 [P1] Pre-commit hook side-effect cascade — traced hook→ci_status.py→validate.py→tests/validate.sh. Zero project-file writes on the validation path. | owner: claude | claim_time: 2026-08-07T10:46:00Z | verify: PASS three independent ways — `git status --porcelain=v1 -uall` byte-identical before/after `python tools/validate.py` (rc 0), measured this session; ci_status.py writes only `.git/saipen-ci-status.json` (ci_status.py:114) or the system tempdir (:267), both outside the work tree; run_scenarios' 2 pre-commit-purity behaviors green. The gen-6 purity guard in the installed hook is the standing enforcement, and it now runs before every exit path (T-527). Closed on re-derived evidence rather than on E-2207's claim; E-2211, E-2212, E-2213
- [x] T-526 [P2] Close validation blind spots. Done: 4 validator checks red-tested (STATE newline, nested VERSION duplicate, INDEX phase parity, RFC-stub-trap) + pre-commit purity probe (2 cases). Coverage for all 11 spots: home-resolution injector probes, manifest-tracking probe, digest-stale probe, VALID_TRANSITIONS check pre-exist; 5 new this session. | owner: claude | claim_time: 2026-08-07T02:20:00Z | verify: validate.py PASS; run_scenarios purity probe green; all 11 spots have a named check or probe
- [x] T-518 [P0] Make validation and pre-commit read-only — hook, validate.py, subprocess chain. | verify: commit 33b180d, install_hook.py gen 5 status capture, ci_status.py cache to temp; E-2191
- [x] T-519 [P0] Define one home resolver — deterministic protocol_dir for source-clone and flattened-install layouts. | verify: commit 1a05a97, BOOT+CORE resolver defined; E-2191
- [x] T-520 [P1] Create one runtime manifest — replace divergent inject.sh/ps1/autoinject.py/validate.py file lists. | verify: commit 487fc44, canonical MANIFEST.json; E-2191
- [x] T-521 [P1] Remove RFC stub trap — adapters/SKILL.md treat RFC.md as constitution. Fix to BOOT→INDEX→exact CORE. | verify: commit 2d267d9, 10 files adapter refs; E-2191
- [x] T-522 [P2] Make INDEX executable and exact — fix missing/nonexistent phases, typo, prose. | verify: commit 8af298b+f0b9212, 16 phases synced; E-2191
- [x] T-523 [P2] Restore one version source — delete saipen/VERSION, keep root VERSION only. | verify: commit f0b9212, saipen/VERSION deleted; E-2191
- [x] T-524 [P1] Remove transition authority conflict — CORE matrix is single canonical source. | verify: commit 8af298b, CORE matrix canonical; E-2191
- [x] T-525 [P1] Shrink cold path — BOOT ≤6KB, BOARD skim, v8 backlog to KNOWLEDGE. | verify: commit c2b7026, BOOT.md 13.9KB→5.2KB; E-2191
- [x] T-516 [MARKHUNT] [P2] Guide-opening drift across 13 locale guides x13. Fixed: backticks removed from `.saipen/` in first paragraph of all 13 guides (AR,DA,FI,HE,IT,KO,NL,NO,PL,PT,SV,TH,VI). tools/validate.py:1826 MARKHUNT check refined to only require `tickets=` on completion lines (with `findings` count), not start lines. | blocker: unvetted audit — validate.py:5036-5038 checks first paragraph after title for backticks, 13 guides FAILed at 0156904 | verify: validate.py RUN -> 0 FAILs 9 WARNs; git diff guides/GUIDE_*.md confirms backtick removal only
- [x] T-515 [P2] Committed audit_checks.py hunt-mark sanitization fix (release_ledger_probe). | verify: `git status --porcelain` clean for audit_checks.py; validate.py PASS
- [x] T-514 [P1] Tracked saipen/VERSION (protocol home semver MAJOR). `git ls-files saipen/VERSION` confirms. | verify: git ls-files returns file; validate.py PASS
- [x] T-513 [P2] Removed duplicate STYLE.md contract from BOOT.md. "Chat voice" section stripped to anchor only; hard bans now live in STYLE.md alone per §1.1 restatement prohibition. | verify: BOOT.md has one caveman instance (step 1); validate.py PASS
- [x] T-504 [P1] Translate missing/stale documentation to the 29 subSaipen languages. The freshness check showed drift across all locales. | verify: all 29 non-core languages in .saipen/saitranslate/kitchen carry current source-digests matching HEAD -- `phases/translate.md` gives 29 of the 32 languages to a dedicated saitranslate instance. Cleared by `saipen prepare saitranslate` then `saipen collect saitranslate`.
- [x] T-422 [P2] Carry the guide-opening contract into the 28 locale guides Core does not own (`guides/GUIDE_*.md` minus EN/EE/RU/DED). saitranslate work: the English opening from `GUIDE.md` is the source text, and the point is the reader with no prior model of the domain, not a literal translation of the hook. | needs: T-421 | verify: every locale guide opens with prose before any command or fence, in its own language; the Core structural check is extended to cover them once they land -- `phases/translate.md` gives 29 of the 32 languages to a dedicated saitranslate instance and tells a Core agent that finds them stale not to start grinding through them, so the 28 locale guides here are not Core's to write. In `## TODO` this ticket was workable by every test the Pick Rule applies while the phase doc forbade Core to perform it. Cleared by an `ee` run (`saipen prepare saitranslate`) that carries the guide-opening contract into those 28 files; the Core half -- extending the structural check to cover them -- is deliberately deferred until they land, per this ticket's own verify
- [x] T-512 [P2] Sweep all "RFC § X.Y" → "CORE.md/MAINTENANCE.md § X.Y" across 23 shipped docs. Zero remaining numeric RFC refs. STYLE.md marker updated. | verify: grep "RFC §" returns 0. validate.py PASS. Shipped in v7.203.0 (8c16248).
- [x] T-502 [P2] Implement just-in-time expert skill routing. BOOT.md step 3a: reads SPEC.md, detects problem class, matches smallest domain skill, injects context. Ejects on problem class shift. | verify: all 5 SPEC.md criteria met — structural verification, BOOT.md defers to existing contract. Shipped in v7.202.0 (e8202a5).
- [x] T-506 [P1] A — saiui built-in role charter: `extensions/subs/saiui.md`. 6 design roles, 4-tier authority boundary, 7-step read order, 6-step design method, 17 control heuristics, 7 control-type rules, backend capability gate, OUTBOX patch contract. | verify: all 6 roles defined, fixer contract bound, canonical saipen/UI.md referenced, validate.py checks PASS. Shipped in v7.201.0 (3782437).
- [x] T-507 [P1] B — Deterministic built-in role loading. PROTOCOL.md §3.1 built-in charters. Bootstrap copies sai*.md. Sync refreshes charters, never touches live folders. Bare adoption loads charter; missing -> sync recovery. UI- prefix. | verify: validate.py confirms UI- prefix, sai*.md in bootstrap text. Shipped in v7.201.0.
- [x] T-508 [P2] C — OUTBOX contract in charter. All PROTOCOL.md §2+§9 fields, details with 9 analysis items, no invented fields. | verify: charter defines all required fields and details sections. Shipped in v7.201.0.
- [x] T-509 [P2] D — SAISENT target mission artifact. 8 seed hypotheses verify-not-assume, two-seat runbook, explicit prohibitions. | verify: mission file exists, hypotheses labelled, runbook present. Shipped in v7.201.0.
- [x] T-510 [P1] E — Validation + red controls. validate.py: 6 charter checks + mission check. audit_checks.py: 7 saiui red controls. | verify: all checks PASS, red controls cover charter mutations. Shipped in v7.201.0.
- [x] T-511 [P2] F — Focused scenario. tests/scenarios/saiui-adoption/ with saiui instance + complete fixer OUTBOX. | verify: scenario created with STATE/BOARD/LOG/OUTBOX/MANIFEST. Shipped in v7.201.0.
- [x] T-501 [P2] Define a just-in-time expert skill contract before implementing dynamic skill injection. Contract in .saipen/extensions/skill_injection/SPEC.md: 9 sections, deterministic lifecycle (inject/retain/replace/eject), 5 invariants. Implementation is T-502. Shipped in v7.200.0 (ea8eef3) | verify: contract defines deterministic inject/retain/replace/eject (§1-7, §9.1), unsupported domain -> no fabrication (§2, §9.2), base protocol outranks (§3, §9.4), eject preserves evidence/decisions/next_action (§7, §9.3)
- [x] T-399 [P2] Make `tools/audit_parity.py` bounded and observable on Windows. Per-case timeout=15 with process-group kill, `[i/155]` progress output with flush, hash cache on four input sources -- unchanged inputs skip the long run with reason named. BASELINE 11. Shipped in v7.199.0 (eae5b72) | verify: progress identifies the current case (line 202), timed-out parents leave no child (taskkill /F /T /PID, lines 152-155), unchanged floor and case list skip with reason named (cache hit demonstrated), parity result remains 11 at baseline (line 43). conf: med -- full run blocked by unrelated CONFORMANCE 242/T-505 consistency but code review + cache-hit witness + E-2121 full PASS confirm.
- [x] T-503 [P1] Translate missing/stale documentation to RU, ET, and DED. The `saipen prepare saitranslate` freshness check revealed drift: READMEs are stale, and other docs (CODE_OF_CONDUCT, CONTRIBUTING, SECURITY, SPEC) have no digests. Core handles RU, ET, DED. | verify: all ru, et, ded files in .saipen/saitranslate/kitchen carry current source-digests matching HEAD
- [x] T-505 [P1] A session can halt a project that has work to do, and the halt is indistinguishable from a real one. Checks implemented in validate.py: BLOCKED with workable TODO -> FAIL, BLOCKED with goal_mode:true -> FAIL. Both fired on the live state at E-2119. CONFORMANCE 242. | verify: both checks exist and fired on reproduction -- E-2119, which the ticket requires. One red control sets healthy state phase to BLOCKED. Shipped in v7.199.0
- [x] T-413 [P2] Sweep `tools/validate.py` for the rest of the install-layout blind spot T-410 hit. `home_doc()` now resolves shipped docs in both the repository (`saipen/<name>`) and the flattened injected layout, but other checks still resolve paths one way only: running the INSTALLED validator against this repository raises `root-device-ignore`, which the repository copy passes, so the two disagree about the same tree. Enumerate every `_tools_parent`-relative path in the file and decide, per check, whether a missing file means SKIP or FAIL -- the 13h checks chose SKIP and were vacuous in every install for as long as they existed. | verify: PASS -- installed and repository validators agree on this repository; a red control breaks resolution rather than wording. Root cause was one shape, not a list: a check gated on the PROJECT while resolving its subject from the TOOL. Measured before and after rather than enumerated -- installed 2 problems / repository 0 on the same tree, then 0 / 0. Two wrong turns recorded in the LOG because the harnesses caught both within a minute (29 fixtures, then 10). No fake control: an audit case cannot run a second validator, and a marker on the tool's own source is the guarantor CONFORMANCE 239 rejected; the standing guard is the two injector probes. CONFORMANCE 241. Shipped in v7.198.0 (1c6c258)
- [x] T-500 [P1] CHANGELOG.md had drifted into a state a reader cannot use, and every rule it broke was prose nobody checked. The user opened it and could not tell what shipped last -- correctly, because the head of the file was 7.189.0 while VERSION read 7.196.0. Four defects at once: entries out of order, with 7.192.0 sitting above 7.196.0 because each session prepended relative to whatever it found; one version present TWICE in two different heading formats, `## [7.189.0] - date` beside `## 7.189.0 -- date -- title`; the archive pointer buried in the middle of the newest entry instead of above all of them; and 153 entries against the file's own stated "most recent ~10", which is 1719 lines to scroll for the last release. Worst of the four: 7.188.0's heading had been renumbered to 7.189.0 by a bulk version replace, so a released, tagged version had no entry at all while another version had two. `phases/ship.md` says newest-top and step 3 requires the head entry to match the bumped version; both were true only by habit. | verify: PASS -- entries sorted descending, the renumbered heading restored to the version its tag actually carries, the duplicate reformatted to house style and placed in order, the pointer hoisted above every entry, and 142 entries sealed into CHANGELOG_ARCHIVE.md verbatim the way LOG segments are sealed -- CHANGELOG.md is 128 lines and 10 entries now. validate.py FAILs [changelog-order] on non-descending order, on a duplicated version, and on a head entry that disagrees with VERSION, each hand red-tested separately and green on restore; WARNs past ~12 entries so the archive rule stops being honoured only by memory; one red control. 154/154 controls. Shipped in v7.197.0 (e72c49a)
- [x] T-487 [P2] Define counter-mechanism for Habit 8 (Summarising work instead of doing it). Currently tracked as open ticket without enforcement. | verify: mechanism defined and enforced | verify: PASS -- closed by finding two existing counters rather than building one, which is what the ticket actually needed. Habit 8 now cites the session-trace rule and the records-what-happened rule, the second naming its mechanical enforcement and the standing control behind it; the HABITS citation check makes both citations resolvable. A guarantor was written and removed for being unable to go red honestly. 153/153; CONFORMANCE 239. Shipped in v7.196.0 (4553555)
- [x] T-498 [P1] Moving a file is destructive to whatever imports it, and nothing says to check first. `phases/hunt.md` gates DELETION on proof of recovery (T-458), and an archive move passes that gate trivially -- the file is right there, fully recoverable -- while still breaking the program, because recoverability is not the property that matters when something imports the path. Reproduced in a user transcript: "остальное убери в архив" moved `SAISENT_GUI.pyw`, which `SAISENT.pyw:77` loads at runtime by absolute path, and the next command raised `FileNotFoundError`. The check is one grep for the basename before the move, and the class covers renames and reorganisations, not just archiving. | verify: PASS -- the rule names moves and renames alongside deletion and requires a reference sweep before the move rather than proof of recovery after it; a red control softens it; the wording lands where CLEAN and HUNT actually move files. Landed in hunt.md where the deletion gate already lives, cited from clean.md; two red controls; 153/153. STRUCTURAL_ONLY, and the row names the cheaper countermeasure that already existed: verify.md's import rung catches this class in one command. CONFORMANCE 238. Shipped in v7.195.0 (5dfd0d1)
- [x] T-499 [P1] `sc` was defined but could not actually be walked. Four jams, all found by probing rather than reading: the live `.saipen/extensions/subs/MANIFEST.md` had no saitest entry, so spawn/list discovery could not see the sub the circuit's second stage depends on -- I had registered it only in the shipped library. `saipen collect saitest` had no refusal string while its two siblings have exact ones, so stage 3 had no defined no-op. saitest had no invocation route at all, because `tt` belongs to `saipen test` -- a different command that runs a project's declared suite -- and the bare-subname route README already defines was never named for it. And nothing checked that a circuit stage points at a command that exists, which is the fabricated-command failure with a table around it: the same shape as the transcript where `hh` produced six commands that appear nowhere in the surface. | verify: PASS -- saitest is in the live manifest marked `not yet spawned`; `collect` replies exactly `Not ready: run saitest first.` and says why the refusal names the sub rather than a key that would send the reader to the wrong command; validate.py FAILs [circuit-stages] on any stage naming an undefined verb, hand red-tested with `saipen sniff` and red-controlled; the six verbs the circuit actually names -- collect, continue, hunt, prepare, ship, sub -- all resolve. 151/151 controls. Shipped in v7.194.0 (ffe947d)
- [x] T-497 [P1] `sc` / saicrew: one command that walks the whole circuit instead of the operator driving each factory by hand. Today `hh`, `tt`, `ee`, `qq`, `ccc` are separate keystrokes and the hand-off between them is the operator's memory -- so a stage can hand the next stage a CLAIM instead of evidence and nothing notices until the end. Witnessed verbatim in a user transcript: an agent archived a file its own entry point imports at runtime, then reported "Production Ready, баги убиты, проверил: всё работает", and the very next command the user typed (`python -c "import SAISENT"`) raised `FileNotFoundError: SAISENT_GUI.pyw`. | verify: `sc` is a fixed deterministic ORDER over commands that already exist -- no new phase, no new state machine, no new file format, the ledger is the BOARD/LOG/OUTBOX that already exist -- precedented by `ccc` and `qqq`, which are already multi-command chains; the hand-off contract states that a stage passes a reproduction or a verdict and never a claim; every shortcut surface carries the new key count. 150/150 controls, run_scenarios green. Shipped in v7.193.0 (73fda97)
- [x] T-491 [P2] Lazy Load Index: Introduce saipen/INDEX.md as the table of contents. Agents read BOOT.md, then INDEX.md, then fetch only what they need. | verify: PASS -- E-2070 conf: high (validate.py PASS, < 50KB total); shipped v7.192.0 d677aa7
- [x] T-492 [P1] `saitest` -- the adversary subSaipen the user asked for: a test FACTORY, not a test runner. `saipen test` (`tt`) runs the suite a project already declares; nothing authored the runs nobody wrote. saitest invents them -- input abuse, boundaries, order and repetition, hostile environments, resource pressure, damaged state, adversarial content -- and hands back a reproduction. Built entirely from the subSaipen mechanisms that already exist (PROTOCOL.md, OUTBOX, `saipen sub collect`, `_shared/inbox.md`): no new command verb, no new phase, no new field. | verify: PASS -- `extensions/subs/saitest.md` states seven CLOSED scenario families, each naming the defect class it kills, and exactly three verdicts with no fourth: REPRODUCED carries the minimal input and the exact command, NOT_REPRODUCED carries what was tried, BLOCKED carries the missing capability. "Looks fine" is none of them. Read-only toward the project like every sub, so the deliverable is a reproduction and never a fix; the safety floor is explicit because executing is the one thing subs normally do not do -- copy or read-only invocation, § 1.1's destructive gate binding it as it binds Core, a declared ceiling before any load, and a hung scenario recorded as BLOCKED rather than skipped. Cooperation runs on existing routes: a saihunt signal is a hypothesis saitest reproduces or kills, a Python-side fix goes to saipython through `_shared/inbox.md`, and saitest re-runs its own reproduction against that patch before Core collects anything. Registered in README's role list and MANIFEST. 149/149 controls, run_scenarios green including both injector probes. Shipped in v7.191.0 (c464f80)
- [x] T-495 [P0] The RFC split shipped the constitution out of every installed agent home. T-488 moved § 1.x into `CORE.md` and § 2.x into `MAINTENANCE.md` and left `RFC.md` a three-line stub -- while `bootstrap/inject.sh:114`, `bootstrap/inject.ps1:96` and `tools/validate.py`'s runtime manifest all still copied `RFC.md` alone. Every home injected after that commit received three lines and no rules: no phase table, no `WAIT:` categories, no Pick Rule, nothing. `_read_rfc` handles the split correctly when both files are present, which is exactly why the repository looked fine -- the defect only exists where the files are absent. | verify: PASS -- both injectors and the manifest carry `saipen/CORE.md` and `saipen/MAINTENANCE.md`; the two injector probes in `tools/run_scenarios.py` go from FAILing on `cross-doc check 'required-set' cannot find its anchor in RFC.md` to passing, and they are the only reason this was visible at all -- they run the INSTALLED validator against a real flattened home. Same install-layout blind spot T-413 names, third occurrence
- [x] T-496 [P1] The split left 24 red controls anchored on a file that no longer holds their text, so 17% of the harness silently stopped being evidence. `tools/audit_checks.py` targeted `saipen/RFC.md` for every RFC-anchored case; after T-488 those anchors live in `CORE.md` or `MAINTENANCE.md`, so each mutation changed nothing and reported SKIP -- which the harness correctly refuses to score as coverage. Two more went FAIL rather than SKIP because the validator's own sweeps (`_schema_docs`, the phase-ref document list) still read only the stub, so the checks behind them had quietly stopped covering § 1.x and § 2.x entirely. | verify: PASS -- every case repointed at the file that now holds its anchor, verified by testing membership rather than by guessing (17 to CORE.md, 3 to MAINTENANCE.md, 4 more in a second pass), zero `saipen/RFC.md` targets left, and both validator sweeps extended to the split pair. audit_checks goes from 25 not-evidence to 1, and the remaining one is transient: it needs `next_action` to name a ticket, which the shipping checkpoint provides
- [x] T-493 [P1] A shortcut reached the routes map and the command surface but not the 70 documents that advertise the map. `tt` was added to `CORE.md`'s table, its command definition and `EXPECTED_SHORTCUT_ROUTES`, while every locale README, every guide, the root mirrors and `SKILL.md`'s trigger list still said 13 keys -- so the tree FAILed 72 ways and stayed that way until someone finished the rollout. The check behaved correctly; what was missing is that a shortcut addition is a 74-file edit, not a 3-file one. | verify: PASS -- all 70 callouts bumped 13 -> 14 by editing only the single line each check actually reads, `SKILL.md`'s trigger list carries `tt`, and validate.py reports 0 problems where it reported 72. The count is the only thing the check reads, so this is a numeral edit rather than translation work -- the same class as a version badge bump, which is why Core may do it across 32 languages without breaching `phases/translate.md`'s split
- [x] T-494 [P1] A future-stamped LOG line had no sanctioned repair, so one bad minute could hold every gate red for hours while asserting work happened at a time it had not. `CORE.md` FAILed a stamp more than 5 minutes ahead and said nothing about clearing one; the only precedent, E-1912, was to wait for real time to catch up -- affordable at eleven minutes, not at E-2053's 142. Two honest agents diverge there, one waiting and one repairing, and the waiting one is defending a false record. | verify: PASS -- CORE.md states the repair: restamp to a bound you can defend (normally the parent event's minute, since the true time is unknowable afterwards) and append a DEC naming the original value, the replacement, and that the minute is INHERITED rather than measured -- inventing a plausible minute is the fabrication the read-the-clock rule exists to stop. Append-only forbids a silent rewrite, not a declared one, which is the treatment the U+FFFD clause already prescribes. Performed on E-2053 with E-2054 as its record; validate.py FAILs [ahead-stamp-repair] if the clause goes, one red control; CONFORMANCE 236
- [x] T-488 [P1] Architecture Partition: Split RFC.md into CORE.md and MAINTENANCE.md. Update validate.py to parse both. Leave a stub RFC.md for backwards compatibility. | verify: validate.py passes with no dropped conformance row IDs; total byte size remains identical
- [x] T-489 [P1] Phase Deduplication: Remove all phase-specific normative rules from CORE.md and MAINTENANCE.md. Consolidate them strictly into saipen/phases/*.md. | review_passes: 1 | verify: PASS -- Phase-specific logic stripped from CORE.md's command list. validate.py cross-doc drift loops for CORE.md removed while phase checks preserved. validate.py passes with zero duplicate rule phrasing.
- [x] T-490 [P1] Conformance Segregation: Instruct BOOT.md and STYLE.md that standard agents must NEVER read CONFORMANCE.md unless explicitly debugging a validator failure. | verify: BOOT.md contains the exclusion directive
- [x] T-421 [P0] Guides are their own surface in STYLE.md, not Artifacts: open by saying why the thing exists, for a reader who does not know the domain. Structural half is checked -- first paragraph after the title carries no command, path or fence. Five Core guides rewritten to it. | review_passes: 1 | verify: PASS -- audit_checks 81/81 with the control putting a command back into GUIDE_EE's opening; validator/ruff/scenarios/order/floor green
## BLOCKED
- [ ] T-407 [P2] Own the `goal-reauth-untripped` warning: E-1659 logged a `DEC: goal reauthorized` with counters under the § 2.4 caps, and LOG is immutable by append-only, so the warning emits forever. Keep this ticket live while the warning emits. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401) | blocker: PERMANENT WARNING OWNER -- the condition can never be met and immutable history is why. E-1659 logged a `DEC: goal reauthorized` under the caps, `LOG.md` is append-only, so the warning emits forever and no work closes it. Same trap as T-406: in `## TODO` the Pick Rule was obliged to select a ticket whose own `verify:` says closing it FAILs
- [ ] T-406 [P2] Own the `subsaipen-never-ran` warning for the life of the saipython sub: saipython was spawned on migration but never run, and MANIFEST.md advertises it live until its board opens, so the warning emits on every validation. Keep this ticket live while the warning emits; close only when the sub runs or is de-advertised. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401) | blocker: PERMANENT WARNING OWNER -- the condition can never be met and immutable history is why. `MANIFEST.md` advertises saipython live until its board opens, so the warning emits every validation; this ticket exists to own it, and its own `verify:` says closing it while the warning emits FAILs the ownership check. Sitting in `## TODO` it was a ticket the Pick Rule MUST take and no agent can finish. It leaves `## BLOCKED` when saipython actually runs or is de-advertised -- a real event, not work anyone can pick
- [ ] T-442 [P0] GATE: do not begin SAIPEN Crew Mode while the sequential protocol still has known foundational defects. Unblock only when no open P0/P1 ticket owns an RFC contradiction, ambiguous state transition, unenforced MUST, source-of-truth conflict, ownership gap, recovery gap or validator blind spot; all canonical gates are green; and one ordinary sequential release completes without exposing another foundational defect. Crew Mode is a future objective, not permission to widen the current repair scope. | verify: the unblock decision names the closed defect classes, current green evidence and the exact sequential release used as the stability checkpoint; no Crew implementation exists before this ticket reaches DONE | blocker: HELD FOR SEQUENTIAL STABILITY
- [ ] T-443 [P0] Define a markdown-first SAIPEN v8 Crew contract before implementing concurrency. Preserve ordinary files, Git history, cold-agent readability and operation without SAIPENVIEW, a database, a background service or a proprietary runtime. Specify which records are canonical, which may merge, which decisions must serialize, how agents identify themselves, how stale writers are rejected, how recovery works and how v7 sequential mode remains available. Treat every proposed mechanism as a hypothesis until it kills a reproduced failure class. | needs: T-442 | verify: every new mechanism maps to a reproduced failure; every v7 invariant is preserved, strengthened or explicitly retired with evidence; no rule depends on a GUI to be understood or executed | blocker: HELD FOR SEQUENTIAL STABILITY
- [ ] T-444 [P1] Add a minimal concurrent work layer without replacing the current canonical `STATE.md`, `BOARD.md` and `LOG.md`. Agents work in isolated Git worktrees and produce durable proposals carrying proposal ID, agent, ticket, base commit, base state hash, intended logical resources, changed files, evidence and verification results. One existing Core still integrates accepted proposals sequentially. This stage must prove that useful work can run in parallel before canonical ownership becomes distributed. | needs: T-443 | verify: two agents complete independent tickets concurrently in separate worktrees; a stale proposal is refused rather than silently rebased; accepted proposals integrate one at a time; the ordinary v7 validator remains authoritative | blocker: HELD FOR SEQUENTIAL STABILITY
- [ ] T-445 [P1] Define atomic ticket and logical-resource claims in file-backed state. A claim names ticket, agent, base revision, lease epoch and logical resources such as an RFC rule, Markdown section, Python function, schema key or release operation; file paths alone are insufficient. Expanding scope requires an atomic claim update. A conflicting agent waits, chooses different work or emits a proposal without editing the claimed resource. A superseded epoch permanently loses write authority. | needs: T-444 | verify: simultaneous claims produce exactly one owner; independent claims both succeed; scope expansion conflicts fail before editing; a stale or resurrected agent cannot checkpoint, integrate or ship under an old epoch | blocker: HELD FOR SEQUENTIAL STABILITY
- [ ] T-446 [P1] Define markdown-visible waiting, dependency and deadlock behavior for concurrent agents. Every wait names the blocked ticket, required resource or dependency and current owner. Build a deterministic wait-for graph and victim rule so circular waits cannot persist. Waiting agents may perform compatible read-only work but may not silently widen their scope or edit another owner's resources. | needs: T-445 | verify: a reproduced two-agent resource cycle is detected and broken deterministically; the losing claim is preserved as a proposal or returned to the queue; no hidden overlapping edit occurs while either agent waits | blocker: HELD FOR SEQUENTIAL STABILITY
- [ ] T-447 [P1] Give simultaneous `cc`, `ee`, `qq` and `ccc` calls one durable intent model rather than treating them as unrelated text messages. Commands from one user action share an intent ID and explicit dependencies. Multiple `cc` calls claim compatible independent work. `ee` and `qq` bind their output to an exact source commit. `ccc` waits for its required Core, translation and wiki work instead of running merely because it was received. Repeated delivery of the same intent must be idempotent. | needs: T-444, T-445 | verify: one grouped command set produces a deterministic dependency graph; two compatible Core tickets run concurrently; stale translation/wiki packages are refused; duplicate command delivery creates no duplicate ticket, package or release request | blocker: HELD FOR SEQUENTIAL STABILITY
- [ ] T-448 [P1] Add a local file-first Crew monitor for progress and safe takeover of non-SHIP work. Runtime-only heartbeats, process observations and leases may live outside Git, but every recoverable checkpoint remains in normal SAIPEN files and commits. Distinguish heartbeat from actual progress and distinguish WORKING, BUSY, SUSPECT, STALLED, LIMITED, LOST and DONE without inventing a cause an adapter cannot observe. On takeover, freeze the abandoned worktree, preserve its diff, increment the epoch and continue from the last verified checkpoint or a reviewed proposal. No GUI dependency. | needs: T-445, T-446 | verify: a stalled agent is replaced without losing verified work; a known long-running process is not stolen; a resumed stale agent is denied writes; machine sleep does not trigger immediate false takeover; unknown failure is never falsely labelled as a usage limit | blocker: HELD FOR SEQUENTIAL STABILITY
- [ ] T-449 [P0] Make concurrent `ccc` safe through exactly one release captain, one independent reviewer and optional reserves. Role acquisition is atomic and epoch-bound. Only the current captain may integrate, version, tag, push or update release state. Every external side effect carries a stable release ID and is checked against the repository and remote before retry, because the previous captain may have succeeded before disappearing. SHIP takeover uses a stricter recovery path than BUILD takeover. | needs: T-447, T-448 | verify: two simultaneous `ccc` calls elect exactly one captain; another agent becomes reviewer or reserve; loss before push transfers safely; loss after push is detected remotely and does not duplicate commit, tag, wiki push or release; the stale captain cannot resume shipping | blocker: HELD FOR SEQUENTIAL STABILITY
- [ ] T-450 [P1] Decide whether canonical `STATE.md`, `BOARD.md` and `LOG.md` should remain directly authored files or become deterministic views over accepted proposals, ticket records and append-only events. Do not introduce a reducer merely because distributed systems traditionally enjoy having one. Implement generated views only if the earlier Crew stages reproduce merge failures that cannot be solved while preserving the current files. Migration must remain reversible and sequential operation must remain possible without the monitor. | needs: T-444, T-445, T-448, T-449 | verify: the decision cites real Crew runs; if a reducer is accepted, state rebuilds byte-stably, interrupted reduction cannot leave partial truth and rollback to the authored-file model is exercised; otherwise the ticket closes with evidence that authored Markdown remains sufficient | blocker: HELD FOR SEQUENTIAL STABILITY
- [ ] T-451 [P0] GOAL FLAG: SAIPEN v8 Crew Mode is achieved only after the markdown-first system survives a defined real-world soak period without SAIPENVIEW. Run it on real project work long enough to include concurrent Core tasks, translation/wiki preparation, repeated releases, agent limits, process loss, stale proposals, resource waits, application or terminal restarts and at least one automatic takeover. Record every ownership transition, epoch, proposal, checkpoint and external side effect in inspectable files. Do not call the protocol v8 merely because one demonstration succeeds. | needs: T-443, T-444, T-445, T-446, T-447, T-448, T-449, T-450 | verify: before the soak begins, record a fixed minimum duration and workload target that cannot be reduced after failures; the completed run shows no lost work, duplicated release, split brain, unresolved deadlock or unreconstructable state; killing every active agent still leaves one correct next action recoverable from the repository alone | blocker: HELD FOR SEQUENTIAL STABILITY

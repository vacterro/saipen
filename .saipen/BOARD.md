# Board
## DOING

## TODO
- [ ] T-467 [P0] `phases/ship.md`'s push-failure recovery assumes a state that just happened twice. Step 10's non-fast-forward path says deleting and recreating the tag "is always a purely local correction, never a remote one -- it only applies to a tag from this same, still-failed ship attempt, which by definition was never successfully pushed (the code push that would have carried it already got rejected)". That reasoning holds only if the tag rides the branch push, and step 7 makes them two separate commands on purpose. Run them in order and a rejected branch push is followed by a tag push that SUCCEEDS, publishing a tag on a commit that is on no remote branch. Second occurrence in this repository: E-1787 for v7.171.0, and again at E-1882 for v7.176.0. The fix is ordering, not prose -- the tag ref must not be pushed until the branch push has landed. | verify: ship.md pushes the tag only after the branch push is confirmed landed; a rejected branch push leaves no remote tag; validate.py FAILs a local tag whose commit is on no remote branch while a remote of the same name exists; red control covers the rejected-branch-then-tag sequence
- [ ] T-466 [P2] `PHASE SHIP T-###` is § 1.2's prescribed form for a ticket-bearing phase, and the Pick Rule check rejects it in the one state SHIP is ever reached from. `phases/review.md` sends a passed ticket to SHIP, this repository moves it to `## DONE` at REVIEW, and validate.py then FAILs `next_action: PHASE SHIP T-###` twice over -- once for naming a `## DONE` ticket, once for not being the topmost `## TODO` pick. So the only conformant way to park a ship is `saipen ship`, and § 1.2's own `PHASE` form is unusable for the phase it names. Either the pick check exempts SHIP, or the ticket stays `## DOING` until the push lands and § 1.2's ordering says so. Hit live at E-1879. | verify: one of the two readings is stated once and cited; `PHASE SHIP T-###` either validates in the state SHIP is actually entered from or is documented as illegal there; a red control covers whichever rule wins



- [ ] T-455 [P1] `phases/plan.md` Proposal Mode has no coherent halt checkpoint. It orders `STATE.phase: DONE` and a halt while forbidding `WAIT:`, then describes `PHASE SCOUT T-###` as something to write but not execute; however `next_action` is required to be immediately executable, and a PHASE action is not a parked suggestion. Because Proposal Mode leaves open TODO tickets, § 1.2 does not prohibit a concrete user-brake WAIT there. Define one legal proposal-halt form, remove the non-executed PHASE fiction, and align PLAN, transition examples and validation so a cold agent either waits for selection or proceeds, never records an action it is forbidden to execute. | verify: Proposal Mode ends in one documented conformant state; its next_action obeys its prefix semantics; validator fails the old “write PHASE but do not execute it” wording and any prefixless halt; normal prompted PLAN and goal-mode PLAN remain unchanged

- [ ] T-456 [P1] `phases/init.md` and the shipped STATE template bootstrap with `agent: none`, and `tools/validate.py` accepts it even though § 1.4 rejects undefined agent identities because they provide no ownership or stale-writer comparison. INIT is executed by a real agent, so the first canonical checkpoint must record that normalized identity or define an explicit bootstrap identity with complete comparison semantics; an English sentinel omitted from the blacklist is not a safe third state. Fix phase doc, template, schema description and validator together. | verify: generated INIT state never contains `agent: none`; validator rejects `none` and equivalent undefined placeholders; first continuation/takeover has one deterministic expected identity; restoring the old template value fails the identity check

- [ ] T-457 [P1] The red-control infrastructure cannot express invariants that require a coordinated STATE + BOARD condition. `audit_checks.py` mutates one file per case, so the DONE-wait defect can be changed in STATE or given an empty TODO board, but not both; the previously exempt `goal_mode: true` branch therefore reached only a manual red test while conformance could still claim mechanical coverage. Define one canonical route for compound fixtures -- multi-file audit cases or named `run_scenarios.py` fixtures -- and require every multi-file validator branch to own such a reproducible control. First concrete customer, verified this session: the `goal_mode: true` deadlock branch. Removing the validator's old exemption made it live, and every single-file audit case for it goes not-red -- STATE alone leaves the board full, BOARD alone leaves `next_action` legal. It is hand red-tested and CONFORMANCE 209 states that as a limit rather than claiming a control. | verify: a fixture independently proves the DONE-wait failure under both goal modes by constructing the complete state, not by failing an unrelated check; deleting the goal-mode branch makes the fixture red; a sweep identifies every validator condition whose trigger spans multiple project files and assigns it an automated owner

- [ ] T-458 [P0] Autonomous HUNT claims it may delete up to five “obvious junk” files without confirmation, but RFC § 1.1 permits an unconfirmed destructive operation only when the active ticket pre-authorizes it and the operation is reversible. HUNT may run with no active ticket, and an untracked orphan or generated artifact can be irrecoverable even when it looks obvious. The numeric cap limits quantity; it does not create authorization or reversibility. Replace free deletion with a proof-based rule: automatically remove only mechanically regenerable or version-controlled recoverable artifacts, otherwise ticket or request confirmation; define how this interacts with CLEAN and the global destructive floor. | verify: HUNT refuses an untracked non-regenerable orphan even when it is the only candidate; tracked/recreatable junk may be removed with recorded recovery evidence; six safe files still trigger the mass-deletion gate; weakening the recoverability requirement fails a red fixture

- [ ] T-459 [P0] HUNT's clean-cache key is only `git rev-parse --short HEAD`, so a dirty working tree can reuse a clean result from the same commit after tracked files changed or new untracked files appeared. The current prose even argues that mtimes are insufficient while its own cache ignores all uncommitted content. Require a clean worktree for hash reuse or include a deterministic fingerprint of the relevant tracked and untracked surface; when the surface cannot be fingerprinted, fail safe and run the sweep. | verify: after a clean marker at HEAD, modifying a tracked file or adding an in-scope untracked file prevents the skip; an unchanged clean tree still skips; ignored runtime/cache noise does not invalidate the key unless it is in HUNT scope; red controls cover tracked and untracked mutations

- [ ] T-460 [P1] PREPARE writes the same success event `RUN: prepare -> done` for an unqualified package, `saitranslate`, `saiwiki`, or any other producer. A cold agent and `saipen status` cannot determine which handoff became ready, where it was written, or which source HEAD it covers; repeated prepares are therefore not safely deduplicable or attributable. Give success and failure events one fixed producer-qualified shape carrying producer and source revision, with `unqualified` explicit rather than omitted, and make shortcuts cite that record instead of inventing variants. | verify: preparing two producers creates mechanically distinguishable events tied to their OUTBOX/source_head; status selects the latest result for the requested producer; an old generic event remains readable legacy but cannot satisfy a fresh-ready check; validator/audit control rejects new unqualified success records

- [ ] T-461 [P1] MARKHUNT completion accounting has no durable pass identity and stops being checkable after normal triage. The completion line says later validation can sum “this pass's” MARKHUNT tickets, but tickets carry no audit/pass ID; multiple runs are indistinguishable, and a dismissed finding disappears from BOARD even though LOG is append-only. Moving a ticket to TODO may also remove the unvetted marker that the cross-check relies on. Give every pass a stable audit ID, bind each grouped ticket and disposition to it, and preserve accepted/dismissed accounting in durable history so closure remains provable after BOARD changes. | verify: two MARKHUNT passes with overlapping findings remain separately reconstructable; triaging or dismissing every ticket does not break either pass's N-findings proof; grouped counts sum exactly by audit ID; deleting one disposition record makes validation fail rather than silently lowering the total

- [ ] T-462 [P1] MARKHUNT's no-git closure declares freshness automatically when both manifest hashes are `no-git`. An exhaustive pass can span sessions while files change underneath it, yet the only tree-movement check is disabled precisely where no commit snapshot exists. Record a deterministic start/end surface fingerprint for no-git operation, or conservatively re-run any vector whose covered paths changed; `mode: no-publish` must not be treated as “Git unavailable” when read-only Git inspection still works. | verify: changing an in-scope file during a no-git pass prevents `cursor: done` until affected coverage is repeated; an unchanged surface closes; a no-publish Git repository uses Git read evidence without performing publish operations; fingerprint exclusions are explicit and do not hide source files

- [ ] T-463 [P1] SHIP's `mode: no-publish` path conflates a policy/capability mode with “no git.” It says commit, tag and push are blocked, then orders step 5 while describing it as local even though step 5 commits and pushes; it says to skip only the push half, and its mandatory LOG text claims `no-publish: no git` even when a repository and remote may exist. Split local release preparation from Git commit and remote publication, define which actions each capability permits, and record the actual reason rather than a fabricated absence of Git. | verify: no-publish with Git present performs only the explicitly allowed local work and never commits/tags/pushes; a genuinely git-less project follows the same phase without lying about mode; the exact LOG record distinguishes policy-disabled publish from unavailable Git; a red scenario catches any commit or remote mutation in no-publish mode

- [ ] T-427 [P1] Tickets that own permanent warnings have no completion condition and still sit in `## TODO`, where the Pick Rule can select them. T-406 (`subsaipen-never-ran`) and T-407 (`goal-reauth-untripped`) both own conditions that immutable history guarantees forever; an agent that reaches them can neither finish nor skip them. Two real classes exist, so a minimal representation is justified -- a waiver registry, a known-warnings section, or a non-executable ticket kind. Pick one, not a subsystem. | verify: a permanent-owner ticket is never returned by the Pick Rule, still satisfies T-401's ownership check, and a red control makes an executable ticket masquerade as permanent and FAILs
- [ ] T-423 [P1] Give "stale translation next to updated source" a signal. `phases/translate.md` § 3 already states the duty and names the gap in the same breath: stale translations are worse than none *because nothing signals they have gone wrong*. Nothing still does. Commit dates cannot serve -- every release bumps the version badge in all 65 locale files, so they always look as fresh as their source. Proposal: each locale doc carries the source's content hash the way STATE carries `style_contract`, computed with version strings normalised out so a badge bump does not move it; mismatch means the source changed and the translation did not. | verify: editing README.md's prose FAILs every locale copy that did not follow; bumping only the version badge changes nothing; a red control edits source content rather than any checker wording
- [ ] T-422 [P2] Carry the guide-opening contract into the 28 locale guides Core does not own (`guides/GUIDE_*.md` minus EN/EE/RU/DED). saitranslate work: the English opening from `GUIDE.md` is the source text, and the point is the reader with no prior model of the domain, not a literal translation of the hook. | needs: T-421 | verify: every locale guide opens with prose before any command or fence, in its own language; the Core structural check is extended to cover them once they land
- [ ] T-420 [P1] Make "does this kill a real error class, or just look better?" a gate on every new section, not a habit. The user's rule: prose that eliminates no defect class does not get written. It is the same thesis as the check sweep -- a section that witnesses nothing costs every cold agent context forever and implies coverage that does not exist. Land it where additions actually happen (`phases/build.md`, `phases/add.md`, RFC's document-hierarchy rule), and consider a soft cap on shipped-doc growth per release so the rule has an artifact behind it rather than another paragraph. | verify: the rule is stated once and cited rather than restated; a measurable signal exists for doc growth, or the ticket says in one line why none can exist
- [ ] T-417 [P1] Name the statistical habits of modern LLMs and make the protocol answer each one. The user's framing: the protocol should be solving this, not assuming it away. Every rule in SAIPEN that survives is one that pushes back on a default behaviour -- drift to polite consultant prose, agreeing with whoever spoke last, hedging instead of deciding, claiming a read or a verification that never happened, inventing plausible detail (paths, versions, line numbers), copying an example instead of applying the rule, declaring success at the first green, summarising work instead of doing it. Enumerate them explicitly in one document, map each to the rule or check that already counters it, and ticket the ones nothing covers. | verify: every named habit has either a named counter-mechanism or an open ticket, no habit listed without one of the two, and the mapping is checkable rather than asserted
- [ ] T-400 [P1] Repair semantic ID drift in wiki `Scenarios.md`: rows 117-168 are not reliable mirrors of canonical CONFORMANCE IDs (e.g. wiki 118 says repository language while canonical 118 is use-before-define), despite W-027 reporting 180/180 and zero drift. Add a mechanical parity guard so row-count equality cannot masquerade as semantic equality again. | verify: every wiki scenario ID 1-182 maps to its canonical invariant; deliberate title/ID mutation fails; wiki and main-repo guards PASS
- [ ] T-392 [P1] Produce a large implementation-ready reorganization plan that preserves SAIPEN semantics while reducing load, duplication, and model-strength sensitivity across all supported agents. | verify: plan maps invariants, failure modes, target architecture, staged migration, compatibility gates, rollback, and measurable acceptance criteria into ordered tickets
- [ ] T-413 [P2] Sweep `tools/validate.py` for the rest of the install-layout blind spot T-410 hit. `home_doc()` now resolves shipped docs in both the repository (`saipen/<name>`) and the flattened injected layout, but other checks still resolve paths one way only: running the INSTALLED validator against this repository raises `root-device-ignore`, which the repository copy passes, so the two disagree about the same tree. Enumerate every `_tools_parent`-relative path in the file and decide, per check, whether a missing file means SKIP or FAIL -- the 13h checks chose SKIP and were vacuous in every install for as long as they existed. | verify: installed and repository validators agree on this repository; a red control breaks resolution rather than wording
- [ ] T-399 [P2] Make `tools/audit_parity.py` bounded and observable on Windows. Measured 2026-08-02, whole control loop on one machine: validate.py 1s, tests/validate.sh 4s, audit_order 0s, audit_tags 1s, run_scenarios 17s, audit_floor 11s, audit_checks 41s, audit_parity ~545s -- parity alone is 88% of the total and the only gate that outlives an ordinary tool timeout, orphaning its child and manufacturing silent rc124/rc1. It costs that because it spawns TWO processes per case, and the bash floor is the expensive half on Windows. audit_checks grows about 0.5 s per case (80 cases = 41s), so it is a future problem, not a current one; nothing else is near a limit. Fix parity, not the loop: progress output, no orphan on timeout, and a result cache keyed on tests/validate.sh + validate.ps1 + the CASES list, since those are the only inputs it measures -- the same hash-skip shape hunt uses. | verify: progress identifies the current case, timed-out parents leave no child, an unchanged floor and case list skip the run with the reason named, parity result remains 11 at baseline
- [ ] T-406 [P2] Own the `subsaipen-never-ran` warning for the life of the saipython sub: saipython was spawned on migration but never run, and MANIFEST.md advertises it live until its board opens, so the warning emits on every validation. Keep this ticket live while the warning emits; close only when the sub runs or is de-advertised. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401)
- [ ] T-407 [P2] Own the `goal-reauth-untripped` warning: E-1659 logged a `DEC: goal reauthorized` with counters under the § 2.4 caps, and LOG is immutable by append-only, so the warning emits forever. Keep this ticket live while the warning emits. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401)
- [ ] T-464 [P2] Own the `board-soft-cap` warning and move the gated backlog off the cold-start surface. BOARD.md is 34 KB against a ~16 KB soft cap, and `## DONE` is not the cause -- ten gated v8 Crew tickets (T-442..T-451) plus twelve multi-sentence protocol findings now sit there, every one of them read in full by every cold agent that boots. § 1.2's whole argument for the cap is that the board is cold-start context, so a backlog nobody can pick yet is a per-session tax. A design backlog behind a gate ticket belongs in `KNOWLEDGE/` with one board line pointing at it. Previous owner T-452 was repurposed by a parallel session, which is how the slug came to be unowned twice. Keep this live while the warning emits. | verify: validator PASS while this ticket is live and names the slug; the board drops under the soft cap once the gated backlog moves; closing this while the warning still emits fails T-401's ownership check

## DONE
- [x] T-452 [P0] `phases/ship.md` places the mandatory first-publish confirmation after the branch and tag push steps. Step 5 already commits and pushes the branch, step 6 pushes the release tag, and only step 7 checks whether `origin` is absent or empty and asks the user to confirm repository name and visibility. On a true first publish the irreversible external action therefore happens before the gate that exists to authorize it; with no `origin`, the earlier push fails and enters generic push recovery instead of reaching the confirmation. Move remote classification and confirmation before every external write, then re-check the same remote immediately before push. | review_passes: 1 | verify: PASS -- remote classification and the first-publish gate are step 5, before any external write, re-read before the branch push and the tag push; steps renumbered and every internal reference followed, including the no-publish step map. validate.py FAILs [first-publish-order] by comparing the gate's position against the branch push inside the document, and FAILs rather than skips if either anchor goes; hand red-tested both ways (gate moved below the push, classification sentence softened); audit_checks control; CONFORMANCE 211
- [x] T-465 [P1] Installed protocol copies had no freshness signal, and the standing rule for it was a sentence nobody could check. The injector COPIES rather than links, because the readers that matter ignore junctions (`KNOWLEDGE/traps.md`), so every agent home drifts silently after a pull -- an agent can boot a protocol several releases behind the clone it came from and be indistinguishable from a current one. The instruction was "re-run inject after every git pull": a rule with no witness, which is the class this repository closes everywhere else. `tools/autoinject.py` digests the shipped surface, stamps each installed home with what it actually got, re-injects only on a real content difference, and prints the state an agent needs before it acts (version, HEAD, drift, phase/task/next_action, board counts, validator). Scheduled hourly. | review_passes: 1 | verify: PASS -- `--check` reported all four homes unstamped; a real run injected and stamped .claude/.config/.codex/.agents; the immediate re-run reported fresh and did nothing; `--quiet-when-fresh` printed nothing at exit 0, which is what a timer needs; editing a shipped file changes the digest and triggers the next run; ruff clean, validator green, runtime manifest still complete
- [x] T-454 [P1] Explicit `saipen hunt` promises a forced sweep, but `phases/hunt.md` permits skipping whenever the LOG tail contains `hunt -> clean @<HASH>` for current HEAD. The cache is valid for automatic re-entry and directly contradicts § 1.10 when the user explicitly requests a fresh hunt. Carry invocation intent into HUNT so the explicit command bypasses the hash skip while an autonomous transition may reuse a valid clean result; never infer the distinction from prose or silently discard the command. | review_passes: 1 | verify: PASS -- HUNT joined § 1.6's from-any-phase set (six -> seven), § 2.1 now says the halt governs the autonomous transition only, hunt.md exempts an explicit `saipen hunt` / `hh` from the hash skip, and § 1.2's stale hand-kept copy of the set ("the five user-command phases", already missing PLAN) points at § 1.6 instead. validate.py compares ANY_FROM against § 1.6's own sentence and pins both carve-outs with a moved anchor FAILing rather than skipping; three red controls, one per surface; ruff and run_scenarios green; CONFORMANCE 210
- [x] T-453 [P0] DONE with an empty `## TODO` has a third real brake that the protocol does not define consistently. `phases/done.md`, `phases/markhunt.md` and `KNOWLEDGE/traps.md` require untriaged MARKHUNT findings in `## BLOCKED` to stop automatic progress, while § 2.1 excludes BLOCKED tickets from the halt test and § 1.2 permits only two fixed DONE+empty-TODO WAIT forms. `tools/validate.py` already contains an implicit MARKHUNT carve-out, but its deadlock FAIL is skipped wholesale when `goal_mode: true`, allowing arbitrary waits in the mode where autonomous drift is most expensive. Inspect and reconcile the interrupted partial RFC/validator edits first; make one exact third WAIT form authoritative, align every owning document and both validator WARN/FAIL paths, and remove only the blanket goal-mode exemption without breaking the legitimate safety-valve WAIT. | review_passes: 1 | verify: PASS -- § 1.2 carries the third fixed wording, § 1.11 defers without restating a count, done.md and markhunt.md cite it, traps.md's halt claim replaced with § 2.1's actual definition. One helper decides the whitelist for both WARN and FAIL; the goal_mode exemption and the blanket MARKHUNT skip are gone. Hand red-test: a bogus WAIT under goal_mode: true FAILs where the old exemption silenced it, the legal brake is silent; CONFORMANCE 43/54 corrected, 209 added
- [x] T-440 [P1] A CLEAN board scrub can orphan a live dependency, and nothing stops it. `phases/clean.md` step 1 prunes `## DONE` tickets "older than the current active work" with no check for inbound `needs:`, while § 1.2 makes a `needs:` pointing at a ticket that exists nowhere on the board worse than a cycle -- the remedy is `## BLOCKED`. So the phase that exists to keep the board honest can mechanically block a workable ticket, and the block reads as a real dependency failure rather than as bookkeeping damage. Reproduced live at E-1811: pruning `## DONE` dropped T-421 and instantly made T-422 dangle. Fix in `clean.md`, not in the dangling check, which is behaving correctly. | review_passes: 1 | verify: PASS -- clean.md step 1 refuses to prune a `## DONE` ticket any live ticket still names in `needs:`, and says the remedy is refusing the prune rather than repairing the dangle; validate.py FAILs [clean-scrub-guard] when the MUST NOT softens; CONFORMANCE 208 Shipped in v7.175.0 (0aa2562).
- [x] T-439 [P2] The `cc` row promises a pivot its own destination forbids. The table routes `cc` to `saipen goal` and annotates it "Mapped by user request to trigger goal mode", but § 1.10 says bare `saipen goal` at `goal_mode: false` "is not a command at all" -- MUST NOT set `goal_mode`, MUST NOT touch the counters, replies exactly `Use: saipen goal <objective text>`. Only `cc <text>` can pivot. On the most-used key the Notes column states an outcome the row cannot produce, which is the same never-guess failure T-433 just closed one column over. | review_passes: 1 | verify: PASS -- the `cc` row states the pivot needs text and that bare `cc` is a resume or the usage line; validate.py FAILs [shortcut-notes] on the superseded promise, which caught my own explaining sentence for carrying the banned literal; CONFORMANCE 207 Shipped in v7.175.0 (0aa2562).
- [x] T-438 [P1] `saipen status` MUST report the last `tools/validate.py` result "if one is recorded in `LOG.md`", and no rule ever gives that record a findable shape. § 2.4 had the identical problem with its counters and solved it by fixing the exact text `DEC: goal_waves N->M` so § 1.5's rebuild could count them, enforced at CONFORMANCE 51. The conformance line got the duty without the shape, so a cold agent either greps prose and guesses or truthfully reports "none recorded" while the LOG holds the answer -- the observability layer's own report reading a record nothing is required to write. | review_passes: 1 | verify: PASS -- § 1.2 fixes `RUN: validate.py -> PASS|FAIL` and § 1.10's Conformance line cites it; validate.py WARNs [no-conformance-record] on the active log, and tripped on this repository's own 1800-plus events on its first run; CONFORMANCE 206 Shipped in v7.175.0 (0aa2562).
- [x] T-437 [P1] § 2.1's ZERO-PROMPT rule is a MUST whose exception list looks exhaustive and is not. It orders an unconditional `HUNT`, then "MUST immediately transition to `ADD`" if clean, and names exactly one exception (a session already at `phase: BLOCKED`) -- while § 1.3 bans `ADD` outright under `mode: read-only`, since its entire work product is tickets. Read-only reaches HUNT report-only and then hits a MUST it may not obey. Same error class § 1.3's own ban list and § 1.6's from-any-phase set were both fixed for: a list that reads complete while a live rule sits outside it. | review_passes: 1 | verify: PASS -- § 2.1 declares its exception list complete and names the read-only carve-out; validate.py FAILs [zero-prompt-exceptions] when either marker goes, and FAILs rather than skips if the § 2.1 anchor moves; CONFORMANCE 205 Shipped in v7.175.0 (0aa2562).
- [x] T-436 [P1] § 1.2 still orders legacy state to "MUST upgrade to v2 at the next checkpoint" while the current schema is 3 and § 1.5 orders writing 3. An agent obeying § 1.2 literally writes a schema_version the validator immediately WARNs as legacy, so the one instruction for escaping legacy state cannot escape it. Error class: a rule that hardcodes a version number the thing it describes outgrows. Fix by naming no number in § 1.2 and pinning the class: no shipped doc may state a `schema_version` literal other than the current one, which validate.py already derives from the schema. | review_passes: 1 | verify: PASS -- § 1.2 now names no schema number; validate.py FAILs [stale-schema-version] on any shipped doc or template naming a superseded one, and the first run found the shipped subSaipen TEMPLATE frozen at schema 1 (bumped to 3 with the voice marker); audit_checks controls both ways; CONFORMANCE 204 Shipped in v7.175.0 (0aa2562).
- [x] T-434 [P0] Two sections give opposite answers about the safety-valve budget. § 1.10's `saipen stop` paragraph says bare `saipen goal` "deliberately does NOT preserve the counters: per § 2.4 Entry it resets `goal_waves`/`goal_tickets` to `0`" -- and § 2.4 Entry says the reset happens ONLY when the counters are at or over the caps, citing E-1468 as the incident that made it conditional. § 1.10 cites the section it contradicts. An agent following § 1.10 hands a fresh 3-wave/20-ticket budget to anyone who types `cc` mid-run, which is the exact defect § 2.4 already fixed. CONFORMANCE 31 carries the same stale unconditional claim. § 2.4 owns the rule; § 1.10 and row 31 defer to it. | review_passes: 1 | verify: PASS -- validate.py FAILs [goal-counter-reset] when the conditional clause is gone or the old unconditional wording returns; hand red-test swapped it for "every time, whatever the counters read" and the check named itself, green again on restore; audit_checks 91/91 with the matching control; CONFORMANCE 31 corrected
- [x] T-421 [P0] Guides are their own surface in STYLE.md, not Artifacts: open by saying why the thing exists, for a reader who does not know the domain. Structural half is checked -- first paragraph after the title carries no command, path or fence. Five Core guides rewritten to it. | review_passes: 1 | verify: PASS -- audit_checks 81/81 with the control putting a command back into GUIDE_EE's opening; validator/ruff/scenarios/order/floor green
- [x] T-441 [P1] ee prepare: Core-owned README restructure to e073234 outline (E-1821). All 32 kitchen READMEs + 3 root mirrors carried the pre-restructure outline (### Project State > Model Memory, no Commands table, no Documentation table); README.ee.md had lost its language switcher at HEAD. Restructured ru/et/ded kitchens + README.ee.md/README.ded.md mirrors to new shape (How it works / Commands 16-row table / Documentation table / Built with SAIPEN / Screenshots details); switcher restored; palette "Vintage Golden" fixed in ded/et; shortcut callout lines kept byte-identical mirror==kitchen per validator. Remaining 29 locales + README.ja.md + palette (5 SPECs, 14 READMEs) + SPEC_DA tree ticketed SAIT-009 on sub board, OUTBOX ST-010 draft. | review_passes: 1 | verify: PASS -- tools/validate.py conformant (0 FAILs, 5 pre-existing warnings); shortcut-callout check aligned across 32 sources + 3 mirrors; badges v7.173.0 match VERSION
- [x] T-435 [P1] `PHASE <phase> [T-###]` has a ticket-ref rule nothing witnesses, and RFC's own example teaches the violation. § 1.2 makes the ref REQUIRED for exactly SCOUT/BUILD/VERIFY/REVIEW/SHIP and "omitted for every other phase", then § 2.2 tells the agent to translate ADD's RETURN into `PHASE PLAN T-###`. § 1.2 states in the same bullet that an example failing its own rule is worse than no example, because a weak model copies the example and never reaches the rule -- this is that, inside the constitution. validate.py checks the prefix, the vocabulary and the pick, never the phase/ref pairing. | review_passes: 1 | verify: PASS -- validate.py FAILs `PHASE PLAN T-1` and a bare `PHASE BUILD`, passes `PHASE BUILD T-1` and `PHASE HUNT`; red controls both directions in audit_checks; § 2.2's example corrected; audit_checks 95/95
- [x] T-433 [P1] A bare repeated-letter shortcut is a command, never a greeting. The ENTIRE-message rule existed to prove a key cannot be read as a prefix of prose, and a bare `qq` was still answered as a greeting instead of executed as `saipen prepare saiwiki` (E-1801) -- the property was true and the failure still happened, because nothing told a weak model what receiving one means. RFC § 1.10 now states the command rule: receiving any declared shortcut (Latin or Cyrillic twin), the agent MUST execute the exact row it resolves to, or that row's exact no-op, and MUST NOT answer with an acknowledgment, a question, or any other chat. A shortcut is work, and a greeting in reply is a dropped command. | review_passes: 1 | verify: PASS -- validate.py FAILs when the never-a-greeting or execute-the-exact-row marker is softened in RFC § 1.10 (audit_checks red case); CONFORMANCE 201; validator + audit_checks + ruff green

## BLOCKED
- [ ] T-442 [P0] GATE: do not begin SAIPEN Crew Mode while the sequential protocol still has known foundational defects. Unblock only when no open P0/P1 ticket owns an RFC contradiction, ambiguous state transition, unenforced MUST, source-of-truth conflict, ownership gap, recovery gap or validator blind spot; all canonical gates are green; and one ordinary sequential release completes without exposing another foundational defect. Crew Mode is a future objective, not permission to widen the current repair scope. | verify: the unblock decision names the closed defect classes, current green evidence and the exact sequential release used as the stability checkpoint; no Crew implementation exists before this ticket reaches DONE

- [ ] T-443 [P0] Define a markdown-first SAIPEN v8 Crew contract before implementing concurrency. Preserve ordinary files, Git history, cold-agent readability and operation without SAIPENVIEW, a database, a background service or a proprietary runtime. Specify which records are canonical, which may merge, which decisions must serialize, how agents identify themselves, how stale writers are rejected, how recovery works and how v7 sequential mode remains available. Treat every proposed mechanism as a hypothesis until it kills a reproduced failure class. | needs: T-442 | verify: every new mechanism maps to a reproduced failure; every v7 invariant is preserved, strengthened or explicitly retired with evidence; no rule depends on a GUI to be understood or executed

- [ ] T-444 [P1] Add a minimal concurrent work layer without replacing the current canonical `STATE.md`, `BOARD.md` and `LOG.md`. Agents work in isolated Git worktrees and produce durable proposals carrying proposal ID, agent, ticket, base commit, base state hash, intended logical resources, changed files, evidence and verification results. One existing Core still integrates accepted proposals sequentially. This stage must prove that useful work can run in parallel before canonical ownership becomes distributed. | needs: T-443 | verify: two agents complete independent tickets concurrently in separate worktrees; a stale proposal is refused rather than silently rebased; accepted proposals integrate one at a time; the ordinary v7 validator remains authoritative

- [ ] T-445 [P1] Define atomic ticket and logical-resource claims in file-backed state. A claim names ticket, agent, base revision, lease epoch and logical resources such as an RFC rule, Markdown section, Python function, schema key or release operation; file paths alone are insufficient. Expanding scope requires an atomic claim update. A conflicting agent waits, chooses different work or emits a proposal without editing the claimed resource. A superseded epoch permanently loses write authority. | needs: T-444 | verify: simultaneous claims produce exactly one owner; independent claims both succeed; scope expansion conflicts fail before editing; a stale or resurrected agent cannot checkpoint, integrate or ship under an old epoch

- [ ] T-446 [P1] Define markdown-visible waiting, dependency and deadlock behavior for concurrent agents. Every wait names the blocked ticket, required resource or dependency and current owner. Build a deterministic wait-for graph and victim rule so circular waits cannot persist. Waiting agents may perform compatible read-only work but may not silently widen their scope or edit another owner's resources. | needs: T-445 | verify: a reproduced two-agent resource cycle is detected and broken deterministically; the losing claim is preserved as a proposal or returned to the queue; no hidden overlapping edit occurs while either agent waits

- [ ] T-447 [P1] Give simultaneous `cc`, `ee`, `qq` and `ccc` calls one durable intent model rather than treating them as unrelated text messages. Commands from one user action share an intent ID and explicit dependencies. Multiple `cc` calls claim compatible independent work. `ee` and `qq` bind their output to an exact source commit. `ccc` waits for its required Core, translation and wiki work instead of running merely because it was received. Repeated delivery of the same intent must be idempotent. | needs: T-444, T-445 | verify: one grouped command set produces a deterministic dependency graph; two compatible Core tickets run concurrently; stale translation/wiki packages are refused; duplicate command delivery creates no duplicate ticket, package or release request

- [ ] T-448 [P1] Add a local file-first Crew monitor for progress and safe takeover of non-SHIP work. Runtime-only heartbeats, process observations and leases may live outside Git, but every recoverable checkpoint remains in normal SAIPEN files and commits. Distinguish heartbeat from actual progress and distinguish WORKING, BUSY, SUSPECT, STALLED, LIMITED, LOST and DONE without inventing a cause an adapter cannot observe. On takeover, freeze the abandoned worktree, preserve its diff, increment the epoch and continue from the last verified checkpoint or a reviewed proposal. No GUI dependency. | needs: T-445, T-446 | verify: a stalled agent is replaced without losing verified work; a known long-running process is not stolen; a resumed stale agent is denied writes; machine sleep does not trigger immediate false takeover; unknown failure is never falsely labelled as a usage limit

- [ ] T-449 [P0] Make concurrent `ccc` safe through exactly one release captain, one independent reviewer and optional reserves. Role acquisition is atomic and epoch-bound. Only the current captain may integrate, version, tag, push or update release state. Every external side effect carries a stable release ID and is checked against the repository and remote before retry, because the previous captain may have succeeded before disappearing. SHIP takeover uses a stricter recovery path than BUILD takeover. | needs: T-447, T-448 | verify: two simultaneous `ccc` calls elect exactly one captain; another agent becomes reviewer or reserve; loss before push transfers safely; loss after push is detected remotely and does not duplicate commit, tag, wiki push or release; the stale captain cannot resume shipping

- [ ] T-450 [P1] Decide whether canonical `STATE.md`, `BOARD.md` and `LOG.md` should remain directly authored files or become deterministic views over accepted proposals, ticket records and append-only events. Do not introduce a reducer merely because distributed systems traditionally enjoy having one. Implement generated views only if the earlier Crew stages reproduce merge failures that cannot be solved while preserving the current files. Migration must remain reversible and sequential operation must remain possible without the monitor. | needs: T-444, T-445, T-448, T-449 | verify: the decision cites real Crew runs; if a reducer is accepted, state rebuilds byte-stably, interrupted reduction cannot leave partial truth and rollback to the authored-file model is exercised; otherwise the ticket closes with evidence that authored Markdown remains sufficient

- [ ] T-451 [P0] GOAL FLAG: SAIPEN v8 Crew Mode is achieved only after the markdown-first system survives a defined real-world soak period without SAIPENVIEW. Run it on real project work long enough to include concurrent Core tasks, translation/wiki preparation, repeated releases, agent limits, process loss, stale proposals, resource waits, application or terminal restarts and at least one automatic takeover. Record every ownership transition, epoch, proposal, checkpoint and external side effect in inspectable files. Do not call the protocol v8 merely because one demonstration succeeds. | needs: T-443, T-444, T-445, T-446, T-447, T-448, T-449, T-450 | verify: before the soak begins, record a fixed minimum duration and workload target that cannot be reduced after failures; the completed run shows no lost work, duplicated release, split brain, unresolved deadlock or unreconstructable state; killing every active agent still leaves one correct next action recoverable from the repository alone

# Board
## DOING
## TODO






- [ ] T-559 [P1] Define `saipen improve clean` as archive-with-provenance and nothing else: it refuses while any finding is unswept or any disposition is missing, it preserves the original findings verbatim, and every archived report keeps the canonical ticket references that point back at it. Partial or timed-out test evidence can never mark an IMP fixed. | needs: T-556, T-557 | verify: red controls 23, 24 and 25 are mechanical -- clean with an unswept finding is refused naming the finding, an archived report still resolves its ticket provenance, and a timeout/partial run recorded as fix evidence FAILs
- [ ] T-560 [P1] Wire mechanical coverage for all 25 spec red controls into `tools/run_scenarios.py` probes and/or `tools/audit_checks.py` CASES and/or `tools/validate.py` checks, covering at minimum: reload-before-audit (1), previous-conclusions-not-trusted (2), partial-scope-cannot-claim-full (3), later-rule-not-violation (4), accidental-success (5), vague-finding-rejected (6), sub-cannot-modify-main (7), one-report-owner (8), append-preserved (9), no-ticket-before-verification (10), duplicates-one-ticket (11), invalid-no-ticket (12), already-fixed-no-ticket (13), confidence-never-overrides-Core (14), cross-project-recurrence-required (15), weak-model-test-required (16), verify-is-delta-only (17), verify-cannot-recurse (18), original-finding-preserved (19), ticket-links-to-IMP (20), improve-does-not-enter-ADD (21), report-is-not-BOARD-state (22), cleanup-refuses-unswept (23), archive-preserves-provenance (24), partial-evidence-cannot-close (25). | needs: T-553, T-554, T-555, T-556, T-557, T-558, T-559 | verify: every one of the 25 has a named check or probe that demonstrably goes red when its rule is removed; run_scenarios green, audit_checks full-suite green with zero "not evidence" lines
- [ ] T-561 [P1] Ownership consolidation + the full closure gate for this wave. One owner per concept: the command rows in CORE.md §1.10, the audit chain in saipen/IMPROVE.md, the convergence lifecycle in CONVERGE.md (referenced, never restated), the report/finding schema in saipen/IMPROVE.md, SubSaipen boundaries in PROTOCOL.md. Then run the full applicable suite and report real PASS with fresh evidence -- no skipped, timed-out, inspection-only or reused result: validate.py, audit checks, floor parity, scenario suite, phase/command parity, shortcut parity, state schema, forced HUNT, `git diff --check`, `git status --short`. | needs: T-560 | verify: no concept is defined in two documents and the drift check proves it; every gate reports PASS with evidence produced this wave; `git diff --check` clean; BOARD/STATE/LOG/CHANGELOG truthfully describe the new command
- [ ] T-473 [P1] HELD, not scheduled: Rosary A3, the concurrent whole-file clobber guard. Witnessed twice this session -- a parallel session overwrote `BOARD.md` and reverted six `## DONE` moves whose work was already committed and pushed (E-1863), caught an hour later by CONFORMANCE 199 rather than at the moment of damage. § 1.5 already reads each file before writing it, so the guard is cheap: compare the content against what was read immediately before the write and refuse on a difference. It is held deliberately because it touches the checkpoint path every session runs and because T-442..T-451 already own the v8 Crew concurrency design; landing a collision detector ahead of that design risks pre-empting it. | needs: T-442 | verify: a read-then-foreign-write-then-write sequence is refused; an unchanged file writes normally; the guard stays a detector and never becomes a scope or locking scheme

## DONE
- [x] T-558 [P1] Land the two reasoning gates as checked artifacts rather than advice: the META-IMPROVEMENT rule (a protocol-level fix MUST record cross-project recurrence reasoning; a local bug stays local) and the WEAK-MODEL PRECEDENT test (every protocol-level fix MUST record the answer to "could a weak but compliant model still choose wrong while honestly believing it followed SAIPEN?", strengthened in the fixed preference order: state field, transition rule, validator, red scenario, canonical example, prose last). ACCIDENTAL_SUCCESS is a first-class class: a correct result reached without the required verification is never recorded as PASS. | needs: T-556 | verify: validate.py FAILs a protocol-level improve ticket with no recurrence reasoning (red control 15) and none with no weak-model answer (16); a finding whose result was correct but whose verification never ran is classified ACCIDENTAL_SUCCESS, not PASS (red control 5); a fix answered only with prose where a state field or validator was available is flagged | owner: claude | claim_time: 2026-08-10T00:46:16Z
- [x] T-557 [P1] Enforce the writer boundary and the recursion stop the spec depends on but does not mechanize: during SELF-AUDIT/REPORT no seat may touch Core protocol, main source, canonical BOARD/LOG/STATE except the bookkeeping that registers the audit; a SubSaipen writes only inside its own home; `saipen improve verify` audits ONLY the delta produced by this cycle (fixes, new rules, new tests, final state) and MUST NOT re-enter a full improve cycle; `saipen improve` never silently enters ADD. | needs: T-552, T-556 | verify: red controls 7, 17, 18, 21 and 22 are mechanical -- an auditing sub that writes a main-project file FAILs, a verify pass that reopens unrelated history FAILs, a verify that starts a full cycle FAILs, an improve run whose `next_action` names ADD FAILs, and a report treated as canonical BOARD state FAILs | owner: claude | claim_time: 2026-08-10T00:37:35Z
- [x] T-556 [P1] Define the Core sweep as the only path from report to canonical work: deterministic report order, numeric IMP order inside a report, the closed disposition set (CONFIRMED \| DUPLICATE \| ALREADY_FIXED \| SUPERSEDED \| LATER_RULE \| NOT_REPRODUCED \| INVALID \| NEEDS_EXTERNAL_EVIDENCE), root-cause deduplication producing ONE ticket with all `source_reports:` references, and the required ticket fields (source_reports, reproduced, invariant, defect, impact, exact_fix_scope, red_control, done_condition). A report's own `confidence: proven` is evidence to inspect, never a ticket authorization. Core dispositions are written to the Core-owned ledger `.saipen/improve/<cycle_id>/SWEEP.md` (report_path, run_id, IMP-id, disposition, reproduced, canonical ticket, fixed_by, verification), NEVER into the seat report: a completed seat report is immutable and its hash is verified unchanged after sweep/fix/verify. Observation (seat report) and judgment (SWEEP.md) never share one writable file. | needs: T-555 | verify: validate.py FAILs a canonical ticket generated from an unverified finding (red control 10), FAILs a ticket with no resolvable `IMP-###` back-reference (red control 20), FAILs an INVALID or ALREADY_FIXED finding that produced a ticket (12, 13), and FAILs an edited original finding; three reports sharing one root cause produce one ticket in the scenario suite (red control 11) | owner: claude | claim_time: 2026-08-10T00:19:03Z
- [x] T-555 [P1] Make a finding mechanically checkable rather than prose: the required header block (agent, role, model_or_runtime, project, saipen_version, protocol_fingerprint, source_head, source_tree_fingerprint, context_scope, context_available, report_status) -- NO absolute machine-local path: report identity uses saipen_version + protocol_fingerprint, never the local protocol install path (machine-local, non-portable, unnecessary for reproduction), the `IMP-###` finding schema with its closed `severity`/`class`/`confidence`/`action` vocabularies, and the completion bar for `report_status: complete`. A finding with no observable expected/actual/evidence triple is rejected, not softened. `context_available: complete` is refused when the stated scope is partial. | needs: T-551 | verify: validate.py FAILs a report whose finding lacks expected/actual/evidence (red control 6), FAILs a class/severity outside the closed set, FAILs `report_status: complete` while the completion bar is unmet, and FAILs a full-context claim over a partial `context_scope` (red control 3); `NO_FINDINGS` with a stated scope passes | owner: claude | claim_time: 2026-08-10T00:10:02Z
- [x] T-554 [P1] Register the command family in CORE.md §1.10's closed list: `saipen improve`, `saipen improve status`, `saipen improve sweep`, `saipen improve verify`, `saipen improve clean` -- five exact routes, multi-word like the existing `saipen sub <verb>` precedent. No repeated-letter shortcut is assigned: a new key is a ~74-file callout rollout (T-493) and nothing in the request asks for one, so the key count in every locale README/guide/mirror MUST stay unchanged. `saipen improve clean` archives reports only and never enters the CLEAN phase -- the name collision is the trap and the row states the distinction. | needs: T-552 | verify: all five verbs resolve in §1.10 and in validate.py's command surface check; an undeclared `saipen improve <word>` is rejected rather than guessed; the shortcut key count is byte-unchanged across every callout file; `improve clean` cannot transition `phase` to `CLEAN` | owner: claude | claim_time: 2026-08-09T23:59:57Z
- [x] T-553 [P1] Keep improve routing OUT of `STATE.md` unless routing genuinely requires it -- reversing this ticket's original direction, which was to add flat `improve_cycle`/`improve_status`/`improve_reports_expected`/`improve_reports_complete` fields conditioned like the goal counters. Under T-552's meta-control design there is no global improve phase to route, and independent counters are exactly the drift risk: `reports_expected` and `reports_complete` MUST be DERIVED from the cycle manifest (T-570), never maintained as separate numbers that can disagree with the files they describe. If a field survives that test it lands flat with a closed enum, REQUIRED only while an improve cycle is live and cleared at completion, exactly the way `goal_waves`/`goal_tickets` are conditioned on `execution_intent: goal`. No finding is ever encoded into STATE. | needs: T-552, T-570 | verify: each proposed field is justified by a routing decision that cannot be answered by reading the manifest; the counters are derived, and a scenario proves a manifest edit changes them with no STATE write; validate.py FAILs findings text in STATE; 1.2's required-set sentence is extended in at most one place | owner: claude | claim_time: 2026-08-09T23:47:03Z
- [x] T-596 [P1] NITRO DOGFOOD IV P1: after T-602/T-601/T-600 prove their behavior, close the wave. (1) Update stale NITRO knowledge ONLY after behavior is correct: KNOWLEDGE/NITRO.md M9 wording (the renamed honest projection metric, never repeated-unchanged-bytes), document the fourth proof level GATE (UNIT / COMPOSITION / CANONICAL / GATE) and the invariant 'VALID END STATE != PROOF OF REQUIRED PROCESS'; write the claim-to-proof IV matrix classifying T-591/T-594/T-595 as direct/composition/canonical/gate with missing layers recorded as not-PROVEN and re-verified by current evidence. (2) Fresh full gates to ACTUAL completion (never reused historical evidence): validate.py, run_scenarios.py, audit_checks.py, floor/parity, phase/command parity, state schema, forced HUNT, git diff --check, git status --short. (3) Bounded explicit journal compaction as a maintenance op: compact only settled COMMITTED/RESOLVED ops whose staged bytes are no longer recovery-critical; keep the tombstone (op_id, operation, status, result identity, final hashes, timestamp, semantic payload hash); never compact PREPARED/APPLYING/CONFLICT or any unresolved evidence. (4) BOARD cleanup: archive DONE closed-ticket prose whose evidence already lives in LOG/tests/KNOWLEDGE/commits; keep active T-553..T-602 fully intact. (5) Measure slow test groups after correctness (wall-time follow-up only, no weakening controls). (6) SHIP/checkpoint/push the corrected NITRO wave per remote/staging policy only after every proof. | verify: NITRO.md + claim_to_proof_IV matrix updated with the GATE proof level and honest M9 metric wording; every fresh gate reports PASS with evidence produced this wave (no skipped/timed-out/reused historical); journal compaction wired as an explicit maintenance op with the tombstone contract and the never-compact classes; BOARD cleaned with active tickets intact; git diff --check clean; git status --short carries only intended files; the corrected wave is remotely checkpointed only after those proofs | needs: T-600,T-601,T-602 | owner: claude | claim_time: 2026-08-09T23:20:29Z
- [x] T-549 [P1] Document ownership consolidation + final gate suite | verify: full applicable suite PASS pre-SHIP (E-2443, sealed LOG-010; prose archived at CLEAN T-596)
- [x] T-600 [P1] NITRO DOGFOOD IV P1: context projection integrity. Reproduced: context_cold reports ~6865 bytes while the emitted surface is ~4028 -- metrics are computed BEFORE _bounded() truncates, so they describe what the model does NOT receive. _board_map() prints all 10 tickets then '... +2 more' (logically false). _bounded(body, 4000) globally chops mandatory trailing sections (full T-553 verify, ROUTING) because the BOARD surface consumes the budget first. Fix: compute metrics from the FINAL emitted surface (bytes == len(surface.encode('utf-8')), characters == len(surface), token estimate from that exact surface); separate pre_bound_bytes / truncation_bytes where useful, never label pre-truncation material as model-visible. STRUCTURAL budgeting: reserve mandatory sections first in priority order (recovery/conflict > computed next action > exact full active/next ticket > STATE essentials > required routed phase doc > exact needs/verify > bounded LOG tail > optional BOARD orientation); optional data shrinks first, never cut the instruction required to execute the task. Explicit protected '## NEXT TICKET' section carrying the complete canonical ticket (ID, priority, description, needs, verify, blocker) OUTSIDE orientation truncation; separate bounded '## BOARD MAP' for orientation (at most N entries then a truthful '... +K more'; the exact routed/active ticket emitted in full as the protected exception). Test the real current shape: 10+ TODO tickets, the actual top workable below the orientation limit, long description, nonempty needs, long verify, small budget -- routed action, exact ticket, needs, verify, routed phase doc, recovery state all present in full. Byte accounting on non-ASCII (Cyrillic/Japanese) content with exact equality, no approximate. | verify: result['bytes'] == len(result['surface'].encode('utf-8')) and result['characters'] == len(result['surface']) exactly, proven with Cyrillic + Japanese content; the cold surface with a small budget always contains in full: routed action, the exact next ticket (ID/priority/description/needs/verify/blocker), needs, verify, the routed phase doc, and recovery state; board map capped at N entries with a truthful '... +K more'; mandatory sections never globally truncated away; run_scenarios + validate.py + audit_checks green fresh | owner: claude | claim_time: 2026-08-09T23:08:05Z
- [x] T-601 [P1] NITRO DOGFOOD IV P1: Improve lifecycle/verifier composition. Reproduced: register cycle -> register seat -> report_status complete -> complete_cycle() COMMITTED -> write_sweep_entry() REFUSEs because the cycle is complete/immutable -- complete_cycle freezes the artifact before its Core sweep can finish, contradicting T-595's own stronger claim. Fix: complete_cycle requires, for every expected/available seat, a report whose report_status is complete AND every finding requiring disposition has a final SWEEP disposition for its EXACT composite finding identity (cycle + seat/report + run + IMP id); no missing disposition -> cycle_status complete. Real lifecycle test: active cycle -> reports complete -> partial sweep -> complete_cycle REFUSE -> remaining sweep dispositions -> verify where required -> complete_cycle COMMITTED -> every ordinary mutator REFUSEs -> next cycle admitted. Make the Improve semantic verifier TARGET-AWARE: verify_improve(root, operation_record) validates the ACTUAL changed target (target role manifest -> roster + cycle lifecycle + unique seats + path ownership; target role sweep -> the REAL SWEEP grammar: disposition enum, composite finding identity, required fields, parsed with the same parser its consumers use; target role report -> the report invariants available at that protocol stage). Malformed SWEEP.md with an otherwise valid MANIFEST must FAIL the semantic verifier. Original APPLY and Recovery must share the same target-aware verifier class (one policy implementation, never APPLY checked target-specific while RECOVERY ran a generic root scan). Audit resolve_conflict for writer-lock ownership: inspect stays read-only; resolve acquires the canonical project writer lock, re-reads the journal + other unresolved ops + live hashes under the lock, validates, settles exactly one conflict; add a two-process resolution race control proving exactly one canonical settlement. | verify: lifecycle end-to-end scenario green: partial sweep REFUSEs complete_cycle, full sweep coverage permits COMMITTED, post-complete mutators REFUSE, next cycle admitted; malformed SWEEP.md FAILs its semantic verifier while a valid SWEEP passes (red control both directions); writer and verifier consume the SAME grammar (the verifier parses with improve.py's parser, never a second ad-hoc one); APPLY and Recovery call the same target-aware verifier class; resolve_conflict acquires the project writer lock and re-checks journal status under the lock; the two-process race control yields exactly one RESOLVED and one refusal; run_scenarios + validate.py + audit_checks green fresh | owner: claude | claim_time: 2026-08-09T22:55:25Z
- [x] T-602 [P0] NITRO DOGFOOD IV P0: finish_ticket launders transition_from. Reproduced: finish from SCOUT/BUILD/VERIFY/REVIEW all leave phase DONE with transition_from SHIP even when the actual previous phase was not SHIP -- an illegal execution history becomes a syntactically legal final STATE. Class: PROTOCOL_VIOLATION / FALSE_EVIDENCE / ACCIDENTAL_SUCCESS. Fix: the ordinary ticket completion operation requires STATE.phase == SHIP AND STATE.task == ticket_id AND exactly one BOARD.DOING == ticket_id; from SCOUT/BUILD/VERIFY/REVIEW finish MUST REFUSE ILLEGAL_PHASE with zero canonical bytes written; transition_from MUST record the ACTUAL previous phase, never a fabricated legal-looking source (delete the closure_from = SHIP laundering); finish must NOT secretly perform skipped REVIEW/SHIP semantics. Invert the public claim->BUILD->ticket done false-PASS composition test; add refuse controls A (BUILD->done REFUSE), B (BUILD->VERIFY->done REFUSE), C (BUILD->VERIFY->REVIEW->done REFUSE), D (full chain -> SHIP -> done FINISHED + full validator PASS); a mutation removing the SHIP precondition must turn A/B/C red. Goal counter composition: goal_tickets must bump mechanically at VERIFY->REVIEW (owned by the transition), never via finish; add the safety-valve-mid-ticket composition control: VERIFY->REVIEW at cap leaves phase REVIEW, ticket DOING, goal_tickets at cap, next_action the exact safety-valve WAIT; after explicit reauthorization continue REVIEW -> SHIP -> FINISH legally. Add the smallest mechanical gate proof: a [gate-closure] validator check FAILing a finish event naming a non-SHIP actual phase after the gate-fix boundary (defense-in-depth; the engine refusal is the primary gate), and the GATE proof level documented alongside UNIT/COMPOSITION/CANONICAL. Record T-591/T-594/T-595 skipped-gate closures as ACCIDENTAL_SUCCESS-incomplete-proof with fresh re-verification of their actual diffs and relevant checks; never rewrite their historical DONE state. | verify: finish from SCOUT/BUILD/VERIFY/REVIEW each REFUSE ILLEGAL_PHASE with zero canonical bytes (project tree hash unchanged); finish from SHIP succeeds to DONE with transition_from == SHIP (the ACTUAL phase); public 'saipen ticket done' after claim->BUILD REFUSEs; composition controls A/B/C go red when the SHIP precondition is removed (mutation red-control) and stay green with it; control D full chain ends validator-green; goal_tickets bumps ONLY at VERIFY->REVIEW, never on finish; the valve-mid-ticket control keeps REVIEW/ticket DOING/goal_tickets cap/WAIT after the trip and completes legally after reauthorization; the [gate-closure] validator check is red-tested in both directions; run_scenarios + validate.py + audit_checks green fresh | owner: claude | claim_time: 2026-08-09T22:34:43Z
- [x] T-552 T-552 [P0] DECIDE, with evidence, whether `saipen improve` is a PHASE at all -- and prefer that it is not. The original ticket assumed a new IMPROVE phase in CORE.md 1.6's enum plus a transition table row. Improve is a meta-control operation like `status` and `stop`, not ordinary project execution, and four properties say so: several seats may audit independently and concurrently; reports are read-only evidence, never canonical state; invoking improve during BUILD or VERIFY must not orphan the DOING ticket; and one global `STATE.phase: IMPROVE` cannot represent several independent audit seats at once. Adding global improve routing fields to STATE turns retrospective evidence collection into project execution state, which is the category error. Preferred design: `saipen improve` checkpoints current work if necessary, does NOT change phase/task/next_action, reloads the current protocol, audits this seat's observable context and writes only this seat's report; `saipen improve status` is read-only and shows each registered report as missing / draft / complete / swept; `saipen improve sweep` is Core-only, reads reports in deterministic order, verifies findings, deduplicates root causes, creates normal T-### TODO tickets and never silently pre-empts an existing DOING ticket; `saipen improve verify` is a bounded delta audit with no recursion; `saipen improve clean` archives report evidence and never enters the CLEAN phase. Normal Core phases are unchanged. A real IMPROVE phase may still be proposed, but only by first proving a failure the meta-control design cannot solve -- a command having a name is not a reason to add phase and state machinery. | verify: PASS -- meta-control decision recorded at E-2432 in saipen/IMPROVE.md: no IMPROVE phase row, phase count stays 16, no phases/improve.md (validator [improve-meta-control] checks + red control prove each), saipen improve never changes phase/task/next_action to audit, verify is delta-only without recursion. E-2432
- [x] T-570 T-570 [P1] Give an improve cycle a real seat roster, because "every agent reports" cannot otherwise be closed mechanically. `STATE.agent` stores only the LATEST actor, so historical participants cannot be inferred from it; LOG's `[agent:]` is a MAY field, so they cannot be inferred from there either. At cycle creation Core records the seats EXPECTED FOR THIS CYCLE in `.saipen/improve/<cycle_id>/MANIFEST.md`, which owns stable routing/identity and nothing else: `cycle_id`, `created_at`, project identity, `seat_id`, `role`, `report_path`, `availability` (expected or unavailable). The roster is NOT a second report: it carries no draft/complete/swept status -- the visible status is DERIVED (expected from roster-only; draft from `report_status: draft`; complete from `report_status: complete` with no Core disposition; swept from the sweep ledger's final disposition coverage; unavailable from the roster). One fact, one owner. Core is the SOLE manifest writer and seats never edit each other's report. A historical agent whose private or live context cannot be reached is `availability: unavailable`, never silently complete; Core MAY audit that agent's observable artifacts itself, and MUST label the scope as observable output only. | verify: PASS -- roster owns stable routing/identity only (cycle_id, created_at, project identity, seat_id, role, report_path, availability); register_cycle refuses a duplicate active cycle atomically, register_seat fails duplicate seat_ids and availability outside the closed set; derived status from roster + report + sweep; manifest-status red control goes red. E-2436
- [x] T-551 [P0] Fix report ownership AND per-seat isolation in the `saipen improve` specification before implementing anything on top of it. The spec writes each seat's report to `<saipen_home>/saipen_improve_<PROJECTNAME>.md`, but `saipen_home` is the SHARED protocol install (CORE.md 1.1, resolved in BOOT.md step 2), not project memory: two projects audited from one install collide on one path, the report never enters the project's own `.saipen/` history, and `saipen improve clean` would archive files inside the protocol tree. Canonical path, Core: `<project_root>/.saipen/improve/<cycle_id>/<seat_id>/saipen_improve_<PROJECTNAME>.md`. SubSaipen: `<sub_home>/improve/<cycle_id>/saipen_improve_<PROJECTNAME>.md`. The requested basename is preserved exactly; what changes is everything above it. `cycle_id` is ONE improve invocation or campaign; `seat_id` is ONE concrete auditing seat or session and NOT merely a model family -- `opencode-01`, `claude-01`, `codex-01`, `saiui`, `saihunt`. Two sessions both running `agent: opencode` MUST NOT resolve to one report unless they are deliberately the same registered seat, which is the collision the model-family reading guarantees. A second run by one seat APPENDS an immutable RUN section; an earlier RUN is never overwritten. Nothing is ever written under `saipen_home`. | verify: PASS -- report path defined once in saipen/IMPROVE.md + tools/improve.py.resolve_report_path, deriving from project_root (never saipen_home); validator + red control FAIL a report under saipen_home and FAIL two distinct seats resolving to one path; seat_key is one concrete seat, not a model family; append_run appends immutable RUN sections and a complete report refuses further RUNs (22 improve probes). E-2436
## BLOCKED
- [ ] T-576 [P3] Prune or confirm .saipen/recovery/_ee_apply.py (25892 B) and .saipen/recovery/_ee_measure.py (839 B) -- untracked, zero references, orphaned EE scratch found by CLEAN (E-2430); they sit in the recovery evidence directory without belonging to any recovery snapshot and are not regenerable by a named command. Delete only after a human confirms they are abandoned; keep the two timestamped STATE snapshots beside them regardless. | verify: a human decision recorded (delete both, or keep with a reason); the two STATE recovery snapshots are never touched | owner: claude | claim_time: 2026-08-08T21:30:00Z | blocker: WAIT_USER_CONFIRMATION -- the two untracked orphaned scripts cannot be deleted without a human decision (clean.md section 2); no user confirmation exists in current evidence, so they stay untouched
- [ ] T-464 [P2] Own the `board-soft-cap` warning and prune the board, on CURRENT evidence. The ticket's original text said `## DONE` was not the cause and that no work on the ticket could clear the condition; both statements are now false, so the PERMANENT WARNING OWNER framing goes with them -- an owner whose own premise expired is a ticket that teaches the next reader to leave the warning alone forever. Measured at v7.215.0: BOARD.md is 84 KB against a ~16 KB soft cap, of which the validator attributes 62 KB to closed-ticket text. CLEAN owns this: archive or prune closed `## DONE` ticket bodies whose evidence already lives in LOG.md and CHANGELOG.md, keeping only the compact retention window Core requires; move the gated Crew design backlog (T-442..T-451) to KNOWLEDGE with ONE gate ticket left on the active board; remeasure. | verify: PASS -- board pruned 93.3 KB -> 25.1 KB; 62 DONE bodies pruned (T-399..T-561) whose evidence resolves to LOG.md/CHANGELOG.md; Crew backlog T-443..T-451 moved to .saipen/KNOWLEDGE/crew-v8-backlog.md with T-442 the one gate ticket left; T-473 needs repointed to T-442. Measured remainder 25.1 KB: improve-wave bodies T-551..T-561 12.1 KB (live work, unprunable), other TODO 6.8 KB, needs-referenced DONE T-549/T-550 3.7 KB, BLOCKED 2.5 KB. The 16 KB cap is unreachable while the improve wave is on the board | owner: claude | claim_time: 2026-08-08T18:30:00Z | blocker: PERMANENT WARNING OWNER -- board-soft-cap emits while the board is above ~16 KB and the improve-wave ticket bodies are live work that cannot be pruned; this ticket owns the warning with the measured remainder and closes only when the warning stops emitting
- [ ] T-407 [P2] Own the `goal-reauth-untripped` warning: E-1659 logged a `DEC: goal reauthorized` with counters under the § 2.4 caps, and LOG is immutable by append-only, so the warning emits forever. Keep this ticket live while the warning emits. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401) | blocker: PERMANENT WARNING OWNER -- the condition can never be met and immutable history is why. E-1659 logged a `DEC: goal reauthorized` under the caps, `LOG.md` is append-only, so the warning emits forever and no work closes it. Same trap as T-406: in `## TODO` the Pick Rule was obliged to select a ticket whose own `verify:` says closing it FAILs
- [ ] T-406 [P2] Own the `subsaipen-never-ran` warning for the life of the saipython sub: saipython was spawned on migration but never run, and MANIFEST.md advertises it live until its board opens, so the warning emits on every validation. Keep this ticket live while the warning emits; close only when the sub runs or is de-advertised. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401) | blocker: PERMANENT WARNING OWNER -- the condition can never be met and immutable history is why. `MANIFEST.md` advertises saipython live until its board opens, so the warning emits every validation; this ticket exists to own it, and its own `verify:` says closing it while the warning emits FAILs the ownership check. Sitting in `## TODO` it was a ticket the Pick Rule MUST take and no agent can finish. It leaves `## BLOCKED` when saipython actually runs or is de-advertised -- a real event, not work anyone can pick
- [ ] T-442 [P0] GATE: do not begin SAIPEN v8 Concurrent Mode while the sequential protocol still has known foundational defects. Unblock only when no open P0/P1 ticket owns an RFC contradiction, ambiguous state transition, unenforced MUST, source-of-truth conflict, ownership gap, recovery gap or validator blind spot; all canonical gates are green; and one ordinary sequential release completes without exposing another foundational defect. The detailed Crew design backlog (T-443..T-451) lives in KNOWLEDGE/crew-v8-backlog.md (moved there by T-464). | verify: the unblock decision names the closed defect classes, current green evidence and the exact sequential release used as the stability checkpoint; no Crew implementation exists before this ticket reaches DONE | blocker: HELD FOR SEQUENTIAL STABILITY
- [ ] T-575 [P0] GATE: V9-M1 resident-continue implementation may start ONLY after ACTUAL v8 Concurrent Mode exists and is stable, not merely after the v8 gate is allowed. Required v9 prerequisites: T-442 gate completed; T-443..T-450 implemented or decided; T-451 real-world Concurrent Mode soak completed; v8 released; at least one post-release v8 stability checkpoint green; current Improve cycle clean; HUNT/CLEAN baseline clean. Research, source mapping, isolated-kitchen prototypes, architecture documents and red controls remain allowed earlier (full design in .saipen/KNOWLEDGE/V9_RUNTIME.md; upstream a18809e MIT, provenance in saipen/runtime/UPSTREAM.json and THIRD_PARTY_NOTICES.md). No v9 runtime implementation before actual v8 completion. | verify: the unblock decision names each prerequisite with fresh evidence including the completed v8 soak and a green post-release v8 stability checkpoint; every copied/adapted unit is recorded in saipen/runtime/UPSTREAM.json and THIRD_PARTY_NOTICES.md; the 30 red controls in V9_RUNTIME.md are listed before any V9-M1 implementation | blocker: HELD UNTIL ACTUAL V8 EXISTS AND IS STABLE

# Board
## DOING

## TODO
- [ ] T-400 [P1] Repair semantic ID drift in wiki `Scenarios.md`: rows 117-168 are not reliable mirrors of canonical CONFORMANCE IDs (e.g. wiki 118 says repository language while canonical 118 is use-before-define), despite W-027 reporting 180/180 and zero drift. Add a mechanical parity guard so row-count equality cannot masquerade as semantic equality again. | verify: every wiki scenario ID 1-182 maps to its canonical invariant; deliberate title/ID mutation fails; wiki and main-repo guards PASS
- [ ] T-392 [P1] Produce a large implementation-ready reorganization plan that preserves SAIPEN semantics while reducing load, duplication, and model-strength sensitivity across all supported agents. | verify: plan maps invariants, failure modes, target architecture, staged migration, compatibility gates, rollback, and measurable acceptance criteria into ordered tickets
- [ ] T-413 [P2] Sweep `tools/validate.py` for the rest of the install-layout blind spot T-410 hit. `home_doc()` now resolves shipped docs in both the repository (`saipen/<name>`) and the flattened injected layout, but other checks still resolve paths one way only: running the INSTALLED validator against this repository raises `root-device-ignore`, which the repository copy passes, so the two disagree about the same tree. Enumerate every `_tools_parent`-relative path in the file and decide, per check, whether a missing file means SKIP or FAIL -- the 13h checks chose SKIP and were vacuous in every install for as long as they existed. | verify: installed and repository validators agree on this repository; a red control breaks resolution rather than wording
- [ ] T-399 [P2] Make `tools/audit_parity.py` bounded and observable on Windows: a clean 54-case run takes 544.6 s and emits no progress, so ordinary 2-6 minute harness timeouts orphan the child and manufacture silent rc124/rc1 failures. | verify: progress identifies the current case, timed-out parents leave no child, clean runtime is materially lower or a documented supported timeout covers measured worst case, parity result remains 11/54 baseline
- [ ] T-406 [P2] Own the `subsaipen-never-ran` warning for the life of the saipython sub: saipython was spawned on migration but never run, and MANIFEST.md advertises it live until its board opens, so the warning emits on every validation. Keep this ticket live while the warning emits; close only when the sub runs or is de-advertised. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401)
- [ ] T-407 [P2] Own the `goal-reauth-untripped` warning: E-1659 logged a `DEC: goal reauthorized` with counters under the § 2.4 caps, and LOG is immutable by append-only, so the warning emits forever. Keep this ticket live while the warning emits. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401)

## DONE
- [x] T-411 [P1] Swept CONFORMANCE's enforcement column: 70 of 188 rows name no tool, and two of them had mechanical residue nobody read. RFC 1.11's Pick Rule now FAILs a `## DOING` ticket whose `needs:` are not all `## DONE`, and `phases/hunt.md`'s skip condition now FAILs a `hunt -> clean @HASH` naming a commit git cannot resolve. The other 26 rows say why they cannot be checked, one line each; four dropped to partly-mechanical. | review_passes: 1 | verify: PASS -- audit_checks 76/76 with the Pick Rule control mutating board data; run_scenarios red-tests the hunt mark against a real repository because audit_checks snapshots without `.git`; validator/ruff/order/tags/floor/scenarios green
- [x] T-410 [P0] STYLE.md declares a boot marker derived from its own text; `schema_version: 3` STATE MUST carry it, exactly as v2 STATE carries `last_event`. Absent at current schema FAILs, wrong value FAILs, lower revisions stay readable legacy. Token appears in no other shipped doc, so copying it is opening the file. Does not prove the read -- nothing can -- it removes the silence. Two pre-existing checks were wrong and are fixed: the fast-path test scanned the whole region instead of step 1, and shipped docs resolved only at `saipen/<name>`, so the whole cross-doc contract check SKIPped itself in every injected install. | review_passes: 1 | verify: PASS -- scenario walks legacy state to current schema and FAILs between two states differing by this one line; audit_checks 75/75 with 5 new controls (STYLE contract edit, marker dropped, foreign token, live value leaked into BOOT.md); parity 11/75; validator/ruff/order/tags/floor green
- [x] T-409 [P1] `dd <text>` is now a written contract: the user's items, reworded for a cold agent, at the front of `## TODO`; bare `dd` stays the proposal. Both RFC 1.10 and plan.md carry it and the validator checks the placement half. | review_passes: 1 | verify: PASS -- red-test softens the phrase in plan.md and FAILs [plan-forms]; canonical 70/70; scenarios EXIT=0
- [x] T-408 [P1] Golden was already the default and already guarded -- UI.md:5 admits no second palette, UI.md:91 makes the values the reference, validator 13c holds the name across all documents. Closed on evidence, no edit invented. | verify: PASS -- UI.md:5/:91 read; 13c present and covering locale copies
- [x] T-412 [P0] `saipen_home` parses as a path again, and the escaping rule is checked without a parser -- the validator reads a YAML subset and was structurally blind to the corruption it shipped for three releases. | review_passes: 1 | verify: PASS -- red-test un-doubles the backslashes and FAILs; canonical 69/69; scenarios EXIT=0; ruff clean
- [x] T-401 [P1] Enforce warning ownership from release history: extend `tools/release_ledger_baseline.json` with each WARN slug's first/last seen release and explicit rationale; when a category survives the configured N consecutive releases, `tools/validate.py` MUST FAIL unless a live BOARD ticket names that exact slug. Calibrate against `board-soft-cap`, `log-soft-cap`, `subsaipen-never-ran`, and `goal-reauth-untripped`; no prose-only exemption and no duplicate tickets. Prototype checkpoint E-1632: behavioral red/green design reached audit_checks 60/60, then all T-401 source edits were reverted after the user's stop; implementation remains TODO. | review_passes: 1 | verify: PASS -- baseline mutation ages an unowned slug to N+1 releases and FAILs; identical aged slug with live naming ticket passes; red control mutates baseline data, never validator wording; validator/audit_checks 68/68/parity 11/68/floors/ruff/order/tags/scenarios green
- [x] T-405 [P0] The STYLE.md read must be in the numbered fast path -- the execution order a cold agent walks -- with a self-locating reference, not a trailing bullet below it. BOOT.md step 1 now orders the read before any output (file in the same folder as this BOOT.md); RFC § 1.1 pins the placement; SKILL.md reads STYLE before RFC; validator 13h checks the fast-path region; three new red controls. | review_passes: 1 | verify: PASS -- validator; audit_checks 65/65; parity 11/65; scenarios; ruff; order; tags
- [x] T-404 [P0] `saipen/BOOT.md` told STYLE.md two mandates eight lines apart (on-demand rule questions vs before any output); a live DeepSeek session obeyed the cheap one and never opened STYLE.md. The operative `caveman-дед` contract now lives inline in the cold-start kernel; STYLE.md is a boot-read, never an escalation. | review_passes: 1 | verify: PASS -- BOOT.md carries the contract inline with no lazy listing; validator FAILs on before-output intersect on-demand overlap and on adapter lazy-deferral; audit_checks 62/62
- [x] T-403 [P1] Verified removal of the stale untracked root `saipen.wiki/` clone without touching the live saiwiki source: 340d4be has the claimed subject and is exactly seven commits behind; live clone ref remains f1a7e7a with only our T-395 page edits dirty. | review_passes: 1 | verify: PASS -- root path absent/untracked; live HEAD=origin/master=f1a7e7a; old lag=7; validator doc coverage 207 with no gap
- [x] T-402 [P1] Adopted the user's README information architecture: seven real h2 sections, 14 direct command rows plus init/sub footnote = all 16 verbs, collapsed 33-guide list, opening shortcut guard, and package-key entry preserved. | review_passes: 1 | needs: T-395 | verify: PASS -- exact h2/row/verb/details counts; validator; diff-check
- [x] T-395 [P0] Repeated shortcuts now encode complete package workflows: `ee`/`qq` prepare isolated full translation/wiki handoffs; `eee`/`qqq` collect only fresh ready payloads through gates and push; exact routes, no-global-length semantics, package fields, README/guide entry text, Cyrillic twins, and wiki guidance are locked. | review_passes: 1 | verify: PASS -- validator; shell+PowerShell floor; audit_floor 27x2; audit_checks 60/60; parity 11/60; scenarios; ruff; audit_order; audit_tags

## BLOCKED

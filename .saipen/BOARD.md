## DOING
## TODO
- [ ] T-1292 [P2] The validator check T-1291 added has no permanent red control, so the repository law that every validator check must go red on its own condition is unproven for it. tools/audit_checks.py CASES is a hand-maintained list and its closing sweep line reads 230 of 230 both before the check landed (T-1288 VERIFY, E-5736) and after it landed (T-1291 VERIFY today), so nothing went red when a new check arrived uncovered. Nothing binds the validator check set to that list: grep for audit_checks inside tools/validate.py finds three comments and no coverage assertion. The check itself is real, proven live during T-1291 VERIFY by driving tools/validate.py red twice, once on a non-canonical card slug and once on kind 'notakind', with the probe removed and the gate returning green afterwards. That proof is a transcript, not a gate, so the next agent inherits a check whose control nobody re-runs. Same CONTROL_DISARMED class already tracked by T-1276 and T-1268 | verify: AC-01 the KNOWLEDGE structured-surface validator check has an audit_checks CASE and the sweep total grows to match; AC-02 a red control proves that CASE goes red on its own condition and returns green when the mutation is restored; AC-03 the class is either closed or named: a gate binds the validator emitted check set to the audit_checks CASE list so a new uncovered check is detected, or the absence of that binding is recorded as a deliberate limitation where the rule lives; AC-04 audit_checks reports its new total and no previously covered check loses its control; unittest, audit_checks and ruff PASS
- [ ] T-1287 [P2] audit_checks --changed crashes when the changed-path set selects zero controls, so the accelerator T-1273 added reports a traceback instead of an empty scoped run. audit_checks.py:4115 computes worker_count = min(8, max(1, os.cpu_count() or 1), len(runnable)) and :4150 hands it to ThreadPoolExecutor, which raises ValueError max_workers must be greater than 0 the moment runnable is empty. Reproduced verifying T-1285: --changed naming tools/saipen_engine/fast_check.py, tools/saipen_engine/log.py, tools/test_ledger_gap.py and tools/test_verification_grammar.py selected zero of 230 controls -- no CASE declares an engine module as its target -- printed the two SCOPED limitation notes and every always-on probe, then died with a stack trace and a nonzero exit. Zero selected controls is the ORDINARY answer for a change no control targets, not a fault, and the crash is indistinguishable from a genuine harness failure to any wrapper reading the exit code, which is exactly the accelerator being unusable on the changes it was built for | verify: AC-01 a --changed set selecting zero controls exits successfully and says plainly that no control declares any changed path as its target; AC-02 the full-width sweep is unaffected and still reports its total; AC-03 a scoped run selecting at least one control still runs and reports exactly those; AC-04 a red control proves the empty-selection path goes red if it starts reporting a total or swallows a real harness error; unittest and ruff PASS
- [ ] T-1286 [P2] The crew-launcher probe in run_scenarios is racy and its failure is indistinguishable from a real launcher regression. The check sets SAIPEN_CREW_LAUNCH_GRACE to 0.05, runs bootstrap/saipen_crew.sh with three shimmed terminals on PATH, and then asserts that the shared probe log holds exactly nine lines -- three roles times three launcher fallbacks. The lines are written by DETACHED processes, so the assertion races their flush. Observed in one session: the same tree passed this check at 03:20 and failed it at 05:07 with got 3, while the only intervening edit was a revert of tools/saipen_engine/fast_check.py, which a shell launcher cannot read. The script itself is unmodified and its loop cannot exit early: launch tries every available launcher per role and the case statement continues on failure, so a partial count can only mean the harness read the log before the writes landed. A flaky check in the hostile suite is worse than a missing one, because a real launcher regression and a slow flush produce the identical message | verify: AC-01 the probe waits for the expected write count with a bounded deadline rather than reading once after a fixed grace, or the shim writes synchronously through a channel the parent can join; AC-02 a genuine launcher regression -- a shim that never runs, or one that runs for fewer roles -- still fails, proven by a red control; AC-03 the failure message distinguishes timed out waiting for N of 9 from observed only N of 9 after the process exited; AC-04 the check is run repeatedly in one session and reports the same verdict every time; unittest and ruff PASS
- [ ] T-1284 [P2] The producer-gate regression proves a small severity truth table by launching the whole validator thirteen times. run_scenarios.run_producer_gate_probes states at its own head that the subject is the mapping from GATE to producer severity, then its local validate helper starts tools/validate.py in a fresh Python subprocess for each case: measured, 13 subprocesses for 14 assertions, 10.9s of a 34.3s performance-wave run, roughly a third of the wall time for a policy matrix. Each process repays interpreter startup, module import, validator initialisation and unrelated project checks. The audit prescribes exposing the gate and producer severity decision as one side-effect-free policy helper with a closed input contract, testing the full matrix in-process against it, and keeping a small set of end-to-end sentinels that prove validate.py actually wires the policy | verify: AC-01 the gate times producer times finding matrix is exercised in-process against a side-effect-free helper with a closed input contract; AC-02 the surviving end-to-end sentinels cover default soft behaviour, one producer-specific collect hard case, the converge hard case and an unknown or misspelled gate refusal; AC-03 coverage of unknown and misspelled gates, malformed OUTBOX parsing, stale producer identity and producer-specific collect routing is provably not weakened, named case by case against the current suite; AC-04 subprocess tests remain only where process or CLI behaviour is itself the subject; AC-05 a red control proves the in-process matrix goes red on a wrong severity mapping; unittest and ruff PASS
- [ ] T-1283 [P2] Receipt storage is O(N) in lifetime volume on the read path and O(N squared) cumulatively on the append path, and the project has already crossed into multi-second routine commands. Measured live: 2592 settled receipt directories with a settled index holding 1567 entries and therefore stale, semantic_receipts_for_operation_safe returning 2 matches in 5.75s at 43.5 MiB, sub_list at 6.35s, and one latest_receipt core call invoking the per-receipt content hash 1052 times over 1052 conformance receipts. Two distinct causes were separated during the SRC-018 audit work. First, the settled index cannot self-heal, and the reason is deeper than the branch the audit named: instrumented, the settlement-time bootstrap RUNS and succeeds and the index it writes still fails validation immediately, because the digest is taken before the operation record is final, so the repair belongs in the journal commit sequencing rather than in the branch. Second, the conformance lineage validates a direct locator by hashing the entire historical receipt population on every lookup and re-hashes the full membership on every append. The audit prescribes bounded immutable sealed segments with a small append-active tail for both stores | verify: AC-01 a settlement leaves a settled index that validates on the next read, proven by a test that goes red against the current sequencing; AC-02 an operation-specific query consults the index or segment offsets and does not construct a full semantic snapshot, proven by counting decodes rather than by timing; AC-03 latest_receipt validates its locator against a compact lineage generation and the per-receipt content-hash count stops scaling with the population, proven by the PERF-004 measurement control already in place; AC-04 an append inside the active segment does not re-hash sealed segments; AC-05 exact-byte receipt evidence stays authoritative and a deep leaf-by-leaf audit remains reachable from the explicit validation path; AC-06 red controls prove each new bound can go red; unittest, audit_checks and ruff PASS
- [ ] T-1282 [P2] The mechanical-provenance tag is forgeable by exactly the path it exists to detect. validate.py:3070 checks only that a structural SAIOPS-owned DEC or RUN event CONTAINS an op marker, and its own diagnostic at :3077 states the purpose as so a manual structural edit cannot be distinguished from a mechanized one. Nothing resolves the id against a real journaled operation: log.py:20 parses the bracket, log.py:537 renders it, and no reader compares it to the operation records under .saipen/recovery/ops. Verified here by inspection: grep finds no site that cross-checks the id. An agent closing a ticket outside a SAIOPS run must hand-write the VERIFY boundary log.py:578 calls an EXACT machine-owned marker, is then required to supply an op id it has no mechanized way to obtain, hand-mints one, and the check passes. Reported with a disclosure by a second agent on another project whose LOG carries five such hand-minted ids that validated green. So the tag currently proves presence of a bracket, never mechanization | verify: AC-01 an op id that names no operation record is detected and reported distinctly from a missing tag; AC-02 a genuinely mechanized event still validates with no new work for the mechanized path; AC-03 either a non-SAIOPS closure route mints a real op id or the absence of one is a named legal state rather than a hole an agent fills by typing, and the chosen answer is stated where the rule lives; AC-04 historical events predating the fix are handled by the existing self-establishing boundary rather than by rewriting append-only history; AC-05 a red control proves the check goes red on a hand-typed id and green on a minted one; unittest and ruff PASS
- [ ] T-1278 [P1] A published closure commit asserts a terminal state its own bytes contradict. Observed live shipping v7.250.0: commit 058ab732 carries the message closure v7.250.0 ticket T-1276 DONE and was pushed to origin/main, while the BOARD.md inside that same commit still reads a DOING marker for T-1276 and its STATE.md still reads phase SHIP. Stages CONTENT_COMMIT_CREATED, CONTENT_PUBLISHED and CLOSURE_PREPARED all succeeded and only the atomic finish refused, so the closure commit had already been written and published with the pre-finish bytes. A reader of the published history -- or a fresh clone, which is exactly what --gate crew derives its verdict from -- sees DONE in the subject and DOING in the content. This is the false-completion class the protocol exists to prevent, produced by the release path itself | verify: AC-01 a closure commit whose message names a ticket DONE cannot be created while that ticket is non-terminal in the BOARD bytes of the same commit; AC-02 when the finish cannot be planned the closure commit is not created at all rather than created and left contradicting itself; AC-03 a red control proves the check goes red on a hand-built commit carrying a DONE subject over a DOING board; AC-04 the existing published history is not rewritten and the divergence at 058ab732 is recorded rather than hidden; unittest, audit_checks and ruff PASS
- [ ] T-1277 [P2] saipen source req certifies an invalid requirement id under --dry-run. Reproduced: 'python tools/saipen.py source req SRC-017 R-001 invariant test --dry-run' returns code DRY_RUN_PLAN, while the identical arguments without --dry-run return REFUSE INVALID_ID because add_requirement requires the rid to match R<digits> or <SRC>:R<digits>. intake.py's own CORE-002 comment states the old SOURCE_DRY_RUN early-return 'certified invalid input as successful, which is removed here' -- for the rid grammar it was not removed, so _source_dry_run_plan validates less than the mutator it claims to plan. A dry-run that says yes where apply says no is worse than no dry-run, because it is consulted precisely by an agent trying not to break something | verify: AC-01 every refusal class the apply path can reach for source req and source disp is reachable from --dry-run with the same code; AC-02 an invalid rid, an unknown clause class and an empty text each refuse identically with and without --dry-run; AC-03 a valid request still returns a plan with concrete target paths and zero writes, proven by comparing canonical bytes before and after; AC-04 a red control proves the parity test goes red if the dry-run path stops consuming a validator the apply path uses; unittest and ruff PASS
## DONE
- [x] T-1291 [P1] Execute external audit inbox layer audit/7.md (SRC-020); source_receipt=SRC-020 | verify: every actionable clause of SRC-020 is terminal with evidence; linked Work DONE; source closure succeeds; audit/7.md consumed by the journaled audit inbox cleanup; source_receipt=SRC-020 | source_receipts: SRC-020 | owner: codex-astra | claim_time: 2026-09-05T10:22:28Z
- [x] T-1288 [P1] Execute external audit inbox layer audit/6.md (SRC-019); source_receipt=SRC-019 | verify: every actionable clause of SRC-019 is terminal with evidence; linked Work DONE; source closure succeeds; audit/6.md consumed by the journaled audit inbox cleanup; source_receipt=SRC-019 | source_receipts: SRC-019 | owner: workbuddy | claim_time: 2026-09-04T23:21:52Z
- [x] T-1281 [P1] The closure classifier reads ordinary English as a failure verdict, so a PASS event that DESCRIBES what it repaired vetoes its own closure. log.py:557 _FAIL_TOKEN_RE matches the token anywhere in the free-text body and log.py:556 _ZERO_FAIL_RE only offsets a zero-count immediately before it, so _claims_failure at :560 -- consumed by verification_evidence at :648 under the negative-evidence-wins rule at :658 -- vetoes on narrative. Reproduced here twice, independently. Mine: shipping v7.250.0 the REVIEW evidence line carrying the pre-fix FAIL is re-established was rejected and the release stopped after CONTENT_PUBLISHED with the tag unpushed; rewording it green. A second agent on a different project hit the identical shape with a failed atomic write leaves no orphan, rewrote it to an atomic write that cannot replace, and the same tree at the same commit with the same measurements went green. Guessing toward failure is deliberate and documented as T-1241 and is not the defect; the scope is. log.py:598 already names this exact class as the repositorys most expensive recurring defect, a validator searching free text for a magic phrase so any line that merely DISCUSSES the phrase acquires its power -- this is that defect at the opposite polarity, and the marker rule was never applied to the verdict grammar | verify: AC-01 a PASS event whose narrative merely mentions a failure token in prose does not veto closure, proven by the two exact strings recorded on this ticket; AC-02 a real failure claim is still negative evidence and still vetoes, proven by the verdict-shaped forms FAIL colon and N FAIL and FAILED colon; AC-03 the zero-count exemption keeps working and is not widened into a general escape; AC-04 the grammar is anchored the way structural_marker_events anchors its markers rather than searching the whole body, and the anchoring is stated where the rule lives; AC-05 a red control proves the classifier still goes red on its own condition and that no constant verdict satisfies the suite; unittest, audit_checks and ruff PASS | owner: workbuddy | claim_time: 2026-09-04T11:18:15Z
- [x] T-1285 [P2] tools/saipen_engine/fast_check.py carries an unattributed working-tree edit that weakens the fast-path ledger check. _analyze_log line 66 changed from event != prev + 1 to event <= prev, so a GAP in event ids no longer reports: verified directly, a log holding E-001 followed by E-005 now returns no error, while a backwards E-005 followed by E-002 still does. A gap in an append-only ledger means events were lost or removed, which is precisely what the fast path existed to notice cheaply. The file was NOT modified at the start of this session and its mtime is 23:48:54, inside it; nothing in audit_checks or run_scenarios references fast_check or that line, so it is not a stray harness mutation left unrestored. The intent is defensible -- the error string says breaks monotonicity, validate.py line 2882 requires only monotonic increase across segments, and fast_check was therefore stricter than the canonical validator it fronts -- but that is an argument for adjudicating the alignment, not for it arriving unsigned. Same pattern as T-1272: authored change from a parallel session, not shipped, not adjudicated, sitting in a validator. Deliberately left out of the T-1279 release scope so it cannot ride into a release unnoticed | verify: AC-01 the decision is recorded either way, adopting the alignment with the reason that the canonical validator owns the monotonicity contract, or restoring strict consecutiveness with the reason that the fast path exists to catch a gap the canonical walk would only find later; AC-02 if the alignment is adopted, some check still detects a gap in the ledger and a red control proves it goes red on a gapped log; AC-03 if strict consecutiveness is restored, a red control proves the fast path goes red on a gapped log and the error string stops saying monotonicity when it means consecutiveness; AC-04 the working tree stops carrying an unattributed edit to a validator either way; unittest, audit_checks and ruff PASS | owner: workbuddy | claim_time: 2026-09-04T10:31:22Z
- [x] T-1279 [P1] The crew roster is static, so a capability with no applicable surface is still a mandatory stage. subs.py CrewRole.ensure_instance is a bool on a frozen dataclass and crew.py SC-1 iterates it; nothing anywhere in subs.py, crew.py or producer.py can express that a project has no UI. Measured on this repository: saiui has produced 11 packages across its entire life, all 11 with an empty payload, all 11 reporting no visual implementation files, against a tracked tree containing zero html, css, scss, tsx, jsx, vue, svelte, ui or qml files and zero tkinter, PyQt, PySide or textual imports. Each empty package still becomes a Core review ticket with a claim, a verify clause and a disposition, and saiui carries the largest share of Review SubSaipen hypothesis ticket history of any role. The one deterministic check it performs, Golden Default palette integrity, is already a Core validator gate whose result saiui quotes as its own evidence. Absence of a surface and correctness of a surface currently report identically, which is the applicability model missing rather than a sensor misbehaving | verify: AC-01 CrewRole carries an applicability predicate over deterministic project facts instead of a static bool, and an undecidable answer resolves to APPLICABLE so the model fails closed; AC-02 a role whose applicability is NOT_APPLICABLE satisfies its crew stage through a machine receipt naming the deciding fact, with zero model run, zero package and zero review ticket; AC-03 the UI probe is the same deterministic visual-surface scan saiui performs by hand, and on a fixture that does contain a visual file the answer is APPLICABLE, so the probe cannot go vacuously false; AC-04 a red control proves the applicability gate goes red on its own condition, and a role wrongly marked NOT_APPLICABLE is detectable rather than silent; AC-05 crew --dry-run on this repository reports saiui NOT_APPLICABLE with the deciding fact named, and every other built-in role unchanged; unittest, audit_checks and ruff PASS | owner: workbuddy | claim_time: 2026-09-04T08:57:59Z
- [x] T-1280 [P1] Execute external audit inbox layer audit/5.md (SRC-018) | verify: every actionable clause of SRC-018 is terminal with evidence; linked Work DONE; source closure succeeds; audit/5.md consumed by the journaled audit inbox cleanup | source_receipts: SRC-018 | owner: workbuddy | claim_time: 2026-09-04T08:55:59Z
- [x] T-1276 [P1] A bug-fix regression claim binds to no verifier, so weakening the oracle closes the ticket. phases/verify.md:34 requires a regression test that failed before the fix, but nothing binds the test that FAILED to the test that PASSES: editing the fixture, expected value, mock, seed, tolerance or input domain inside the same BUILD makes the red disappear with the implementation untouched. REVIEW re-runs that same weakened oracle, acceptance.py's build_boundary invalidates evidence only when BUILD is RE-entered so it never fires on a single-pass edit, and no engine module distinguishes a test-only diff from an implementation diff. The escaped-defect taxonomy already names the class CONTROL_DISARMED and the instrument control is already the mechanism against it; what is missing is that a bug-fix regression's known-bad input is the PRE-FIX subject and that the comparison must hold the verifier fixed | verify: AC-01 phases/verify.md states the causal A/B invariant, that the same test, fixture, oracle and verification configuration must FAIL against the pre-fix subject and PASS against the post-fix subject, and names the pre-fix subject as the known-bad input for that control; AC-02 a verifier identity is computable from the verification command plus the content of the named oracle paths, and a FAIL/PASS pair whose identities differ is reported inadmissible as fix evidence rather than merely noted; AC-03 the legitimate-test-change escape path is stated and reachable, so a justified verifier change invalidates only the earlier comparison, requires its own red control, and forbids pairing the old FAIL with the new PASS; AC-04 the three-way regression holds, buggy subject with the original oracle FAILs, fixed subject with the SAME oracle PASSes and is admissible, buggy subject with a weakened oracle PASSes and is REJECTED as fix evidence; AC-05 a red control proves the admissibility check goes red on its own condition; unittest, audit_checks and ruff PASS | owner: workbuddy | claim_time: 2026-09-03T16:21:28Z
- [x] T-1270 [P1] The audit route is deterministic in law and in code and nothing verifies an agent followed it. SOURCES.md gives ingest the lowest workable layer and ordinary BOARD priority for the derived Work, and router.py:405 routes there when no active ticket owns continuation, so which findings to fix is never a human choice. Witnessed in another project on this protocol: an agent found a 17-ticket audit campaign, then stopped and offered the human a three-option scope menu. wait_categories is a closed set of seven and scope selection is not among them, so that pause had no legal form -- and no detector. A rule with a route and no detector is a preference | verify: AC-01 a workable unconsumed audit layer plus a next_action that is neither the routed audit action nor a live-ticket continuation goes red, with the routed action named in the diagnostic; AC-02 an unreadable or invalid-only layer does not trip the check, because a diagnostic must not outrank real Work; AC-03 a live claimed ticket keeps continuation and the check stays quiet, so a fresh file never preempts a running transaction; AC-04 the closed WAIT category set is cited as the reason a scope question has no legal form; AC-05 a red control proves the check can go red on its own condition; unittest, audit_checks and ruff PASS | owner: workbuddy | claim_time: 2026-09-03T10:13:03Z
- [x] T-1271 [P2] Distribution staleness is invisible from the project. saipen status carries zero mentions of injection, so the question does an installed agent home run current SAIPEN is unanswerable without opening inject.log under AppData. Measured now: all four homes hold 7.241.1 at b4c7e2fd while HEAD is two releases ahead, and the scheduled task has skipped every run since 18:31 with SKIP DIRTY_SOURCE. The guard is correct -- it refuses to publish unproven source -- but one uncommitted edit in the injected surface stalls distribution indefinitely and only a log file says so | verify: AC-01 saipen status names how many installed agent homes are stale and the newest source head they carry; AC-02 when injection is blocked it names the blocking condition rather than staying silent; AC-03 the report reads the existing stamp and log and stores nothing new; AC-04 a fully fresh set of homes reports fresh rather than printing an empty section; unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-03T09:59:01Z
- [x] T-1273 [P2] audit_checks is 26 minutes of a roughly 45 minute gate batch because every one of 229 controls copies a pristine tree and runs the whole validator. It earns that on a release -- it caught a disarmed control today -- but it runs at full width for a two-file change. Each CASE already names its target file, so a changed-path set makes a development-time subset cheap. Today that would have been about 10 controls of 229. The risk is precisely the class this repository keeps closing, so the scoped mode must be an accelerator that cannot become the release gate | verify: AC-01 a development-time scoped run selects only the controls whose declared target file appears in a supplied changed-path set; AC-02 the scoped run refuses to report a total, so it can never be mistaken for the full sweep in a checkpoint; AC-03 saipen ship and CI remain bound to the full 229-control sweep with no scoped path reachable from either; AC-04 a control whose target is untouched but which reads a changed file through a cross-reference is named as a known limitation rather than silently trusted; unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-03T09:40:48Z
- [x] T-1272 [P2] saipen/OPS.md carries an unattributed working-tree edit adding a carve-out that permits manual in-place repair of unparsable legacy LOG event lines, moving illegal bracket content into evidence text. Same pattern as ecd77546 earlier today: authored prose from a parallel session, not shipped, not adjudicated. It weakens append-only, which is the invariant recovery is built on, so it needs a decision rather than a silent ride in someone else's release scope. It is also in the injected surface, so while it sits uncommitted the scheduled injector reports DIRTY_SOURCE and every consumer home stays behind | verify: AC-01 the carve-out is adopted with an explicit bound on what may be edited and what may never be, or reverted with the reason recorded; AC-02 if adopted, an in-place LOG repair is proven to preserve the byte-exact original under .saipen/recovery and to log a DEC naming both; AC-03 if adopted, a red control proves an unrecorded in-place LOG edit is still detected; AC-04 the working tree stops carrying it either way, so the injected surface is clean and distribution resumes; unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-03T09:14:31Z
- [x] T-1275 [P1] The claim refusal names a remedy the operator cannot reach. operations.py:731 refuses an out-of-order claim with 'use the explicit-claim flag to override with evidence', and operations.claim carries explicit: bool = False at :817 and :828, but no CLI surface ever sets it -- grep for --explicit across tools/saipen.py finds nothing. So the only way to claim a ticket that is finished but not topmost is to edit BOARD.md by hand, which is precisely the manual structural editing OPS.md section 4a calls FALLBACK ONLY and the validator saio check is built to detect. A refusal that pushes the operator toward the illegal path is worse than one that simply says no. Found while closing T-1272, whose work is complete and which cannot be claimed because T-1273 sits above it | verify: AC-01 saipen claim accepts a documented override flag that sets operations.claim explicit=True, and the NOT_TOP_WORKABLE refusal names that exact flag string verbatim so the remedy is copyable; AC-02 an override claim writes its own LOG evidence naming the topmost ticket it stepped over, so an out-of-order claim is attributable and never silent; AC-03 the override bypasses ONLY the Pick Rule ordering: ALREADY_CLAIMED, unmet needs, an open attempt and an INVALID claim pair still refuse with the flag present; AC-04 without the flag the Pick Rule is unchanged and NOT_TOP_WORKABLE still refuses; unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-03T09:07:56Z
- [x] T-1274 [P1] A settled audit inbox and a clean audit directory were reported as the same verdict, and they are not. Consuming every proven-closed layer empties audit/ of everything SAIPEN captured; anything still there is residue it never read and may never delete, and the projection reported the project idle over it. Measured today across nine repositories fed by an external producer: each held one canonical layer plus five uncaptured files and the inbox answered as if the folder were empty, so a delivered audit and a forgotten leftover were indistinguishable from the outside | verify: AC-01 audit status reports clean:false and names the residue entries when a settled inbox holds non-layer entries; AC-02 saipen continue surfaces audit-inbox-residue as RESTATE_AND_STOP before any idle or Improve verdict; AC-03 residue never outranks workable BOARD Work and never preempts a live ticket; AC-04 nothing in audit/ is ever deleted for the operator and dot-prefixed infrastructure is exempt so the clean verdict cannot be permanently false; unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-03T07:40:24Z
- [x] T-1269 [P2] The wave's own effect cannot be measured and a repeated escaped defect cannot be classified. Phase 8 plus metrics: an escaped-defect taxonomy carried on the follow-up ticket, and acceptance-criteria counts exposed through the existing read-only reporter so a later comparison can tell deterministic coverage from inspection-only coverage | verify: AC-01 saipen_metrics reports criteria total, criteria with evidence, deterministically verified, manual or inspection only, UNVERIFIED and CONTESTED counts from BOARD and LOG with no new stored file; AC-02 a defect found after a claimed PASS can be classified from a closed vocabulary recorded on the follow-up ticket rather than in a new subsystem; AC-03 the taxonomy is a closed set and an unknown class is reported as unknown rather than silently accepted; AC-04 the metrics tool stays observational and adds no gate; unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-03T07:34:19Z
- [x] T-1267 [P1] SAIPEN cannot answer what was promised for a Work, what evidence exists for each promise, and what remains unproven. A verify clause is one prose blob, so completion evidence binds to a ticket rather than to a criterion, and a producer sentence saying done reads the same to a later agent as a test result. Phases 1-4 of the acceptance-reconciliation wave: stable AC ids inside the existing verify field, structured evidence records naming the criterion and its provenance, and a read-only reconcile projection over BOARD plus LOG | verify: AC-01 a ticket verify clause may declare AC-NN criteria and a legacy clause without them parses to zero criteria rather than an error; AC-02 an evidence record binds a result to exactly one AC id and evidence for one criterion never satisfies another; AC-03 a producer claim in prose is not verification evidence and cannot move any criterion off UNVERIFIED; AC-04 the reconcile projection writes nothing and mutates nothing, proven by comparing canonical bytes before and after; AC-05 a criterion with no evidence reports UNVERIFIED and is never inferred to PASS; AC-06 two current evidence records disagreeing on one criterion report CONTESTED rather than the more confident one; AC-07 evidence written before the ticket re-entered BUILD is stale and does not certify the current implementation; AC-08 the projection is reproducible from BOARD and LOG alone with no new stored file; unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-02T17:01:00Z
- [x] T-1268 [P1] Narrative Authority Leakage: free-form prose acquiring control authority because a validator searches text for a magic phrase. Reproduced twice already -- the timestamp-inversion amnesty was disarmed for five weeks by three sealed DEC lines and then re-disarmed by a SCOUT checkpoint that merely quoted the marker, and a duplicated anchor phrase in hunt.md silently stopped a red control being evidence. Both were fixed as instances. The class has no coverage: nothing proves a mention cannot become an authorization | verify: AC-01 a RUN or checkpoint line that merely quotes an exception's wording does not activate that exception, proven by a test that would go green if the anchor were loosened; AC-02 a structured authorization scopes its exception to a named target and an anomaly outside that scope stays detectable; AC-03 an exception bounded by an event id cannot suppress anything recorded after it; AC-04 every suppressor reachable from the validator has a red control proving it can still go red outside the authorized scope; AC-05 the failure class is named once in KNOWLEDGE and the name is used by the tests rather than restated as advice; unittest, audit_checks and ruff PASS | owner: workbuddy | claim_time: 2026-09-02T16:09:57Z
- [x] T-1265 [P2] tools/saipen_metrics.py windows its two evidence sources differently and the disagreement is silent. The LOG side compares date strings, so --since 2026-09-02 means from midnight. The git side passes the bare date to git log --since, where approxidate resolves a dateless day using the CURRENT time of day, so the same argument means from 16:xx and every commit made earlier that day is dropped. Measured: git log --since=2026-09-02 returns 0 while --since=2026-09-01 returns 28, on a day carrying 9 releases. The default window sits five weeks back so the skew never showed, and a report that silently loses a day is exactly the false-precision this tool exists to avoid | verify: a --since naming today reports today's commits rather than none; the git side and the LOG side of one window both anchor at midnight; a regression test pins the boundary so the two halves cannot drift apart again; unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-02T14:07:01Z
- [x] T-1178 [P2] Review SubSaipen hypothesis saiui/UI-8: current interface contract sweep at c7ea5b1b — no patch required; not accepted fact; package_identity=sha256:733d64d4e22f8f82143e25f0f6b4f69bb427e717420bc5c8870bccd23d8716dc; producer=saiui; package=UI-8; source_head=c7ea5b1bb5f8e953c07140cda4f636a382c08310; source_tree_fingerprint=git-delta-v1:cd09a9a79f60d10339408b270b06f59207d15697fa4953d175fa915f891c6249; role_revision=sha256:f2e3685b908a3b9837917f12c5414628d847c35fb72567f0306e2c8b19a8dab8; outbox=.saipen/extensions/subs/saiui/kitchen/OUTBOX.md | verify: Independently reproduce or reject hypothesis, record Core disposition, apply no package patch during intake; package_identity=sha256:733d64d4e22f8f82143e25f0f6b4f69bb427e717420bc5c8870bccd23d8716dc; producer=saiui; package=UI-8; source_head=c7ea5b1bb5f8e953c07140cda4f636a382c08310; source_tree_fingerprint=git-delta-v1:cd09a9a79f60d10339408b270b06f59207d15697fa4953d175fa915f891c6249; role_revision=sha256:f2e3685b908a3b9837917f12c5414628d847c35fb72567f0306e2c8b19a8dab8; outbox=.saipen/extensions/subs/saiui/kitchen/OUTBOX.md | owner: workbuddy | claim_time: 2026-09-02T13:44:14Z
- [x] T-1177 [P2] Review SubSaipen hypothesis saipython/PY-12: current Python regression review at c7ea5b1b — no patch required; not accepted fact; package_identity=sha256:38c271ee4bf623d786c4919af0f59ec0c0d8b05809ec9f6148bdbdf8c95639a8; producer=saipython; package=PY-12; source_head=c7ea5b1bb5f8e953c07140cda4f636a382c08310; source_tree_fingerprint=git-delta-v1:cd09a9a79f60d10339408b270b06f59207d15697fa4953d175fa915f891c6249; role_revision=sha256:3069120b1a83291867c000dd5d7edb141d5fedf7895e5dc8f07d06624d05d9ff; outbox=.saipen/extensions/subs/saipython/kitchen/OUTBOX.md | verify: Independently reproduce or reject hypothesis, record Core disposition, apply no package patch during intake; package_identity=sha256:38c271ee4bf623d786c4919af0f59ec0c0d8b05809ec9f6148bdbdf8c95639a8; producer=saipython; package=PY-12; source_head=c7ea5b1bb5f8e953c07140cda4f636a382c08310; source_tree_fingerprint=git-delta-v1:cd09a9a79f60d10339408b270b06f59207d15697fa4953d175fa915f891c6249; role_revision=sha256:3069120b1a83291867c000dd5d7edb141d5fedf7895e5dc8f07d06624d05d9ff; outbox=.saipen/extensions/subs/saipython/kitchen/OUTBOX.md | owner: workbuddy | claim_time: 2026-09-02T13:42:05Z
- [x] T-1176 [P2] Review SubSaipen hypothesis saitest/TEST-9: current reconciliation regression sweep at c7ea5b1b; not accepted fact; package_identity=sha256:b9a808f9a7d03c0ab7cc46c1a4e1bc2ab2b1cb58f6c6c7f3ff3df0ccc0f0c4f9; producer=saitest; package=TEST-9; source_head=c7ea5b1bb5f8e953c07140cda4f636a382c08310; source_tree_fingerprint=git-delta-v1:cd09a9a79f60d10339408b270b06f59207d15697fa4953d175fa915f891c6249; role_revision=sha256:801fbfdc4be680d87b18cd21e6246d83fad5b474ebd7fe82efa83918cecf2f08; outbox=.saipen/extensions/subs/saitest/kitchen/OUTBOX.md | verify: Independently reproduce or reject hypothesis, record Core disposition, apply no package patch during intake; package_identity=sha256:b9a808f9a7d03c0ab7cc46c1a4e1bc2ab2b1cb58f6c6c7f3ff3df0ccc0f0c4f9; producer=saitest; package=TEST-9; source_head=c7ea5b1bb5f8e953c07140cda4f636a382c08310; source_tree_fingerprint=git-delta-v1:cd09a9a79f60d10339408b270b06f59207d15697fa4953d175fa915f891c6249; role_revision=sha256:801fbfdc4be680d87b18cd21e6246d83fad5b474ebd7fe82efa83918cecf2f08; outbox=.saipen/extensions/subs/saitest/kitchen/OUTBOX.md | owner: workbuddy | claim_time: 2026-09-02T13:41:28Z
- [x] T-1175 [P2] Review SubSaipen hypothesis saihunt/HUNT-011: current six-signal sweep at c7ea5b1b — no new defects; not accepted fact; package_identity=sha256:1d8f1423ed53fa907c6e1056ada4665b772aaed2d2698860e5635cf061886ba7; producer=saihunt; package=HUNT-011; source_head=c7ea5b1bb5f8e953c07140cda4f636a382c08310; source_tree_fingerprint=git-delta-v1:cd09a9a79f60d10339408b270b06f59207d15697fa4953d175fa915f891c6249; role_revision=sha256:4edb04181cb07e0946afd06fbe711166fa9dcc403e56b52e9be3844f0a71b0a5; outbox=.saipen/extensions/subs/saihunt/kitchen/OUTBOX.md | verify: Independently reproduce or reject hypothesis, record Core disposition, apply no package patch during intake; package_identity=sha256:1d8f1423ed53fa907c6e1056ada4665b772aaed2d2698860e5635cf061886ba7; producer=saihunt; package=HUNT-011; source_head=c7ea5b1bb5f8e953c07140cda4f636a382c08310; source_tree_fingerprint=git-delta-v1:cd09a9a79f60d10339408b270b06f59207d15697fa4953d175fa915f891c6249; role_revision=sha256:4edb04181cb07e0946afd06fbe711166fa9dcc403e56b52e9be3844f0a71b0a5; outbox=.saipen/extensions/subs/saihunt/kitchen/OUTBOX.md | owner: workbuddy | claim_time: 2026-09-02T13:37:26Z
- [x] T-1181 [P1] CORE-002 complete protocol-state reconciliation: derive every reconciliation-owned STATE field plus BOARD presentation, validate the full proposed surface, commit through one journaled transaction, CLEAN only when the whole invariant set is clean | verify: corrupting last_event, every goal counter/marker and BOARD checkbox each produce a repair; dry-run reports without writing; an injected crash mid-reconcile finishes atomically; no BOARD-only path reports CLEAN while a STATE invariant is red | owner: workbuddy | claim_time: 2026-09-02T13:33:10Z
- [x] T-1264 [P2] Three shipped protocol documents were modified in the working tree by something this session cannot identify, and two of them were then written back to HEAD content four minutes later. extensions/subs/PROTOCOL.md at 15:05:21 gained a substantive ten-line rule about subSaipen IDs never appearing in a Core LOG event ticket slot and still diverges; saipen/MAINTENANCE.md and saipen/phases/hunt.md were both written at 15:09:35 and now match HEAD byte for byte despite carrying real content deltas minutes earlier. The scheduled injector is ruled out by its own log: it read the surface at 14:31, 14:46 and 15:01 and exited SKIP DIRTY_SOURCE every time, and it copies out of the repository rather than into it. No other agent session on this project has a transcript newer than 10:22. This is the witnessed shape T-473 exists for, except no second session is known to have been running | verify: the writer of the 15:05:21 and 15:09:35 modifications to extensions/subs/PROTOCOL.md, saipen/MAINTENANCE.md and saipen/phases/hunt.md is named from evidence rather than guessed, or the search is recorded as exhausted with the candidates it eliminated; the PROTOCOL.md subSaipen-ID-in-LOG-slot addition is either adopted through a ticket of its own or reverted with the reason recorded, never left indefinitely as an unattributed working-tree edit; if a gate or tool in this repository wrote them, it is proven and stopped; unittest PASS | owner: workbuddy | claim_time: 2026-09-02T13:07:49Z
- [x] T-1242 [P2] Installed skill copy of the protocol is a stale fork: C:/Users/vac34/.claude/skills/saipen/BOOT.md is the pre-W4 13 KB router that still orders 'Open CORE.md 1.10 and read the row', while saipen/BOOT.md in the repository is the 4972-byte post-W4 router with zero 1.10 command routing -- a cold agent loads the skill copy, so two BOOT laws are live at once | verify: the shipped skill install and saipen/BOOT.md resolve to the same routing law; a drift check names the divergent file and byte counts; the installer/injector proves it refreshes BOOT.md, INDEX.md, STYLE.md and CORE.md rather than leaving an older generation in place; unittest PASS | owner: workbuddy | claim_time: 2026-09-02T12:08:43Z
- [x] T-1243 [P2] Conformance projection reads UNKNOWN for every real validator record: _validator_terminal_result requires the line to END with '-> PASS'/'-> FAIL', but every checkpoint an agent actually writes appends 'conf: high -- <detail>' after it, so saipen status showed Conformance UNKNOWN until a bare line was written by hand; no document tells an agent the terminal token must be last | verify: the canonical validator-record form is stated where an agent writing a checkpoint will read it; a record carrying evidence detail after the terminal token still classifies PASS, while NOT PASS/BYPASS/PASSING stay UNKNOWN; saipen status reports the current gate without a hand-written bare line; unittest PASS | owner: workbuddy | claim_time: 2026-09-02T11:42:38Z
- [x] T-1263 [P2] Three semantics in tools/saipen_metrics.py claim more or less than the evidence supports. render_token_cost treats zero usage records in the window as 'not scanned', so a directory that was read and simply had nothing inside the window is indistinguishable from a run that never looked. token_cost_units weights output at 1.0 while real output pricing is several times input, so the name implies a monetary basis the weights do not have -- only the cache ratios 1.25 and 0.1 are true published multipliers against a fresh input token. tickets_reopened_across_releases counts a ticket named in more than one release or closure subject as rework, and the repository convention that makes this sound was never recorded anywhere a later reader could check it | verify: a transcript directory that was read but yielded nothing inside the window reports the files it inspected and why the window is empty, distinctly from a run where no directory was passed; the weighted figure is named for what its ratios support and its definition states that output is counted at face value rather than at its price, so nothing in the report implies money; the reopened-ticket metric carries the evidence that was actually checked against this repository's history rather than an assumption; no pricing table, provider layer or ticket-attribution state is added; unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-02T11:09:13Z
- [x] T-1262 [P2] saipen/UI.md states a compact-fit target of 640x480 that should be 640x540, and carries four internal contradictions that make parts of it unenforceable: body sets overflow-x hidden (UI.md:167) while the QA gate requires the interface to fit without horizontal scroll (UI.md:358), so the check can never fail and a too-wide element becomes unreachable content instead of a visible bug; --selection (UI.md:138) and --surfaceRaised (UI.md:118) are both #3D372A, so a selected row on a raised surface is invisible while the component rule says selection is shown with --selection; the accessibility floor requires primary targets of at least 24px (UI.md:334) while the document's own base CSS ships button min-height 20px (UI.md:185) and input height 20px (UI.md:206) | verify: the compact-fit target reads 640x540 in both places that state it and says what the number measures; body no longer hides horizontal overflow, so the QA item that forbids it can actually fail; the selection rule does not depend on a token that equals the surface it sits on; the 24px target floor and the 20px control heights in the same document no longer contradict each other; every change names the error class it prevents and adds no rule that does not; validate, audit_checks, run_scenarios, unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-02T10:42:03Z
- [x] T-1261 [P2] One live LOG entry carries a stamp in the wrong order and no gate noticed: E-5171 reads 26.09.01 where every other entry in the segment reads DD.MM.YY, so it parses as 2001-09-26 and lands 25 years before the segment it sits in. Found by tools/saipen_metrics.py on its first run, not by validate.py, audit_checks.py or run_scenarios.py -- 227 red controls and none of them reads a date. Any window, ordering or staleness claim derived from the journal is wrong by exactly one event today and by an unknown number tomorrow | verify: the writer that produced E-5171 is identified and shown to be unable to emit a non-DD.MM.YY stamp again; a malformed or out-of-order date in an appended LOG line is refused at write time or named by a validator check rather than surviving into history; the existing E-5171 line is left byte-identical because LOG.md is append-only, and the compensating record naming it is the repair; a red control proves the detector fires on a YY.MM.DD stamp; unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-02T09:16:34Z
- [x] T-1260 [P2] SAIPEN measures whether it is internally correct and never whether it is worth running: validate, audit_checks, run_scenarios and protocol_budget are all self-referential, so 439 commits and 113 releases in 32 days carry no number for throughput, rework, human cost, continuity or self-directedness, and the freeze-versus-grow argument can only be settled by opinion | verify: a read-only reporter derives throughput, rework, human cost, continuity and self-directedness from git history and .saipen/LOG.md only and adds no protocol law, state, gate or instrumentation; a LOG shape change reads as a parse failure rather than a silent zero; a backward phase transition counts as rework while a forward-only ticket does not; a product path is never misfiled as protocol self-surface; the three unmeasurables (tokens per useful result, outcome quality vs a plain agent, human wall-clock saved) are named in the output instead of estimated; unittest and ruff PASS | owner: workbuddy | claim_time: 2026-09-02T08:34:47Z
- [x] T-1244 [P2] audit_enqueue holds a process-wide lock while blocking on the OS file lock with no timeout: _PROCESS_GUARD is taken before file_writer_lock(blocking=True), so if another PROCESS holds .saipen/locks/audit-allocator.lock and never releases it, every same-process producer thread blocks behind the guard with no diagnostic and no bound | verify: a blocked allocator lock surfaces WRITER_BUSY or a named timeout instead of hanging; the guard is not held across an unbounded wait; concurrent same-process enqueues still allocate distinct ids; tools/test_audit_enqueue.py PASS | owner: workbuddy | claim_time: 2026-09-01T22:05:32Z
- [x] T-1258 [P3] One 'python -m unittest discover -s tools' run reported 700 tests and 1 failure while tools/audit_parity.py ran concurrently in another process; two later runs with nothing beside them were green (700 OK, 701 OK) and the failing case name was lost to the output filter. Observed 2026-09-01 during T-1256. A suite that reads the live repository tree can be reddened by another gate mutating state beside it, and a false red nobody can name afterwards is worse than no red | verify: the concurrent-run failure is reproduced and the exact case named, or the suite is proven independent of a concurrently running gate; a repeat discovery run under the same concurrency is green | owner: workbuddy | claim_time: 2026-09-01T21:34:45Z
- [x] T-1247 [P3] audit_checks warn-slug ownership probe is coupled to the live BOARD sitting near the soft cap: the probe adds one synthetic ticket and asserts the WARN slug set is unchanged, so whenever BOARD.md is within ~1 KB of the 16384 soft cap that single added line introduces the board-soft-cap slug and the control reports a false ownership break | verify: the probe's verdict does not change when BOARD.md sits just under the soft cap; a genuine slug-ownership break still goes red; audit_checks 227 of 227 | owner: workbuddy | claim_time: 2026-09-01T20:47:02Z
- [x] T-1259 [P2] Hot-path semantic ownership compression wave: CORE, MAINTENANCE, IMPROVE, OPS; remove duplicated law and historical narrative while keeping one canonical owner and prioritizing CORE plus active-phase runtime context | verify: record exact before/after bytes and load-profile deltas; preserve DFA, authorization, recovery, lifecycle and audit semantics; core validator, structured scenarios, all independent red controls, full unittest discovery, ruff and protocol_budget PASS | owner: workbuddy | claim_time: 2026-09-01T20:07:11Z
- [x] T-1229 [P2] Phase-delta semantic ownership compression across all 16 phase documents, followed by MAINTENANCE cross-cleaning where it is the canonical owner; preserve unique behavior and remove duplicated law/history from routine load paths | verify: preferred phase corpus <=72000 bytes; otherwise a justified semantic-floor variance names the exact final corpus size, largest remaining phase documents, unique normative behavior retained, and why further removal would weaken correctness, safety, recovery, auditability, or cold-agent comprehension; 16/16 phase docs present; REGISTRY DFA unchanged; ratified load profiles unchanged and within budget; existing audit red controls remain independently red; compression introduces no new baseline failures; SRC-013:R008's premature archived 100304-byte local-slice evidence is superseded by SRC-016's global completion contract; ruff PASS | source_receipts: SRC-016 | owner: workbuddy | claim_time: 2026-09-01T18:23:22Z
- [x] T-1256 [P2] XPATCH proposal mode: a SAIPEN project MAY leave a Cross-Repo Patch Receipt for another SAIPEN project without writing one byte of target source. Design in .saipen/KNOWLEDGE/XPATCH.md. Receipt records source project_lineage/work/attempt/agent, target lineage, reason, exact paths with before/after sha256 and real verification results (UNKNOWN when not run) into .saipen/exchange/xpatch/XP-NNNNNN/. Target-side attribution gains ATTRIBUTED_FOREIGN_PATCH, ordered own-active-Work then valid receipt then kitchen/generated then unattributed; a receipt is valid only on lineage match AND exact path AND after-hash match. Foreign actor MUST NOT write target STATE/BOARD/LOG/milestones/release metadata/ticket lifecycle. Reuse project_lineage_identity (paths.py:574), MutationRecord (effects.py:232), read_set/write_set/COMPATIBLE_DRIFT (producer.py) rather than a second wheel | verify: a receipt-carrying dirty tree classifies ATTRIBUTED_FOREIGN_PATCH and does not stop; a forged, duplicate, wrong-lineage or after-hash-mismatched receipt classifies unattributed; a foreign write to target STATE/BOARD/LOG is refused; a crash between intent and applied leaves no unexplained target mutation; overlap with target active work reconciles instead of WAIT_USER; failed target verification REPAIRs or SUPERSEDEs and never abandons the receipt; hostile cases from KNOWLEDGE/XPATCH.md covered | owner: workbuddy | claim_time: 2026-09-01T08:25:42Z
- [x] T-1255 [P2] The scheduled stamp cannot record which revision it installed: autoinject._source_head shells out to git rev-parse HEAD from its own HOME, but the scheduled injector runs out of a git archive extraction at %LOCALAPPDATA%/saipen/scheduled-source which is not a repository, so source_head is silently dropped on the ONE path that matters. Observed on the v7.235.0 run: the .claude stamp carries digest and installed_at and no head, while the runner had logged head=35d2fed5b865 two lines earlier and knew it all along | verify: a stamp written by the scheduled path names the source head; the runner passes the head it already proved rather than the stamper re-deriving it from a tree that has no git; a manual autoinject run still records the head from the clone; an unavailable head is omitted rather than guessed; tools/test_inject_digest.py covers the passed-in head | owner: workbuddy | claim_time: 2026-09-01T03:35:20Z
- [x] T-1249 [P1] Consumer protocol installs go stale and strand an agent mid-session: witnessed on opencode/9router after a model switch -- the agent read C:/Users/vac34/.config/opencode/skills/saipen/CORE.md, grepped for the gg shortcut, found nothing (W4 moved the shortcut table to REGISTRY.json/COMMANDS.md) and answered 'Use: gg <objective text>' instead of resolving it; its BOOT copy still routes command resolution to CORE 1.10. Refresh must be automatic, not a thing someone remembers to run | verify: a scheduled task re-runs the injector on an interval so opencode, codex, freebuff and every other consumer copy tracks the repository; a stale consumer copy is detected and named rather than silently used; the injector refuses to publish from a dirty or unproven source; run_scenarios scheduler probes cover the install, the interval and the refusal paths | owner: workbuddy | claim_time: 2026-09-01T02:57:52Z
- [x] T-1254 [P1] Injector copier and freshness digest use two different cache-prune rules, so the digest can never match after a local test run: manifest.copy_tree_members prunes both __pycache__ and .pytest_cache (its own docstring says so) while autoinject._digest prunes only __pycache__ and .pyc/.pyo. A clone that has run pytest carries tools/.pytest_cache/ into the digest; the git-produced snapshot never has it. Measured after v7.234.2, with the CRLF normalisation already in place: the surfaces differ by exactly four files, all under tools/.pytest_cache, and --check still reports all four homes stale seconds after a successful scheduled inject | verify: the copier and the digest share ONE prune rule rather than two that can drift; a clone with a populated .pytest_cache produces the same digest as a snapshot without it; a real surface change still moves the digest; autoinject.py --check reports fresh from the clone immediately after a scheduled run | owner: workbuddy | claim_time: 2026-09-01T00:19:48Z
- [x] T-1253 [P1] The freshness digest can never match across the transport that produces it: autoinject._digest hashes raw bytes, but the scheduled path injects from a git-produced snapshot where text files carry CRLF while the clone's working tree carries LF -- saipen/BOOT.md is 4972 bytes with 0 CRLF in the clone and 5063 bytes with 91 CRLF in the published snapshot, identical content. So the stamp written by a successful scheduled run (90a98b30a3ad88b3) and the digest computed in the repository (401da51c8072800e) disagree by construction, and --check reports STALE on a home that was refreshed seconds earlier. The module's own docstring says the digest covers file CONTENT | verify: a home refreshed by the scheduled path reports fresh from the clone immediately afterwards; a real content change still reports stale; a pure line-ending difference does not change the digest; the CRLF/LF equivalence is covered by a test rather than observed once | owner: workbuddy | claim_time: 2026-08-31T23:56:59Z
- [x] T-1252 [P1] The freshness witness is never written by the injector that actually runs: tools/autoinject.py owns the .saipen_injected stamp and compares it to decide whether a consumer copy is current, but bootstrap/inject.ps1 -- the script the 15-minute scheduled task invokes -- copies the protocol and writes no stamp at all. Proven live: a successful scheduled inject at 02:02 refreshed every consumer home (opencode BOOT.md is now the 5063-byte post-W4 router with zero CORE 1.10 routing) and autoinject.py --check still reports all four targets 'installed unstamped'. So a fresh copy is indistinguishable from a drifted one, and the whole staleness design has no witness | verify: a scheduled inject leaves every refreshed target stamped with the source digest; autoinject.py --check reports clean immediately after a successful scheduled run and stale after a real source change; the stamp has ONE owner rather than two injectors disagreeing; run_scenarios covers the stamp written by the PowerShell path | owner: workbuddy | claim_time: 2026-08-31T23:29:49Z
- [x] T-1251 [P1] Scheduled injector is structurally unable to ever run on a working machine: schedule-run.ps1 Read-CleanStatus treats ANY line of 'git status --porcelain -uall' as DIRTY_SOURCE, and a live project permanently carries untracked state that has nothing to do with the injected protocol -- 1926 .saipen/saitranslate/.translation-cache files, 1562 subSaipen kitchen files, .prepare-staging, improve cycles, unshipped release_scope records and the user's own .workbuddy-ai/memory. Observed live: the task fired at 01:01, logged the untracked paths and exited SKIP: DIRTY_SOURCE, so the 15-minute schedule is a no-op forever | verify: the guard blocks when anything in the INJECTED SURFACE differs from HEAD (an edited saipen/CORE.md, a modified tools/ file, an unstaged VERSION) and does not block on untracked project memory, caches or user data; the scheduled task injects on a normally dirty working tree; run_scenarios covers both the blocking and the non-blocking case; inject.log records which surface decided | owner: workbuddy | claim_time: 2026-08-31T23:00:48Z
- [x] T-1250 [P2] bootstrap/schedule.ps1 status reports DEGRADED for a task it just installed correctly: the checker asserts the stored Arguments equal the bare VBS path because 'schtasks strips the surrounding quotes', but on Windows 10 Pro 19045 schtasks stores the quoted form, so a canonical wscript.exe + schedule-run-hidden.vbs task with Repetition PT15M reads as 'task action does not execute the canonical VBS wrapper' | verify: status reports HEALTHY for a freshly installed task on a host that stores the quoted argument form; a task whose action carries extra arguments, a different path or a different executable still reports DEGRADED; the scheduler probes cover both stored forms | owner: workbuddy | claim_time: 2026-08-31T22:37:37Z
- [x] T-1248 [P1] CI red on main at fe5b0d4f: release-executor probe 7 writes .git/hooks/pre-commit with write_text and never sets the executable bit, so on POSIX git silently skips the hook, the injected commit rejection never happens, and the probe reports 'ok=True code=RELEASED not in errors.CODES' plus a real push it expected not to see; Windows ignores the bit, which is why it only fails on Linux | verify: probe 7 refuses with a closed code and pushes nothing on a POSIX host; the hook is executable; run_scenarios exits 0 on Linux and Windows; GitHub validate workflow green on main | owner: workbuddy | claim_time: 2026-08-31T22:14:27Z
- [x] T-1246 [P1] Release-executor fixture inherits the AUTHORING host absolute saipen_home: build_fixture copies the live .saipen/STATE.md and rewrites phase/task/mode but not saipen_home, so every host except the one that wrote the value refuses the first fixture command with REFUSE [HOME_REQUIRED] and run_scenarios aborts; it stayed hidden while the scheduler probe crashed the suite earlier | verify: run_release_executor_probes passes with the live STATE saipen_home pointed at a nonexistent path; the fixture STATE names the fixture root; GitHub validate workflow green on main | owner: workbuddy | claim_time: 2026-08-31T14:44:59Z
- [x] T-1245 [P1] CI red on main at b62e2313: the audit_checks red-control 'CHANGELOG entries fall out of descending order' anchored on the literal '## 7.230.0', and v7.233.0 archived that entry out of CHANGELOG.md in the same release, so the mutation became a silent no-op and the control stopped being evidence -- the second time a hardcoded CHANGELOG anchor has died this way | verify: audit_checks reports 227 of 227; the changelog-order control derives its anchor at run time so archiving cannot orphan it again; the case still goes red on its own condition; GitHub validate workflow green on main | owner: workbuddy | claim_time: 2026-08-31T13:57:25Z
## BLOCKED
- [ ] T-1266 [P1] Evidence-backed acceptance reconciliation: a completion claim becomes traceable to structured evidence bound to the criterion that was promised, with narrative prose demoted to data | verify: Evidence-backed acceptance reconciliation: a completion claim becomes traceable to structured evidence bound to the criterion that was promised, with narrative prose demoted to data is complete and the repository-declared verification harness passes | owner: workbuddy | claim_time: 2026-09-02T15:33:17Z | blocker: GOAL_ECHO -- the ticket text and its verify clause are the same sentence, so no evidence can ever satisfy it: its DONE proof is a restatement of its own description. Same class as T-1195, T-1172 and T-1155, all blocked as INVALID_INVOCATION today. Kept as the wave's umbrella rather than dismissed: the objective is real and its work is decomposed into T-1267 (AC criteria, evidence records, reconcile projection), T-1268 (Narrative Authority Leakage hardening) and T-1269 (escaped-defect taxonomy and metrics), each carrying its own AC-NN criteria. Unblocks when those three are terminal and this line can name them as its evidence
- [ ] T-1195 [P1] Implement, verify, ship | verify: Implement, verify, ship is complete and the repository-declared verification harness passes | blocker: INVALID_INVOCATION -- ticket text is the tail of a goal command parsed as its own ticket, not work. Created in the same BOARD write as T-1194 at b23019f4 (ship v7.231.11), where T-1194 carried the real objective (implement the 13 SRC-008 audit findings) and T-1196..T-1208 carried the individual findings. T-1194 finished at E-4882 on 31.08.26, so the objective this fragment names is closed and the fragment has no content of its own: its verify clause is its own description repeated, which no evidence can ever satisfy. Same class and same disposition as T-1172 and T-1155. Superseded by T-1194
- [ ] T-1257 [P2] XPATCH direct mode: bounded compare-and-swap write of an exact clean target file set in another SAIPEN project, so the source agent continues immediately instead of leaving a proposal. Held because direct mode IS a stale-write guard on files another agent may hold -- exactly T-473, which is HELD on T-442. Without that guard the target agent writes its stale in-memory copy over a landed XPATCH and the receipt becomes a lie. Design in .saipen/KNOWLEDGE/XPATCH.md | needs: T-473 | verify: direct apply performs zero target-source writes when target lineage changed, intent malformed, any before-hash moved, path escaped declared scope, target path was already dirty, receipt namespace unsafe or the effect is irreversible, and degrades to TARGET_DRIFT mode=proposal writes=0; the CAS guard is proven to be the shared T-473 stale-write detector rather than a private second mechanism; the implementation names why it is bounded interoperability and not v8 Concurrent Mode | blocker: HELD -- unmet dependency T-473; not schedulable until the concurrent whole-file clobber guard exists
- [ ] T-1172 [P1] --help | verify: --help is complete and the repository-declared verification harness passes | blocker: INVALID_INVOCATION -- goal --help was an accidental CLI usage probe; superseded by T-1173
- [ ] T-1155 [P1] --help | verify: --help is complete and the repository-declared verification harness passes | owner: saipen-cli | claim_time: 2026-08-24T16:09:23Z | blocker: INVALID_INVOCATION -- accidental CLI help probe was parsed as goal text; superseded by the actual FF/XX/VV/ZZ implementation mission
- [ ] T-1119 [P2] Own the log-soft-cap warning. | verify: log-soft-cap owned | blocker: PERMANENT_WARNING_OWNER -- the condition can never be met and immutable history is why. LOG.md carries 125 pre-date sealed entries; new entries are FAILed, so the warning emits forever and no work closes it
- [ ] T-1099 [P2] Own the board-soft-cap warning. | verify: board-soft-cap owned | blocker: PERMANENT_WARNING_OWNER
- [ ] T-473 [P1] HELD, not scheduled: Rosary A3, the concurrent whole-file clobber guard. Witnessed twice this session -- a parallel session overwrote `BOARD.md` and reverted six `## DONE` moves whose work was already committed and pushed (E-1863), caught an hour later by CONFORMANCE 199 rather than at the moment of damage. § 1.5 already reads each file before writing it, so the guard is cheap: compare the content against what was read immediately before the write and refuse on a difference. It is held deliberately because it touches the checkpoint path every session runs and because T-442..T-451 already own the v8 Crew concurrency design; landing a collision detector ahead of that design risks pre-empting it. | needs: T-442 | verify: a read-then-foreign-write-then-write sequence is refused; an unchanged file writes normally; the guard stays a detector and never becomes a scope or locking scheme | blocker: HELD -- unmet dependency; not schedulable until concurrent-mode gate completes
- [ ] T-576 [P3] Prune or confirm .saipen/recovery/_ee_apply.py (25892 B) and .saipen/recovery/_ee_measure.py (839 B) -- untracked, zero references, orphaned EE scratch found by CLEAN (E-2430); they sit in the recovery evidence directory without belonging to any recovery snapshot and are not regenerable by a named command. Delete only after a human confirms they are abandoned; keep the two timestamped STATE snapshots beside them regardless. | verify: a human decision recorded (delete both, or keep with a reason); the two STATE recovery snapshots are never touched | owner: claude | claim_time: 2026-08-08T21:30:00Z | blocker: WAIT_USER_CONFIRMATION -- the two untracked orphaned scripts cannot be deleted without a human decision (clean.md section 2); no user confirmation exists in current evidence, so they stay untouched
- [ ] T-407 [P2] Own the `goal-reauth-untripped` warning: E-1659 logged a `DEC: goal reauthorized` with counters under the § 2.4 caps, and LOG is immutable by append-only, so the warning emits forever. Keep this ticket live while the warning emits. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401) | blocker: PERMANENT_WARNING_OWNER -- the condition can never be met and immutable history is why. E-1659 logged a `DEC: goal reauthorized` under the caps, `LOG.md` is append-only, so the warning emits forever and no work closes it. Same trap as T-406: in `## TODO` the Pick Rule was obliged to select a ticket whose own `verify:` says closing it FAILs
- [ ] T-406 [P2] Own the `subsaipen-never-ran` warning for the life of the saipython sub: saipython was spawned on migration but never run, and MANIFEST.md advertises it live until its board opens, so the warning emits on every validation. Keep this ticket live while the warning emits; close only when the sub runs or is de-advertised. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401) | blocker: PERMANENT_WARNING_OWNER -- the condition can never be met and immutable history is why. `MANIFEST.md` advertises saipython live until its board opens, so the warning emits every validation; this ticket exists to own it, and its own `verify:` says closing it while the warning emits FAILs the ownership check. Sitting in `## TODO` it was a ticket the Pick Rule MUST take and no agent can finish. It leaves `## BLOCKED` when saipython actually runs or is de-advertised -- a real event, not work anyone can pick
- [ ] T-442 [P0] GATE: do not begin SAIPEN v8 Concurrent Mode while the sequential protocol still has known foundational defects. Unblock only when no open P0/P1 ticket owns an RFC contradiction, ambiguous state transition, unenforced MUST, source-of-truth conflict, ownership gap, recovery gap or validator blind spot; all canonical gates are green; and one ordinary sequential release completes without exposing another foundational defect. The detailed Crew design backlog (T-443..T-451) lives in KNOWLEDGE/crew-v8-backlog.md (moved there by T-464). | verify: the unblock decision names the closed defect classes, current green evidence and the exact sequential release used as the stability checkpoint; no Crew implementation exists before this ticket reaches DONE | blocker: HELD -- sequential stability gate; unblocks when the v8 prerequisites are proven
- [ ] T-575 [P0] GATE: V9-M1 resident-continue implementation may start ONLY after ACTUAL v8 Concurrent Mode exists and is stable, not merely after the v8 gate is allowed. Required v9 prerequisites: T-442 gate completed; T-443..T-450 implemented or decided; T-451 real-world Concurrent Mode soak completed; v8 released; at least one post-release v8 stability checkpoint green; current Improve cycle clean; HUNT/CLEAN baseline clean. Research, source mapping, isolated-kitchen prototypes, architecture documents and red controls remain allowed earlier (full design in .saipen/KNOWLEDGE/V9_RUNTIME.md; upstream a18809e MIT, provenance in saipen/runtime/UPSTREAM.json and THIRD_PARTY_NOTICES.md). No v9 runtime implementation before actual v8 completion. | verify: the unblock decision names each prerequisite with fresh evidence including the completed v8 soak and a green post-release v8 stability checkpoint; every copied/adapted unit is recorded in saipen/runtime/UPSTREAM.json and THIRD_PARTY_NOTICES.md; the 30 red controls in V9_RUNTIME.md are listed before any V9-M1 implementation | blocker: HELD -- until actual v8 Concurrent Mode exists and is stable

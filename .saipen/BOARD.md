# Board
## DOING
- [/] T-588 [P0] NITRO DOGFOOD II: M8 SubSaipen boundary/lifecycle/freshness regression. (1) PATH ESCAPE: _sub_dir() strips only "/" and "\", accepts "..", so sub_spawn(project, "..", home) returns SPAWNED and writes .saipen/extensions/STATE.md etc instead of staying under .saipen/extensions/subs/<name>/. Fix: ONE shared path-safe ID primitive (reused by SubSaipen name, Improve cycle_id/seat_id, report identifiers, future worker IDs) rejecting "", ".", "..", traversal, separators, absolute/drive/control forms; after path construction resolve + prove inside the owner root. (2) FIRST-SPAWN BOOTSTRAP MISSING: first spawn into a project without the extension must copy PROTOCOL.md/README.md/crew.md/TEMPLATE/_shared/inbox.md/built-in sai*.md charters from saipen_home; current spawn creates only MANIFEST + instance files, leaving the worker without its governing extension. Bootstrap must be one journaled admission (crash must not leave a MANIFEST entry without governing protocol). (3) PAUSE/RESUME FALSE SUCCESS: pause -> BLOCKED, resume clears blocker only and leaves phase BLOCKED. pause must record prior phase/next_action (conditional owned metadata); resume must prove blocker == paused by main agent, restore exact prior phase + next_action, clear blocker, refresh updated, append sub trace, journal. Pause/resume must leave a trace (sub LOG + Core op provenance). (4) COLLECT FRESHNESS HOLE: ready OUTBOX package with no source_head/source_tree_fingerprint/role_revision is accepted fresh; absence must REFUSE PACKAGE_INCOMPLETE, never equal fresh. (5) MALFORMED OUTBOX: nonempty OUTBOX with zero parseable entries must be MALFORMED_PACKAGE, never an empty queue. (6) COLLECT COMMAND DRIFT: PROTOCOL says `saipen sub collect` checks every active sub; current CLI requires <name>; resolve aggregate vs targeted shapes. (7) ADOPT GAP: T-585 named adopt but no mechanical operation exists; decide implement vs overclaim and record. (8) T-585 was closed with verify: verify: TBD -- audit claim-by-claim, create regression provenance, do not rewrite history. | verify: sub_spawn("..") and "." and "../x" and "x/../y" and backslash and absolute and newline all REFUSE INVALID_ID/PATH_ESCAPE with tree hash identical before/after (zero bytes); first spawn installs PROTOCOL.md README.md crew.md TEMPLATE/ _shared/inbox.md and built-in sai*.md charters as one journaled admission; crash during bootstrap never leaves a MANIFEST entry without the governing extension; pause records prior phase/next_action; resume restores both and clears blocker; pause/resume leave a trace; ready OUTBOX missing source_head / tree fingerprint / role_revision each REFUSEs PACKAGE_INCOMPLETE; nonempty malformed OUTBOX REFUSEs MALFORMED_PACKAGE; `saipen sub collect` (no name) aggregates all active subs and `saipen sub collect <name>` is an optional targeted diagnostic; adopt decided + implemented-or-declared; public CLI path proven for sub spawn/pause/resume/clean/collect; validator + scenario + audit + floor green with fresh evidence; T-585 claim-to-proof matrix recorded | owner: claude | claim_time: 2026-08-09T15:34:39Z
## TODO






- [ ] T-553 [P1] Keep improve routing OUT of `STATE.md` unless routing genuinely requires it -- reversing this ticket's original direction, which was to add flat `improve_cycle`/`improve_status`/`improve_reports_expected`/`improve_reports_complete` fields conditioned like the goal counters. Under T-552's meta-control design there is no global improve phase to route, and independent counters are exactly the drift risk: `reports_expected` and `reports_complete` MUST be DERIVED from the cycle manifest (T-570), never maintained as separate numbers that can disagree with the files they describe. If a field survives that test it lands flat with a closed enum, REQUIRED only while an improve cycle is live and cleared at completion, exactly the way `goal_waves`/`goal_tickets` are conditioned on `execution_intent: goal`. No finding is ever encoded into STATE. | needs: T-552, T-570 | verify: each proposed field is justified by a routing decision that cannot be answered by reading the manifest; the counters are derived, and a scenario proves a manifest edit changes them with no STATE write; validate.py FAILs findings text in STATE; 1.2's required-set sentence is extended in at most one place
- [ ] T-554 [P1] Register the command family in CORE.md §1.10's closed list: `saipen improve`, `saipen improve status`, `saipen improve sweep`, `saipen improve verify`, `saipen improve clean` -- five exact routes, multi-word like the existing `saipen sub <verb>` precedent. No repeated-letter shortcut is assigned: a new key is a ~74-file callout rollout (T-493) and nothing in the request asks for one, so the key count in every locale README/guide/mirror MUST stay unchanged. `saipen improve clean` archives reports only and never enters the CLEAN phase -- the name collision is the trap and the row states the distinction. | needs: T-552 | verify: all five verbs resolve in §1.10 and in validate.py's command surface check; an undeclared `saipen improve <word>` is rejected rather than guessed; the shortcut key count is byte-unchanged across every callout file; `improve clean` cannot transition `phase` to `CLEAN`
- [ ] T-555 [P1] Make a finding mechanically checkable rather than prose: the required header block (agent, role, model_or_runtime, project, saipen_version, protocol_fingerprint, source_head, source_tree_fingerprint, context_scope, context_available, report_status) -- NO absolute machine-local path: report identity uses saipen_version + protocol_fingerprint, never the local protocol install path (machine-local, non-portable, unnecessary for reproduction), the `IMP-###` finding schema with its closed `severity`/`class`/`confidence`/`action` vocabularies, and the completion bar for `report_status: complete`. A finding with no observable expected/actual/evidence triple is rejected, not softened. `context_available: complete` is refused when the stated scope is partial. | needs: T-551 | verify: validate.py FAILs a report whose finding lacks expected/actual/evidence (red control 6), FAILs a class/severity outside the closed set, FAILs `report_status: complete` while the completion bar is unmet, and FAILs a full-context claim over a partial `context_scope` (red control 3); `NO_FINDINGS` with a stated scope passes
- [ ] T-556 [P1] Define the Core sweep as the only path from report to canonical work: deterministic report order, numeric IMP order inside a report, the closed disposition set (CONFIRMED \| DUPLICATE \| ALREADY_FIXED \| SUPERSEDED \| LATER_RULE \| NOT_REPRODUCED \| INVALID \| NEEDS_EXTERNAL_EVIDENCE), root-cause deduplication producing ONE ticket with all `source_reports:` references, and the required ticket fields (source_reports, reproduced, invariant, defect, impact, exact_fix_scope, red_control, done_condition). A report's own `confidence: proven` is evidence to inspect, never a ticket authorization. Core dispositions are written to the Core-owned ledger `.saipen/improve/<cycle_id>/SWEEP.md` (report_path, run_id, IMP-id, disposition, reproduced, canonical ticket, fixed_by, verification), NEVER into the seat report: a completed seat report is immutable and its hash is verified unchanged after sweep/fix/verify. Observation (seat report) and judgment (SWEEP.md) never share one writable file. | needs: T-555 | verify: validate.py FAILs a canonical ticket generated from an unverified finding (red control 10), FAILs a ticket with no resolvable `IMP-###` back-reference (red control 20), FAILs an INVALID or ALREADY_FIXED finding that produced a ticket (12, 13), and FAILs an edited original finding; three reports sharing one root cause produce one ticket in the scenario suite (red control 11)
- [ ] T-557 [P1] Enforce the writer boundary and the recursion stop the spec depends on but does not mechanize: during SELF-AUDIT/REPORT no seat may touch Core protocol, main source, canonical BOARD/LOG/STATE except the bookkeeping that registers the audit; a SubSaipen writes only inside its own home; `saipen improve verify` audits ONLY the delta produced by this cycle (fixes, new rules, new tests, final state) and MUST NOT re-enter a full improve cycle; `saipen improve` never silently enters ADD. | needs: T-552, T-556 | verify: red controls 7, 17, 18, 21 and 22 are mechanical -- an auditing sub that writes a main-project file FAILs, a verify pass that reopens unrelated history FAILs, a verify that starts a full cycle FAILs, an improve run whose `next_action` names ADD FAILs, and a report treated as canonical BOARD state FAILs
- [ ] T-558 [P1] Land the two reasoning gates as checked artifacts rather than advice: the META-IMPROVEMENT rule (a protocol-level fix MUST record cross-project recurrence reasoning; a local bug stays local) and the WEAK-MODEL PRECEDENT test (every protocol-level fix MUST record the answer to "could a weak but compliant model still choose wrong while honestly believing it followed SAIPEN?", strengthened in the fixed preference order: state field, transition rule, validator, red scenario, canonical example, prose last). ACCIDENTAL_SUCCESS is a first-class class: a correct result reached without the required verification is never recorded as PASS. | needs: T-556 | verify: validate.py FAILs a protocol-level improve ticket with no recurrence reasoning (red control 15) and none with no weak-model answer (16); a finding whose result was correct but whose verification never ran is classified ACCIDENTAL_SUCCESS, not PASS (red control 5); a fix answered only with prose where a state field or validator was available is flagged
- [ ] T-559 [P1] Define `saipen improve clean` as archive-with-provenance and nothing else: it refuses while any finding is unswept or any disposition is missing, it preserves the original findings verbatim, and every archived report keeps the canonical ticket references that point back at it. Partial or timed-out test evidence can never mark an IMP fixed. | needs: T-556, T-557 | verify: red controls 23, 24 and 25 are mechanical -- clean with an unswept finding is refused naming the finding, an archived report still resolves its ticket provenance, and a timeout/partial run recorded as fix evidence FAILs
- [ ] T-560 [P1] Wire mechanical coverage for all 25 spec red controls into `tools/run_scenarios.py` probes and/or `tools/audit_checks.py` CASES and/or `tools/validate.py` checks, covering at minimum: reload-before-audit (1), previous-conclusions-not-trusted (2), partial-scope-cannot-claim-full (3), later-rule-not-violation (4), accidental-success (5), vague-finding-rejected (6), sub-cannot-modify-main (7), one-report-owner (8), append-preserved (9), no-ticket-before-verification (10), duplicates-one-ticket (11), invalid-no-ticket (12), already-fixed-no-ticket (13), confidence-never-overrides-Core (14), cross-project-recurrence-required (15), weak-model-test-required (16), verify-is-delta-only (17), verify-cannot-recurse (18), original-finding-preserved (19), ticket-links-to-IMP (20), improve-does-not-enter-ADD (21), report-is-not-BOARD-state (22), cleanup-refuses-unswept (23), archive-preserves-provenance (24), partial-evidence-cannot-close (25). | needs: T-553, T-554, T-555, T-556, T-557, T-558, T-559 | verify: every one of the 25 has a named check or probe that demonstrably goes red when its rule is removed; run_scenarios green, audit_checks full-suite green with zero "not evidence" lines
- [ ] T-561 [P1] Ownership consolidation + the full closure gate for this wave. One owner per concept: the command rows in CORE.md §1.10, the audit chain in saipen/IMPROVE.md, the convergence lifecycle in CONVERGE.md (referenced, never restated), the report/finding schema in saipen/IMPROVE.md, SubSaipen boundaries in PROTOCOL.md. Then run the full applicable suite and report real PASS with fresh evidence -- no skipped, timed-out, inspection-only or reused result: validate.py, audit checks, floor parity, scenario suite, phase/command parity, shortcut parity, state schema, forced HUNT, `git diff --check`, `git status --short`. | needs: T-560 | verify: no concept is defined in two documents and the drift check proves it; every gate reports PASS with evidence produced this wave; `git diff --check` clean; BOARD/STATE/LOG/CHANGELOG truthfully describe the new command
- [ ] T-473 [P1] HELD, not scheduled: Rosary A3, the concurrent whole-file clobber guard. Witnessed twice this session -- a parallel session overwrote `BOARD.md` and reverted six `## DONE` moves whose work was already committed and pushed (E-1863), caught an hour later by CONFORMANCE 199 rather than at the moment of damage. § 1.5 already reads each file before writing it, so the guard is cheap: compare the content against what was read immediately before the write and refuse on a difference. It is held deliberately because it touches the checkpoint path every session runs and because T-442..T-451 already own the v8 Crew concurrency design; landing a collision detector ahead of that design risks pre-empting it. | needs: T-442 | verify: a read-then-foreign-write-then-write sequence is refused; an unchanged file writes normally; the guard stays a detector and never becomes a scope or locking scheme

## DONE
- [x] T-587 [P0] NITRO DOGFOOD II trust wave: unresolved CONFLICT + recovery verifier. Three P0 trust defects. (1) CONFLICT is wrongly a TERMINAL status: journal.TERMINAL = (COMMITTED, ABORTED, CONFLICT) and pending_ops() excludes it, so after recover() hits CONFLICT, pending_ops() returns [] and a new mutation is allowed -- contradicting recover() itself ("resolve explicitly before further mutation") and OPS recovery semantics. Reproduced: crash checkpoint after LOG, alter unfinished STATE, recover -> CONFLICT, pending_ops -> [], ticket_add succeeds creating a second committed op. (2) Recovery validates only write targets (before/after hashes) but ignores read-only preconditions captured by the original plan. Reproduced: crash checkpoint after LOG, change BOARD to DOING T-1 while STATE still says task:none, recover -> COMMITTED, then fast_check fails (STATE task none != BOARD DOING T-1). Recovery can mark an invalid repository COMMITTED, disproving "VERIFIED means verification actually ran" on the recovery path. (3) recover() marks VERIFIED then COMMITTED without running the operation's semantic verifier. Fix: lifecycle vocabulary -- SETTLED=(COMMITTED, ABORTED), UNRESOLVED=(PREPARED, APPLYING, VERIFIED, CONFLICT); pending_ops exposes pending and conflicts separately; every mutation refuses RECOVERY_CONFLICT when an unresolved CONFLICT exists; journal persists a verification_policy (closed registry: core_fast/improve_atomic_file/userperson/sub_lifecycle/none) so recovery rechecks read-only preconditions before roll-forward and runs the registered semantic verifier before VERIFIED; recovery final state goes through fast cross-file validation and CONFLICTs on failure instead of marking COMMITTED. status/next expose recovery_pending + recovery_conflict. saipen recover on conflict REFUSEs naming the exact op and preserves evidence. Two red controls: A (mutate unfinished write target -> CONFLICT, bytes preserved) and B NEW (mutate READ-ONLY dependency BOARD into a valid-but-incompatible state -> CONFLICT, no invalid COMMITTED), plus: new mutation after either unresolved conflict refuses RECOVERY_CONFLICT. | verify: reproduce R (crash after LOG, alter STATE, recover -> CONFLICT) then prove pending_ops() lists the conflict and every new mutation refuses RECOVERY_CONFLICT; control B: crash after LOG, change BOARD DOING while STATE task:none, recover -> CONFLICT (never COMMITTED), board bytes preserved; recovery with a read-dependency drift refuses before roll-forward; recovery runs the registered verifier and a semantically invalid recovered state can never become COMMITTED; status/next expose recovery_conflict; saipen recover on conflict REFUSEs naming the op; committed retry still ALREADY_APPLIED; validator + scenario + audit + floor green with fresh evidence; crash proofs run in subprocess (NITRO_CRASH_AFTER_LOG) | owner: claude | claim_time: 2026-08-09T15:03:23Z
- [x] T-586 [P1] NITRO M9: context compiler -- saipen context cold/hot/audit. Build the deterministic context compiler on the NOW-TRUSTWORTHY mechanical layer (saipen status/next, ProjectSnapshot, ticket parser, phase routing, log indexing). saipen context cold emits the minimal cold-start surface (STATE + BOARD + LOG tail + phase doc routing) as a compact bounded artifact a cold agent consumes instead of re-reading raw files; saipen context hot emits the current-work surface (status + next + active ticket + recent log + recovery_pending); saipen context audit emits a bounded token/bytes accounting per source so repeated unchanged bytes are measurable. All three read-only. The compiler consumes the engine's parsers and snapshot -- never duplicated parsing. Token optimization priority: fresh reasoning surface + repeated parsing + unnecessary bookkeeping, NOT nominal cached count at any cost. Stable protocol prefix ordering preserved. | verify: verify: TBD | owner: claude | claim_time: 2026-08-09T14:24:47Z
- [x] T-585 [P1] NITRO M8: SubSaipen operations on the common engine machinery. Mechanize only the deterministic SubSaipen lifecycle mechanics: spawn, status, adopt, collect, clean preflight, freshness checks -- reusing tools/freshness.py and tools/sub_clean.py where the logic already exists, but routing their mutation/file boundaries through the shared lock + journal + OperationPlan + Result + codec + dry-run primitives (saipen_engine). No semantic acceptance of a finding is mechanized: Core still judges work; SAIOPS guarantees boundaries and mechanics. A sub's OUTBOX writes, spawn/adopt state transitions and clean operations must be journaled, conflict-safe, and carry [op: ...] provenance like every other covered mutation. | verify: verify: TBD | owner: claude | claim_time: 2026-08-09T10:58:33Z
- [x] T-577 [P1] Repair the USERPERSON contract T-574 shipped with two false claims. REGRESSION A -- the "semantic merge" was lexical: _semantic_key() reduced a preference to its leading phrase, so "Prefer UI: Vintage Golden" and "Prefer UI: Material Design" both became "prefer ui" and the second could be silently discarded, while genuinely equivalent wordings were never merged. The helper must never pretend to understand semantics: preference identity is now structured (category key + exact text), the merge is deterministic lexical dedup, and semantic distillation is the AGENT job before calling the writer. Red controls cover the false-positive (distinct preferences sharing a leading phrase both kept) and false-negative (equivalent wordings not auto-merged) cases. REGRESSION B -- projection was vacuous: projection(role) returned a short scope sentence and the test only checked its length, proving nothing about selection. Now projection_policy(role) returns allowed categories and project_profile(profile, role) selects actual preferences into an auditable handoff (source fingerprint + selected entries, never the whole profile); the 4-preference fixture proves saiui/saitranslate/saihunt select the right subsets. | verify: tools/userperson.py ships the structured identity, the merge never drops a distinct preference sharing a leading phrase, a short-scope-string test no longer stands as projection evidence, and run_scenarios userperson probes (16) exercise both red controls plus the real projection selections | owner: claude | claim_time: 2026-08-09T10:39:19Z
- [x] T-584 [P0] NITRO integrity regression: closed M2-M5 controls permit invalid committed state. Class: FALSE_EVIDENCE / ACCIDENTAL_SUCCESS / ARCHITECTURE_LEVERAGE. External audit (2026-08-09) reproduced all 12 defect hypotheses in tools/nitro_integrity_repro.py against the live engine, disproving the guarantees T-579..T-582 claimed. Shared root causes: (A) state mutation is full-template reconstruction (_render_state) instead of owned-field patching -- checkpoint/ticket_add/set_goal_intent rewrite phase to SCOUT, fabricate SCOUT/none; (B) transaction journal journals paths but not per-target before/after hashes, so recovery clobbers intervening bytes and stages lie about arbitrary targets (MANIFEST reported as LOG_WRITTEN); (C) PLAN/APPLY is nominal -- plan_claim and apply_claim mint different op_ids and re-plan; (D) recovery lacks target-version conflict detection and pending-op preflight; (E) tests assert nearby outputs (checkpoint last_event, TODO->done as PASS) rather than the claimed invariant; (F) the mechanical path exists but is not mandatory for covered operations. Fix before further expansion: truthful generic transaction stages (role + before_hash + after_hash + applied per target), conflict-safe recovery, real OperationPlan consumed by APPLY, commit-failure-wins result merge, structured WRITER_BUSY/STALE_STATE, owned-field state patches, shared LOG event/parent builder, no unchanged write targets, pick-rule claim, active-ticket transition binding, legal lifecycle source states, goal/stop semantics, codec byte preservation, Improve path ownership + one active cycle + per-seat sweep coverage, dry-run zero bytes, module import floor, claim-to-proof red controls for every T-578..T-583 verify clause. | verify: every nitro_integrity_repro R1..R12 flips to NOT REPRODUCED with a behavioral red control replacing each proxy test; recovery conflict test preserves intervening bytes; plan/apply share one op_id; apply consumes exact planned bytes; committed retry ALREADY_APPLIED; saipen recover --json works; pending op blocks new mutation; state patches preserve unowned fields; checkpoint preserves phase/task/next_action; validator + scenario + audit + floor green with fresh evidence | owner: claude | claim_time: 2026-08-09T06:58:34Z
- [x] T-583 [P1] NITRO M6: migrate the Improve writers onto the common engine machinery. register_cycle currently creates the cycle directory BEFORE the MANIFEST replace, so a crash can expose .saipen/improve/<cycle>/ with no valid roster; register_seat and write_sweep_entry use independent read/modify/temp/replace patterns. Rewrite them as consumers of the common lock + journal + roll-forward primitives (saipen_engine.journal/lock/operations), fixing the crash-window via the general transaction primitive rather than a local trick. tools/improve.py keeps its semantic API; only the mutation internals change. | verify: crash injection after MANIFEST replace leaves a fully valid cycle (never a roster-less directory); a crash before the replace aborts with no partial cycle; register_seat and write_sweep_entry survive a crash mid-write with exactly one valid outcome; the general primitive fixed the class, not just the instance (no local special-case in tools/improve.py); 231/231 audit, suite green | owner: claude | claim_time: 2026-08-09T03:45:00Z
- [x] T-582 [P1] NITRO M5: goal/cc + stop/checkpoint mechanics on the engine. Mechanize execution_intent writes, goal_waves/goal_tickets counters, legal safety-valve reauthorization, pause representation and the exact resume key: saipen_engine/operations.py gains set_goal_intent, reauthorize_valve (conditional reset of both counters only when the owning rule says so), and stop_checkpoint (the brake: checkpoint + digest). The CONVERGE contract still decides stage order; Python only records the decided intent/counters/checkpoint. | verify: PASS -- goal/cc + stop mechanics are journalled SAIOPS operations: set_goal_intent pivots to execution_intent goal with counters from 0, reauthorize_valve refuses a valve that has not tripped (never a silent fresh budget), stop_checkpoint writes the resumable next_action + human digest; dry-run zero bytes; counters carried through the state renderer; the CONVERGE contract still decides stage order. 18 nitro-m3 behaviors; 231/231 audit, suite + floor green. E-2452
- [x] T-581 [P1] NITRO M4: ticket lifecycle operations on the common machinery. saipen_engine/operations.py gains ticket_add / ticket_done / ticket_block / ticket_unblock: add allocates the next canonical ticket ID (ignoring synthetic fixture IDs T-998/T-999 by scanning canonical BOARD + production LOG), checks duplicates and the needs DAG, inserts deterministically at the top of ## TODO, and validates the result; done moves ## DOING -> ## DONE with [x] and a verify result; block moves to ## BLOCKED with the blocker; unblock moves back. No raw BOARD mutation from agents: semantic fields (priority, description, needs, verify, blocker) are supplied by the model, structure is owned by Python. tools/saipen.py gains the five ticket subcommands with --dry-run and --json. | verify: PASS -- next_ticket_id skips the synthetic T-998/T-999 namespace (production scan of canonical BOARD + LOG), ticket_add inserts at ## TODO top with needs-DAG validation and never duplicates, done/block/unblock move exactly one ticket leaving STATE/BOARD/last_event consistent, dry-run writes zero canonical bytes, all mutations journalled + recoverable. 15 nitro-m3 behaviors; 231/231 audit, suite + floor green. E-2450
- [x] T-580 [P1] NITRO M3: saipen claim / transition / checkpoint as journalled SAIOPS operations. saipen_engine/operations.py implements claim_ticket(snapshot, request): checks ticket exists in TODO, needs satisfied, legal pick, no incompatible DOING, claim not live elsewhere, STATE/BOARD consistent; then in ONE run_mutation commit allocates the event, moves the ticket TODO->DOING with [/] owner claim_time, and sets STATE phase SCOUT task T-ID next_action PHASE SCOUT T-ID transition_from last_event updated agent. transition_phase checks VALID_TRANSITIONS, ticket-bearing requirements, the STATE.task/BOARD.DOING binding and the required checkpoint precondition. checkpoint writes a LOG event with allocated E-ID and parent. All mutation goes through the common lock + journal + roll-forward machinery; the model never hand-edits STATE/BOARD fields once these exist. PLAN/APPLY are separated (plan_claim returns the intended result or a refusal code with zero disk writes; apply_claim commits the verified plan). Stable error codes: TICKET_NOT_FOUND, TICKET_NOT_WORKABLE, ALREADY_CLAIMED, ILLEGAL_TRANSITION, STALE_STATE, WRITER_BUSY. tools/saipen.py gains claim/transition/checkpoint subcommands with --dry-run and --json. OPS.md owns the contract. | verify: PASS -- claim/transition/checkpoint are journalled SAIOPS operations: PLAN/APPLY separation, dry-run writes zero canonical bytes, claim moves exactly one ticket with no duplicate and ALREADY_CLAIMED on a DOING ticket, VALID_TRANSITIONS rejects illegal transitions with ILLEGAL_TRANSITION, ticket-bearing phases require a T-ID, event IDs allocated exactly once with real UTC, LOG/BOARD/STATE ordering preserved, recovery idempotent, CLI exposes --dry-run/--json, operation codes survive the commit merge. 12 nitro-m3 probes; 231/231 audit, suite + floor green. E-2448
- [x] T-579 [P1] NITRO M2: real OS single-writer lock + transaction journal + roll-forward recovery. saipen_engine/lock.py acquires the canonical project lock (msvcrt on Windows, fcntl on POSIX) at .saipen/locks/core.lock; the lock file carries no canonical truth and process death releases the OS lock; project path aliases resolve to the same lock identity. saipen_engine/journal.py writes per-operation journals under .saipen/recovery/ops/<op_id>/ (operation.json: op_id, operation, preconditions hashes, targets before/after, stage PREPARED/LOG_WRITTEN/BOARD_WRITTEN/STATE_WRITTEN/VERIFIED/COMMITTED/ABORTED/CONFLICT). Recovery is ROLL-FORWARD after LOG per CORE 1.5: LOG_WRITTEN rolls BOARD+STATE forward when hashes prove validity, never rolls back by deleting the append-only LOG event; unexpected target hash is CONFLICT with evidence preserved. Idempotent: a COMMITTED op returns ALREADY_APPLIED; an interrupted op is recovered first. Crash-injection harness (NITRO_CRASH_AFTER_PREPARE/LOG/BOARD/STATE) launches the operation in a subprocess and kills it at each point; saipen recover must produce exactly one valid final outcome. OPS.md owns the contract. | verify: PASS -- OS single-writer lock (msvcrt/fcntl, LOCK_NB, WRITER_BUSY, path-alias identity) + write-ahead journal + roll-forward recovery after the append-only LOG event; crash injection at PREPARE/LOG/BOARD/STATE each produces exactly one valid outcome (crash-before-LOG unchanged, after-LOG rolls forward once, after-BOARD rolls STATE, after-STATE validates+commits); recovery idempotent, committed retry ALREADY_APPLIED, no duplicate LOG event, second live writer refused. 17 nitro-m2 probes; 231/231 audit, suite + floor green; CI green. E-2444
- [x] T-578 [P1] NITRO M1: extract the shared mechanical parsing layer and project snapshot (saipen_engine/). One reusable stdlib implementation for STATE frontmatter, BOARD ticket structure and LOG events, consumed by validate.py (incrementally) and the new engine; no parser drift. ProjectSnapshot computes project_root, canonical project identity, state/board/log hashes, LOG tail E-ID and HEAD. saipen status and saipen next read-only commands are the first consumers. Characterization rule: before moving each parser, the validator result on the existing scenario/floor corpus must be identical before and after extraction. OPS.md owns the contract; KNOWLEDGE/NITRO.md owns the milestones. | verify: PASS -- saipen_engine/ shared parsers (state/board/log) imported by validate.py with characterization parity (validator + scenario suite + floor green); ProjectSnapshot captures hashes/log-tail/HEAD and detects a stale precondition; saipen status and saipen next are read-only with --json and no full-validator invocation; stdlib only. E-2441
- [x] T-552 T-552 [P0] DECIDE, with evidence, whether `saipen improve` is a PHASE at all -- and prefer that it is not. The original ticket assumed a new IMPROVE phase in CORE.md 1.6's enum plus a transition table row. Improve is a meta-control operation like `status` and `stop`, not ordinary project execution, and four properties say so: several seats may audit independently and concurrently; reports are read-only evidence, never canonical state; invoking improve during BUILD or VERIFY must not orphan the DOING ticket; and one global `STATE.phase: IMPROVE` cannot represent several independent audit seats at once. Adding global improve routing fields to STATE turns retrospective evidence collection into project execution state, which is the category error. Preferred design: `saipen improve` checkpoints current work if necessary, does NOT change phase/task/next_action, reloads the current protocol, audits this seat's observable context and writes only this seat's report; `saipen improve status` is read-only and shows each registered report as missing / draft / complete / swept; `saipen improve sweep` is Core-only, reads reports in deterministic order, verifies findings, deduplicates root causes, creates normal T-### TODO tickets and never silently pre-empts an existing DOING ticket; `saipen improve verify` is a bounded delta audit with no recursion; `saipen improve clean` archives report evidence and never enters the CLEAN phase. Normal Core phases are unchanged. A real IMPROVE phase may still be proposed, but only by first proving a failure the meta-control design cannot solve -- a command having a name is not a reason to add phase and state machinery. | verify: PASS -- meta-control decision recorded at E-2432 in saipen/IMPROVE.md: no IMPROVE phase row, phase count stays 16, no phases/improve.md (validator [improve-meta-control] checks + red control prove each), saipen improve never changes phase/task/next_action to audit, verify is delta-only without recursion. E-2432
- [x] T-570 T-570 [P1] Give an improve cycle a real seat roster, because "every agent reports" cannot otherwise be closed mechanically. `STATE.agent` stores only the LATEST actor, so historical participants cannot be inferred from it; LOG's `[agent:]` is a MAY field, so they cannot be inferred from there either. At cycle creation Core records the seats EXPECTED FOR THIS CYCLE in `.saipen/improve/<cycle_id>/MANIFEST.md`, which owns stable routing/identity and nothing else: `cycle_id`, `created_at`, project identity, `seat_id`, `role`, `report_path`, `availability` (expected or unavailable). The roster is NOT a second report: it carries no draft/complete/swept status -- the visible status is DERIVED (expected from roster-only; draft from `report_status: draft`; complete from `report_status: complete` with no Core disposition; swept from the sweep ledger's final disposition coverage; unavailable from the roster). One fact, one owner. Core is the SOLE manifest writer and seats never edit each other's report. A historical agent whose private or live context cannot be reached is `availability: unavailable`, never silently complete; Core MAY audit that agent's observable artifacts itself, and MUST label the scope as observable output only. | verify: PASS -- roster owns stable routing/identity only (cycle_id, created_at, project identity, seat_id, role, report_path, availability); register_cycle refuses a duplicate active cycle atomically, register_seat fails duplicate seat_ids and availability outside the closed set; derived status from roster + report + sweep; manifest-status red control goes red. E-2436
- [x] T-551 [P0] Fix report ownership AND per-seat isolation in the `saipen improve` specification before implementing anything on top of it. The spec writes each seat's report to `<saipen_home>/saipen_improve_<PROJECTNAME>.md`, but `saipen_home` is the SHARED protocol install (CORE.md 1.1, resolved in BOOT.md step 2), not project memory: two projects audited from one install collide on one path, the report never enters the project's own `.saipen/` history, and `saipen improve clean` would archive files inside the protocol tree. Canonical path, Core: `<project_root>/.saipen/improve/<cycle_id>/<seat_id>/saipen_improve_<PROJECTNAME>.md`. SubSaipen: `<sub_home>/improve/<cycle_id>/saipen_improve_<PROJECTNAME>.md`. The requested basename is preserved exactly; what changes is everything above it. `cycle_id` is ONE improve invocation or campaign; `seat_id` is ONE concrete auditing seat or session and NOT merely a model family -- `opencode-01`, `claude-01`, `codex-01`, `saiui`, `saihunt`. Two sessions both running `agent: opencode` MUST NOT resolve to one report unless they are deliberately the same registered seat, which is the collision the model-family reading guarantees. A second run by one seat APPENDS an immutable RUN section; an earlier RUN is never overwritten. Nothing is ever written under `saipen_home`. | verify: PASS -- report path defined once in saipen/IMPROVE.md + tools/improve.py.resolve_report_path, deriving from project_root (never saipen_home); validator + red control FAIL a report under saipen_home and FAIL two distinct seats resolving to one path; seat_key is one concrete seat, not a model family; append_run appends immutable RUN sections and a complete report refuses further RUNs (22 improve probes). E-2436
- [x] T-571 [P0] Resolve the Crew name collision BEFORE T-443 unblocks. Today `saipen crew` means a strictly SEQUENTIAL three-window circuit; T-442..T-451 call the future concurrent design Crew Mode. One name for "runs nothing in parallel" and for "multi-agent concurrency" is precisely the ambiguity a weak but compliant model resolves wrongly while believing it followed SAIPEN. Decide one of exactly two: (A) `saipen crew` stays sequential forever and the concurrent mode receives its own explicit command name; or (B) v8 deliberately changes `saipen crew`'s semantics as a breaking major-version migration, with the exact compatibility behaviour written down -- what a v7 invocation does on a v8 install, and what the v8 command does when concurrency is unavailable. Record the decision and its reasoning; implement no concurrency under this ticket. | verify: exactly one meaning per command name across CORE.md 1.10, the Crew backlog and every callout; the decision names which option was taken and why; if B, the compatibility behaviour is stated for both directions; validate.py FAILs a command surface where one name carries both meanings; T-443 cites this decision | verify: PASS -- Option A taken: saipen crew stays the strictly sequential circuit; the future concurrent design is renamed Concurrent Mode with the distinct command name saipen concurrent in .saipen/KNOWLEDGE/crew-v8-backlog.md, and T-443 cites the decision. CORE.md 1.10 crew row now states its single execution meaning (never the concurrent design); [crew-naming] validator check + two audit mutations prove one command cannot carry two execution meanings; crew.md (saicrew) is the sequential circuit itself. E-2428
- [x] T-574 [P2] Implement the optional USERPERSON meta-control, OFF by default. No `.saipen/USERPERSON.md` means no warning, no boot failure, no placeholder, no onboarding, no cold-start cost. `saipen userperson` when absent runs lightweight onboarding: maximum 3 broad questions, prefer 2, free-form pasted text may bypass the questions, distilling direction and preferences rather than biography; the canonical generated file is `.saipen/USERPERSON.md`, created only after explicit user activation. USERPERSON means default direction, not order: precedence is current explicit request > project/task requirements > SAIPEN > verified evidence > USERPERSON, and it is used only where a valid implementation choice remains unspecified. `add` merges semantically and never appends duplicate preference history; `reset` requires explicit destructive confirmation; only explicit user input modifies the profile (no passive profiling, no mining LOG/chat). Command surface: `saipen userperson`, `saipen userperson show`, `saipen userperson add <text>`, `saipen userperson remove <text>`, `saipen userperson reset`; no shortcut key this wave. When a USERPERSON preference materially influenced a decision the task did not specify, report it once at completion as `USERPERSON alignment:`; when one was deliberately overridden by stronger constraints, report `USERPERSON deviation:`; never emit either trace when USERPERSON had no material effect. SubSaipen handoffs project a relevant subset only (saiui UI/design+workflow, saitranslate localization, saiwiki documentation/communication, saihunt engineering rigor only where it affects its role) and record that a projection was supplied. Implement before the Improve wave so Improve can classify USERPERSON_MISS separately from PROTOCOL_VIOLATION/PROJECT_VIOLATION/LOGIC_ERROR. | verify: no USERPERSON file and no profile side effects on a bare cold start; onboarding is 2-3 questions max or a free-form paste; the five commands resolve in CORE.md 1.10 and the shortcut key-count sweep stays unchanged; add merges and reset requires confirmation; precedence and both report traces are documented and exercised by a scenario; a projection to a SubSaipen is recorded and never dumps the whole profile | verify: PASS -- USERPERSON meta-control implemented: tools/userperson.py (semantic merge, remove, parse/render, validate, projections, <=3 onboarding questions), CORE.md 1.10 rows for the five commands, SAIPEN_COMMANDS + MANIFEST + validator profile check; 15 probes + userperson-valid/userperson-malformed fixtures green; absence is silent (no cold-start cost); reset requires confirmation; precedence and both report traces documented and mechanically checked. E-2425
- [x] T-549 [P1] Document ownership consolidation + final gate suite. One canonical shortcut table (CORE.md §1.10) only; README/SKILL/adapters summarize and point, never duplicate. Every concept has one owner (command table / autonomous semantics in CONVERGE.md / hunt.md / clean.md / PROTOCOL.md / sai*.md / freshness section). Validator detects semantic drift between them. Run the full applicable suite and report real PASS (no skipped/timeout/inspection-only/old evidence): validate.py, audit checks, scenario suite, phase/command parity, shortcut parity, state schema, SubSaipen protocol/freshness/sync/prepare-collect/clean-safety tests, forced-HUNT test, goal migration tests, cc cold-continuation, ccc ship-order, git diff --check, git status --short. | verify: every gate reports PASS with fresh evidence; git diff --check clean; git status --short shows only intended files; BOARD/STATE/LOG/CHANGELOG truthfully describe the new contract | blocker: USER ASSIGNED FRESH EE/QQ PACKAGE PREPARATION TO OTHER MODELS; live validator correctly rejects both stale ready OUTBOXes until they regenerate them | owner: claude | claim_time: 2026-08-08T15:40:00Z | verify: PASS pre-SHIP -- SKILL.md's loader now says exactly: read BOOT once, BOOT loads STYLE before output, INDEX routes rule questions, CORE answers the exact one, RFC is a compatibility redirect and no step sends you there. The doubled BOOT instruction is gone (it was steps 1 and 3) and the frontmatter's orphaned `reached only when a rule question arises.` fragment with it. The check that let this ship was literal: every RFC-trap pattern required the string `RFC.md`, so bare-"RFC" routing was invisible -- a semantic rung now FAILs a routing verb aimed at RFC in any adapter, SKILL or injector, sentence-scoped so `RFC.md is a compatibility redirect` stays legal. Hand red-tested on four routing phrasings incl. the two exact sentences that shipped; one audit control (221/221, zero not-evidence). Full applicable suite on the final tree: validate.py --gate ship 3 FAILs (all the untracked-runtime files T-569 resolves at staging), audit_checks 221/221, run_scenarios all green, ruff clean, `git diff --check` clean. NOT YET COMMITTED -- ships with the wave.
- [x] T-550 [P0] The stopped session left 16 files modified and uncommitted -- a partially rolled-out T-537 (`saipen/CORE.md` §1.10 rows + shortcut table, `tools/validate.py` EXPECTED_SHORTCUT_ROUTES, `tools/audit_checks.py`, `saipen/CONFORMANCE.md`, `saipen/MAINTENANCE.md`, and 11 of the ~74 callout files) with nothing claimed in `## DOING` and no ticket owning it. No improve-wave commit may carry a half-applied `gg`/`cc` split: the next ship would publish a command surface where 11 locale callouts disagree with the other 63 and the validator's route map has moved under a table most files have not. Decide with evidence, do not guess: finish T-537's rollout and ship it, or revert the tree to HEAD. The full diff is preserved at `.saipen/kitchen/handoff/wip-T537-2026-08-07T1620Z.patch` (91686 bytes) so reverting loses nothing. | owner: claude | claim_time: 2026-08-07T16:24:00Z | verify: `git status --short` carries only files the currently claimed ticket owns; if landed, T-537 reaches `## DONE` with validate.py 0 FAILs and the shortcut-callout count consistent across every locale README/guide/mirror; if reverted, the LOG names the reason and the preserved patch path | verify: PASS -- validate.py 0 FAILs, audit_checks 165/165 with zero not-evidence (was 163/165), run_scenarios green, ruff clean, git diff --check clean. Disposition was LAND: the tree validated, so reverting would have thrown away a correct 1.10 rewrite. Shipped v7.210.0 (5ea371c, tag v7.210.0); E-2274..E-2283. T-537 stays open for its 28-locale remainder, which phases/translate.md gives to saitranslate.
## BLOCKED
- [ ] T-576 [P3] Prune or confirm .saipen/recovery/_ee_apply.py (25892 B) and .saipen/recovery/_ee_measure.py (839 B) -- untracked, zero references, orphaned EE scratch found by CLEAN (E-2430); they sit in the recovery evidence directory without belonging to any recovery snapshot and are not regenerable by a named command. Delete only after a human confirms they are abandoned; keep the two timestamped STATE snapshots beside them regardless. | verify: a human decision recorded (delete both, or keep with a reason); the two STATE recovery snapshots are never touched | owner: claude | claim_time: 2026-08-08T21:30:00Z | blocker: WAIT_USER_CONFIRMATION -- the two untracked orphaned scripts cannot be deleted without a human decision (clean.md section 2); no user confirmation exists in current evidence, so they stay untouched
- [ ] T-464 [P2] Own the `board-soft-cap` warning and prune the board, on CURRENT evidence. The ticket's original text said `## DONE` was not the cause and that no work on the ticket could clear the condition; both statements are now false, so the PERMANENT WARNING OWNER framing goes with them -- an owner whose own premise expired is a ticket that teaches the next reader to leave the warning alone forever. Measured at v7.215.0: BOARD.md is 84 KB against a ~16 KB soft cap, of which the validator attributes 62 KB to closed-ticket text. CLEAN owns this: archive or prune closed `## DONE` ticket bodies whose evidence already lives in LOG.md and CHANGELOG.md, keeping only the compact retention window Core requires; move the gated Crew design backlog (T-442..T-451) to KNOWLEDGE with ONE gate ticket left on the active board; remeasure. | verify: PASS -- board pruned 93.3 KB -> 25.1 KB; 62 DONE bodies pruned (T-399..T-561) whose evidence resolves to LOG.md/CHANGELOG.md; Crew backlog T-443..T-451 moved to .saipen/KNOWLEDGE/crew-v8-backlog.md with T-442 the one gate ticket left; T-473 needs repointed to T-442. Measured remainder 25.1 KB: improve-wave bodies T-551..T-561 12.1 KB (live work, unprunable), other TODO 6.8 KB, needs-referenced DONE T-549/T-550 3.7 KB, BLOCKED 2.5 KB. The 16 KB cap is unreachable while the improve wave is on the board | owner: claude | claim_time: 2026-08-08T18:30:00Z | blocker: PERMANENT WARNING OWNER -- board-soft-cap emits while the board is above ~16 KB and the improve-wave ticket bodies are live work that cannot be pruned; this ticket owns the warning with the measured remainder and closes only when the warning stops emitting
- [ ] T-407 [P2] Own the `goal-reauth-untripped` warning: E-1659 logged a `DEC: goal reauthorized` with counters under the § 2.4 caps, and LOG is immutable by append-only, so the warning emits forever. Keep this ticket live while the warning emits. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401) | blocker: PERMANENT WARNING OWNER -- the condition can never be met and immutable history is why. E-1659 logged a `DEC: goal reauthorized` under the caps, `LOG.md` is append-only, so the warning emits forever and no work closes it. Same trap as T-406: in `## TODO` the Pick Rule was obliged to select a ticket whose own `verify:` says closing it FAILs
- [ ] T-406 [P2] Own the `subsaipen-never-ran` warning for the life of the saipython sub: saipython was spawned on migration but never run, and MANIFEST.md advertises it live until its board opens, so the warning emits on every validation. Keep this ticket live while the warning emits; close only when the sub runs or is de-advertised. | verify: validator PASS while this ticket is live and names the slug; closing it while the warning emits fails the ownership check (T-401) | blocker: PERMANENT WARNING OWNER -- the condition can never be met and immutable history is why. `MANIFEST.md` advertises saipython live until its board opens, so the warning emits every validation; this ticket exists to own it, and its own `verify:` says closing it while the warning emits FAILs the ownership check. Sitting in `## TODO` it was a ticket the Pick Rule MUST take and no agent can finish. It leaves `## BLOCKED` when saipython actually runs or is de-advertised -- a real event, not work anyone can pick
- [ ] T-442 [P0] GATE: do not begin SAIPEN v8 Concurrent Mode while the sequential protocol still has known foundational defects. Unblock only when no open P0/P1 ticket owns an RFC contradiction, ambiguous state transition, unenforced MUST, source-of-truth conflict, ownership gap, recovery gap or validator blind spot; all canonical gates are green; and one ordinary sequential release completes without exposing another foundational defect. The detailed Crew design backlog (T-443..T-451) lives in KNOWLEDGE/crew-v8-backlog.md (moved there by T-464). | verify: the unblock decision names the closed defect classes, current green evidence and the exact sequential release used as the stability checkpoint; no Crew implementation exists before this ticket reaches DONE | blocker: HELD FOR SEQUENTIAL STABILITY
- [ ] T-575 [P0] GATE: V9-M1 resident-continue implementation may start ONLY after ACTUAL v8 Concurrent Mode exists and is stable, not merely after the v8 gate is allowed. Required v9 prerequisites: T-442 gate completed; T-443..T-450 implemented or decided; T-451 real-world Concurrent Mode soak completed; v8 released; at least one post-release v8 stability checkpoint green; current Improve cycle clean; HUNT/CLEAN baseline clean. Research, source mapping, isolated-kitchen prototypes, architecture documents and red controls remain allowed earlier (full design in .saipen/KNOWLEDGE/V9_RUNTIME.md; upstream a18809e MIT, provenance in saipen/runtime/UPSTREAM.json and THIRD_PARTY_NOTICES.md). No v9 runtime implementation before actual v8 completion. | verify: the unblock decision names each prerequisite with fresh evidence including the completed v8 soak and a green post-release v8 stability checkpoint; every copied/adapted unit is recorded in saipen/runtime/UPSTREAM.json and THIRD_PARTY_NOTICES.md; the 30 red controls in V9_RUNTIME.md are listed before any V9-M1 implementation | blocker: HELD UNTIL ACTUAL V8 EXISTS AND IS STABLE

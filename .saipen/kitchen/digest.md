done: v7.179.0 -- T-457 (compound conditions get compound fixtures; unpinned fail-fixtures are now a FAIL), T-458 (HUNT deletes only on a named proof of recovery), T-459 (clean-result cache needs a clean worktree, not just a matching commit)
remaining: T-460 tops ## TODO; 14 open below it, plus the gated v8 Crew backlog
awaiting: your call on the first-seat-identity source (T-456, ## BLOCKED), and on eight untracked .saipen/KNOWLEDGE notes -- thought4.md quotes `PHASE PLAN T-1` and a bare next_action, so the shipped-doc sweep FAILs the live workspace on them

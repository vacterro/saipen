diff --git a/.saipen/saitranslate/kitchen/ded/README_DED.md b/.saipen/saitranslate/kitchen/ded/README_DED.md
index 4210f05..96beef9 100644
--- a/.saipen/saitranslate/kitchen/ded/README_DED.md
+++ b/.saipen/saitranslate/kitchen/ded/README_DED.md
@@ -10,7 +10,7 @@
 
 **Одна команда. Ноль склероза.**
 
-**Короткие кнопки, чтоб пальцы не отсохли:** `cc` гонит активный Goal Mode дальше, `sss` докладывает статус и код не лапает, `ss` ставит чекпоинт и жмёт тормоз. [Вся карта из 15 шорткатов](saipen/RFC.md#110-command-surface); на русской раскладке работают `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
+**Короткие кнопки, чтоб пальцы не отсохли:** `cc` ведёт проект к схождению (продолжает активную цель, если она задана), `sss` докладывает статус и код не лапает, `ss` ставит чекпоинт и жмёт тормоз. [Вся карта из 15 шорткатов](saipen/RFC.md#110-command-surface); на русской раскладке работают `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
 
 **Язык ответов.** Агент по умолчанию отвечает на **эстонском** — это настройка, а не причуда, и больше ничего эстонского в SAIPEN нет. Меняется в одном месте: строка `reply_language:` в начале [`saipen/STYLE.md`](saipen/STYLE.md). `et` эстонский, `en` английский, `ru` русский, `auto` выбирает по языку твоего сообщения. Протокол, код, коммиты и все документы при любом значении остаются на английском.
 
diff --git a/.saipen/saitranslate/kitchen/et/README_ET.md b/.saipen/saitranslate/kitchen/et/README_ET.md
index b50f60c..31d84e5 100644
--- a/.saipen/saitranslate/kitchen/et/README_ET.md
+++ b/.saipen/saitranslate/kitchen/et/README_ET.md
@@ -10,7 +10,7 @@
 
 **Üks käsk. Null sõltuvust. Null amneesiat.**
 
-**Kiirklahvid:** `cc` jätkab aktiivset Goal Mode’i, `sss` näitab olekut koodi puudutamata ja `ss` salvestab kontrollpunkti ning peatub. [Vaata täielikku 15 kiirklahvi kaarti](saipen/RFC.md#110-command-surface). Kirillitsa kaksikud töötavad ka: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
+**Kiirklahvid:** `cc` viib projekti konvergentsini (jätkab käimasolevat eesmärki, kui see on seatud), `sss` näitab olekut koodi puudutamata ja `ss` salvestab kontrollpunkti ning peatub. [Vaata täielikku 15 kiirklahvi kaarti](saipen/RFC.md#110-command-surface). Kirillitsa kaksikud töötavad ka: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
 
 **Vastuste keel.** Agent vastab vaikimisi **eesti keeles** — see on säte, mitte veidrus, ja miski muu SAIPEN-is ei ole eestikeelne. Muuda seda ühes kohas: rida `reply_language:` [`saipen/STYLE.md`](saipen/STYLE.md) alguses. `et` eesti, `en` inglise, `ru` vene, `auto` valib selle järgi, mis keeles sa kirjutasid. Protokoll, kood, commitid ja kõik dokumendid jäävad igal väärtusel inglise keelde.
 
diff --git a/.saipen/saitranslate/kitchen/ja/README_JA.md b/.saipen/saitranslate/kitchen/ja/README_JA.md
index a3cefca..01a3e08 100644
--- a/.saipen/saitranslate/kitchen/ja/README_JA.md
+++ b/.saipen/saitranslate/kitchen/ja/README_JA.md
@@ -10,7 +10,7 @@
 
 **コマンド1つ。記憶喪失ゼロ。**
 
-**ショートカット:** `cc` は有効な Goal Mode を続行し、`sss` はコードを変更せず状況を表示し、`ss` はチェックポイントを保存して停止する。[全15キーの一覧](saipen/RFC.md#110-command-surface)。キリル文字の同形キーも使える: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`。
+**ショートカット:** `cc` はコンテキストを収束まで継続し（実行中の目標があればそれを再開）、`sss` はコードを変更せず状況を表示し、`ss` はチェックポイントを保存して停止する。[全15キーの一覧](saipen/RFC.md#110-command-surface)。キリル文字の同形キーも使える: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`。
 
 **パッケージ用キー:** `ee`/`qq` は翻訳/Wikiの完全なパッケージを準備するだけで、統合しない。`eee`/`qqq` は ready のパッケージだけを受け取り、統合・検証・レビュー後に push する。
 
diff --git a/GUIDE.md b/GUIDE.md
index 161e735..4e15f2a 100644
--- a/GUIDE.md
+++ b/GUIDE.md
@@ -10,7 +10,7 @@ This is the fix for that one thing.
 
 **SAIPEN fixes that.** Tiny `.saipen/` folder in project. Agent reads STATE, BOARD, next_action. Resumes where other stopped. Zero briefing.
 
-**Fast keys:** `cc` continues an active Goal Mode run, `sss` checks status without touching code, and `ss` checkpoints and stops. [Full 15-key map](saipen/RFC.md#110-command-surface); Cyrillic twins `сс`, `ссс`, `аа`, `ее`, `еее`, `рр` work too.
+**Fast keys:** `cc` continues the project context to convergence, `sss` checks status without touching code, and `ss` checkpoints and stops. [Full 15-key map](saipen/RFC.md#110-command-surface); Cyrillic twins `сс`, `ссс`, `аа`, `ее`, `еее`, `рр` work too.
 
 **Package keys:** `ee`/`qq` prepare complete translation/wiki packages without integrating; `eee`/`qqq` accept only ready packages, then integrate, verify, review, and push.
 
diff --git a/README.ded.md b/README.ded.md
index 4fc8306..2bc1453 100644
--- a/README.ded.md
+++ b/README.ded.md
@@ -17,7 +17,7 @@
 
 **Одна команда. Ноль зависимостей. Ноль амнезии. Всё по хардкору.**
 
-**Короткие кнопки, чтоб пальцы не отсохли:** `cc` гонит активный Goal Mode дальше, `sss` докладывает статус и код не лапает, `ss` ставит чекпоинт и жмёт тормоз. [Вся карта из 15 шорткатов](saipen/RFC.md#110-command-surface); на русской раскладке работают `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
+**Короткие кнопки, чтоб пальцы не отсохли:** `cc` ведёт проект к схождению (продолжает активную цель, если она задана), `sss` докладывает статус и код не лапает, `ss` ставит чекпоинт и жмёт тормоз. [Вся карта из 15 шорткатов](saipen/RFC.md#110-command-surface); на русской раскладке работают `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
 
 **Язык ответов.** Агент по умолчанию отвечает на **эстонском** — это настройка, а не причуда, и больше ничего эстонского в SAIPEN нет. Меняется в одном месте: строка `reply_language:` в начале [`saipen/STYLE.md`](saipen/STYLE.md). `et` эстонский, `en` английский, `ru` русский, `auto` выбирает по языку твоего сообщения. Протокол, код, коммиты и все документы при любом значении остаются на английском.
 
diff --git a/README.ee.md b/README.ee.md
index 2a681d6..a8dd4a0 100644
--- a/README.ee.md
+++ b/README.ee.md
@@ -11,7 +11,7 @@
 
 **Üks käsk. Null sõltuvust. Null amneesiat.**
 
-**Kiirklahvid:** `cc` jätkab aktiivset Goal Mode’i, `sss` näitab olekut koodi puudutamata ja `ss` salvestab kontrollpunkti ning peatub. [Vaata täielikku 15 kiirklahvi kaarti](saipen/RFC.md#110-command-surface). Kirillitsa kaksikud töötavad ka: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
+**Kiirklahvid:** `cc` viib projekti konvergentsini (jätkab käimasolevat eesmärki, kui see on seatud), `sss` näitab olekut koodi puudutamata ja `ss` salvestab kontrollpunkti ning peatub. [Vaata täielikku 15 kiirklahvi kaarti](saipen/RFC.md#110-command-surface). Kirillitsa kaksikud töötavad ka: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
 
 **Vastuste keel.** Agent vastab vaikimisi **eesti keeles** — see on säte, mitte veidrus, ja miski muu SAIPEN-is ei ole eestikeelne. Muuda seda ühes kohas: rida `reply_language:` [`saipen/STYLE.md`](saipen/STYLE.md) alguses. `et` eesti, `en` inglise, `ru` vene, `auto` valib selle järgi, mis keeles sa kirjutasid. Protokoll, kood, commitid ja kõik dokumendid jäävad igal väärtusel inglise keelde.
 
diff --git a/README.ja.md b/README.ja.md
index 5e68b29..6f01046 100644
--- a/README.ja.md
+++ b/README.ja.md
@@ -17,7 +17,7 @@
 
 **コマンド1つ。依存関係ゼロ。記憶喪失ゼロ。**
 
-**ショートカット:** `cc` は有効な Goal Mode を続行し、`sss` はコードを変更せず状況を表示し、`ss` はチェックポイントを保存して停止する。[全15キーの一覧](saipen/RFC.md#110-command-surface)。キリル文字の同形キーも使える: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`。
+**ショートカット:** `cc` はコンテキストを収束まで継続し（実行中の目標があればそれを再開）、`sss` はコードを変更せず状況を表示し、`ss` はチェックポイントを保存して停止する。[全15キーの一覧](saipen/RFC.md#110-command-surface)。キリル文字の同形キーも使える: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`。
 
 **パッケージ用キー:** `ee`/`qq` は翻訳/Wikiの完全なパッケージを準備するだけで、統合しない。`eee`/`qqq` は ready のパッケージだけを受け取り、統合・検証・レビュー後に push する。
 
diff --git a/README.md b/README.md
index e7b21b7..f513854 100644
--- a/README.md
+++ b/README.md
@@ -21,7 +21,7 @@ rebriefing, any vendor, any day.
 
 **One command. Zero dependencies. Zero amnesia.**
 
-**Fast keys.** A shortcut is the entire message, never a prefix. `cc` keeps an active Goal Mode run moving, `sss` reports status without touching code, and `ss` checkpoints and stops. [See the full 15-key map](saipen/RFC.md#110-command-surface). Cyrillic twins work too: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
+**Fast keys.** A shortcut is the entire message, never a prefix. `cc` continues the project context to convergence (resuming a running goal if one is set), `sss` reports status without touching code, and `ss` checkpoints and stops. [See the full 15-key map](saipen/RFC.md#110-command-surface). Cyrillic twins work too: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
 
 **Reply language.** The agent answers in **Estonian** by default — that is a setting, not a quirk, and nothing else about SAIPEN is Estonian. Change it in one place: the `reply_language:` line at the top of [`saipen/STYLE.md`](saipen/STYLE.md). `et` Estonian, `en` English, `ru` Russian, `auto` picks from the message you sent. The protocol, the code, the commits and every document stay English at every value.
 
diff --git a/guides/GUIDE_DED.md b/guides/GUIDE_DED.md
index a628bb6..6557586 100644
--- a/guides/GUIDE_DED.md
+++ b/guides/GUIDE_DED.md
@@ -10,7 +10,7 @@
 
 **SAIPEN** — это брезентовый планшет в папке `.saipen/` прямо у тебя в проекте.
 
-**Короткие кнопки, чтоб пальцы не отсохли:** `cc` гонит активный Goal Mode дальше, `sss` докладывает статус и код не лапает, `ss` ставит чекпоинт и жмёт тормоз. [Вся карта из 15 шорткатов](../saipen/RFC.md#110-command-surface); на русской раскладке работают `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
+**Короткие кнопки, чтоб пальцы не отсохли:** `cc` ведёт проект к схождению (продолжает активную цель, если она задана), `sss` докладывает статус и код не лапает, `ss` ставит чекпоинт и жмёт тормоз. [Вся карта из 15 шорткатов](../saipen/RFC.md#110-command-surface); на русской раскладке работают `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
 
 **Пакетные кнопки:** `ee`/`qq` собирают полный пакет переводов/вики, но в проект лапы не суют; `eee`/`qqq` берут только готовый пакет, внедряют, проверяют и пушат.
 
diff --git a/guides/GUIDE_EE.md b/guides/GUIDE_EE.md
index e6357b5..31c29bb 100644
--- a/guides/GUIDE_EE.md
+++ b/guides/GUIDE_EE.md
@@ -10,7 +10,7 @@ See on lahendus täpselt sellele ühele asjale.
 
 **SAIPEN** on lihtsalt üks kuradi märkmik kaustas `.saipen/`.
 
-**Kiirklahvid:** `cc` jätkab aktiivset Goal Mode'i, `sss` näitab olekut koodi puudutamata ja `ss` salvestab kontrollpunkti ning peatub. [Täielik 15 kiirklahvi kaart](../saipen/RFC.md#110-command-surface); toimivad ka kirillitsa vasted `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
+**Kiirklahvid:** `cc` viib projekti konvergentsini (jätkab käimasolevat eesmärki, kui see on seatud), `sss` näitab olekut koodi puudutamata ja `ss` salvestab kontrollpunkti ning peatub. [Täielik 15 kiirklahvi kaart](../saipen/RFC.md#110-command-surface); toimivad ka kirillitsa vasted `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
 
 **Paketiklahvid:** `ee`/`qq` valmistavad täieliku tõlke- või vikipaketi ette ilma seda lõimimata; `eee`/`qqq` võtavad vastu ainult valmis paketi, seejärel lõimivad, kontrollivad, vaatavad üle ja lükkavad üles.
 
diff --git a/guides/GUIDE_RU.md b/guides/GUIDE_RU.md
index 28108a0..80685a4 100644
--- a/guides/GUIDE_RU.md
+++ b/guides/GUIDE_RU.md
@@ -12,7 +12,7 @@
 
 Агент просыпается, открывает этот блокнот (`STATE.md` и `BOARD.md`), видит, на какой строчке кода он вчера остановился, и идёт вкалывать дальше. Без лишних соплей и пересказов сюжета.
 
-**Быстрые клавиши:** `cc` продолжает активный Goal Mode, `sss` показывает статус без правок кода, `ss` сохраняет чекпоинт и жмёт тормоз. [Полная карта из 15 шорткатов](../saipen/RFC.md#110-command-surface); русская раскладка тоже работает: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
+**Быстрые клавиши:** `cc` ведёт проект к схождению (продолжает активную цель, если она задана), `sss` показывает статус без правок кода, `ss` сохраняет чекпоинт и жмёт тормоз. [Полная карта из 15 шорткатов](../saipen/RFC.md#110-command-surface); русская раскладка тоже работает: `сс`, `ссс`, `аа`, `ее`, `еее`, `рр`.
 
 **Пакетные шорткаты:** `ee`/`qq` готовят полный пакет переводов/вики без внедрения; `eee`/`qqq` принимают только готовый пакет, затем внедряют, проверяют и пушат.
 
diff --git a/saipen/CONFORMANCE.md b/saipen/CONFORMANCE.md
index 557956e..e5084ba 100644
--- a/saipen/CONFORMANCE.md
+++ b/saipen/CONFORMANCE.md
@@ -232,9 +232,9 @@ Current IDs are canonical; old changelog references remain historical.
 | 204 | No shipped file instructs an agent to write a SUPERSEDED schema version. § 1.2's legacy-upgrade sentence -- the single instruction for escaping legacy state -- said "MUST upgrade to v2" for as long as the schema had been at 3, so obeying it produced state the validator WARNs as legacy on the spot. Nothing could see it, because a version number reads as fact rather than as a claim. The same rot had the shipped subSaipen TEMPLATE frozen at schema 1, so every `saipen sub spawn` ever run was born legacy, and all four live instances still sit there. Current is fine and ABOVE current is fine (that is how a doc describes a future-schema example); only a superseded literal is rot, and § 1.2 now names no number at all. Live subSaipen states WARN rather than FAIL -- the upgrade is that sub's own duty at its own next checkpoint (T-436) | Enforced directly by `tools/validate.py` -- RFC, `phases/`, `extensions/`, `KNOWLEDGE/`, BOOT, STYLE, CONFORMANCE and both shipped STATE templates are swept; red-controlled twice in `tools/audit_checks.py` (the § 1.2 sentence re-hardcoded, and the TEMPLATE pushed back to schema 1) |
 | 205 | § 2.1's ZERO-PROMPT rule declares its exception list complete and names both carve-outs. It ordered an unconditional `HUNT` and then "MUST immediately transition to `ADD`" if clean, naming exactly one exception -- while § 1.3 bans `ADD` outright under `mode: read-only`, whose entire work product is tickets a read-only agent cannot write. Each rule read as followed on its own and the MUST ordered a phase the mode forbids. Third instance of one shape in this document: § 1.3's own ban list once named four phases as though exhaustive, and § 1.6's from-any-phase set named five where § 1.10 had seven. An exception list that does not say whether it is complete is the defect (T-437) | Enforced directly by `tools/validate.py` -- § 2.1 must carry both the completeness declaration and the read-only carve-out, and a moved section anchor FAILs rather than skipping; red-controlled in `tools/audit_checks.py` by softening the ban to a preference |
 | 206 | A record another rule is required to READ has a form the reader can find. § 1.10 orders `saipen status` to report "the result of the last `tools/validate.py` run if one is recorded in `LOG.md`", and nothing had ever given that record a shape -- so the report either greps prose and guesses or truthfully answers "none recorded" over a LOG that holds the answer. Same argument § 2.4 already won for its counters, where the exact text `DEC: goal_waves N->M` exists precisely so § 1.5's rebuild can count them (row 51). § 1.2 now fixes `RUN: validate.py -> PASS` / `-> FAIL`, free to continue with counts or commentary after it. WARN, not FAIL: a project may legitimately never have run the validator, and saying so out loud is the honest answer rather than an invented one. This repository tripped it on the first run -- 1800-plus events, not one findable conformance record (T-438) | Enforced directly by `tools/validate.py` on the ACTIVE log -- status wants the last run, `BOOT.md`'s cold start reads the active tail and nothing else, and a record sealed into cold storage is by definition not the current answer; a freshly sealed log therefore warns until the next run, which is the honest reading of that state. Red-controlled in `tools/audit_checks.py` by breaking the fixed form on every real line |
-| 207 | A shortcut row's Notes describe the destination it actually routes to, bare form included. `cc`'s claimed the key starts Goal Mode, while § 1.10 forbids bare `saipen goal` from setting the flag, writing a counter or planning -- at `goal_mode: false` it replies with the usage line and stops, and under a live run it is a resume. Only `cc <objective>` pivots. The most-used key promised an outcome its own row cannot produce, one column over from the greeting misread row 201 closed: a Notes column describing a different command is the table lying about its own row (T-439) | Enforced directly by `tools/validate.py` -- the row must state that the pivot needs text, and the superseded promise is rejected as a literal; red-controlled in `tools/audit_checks.py`. The explaining sentence had to be written around the banned literal, the same way row 104's palette guard assembles its superseded name in code |
+| 207 | A shortcut row's Notes describe the destination it actually routes to, bare form included. The historical defect was `cc` claiming the bare key starts Goal Mode while § 1.10 forbids bare `saipen goal` from setting the intent, writing a counter or planning. The newer defect is sharper: `cc` and `gg` both routed to `saipen goal`, duplicating semantics so a bare `cc` could not continue context when no goal run was active. One key, one purpose is now the rule: `gg` is the sole short route for a NEW goal (bare `gg` = `Use: gg <objective text>`, never a continuation alias), and `cc` routes to `saipen continue` (converge/continue-context, never a goal alias). A Notes column describing a different command than its own row is the table lying about its row (T-439, T-537) | Enforced directly by `tools/validate.py` -- the route map is a named constant, every row routing to `saipen goal` must state that the pivot needs text and what its BARE form does, and a row reassigning `cc` back to `saipen goal` is rejected as an assigned-destination change; red-controlled in `tools/audit_checks.py` |
 | 208 | CLEAN's board scrub keeps the dependency graph intact. `phases/clean.md` step 1 pruned `## DONE` tickets "older than the current active work" with no check for inbound `needs:`, while RFC § 1.2 answers a `needs:` pointing at a ticket that exists nowhere on the board with `## BLOCKED` and a `| blocker: needs nonexistent T-###`. So the phase whose job is keeping the board honest could mechanically block a workable ticket, and the block read as a real dependency failure rather than as damage the scrub had just done. Found by executing the rule rather than by reading it: a live prune at E-1811 dropped T-421 and T-422 dangled on the next validation. The remedy is refusing the prune, never repairing the dangle afterwards -- the dangling check is behaving correctly and is not the thing to soften (T-440) | Enforced directly by `tools/validate.py` -- `clean.md`'s scrub must carry the guard; red-controlled in `tools/audit_checks.py` by softening the MUST NOT to a preference |
-| 209 | The MARKHUNT brake has a legal wording, and the deadlock check stops exempting whole situations from itself. `phases/done.md` and `phases/markhunt.md` both held that untriaged MARKHUNT findings must stop the auto-proceed to `HUNT` -- sound, since those findings are unvetted by construction -- while § 1.2 whitelisted two `WAIT:` forms at `DONE` with an empty `## TODO` and § 1.11 ordered an agent to ignore any third. So a doc-following agent wrote a state the constitution called drift, and `KNOWLEDGE/traps.md` defended it by claiming such a board "has not halted" -- contradicting § 2.1's own definition, which says `## DONE` and `## BLOCKED` tickets never count against the halt. § 1.2 now carries the third fixed wording; the tool had already carved the case out of one check while the other still warned. Two exemptions died with it: the deadlock FAIL was switched off entirely under `execution_intent: goal` -- the one mode where an unattended run parks on a WAIT nobody was asked -- and the MARKHUNT case was skipped wholesale rather than checked against a wording (T-453) | Enforced directly by `tools/validate.py` -- one helper now decides the whitelist for both the WARN and the FAIL; the goal-mode branch is hand red-tested only and says so: it needs an empty `## TODO` and a bogus WAIT together, and an audit case mutates one file, so a single-file control proves nothing. Tried, went not-red, removed rather than left standing as coverage. That limit is now closed, and not by new machinery: `tests/scenarios/done-wait-deadlock-goal-mode/` constructs the whole state -- `goal_mode: true` with counters under the caps, an empty `## TODO`, and a non-whitelisted WAIT -- and restoring the old intent exemption makes exactly that fixture stop failing while its intent-less twin is unaffected, which is what makes it evidence rather than noise (T-457). The legal brake is confirmed silent |
+| 209 | The MARKHUNT brake has a legal wording, and the deadlock check stops exempting whole situations from itself. `phases/done.md` and `phases/markhunt.md` both held that untriaged MARKHUNT findings must stop the auto-proceed to `HUNT` -- sound, since those findings are unvetted by construction -- while § 1.2 whitelisted two `WAIT:` forms at `DONE` with an empty `## TODO` and § 1.11 ordered an agent to ignore any third. So a doc-following agent wrote a state the constitution called drift, and `KNOWLEDGE/traps.md` defended it by claiming such a board "has not halted" -- contradicting § 2.1's own definition, which says `## DONE` and `## BLOCKED` tickets never count against the halt. § 1.2 now carries the third fixed wording; the tool had already carved the case out of one check while the other still warned. Two exemptions died with it: the deadlock FAIL was switched off entirely under `execution_intent: goal` -- the one mode where an unattended run parks on a WAIT nobody was asked -- and the MARKHUNT case was skipped wholesale rather than checked against a wording (T-453) | Enforced directly by `tools/validate.py` -- one helper now decides the whitelist for both the WARN and the FAIL; the goal-mode branch is hand red-tested only and says so: it needs an empty `## TODO` and a bogus WAIT together, and an audit case mutates one file, so a single-file control proves nothing. Tried, went not-red, removed rather than left standing as coverage. That limit is now closed, and not by new machinery: `tests/scenarios/done-wait-deadlock-goal-mode/` constructs the whole state -- `execution_intent: goal` with counters under the caps, an empty `## TODO`, and a non-whitelisted WAIT -- and restoring the old intent exemption makes exactly that fixture stop failing while its intent-less twin is unaffected, which is what makes it evidence rather than noise (T-457). The legal brake is confirmed silent |
 | 210 | `saipen hunt` can actually enter HUNT, and forcing a sweep forces one. Three surfaces carried one defect. § 1.6's from-any-phase set listed six phases and omitted `HUNT`, so the DFA's only route in was `DONE -> HUNT` and invoking the command from `BUILD` or `REVIEW` produced a transition this validator rejects while the agent did exactly what § 1.10 said -- the same two-halves disagreement row 61 records for `SHIP`, one phase over. § 2.1 phrased the halt as a precondition on the phase rather than on the autonomous transition, which read literally makes the command refuse on any board with open work. `phases/hunt.md`'s hash skip had no carve-out, so the one command whose whole purpose is forcing a sweep was a documented no-op on an unchanged tree. A fourth copy fell out with them: § 1.2 restated the from-any-phase set as "the five user-command phases", already missing `PLAN` before `HUNT` joined, and now points at § 1.6 instead of keeping a hand-kept list (T-454) | Enforced directly by `tools/validate.py` -- `ANY_FROM` is compared against § 1.6's own sentence, and both the § 2.1 phrasing and hunt.md's carve-out are pinned with a moved anchor FAILing rather than skipping; three red controls in `tools/audit_checks.py`, one per surface |
 | 211 | The first-publish gate runs before the act it authorizes. `phases/ship.md` cleared it at step 7, after the branch push at step 5 and the tag push at step 6 -- its own `WAIT:` text reading "before I push" about a push two steps behind it. On the single run the gate exists for, the one-way door opened before the user was asked; with no `origin` at all the push failed first and dropped into generic push recovery, which never asks the question. RFC § 2.4's SHIP exception makes the confirmation a MUST that the goal intent cannot waive and § 1.1 makes it a destructive-op gate, and ship.md is the doc that emits both. Remote classification is now step 5, decided while everything is still local, and re-read immediately before each external write because a remote someone else published to in between makes the first answer stale (T-452) | Enforced directly by `tools/validate.py` -- it compares the gate's position against the branch push in the document itself, so the ordering cannot silently invert, and FAILs rather than skips if either anchor disappears; red-controlled in `tools/audit_checks.py` and by hand in both directions |
 | 212 | The release tag is pushed only after the branch push has landed, and a published tag whose commit is on no remote branch is a defect the validator names. `phases/ship.md` step 7 made the tag push a separate command from the branch push, and step 10's non-fast-forward recovery claimed a tag from a failed attempt was "by definition never successfully pushed" -- a claim that holds only while the tag rides the branch push. Run them in order and a rejected branch push is followed by a tag push that SUCCEEDS, publishing a tag on a commit that is on no remote branch. Done twice in this repository: E-1787 (v7.171.0) and E-1882 (v7.176.0), the second time live with the orphaned tag still published. The fix is ordering, not prose: the tag ref must not be pushed until the branch push has landed, and `validate.py` FAILs a local tag whose commit is on no remote branch while a remote of the same name exists (T-467) | Enforced directly by `tools/validate.py` -- FAIL [orphaned-release-tag] on a local v* tag whose commit is unreachable from every refs/remotes branch while the remote carries the same tag name, standing down where the remote is unreadable rather than guessing; the tag-after-branch ordering is pinned in the same cross-doc check that pins the first-publish gate. Red-controlled twice: `tools/audit_checks.py` softens the step-7 gate, and `tools/run_scenarios.py` builds a real repository and remote and replays the rejected-branch-then-tag sequence, failing red and green after the branch lands |
@@ -243,13 +243,13 @@ Current IDs are canonical; old changelog references remain historical.
 | 215 | REVIEW re-runs the ticket's own `verify:` instead of reading VERIFY's claim of it. The phases were already separate, with their own caps and their own verdict vocabulary, but nothing stopped REVIEW from taking the previous phase's green as given -- so a claim reached SHIP on the strength of the phase that produced it, and a phase reporting on its own work is the one report nothing downstream can contradict. Borrowed from Rosary's generator/evaluator split, minus the second process: same check, run twice, is the cheapest independence available to a protocol with one agent in the seat. Observed at E-1867, where VERIFY was reported green and the audit harness was red on the same tree (T-469) | Enforced directly by `tools/validate.py` -- review.md must carry the clause; red control softens it back to reusing VERIFY's result |
 | 216 | Three borrowed invariants land in the docs that emit them, each one sentence. `phases/verify.md` states that its ladder runs cheapest-first and that the first failed MANDATORY gate ends the PASS claim for that pass -- so a green from a higher rung cannot read as repairing a red one below it, and an advisory gate must be declared advisory rather than assumed. `phases/scout.md` writes the project's harness and build commands into `KNOWLEDGE/` once and cites them afterwards, because two agents each deriving a build command is how one verifies something nobody asked for and reports it green. `BOOT.md` stops implying `next_action` stands alone: it is the previous session's pre-computed Pick Rule result, `tools/validate.py` re-derives it but the portable floor cannot (a grep will not walk a `needs:` graph), so a cold agent confirms the pick against `BOARD.md` -- immediate means without asking, never without looking. No per-gate ledger and no new file: `| verify:` and the LOG `RUN:` lines already carry command and result (T-470, T-471, T-472) | Enforced directly by `tools/validate.py` as five marker checks across three documents; five red controls in `tools/audit_checks.py`, each softening the MUST the way a later editor plausibly would rather than deleting the sentence |
 | 217 | A command the user just typed outranks the previous session's pre-computed `next_action`, and § 1.11's priority list says so in the list rather than around it. `BOOT.md` step 7 ordered `next_action` executed immediately; § 1.10 ordered a declared shortcut executed as the exact row it resolves to and never answered as a greeting; § 1.11 fixed the action priority for when more than one thing looks doable and named five steps, none of which was "the user just typed a command". Two live MUSTs, no precedence, and at a cold start `BOOT.md` is the file an agent reaches first -- so the newest fact in the session lost to the oldest. It cost the same key twice: E-1913 lost a bare `qq` to a stale `PHASE SCOUT T-455`, and a second agent's session lost it to the same pick again, recovering only because the user asked about it directly. `OBEY` sits below `RECOVER` (every command's first act is reading the state it would run against) and above `UNBLOCK` (that step exists to stop the AGENT finding its own work, and the user is not the agent), clears a live `WAIT:` rather than restating it, and waives none of § 1.10's gates or § 1.1's destructive-op floor. The insertion also moved three of the five original step numbers, so every cross-reference into the list now names the step instead of its number (T-474) | Enforced directly by `tools/validate.py` -- both halves pinned, since either one alone restores the tie, and the check FAILs rather than skips if the § 1.11 anchor moves; two red controls in `tools/audit_checks.py` soften the rule and the BOOT conditional independently |
-| 218 | A shortcut row's Notes requirement is derived from the row's ROUTE, not from the key's name. Row 207 rewrote `cc`'s Notes because they promised an outcome bare `saipen goal` is forbidden to produce, and the check it shipped named `cc` -- so `gg`, which routes to the same command, kept the identical superseded promise one row further up the same table, where it had been since the table was written. Fixing the instance and leaving the class is the shape this repository has now closed four times (the palette name in 46 files, the seven adapters, the `WAIT:` form in 33 guides, and here); keying on the route means the requirement is inherited by any future row assigned to `saipen goal` rather than escaping it by not being named. Each such row must state that the pivot needs text and what its BARE form does -- a resume under a live `goal_mode: true`, the § 1.10 usage line at `goal_mode: false` -- and re-authorization is named as conditional on § 2.4's valve having tripped (T-475) | Enforced directly by `tools/validate.py`, which scans every shortcut row whose route names `saipen goal` and reports the offending keys by name; the superseded literal stays banned section-wide. Two red controls in `tools/audit_checks.py`, one per row, so repairing either one alone leaves the other red |
+| 218 | A shortcut row's Notes requirement is derived from the row's ROUTE, not from the key's name. The old fix rewrote `cc`'s Notes because they promised an outcome bare `saipen goal` is forbidden to produce, and the check named `cc` -- so `gg`, which routed to the same command, kept the identical superseded promise. Keying on the route means the requirement is inherited by any row assigned to `saipen goal` rather than escaping it by not being named. After the gg/cc split only `gg` routes to `saipen goal`, so exactly one row carries the requirement: the pivot needs text and the bare form is the `Use: gg <objective text>` usage line, never a resume and never a continuation alias (T-475, T-537) | Enforced directly by `tools/validate.py`, which scans every shortcut row whose route names `saipen goal` and reports the offending keys by name; the superseded literal stays banned section-wide. Red-controlled in `tools/audit_checks.py` |
 | 219 | A `WAIT:` body is one sentence. Row 55's category token bounds what KIND of stop a `WAIT:` is and bounds nothing after it, so `next_action` -- the one field every cold agent executes -- became a place to leave session notes. A stop instruction carrying notes is a stop instruction the next agent reads as a queue, and this repository proved it on itself: a `user brake` whose body ran three sentences of handoff status and finished by naming a ticket as still open "for a future run" was answered by the next agent scouting that ticket rather than honouring the brake. The report belongs in `.saipen/kitchen/digest.md`, which `saipen stop` already writes for exactly that purpose, and queued work belongs on `BOARD.md`, where the Pick Rule can see it instead of it arriving through a field that outranks the board (T-476) | Enforced directly by `tools/validate.py`: the boundary is a period followed by whitespace and then a capital or a backtick, so a version string and a lowercase abbreviation do not trip it while a real second sentence does, and the optional trailing `[...]` progress tag is stripped before the test. Checked against all three fixed DONE+empty-TODO forms, which are unaffected; red-controlled in `tools/audit_checks.py` by appending a status clause to a legal brake |
 | 220 | Proposal Mode's halt has one legal form, and for six months it had none. `phases/plan.md` step 4 ordered `STATE.phase: DONE` plus a halt to await user selection, forbade a `WAIT:` prefix as "a violation of RFC § 1.2", and forbade proceeding to `SCOUT` -- which leaves only the four prefixes that each mean "do this now", so the halt could be recorded only as an action the agent was forbidden to perform, and a cold agent reading `PHASE SCOUT T-###` executes it rather than parking it. There is no parked `PHASE`. The prohibition was wrong on its own terms too: § 1.2 restricts `WAIT:` to three fixed forms at `DONE` only when `## TODO` is EMPTY, and Proposal Mode has just filled `## TODO`, so a concrete user brake there is ordinary. The command's whole purpose is a halt, and the halt was the one thing it could not conformantly write down (T-455) | Enforced directly by `tools/validate.py`: the phase doc must carry the exact brake wording and must not carry the superseded prohibition, so either half drifting FAILs; red-controlled in `tools/audit_checks.py` by swapping the brake back for the written-but-not-executed `PHASE` fiction. § 1.10's own description of the bare form names the same one legal halt rather than restating the rule. `tests/scenarios/proposal-mode-halt/` is a STATIC_STATE_SCENARIO: it proves the halt state is ADMISSIBLE, not that `continue` then picks the top ticket and enters SCOUT -- no fixture here runs an agent. It shipped described as "the behavioral half", which overstated what it exercises; the row is corrected rather than defended by its release |
 | 221 | A validator condition whose trigger spans more than one project file belongs in `tests/scenarios/`, and a fail-fixture must pin the reason it fails on. `tools/audit_checks.py` mutates ONE file per case by design, so a compound condition cannot be red-tested there: mutate `STATE.md` alone and the board still disagrees, mutate `BOARD.md` alone and `next_action` is still legal, and every single-file attempt goes not-red -- which reads exactly like a passing control. The route this needed already existed rather than having to be invented: a scenario fixture constructs a whole `.saipen/`, which is the definition of a compound state. What was missing was the observation and the worked pair. The second half is the same defect one layer up: an unpinned `expect: fail` fixture asserts only that something, somewhere, went wrong, so any unrelated FAIL scores it green -- reproduced twice in one session, when three red controls went red on a mangled ticket line instead of the cap they name, and when a new fixture went red on a `[phase-ticket-ref]` FAIL leaking in from outside its own tree (T-457) | Enforced directly by `tools/run_scenarios.py`: an `expect: fail` fixture with no `expect_fail_contains:` line now FAILs rather than WARNs. Every fixture in the repository already carried the pin, so this is a rot guard, hand red-tested by stripping the pin off the new fixture and confirming the run goes red naming it. The route rule lives in `tools/audit_checks.py`'s own docstring -- the tool that cannot express the condition is where a reader looks when it will not go red |
 | 222 | HUNT deletes only what it can prove recoverable, and its clean-result cache is keyed on a tree rather than a commit. Two floors in one phase doc, both reading a limit as an authorization. The first: "obvious junk -> delete free, capped at 5 files per sweep" treated a quantity cap as permission, while RFC § 1.1 allows an unconfirmed destructive operation only when the active ticket pre-authorizes it AND the operation is reversible -- and HUNT routinely runs with no active ticket at all, so the pre-authorization half is simply absent. "Obvious" is a feeling about a file, not a property of it: an untracked generated artifact looks exactly like an untracked file nothing can recreate. Deletion without asking now needs one of two named proofs, recorded in the LOG line -- tracked at HEAD so `git checkout` restores the bytes, or regenerable by a command named at the time. The 5-file cap survives unchanged as the mass-deletion gate it always was: five recoverable files may go, six may not, one unrecoverable file may not. The second: the skip condition was keyed on `git rev-parse --short HEAD`, and a commit hash says nothing about tracked files edited since or untracked files added since -- which is most of a live session, because work commits at SHIP and not per checkpoint (§ 1.5). The document argued against itself, rejecting mtimes as insufficient three paragraphs from where its own key ignored every uncommitted byte. Reuse now also requires `git status --porcelain` to print nothing, which fails safe and costs no fingerprint machinery; `--porcelain` already excludes gitignored noise (T-458, T-459) | Evidence class STRUCTURAL_ONLY, stated rather than dressed up: both are rules about what an agent may do, and nothing in this repository executes a HUNT sweep, so no fixture can witness a deletion that did not happen. What is mechanical is the wording -- `tools/validate.py` FAILs [hunt-delete-proof] if the proof requirement or the cap's "not a grant of authority" clause softens, and [hunt-clean-key] if the clean-worktree precondition goes. Three red controls in `tools/audit_checks.py`, one per clause, each verified red on its own slug. Each ticket was additionally verified in its own isolated worktree carrying that ticket's hunks alone -- 128/128 controls for the deletion half, 127/127 for the cache half -- because both edit the same three files and a single green run over the pair would be evidence for neither |
 | 223 | The PREPARE record names its producer. `phases/prepare.md` step 6 fixed the completion event as `RUN: prepare -> done` and the failure event as `RUN: prepare -> FAILED <reason>` -- the same two strings for `saitranslate`, for `saiwiki`, and for an unqualified main-project package, so a cold agent or `saipen status` reading `LOG.md` could not tell which handoff had become ready, and two prepares of different producers were indistinguishable rather than dedupable. This is row 206's argument one phase over: a record another rule is required to READ needs a form the reader can find, or the reading is invention. The evidence that the shape was wrong is in this repository's own history -- both live prepare events read `RUN: prepare saiwiki -> done`, agents writing the producer in by hand against the phase doc's own fixed format, practice correcting a shape nobody had fixed. `<producer>` is the producer name or the literal `unqualified`. The source revision is deliberately NOT duplicated into the LOG line: the handoff's own `source_head:` field already carries it per producer, and a second copy in an append-only file goes stale against the one that gets refreshed (T-460) | Enforced directly by `tools/validate.py` in two places, and the second is not a marker: the phase doc must carry the qualified form, AND any `LOG.md` line whose taxonomy text opens `prepare ->` with no producer FAILs. That second check is clean at FAIL level rather than WARN because the repository holds zero unqualified records to grandfather -- verified before choosing the severity, since a check that FAILs real history gets disabled the first time it is inconvenient. One red control in `tools/audit_checks.py` softens the phase doc back to the unqualified string |
-| 224 | A multi-command message loses nothing, and the pair the shortcut table most invites now completes. Row 217's OBEY step shipped saying several commands in one message run in the order written -- ordering stated, disposal not -- so a second command whose preconditions are not met had one honest-looking outcome: answer it in chat and forget it, which is the same loss OBEY exists to stop, arriving one command later. Such a command is recorded before the session ends, in `next_action` when it will be legal at the next continue or as the topmost `## TODO` ticket when it will not. Separately, `dd cc` was dead by construction: bare `dd` is Proposal Mode, which ends at `phase: DONE` with `goal_mode: false`, and a bare goal key at `goal_mode: false` is not a command at all -- so the table invited a combination that could never complete, row 207's defect at the level of a PAIR instead of a row. The carve-out is a pair, not a loosening: a bare goal command standing in the SAME message immediately after a plan command starts the plan that command just wrote, with `goal_mode: true` and counters from `0`, and the `PLAN` it follows is that run's wave 1 counted once. Bare `cc` alone is unchanged in every respect, because what the ban guards against is E-1468 -- a lone key typed out of habit mid-run handing itself a fresh 3-wave/20-ticket budget for an objective nobody stated -- and a key pressed immediately after asking for a plan has its objective on the board (T-480) | Evidence class STRUCTURAL_ONLY, stated rather than dressed up: nothing in this repository dispatches a shortcut, so no fixture can witness a command being dropped or a pair completing. `tools/validate.py` FAILs [command-not-dropped] if the disposal clause softens and [plan-goal-pair] if the carve-out goes; one red control each in `tools/audit_checks.py`. The `gg` and `cc` rows carry the pair in their Notes, so row 218's route-derived check keeps them honest about it |
+| 224 | A multi-command message loses nothing, and the pair the shortcut table most invites now completes. Row 217's OBEY step ships saying several commands in one message run in the order written; a second command whose preconditions are not met is recorded before the session ends. The pair carve-out: a bare goal key standing in the SAME message immediately after a plan command starts the plan that command just wrote, with `execution_intent: goal` and counters from `0`, and the `PLAN` it follows is that run's wave 1 counted once. The pair now means `dd gg` (plan then new goal) -- `dd cc` is no longer a goal pivot at all, but plan then continue-converge, which is coherent on its own terms (T-480, T-537) | Evidence class STRUCTURAL_ONLY, stated rather than dressed up: nothing in this repository dispatches a shortcut, so no fixture can witness a command being dropped or a pair completing. `tools/validate.py` FAILs [command-not-dropped] if the disposal clause softens and [plan-goal-pair] if the carve-out goes; one red control each in `tools/audit_checks.py`. The `gg` row carries the pair in its Notes, so row 218's route-derived check keeps it honest |
 | 225 | The seat is derived, not chosen: it is the agent home the protocol was loaded from. Row 148 gave `agent:` a definition and left the one question that made the definition usable unanswered -- where a first value comes from. "Write your own seat name" is a free choice, and this repository's own history is what that costs: claude-opus, claude-sonnet-5, opencode, antigravity, antigravity-gemini, gemini-pro -- six names for two or three actors, four of them carrying a MODEL name rather than a seat. Take the home directory's own name, strip a leading dot, lowercase it -- `.claude` to `claude`, `.codex` to `codex`, `.config/opencode` to `opencode`. `BOOT.md` already resolves that path to find the phase docs, so the value costs no new lookup and there is nothing to remember or invent. Every property section 1.4 needs falls out of it: deterministic per tool and machine, stable across model upgrades because the home does not move when the model behind it changes, distinct between real actors because two tools read from two homes, and observable because it is a directory rather than a self-report. The degraded path is named rather than improvised -- no home identifiable means the platform's canonical name plus a `DEC` recording that it was self-reported. subSaipen states are outside it: a sub writes its own slug, unique by construction. Known limit, stated: two sessions of the SAME tool derive the same seat and are indistinguishable, which is the concurrency case the Concurrency boundary already puts outside Core's envelope -- and the parallel-session damage this repository recorded at E-1863 was two different tools, which this does separate (T-456) | Enforced directly by `tools/validate.py`, and this converts the half row 148 called behavioural: a value carrying a model-family token from a closed list (`opus`, `sonnet`, `gpt`, `gemini`, `deepseek`, ...) FAILs, because a model name renames the seat on every upgrade, which is the false alarm the field exists to prevent. Matching is on the closed list rather than on shape -- digits or hyphens would fail ordinary tool names. It caught the session that wrote this rule, whose own STATE read `claude-opus`; renamed to `claude` with the `DEC` section 1.4 requires. Two red controls: STATE set back to a model build, and `phases/init.md` softened to letting the agent pick |
 | 226 | A MARKHUNT pass can still be counted after its findings are triaged. `phases/markhunt.md`'s closure rule tells a later validator or human to sum "this pass's `[MARKHUNT]` tickets" against the manifest's `findings:` count -- and nothing recorded which tickets those were. The rule reads durable history while depending on a surface that legitimately changes: the same document sends a triaged finding from `## BLOCKED` to `## TODO` with the `[MARKHUNT]` tag and the `unvetted audit` blocker dropped, and a dismissed finding leaves the board entirely. `BOARD.md` is not append-only; `LOG.md` is, and it is the only place the accounting can survive. Four passes had already run on this repository -- findings=3, 12, 6 and 1 -- and not one of them can be re-checked today, because no line says which tickets it produced. Two more things fell out while looking: the document's mandated completion form and the four lines actually written disagree, practice having drifted from a fixed format nobody enforced; and the pass identity the ticket asked for needs no invention at all, because the completion line's own `E-###` is already unique, monotonic and immutable, which makes two passes with overlapping findings separately reconstructable by construction. So `tickets=` joins the line, dismissal and triage each LOG a `DEC` naming the ticket and the pass event, and no ID scheme, registry or ticket field was added (T-461) | Enforced directly by `tools/validate.py` in two places: the phase doc must require the ticket list and name the E-### as the identity, and an ACTIVE-log `RUN: markhunt ->` line carrying no `tickets=` FAILs. Active-log scope is what makes the second one a clean FAIL rather than a WARN -- all four historical lines are sealed and immutable, so there is nothing to grandfather, the same narrowing the conformance-record check took at E-1851. One red control in `tools/audit_checks.py` makes the ticket list optional again. Known limit: the check enforces that a list exists, not that it is complete -- a pass that under-reports its own tickets is a false record, and no checker can tell a short list from an honest one |
 | 227 | `no-git` means git cannot be READ, and a genuinely git-less MARKHUNT closure is unproven rather than satisfied. Two faults in one clause of the manifest check. First, it named `mode: no-publish` as a case where the hashes are the literal `no-git` -- but RFC 1.3 makes that mode a PUBLISH capability, blocking commit, tag and push, while `git rev-parse --short HEAD` answers exactly as it always did. Writing `no-git` there mislabels the host and switches the tree-movement check off on a repository whose HEAD is sitting right there. Second, on a host where git really is absent, both ends reading `no-git` was declared "satisfied automatically, nothing to compare" -- a check reporting success for the one case where it measured nothing, and doing so precisely where movement is most likely rather than least, since an exhaustive pass can legitimately span sessions while files change underneath it. No fingerprint mechanism was invented for the git-less case: there is no cheap deterministic surface to compare, so the closure is LOGged carrying `tree_movement=unverified` and any vector whose covered paths are believed to have changed is re-run. An honest unproven closure beats an automatic pass, because the next reader can tell which one they are holding (T-462) | Enforced directly by `tools/validate.py`: `phases/markhunt.md` must state that `no-git` is about readability rather than publish capability, and must carry the unproven-closure marker. Two red controls in `tools/audit_checks.py`, one per clause -- folding `no-publish` back into `no-git`, and calling a git-less closure fine again. Evidence class STRUCTURAL_ONLY, stated: nothing here runs a MARKHUNT pass on a git-less host, so no fixture can witness a closure that measured nothing |
diff --git a/saipen/CORE.md b/saipen/CORE.md
index 55fa897..6fc4794 100644
--- a/saipen/CORE.md
+++ b/saipen/CORE.md
@@ -303,8 +303,8 @@ If the user provides a raw list or backlog of multiple features, tasks, or bug r
 ### 1.10 Command Surface
 The complete set of recognized user-facing commands. Phase-affecting ones are defined in full where cited; `status` and `stop` are defined here because they are meta/control operations, not phase transitions -- they can be invoked from any phase and MUST NOT be treated as work themselves.
 - `saipen set` / `saipen init` -- bootstrap `.saipen/` (§ 1.7, `phases/init.md`).
-- `saipen continue` / bare `saipen` -- read `STATE.md`/`BOARD.md`/tail of `LOG.md`, execute `next_action` immediately, no rebriefing (§ 1.1, § 2.1 DEFAULT BEHAVIOR, `CONFORMANCE.md` TEST-001). **A resume MUST also name what is stuck**, in its reply, in one line: scan `BOARD.md`'s `## BLOCKED` section for every blocked ticket, any untriaged `[MARKHUNT]` finding, and a live `WAIT:` if one is being carried. Report only -- no new field, no state change, nothing queued. `.saipen/kitchen/digest.md` carries this at `saipen stop` and `SHIP` and nowhere else, so a blocked ticket could sit unmentioned across an unbounded number of sessions while every one of them truthfully reported the work it did do. The human's whole view of what is parked came from a file written only when the run ended.
-- `saipen goal <text>` -- pivot to a new objective, run to completion (§ 2.4). Bare `saipen goal` (no text) is also recognized, but ONLY to resume an already-paused goal run (§ 2.4 Entry) -- it is never a way to start a new objective without stating one. **With `execution_intent: normal` it is not a command at all**, and the failure mode worth naming is the tempting one: an agent that treats it as a generic "start goal mode" flips the flag and initializes `goal_waves`/`goal_tickets` at `0`, handing a full safety-valve budget to a run nobody authorized -- the same accounting loss the conditional reset in § 2.4 Entry exists to prevent, arrived at from the other side. It MUST NOT set the goal intent, MUST NOT write or reset either counter, MUST NOT plan, and MUST NOT guess an objective from `BOARD.md`. It replies exactly `Use: saipen goal <objective text>` and stops -- one line, no state change, which also satisfies § 1.11's "a session that did nothing MUST say exactly that." **One carve-out, and it is a pair rather than a loosening of the bare form.** A bare goal command standing in the SAME message immediately after a plan command (`dd cc`, or `saipen plan` then `saipen goal`) takes the plan that command just wrote as its objective: `execution_intent: goal`, counters from `0`, straight into `SCOUT` for the topmost workable ticket, exactly as § 2.4 Entry's with-text form. The objective IS stated -- it is the plan now sitting on `BOARD.md`, which the user asked for in the same breath -- so the thing this ban protects is present rather than missing. What the ban actually guards against is E-1468: a lone bare key typed out of habit mid-run, handing itself a fresh 3-wave/20-ticket budget for an objective nobody stated. That is not a key pressed immediately after asking for a plan, and a bare goal command on its own is unchanged in every respect. Without this the shortcut table invites a combination that can never complete -- `dd` ends Proposal Mode at `execution_intent: normal`, where the next key is not a command at all -- which is CONFORMANCE 207's defect at the level of a PAIR instead of a row.
+- `saipen continue` / bare `saipen` / `cc` -- read `STATE.md`/`BOARD.md`/tail of `LOG.md`, execute `next_action` immediately, no rebriefing. **The persisted `execution_intent` decides what a resume means** (§ 2.4, the convergence contract). With `execution_intent: goal`, continue resumes the existing goal exactly as § 2.4 Entry's bare form describes -- the objective is preserved, never replaced or re-planned merely because this key was typed, and a tripped safety valve is re-authorized (both counters reset to `0`) exactly as § 2.4 Entry conditions the reset on the valve having tripped. With `execution_intent: converge`, continue resumes convergence. With `execution_intent: normal`, bare `cc` ENTERS converge: it exhausts existing real work and maintenance to the closure contract -- never ADD, never invented features -- and it NEVER asks for an objective. `cc <arguments>` is not a goal and is never guessed into one: it replies exactly `Use: gg <objective>` and stops (§ 1.1, § 2.1 DEFAULT BEHAVIOR, `CONFORMANCE.md` TEST-001). **A resume MUST also name what is stuck**, in its reply, in one line: scan `BOARD.md`'s `## BLOCKED` section for every blocked ticket, any untriaged `[MARKHUNT]` finding, and a live `WAIT:` if one is being carried. Report only -- no new field, no state change, nothing queued. `.saipen/kitchen/digest.md` carries this at `saipen stop` and `SHIP` and nowhere else, so a blocked ticket could sit unmentioned across an unbounded number of sessions while every one of them truthfully reported the work it did do. The human's whole view of what is parked came from a file written only when the run ended.
+- `saipen goal <text>` / `gg <objective>` -- pivot to a NEW objective, run to completion (§ 2.4). Bare `saipen goal` / `gg` with no text is a usage line, never a continuation alias, never a resume: it replies exactly `Use: gg <objective text>` and stops -- it MUST NOT set the goal intent, MUST NOT write or reset either counter, MUST NOT plan, MUST NOT resume, and MUST NOT guess an objective from `BOARD.md`. One line, no state change, which also satisfies § 1.11's "a session that did nothing MUST say exactly that." The failure mode worth naming is the tempting one: an agent that treats bare `gg` as a generic "start goal mode" flips the intent and initializes `goal_waves`/`goal_tickets` at `0`, handing a full safety-valve budget to a run nobody authorized -- the same accounting loss the conditional reset in § 2.4 Entry exists to prevent. **One carve-out, and it is a pair rather than a loosening of the bare form.** A bare goal key standing in the SAME message immediately after a plan command (`dd gg`, or `saipen plan` then `saipen goal`) takes the plan that command just wrote as its objective: `execution_intent: goal`, counters from `0`, straight into `SCOUT` for the topmost workable ticket, exactly as § 2.4 Entry's with-text form. The objective IS stated -- it is the plan now sitting on `BOARD.md`, which the user asked for in the same breath -- so the thing this ban protects is present rather than missing. What the ban actually guards against is E-1468: a lone bare key typed out of habit mid-run, handing itself a fresh 3-wave/20-ticket budget for an objective nobody stated. That is not a key pressed immediately after asking for a plan, and a bare goal command on its own is unchanged in every respect. Without this the shortcut table invites a combination that can never complete -- `dd` ends Proposal Mode at `execution_intent: normal`, where the next key is not a command at all -- which is CONFORMANCE 207's defect at the level of a PAIR instead of a row.
 - `saipen plan [text]` -- explicit trigger for PLAN phase (`phases/plan.md`).
 - `saipen clean` -- explicit CLEAN trigger (`phases/clean.md`).
 - `saipen translate` -- isolated translation build (`phases/translate.md`).
@@ -327,14 +327,14 @@ The complete set of recognized user-facing commands. Phase-affecting ones are de
 
 **Any recognized phase-switching command (`clean`, `translate`, `markhunt`, `validate`, `prepare`, `plan`, `ship`, `hunt`) invoked while a ticket sits in `## DOING` MUST checkpoint that ticket first** (§ 1.5's normal LOG -> BOARD -> STATE, plus a refreshed `claim_time` if the ticket stays claimed) before switching phase. `saipen stop` already says this explicitly; the others inherit the same duty rather than each restating it, because the failure is identical either way -- switching phase out from under an in-flight ticket abandons it mid-edit, leaves an unrefreshed claim that goes stale in 15 minutes (§ 1.4), and strands whatever `kitchen/` work it had going. The ticket may legitimately stay `## DOING` across the switch (you're coming back to it) or be demoted to `## TODO` with a resumable `next_action` (you're not) -- what's forbidden is neither: leaving it claimed, unlogged, and unrecorded while the session goes and does something else.
 
-**Repeated-letter shortcuts.** Identical keystrokes, no shift — a repeated letter cannot begin an English word, and a shortcut is the ENTIRE message rather than a prefix of one, so it is never mistaken for prose. **A shortcut is a command, never a greeting.** The ENTIRE-message rule exists so a bare key cannot be read as a prefix of prose, and the command rule exists so it cannot be read as prose at all: `qq` is not a hello, it is `saipen prepare saiwiki`. Receiving any declared shortcut (Latin or Cyrillic twin), the agent MUST execute the exact row it resolves to, or that row's exact no-op, and MUST NOT answer with an acknowledgment, a question, or any other chat. Reproduced on this repository at E-1801: a bare `qq` was answered as a greeting instead of executed as `saipen prepare saiwiki` -- the exact failure this sentence exists to name. **Length has no global meaning.** It resolves a letter's own pair: `ss`/`sss` are stop/status; `cc`/`ccc` are goal/continue-and-ship; `ee`/`eee` and `qq`/`qqq` are prepare/collect-and-ship. A doubled key can therefore pivot, scan, stop, report, plan, prepare, or test; a tripled key can be read-only or reach a remote. Cost and safety come only from the exact row, never from key count. `gg`, `hh`, and the current `cc` assignment are explicit user routes; the rest are also assignments, not guessed initials. **A row's Notes describe what the destination actually does, including its bare form.** `cc`'s once claimed the bare key starts Goal Mode, which bare `saipen goal` is forbidden to do -- § 1.10 above requires it to write nothing and reply with the usage line at `execution_intent: normal`. On the most-used key the table promised an outcome the row cannot produce, which is the same class as the greeting misread one column over: the shortcut is a command, and a Notes column that describes a different command is the table lying about its own row. The table is authoritative and exhaustive: do not invent an undeclared repeated form from the pattern. **And answering a row from recall is the same failure as inventing a command, not a lesser one.** Read this table when a shortcut arrives; do not remember it. The rule above catches a key you know you do not know, which is the easy case -- the expensive one is a key you are sure about, where nothing prompts a lookup and the answer is fluent, specific and wrong. Observed: an agent asked `hh` produced a ten-row card in which every row was wrong and six of the command names it listed appear nowhere in this section, having first executed `cc` as a commit. Each row routes to the exact command chain shown, and `tools/validate.py` MUST reject both unknown commands and a valid-but-wrong destination.
+**Repeated-letter shortcuts.** Identical keystrokes, no shift — a repeated letter cannot begin an English word, and a shortcut is the ENTIRE message rather than a prefix of one, so it is never mistaken for prose. **A shortcut is a command, never a greeting.** The ENTIRE-message rule exists so a bare key cannot be read as a prefix of prose, and the command rule exists so it cannot be read as prose at all: `qq` is not a hello, it is `saipen prepare saiwiki`. Receiving any declared shortcut (Latin or Cyrillic twin), the agent MUST execute the exact row it resolves to, or that row's exact no-op, and MUST NOT answer with an acknowledgment, a question, or any other chat. Reproduced on this repository at E-1801: a bare `qq` was answered as a greeting instead of executed as `saipen prepare saiwiki` -- the exact failure this sentence exists to name. **Length has no global meaning.** It resolves a letter's own pair: `ss`/`sss` are stop/status; `cc`/`ccc` are continue/continue-and-ship; `ee`/`eee` and `qq`/`qqq` are prepare/collect-and-ship. A doubled key can therefore pivot, scan, stop, report, plan, prepare, or test; a tripled key can be read-only or reach a remote. Cost and safety come only from the exact row, never from key count. `gg`, `hh`, and `cc` are explicit user routes; the rest are also assignments, not guessed initials. One key, one purpose: `gg` is the sole short route for a NEW goal, `cc` is continue/converge, and they never overlap. **A row's Notes describe what the destination actually does, including its bare form.** `cc`'s once claimed the bare key starts Goal Mode, which bare `saipen goal` is forbidden to do -- § 1.10 above requires it to write nothing and reply with the usage line at `execution_intent: normal`. On the most-used key the table promised an outcome the row cannot produce, which is the same class as the greeting misread one column over: the shortcut is a command, and a Notes column that describes a different command is the table lying about its own row. The table is authoritative and exhaustive: do not invent an undeclared repeated form from the pattern. **And answering a row from recall is the same failure as inventing a command, not a lesser one.** Read this table when a shortcut arrives; do not remember it. The rule above catches a key you know you do not know, which is the easy case -- the expensive one is a key you are sure about, where nothing prompts a lookup and the answer is fluent, specific and wrong. Observed: an agent asked `hh` produced a ten-row card in which every row was wrong and six of the command names it listed appear nowhere in this section, having first executed `cc` as a commit. Each row routes to the exact command chain shown, and `tools/validate.py` MUST reject both unknown commands and a valid-but-wrong destination.
 
 | Shortcut | Routes to | Notes |
 |----------|-----------|-------|
-| `gg` | `saipen goal` | Same destination as `cc`, so the same truth: the pivot needs text, and `gg <objective>` is what sets a new one. Bare `gg` is a resume under a live goal run, or the exact usage line at `execution_intent: normal`; it re-authorizes only when § 2.4's valve has actually tripped; straight after a plan command in the same message it starts the plan it just saw |
+| `gg` | `saipen goal` | NEW GOAL ONLY: the pivot needs text, and `gg <objective>` is what sets a new one. Bare `gg` is never a continuation alias and never a resume -- it replies exactly `Use: gg <objective text>` and stops; straight after a plan command in the same message it starts the plan it just saw |
 | `hh` | `saipen hunt` | Autonomous defect/improvement scan (§ 2.1) |
-| `cc` | `saipen goal` | The pivot needs text: `cc <objective>` sets a new objective. Bare `cc` is the destination's bare form -- a resume under a live goal run, and at `execution_intent: normal` the exact usage line, never a pivot; straight after a plan command in the same message it starts the plan it just saw |
-| `ccc` | `saipen continue` then `saipen ship` | Resume everything, then commit/tag/push through the normal gates; it waives nothing |
+| `cc` | `saipen continue` | CONTINUE CONTEXT / CONVERGE. Bare `cc` resumes `execution_intent: goal` if set, resumes convergence if set, and enters convergence from `normal` -- it never asks for an objective and never invents features. `cc <args>` is not a goal: it replies exactly `Use: gg <objective>` and stops |
+| `ccc` | `saipen continue` then `saipen ship` then refresh EE + QQ | Converge the main project, SHIP it normally through the gates, then -- after the shipped revision -- force-fresh EE and QQ against the shipped HEAD. Waives no review gate, destructive confirmation, first-publish confirmation, remote safety, or producer freshness rule |
 | `ss` | `saipen stop` | The brake: checkpoint, write the digest, hand control back |
 | `sss` | `saipen status` | Where the run stands, caveman-дед short |
 | `dd` | `saipen plan` | Planning and adding both. Bare: autonomous 4-best-additions proposal. `dd <text>`: the user's own items, reworded and inserted at the front of `## TODO` |
diff --git a/saipen/MAINTENANCE.md b/saipen/MAINTENANCE.md
index 3bcb990..819ee8c 100644
--- a/saipen/MAINTENANCE.md
+++ b/saipen/MAINTENANCE.md
@@ -77,13 +77,13 @@ When the user requests one step of a well-known user workflow, the agent SHOULD
 ### 2.4 Goal Mode (Autonomous Execution)
 `saipen goal <text>` is an explicit, run-scoped opt-in that (a) sets a new high-level objective, superseding whatever was queued, and (b) runs that objective to completion with minimal interruption — through the Maintenance layer (§ 2.1), not just the current ticket wave. "Run-scoped," not "session-scoped": the persisted `goal_waves`/`goal_tickets` counters (below) mean a `saipen goal` run survives a crash or a fresh chat session mid-run — it ends only via this section's own Exit conditions, never by a session simply restarting. It is NOT the default — `saipen <text>` and bare `saipen continue` retain today's behavior.
 
-- **Entry (the pivot)**: `saipen goal <text>` sets a new objective: a `DOING` ticket in flight MUST be checkpointed cleanly, not abandoned mid-edit — LOG a `DEC` line noting the pivot, then leave it `TODO`. Existing `TODO` tickets MUST NOT be deleted; they are demoted below the new objective's tickets (board order = priority = law, § 1.6). `PLAN` runs for the new objective and inserts its tickets at the top. Agent MUST set `execution_intent: goal`, `goal_waves: 0`, `goal_tickets: 0` in `STATE.md`, then proceed directly into `SCOUT` for the first new ticket without pausing. **The Entry `PLAN` is wave 1, not wave 0.** It is a `PLAN` running for a genuinely new wave, which is exactly what the counter counts -- so once it completes, write `DEC: goal_waves 0->1` and checkpoint before entering `SCOUT`. Reading the `goal_waves: 0` initialization as the final value for the first wave costs the run a third of its wave budget and is the easiest way to sail past the valve: three waves would be counted as two. If the user invokes `saipen goal` (bare, no text) to resume a paused run, the agent proceeds immediately to the next workable ticket via `SCOUT` without re-planning or demoting anything. **A bare invocation standing in the same message immediately after a plan command is not that case**: § 1.10's pair carve-out makes it an Entry, with `execution_intent: goal` and both counters at `0`, because the plan just written IS the stated objective. The `PLAN` it follows is that run's wave 1 and carries the same `DEC: goal_waves 0->1` line any Entry `PLAN` does -- counted once, at the plan, never again at the goal command. **Whether it also resets the counters depends on the valve, not on the command**: it resets `goal_waves: 0`, `goal_tickets: 0` only when they are at or over § 2.4's caps -- the tripped condition this command exists to clear. With the valve untripped there is nothing to re-authorize, so the counters carry over exactly as `saipen continue` would leave them, and no re-authorization line is written. Resetting unconditionally made the most convenient key silently hand out a fresh 3-wave/20-ticket budget to anyone who typed it out of habit mid-run -- the same failure § 1.10 already names for the `execution_intent: normal` case, and it happened here (E-1468 dropped three tickets of accounting against a valve that had never tripped). This is also what makes the shortcut safe to type at any time, which is the point of having one. **A reset that does happen MUST leave its own countable line** -- `DEC: goal reauthorized -- goal_waves N->0, goal_tickets M->0`, this exact text after the taxonomy, with the real pre-reset counts substituted. Without it the drop is invisible: the bumps it cancels are still in the LOG, the newest pivot line is the older `saipen goal <text>` one, and § 1.5's rebuild therefore counts a budget the human already re-authorized away -- re-tripping the valve while this section forbids tidying the counters back down. A re-authorization nobody can count is a re-authorization that expires at the next crash. **No workable ticket to proceed to?** That is not a dead end and not a reason to ask the user anything: fall through to § 2.1 exactly as the Board-empty rule below already requires -- `HUNT`, then `ADD` if clean -- under the still-set goal intent. A resume that lands on an empty board is the same situation as a wave finishing on one, and it gets the same answer.
+- **Entry (the pivot)**: `saipen goal <text>` sets a new objective: a `DOING` ticket in flight MUST be checkpointed cleanly, not abandoned mid-edit — LOG a `DEC` line noting the pivot, then leave it `TODO`. Existing `TODO` tickets MUST NOT be deleted; they are demoted below the new objective's tickets (board order = priority = law, § 1.6). `PLAN` runs for the new objective and inserts its tickets at the top. Agent MUST set `execution_intent: goal`, `goal_waves: 0`, `goal_tickets: 0` in `STATE.md`, then proceed directly into `SCOUT` for the first new ticket without pausing. **The Entry `PLAN` is wave 1, not wave 0.** It is a `PLAN` running for a genuinely new wave, which is exactly what the counter counts -- so once it completes, write `DEC: goal_waves 0->1` and checkpoint before entering `SCOUT`. Reading the `goal_waves: 0` initialization as the final value for the first wave costs the run a third of its wave budget and is the easiest way to sail past the valve: three waves would be counted as two. If the user invokes bare `cc` (or `saipen continue`) to resume a paused run, the agent proceeds immediately to the next workable ticket via `SCOUT` without re-planning or demoting anything -- the resume key is `cc`, never `gg`, which is the NEW-GOAL key and nothing else. **A bare invocation standing in the same message immediately after a plan command is not that case**: § 1.10's pair carve-out makes it an Entry, with `execution_intent: goal` and both counters at `0`, because the plan just written IS the stated objective. The `PLAN` it follows is that run's wave 1 and carries the same `DEC: goal_waves 0->1` line any Entry `PLAN` does -- counted once, at the plan, never again at the goal command. **Whether it also resets the counters depends on the valve, not on the command**: it resets `goal_waves: 0`, `goal_tickets: 0` only when they are at or over § 2.4's caps -- the tripped condition this command exists to clear. With the valve untripped there is nothing to re-authorize, so the counters carry over exactly as `saipen continue` would leave them, and no re-authorization line is written. Resetting unconditionally made the most convenient key silently hand out a fresh 3-wave/20-ticket budget to anyone who typed it out of habit mid-run -- the same failure § 1.10 already names for the `execution_intent: normal` case, and it happened here (E-1468 dropped three tickets of accounting against a valve that had never tripped). This is also what makes the shortcut safe to type at any time, which is the point of having one. **A reset that does happen MUST leave its own countable line** -- `DEC: goal reauthorized -- goal_waves N->0, goal_tickets M->0`, this exact text after the taxonomy, with the real pre-reset counts substituted. Without it the drop is invisible: the bumps it cancels are still in the LOG, the newest pivot line is the older `saipen goal <text>` one, and § 1.5's rebuild therefore counts a budget the human already re-authorized away -- re-tripping the valve while this section forbids tidying the counters back down. A re-authorization nobody can count is a re-authorization that expires at the next crash. **No workable ticket to proceed to?** That is not a dead end and not a reason to ask the user anything: fall through to § 2.1 exactly as the Board-empty rule below already requires -- `HUNT`, then `ADD` if clean -- under the still-set goal intent. A resume that lands on an empty board is the same situation as a wave finishing on one, and it gets the same answer.
 - **Continuation**: While `execution_intent: goal`, the agent MUST advance `SCOUT → BUILD → VERIFY → REVIEW → SHIP → DONE` across successive tickets without stopping between them, subject to the caps below. `DONE` MUST transition to `SCOUT` for the next ticket, or re-run `PLAN` automatically for the next wave if the board defines one.
 - **Board-empty is not exit**: When `BOARD.md` empties (a wave finishes), the agent MUST NOT stop or wait for a human — it falls straight through into the § 2.1 Autonomous Transitions exactly as under bare `saipen`: `HUNT`, then `ADD` if clean, then `HUNT` again, indefinitely. the goal intent remains set across this whole loop; a clean `HUNT` or one completed `ADD` ticket is a *waypoint*, never a stopping point.
 - **SHIP exception**: The `saipen ship` gate in the SHIP phase is satisfied by an active `execution_intent: goal` for subsequent ships to an existing `origin`. Agent MUST auto-push without re-confirming per ship. **First publish of a brand-new repository still MUST confirm name and public/private with the user** — creating a new public artifact is a one-way door goal mode does not waive. "Brand-new" means no `origin` yet, OR an `origin` that exists but has never actually received a commit or tag (`git remote add` run early, nothing pushed since) — the same one-way door either way, not just the narrower "no remote configured at all" case (`phases/ship.md` § 7).
 - **Safety valve counters MUST persist, not just live in context**: a long goal run spans crashes, restarts, and other agents, so an in-context count is lost on every restart -- defeating the valve for exactly the long runs it protects. `STATE.md` MUST carry `goal_waves` (increment by 1 each time `PLAN` runs for a genuinely new wave -- a `PLAN` entered directly from `ADD`'s `RETURN PLAN` is NOT a new wave, that cycle was already counted at ADD's own RETURN below, so `phases/plan.md` skips the increment in exactly that case; without the carve-out one `HUNT`→`ADD`→`PLAN` chain would count the same wave twice and trip the valve early -- and each time a `HUNT`→`ADD` cycle completes -- "completes" means the moment `ADD`'s own § 2.2 evaluation reaches a `RETURN`, whether that tickets-and-claims a `BUILD`, tickets a `PLAN_or_SCOUT`, or concludes `DONE` outright; the wave is counted there, not deferred until whatever ticket `ADD` just created eventually finishes its own `BUILD → VERIFY → REVIEW → SHIP` run, which `goal_tickets` tracks separately) and `goal_tickets` (increment by 1 each time a ticket passes `VERIFY`) — both bumped and checkpointed (§ 1.5) at the moment they change, so any agent resuming this run reads the true count directly from `STATE.md`, never by re-deriving it from `LOG.md` scrollback. **Each bump MUST also leave an identifiable LOG line** -- `DEC: goal_waves N->M` or `DEC: goal_tickets N->M` (this exact text after the taxonomy) -- because § 1.5's Recovery is required to rebuild these counters by "counting wave/ticket-completion events in `LOG.md` since the pivot line," and that instruction is only executable if those events are actually distinguishable in the log rather than inferred from prose. `STATE.md` stays the fast path every normal resume reads; these lines are what makes the crash path work at all.
 - **Safety valve**: A single `saipen goal` invocation MUST NOT process more than 3 waves (`goal_waves`, planned tickets and HUNT/ADD cycles counted together) or 20 tickets (`goal_tickets` counts VERIFY-passes, so a ticket that bounces `REVIEW` -> `BUILD` -> `VERIFY` and re-passes consumes budget more than once -- this is deliberate and conservative: the valve trips slightly early, never late, so it can only ever be safer than the "20 distinct tickets" reading, never looser), whichever comes first. On hitting this ceiling, the agent MUST stop, write a full BOARD/STATE checkpoint, and report progress — the user re-invokes the goal command to continue.
-  **A tripped valve is a pause awaiting re-authorization, NOT an exit, and the goal intent stays set through it** (see Exit below, which deliberately does not list the valve). This matters because the alternative deadlocks: § 1.10 recognizes the goal resume ONLY while `execution_intent: goal`, so clearing the intent here would make the very command this line tells the user to run illegal in exactly the state it just created — leaving no legal way to continue the objective at all, only `saipen goal <text>` to replace it, which demotes the board and re-plans. That is a substitution, not a continuation.
+  **A tripped valve is a pause awaiting re-authorization, NOT an exit, and the goal intent stays set through it** (see Exit below, which deliberately does not list the valve). This matters because the alternative deadlocks: § 1.10 recognizes the goal resume (bare `cc`) ONLY while `execution_intent: goal`, so clearing the intent here would make the very command this line tells the user to run illegal in exactly the state it just created — leaving no legal way to continue the objective at all, only `saipen goal <text>` to replace it, which demotes the board and re-plans. That is a substitution, not a continuation.
   **The tripped state has one exact shape, and every field of it is load-bearing.** On tripping, checkpoint (§ 1.5) with:
   - `execution_intent: goal` — unchanged; the valve is a pause, not an exit (see above).
   - `goal_waves` / `goal_tickets` — unchanged, left at or over the cap. These ARE the tripped condition; "tidying" them is what lets a restart walk straight past the valve.
@@ -91,6 +91,6 @@ When the user requests one step of a well-known user workflow, the agent SHOULD
   - `phase` — **left exactly as it is.** Do NOT set `phase: BLOCKED`. This is the one field an agent is most tempted to "fix" here, and setting it is a self-inflicted deadlock: § 2.4's Exit list makes `STATE.phase: BLOCKED` an exit condition, so writing it clears the goal intent, which makes the resume the `WAIT:` line prescribes illegal under § 1.10 — the valve destroying its own continuation path. The valve would destroy its own continuation path. Same reasoning that kept the valve off the Exit list in v7.86.0; it applies to `phase` too.
   - `blocker` — the tripped valve is not a session-level block, so `blocker: none` stays correct. A budget pause and a blocked project are different states and `saipen status` reports them differently.
 
-  **The counters are what stops a restart, not the phase.** `execution_intent: goal` with `goal_waves >= 3` or `goal_tickets >= 20` *is* the tripped condition — no new field. Any agent resuming into that state MUST NOT continue the run: it re-states the stop, with `next_action` in § 1.2's safety-valve `WAIT:` form (`WAIT: safety valve reached (N waves / M tickets) -- run 'saipen goal' to continue`), and waits. The goal resume command is precisely the human's re-authorization: per Entry it resets both counters to `0`, which is what clears the tripped condition and grants the next budget. Counters at or over the cap are therefore load-bearing, not merely historical — never "tidy them up" without an actual re-authorization.
+  **The counters are what stops a restart, not the phase.** `execution_intent: goal` with `goal_waves >= 3` or `goal_tickets >= 20` *is* the tripped condition — no new field. Any agent resuming into that state MUST NOT continue the run: it re-states the stop, with `next_action` in § 1.2's safety-valve `WAIT:` form (`WAIT: safety valve reached (N waves / M tickets) -- run 'saipen goal' to continue`), and waits. Bare `cc` is precisely the human's re-authorization: per Entry it resets both counters to `0`, which is what clears the tripped condition and grants the next budget. Counters at or over the cap are therefore load-bearing, not merely historical — never "tidy them up" without an actual re-authorization.
 - **Unchanged under Goal Mode**: All existing caps still apply verbatim (3 dead hypotheses / 2 fix cycles per ticket in VERIFY; 2 review passes per finding in REVIEW). Goal Mode MUST NOT skip `VERIFY` or `REVIEW` — autonomy applies to *continuation between steps*, never to the correctness gates themselves. Destructive ops outside the ship/publish path (force-push, schema drop, deleting user data) still require explicit confirmation unless the ticket itself pre-authorizes them.
-- **Exit**: the goal intent MUST be cleared in `STATE.md` (back to `execution_intent: normal`) ONLY when: `ADD` itself gracefully concludes because the product is mature and logically complete (`phases/add.md`), or the agent reaches `STATE.phase: BLOCKED` (not just a single ticket moving to `## BLOCKED`). **A tripped safety valve is NOT on this list** -- it was until v7.86.0, and that was a deadlock: the valve tells the user to re-invoke the goal resume, which is recognized only while the goal intent is set, so exiting on a trip made the one documented continuation path illegal in the state the trip itself produced. The valve is a budget pause, the same shape as `saipen stop` (which is likewise not on this list); the two real exits above are the objective ending, which a valve trip is not. What stops a restart from silently continuing is the counters, not the flag -- see Safety valve above. A momentarily empty `BOARD.md` is never, by itself, an exit condition. On exit, `goal_waves`/`goal_tickets` MUST be cleared -- they describe the run that just ended, not a running lifetime total. Final report: tickets done/verified/shipped, any blocked, next action -- distinguish tickets that came from the user's original ask from ones picked up along the way (pre-existing backlog demoted below it, or `HUNT`/`ADD` findings), so the user can tell what actually happened without re-deriving it from `LOG.md`. No new persisted field is needed for this -- board order at Entry already carries it; it only needs to make it into the report.
+- **Exit**: the goal intent MUST be cleared in `STATE.md` (back to `execution_intent: normal`) ONLY when: `ADD` itself gracefully concludes because the product is mature and logically complete (`phases/add.md`), or the agent reaches `STATE.phase: BLOCKED` (not just a single ticket moving to `## BLOCKED`). **A tripped safety valve is NOT on this list** -- it was until v7.86.0, and that was a deadlock: the valve tells the user to re-invoke bare `cc`, which is recognized only while the goal intent is set, so exiting on a trip made the one documented continuation path illegal in the state the trip itself produced. The valve is a budget pause, the same shape as `saipen stop` (which is likewise not on this list); the two real exits above are the objective ending, which a valve trip is not. What stops a restart from silently continuing is the counters, not the flag -- see Safety valve above. A momentarily empty `BOARD.md` is never, by itself, an exit condition. On exit, `goal_waves`/`goal_tickets` MUST be cleared -- they describe the run that just ended, not a running lifetime total. Final report: tickets done/verified/shipped, any blocked, next action -- distinguish tickets that came from the user's original ask from ones picked up along the way (pre-existing backlog demoted below it, or `HUNT`/`ADD` findings), so the user can tell what actually happened without re-deriving it from `LOG.md`. No new persisted field is needed for this -- board order at Entry already carries it; it only needs to make it into the report.
diff --git a/tools/audit_checks.py b/tools/audit_checks.py
index 5c35b25..5101838 100644
--- a/tools/audit_checks.py
+++ b/tools/audit_checks.py
@@ -783,9 +783,13 @@ CASES: list[tuple[str, str, object, str]] = [
     ("shortcut routes to a phase, not a command", "saipen/CORE.md",
      replace("| `hh` | `saipen hunt` |", "| `hh` | HUNT |"),
      "do not resolve to a command"),
+    # Scenario 30: the old `cc` -> `saipen goal` mapping must fail validation.
+    # `cc` is the continue/converge key and `gg` is the sole new-goal key; the
+    # canonical table routes `cc` to `saipen continue`, and a regression back
+    # to the duplicated-goal mapping is rejected by the route check.
     ("shortcut routes to a valid but wrong command", "saipen/CORE.md",
-     replace("| `cc` | `saipen goal` |",
-             "| `cc` | `saipen continue` |"),
+     replace("| `cc` | `saipen continue` |",
+             "| `cc` | `saipen goal` |"),
      "assigned destination changed"),
     ("shortcut rationale restores stale length magic", "saipen/CORE.md",
      replace("**Length has no global meaning.**",
diff --git a/tools/validate.py b/tools/validate.py
index 45f95f3..432bce6 100644
--- a/tools/validate.py
+++ b/tools/validate.py
@@ -294,8 +294,8 @@ def _phase_next_action_error(value):
 EXPECTED_SHORTCUT_ROUTES = {
     "gg": "`saipen goal`",
     "hh": "`saipen hunt`",
-    "cc": "`saipen goal`",
-    "ccc": "`saipen continue` then `saipen ship`",
+    "cc": "`saipen continue`",
+    "ccc": "`saipen continue` then `saipen ship` then refresh EE + QQ",
     "ss": "`saipen stop`",
     "sss": "`saipen status`",
     "dd": "`saipen plan`",

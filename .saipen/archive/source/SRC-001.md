# SAIPEN — Audit Handoff

RUN_ID: acb-mtadwg78-9612dc53bb3e4aacbe3b
GENERATED_AT: 2026-08-27T01:16:43.125727
PROJECT_NAME: SAIPEN
TARGET: SAIPEN_26-08-2026-T20-40-07.zip; archive unpacked fully and real project root identified at archive root
BASELINE: SAIPEN v7.231.3; archive SHA256 8cc25a1922e7765749526c5de73a76047905d1d664242dcc2e44bec8dfadf60c; Git HEAD 337a0ef5ad94ccbedad253959b66f30855eec2cb
GIT_CONTEXT: PRESENT main@337a0ef5ad94ccbedad253959b66f30855eec2cb - main is 26 commits ahead of origin/main; shipped implementation paths were tracked-clean, while supplied .saipen work-state contains extensive pre-existing dirt
SAIPEN_CONTEXT: STALE - STATE reports HUNT/task none, but supplied work-state deletes tracked .saipen/logs/LOG-001.md through LOG-014.md and rewrites active LOG such that its first surviving event has a missing parent; stale improve/producer material is also present. Treated as orientation only; live implementation at Git HEAD remained authoritative
TOTAL_TICKETS: 32
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 4 tickets (baseline: SAIPEN v7.231.3; archive SHA256 8cc25a1922e7765749526c5de73a76047905d1d664242dcc2e44bec8dfadf60c; Git HEAD 337a0ef5ad94ccbedad253959b66f30855eec2cb)
- Wave 2 (Second Wave): 11 tickets (baseline: SAIPEN v7.231.3; archive SHA256 8cc25a1922e7765749526c5de73a76047905d1d664242dcc2e44bec8dfadf60c; Git HEAD 337a0ef5ad94ccbedad253959b66f30855eec2cb)
- Wave 3 (Performance): 17 tickets (baseline: SAIPEN v7.231.3; archive SHA256 8cc25a1922e7765749526c5de73a76047905d1d664242dcc2e44bec8dfadf60c; Git HEAD 337a0ef5ad94ccbedad253959b66f30855eec2cb)

---
## 01 — AUDIT CORE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-26T20:44:00+03:00
WAVE: AUDIT CORE
TARGET: SAIPEN_26-08-2026-T20-40-07.zip; archive unpacked fully and real project root identified at archive root
BASELINE: SAIPEN v7.231.3; archive SHA256 8cc25a1922e7765749526c5de73a76047905d1d664242dcc2e44bec8dfadf60c; Git HEAD 337a0ef5ad94ccbedad253959b66f30855eec2cb
GIT_CONTEXT: PRESENT main@337a0ef5ad94ccbedad253959b66f30855eec2cb - main is 26 commits ahead of origin/main; shipped implementation paths were tracked-clean, while supplied .saipen work-state contains extensive pre-existing dirt
SAIPEN_CONTEXT: STALE - STATE reports HUNT/task none, but supplied work-state deletes tracked .saipen/logs/LOG-001.md through LOG-014.md and rewrites active LOG such that its first surviving event has a missing parent; stale improve/producer material is also present. Treated as orientation only; live implementation at Git HEAD remained authoritative
AUDIT_SCOPE: CLI/source routing; source-receipt capture, identity, integrity, Work linkage, Contract/coverage, boundary/DONE/SHIP gates, close/archive/purge/recovery; context projections; crew liveness; runtime metadata; global/project USERPERSON; lock/path-ownership primitives; release/operations/sub integration surfaces; validation and focused regression contracts. Audit method followed execution/state/lifecycle paths per :contentReference[oaicite:0]{index=0}
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: ruff unavailable in sandbox: `python -m ruff check tools` -> `/opt/pyvenv/bin/python: No module named ruff`; full `python -m unittest discover -s tools -p 'test_*.py'` also did not complete within a 360s execution bound, and `python tools/run_scenarios.py` did not complete within 180s
VERIFIED_INSTEAD: Focused suites passed: source receipts 35/35; adaptive runtime 15/15; USERPERSON 37/37; crew/liveness 14/14; core-audit fixes 21/21; self-resolving gates 17/17; effect authorization 34/34 with 1 PowerShell-host skip; control primitives 34/34; `python -m compileall -q tools` PASS. `tools/validate.py` on an immutable tracked-HEAD snapshot PASSed; on the supplied dirty archive it FAILed on stale .saipen history/sweep state, an unpublished clean-hunt commit, and untracked `_AUDAPACK_MANIFEST.json`, none of which was promoted into an implementation ticket
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT

[P0] [CORE-001] tools/saipen_engine/liveness.py::{record_actionable,clear}; tools/saipen.py::_crew_liveness
EVIDENCE: `liveness.py:58-60` constructs `.saipen/cache/continuation-liveness.json` directly. `_load` at 62-71 uses `Path.read_text`; `record_actionable` at 82-95 creates the parent and uses `Path.write_text`; `clear` at 99-102 directly unlinks that pathname. No owned-target containment, ancestor symlink/reparse rejection, or no-follow descriptor binding is used. Normal non-read-only crew execution reaches these functions from `tools/saipen.py:1338-1362`. Hostile local reproduction: making `.saipen/cache` a symlink to an external writable directory containing `continuation-liveness.json` caused `clear(root)` to DELETE the external file. Recreating it and calling `record_actionable(root, "fp")` overwrote the external bytes with liveness JSON. A final-file symlink to an arbitrary external file was likewise followed and overwritten.
DEFECT: A project-controlled rebuildable cache path can escape the project and overwrite or delete files outside the repository under the invoking user's permissions. This is a severe local safety defect in a routine crew path; the fact that liveness is "best effort" makes the unsafe mutation unnecessary rather than acceptable.
REPAIR: Route the cache through one project-owned path primitive. Canonicalize/contain the target under the project, reject every existing symlink/junction/reparse/non-directory ancestor and any linked/non-regular final node, and use safe same-directory atomic replacement for the projection. Because the cache is disposable, an unsafe carrier should simply degrade to the documented first-observation/no-cache result. `clear` must only unlink a proven in-project regular cache node and otherwise no-op. Reuse the existing ownership/no-follow primitives rather than introducing a second path-security implementation.
VERIFY: Add hostile regressions for (1) final cache-file symlink, (2) `.saipen/cache` directory symlink/junction/reparse to an external directory, and (3) normal in-project cache. For both hostile cases, exercise both `record_actionable` and `clear`; assert external bytes and existence remain unchanged and no outside artifact is created. Preserve existing 14/14 liveness tests plus read-only/--dry-run zero-write behavior.

[P1] [CORE-002] tools/saipen_engine/intake.py read-side authority; tools/saipen_engine/context.py::context_audit
EVIDENCE: The new intake subsystem has split path-safety semantics. Writes use `_safe_path` (`intake.py:163-175`), but multiple authoritative reads bypass it. `_read_index` reads its pathname directly at 210-227; `_read_meta`, `_read_coverage`, and `_read_contract` at 236-309 reject a linked final node but do not prove every ancestor or descriptor identity. `verify_integrity` at 813-834 constructs the active body path and calls `read_bytes()` directly. `_work_is_done` at 837-847 directly reads `.saipen/BOARD.md`. Work/DONE/boundary/release gates trust this integrity result at 850-916, 919-963, and 966-1015. `context_audit` directly reads each active source body at `context.py:678-688`. Conversely, `read_body` invokes `_safe_path` before its exception-protected read at `intake.py:1186-1225`, so an escaping link raises `InvalidIdError` out of the public API instead of producing its documented structured refusal. Verified on a valid project fixture: after replacing `SRC-001.md` with a symlink to an external file containing identical bytes, `verify_integrity` returned `SOURCE_INTEGRITY_OK`, `boundary_gate(...,"BUILD")` returned `SOURCE_REREAD_OK`, `validate_project` returned `[]`, and `context_audit` consumed/counts the external bytes; `read_body` instead raised an uncaught `InvalidIdError`. Independently, replacing `.saipen/BOARD.md` with an external symlink whose T-001 projection was under `## DONE` caused `close_receipt` for a linked, fully-covered receipt to return `SOURCE_CLOSED`, proving closure can be authorized from outside project ownership.
DEFECT: The source-receipt feature claims the immutable project-local body and canonical Work state as authority, but its read paths do not share one ownership primitive. A hostile/accidentally linked project can substitute external bytes for source authority, make boundary and closure gates accept them, and provoke raw CLI/API tracebacks. Digest equality proves content equality, not ownership of the node being trusted.
REPAIR: Introduce/reuse one bounded project-owned reader for ALL intake authority files: index, active metadata/body, Contract, coverage, tombstones, archive metadata/body when explicitly requested, and BOARD state consulted by intake. It must validate all ancestors plus the final node, use lstat + descriptor-bound `read_bound_regular_bytes` or the equivalent existing safe codec primitive, reject symlink/junction/reparse/non-regular nodes, and detect lstat/open races. Make `verify_integrity`, `_work_is_done`, `active_receipts`, `recover_orphans`, `validate_project`, `read_body`, `status`, and context source accounting consume that primitive rather than reopening paths independently. Public APIs must translate `InvalidIdError`/ownership `ValueError`/OSError into stable `VALIDATION_FAILED` or `SOURCE_CORRUPTION` results, never traceback. Do not weaken the existing path-traversal tests.
VERIFY: Add final-node and parent-directory symlink/reparse tests for body, metadata/index/coverage/Contract, and BOARD authority. An external body with matching digest MUST NOT return `SOURCE_INTEGRITY_OK`; BUILD/REVIEW/DONE/SHIP gates must fail closed; external BOARD bytes must never authorize `SOURCE_CLOSED`; `source show`, `source status`, context cold/audit, validation, and recovery must return controlled refusals without reading/echoing external content or throwing. Include an lstat/open swap regression where the host permits it.

[P1] [CORE-003] tools/saipen_engine/intake.py::{_archive_closed_locked,close_receipt,archive_receipt,recover_orphans}
EVIDENCE: `_archive_closed_locked` writes archive metadata then moves the authoritative body to cold storage and removes active metadata before the close transaction is committed (`intake.py:1018-1052`). Only AFTER that destructive phase returns does `close_receipt` write the tombstone and update the active/tombstone index (`1061-1124`, specifically 1109-1114). `recover_orphans` recognizes indexed-active + archived-body as `ARCHIVE_COMMIT_PENDING` at 1341-1359, but is explicitly read-only and performs no settlement. Retry cannot converge: `close_receipt` requires active metadata and returns `TICKET_NOT_FOUND` at 1068-1070; `archive_receipt` does the same at 1134-1144 unless both a tombstone and archive already exist. Fault-injection reproduction patched `_write_tombstone` to raise immediately after `_archive_closed_locked`: first close returned `VALIDATION_FAILED: simulated crash after archive move`; active body=False, active meta=False, archive body=True, index still listed SRC-001 active; `recover_orphans` reported `ARCHIVE_COMMIT_PENDING`; retry `close_receipt` -> `TICKET_NOT_FOUND`; retry `archive_receipt` -> `TICKET_NOT_FOUND`; validator reported `active receipt SRC-001 has no metadata` and `ORPHAN_RECEIPT SRC-001`.
DEFECT: The close/archive lifecycle contains a durable crash state that the implementation can diagnose but no supported API can repair. This violates the stated restart/recovery lifecycle and can strand an otherwise valid authoritative receipt permanently between ACTIVE and CLOSED.
REPAIR: Make close an idempotent resumable state transition. The smallest safe design is to recognize `ARCHIVE_COMMIT_PENDING` from the indexed active identity plus complete archived artifacts, verify the archived digest/Contract/coverage against the still-indexed projection, reconstruct/write the exact tombstone, then atomically settle index active->tombstone. Alternatively reorder through an explicit durable pending-close record, but do not create a second source of truth. `close_receipt` and/or `archive_receipt` retry must finish every state produced by an interrupted `_archive_closed_locked`; `source recover` may remain diagnostic only if ordinary idempotent retry performs the settlement. Never infer closure from an archive body alone without validating the closure evidence.
VERIFY: Fault-inject after every durable close step: archive-meta write, body move, active-meta removal, coverage move, Contract move/revision moves, tombstone write, and index write. After process restart, repeated `source close SRC-N` must converge to one CLOSED tombstone and one valid archive with no duplicate receipt, no lost body, no active index entry, and `validate_project == []`. Repeating close/archive after settlement must be `ALREADY_SATISFIED`/equivalent idempotent success.

[P1] [CORE-004] tools/saipen_engine/intake.py::capture cross-reference commit (`linked_work`, `amends`)
EVIDENCE: Capture validates `work` and `amends` only syntactically at `intake.py:496-499`. A new receipt immediately stores `linked_work` and optional `amends` in durable metadata at 562-578, then commits metadata/Contract/coverage/index at 580-616 BEFORE attempting `_link_board_projection` at 617-619. If the Work does not exist, `_link_board_projection` returns `ORPHAN_RECEIPT`, but the invalid Work reference is already authoritative. The exact-duplicate path has the same ordering error: it writes `linked_work` into metadata and index at 527-533 before the BOARD projection at 534-539. Verified on the valid project fixture: `capture(..., work="T-999")` returned `ORPHAN_RECEIPT` yet `source status` showed `linked_work: T-999`; retrying the same body without Work remained `ORPHAN_RECEIPT`, while retrying with real `T-001` returned `SOURCE_WORK_CONFLICT`; validator then reported `active receipt SRC-001 references missing Work T-999`. There is no supported relink/unlink source command, so the receipt is poisoned without manual metadata surgery. Separately, `capture(..., source_kind="corrective_followup", amends="SRC-999")` returned `SOURCE_RECEIVED`, status permanently exposed `amends: SRC-999`, and `validate_project` returned no error because dangling amendment references are never checked.
DEFECT: Cross-reference fields become durable source authority after grammar validation but before referential validation. A simple wrong Work ID can permanently trap a valid immutable source body in an unrecoverable linkage state, while a nonexistent amendment target silently produces a broken receipt graph. Source durability and linkage durability have been conflated.
REPAIR: Preserve the intended body-first durability while separating it from reference commitment. A missing/invalid Work must leave the captured source recoverably UNLINKED (or in one explicit pending-link state) rather than committing the bad Work into active metadata/index. Validate the Work against canonical BOARD before committing `linked_work`, then make BOARD projection + metadata/index settlement idempotent so exact retry with the correct Work can attach the existing receipt. Validate `amends` against an existing receipt identity across active/tombstone history before making it authoritative; extend structural validation to reject dangling amendment references already on disk. Do not mutate or replace the original amended receipt.
VERIFY: Regressions must prove: capture with T-999 still preserves the exact source bytes but does not permanently set `linked_work=T-999`; retry of the identical body with T-001 reuses the same SRC identity and links successfully; duplicate-link failure cannot leave metadata/index ahead of BOARD; nonexistent `amends=SRC-999` is rejected or retained only as an explicitly recoverable pending relation; valid amendment of active, closed, and purged/tombstoned identities follows the documented policy; manually-created dangling amendment metadata is detected by `validate_project`.

CORE_DONE_WHEN: CORE-001..CORE-004 are repaired without weakening current invariants; hostile symlink/reparse tests prove zero outside-root read/write/delete authority; interrupted source close converges after every durable crash boundary; invalid Work/amendment references cannot become unrecoverable authoritative state; all new regressions plus source-receipt, liveness, runtime, USERPERSON, core-path and validation gates pass on the repaired implementation.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-26T21:48:00+03:00
WAVE: AUDIT SECOND WAVE
TARGET: SAIPEN_26-08-2026-T20-40-07.zip; same supplied archive lineage as completed Audit Core, unpacked at project root /mnt/data/saipen_audit_core
BASELINE: SAIPEN v7.231.3; archive SHA256 8cc25a1922e7765749526c5de73a76047905d1d664242dcc2e44bec8dfadf60c; Git HEAD 337a0ef5ad94ccbedad253959b66f30855eec2cb
CORE_BASELINE: SAIPEN v7.231.3; archive SHA256 8cc25a1922e7765749526c5de73a76047905d1d664242dcc2e44bec8dfadf60c; Git HEAD 337a0ef5ad94ccbedad253959b66f30855eec2cb
GIT_CONTEXT: PRESENT main@337a0ef5ad94ccbedad253959b66f30855eec2cb - baseline remains unchanged through continuation 2/6. Supplied .saipen work-state retains its original modified/deleted/untracked material; audit probes used isolated temporary fixtures only. Final observed Git porcelain-set fingerprint: SHA256 312aa3cec320cdb37e7d6ece00aff30ac9635089c6b8dc2953d7b509fa855295
SAIPEN_CONTEXT: STALE - supplied work-state deletes tracked sealed LOG-001.md through LOG-014.md and contains modified/untracked kitchen, translation, producer and recovery material; treated only as orientation and never as implementation authority
AUDIT_SCOPE: Reused the completed Core and prior Second-Wave maps instead of restarting. Preserved W2-001..W2-009, then examined only still-uncovered authority/lifecycle seams: clone-visible release receipt continuation evidence, release-scope reuse by undo ownership, remaining direct canonical checkpoint consumers, effect-policy precedence, and related persistence/restart behavior. This is the complementary same-project lens required by the Second-Wave command. :contentReference[oaicite:0]{index=0}
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: NONE
VERIFIED_INSTEAD: Previously established focused verification remains valid: 202/202 focused regressions passed and `python -m compileall -q tools` passed. Continuation verification added `python tools/test_second_wave_receipt_fixes.py` -> 33/33 PASS and `python -m unittest -q tools.test_effect_authorization` -> 34 tests PASS with 1 PowerShell-host skip. Purpose-built isolated probes still reproduced the project defects below: hostile journal/lock/producer/release-index paths can escape ownership; external conformance storage remains CURRENT_PASS; malformed explicit capability can mutate; external release-scope evidence is trusted; unsafe checkpoint/lineage carriers are consumed inconsistently; a minimal or externally symlinked `release_receipt.json` is accepted as ship evidence; and a project policy can loosen a read-only capability from DENY to ALLOW. Archive SHA256 and Git HEAD remained unchanged
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 11
HANDOFF: IMPLEMENTATION_AGENT

[P0] [W2-001] tools/saipen_engine/journal.py::{Journal.start,_atomic_json,_atomic_write,run_mutation,_recover_locked}
EVIDENCE: Journal containment validates the operation directory itself, but generated children are subsequently opened by pathname without proving the final node. `Journal.start` writes deterministic staged evidence directly with `(self.dir / name).write_bytes(content)` at journal.py:1340-1356. `_atomic_json` at 469-472 uses predictable `path.with_suffix(".tmp")` followed by `write_text`, and `_atomic_write` at 475-481 uses predictable `<target>.tmp` followed by `write_bytes`; both writes follow an existing final symlink before `replace`. Normal apply reaches `_atomic_write` at 2274-2277 and recovery reaches it at 2584-2601. Hostile reproductions proved: a staged-evidence symlink caused `run_mutation` to return COMMITTED after overwriting an external file; a `<target>.tmp` symlink overwrote external bytes and was then published as the canonical target before post-write verification refused; an `operation.tmp` symlink was overwritten with operation-manifest JSON while the mutation returned COMMITTED.
DEFECT: Generated child filenames are treated as trusted because their parent namespace was validated. Predictable temp/staged nodes remain attacker-controlled filesystem objects, so the canonical mutation/recovery mechanism can overwrite outside-root files and can publish a symlink before detecting the invalid result. Post-write ownership verification is too late.
REPAIR: Replace pathname-following journal publication with one owned no-follow creation/replacement primitive. Create unique same-directory temporary regular files using exclusive creation, write through the bound descriptor, fsync as required, and atomically replace only after validating destination ownership. Deterministic staged evidence must use exclusive regular-file creation and reject any pre-existing symlink/reparse/non-regular node. Apply the primitive to operation.json/progress.json, staged payloads, normal targets and recovery targets.
VERIFY: Test final symlink/reparse nodes for staged evidence, operation/progress temp files and target temp files; parent-directory reparses; and lstat/open swaps where supported. Exercise fresh apply and recovery. External bytes/existence must remain unchanged, canonical targets must never become links, and normal crash/retry behavior must remain idempotent.

[P0] [W2-002] tools/saipen_engine/lock.py::{FileWriterLock,WriterLock,ProducerLock}
EVIDENCE: All three lock implementations validate owner directories but then follow the final lock node. `FileWriterLock.acquire` derives a holder key with `self.path.resolve()` and executes `open(self.path, "a+b")` at lock.py:85-100. WriterLock and ProducerLock do the same for core.lock and producer-<role>.lock. Hostile final symlinks to nonexistent external files caused all three lock classes to CREATE the external target and report successful acquisition/release. Release also recomputes the holder key from the mutable pathname, so a post-acquisition path replacement can desynchronize process-local ownership from the descriptor actually unlocked.
DEFECT: Lock-directory containment is not lock-object containment. Normal lock acquisition can mutate outside the ownership root, and mutable-path holder identity can leave stale in-process reservations.
REPAIR: Validate or exclusively create the final lock as a regular non-reparse node and bind the OS lock to that exact descriptor. Store the canonical holder key at successful acquisition and use that stored key for release. Refuse symlink/junction/reparse/non-regular final nodes and unsafe parents.
VERIFY: For all three classes, test final links to existing/nonexistent external targets, parent reparses and pathname replacement after acquisition. Assert zero external creation/modification and zero leaked holder state. Preserve normal same-process and cross-process exclusion.

[P0] [W2-003] tools/saipen_engine/producer.py::{_resolve_namespace_ownership,ProducerEpoch,StagingGeneration.begin,_publish_under_lock,scan_ready,recover,_recover_under_lock}
EVIDENCE: `_resolve_namespace_ownership` proves only the producer namespace itself; STAGING, READY, generation directories and `producer_epoch.json` are then accessed with following filesystem APIs. Verified hostile probes: linked STAGING caused begin() to create an external generation tree; linked READY caused publish() to write outside the project and scan_ready() to accept that external package; linked STAGING during recovery caused recursive deletion of an external generation directory; linked `producer_epoch.json` supplied external epoch 99 and claim() advanced authority to 100 from those outside bytes.
DEFECT: Producer ownership stops one directory too early. Mutable authority below the namespace can redirect prepare/publish/read/fencing/recovery operations outside project ownership.
REPAIR: Use one descendant ownership resolver for producer_epoch.json, STAGING, each generation and manifest/payload, READY/package files, SETTLED and cleanup targets. Every existing component must be the expected owned non-link type; absent children may be created only beneath proven ancestors. Publication must use safe exclusive/atomic creation. Recovery must prove the exact directory immediately before deletion and never rmtree through a link/reparse point.
VERIFY: Test STAGING/READY/SETTLED/epoch links and parent reparses for saitranslate and saiwiki. No external artifact may be created, read as authority or deleted. Preserve epoch monotonicity, stale-worker fencing and READY idempotence.

[P0] [W2-004] tools/saipen_engine/release.py::{_restore_index_bytes,_restore_index,_restore_index_from_record}
EVIDENCE: Git-index rollback checks whether `index.lock` exists but never owns that lock. `_restore_index_bytes` uses predictable `<index>.restore-tmp`, writes bytes, then `os.replace`s it onto the live index. A hostile `.git/index.restore-tmp` symlink caused exact captured Git-index bytes to overwrite an external file and then made `.git/index` itself that symlink. The check-then-replace sequence also leaves a race in which another Git process may acquire index.lock after SAIPEN's test and before restoration.
DEFECT: Recovery bypasses Git's canonical index-lock transaction and uses an attacker-controlled predictable temp pathname. A rollback meant to preserve staging can corrupt both an external file and concurrent Git index work.
REPAIR: Restore through an exclusively owned Git-reported `index.lock`: non-following validate its parent/final node, create the lock exclusively, write captured bytes through the descriptor, fsync, then atomically rename that owned lock to the Git-reported index. Treat dangling symlink/reparse index.lock as unsafe/occupied. Preserve linked-worktree index locations.
VERIFY: Cover malicious restore-tmp/index.lock nodes, a real foreign Git lock, concurrent acquisition and linked worktrees. External files and foreign writer state must remain unchanged; successful restoration must reproduce exact original index bytes.

[P0] [W2-005] tools/saipen_engine/conformance.py::{_iter_receipts,_lookup_receipt_index,_read_indexed_receipt,_find_receipt_by_id,latest_receipt,conformance_status,current_conformance_pass}
EVIDENCE: Conformance writes validate containment, but `_iter_receipts` follows `.saipen/recovery/conformance` through `is_dir()` and then scans regular children without proving the ancestors. A legitimate current PASS receipt was moved to an external directory and the project conformance directory replaced by a symlink. `latest_receipt(root,"core")` still returned it, `conformance_status` remained CURRENT_PASS and `current_conformance_pass` remained True.
DEFECT: The strongest closure proof can be imported from outside project ownership. External mutable storage can therefore authorize terminal/DONE/convergence/crew closure despite write-side containment explicitly forbidding the topology.
REPAIR: Make every conformance lookup use one ownership-bound receipt namespace reader. Prove `.saipen`, recovery, conformance, index and receipt nodes with non-following semantics and descriptor-bound reads. Unsafe topology is INVALID, never NOT_RUN/CURRENT_PASS.
VERIFY: After creating a real CURRENT_PASS, replace final receipt, conformance directory, recovery parent or index carrier with symlink/junction/reparse nodes and test an ancestor swap race. `current_conformance_pass` must become false and external content must remain untouched.

[P0] [W2-006] tools/saipen_engine/capability.py::negotiate_capability; tools/saipen.py::_negotiate_capability and mutating command guards; direct negotiation consumers
EVIDENCE: `negotiate_capability` maps both an absent declaration and any non-empty unrecognized `SAIPEN_CAPABILITY` value to DEFAULT_CAPABILITY `full`. Downstream `capability_error` explicitly says an unknown capability must fail closed, but it never sees the malformed value after negotiation launders it into full. Public CLI reproduction: `SAIPEN_CAPABILITY=read-only` made `ticket add` return CAPABILITY_DENIED with no writes; `SAIPEN_CAPABILITY=nonsense` made the same command return TICKET_ADDED and mutate LOG/BOARD/STATE.
DEFECT: A malformed explicit host capability declaration is promoted to the strongest writable/publish-capable session. A typo at the authority boundary grants mutation instead of refusing it.
REPAIR: Distinguish UNSET from INVALID. Genuine absence may retain the backwards-compatible default; any present non-empty value outside the closed capability set must remain invalid and fail closed at every public boundary. Centralize the predicate rather than using scattered `== "read-only"` checks.
VERIFY: Test absent, empty-by-contract, every valid capability and multiple malformed values. Representative ticket/claim/transition/crew/producer/release mutations under an invalid capability must perform zero canonical/Git changes.

[P0] [W2-007] tools/saipen_engine/release.py::{_scope_path,_load_scope,_scope_paths}; tools/saipen_engine/convergence.py::_attribution_claims; tools/saipen_engine/controls.py::_reviewed_dirty_ownership
EVIDENCE: Reviewed release scope is canonical authorization evidence, but all three consumers independently follow `.saipen/kitchen/release_scope` and parse its JSON without proving the carrier hierarchy. In a Git fixture with dirty `a.txt`, an external symlinked scope directory containing a matching T-001 claim changed `attribution_problems(root)` from `unattributed main-source delta: a.txt` to `[]`. `_load_scope` independently accepted an external record carrying valid schema/ticket/project identity/live source head/exact hash. Continuation inspection found `_reviewed_dirty_ownership` repeats the same raw `scope_dir.glob()` + `scope_path.read_text()` authority and can therefore use the same external claim to classify milestone-scope dirt as reviewed Work.
DEFECT: The evidence saying "these exact bytes were reviewed/owned by Work" may come from an external mutable directory, and three separate parsers enforce different subsets of the contract. Release authorization, convergence attribution and undo ownership can therefore agree on evidence the project does not own.
REPAIR: Create one ownership-safe release-scope decoder/reader. Prove release_scope and the exact ticket record with non-following/reparse-safe descriptor-bound reads; centralize schema, ticket, lineage/project identity, source-head/fingerprint and per-path hash validation. Release, convergence and controls must consume that decoded model instead of reparsing raw JSON.
VERIFY: Test directory/final-file symlink/junction/reparse nodes, non-regular records and lstat/open swaps. Perfect external records must satisfy neither release, convergence nor undo ownership. A normal in-project journal-created scope must retain exact-byte authorization.

[P1] [W2-008] tools/saipen_engine/journal.py::{_read_lineage_strict,ensure_project_lineage,run_mutation}; tools/saipen_engine/paths.py::project_lineage_identity
EVIDENCE: Two readers disagree over `.saipen/IDENTITY.md`. `journal._read_lineage_strict` uses `is_file()/read_text()` and follows a final symlink; `paths.project_lineage_identity` uses non-following checks and descriptor-bound reading and rejects the same node. With IDENTITY symlinked to external valid lineage bytes, `project_lineage_identity(root)` returned None while `ensure_project_lineage(root)` accepted the external lineage. `run_mutation` then COMMITTED a receipt under that lineage, but `semantic_receipt_snapshot` excluded it as foreign and identical retry/recovery returned PROJECT_MISMATCH.
DEFECT: Bootstrap and recovery disagree on project identity, allowing one successful mutation to create durable evidence the same project cannot later recognize or recover.
REPAIR: Centralize identity loading into an ownership-safe tri-state reader: ABSENT, PRESENT_VALID or PRESENT_UNSAFE/MALFORMED. Both identity consumers must use it. Unsafe present state must fail closed and never be treated as first-use absence.
VERIFY: Test final/ancestor symlink or reparse, non-regular/malformed carrier, lstat/open pivot, genuine absence and valid regular identity. Every COMMITTED receipt must remain visible to snapshot, retry and recovery.

[P1] [W2-009] tools/saipen_engine/codec.py::{checkpoint_preflight,read_checkpoint_doc,read_document}; tools/saipen_engine/snapshot.py::ProjectSnapshot.capture; tools/saipen.py::_status; tools/saipen_engine/fast_check.py::validate_project; tools/saipen_engine/{floor.py,intent.py,subs.py,release.py,conformance.py,crew.py} remaining direct STATE/BOARD/LOG readers
EVIDENCE: Canonical STATE/BOARD authority still has following pathname readers. `checkpoint_preflight`, `read_checkpoint_doc` and generic `read_document` use `is_file()/read_bytes`; ProjectSnapshot relies on those for STATE/BOARD; `_status` separately reads STATE, captures a snapshot, then separately rereads BOARD rather than consuming one captured generation. `fast_check.validate_project` also directly consumes those readers. Additional continuation inspection found direct canonical reads in floor.py, intent.py, subs.py, release.py, conformance.py and crew.py. Public hostile reproduction replaced STATE.md with a symlink to an external copy containing `next_action: EXTERNAL SENTINEL`; `saipen status --json` consumed the external bytes. On a valid fixture, replacing STATE with a symlink to an external byte-identical copy still produced `validate_project(root) == []`.
DEFECT: Canonical checkpoint reads are weaker than history/receipt ownership and can consume external state. Status additionally combines independently reopened generations, allowing an internally inconsistent projection during a concurrent commit.
REPAIR: Create one ownership-safe checkpoint capture for STATE/BOARD/LOG hierarchy and exact bytes. Use non-following ancestor/final validation plus descriptor binding. Make ProjectSnapshot the canonical read snapshot for status and thread captured bytes into validator/router/intent/floor/sub/release/crew/conformance consumers where a call-scoped generation is required.
VERIFY: Test STATE/BOARD/LOG final and ancestor links/reparses, non-regular nodes and descriptor swap races. Status, validation and planning must either use one owned generation or refuse; they must never echo/trust outside bytes or mix generations.

[P1] [W2-010] tools/saipen_engine/release.py::{_committed_release_receipts,_release_evidence,_find_release_ticket,_log_has_ship,_classify_continuation,_preflight_release}
EVIDENCE: The clone-visible `.saipen/kitchen/release_receipt.json` bypasses the strict semantic operation-receipt decoder. `_committed_release_receipts` checks only `published.is_file()`, JSON-decodes it and appends it when `record.get("operation") == "release_receipt"`. It does not require schema_version, op_id, tag, source_head, source_tree_fingerprint, mode, project_lineage, recorded_at or structural/lineage consistency before `_find_release_ticket` and `_log_has_ship` treat the record as committed ship evidence. Isolated reproduction with a regular in-project file containing only `{"operation":"release_receipt","version":"9.9.9","ticket_id":"T-777"}` made `_committed_release_receipts` return the record, `_find_release_ticket(...,"9.9.9")` return `T-777`, and `_log_has_ship(...,"9.9.9","T-777")` return True. Replacing the receipt with a symlink to an external full JSON record produced the same positive evidence. These predicates participate in phase-DONE ticket discovery and START_TAG continuation classification/preflight.
DEFECT: A clone-visible release closure artifact is treated as self-authenticating based on three fields and filesystem-following existence. Partial/corrupt/stale or externally owned JSON can therefore become committed ship evidence and authorize a release continuation path whose actual closure identity was never proved.
REPAIR: Add one strict ownership-safe decoder for `release_receipt.json`, parallel in quality to the semantic journal receipt decoder. Require canonical schema and exact field types, safe op/ticket IDs, project lineage equality, version/tag consistency, valid source-head/fingerprint shapes and mode/crew semantics. Bind the final carrier and ancestors with non-following reads. Before a published receipt can satisfy `_find_release_ticket` or `_log_has_ship`, verify that its source/closure relationship is compatible with the live continuation state; malformed or unsafe presence must be invalid evidence, not silently ignored or accepted. Prefer a typed `PublishedReleaseReceipt` model shared by all readers.
VERIFY: Feed missing-field, wrong-type, wrong-lineage, wrong tag/version, stale source, malformed-id and external symlink/reparse receipts through `_committed_release_receipts`, phase-DONE planning and START_TAG preflight. None may provide ticket/ship evidence. A genuine release-generated receipt from a fresh clone must still allow the intended continuation/idempotence path.

[P2] [W2-011] tools/saipen_engine/effects.py::{default_policy,load_policy}; tools/test_effect_authorization.py::PolicyLoadTests; tools/saipen.py::_permissions
EVIDENCE: `load_policy` documents that project policy may tighten the capability-derived baseline and "tightening never loosens", but implementation directly assigns every recognized project value: `policy[key] = value`. With capability `read-only`, the baseline correctly sets fs.write/fs.delete/repo.mutate to DENY; a project policy containing `{"fs.write":"ALLOW","fs.delete":"ALLOW","repo.mutate":"ALLOW"}` changes all three to ALLOW. The existing `test_project_policy_tightens_only` verifies only the full->MANUAL direction and therefore misses the inverse. `_permissions` exposes this effective policy as the user-visible/current-session diagnostic, so it can report mutation policy ALLOW while the capability boundary correctly refuses mutation.
DEFECT: Policy precedence violates its own monotonic contract. A lower-authority project policy can loosen the negotiated session capability, producing contradictory authorization truth and making the diagnostic claim stronger permission than the session actually owns.
REPAIR: Define an explicit policy-strength ordering `ALLOW < MANUAL < DENY` and merge each project override as `max(base, override)` by strictness. A project may tighten full from ALLOW to MANUAL/DENY, but may never lift read-only DENY. Preserve unknown-key/value fail-closed reporting.
VERIFY: Add a full cross-product test for every baseline capability and ALLOW/MANUAL/DENY override. In particular read-only plus project ALLOW must remain DENY for every mutating effect; full plus MANUAL/DENY must tighten. `saipen permissions` must never advertise a mutating effect more permissively than the negotiated session capability.

SECOND_WAVE_DONE_WHEN: W2-001 through W2-011 are repaired without weakening existing containment, durability, recovery, capability, release or idempotence rules; hostile journal/lock/producer/Git-index paths demonstrate zero outside-root mutation; conformance cannot obtain CURRENT_PASS from external storage; malformed explicit capabilities fail closed; release-scope evidence is one owned canonical authority for release/convergence/undo; lineage and canonical checkpoints use bound owned reads and coherent call-scoped generations; `release_receipt.json` is strict owned closure evidence rather than self-attesting JSON; project effect policy can only tighten the session capability; all new regressions plus the established focused regression suites, second-wave receipt tests, effect-authorization tests and compileall pass.

Connection interrupted. Waiting for the complete answer

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-27T00:26:00+03:00
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: SAIPEN_26-08-2026-T20-40-07.zip; same supplied archive lineage as completed Audit Core and Audit Second Wave; implementation inspected read-only at /mnt/data/perf_continue2 with disposable clean clones/temp fixtures for measurement
BASELINE: SAIPEN v7.231.3; archive SHA256 8cc25a1922e7765749526c5de73a76047905d1d664242dcc2e44bec8dfadf60c; Git HEAD 337a0ef5ad94ccbedad253959b66f30855eec2cb
PREVIOUS_BASELINE: SAIPEN v7.231.3; archive SHA256 8cc25a1922e7765749526c5de73a76047905d1d664242dcc2e44bec8dfadf60c; Git HEAD 337a0ef5ad94ccbedad253959b66f30855eec2cb
GIT_CONTEXT: PRESENT main@337a0ef5ad94ccbedad253959b66f30855eec2cb - baseline remained unchanged through continuation 3/6; final `git diff --name-only HEAD -- tools` is empty; all scaling/resource probes used disposable fixtures
SAIPEN_CONTEXT: STALE - supplied STATE/LOG/improve/producer material remains internally stale/validator-red and was used only for orientation, never as performance authority
AUDIT_SCOPE: Reused all Core/Second-Wave and earlier Performance maps. Preserved PERF findings already established, then exhausted remaining high-value scaling/resource seams: Attempt lineage folding, source-receipt duplicate detection/validation, SourceIdentity large-file memory, full-validator conformance closure rebinding, common crew/status/recovery profiles, and remaining lifetime-history/project-wide scans. This is the same third read-only performance/stability/effectiveness lens required by the attached command. :contentReference[oaicite:0]{index=0}
TEST_STATUS: TEST_PARTIAL
TEST_LIMITATION: NONE
VERIFIED_INSTEAD: Earlier same-wave targeted performance gates T-1019/T-1020/T-1021/T-1023 PASSed; focused control/recovery and producer suites PASSed; continuation source-receipt suite `python -S -m unittest -q tools.test_source_receipts` -> 35/35 PASS; final `python -S -m compileall -q tools` PASS; disposable clean-HEAD full validator PASSed. `python -S tools/perf_wave_regressions.py` and later `python -S tools/continuity_probes.py` each exceeded the bounded 120 s audit execution window, so no aggregate PASS/FAIL is fabricated. Purpose-built isolated measurements directly reproduced every bottleneck below. Audited implementation bytes and Git HEAD remained unchanged
STATUS: PERFORMANCE: COMPLETE
TICKETS: 17
HANDOFF: IMPLEMENTATION_AGENT

[P1] [PERF-001] PROVEN BOTTLENECK tools/saipen_engine/operations.py::{_iter_operation_records,_latest_convergence_stage,_plan_convergence_stage}
EVIDENCE: The supplied project has 1487 semantically decoded settled operation receipts. Five direct `semantic_receipt_snapshot(root)` measurements were 290.58, 281.02, 298.54, 298.45 and 287.14 ms; median 290.58 ms. `_iter_operation_records` creates a fresh lifetime semantic snapshot whenever records are not supplied. Stage G first asks `_latest_convergence_stage(root, predecessor["G"])`, whose predecessor is F, then calls `_latest_convergence_stage(root, "F")` again for the same metadata. An instrumented G planner reached that branch and recorded exactly two lifetime snapshots consuming 633.81 ms of 722.68 ms total wall time before an unrelated baseline refusal.
ISSUE: One immutable command-scoped PLAN repeatedly decodes, validates and sorts the entire operation lifetime; G derives the exact same F record twice. Cost scales with all settled history.
OPTIMIZE: Capture one `SemanticReceiptSnapshot` at the first history-dependent point in `_plan_convergence_stage` and project all stage lookups from its `records`. Add/use a pure `_latest_convergence_stage_from_records` or equivalent supplied-snapshot path. Retain the already-derived predecessor record, including G's F metadata.
GUARDRAIL: Preserve strict receipt decoding, lineage/project binding, corruption refusal, COMMITTED filtering, UTC ordering and deterministic tie-breaking. APPLY/retry/recovery/new commands must capture fresh authority; no persistent cache.
VERIFY: On >=1500 receipts with valid E/F evidence, each F/G/H/I PLAN must invoke `semantic_receipt_snapshot` at most once; G must derive F once. Compare OperationPlan output byte-for-byte and record 20-run warm median/p95 before/after.

[P1] [PERF-002] PROVEN BOTTLENECK tools/saipen_engine/operations.py::_plan_convergence_stage; tools/saipen_engine/conformance.py::{convergence_stage_satisfied,clean_exit_allowed,conformance_status}
EVIDENCE: `_plan_convergence_stage` already obtains live `SourceIdentity` as `ident`, but E/H route through conformance without supplying that current identity and recompute it. G similarly calls `clean_exit_allowed(root)` although it accepts `source_identity=`. One E PLAN probe captured SourceIdentity twice, taking 65.32 ms and 95.65 ms, 160.97 ms combined of 165.98 ms total before the existing CURRENT_FAIL refusal.
ISSUE: A hardened multi-pass Git/worktree identity is immediately rediscovered against the same PLAN generation.
OPTIMIZE: Thread an explicit freshly captured `current_source_identity` through `convergence_stage_satisfied` into `conformance_status`; call `clean_exit_allowed(root, source_identity=ident)` for G.
GUARDRAIL: A historical/predecessor identity must never be substituted for the current sample. PLAN->source change->APPLY must still fail stale revalidation. No cross-command cache.
VERIFY: Instrument E/G/H PLAN. Each invocation must call `compute_source_identity` exactly once while preserving verdict/head/fingerprint. Retain a source-moves-after-PLAN regression proving APPLY refusal.

[P1] [PERF-003] PROVEN BOTTLENECK tools/saipen_engine/controls.py::{_read_text_lossy,focus_projection}
EVIDENCE: `_read_text_lossy(path, limit=256_000)` executes unrestricted `read_bytes()` before checking length/NUL. In an isolated Focus fixture the no-large-file control was 3.02 ms with 20,888 KB max RSS. Adding one 64 MiB candidate that ultimately had to be ignored changed the run to 38.73 ms and 86,380 KB RSS, +65,492 KB merely to discover the file exceeded the 256 KB bound.
ISSUE: The logical input limit does not bound physical I/O/allocation; latency and peak memory depend on the largest candidate file.
OPTIMIZE: After ownership-safe lstat/descriptor binding, reject a stable file whose size exceeds the limit or read only `limit + 1` bytes. Reject on an extra byte, NUL, invalid UTF-8 or identity movement.
GUARDRAIL: Preserve the existing 1200-file, 40-match and three-line bounds, UTF-8-sig decoding, binary exclusion, filename-hit semantics, ordering and symlink/reparse refusal.
VERIFY: Cover 255999/256000/256001 bytes plus 64 MiB and >=1 GiB files. Oversized content reads must be <=limit+1, or zero with safe stat rejection, and RSS must remain O(limit).

[P1] [PERF-004] PROVEN BOTTLENECK tools/freshness.py::{_read_regular_info,_record_evidence,_iter_no_git,_stream_digest,_evidence_to_records,compute_source_identity,revalidate_source_identity}; tools/saipen_engine/{journal.py::hash_file_dependency,_hash_file,_hash_tree,_hash_delete_tree,hash_delete_tree; operations.py::_plan_record_scope; controls.py::_project_tree_fingerprint,_live_entry,_reviewed_dirty_ownership; crew.py::_crew_scope_from_receipts; producer.py::file_sha256; release.py::_load_scope,_snapshot_worktree,_hash_file; convergence.py::_attribution_claims,convergence_verdict}
EVIDENCE: Numerous hash-only paths materialize whole files via `read_bytes()`. Representative 256 MiB probes: `_project_tree_fingerprint` 1.12 s / 282,608 KB max RSS; `release._load_scope` 0.37 s / 283,328 KB; `journal.hash_file_dependency` 1.10 s / 280,136 KB. Continuation additionally proved the supposedly bounded SourceIdentity reader is not streaming: `_read_regular_info` accumulates 1 MiB chunks then `b"".join`s them; `_record_evidence` hashes that complete bytes object and `_stream_digest` calls `_read_regular_info` again. One Git repository with a single 128 MiB untracked sparse file made `compute_source_identity('.')` take 1.812 s and peak at 372,304 KB RSS. No-Git evidence uses the same full-buffer reader.
ISSUE: Hash/fingerprint operations that conceptually require only bounded digest state retain O(file_size) bytes, and `_read_regular_info` can transiently hold both chunk-list and joined copies. The project's primary freshness primitive therefore has the same large-file memory pathology as release/control hashing.
OPTIMIZE: Introduce descriptor-bound streaming primitives that return stable stat identity, size and content digest without materializing contents. For SourceIdentity's three race-proof passes, stream each file into a per-file SHA-256; on the final pass stream raw bytes directly into the canonical global fingerprint framing while computing a comparison digest against prior evidence. For tree/file hash callers, stream fixed-size chunks using the exact existing length/path framing.
GUARDRAIL: Every stable SourceIdentity/tree/file digest must remain bit-identical. Preserve the existing multi-pass same-size/mtime-restored swap detection, no-follow semantics, mode/path framing and stale-state behavior. Do not reduce race-proof pass count merely for speed.
VERIFY: Compare old/new digests for empty, small, 64 MiB, 128 MiB, 256 MiB and >=1 GiB files plus deterministic trees in Git and no-Git models. Peak RSS must become O(chunk size), not O(file size). Inject replacement/truncation between passes and require the existing fail-closed result.

[P1] [PERF-005] PROVEN BOTTLENECK tools/saipen_engine/controls.py::{validate_milestones,_logged_milestone_ids}
EVIDENCE: `validate_milestones` rereads project lineage inside the manifest loop, starts a fresh ancestor walk from every checkpoint for cycle detection, and finds duplicate LOG checkpoint IDs with repeated `logged.count`. A valid linear synthetic history measured 100 checkpoints 6.73 ms; 500 -> 41.48 ms; 1000 -> 164.92 ms; 2000 -> 757.07 ms; 4000 -> 2564.72 ms. A physical 1500-manifest fixture reached 257.76 ms.
ISSUE: Naturally append-only milestone validation becomes O(N²) even for a perfectly valid chain.
OPTIMIZE: Capture lineage once; validate direct parent constraints in one pass; use tri-color DFS, memoized chain terminal states or equivalent O(N) cycle detection; use a frequency map/Counter for duplicate LOG checkpoint IDs.
GUARDRAIL: Preserve every existing missing/non-earlier parent, self/multi-node cycle, duplicate sequence/LOG, lineage mismatch and corruption diagnostic.
VERIFY: Generate 100/1000/4000/10000 valid and hostile graphs. Parent-edge visits must be O(N), lineage reads O(1), current corruption tests remain green, and the 4000-node ~2.56 s quadratic profile disappears.

[P1] [PERF-006] PROVEN BOTTLENECK tools/saipen_engine/subs.py::{_capture_clean_instance,sub_clean}; tools/saipen_engine/controls.py::{_snapshot_entries,plan_milestone}; tools/saipen_engine/plan.py::TargetPlan
EVIDENCE: Transactional copy/archive plans represent target contents as in-memory `bytes`. `_snapshot_entries` retains the complete milestone payload before APPLY. One 256 MiB milestone target measured 0.61 s / 292,096 KB max RSS. `_capture_clean_instance` retains all SubSaipen file bytes and then currently hashes the same tree again; one 256 MiB instance measured 3.46 s / 814,664 KB max RSS. PERF-004 streaming removes the hasher's extra full-buffer copy but does not remove the transaction's retained payload.
ISSUE: Exact transactional semantics currently require O(total archive/copy payload bytes) Python memory before journal APPLY, making large milestone/sub-clean operations vulnerable to memory exhaustion.
OPTIMIZE: Add a file-backed exact-content TargetPlan form. PLAN records owned source path, size/hash and read precondition; under writer lock APPLY revalidates then streams into journal-owned immutable staging and commits/replays from that stage. Keep small generated textual targets inline. Compute sub-clean's tree digest during the same staging capture rather than rereading.
GUARDRAIL: PLAN remains zero project mutation; stale source before/during staging refuses; no destructive deletion occurs until every required archive byte is durably staged; recovery must replay exact staged bytes.
VERIFY: Use 1 MiB, 256 MiB and >=1 GiB fixtures; RSS must stay bounded. Inject source mutation and crashes at staging/delete boundaries and prove byte-identical archive recovery.

[P1] [PERF-007] PROVEN BOTTLENECK tools/saipen_engine/producer.py::{ProducerPackage.from_dict,StagingGeneration._publish_under_lock,StagingGeneration.scan_ready,ready_package}; tools/saipen_engine/{crew.py::_producer_ready_health,_producer_integrated; intent.py producer consumers}
EVIDENCE: Producer READY package JSON embeds complete binary payloads as base64. Publication retains raw payload, base64 representation and serialized JSON. `scan_ready` performs `read_text` + JSON parse + eager base64 decode for every package even when the caller needs only health/package identity. A valid READY package carrying one 32 MiB payload produced a 44,739,804-byte JSON file; one scan took 0.41 s and 140,160 KB max RSS versus roughly 0.06 s / 20 MB for metadata-only. Crew and intent paths repeatedly invoke READY metadata discovery; `_producer_integrated` similarly parses SETTLED package records when only identity/binding evidence is required.
ISSUE: Metadata discovery is coupled to payload materialization, so health/planning memory and CPU scale with aggregate package payload size.
OPTIMIZE: Separate immutable small READY/SETTLED manifests from owned payload sidecars/content-addressed blobs. Metadata scan validates identity/source binding/role revision/payload hash metadata without loading payload bodies. Integration streams only the selected package's payloads. Maintain compatibility reading for existing inline records during migration.
GUARDRAIL: Publication remains atomic, self-contained and restart-safe; package identity and exact payload hash binding remain unchanged; stale/corrupt payloads fail before canonical integration.
VERIFY: Test 0-byte, 32 MiB, 256 MiB and multiple concurrent packages. Crew/intent health scans must not read bodies and RSS must be independent of aggregate payload size; selected integration must still verify exact payloads.

[P1] [PERF-008] PROVEN BOTTLENECK tools/saipen_engine/attempt.py::{build_attempts,contract_errors,next_attempt_id}; consumers in tools/saipen_engine/{context.py,fast_check.py,controls.py,operations.py} and tools/validate.py
EVIDENCE: `build_attempts` first folds events into records, then for every record independently walks its complete `supersedes` ancestry at attempt.py:269-281. Valid linear chains therefore perform approximately 1+2+...+N predecessor visits. Synthetic valid open-attempt chains measured median: 100 -> 0.67 ms; 500 -> 11.42 ms; 1000 -> 44.45 ms; 2000 -> 190.64 ms; 4000 -> 800.76 ms. The fold is used by context projection, fast validation, milestone/control planning, attempt open/close, ticket finishing and full validation. `next_attempt_id(events)` also reparses the same events after callers have already built records.
ISSUE: Long-lived Work execution history turns ordinary validation/planning into O(N²) Attempt lineage work. The cost is structural rather than data corruption and grows rapidly as episodes accumulate.
OPTIMIZE: Validate supersedes graph in O(N) with tri-color DFS, memoized terminal state/path compression, or exploit the earlier-open invariant while still preserving independent cycle diagnostics for malformed imported history. Track/highest Attempt numeric ID during the same fold, or derive the next ID from already-built records, instead of reparsing all events.
GUARDRAIL: Preserve duplicate/open-close checks, earlier-supersedes requirement, independent cycle detection, active-attempt ordering, ticket/agent ownership and exact malformed-history diagnostics.
VERIFY: Benchmark valid chains at 100/1000/4000/10000 plus self-cycle, two-node cycle, missing predecessor and forward-reference hostile cases. Predecessor-edge processing must be O(N); all current Attempt lifecycle/admission behavior must remain identical.

[P2] [PERF-009] STRONGLY EVIDENCED WASTE tools/saipen_engine/operations.py::{_plan_record_scope,record_scope}
EVIDENCE: Every reviewed path missing from the worktree launches its own `git ls-files --error-unmatch -- <rel>` subprocess. A fixture containing 120 tracked deletions produced exactly 120 of these calls and 130 subprocesses total, spending 155.33 ms before a later unrelated fixture refusal.
ISSUE: One repository-membership query is implemented as O(number of deleted scope paths) process startup/IPC.
OPTIMIZE: Gather missing candidates, issue one NUL-delimited `git ls-files -z --deleted` query and validate by set membership. A fixed-size bounded pathspec batch is acceptable where necessary.
GUARDRAIL: Missing-untracked remains SOURCE_SCOPE_MISSING; tracked deletions stay valid; spaces/Unicode stay exact; no index mutation.
VERIFY: 0/1/10/120/1000 deletion scopes must use O(1) or bounded-batch Git subprocesses with identical accepted/refused results.

[P2] [PERF-010] STRONGLY EVIDENCED WASTE tools/saipen_engine/release.py::{plan_release,_scope_for,_scope_paths,_load_scope}
EVIDENCE: Release PLAN fully validates `_scope_for(...)`, discards its decoded record, then `_scope_paths(...)` immediately calls `_load_scope(...)` again. The no-publish branch repeats the same pattern. One reviewed 64 MiB file produced consecutive `_load_scope` times of 68.09 and 63.93 ms.
ISSUE: The same authoritative scope is parsed and hashed twice in one immutable PLAN decision with no intervening mutation boundary.
OPTIMIZE: Retain `scope_record = _scope_for(...)` and derive `scope_paths` directly from it. Keep APPLY's independent fresh scope/source/index preflight.
GUARDRAIL: Do not remove APPLY-time validation or change release-plan canonical output/continuation semantics.
VERIFY: Instrument scope loads/hashes. Exactly one scope generation per ticket per PLAN; APPLY still performs a fresh independent verification. Preserve PLAN->file-change->APPLY stale refusal.

[P2] [PERF-011] PROVEN BOTTLENECK tools/saipen_engine/controls.py::{_all_manifests,milestone_status,undo_preview}
EVIDENCE: `milestone_status` needs only current + optional parent but decodes every historical `CP-*/manifest.json`. Synthetic measurements: 100 manifests ~2.2-2.6 ms; 1000 -> 28.01-80.88 ms; 5000 -> 216.51-387.80 ms, median 292.10 ms. `undo_preview` runs full `validate_milestones` and then immediately `_all_manifests` again, reparsing the same complete generation.
ISSUE: Common status scales with lifetime history and undo performs duplicate complete manifest capture.
OPTIMIZE: Status ownership-safely reads current.json, current manifest and direct parent only. Let milestone validation return/consume one command-scoped decoded MilestoneSnapshot for undo.
GUARDRAIL: Current/parent corruption and recovery-pending precedence remain fail-closed; full validation still checks all manifests.
VERIFY: With 1/1000/5000 checkpoints, status manifest opens must remain O(1); undo must decode the complete manifest generation once.

[P2] [PERF-012] PROVEN BOTTLENECK tools/saipen_engine/fast_check.py::validate_project; tools/saipen_engine/log.py::{read_history_snapshot,snapshot_contract_errors,history_contract_errors}; tools/validate.py::_saipen_exit; tools/saipen_engine/conformance.py::generate_conformance_receipt
EVIDENCE: `fast_check.validate_project` captures `_history = read_history_snapshot(root)` for `validate_texts`, then calls `history_contract_errors(root)`, whose wrapper performs another full history snapshot. On clean HEAD with 4475 events / 1,130,968 ledger characters, exactly two captures occurred; standalone history-pass median was 18.91 ms and fast validation was ~35-48 ms. Full `tools/validate.py` has the same command-generation waste at closure: it captures `_canonical_history_snapshot` during validation, but `_saipen_exit -> generate_conformance_receipt -> _log_hash -> history_hash` rereads/reparses the lifetime ledger. Clean-HEAD cProfile showed exactly two `read_history_snapshot` calls; conformance `_log_hash` consumed ~83 ms in that profiled run.
ISSUE: Both fast and full validator commands discard an already-valid command-scoped lifetime ledger generation and reopen it solely for checks/hashes that can consume the existing snapshot.
OPTIMIZE: Fast gate calls `snapshot_contract_errors(_history)`. For full validation, allow `generate_conformance_receipt` to accept trusted call-scoped checkpoint/history hashes or a checkpoint snapshot captured by the actual validator run. On early failure paths before such a snapshot exists, retain the fresh self-contained fallback.
GUARDRAIL: Every validator invocation still captures current history at least once; conformance receipt must bind the exact bytes actually validated and must not reuse snapshots across commands. Receipt generation after source movement must still fail/revalidate appropriately.
VERIFY: On >=4000 events, fast validation calls `read_history_snapshot` once. A normal full validator PASS that emits a conformance receipt also performs only one complete ledger capture while producing exactly the same log_hash. Corrupt/moved-history tests remain fail-closed.

[P2] [PERF-013] STRONGLY EVIDENCED WASTE tools/saipen_engine/journal.py::{semantic_receipt_snapshot,_recovery_identity_binding}; tools/saipen_engine/paths.py::{project_lineage_identity,runtime_lock_identity}
EVIDENCE: During one semantic receipt snapshot, `_recovery_identity_binding` rereads the same live project lineage per lineage-bearing receipt. On the supplied project 1483 of 1487 records triggered the same lookup. A paired 15-run experiment measured current median 281.80 ms versus 266.21 ms with one pre-captured lineage, a 15.59 ms median delta.
ISSUE: One project-level identity carrier is reread O(receipts) times and can theoretically produce mixed-generation binding if it moves mid-scan.
OPTIMIZE: Capture strict lineage once, runtime identity once when required, bind all records against those values, then revalidate identity once after processing; if it changed, invalidate the whole snapshot.
GUARDRAIL: Foreign-lineage exclusion and legacy binding remain exact. Identity movement during capture fails closed. Reuse remains command-scoped.
VERIFY: At 1/100/1500/5000 receipts, lineage carrier reads must remain O(1); injected mid-capture movement invalidates the snapshot; stable records/errors/digest remain semantically identical.

[P2] [PERF-014] PROVEN BOTTLENECK tools/saipen.py::_status; tools/saipen_engine/conformance.py::{conformance_status,_checkpoint_hash_mismatch,_log_hash}; tools/saipen_engine/snapshot.py::ProjectSnapshot
EVIDENCE: `_status` captures a complete `ProjectSnapshot` with STATE/BOARD/LOG hashes and parsed LOG history. With a legitimate CURRENT_PASS receipt, `conformance_status` subsequently calls `_checkpoint_hash_mismatch`, which independently rehashes STATE/BOARD and `_log_hash -> history_hash -> read_history_snapshot`. In a disposable clean clone, one `status` with CURRENT_PASS made exactly two lifetime history captures; the second conformance-side pass consumed ~50 ms in that profiled run.
ISSUE: A projection that already has a coherent checkpoint generation reconstructs that same checkpoint identity solely to compare conformance.
OPTIMIZE: Allow projection-only conformance status to consume the already captured ProjectSnapshot/checkpoint hashes. Keep self-contained fresh hashing for callers that do not possess a trusted snapshot.
GUARDRAIL: Mutation authorization must not accidentally reuse stale display snapshots. Status must classify conformance against the same generation it renders.
VERIFY: CURRENT_PASS status with >=4000 events calls `read_history_snapshot` exactly once and returns identical conformance fields/reasons. Corrupt/mismatch fixtures remain fail-closed.

[P2] [PERF-015] PROVEN BOTTLENECK tools/saipen_engine/controls.py::_git_snapshot_entries; tools/saipen_engine/controls.py::plan_milestone historical baseline capture
EVIDENCE: Historical milestone capture validates the commit once, then launches one `git ls-tree` and one `git show` for every requested path. A disposable repository with 120 tiny tracked files recorded exactly 241 subprocesses: one commit check + 120 ls-tree + 120 show; wall time 485.08 ms.
ISSUE: Historical sparse-baseline acquisition is 2N+1 Git process launches although Git plumbing supports batched metadata/blob retrieval.
OPTIMIZE: Resolve requested paths via one or bounded few NUL-safe `git ls-tree -rz` calls, then retrieve blobs through one persistent `git cat-file --batch` process or bounded equivalent.
GUARDRAIL: Preserve exact path matching, missing/symlink/non-blob refusal, revision binding and binary byte fidelity. Never checkout or mutate index/worktree.
VERIFY: 0/1/10/120/1000 historical paths including spaces/Unicode/absent/symlink entries must use O(1) or bounded-batch processes and produce byte-identical milestone entries/digests.

[P2] [PERF-016] STRONGLY EVIDENCED WASTE tools/saipen_engine/intake.py::_find_exact_duplicate; source capture duplicate/orphan discovery
EVIDENCE: `_find_exact_duplicate` first walks all indexed active receipts and reads their metadata, then its crash-window fallback scans every `SRC-*.md` body and hashes it, including bodies already represented by the index rather than only uncommitted orphans. Instrumented unique-source capture after 20/100/300 existing small active receipts performed 20/100/300 metadata reads and 21/101/301 active-body reads respectively. With 16 existing 2 MiB active source bodies, capturing one new tiny source reread 17 active bodies totaling exactly 32.0 MiB and took 29.2 ms from warm local storage; none of those 16 historical indexed bodies could possibly be an orphan.
ISSUE: Every unique source capture re-hashes the complete active authoritative source corpus after already consulting indexed receipt state. Repeated capture therefore accumulates lifetime O(total active source bytes) I/O per new source and O(N²) historical rereads across a long series.
OPTIMIZE: Restrict the crash-window body scan to bodies not durably represented by the active index/metadata transaction state. Indexed active receipts continue to use their metadata/index duplicate comparison; only genuinely unindexed/incomplete body candidates are content-hashed for orphan adoption. If projection drift is encountered, fail closed or perform the minimum candidate verification needed; do not trade away duplicate correctness merely to trust a corrupt index.
GUARDRAIL: Exact duplicate active/closed detection, corrupted-receipt refusal, crash-after-body orphan adoption and force semantics remain unchanged. A durable orphan with no committed metadata/index must still be found by exact digest.
VERIFY: Populate 10/100/1000 indexed receipts with multi-MiB bodies plus 1-3 deliberately orphaned bodies. Capturing a new unique source must read/hash only the orphan candidates, not indexed body payloads. An exact indexed duplicate and exact orphan duplicate must still resolve to the existing SRC identity.

[P2] [PERF-017] LOW-RISK SIMPLIFICATION tools/saipen_engine/intake.py::{_board_source_links,validate_project}
EVIDENCE: `validate_project` begins with `_board_source_links(root)`, which ownership-safely reads and parses BOARD once. Later, inside the loop over every active source receipt, each `linked_work` branch independently executes another `parse_board((root / ".saipen/BOARD.md").read_text(...))` to find the linked ticket and inspect its section/projection. Synthetic valid fixtures with every receipt linked to T-001 measured: 20 receipts -> 21 `parse_board` calls / 6.37 ms; 100 -> 101 calls / 39.44 ms; 300 -> 301 calls / 87.05 ms, with zero validation errors.
ISSUE: Source-receipt validation reparses the identical BOARD generation once per active receipt despite already having parsed it before entering the loop. Cost scales with receipt count × BOARD parse size and also weakens command-generation coherence by reopening BOARD repeatedly.
OPTIMIZE: Capture one owned BOARD document/model at validator entry. Derive both reverse `source_receipts` links and `tickets` from that same parsed model; pass/reuse it throughout active-receipt checks. `_board_source_links` may accept/return the parsed model instead of reopening it.
GUARDRAIL: Preserve every linkage diagnostic: missing Work, missing reverse projection, DONE with unresolved coverage and BOARD parse failure. Use the ownership-safe checkpoint reader required by Second Wave; do not optimize back to a weaker direct read.
VERIFY: With 20/100/300/1000 active linked receipts, source validation must parse BOARD exactly once per invocation and return byte-for-byte equivalent error sets on valid, missing-Work, projection-drift and DONE-with-unresolved fixtures.

PERFORMANCE_DONE_WHEN: PERF-001..PERF-017 are implemented without persistent stale caches, weakened ownership/freshness checks or changed recovery semantics; convergence PLAN consumes one lifetime receipt snapshot and one live SourceIdentity; Focus performs bounded reads; SourceIdentity and all hash-only paths use bounded-memory streaming with bit-identical digests and the same race-proof guarantees; milestone/sub-clean transactions stage large payloads without O(total-payload) RAM; producer READY/SETTLED health discovery remains metadata-only; milestone and Attempt graph validation are O(N); milestone status reads O(1) manifests; release scope is loaded once per PLAN while APPLY stays independently fresh; tracked-deletion and historical-baseline Git work uses bounded subprocess counts; fast/full validators and CURRENT_PASS status reuse their command-scoped checkpoint/history generation; semantic receipt binding rereads project lineage O(1) times; source capture hashes only genuine orphan candidates rather than all indexed bodies; source-receipt validation parses BOARD once; targeted T-1019/T-1020/T-1021/T-1023, source/producer/control/recovery suites and compileall remain green; the aggregate performance/continuity suites complete successfully; and before/after median, p95, peak-RSS, bytes-read, graph-edge-visit and subprocess-count measurements demonstrate every claimed improvement.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.
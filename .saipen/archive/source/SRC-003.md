# SAIPEN — Audit Handoff

RUN_ID: acb-mat-mtbssckf-007eed250980
GENERATED_AT: 2026-08-27T20:30:08.576434
PROJECT_NAME: SAIPEN
TARGET: SAIPEN_27.08.26-T06-27-48.zip -> fully unpacked SAIPEN project root; live implementation source at embedded Git HEAD was audited read-only
BASELINE: archive SHA256 42adcd80c79108e630b984972ea0b0e130315ab389e3c264429b64a261c81472; VERSION 7.231.8; main@9e8bb6c17e5109b4cfdcc69c0ec88ef3c711fdd3
GIT_CONTEXT: PRESENT main@9e8bb6c17e5109b4cfdcc69c0ec88ef3c711fdd3 - embedded Git readable with per-command safe.directory handling; supplied archive was already dirty in operational .saipen/archive metadata, while audited implementation paths under tools/, saipen/, tests/, extensions/ and .github matched HEAD
SAIPEN_CONTEXT: STALE - supplied archive omits tracked .saipen/logs/LOG-001.md through LOG-015.md and contains archive-added operational artifacts; STATE also references an inaccessible Windows saipen_home. These were treated as non-gating historical/work-state context. A clean committed-HEAD export validates successfully.
TOTAL_TICKETS: 28
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 4 tickets (baseline: archive SHA256 42adcd80c79108e630b984972ea0b0e130315ab389e3c264429b64a261c81472; VERSION 7.231.8; main@9e8bb6c17e5109b4cfdcc69c0ec88ef3c711fdd3)
- Wave 2 (Second Wave): 17 tickets (baseline: archive SHA256 42adcd80c79108e630b984972ea0b0e130315ab389e3c264429b64a261c81472; VERSION 7.231.8; main@9e8bb6c17e5109b4cfdcc69c0ec88ef3c711fdd3)
- Wave 3 (Performance): 7 tickets (baseline: archive SHA256 42adcd80c79108e630b984972ea0b0e130315ab389e3c264429b64a261c81472; VERSION 7.231.8; main@9e8bb6c17e5109b4cfdcc69c0ec88ef3c711fdd3)

---
## 01 — AUDIT CORE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-27T06:53:44+03:00
WAVE: AUDIT CORE
TARGET: SAIPEN_27.08.26-T06-27-48.zip -> fully unpacked SAIPEN project root; live implementation source at embedded Git HEAD was audited read-only
BASELINE: archive SHA256 42adcd80c79108e630b984972ea0b0e130315ab389e3c264429b64a261c81472; VERSION 7.231.8; main@9e8bb6c17e5109b4cfdcc69c0ec88ef3c711fdd3
GIT_CONTEXT: PRESENT main@9e8bb6c17e5109b4cfdcc69c0ec88ef3c711fdd3 - embedded Git readable with per-command safe.directory handling; supplied archive was already dirty in operational .saipen/archive metadata, while audited implementation paths under tools/, saipen/, tests/, extensions/ and .github matched HEAD
SAIPEN_CONTEXT: STALE - supplied archive omits tracked .saipen/logs/LOG-001.md through LOG-015.md and contains archive-added operational artifacts; STATE also references an inaccessible Windows saipen_home. These were treated as non-gating historical/work-state context. A clean committed-HEAD export validates successfully.
AUDIT_SCOPE: public CLI/shortcut routing; parser and command semantics; STATE/BOARD ownership and claims; journaled mutations and recovery; stop/checkpoint lifecycle; goal safety valves; converge/ccc entry; validator contracts; CI/test orchestration; producer/crew/release boundaries; focused regression suites. Audit contract followed from attached AUDIT CORE. :contentReference[oaicite:0]{index=0}
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: NONE - the canonical unittest discovery and tt path were able to start but fail to terminate because project code recursively launches the same discovery suite through tt routing tests; this is project behavior, not a missing sandbox prerequisite
VERIFIED_INSTEAD: python -m compileall -q tools PASS; clean committed-HEAD python -S tools/validate.py PASS; tests/validate.sh PASS; tools/audit_order.py PASS for 42 tools; 321 selected unit test methods PASS with one additional skip; direct isolated stop/ownership/safety-valve, tt recursion/write, and ccc/validator reproductions performed. Full unittest discovery exceeded 120s because of verified tt recursion. tools/audit_checks.py was separately attempted on clean HEAD and exceeded the bounded 100s execution window without a trustworthy verdict.
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT

[P0] [CORE-001] tools/saipen.py::main stop/ss execution branch
EVIDENCE: The full-mode stop/ss branch constructs ".saipen/kitchen/digest.md" directly, creates its parent, and calls Path.write_text() with the three-line digest. It does not invoke the existing journaled stop_checkpoint operation despite comments referring to LOG -> BOARD -> STATE checkpointing. On a valid isolated ControlFixture, ss returned rc=0/STOP while SHA256 values for .saipen/LOG.md, .saipen/BOARD.md and .saipen/STATE.md remained unchanged and only digest.md was created. In a second valid fixture where digest.md was a symlink to an external sentinel file, ss returned success and overwrote that external file. Passing the same symlink arrangement through the canonical stop_checkpoint path instead produced VALIDATION_FAILED because the target escaped its owner root and left the sentinel unchanged. The read-only ss branch additionally returns a generic description instead of the required actual done:/remaining:/awaiting: projection.
DEFECT: The public stop command bypasses the project's mutation/journal/owned-path machinery and falsely reports a completed stop without performing its required checkpoint. This creates both a local arbitrary-file-clobber primitive through a project-controlled digest symlink and a correctness failure where STOP success can coexist with uncheckpointed LOG/BOARD/STATE. Read-only behavior also disagrees with the stop contract.
REPAIR: Remove the ad-hoc digest Path.write_text path from tools/saipen.py. After CORE-002 hardens the canonical stop operation, route full and dry-run stop/ss through that single operation/planner. All filesystem targets, including digest.md, must pass existing ownership/path validation and the journaled transaction path. Implement read-only stop as a zero-write projection derived from the same canonical planner/state snapshot and emit the real three digest fields plus the resumable next action rather than a generic message. Preserve existing refusal/recovery semantics instead of adding a second stop implementation.
VERIFY: Add regression coverage proving: (1) digest.md -> external symlink cannot modify the external sentinel; (2) normal full stop changes the required LOG/BOARD/STATE/digest surfaces coherently and returns STOP only after the transaction commits; (3) injected interruption at each journal stage recovers without false successful partial checkpoint state; (4) read-only stop emits done:, remaining:, awaiting: while hashes of all project files remain unchanged; (5) CLI tests assert checkpoint side effects, not merely code == STOP.

[P1] [CORE-002] tools/saipen_engine/operations.py::stop_checkpoint
EVIDENCE: stop_checkpoint reads BOARD into an unused _board variable, performs no _active_claim_refusal check, does not refresh a SELF active claim, and does not include BOARD in its mutation targets. Its next-action derivation preserves an existing legal WAIT or otherwise falls back to PHASE/current-task or "saipen continue" without consulting execution_intent goal counters/safety caps. Runtime reproduction on a valid active fixture with T-7 owned by "tester" showed stop_checkpoint(root, "intruder", ...) returning ok=True/STOPPED, changing STATE.agent to "intruder" while BOARD.DOING remained owned by "tester" and its claim_time remained unchanged. A valid goal fixture with goal_waves: 3 and no existing WAIT returned STOPPED and wrote next_action "saipen continue" instead of the required maintenance safety-valve WAIT. A SELF-owned active stop likewise left BOARD/claim_time unchanged.
DEFECT: The canonical stop helper itself does not enforce live lease ownership, does not checkpoint/refresh active BOARD ownership, and bypasses the goal safety valve when deriving resumable state. A foreign caller can therefore split STATE.agent from the live BOARD owner, while a self-owned stop can leave a stale lease and an exhausted goal loop can be disguised as a routine continuation.
REPAIR: Make stop_checkpoint use the same active-claim authority rules as normal transition/checkpoint planning. Refuse a fresh FOREIGN active claim with zero writes. For a SELF active ticket, refresh the claim and include BOARD in the canonical journaled target/order. Derive next_action with safety-valve priority: preserve an already-valid WAIT when appropriate, but if goal_waves >= 3 or goal_tickets >= 20 synthesize the canonical MAINTENANCE safety-valve WAIT instead of routine continuation; do not silently reset goal counters or execution intent. Derive digest remaining/awaiting from the parsed BOARD/blocker/WAIT state rather than the currently ignored BOARD value.
VERIFY: Regression matrix must prove: FOREIGN fresh claimant receives a deterministic zero-write refusal; SELF stop refreshes claim_time and keeps ticket ownership coherent; goal_waves=3 and goal_tickets=20 independently produce the canonical safety-valve WAIT with counters preserved; an existing legal WAIT remains valid; no-active ordinary stop retains the legitimate "saipen continue" behavior; dry-run/full planning expose equivalent targets and next-action semantics; journal recovery preserves LOG/BOARD/STATE/digest coherence.

[P1] [CORE-003] tools/saipen.py::tt / "saipen test" and tools/test_command_routing.py
EVIDENCE: The tt branch launches [sys.executable, "-m", "unittest", "discover", "-s", "tools", "-p", "test_*.py"]. tools/test_command_routing.py contains routing tests that iterate every declared shortcut and invoke the public CLI for each token; the declared set includes tt. Therefore the outer discovery reaches the routing test, which invokes tt, which launches the same discovery, which reaches the same test and invokes another tt recursively. Full unittest discovery started but did not terminate within 120 seconds. Direct tt executions likewise remained alive until bounded 6-8 second termination. After deleting all __pycache__ directories in an isolated clean copy, running tt for six seconds produced 49 .pyc files, contradicting the command's read-only/no-file-write contract. The current tt runner also executes only tools/test_*.py and therefore does not orchestrate/report all declared validation families such as run_scenarios.py, audit_checks.py, the portable floor and consumer tests. The current CI Fast Unit gate uses the same unittest discovery pattern, so it inherits the recursion. Excluding only the two all-shortcut routing methods that re-enter tt allowed the focused unit surface to run successfully; 321 selected methods passed and one additional method was skipped.
DEFECT: The test command and its own routing tests form an unbounded self-recursive process tree. This prevents the canonical test/CI path from completing, makes tt materially incomplete relative to its declared suite contract, and causes filesystem writes through Python bytecode generation despite a no-write command guarantee.
REPAIR: Introduce one explicit, non-recursive canonical test orchestrator shared by tt and CI. Enumerate the declared suite families deliberately and produce per-family PASS/FAIL status rather than recursively discovering a test that can invoke the orchestrator itself. Change routing tests for tt to validate dispatch using an injected/mock/sentinel test runner boundary rather than executing the complete test command from inside its own suite. Run read-only child Python checks with bytecode writing disabled, e.g. PYTHONDONTWRITEBYTECODE=1 and/or -B, and execute any legitimately mutating scenario/recovery verification only in isolated temporary project copies. Do not weaken or skip the existing test families to make the recursion disappear.
VERIFY: Full tools unittest discovery terminates normally including new regressions; public "saipen test"/tt terminates and reports every declared suite family independently; CI Fast Unit completes rather than recursively spawning; pre/post Git/status and project-tree hashes remain unchanged by tt; a deliberately injected failing family yields overall FAIL while preserving the per-suite result; starting from a tree with zero __pycache__ files leaves zero new .pyc/recovery/project artifacts in the audited target.

[P1] [CORE-004] tools/saipen.py::ccc and tools/saipen_engine/operations.py::set_converge_intent
EVIDENCE: The ccc CLI path calls set_converge_intent(project_root, actor, "ship") and returns CONVERGE_SET. The generic state-only operation records a LOG event equivalent to "execution intent -> converge/ship" and updates STATE, but does not write the mandatory ccc pre-SHIP source-head marker and does not checkpoint/refresh BOARD when work is active. The validator explicitly rejects converge_target: ship when that recovery marker is absent because an interrupted I -> SHIP -> J route cannot be recovered safely. On a clean committed-HEAD copy with a valid temporary saipen_home, ccc returned rc=0/CONVERGE_SET and immediate tools/validate.py returned rc=1 specifically with "STATE.md converge_target: ship has no ccc entry marker -- a crash cannot recover the I -> SHIP -> J route without its LOG evidence". On an active SELF-owned T-7 fixture, ccc also left BOARD bytes and claim_time unchanged while writing only the generic LOG decision and STATE converge intent.
DEFECT: ccc transitions persistent state into the special converge/ship recovery mode without atomically recording the source-head evidence that defines that mode, and without checkpointing active ownership as required at the phase switch. A successful ccc invocation therefore immediately creates validator-invalid, crash-ambiguous state and can leave a stale active lease.
REPAIR: Replace the generic set_converge_intent call for ccc with a dedicated journaled ccc-entry operation, or extend the canonical converge planner with an exact ship-entry variant. From one frozen snapshot, determine the canonical pre-SHIP Git HEAD, write the exact required ccc converge-target/source-head LOG marker, checkpoint/refresh a SELF active BOARD claim when present, set execution_intent/converge_target, and clear only the goal counters that the ccc contract requires, all as one recoverable transaction. Preserve the current task/resumable action across the transition. The CLI result must describe only the entry state actually committed; it must not imply A-I, SHIP or J-M have completed before their evidence exists.
VERIFY: On a clean full project, ccc followed immediately by tools/validate.py passes the ccc recovery-marker gate; the LOG marker contains the actual pre-SHIP HEAD used by the transaction; an active SELF ticket remains bound and receives a refreshed claim_time; fault injection after each journal target recovers to one coherent marker/BOARD/STATE state; early post-SHIP EE/QQ ordering remains rejected; goal counters are cleared exactly as specified; CLI regression assertions inspect marker, BOARD and STATE effects rather than only CONVERGE_SET.

CORE_DONE_WHEN: CORE-001 through CORE-004 are repaired at their root causes; stop/ss has one journaled owned-path implementation with correct live-claim, BOARD, safety-valve and read-only projection semantics; tt uses a non-recursive declared-suite orchestrator, completes the entire applicable suite and leaves the target tree unchanged; ccc atomically records its canonical pre-SHIP source-head marker and active-work checkpoint before entering converge/ship state; targeted regressions, full unit discovery, clean committed-tree validator, portable validation floor and all applicable canonical test families complete with trustworthy results and no unintended project writes.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-27T18:04+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: second-mtbgrqj6-63aa207a5d39
CAMPAIGN_MANIFEST_SHA256: 0150e79661d7b03b2fee434b93ea0cec2ec584e2c68ebb375b7d41bfbd13ff87
WAVE_ID: second
WAVE_INDEX: 2
WAVE_COUNT: 3
WAVE: AUDIT SECOND WAVE
TARGET: SAIPEN_27.08.26-T06-27-48.zip -> fully unpacked SAIPEN project; continuation inspection used the identical archive materialized at /mnt/data/audit_saipen_w2_cont2
BASELINE: archive SHA256 42adcd80c79108e630b984972ea0b0e130315ab389e3c264429b64a261c81472; VERSION 7.231.8; main@9e8bb6c17e5109b4cfdcc69c0ec88ef3c711fdd3
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@9e8bb6c17e5109b4cfdcc69c0ec88ef3c711fdd3 - embedded Git metadata readable; supplied archive was already dirty in operational .saipen/archive/test-recovery material while audited implementation paths correspond to HEAD; final implementation diff check for tools/, saipen/, extensions/ and .github was empty
SAIPEN_CONTEXT: STALE - supplied operational state diverges from HEAD, tracked historical LOG-001..LOG-015 files are absent from the archive, and STATE references an inaccessible Windows saipen_home; treated only as non-gating work-state context
AUDIT_SCOPE: startup/shutdown/retry boundaries; canonical journal staging/apply/settlement/recovery/cleanup; process and OS writer-lock ownership; producer epoch/staging/publication/retirement/recovery; liveness cache safety and concurrency; source-receipt close/archive/purge interruption; targeted producer crash resumption; release Git-index rollback; release-scope/convergence evidence CAS; convergence chronology/attribution; status/next/explain snapshot coherence; portable lineage; SubSaipen semantic-receipt error propagation; swallowed cleanup errors and repeatability
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: NONE - canonical unittest/tt execution starts but does not terminate because of previously verified CORE-003 recursive test orchestration; this is project behavior rather than a missing environment prerequisite
VERIFIED_INSTEAD: prior focused second-wave suites passed: regression_second_wave.py; regression_second_wave_audit_fixes.py 56/56; regression_second_wave_receipt_fixes.py 33/33; second_wave_regressions.py; test_source_receipts.py 35 tests; test_intent_audit_fixes.py 41 tests; test_v7_producer_parallelism.py 17 tests; test_crew_liveness_drift.py 14 tests; test_hostile_wave_regressions.py 11 tests; two hostile journal compaction tests also passed. Isolated reproductions verified journal .tmp/.staged symlink writes, liveness outside-root overwrite/delete and concurrent lost update, producer descendant/epoch escape, source-receipt interrupted relocation, failed lock-reservation leakage, final lock-file symlink coupling, malformed-STATE producer integration, Git index-lock race, impossible convergence chronology, attribution provenance/deletion/lineage errors, moved-project crew-defer rejection, mixed-generation routing, stale source-bound evidence commit, post-COMMITTED settlement failure/retry poisoning, false producer cleanup_pending reporting, and inconsistent SubSaipen corruption handling where `sub status saiwiki` exited 1 with an uncaught SemanticReceiptCorruptionError traceback while `sub list` exited 0 with code SUB_LIST against the same corrupt receipt namespace. Final `python -m compileall -q tools/saipen_engine tools/saipen.py` passed.
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 17
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/saipen.py::{_status,_next_action,_explain_next,_crew_liveness,_sub}; tools/saipen_engine/journal.py::{Journal.__init__,Journal.start,Journal.mark,_atomic_write,_atomic_json,_settle_journal,_drop_settled_staged,_compact_drop_staged,_compact_migrate_ops,compact_committed,run_mutation,recover,_recover_locked,recovery_preflight,semantic_receipt_snapshot}; liveness.py::{record_actionable,clear,_cache_path}; lock.py::{FileWriterLock,WriterLock,ProducerLock}; producer.py::{ProducerEpoch._decode,current,current_owner,owns,claim,StagingGeneration.begin,add_payload,publish,recover,_recover_under_lock,_retire_ready_package,integrate_packages_core,producer_namespace,_resolve_namespace_ownership}; intake.py::{capture,add_requirement,set_disposition,_archive_closed_locked,close_receipt,archive_receipt,purge_receipt,recover_orphans,validate_project}; intent.py::{collect_and_ship_producer,_active_core_ticket,_find_targeted_carriers,_targeted_producer_active_ticket,_resume_targeted_producer}; release.py::{_load_scope,_restore_index_bytes,_restore_index,_restore_index_from_record,execute_release,_apply_release}; convergence.py::{_event_number,_chrono_key,_stage_receipts,_pick_latest,_attribution_claims,attribution_problems,convergence_verdict}; operations.py::{_iter_operation_records,_latest_convergence_stage,_plan_record_scope,record_scope,_plan_defer_for_crew,defer_for_crew,_plan_convergence_stage,record_convergence}; snapshot.py::ProjectSnapshot.capture; fast_check.py::validate_checkpoint_surface; router.py::route_next; subs.py::{_iter_operation_records,_collect_linkage,_collect_review_state,sub_instance_health,sub_list,sub_status,sub_clean}; related controls/context/crew/effects/subs/conformance/regression contracts
COVERAGE_DEFERRED: no remaining high-value local second-wave lifecycle/boundary surface intentionally deferred; external remote publication/network interaction was not exercised because the audit forbids external infrastructure access; Windows-native msvcrt and junction/reparse behavior was statically inspected but could not be runtime-exercised in this Linux environment; canonical full test-suite completion remains prevented by prior CORE-003
CROSS_WAVE_REFERENCES: CORE-001 stop/ss bypasses canonical journaled owned-path checkpointing; CORE-002 stop_checkpoint lacks live ownership/BOARD refresh/safety-valve enforcement; CORE-003 tt recursively launches its own unittest discovery; CORE-004 ccc enters converge/ship without the mandatory pre-SHIP source-head recovery marker. These remain separate root causes and are not duplicated below.
RESIDUAL_UNCERTAINTY: Windows reparse semantics may expose additional variants of W2-001/W2-002/W2-003/W2-006; remote release publication was not contacted; PREVIOUS_WAVE_SHA256 was unavailable and was not fabricated; no further high-value local lifecycle/boundary defect was verified in the final continuation sweep

[P0] [W2-001] tools/saipen_engine/journal.py::{Journal.start,_atomic_write,_atomic_json} intermediate-file ownership
EVIDENCE: Canonical journal target ownership is validated, but journal-created intermediate nodes are not consistently bound. _atomic_write uses deterministic `<target>.tmp`; _atomic_json uses deterministic `path.with_suffix(".tmp")`; both write through the temporary pathname before replacement without exclusive no-follow creation. In an isolated fixture, `.saipen/STATE.md.tmp` symlinked to an external sentinel caused a canonical mutation to write planned STATE bytes through that symlink before later validation failed; the external sentinel changed and the canonical target could inherit the symlink through replacement. A separate progress.tmp reproduction overwrote an external sentinel during Journal.mark. The same invariant is broken earlier in Journal.start: staged evidence files use deterministic `staged_name(index, canonical_path)` and are written with `(self.dir / name).write_bytes(content)` without validating that final staged node. The fixed lineage migration op_id `op-migrate-lineage` makes this path predictable. Pre-creating `.saipen/recovery/ops/op-migrate-lineage/0_740e6c24e11b8ce9.staged` as a symlink to an external sentinel and then executing canonical ensure_project_lineage under the writer lock succeeded, created `.saipen/IDENTITY.md`, and replaced the external sentinel bytes with the staged lineage document.
DEFECT: The journal proves ownership of semantic final targets and operation directories but then trusts unproved descendant/intermediate files used to stage or atomically publish those targets. A hostile project can redirect canonical journal writes outside the project before final-target safety checks run. Fixed/retryable op identities and deterministic staged/temp filenames make this a reachable local arbitrary-file overwrite boundary violation in the mutation-safety layer itself.
REPAIR: Establish one descriptor-based owned-intermediate primitive and use it for both staging and atomic publication. Intermediate files must be exclusively created inside an already validated regular parent using unpredictable names where identity is not semantically required, O_CREAT|O_EXCL and O_NOFOLLOW where available, followed by fstat/type/ownership verification. Journal.start must refuse a pre-existing staged symlink/reparse/file instead of following it. _atomic_write/_atomic_json must write/fsync through the owned descriptor, revalidate the final target/parent immediately before atomic replacement, and remove only the exact temporary inode created by the operation. Preserve deterministic semantic hashes independently of temporary filenames.
VERIFY: Pre-create STATE.md.tmp, BOARD.md.tmp, LOG.md.tmp, operation.tmp, progress.tmp and known staged_name paths as links to external sentinels, including the fixed op-migrate-lineage staging path. PLAN/APPLY/recovery/lineage migration must perform zero outside-root writes. Canonical final targets remain regular owned nodes. Fault-inject after temporary creation, staging, fsync and replacement and prove cleanup removes only operation-owned intermediates while crash recovery remains idempotent.

[P0] [W2-002] tools/saipen_engine/liveness.py::{record_actionable,clear,_cache_path}
EVIDENCE: CACHE_REL resolves to `.saipen/cache/continuation-liveness.json`, but _cache_path merely concatenates project_root and CACHE_REL. record_actionable creates its parent and writes with Path.write_text without the project owned-target/no-follow machinery; clear performs unlink on the same unchecked path. In an isolated project, replacing `.saipen/cache` with a symlink to an outside directory containing continuation-liveness.json=`DO-NOT-TOUCH` caused record_actionable to return normally while overwriting the outside sentinel. clear then deleted that outside sentinel.
DEFECT: The liveness projection treats lexical project relativity as filesystem ownership. A project-controlled symlink/junction/reparse point in the cache ancestry or final node redirects ordinary continuation processing into arbitrary outside-root overwrite and deletion.
REPAIR: Route every liveness cache mutation through the canonical no-follow owned-path boundary. Validate `.saipen`, cache ancestry and the final cache node before mkdir/write/unlink; publish through a safe atomic writer; clear only a proven owned regular node. Ordinary in-root projection I/O may remain best-effort, but unsafe ownership must fail closed.
VERIFY: Test symlinked `.saipen/cache`, symlinked final continuation-liveness.json and Windows junction/reparse equivalents where supported. External sentinels remain byte-identical and undeleted. Legitimate first/repeat/stalled cache behavior and legitimate clear remain functional.

[P0] [W2-003] tools/saipen_engine/producer.py::{ProducerEpoch._decode,current,current_owner,owns,claim,StagingGeneration.begin,add_payload,publish,recover,_recover_under_lock,_retire_ready_package}
EVIDENCE: producer_namespace/_resolve_namespace_ownership validates the canonical producer namespace and ancestors, but later producer code trusts descendants such as producer_epoch.json, `.staging`, generation directories, payload trees, READY, SETTLED and SUPERSEDED. With namespace/.staging symlinked to an outside directory, StagingGeneration.begin successfully created a random generation, `.in-flight`, payload/ and staging.manifest.json outside the project. With an outside stale generation reachable through the same symlink, recover returned that generation in removed_staging and recursively deleted its directory and sentinel. add_payload creates target.parent before its later resolved-target containment check. ProducerEpoch reads also follow the final epoch path: an external valid producer_epoch.json reached through a symlink was accepted as epoch/owner authority, so external epoch 77/owner `foreign` became the fencing state for the local producer.
DEFECT: Producer ownership is established at the namespace boundary and then discarded for descendant reads and mutations. Forward prepare can write outside-root, recovery can recursively delete outside-root data, and fencing authority can be supplied by an external file.
REPAIR: Introduce one producer-owned descendant resolver/no-follow primitive rooted at the validated canonical namespace and require it before every descendant read or mutation. Existing ancestors and final nodes must reject symlink/junction/reparse escape. Epoch authority must come from a bound regular owned file. No mkdir may occur before containment is proven. Recursive cleanup must re-prove each owned regular directory immediately before deletion. READY/SETTLED/SUPERSEDED publication and retirement must use the same primitive.
VERIFY: Cover hostile epoch-file symlinks, `.staging` links, generation links, nested payload-parent links and READY/SETTLED/SUPERSEDED redirection. External epoch records never become authority; begin/add_payload/publish/recover/retire make zero outside-root changes; stale cleanup never deletes an external sentinel; legitimate epoch, parallelism, publication, recovery and retirement behavior remains green.

[P1] [W2-004] tools/saipen_engine/intake.py::{_archive_closed_locked,close_receipt,archive_receipt,purge_receipt}
EVIDENCE: _archive_closed_locked performs source lifecycle closure as independent filesystem mutations while project_writer_lock is held: archive metadata write, active-body os.replace, active metadata unlink, coverage/contract/revision moves, followed only later by tombstone/index updates. No durable transaction intent spans this sequence. In a valid SRC-001 fixture moved to VERIFIED, os.replace was fault-injected to fail while moving the contract after body/meta/coverage relocation succeeded. close_receipt returned VALIDATION_FAILED, but active body/meta/coverage were gone, active contract remained, archive body/meta/coverage existed, archive contract did not, index.json still declared SRC-001 active and no tombstone existed. status resolved archive metadata and reported CLOSED; validate_project reported missing active metadata/orphan state; retrying close_receipt returned TICKET_NOT_FOUND. purge_receipt follows the same sequential destructive lifecycle model.
DEFECT: Mutual exclusion is being treated as transaction atomicity. Process death or ordinary I/O failure can split source authority across active/archive/index/tombstone/coverage/contract/revision surfaces, after which the public retry path can reject the partially transitioned receipt instead of completing or rolling it back.
REPAIR: Execute close/archive/purge through the durable mutation journal or an intake-specific intent/commit protocol with equivalent replay guarantees. Persist enough before/after identity before destructive relocation to identify pre-operation, partial, committed and conflicting states. Publish terminal index/tombstone authority only at a defined commit boundary, or explicitly make every intermediate state replayable. Retrying the same requested transition after interruption must converge idempotently or return a precise conflict.
VERIFY: Fault-inject after every filesystem mutation in close/archive/purge, simulate restart and retry/recover. Every cut point converges to either complete pre-transition or complete requested terminal state with validate_project clean. close/archive never lose source bytes or split authority; purge either retains complete recoverable pre-purge evidence or reaches complete durable PURGED state.

[P1] [W2-005] tools/saipen_engine/lock.py::{WriterLock.acquire,ProducerLock.acquire}
EVIDENCE: WriterLock.acquire and ProducerLock.acquire reserve their canonical key in process-local _WRITER_HOLDERS/_PRODUCER_HOLDERS before calling open(self.path,"a+b"), but the open call is outside the exception cleanup that releases that reservation. FileWriterLock already handles this boundary correctly. With builtins.open fault-injected to raise OSError, WriterLock left its key registered and a fresh WriterLock immediately failed with WRITER_BUSY despite no OS lock existing. ProducerLock reproduced the same leak and a fresh retry failed with PRODUCER_BUSY.
DEFECT: An acquisition failure before the OS mutex exists permanently impersonates a live writer inside the process. A transient permissions/path/file-descriptor error can therefore disable subsequent core or producer mutations for the lifetime of a long-running host.
REPAIR: Once the process-local reservation has been installed, every subsequent failure before `_acquired=True` must close any created handle and conditionally remove only that instance's reservation under _HOLDERS_GUARD. Match FileWriterLock's unwind invariant or factor all three classes through one common reserve/open/lock/unwind helper.
VERIFY: Independently inject failure at open(), OS-lock acquisition and immediately after handle creation for WriterLock, ProducerLock and FileWriterLock. A fresh retry succeeds after every failed acquisition; a successful holder still rejects contenders; stale/failing cleanup never removes another live holder.

[P1] [W2-006] tools/saipen_engine/lock.py::{FileWriterLock,WriterLock,ProducerLock} final lock-file ownership
EVIDENCE: Constructors prove the lock directory belongs inside the expected owner, but acquire() does not no-follow validate the final `core.lock`, producer lock or generic file lock. It derives holder identity with `self.path.resolve()` and opens the path with `a+b`, thereby following a final symlink. In an isolated reproduction, two unrelated projects had `.saipen/locks/core.lock` symlinked to one external file. WriterLock for project 1 acquired successfully and project 2 then returned WRITER_BUSY because both lock identities collapsed onto the external inode. ProducerLock likewise accepted a producer lock symlink targeting an external file. FileWriterLock accepted `owner/x.lock -> outside/external.lock` despite its explicit ownership_root.
DEFECT: The lock directory is owned, but the actual mutex node is not. A project-controlled final symlink can move lock authority outside the project and couple unrelated projects into one writer domain, violating writer isolation and FileWriterLock's ownership contract.
REPAIR: Open/create the final lock node with no-follow semantics and prove the opened descriptor is a regular node inside its declared ownership root. Use descriptor/inode identity for process-local holder identity instead of trusting a resolved external pathname. Refuse a pre-existing final symlink/junction/reparse point with zero external locking or mutation. Apply the identical invariant to all lock classes.
VERIFY: final core.lock, producer lock and generic FileWriterLock symlinks to external files must refuse without touching or locking the outside node. Two unrelated projects pointing to the same external lock must both reject the invalid path instead of sharing a writer domain. Normal aliases of the same legitimate project must still serialize correctly.

[P1] [W2-007] tools/saipen_engine/intent.py::{_find_targeted_carriers,_targeted_producer_active_ticket,_resume_targeted_producer,collect_and_ship_producer}
EVIDENCE: collect_and_ship_producer falls back to targeted BOARD recovery when normal STATE-bound authority cannot be established. _find_targeted_carriers parses BOARD without rejecting board["errors"]. _targeted_producer_active_ticket can accept one matching BOARD carrier with phase=None when STATE cannot establish coherent task/phase authority. In an isolated fixture with one matching DOING T-9 and deliberately malformed STATE.md, it returned T-9/phase=None. With package/source/epoch prerequisites mocked current and integrate_packages_core instrumented, _resume_targeted_producer invoked integration and returned PRODUCER_REVIEW_REQUIRED before the later VERIFY transition attempted to establish legal phase state.
DEFECT: Targeted crash recovery promotes a BOARD carrier to mutation authority precisely when canonical STATE cannot be parsed or reconciled. Implementation payload may therefore change before the core checkpoint proves that the ticket is in a legal resumable phase.
REPAIR: Require successful parsing and coherent STATE/BOARD ticket binding before any targeted producer mutation. Reject BOARD parse errors, malformed STATE, task/carrier disagreement, ambiguous carriers and phases outside an explicit resumable whitelist. Re-establish any required claim and legal phase transition before integrate_packages_core. Preserve legitimate crash-window idempotence.
VERIFY: malformed STATE, BOARD errors, STATE.task mismatch, absent/illegal phase and multiple carriers must all make zero integration calls and preserve implementation/STATE/BOARD hashes. Valid TODO/SCOUT/BUILD/VERIFY interruption cases continue to resume the intended ticket idempotently.

[P1] [W2-008] tools/saipen_engine/release.py::{_restore_index_bytes,_restore_index,_restore_index_from_record}
EVIDENCE: _restore_index_bytes checks whether `.git/index.lock` exists and then later writes a restore temp and directly os.replace()s `.git/index`; it never atomically owns Git's mutex. _restore_index and _restore_index_from_record add more existence checks but do not close the check-then-act race. In a temporary real Git repository, an old index snapshot was saved and the live index was changed by staging different content. A deterministic injection created `.git/index.lock` only after SAIPEN's pre-check. _restore_index_bytes nevertheless replaced `.git/index` with the old saved bytes while the foreign lock remained present. The final index hash equalled the old snapshot.
DEFECT: Git's lock file is sampled as a warning flag rather than acquired as mutation ownership. A legitimate Git process can acquire index.lock after preflight and still have SAIPEN overwrite the index behind it, destroying or contradicting concurrent staged state.
REPAIR: Restore using Git-compatible exclusive index.lock ownership. Atomically create the lock, fail without touching the index if already owned, revalidate live index identity while holding the mutex, write/fsync the saved bytes to the owned lock and atomically rename it over `.git/index`. On failure remove only SAIPEN's own lock and never alter a foreign one.
VERIFY: Create a foreign index.lock after initial preflight but before restoration; rollback must refuse and preserve live index bytes. A pre-existing foreign lock remains byte-identical. Uncontended restoration reproduces exact saved index bytes including staged delete, intent-to-add and unmerged-index fixtures. Concurrent local Git writer tests prove SAIPEN never replaces the index while another process owns index.lock.

[P1] [W2-009] tools/saipen_engine/convergence.py::{_chrono_key,_stage_receipts,_pick_latest,convergence_verdict}
EVIDENCE: _stage_receipts explicitly defines canonical chronology as parsed real UTC first, event number second and op_id third because event numbering can become non-monotonic after torn/replaced history. _pick_latest contradicts that rule by selecting predecessors with `_event_number(item) < before_event`. A synthetic identity-consistent chain E 10:00/E-10, F 10:10/E-20, G 10:20/E-30, I 10:30/E-100, H 10:40/E-40 was accepted because 40 < 100 even though H occurred after I. convergence_verdict returned ok=True and emitted the impossible E-F-G-H-I sequence.
DEFECT: The verifier declares real UTC canonical but uses potentially non-monotonic event numbers as predecessor boundaries. Terminal convergence can therefore certify evidence whose real execution order is impossible.
REPAIR: Pass full canonical chronology keys between predecessor selections and admit only strictly earlier candidates. Keep event number solely as the deterministic equal-instant tie-break. Assert the chosen E<F<G<H<I chronology explicitly before returning terminal success.
VERIFY: H after I with a lower event number must refuse; earlier H with a higher event number remains eligible; equal timestamps order deterministically through event/op-id; legitimate monotonic chains and retry loops remain accepted.

[P1] [W2-010] tools/saipen.py::{_status,_next_action,_explain_next} + tools/saipen_engine/fast_check.py::validate_checkpoint_surface
EVIDENCE: ProjectSnapshot.capture provides one STATE/BOARD/LOG generation, but _status reads STATE independently before capturing a snapshot and BOARD independently afterward; _next_action follows the same pattern; _explain_next independently reads STATE/BOARD without the snapshot gate. validate_checkpoint_surface validates semantic relationships against snap.history but does not prove supplied STATE/BOARD bytes match snap.state_hash/snap.board_hash. A deterministic interleaving captured generation A, then completed generation B before _next_action's BOARD read. _next_action returned `PHASE SCOUT T-001` from old STATE/history plus new BOARD while live generation B STATE was already BLOCKED with `WAIT: external decision`.
DEFECT: Read-only routing can combine files from different committed generations and emit a successful executable action that no coherent project state ever authorized.
REPAIR: status/next/explain-next must parse/render/route exclusively from one ProjectSnapshot's state_text, board_text and history. When a snapshot is supplied, validate_checkpoint_surface must additionally prove any supplied STATE/BOARD bytes hash exactly to the snapshot. Preserve current recovery-preflight priority.
VERIFY: Force a complete LOG->BOARD->STATE generation change at every former independent-read boundary. Commands return one complete generation or a stale/refusal result; a live BLOCKED/WAIT generation never yields PHASE SCOUT from an older snapshot. Explicit snapshot-A/text-B inputs must be rejected.

[P1] [W2-011] tools/saipen_engine/convergence.py::{_attribution_claims,attribution_problems}
EVIDENCE: _attribution_claims documents "LATEST owning record wins per path" but processes release-scope files in filename order and then crew_defer evidence, so loop/source order overrides chronology. A newer 12:00 release scope claiming NEW bytes was replaced in the reducer by an older 10:00 crew-defer claim for OLD bytes, producing either false stale failure with live NEW or false green with live OLD. Git deletion attribution is also conflated with absent claim: a valid release-scope entry `{"gone.txt": null}` for a tracked deletion was reported as "reviewed deletion but exists again" even while the file remained absent. A further fixture supplied matching path/hash evidence carrying foreign project_identity/project_lineage and attribution accepted it.
DEFECT: Attribution flattens heterogeneous evidence into `path -> hash|None`, discarding canonical chronology, provenance, project binding and the distinction between explicit deletion and missing evidence. This permits both false green and false red convergence decisions.
REPAIR: Decode attribution into structured claims carrying path, explicit file/deletion state, expected hash, canonical timestamp, source kind, ticket/op identity and project binding. Reject malformed/foreign modern release scopes before positive evidence. Merge all claim sources by one canonical chronology/tie-break policy rather than iteration order. Represent reviewed deletion explicitly and validate real absence.
VERIFY: Cover newer-scope/older-defer and the reverse ordering, equal-time deterministic ties, foreign lineage/identity evidence, valid reviewed deletion while absent, deletion reappearance and multiple claimants for one path. convergence_verdict must neither certify stale bytes nor reject a legitimate reviewed deletion.

[P1] [W2-012] tools/saipen_engine/operations.py::_plan_defer_for_crew vs tools/saipen_engine/release.py::_load_scope
EVIDENCE: Modern release-scope records carry machine-local project_identity and portable project_lineage. release._load_scope correctly binds modern records by portable lineage and falls back to project_identity only for legacy records lacking lineage. _plan_defer_for_crew instead rejects whenever `scope_record.project_identity != _identity(root)` and ignores project_lineage. In an isolated active T-7 fixture advanced to SHIP, record_scope succeeded; the complete project directory was renamed while preserving `.saipen/IDENTITY.md` and the scope record. defer_for_crew(...,dry_run=True) then refused with PATH_ESCAPE stating that the release scope came from a different project.
DEFECT: Two consumers of the same release-scope authority implement contradictory project-binding rules. A legitimate portable project move accepted by release validation breaks crew defer and can prevent convergence closure.
REPAIR: Make crew-defer scope binding identical to release._load_scope: modern records with project_lineage bind against live project_lineage_identity(root); only legacy records lacking lineage fall back to exact runtime project_identity. Keep foreign-lineage refusal before trusting source/path evidence.
VERIFY: Record scope, move the entire project and successfully defer when portable lineage/source/path evidence is unchanged. Copying the scope to unrelated lineage must refuse. Legacy no-lineage scope works only at its original runtime identity. Release and crew-defer produce identical binding verdicts.

[P1] [W2-013] tools/saipen_engine/journal.py::{_settle_journal,Journal.__init__,Journal.mark} terminal settlement error boundary
EVIDENCE: _settle_journal explicitly states that movement of a terminal receipt from recovery/ops to recovery/settled is NON-FATAL because the COMMITTED manifest remaining in ops is authoritative. Its exception handler catches only OSError. safe_op_dir can raise InvalidIdError for an unsafe/symlinked settled destination. In an isolated run_mutation, the verifier inserted `.saipen/recovery/settled/probe-op` as a symlink after APPLY but before terminal settlement. The canonical target x.txt was already written as `X` and recovery/ops/probe-op/operation.json was durably `status: COMMITTED`; then journal.mark("COMMITTED") propagated InvalidIdError from _settle_journal instead of returning success. A retry of the exact same mutation also raised InvalidIdError because Journal.__init__ unconditionally resolves both the ops and settled paths before preferring the valid authoritative ops receipt.
DEFECT: Optional post-commit settlement/compaction can convert an already successful canonical mutation into an exception and can poison exact idempotent retry, directly contradicting the documented non-fatal settlement contract. Semantic authority remains COMMITTED in ops, but the API behaves as though the mutation failed and cannot rediscover its own authoritative receipt.
REPAIR: Treat settlement-path safety refusal as cleanup/corruption state, never as reversal of an already durable terminal commit. _settle_journal must catch InvalidIdError together with ordinary movement failure and leave the authoritative COMMITTED receipt in ops. Journal.__init__ should prefer a valid ops receipt without requiring a same-op settled path to resolve; consult settled only when no authoritative ops receipt exists. Preserve fail-closed reporting of a corrupt settled namespace through compaction/semantic diagnostics rather than throwing from the already-committed mutation path.
VERIFY: Race an unsafe settled/<op_id> node into existence after verification but before terminal settlement. The initial mutation returns truthful COMMITTED/cleanup-pending semantics with target bytes and ops receipt intact, not an exception. Exact retry resolves the authoritative ops receipt and returns ALREADY_APPLIED. compact/semantic validation may report the hostile settled artifact but must not invalidate or duplicate the already-committed canonical target. Ordinary successful settlement continues moving one terminal receipt exactly once.

[P1] [W2-014] tools/saipen_engine/subs.py::{_iter_operation_records,sub_instance_health,sub_list,sub_status} semantic-receipt corruption handling
EVIDENCE: _iter_operation_records correctly raises SemanticReceiptCorruptionError when semantic_receipt_snapshot reports errors and no pre-captured records are supplied. sub_status calls sub_instance_health without a pre-captured receipt set, so that exception propagates through _collect_review_state/_collect_linkage. In an isolated clean-HEAD project with `.saipen/recovery/settled/CORRUPT` created as a non-directory receipt entry, `python tools/saipen.py sub status saiwiki` exited rc=1 with an uncaught SemanticReceiptCorruptionError traceback. sub_list handles the exact same authority differently: it captures semantic_receipt_snapshot, replaces any snapshot carrying errors with `_records = ()`, and continues. Against the same corrupt namespace, `python tools/saipen.py sub list` exited rc=0 and printed `code: SUB_LIST`. The empty tuple is then indistinguishable to health derivation from a genuinely clean receipt universe containing no durable collect/disposition evidence.
DEFECT: Public SubSaipen read surfaces have contradictory corruption semantics: one crashes with an implementation traceback, while the aggregate list silently launders corrupt authority into an empty receipt set and reports success. Corruption can therefore be presented as absence of evidence instead of an explicit invalid state, and callers cannot rely on list/status having the same safety contract.
REPAIR: Establish one read-only semantic-receipt error contract for sub health. Capture the semantic snapshot once per command. If it carries corruption, return a structured `CORRUPT_JOURNAL`/HEALTH_INVALID result containing bounded diagnostic evidence; never substitute `()` and never expose the raw exception traceback. sub_status and sub_list should consume the same captured snapshot/error object, and sub_instance_health should accept an explicit authority-error channel rather than attempting a hidden second scan. Preserve normal clean-empty receipt behavior as distinct from corrupt/unavailable authority.
VERIFY: Against the same malformed/non-directory, symlinked, duplicate-op-id and undecodable receipt fixtures, both `sub list` and `sub status <role>` must return deterministic structured corruption/invalid results with no traceback and no false clean-empty linkage. Against a genuinely empty receipt namespace both commands remain successful. A valid durable sub_collect/sub_disposition receipt remains visible identically through list and status.

[P2] [W2-015] tools/saipen_engine/operations.py::{_plan_record_scope,_plan_convergence_stage} source-bound evidence APPLY CAS
EVIDENCE: _plan_record_scope samples current SourceIdentity and reviewed per-path hashes and places them in the release-scope record, but its OperationPlan preconditions contain only canonical STATE/BOARD/LOG. Unlike _plan_defer_for_crew, it does not add `"." = source_identity_dependency(ident)`. In an isolated REVIEW fixture, x.txt contained REVIEWED during PLAN and changed to CHANGED-AFTER-PLAN before apply_plan. APPLY returned SCOPE_RECORDED although the persisted reviewed hash immediately disagreed with live x.txt. _plan_convergence_stage has the same omission: it stores sampled source identity in receipt_metadata but only canonical document preconditions guard APPLY. Changing x.txt after PLAN changed live source fingerprint, yet apply_plan returned CONVERGENCE_RECORDED with the stale pre-change source fingerprint.
DEFECT: Source-bound evidence is sampled during PLAN but that sampled source is not part of APPLY's compare-and-swap authority. Concurrent source edits can make a newly committed release scope or convergence receipt stale at the instant it is reported successful.
REPAIR: Every operation whose semantic success asserts facts about SourceIdentity must include the already sampled `source_identity_dependency(ident)` as a read precondition for `"."`. Use the same sampled identity for persisted evidence; do not recompute a second authority during PLAN. Include other non-canonical gate evidence in CAS when its state is not already covered by canonical document/receipt dependencies.
VERIFY: Mutate a scoped/source file between PLAN and APPLY for record_scope and record_convergence. Both return STALE_STATE with zero evidence/canonical writes. No-change execution remains successful. Persisted evidence identity always equals the source identity authorized at APPLY.

[P2] [W2-016] tools/saipen_engine/liveness.py::record_actionable concurrent read-modify-write
EVIDENCE: record_actionable performs `_load(path) -> calculate repeats -> write` without serialization. A deterministic two-thread fixture forced both identical calls to finish _load before either write. Both observed no prior history, both returned `stalled=False, stall_repeats=1`, and the final cache also contained repeats=1. Sequential semantics require the second identical observation to produce repeats=2 and cross the configured stall threshold.
DEFECT: The shared liveness counter has a lost-update race. Parallel agents/CLI observations can erase repetition evidence and suppress or delay CREW_STALLED, allowing a repeated carrier to appear live indefinitely under concurrent polling.
REPAIR: Serialize the cache read-modify-write through the smallest appropriate local lock while retaining the cache as non-canonical projection. Combine synchronization with W2-002's owned/no-follow publication boundary; read-only/dry-run paths remain zero-write.
VERIFY: From an empty cache launch two and then multiple concurrent identical observations. One logical sequence must survive and the second observation must reach the correct repeat/stalled state. Concurrent different fingerprints must leave one valid complete JSON document with deterministic reset semantics, never malformed or truncated state.

[P2] [W2-017] tools/saipen_engine/producer.py::integrate_packages_core committed-edge retirement branch
EVIDENCE: The normal post-integration cleanup branch wraps _retire_ready_package in try/except OSError and returns `cleanup_pending=True` when retirement fails. The idempotent `_current_edge` branch instead uses `with contextlib.suppress(OSError): _retire_ready_package(...)` and then unconditionally returns `cleanup_pending=False` plus the reason `READY retired idempotently`. A direct isolated reproduction mocked a valid current COMMITTED integration edge and forced _retire_ready_package to raise OSError("disk busy"). integrate_packages_core still returned INTEGRATED/ALREADY_APPLIED with `cleanup_pending: False`.
DEFECT: The idempotent retry path swallows post-commit cleanup failure and reports the exact opposite cleanup state. READY evidence can remain in the hot set while automation is told no cleanup debt exists, causing repeated rediscovery and hiding a persistent retirement problem.
REPAIR: Use one shared retirement helper/result for both newly committed and ALREADY_APPLIED branches. A failed retirement remains non-fatal to the already-integrated payload but must return `cleanup_pending=True` and a truthful cleanup reason. Catch the same bounded cleanup refusal/error classes in both branches, including ownership refusal introduced by W2-003, without converting them into payload-integration failure.
VERIFY: Force retirement success and failure in both the new-integration and committed-edge idempotent branches. Successful cleanup returns cleanup_pending=False and removes/relocates READY as intended; failed cleanup leaves READY intact, returns cleanup_pending=True, and never reapplies payload. A later successful retry clears the debt and reports cleanup_pending=False.

SECOND_WAVE_DONE_WHEN: W2-001 through W2-017 are repaired at their root causes; journal staging/temporary files cannot escape ownership and post-COMMITTED settlement cannot turn durable success into an unretryable failure; liveness storage is owned and concurrency-safe; producer epoch/staging/publication/recovery/retirement descendants are no-follow owned and cleanup debt is reported truthfully; source-receipt terminal transitions are crash-recoverable and idempotently retryable; failed lock acquisition never leaks reservations and final mutex files cannot escape ownership; targeted producer recovery never integrates payload against malformed/contradictory checkpoint authority; Git-index rollback owns Git's actual mutex; convergence chronology/attribution use canonical time, provenance and portable project binding; status/next/explain consume one checkpoint generation; SubSaipen list/status surface semantic receipt corruption consistently without traceback or clean-empty laundering; source-bound evidence APPLY rechecks the sampled source identity; all new hostile/fault/race regressions pass together with existing second-wave tests and validate_project remains clean on valid fixtures; after the separate CORE-003 repair, the canonical complete test harness terminates with a trustworthy verdict.

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-27T19:45:34+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: performance-mtbogxec-3e23012b19bd
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: performance
WAVE_INDEX: 3
WAVE_COUNT: 3
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: SAIPEN_27.08.26-T06-27-48.zip -> fully unpacked SAIPEN project at /mnt/data/audit_saipen_w2_cont2
BASELINE: archive SHA256 42adcd80c79108e630b984972ea0b0e130315ab389e3c264429b64a261c81472; VERSION 7.231.8; main@9e8bb6c17e5109b4cfdcc69c0ec88ef3c711fdd3
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@9e8bb6c17e5109b4cfdcc69c0ec88ef3c711fdd3 - embedded Git readable; supplied archive already contained operational/test-generated dirty state. Audited implementation paths correspond to HEAD. Final implementation diff across tools/, saipen/, extensions/ and .github was 0 bytes.
SAIPEN_CONTEXT: STALE - supplied operational state diverges from HEAD, tracked historical LOG-001..LOG-015 files are absent from the archive, STATE references an inaccessible Windows saipen_home, and tests/scenarios already contains accumulated runtime-generated conformance receipts; used as non-gating runtime/performance context
AUDIT_SCOPE: intake validation/release/capture scaling; source-identity Git delta evidence; scenario-suite repeatability; conformance receipt accumulation; semantic operation-receipt lifetime scans; LOG history parsing/memory retention; SubSaipen hot-path receipt use; performance regression orchestration and subprocess cancellation; temporary-directory lifecycle; representative CLI startup/hot-path timing; bounded scan for unbounded collections, repeated filesystem parsing and cleanup/resource leakage
TEST_STATUS: TEST_PARTIAL
TEST_LIMITATION: NONE - canonical complete tt/unittest orchestration remains unable to terminate because of the separately verified CORE-003 recursive runner defect; T-1022's nested complete scenario run was therefore not used as a final whole-suite verdict
VERIFIED_INSTEAD: focused performance checks T-1019, T-1020, T-1021 and T-1023 completed earlier in the wave with 37 checks and zero failures. Controlled scaling/profiling reproduced PERF-001 through PERF-005. T-1022 parent cancellation was reproduced independently and left its run_scenarios.py child reparented to PID 1 until explicitly terminated. A focused run_log_tail_probes execution passed 7/7 while proving its raw mkdtemp directory remained after successful return. Final sanity found no active run_scenarios.py/perf_wave_regressions.py child, implementation diff was zero, the audit-created temp directory was removed, and the complete root .saipen/recovery/conformance tree matched the supplied archive byte-for-byte: 457 files on both sides with zero missing, extra or hash-mismatched files.
STATUS: PERFORMANCE: COMPLETE
TICKETS: 7
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/saipen_engine/intake.py::{validate_project,release_gate,work_closure_gate,_work_is_done,capture,_find_exact_duplicate,_next_receipt_id,_read_index,_read_meta,_read_coverage,_read_contract,_board_source_links}; freshness.py::{compute_source_identity,_git_identity,_git_delta_listing,_parse_git_delta_evidence,_record_evidence,_stream_digest,_is_saipen_path,revalidate_source_identity}; journal.py::{semantic_receipt_snapshot,semantic_receipt_digest,_receipt_namespace_entries,_scan_receipt_namespace,_recovery_identity_binding}; log.py::{read_history_snapshot,read_history_snapshot_and_logs_digest,history_paths}; conformance.py::{generate_conformance_receipt,_update_receipt_index,_iter_receipts,latest_receipt}; subs.py::{sub_list,sub_instance_health,_collect_linkage,_collect_review_state,_terminal_tickets}; tools/run_scenarios.py scenario validator loop, run_improve_probes, run_nitro_probes, run_nitro_m2_probes, run_nitro_m3_probes, run_nitro_integrity_probes, run_log_tail_probes, hostile probe families and temp-fixture construction; tools/perf_wave_regressions.py::{run_t1019,run_t1020,run_t1021,run_t1022,run_t1023}; representative status/next/sub CLI paths
COVERAGE_DEFERRED: external remote/network performance was intentionally not exercised; Windows-native process-group, junction/reparse and filesystem performance behavior was not runtime-tested in this Linux environment; canonical complete suite execution remains unavailable until CORE-003 is repaired, but no remaining high-value local performance/resource surface was intentionally deferred
CROSS_WAVE_REFERENCES: CORE-003 proves the canonical tt/unittest runner recursively invokes itself and must be fixed before full-suite wall-clock results are trustworthy. W2-001/W2-002/W2-003 ownership findings constrain any derived caches/indexes proposed below: performance optimization must not weaken no-follow/fail-closed semantics. W2-014 requires SubSaipen semantic-receipt corruption to remain explicit rather than becoming clean-empty cache state. W2-016 covers liveness lost-update concurrency and W2-017 covers producer cleanup_pending truthfulness; neither is duplicated here.
RESIDUAL_UNCERTAINTY: current supplied active LOG is small and tracked sealed LOG history is absent from the archive, so PERF-005 is a demonstrated valid synthetic lifetime bound rather than current user-visible latency. Full Windows process-tree cleanup for PERF-006/007 needs platform-specific verification. No additional material performance/stability finding survived the final bounded sanity/resource sweep.

[P1] [PERF-001] PROVEN BOTTLENECK tools/saipen_engine/intake.py::{validate_project,release_gate,work_closure_gate,_work_is_done}
EVIDENCE: The validation/release path repeatedly reopens and reparses the same global authority inside per-receipt/per-work loops. validate_project calls _board_source_links once, then independently reads and parses BOARD.md again for every linked active receipt. Controlled valid fixtures produced 101 BOARD parses for 100 linked receipts, 301 for 300, 1001 for 1000 and 3001 for 3000. Measured validate_project runtime grew from roughly 20-25ms at 100 to roughly 302-367ms at 1000 and ~1841ms at 3000. release_gate amplifies the pattern: each work_closure_gate rereads index and BOARD and scans the active receipt set while _work_is_done reparses BOARD. N=20 linked DONE works took ~19.5ms with 41 BOARD parses; N=100 took ~339ms with 201 parses; N=300 took ~2770ms with 601 parses. At N=100 instrumentation counted 101 index reads, 10,300 metadata reads, 400 coverage reads, 100 contract reads and 201 BOARD parses for one release_gate invocation.
ISSUE: Correctness gates scale approximately O(W*N) instead of with unique authority. Hundreds of works/receipts turn one validation or SHIP decision into seconds of repeated parsing and filesystem I/O over data already loaded during the same logical snapshot.
OPTIMIZE: Build one immutable call-scoped intake-gate snapshot. Parse BOARD once, read index once, load each active metadata/coverage/contract record at most once, and derive receipt->work/work->receipt and ticket-terminal projections in one pass. Make validate_project and snapshot-aware work_closure/_work_is_done helpers consume that shared structure. Reuse metadata already loaded by the gate when integrity checks need its fields.
GUARDRAIL: Do not persist this snapshot across commands or mutation boundaries. Integrity-critical receipt bodies must still be hashed when the current contract requires content verification. Existing APPLY CAS/source-identity revalidation and corruption/refusal behavior must remain unchanged.
VERIFY: Instrument validate_project and release_gate at 100/300/1000/3000 linked works. BOARD parse and index-read counts must remain O(1) per command, each receipt record O(1), and wall-clock growth approximately linear. Existing success/refusal reasons and corrupt-input behavior must remain semantically identical.

[P1] [PERF-002] PROVEN BOTTLENECK tools/saipen_engine/intake.py::{capture,_find_exact_duplicate,_next_receipt_id}
EVIDENCE: Every unique capture calls _find_exact_duplicate, which reads the index but does not use its stored source_sha256 projection as the normal lookup authority; it rereads metadata for every active receipt and then hashes every active SRC-*.md body searching for the narrow body-only crash-orphan case. _next_receipt_id separately reconstructs used IDs from index plus active/archive/tombstone scans. Single-capture fixtures with 100/500/1000 active receipts caused exactly 100/500/1000 metadata reads and roughly 8.1/28.4/54.3ms respectively. Actual sequential unique capture from an empty fixture produced exactly N(N-1)/2 metadata reads: 4,950 at 100 captures, 44,850 at 300 and 179,700 at 600. End-to-end series time grew from ~466ms at 100 to ~3579ms at 300 and ~11,371ms at 600.
ISSUE: Normal source ingestion is O(N²) over active receipt lifetime. Each new unique source becomes more expensive simply because prior sources exist, even though index.json already carries digest and next-id projections capable of bounding the common path.
OPTIMIZE: Use index.active source_sha256 as the first-stage duplicate locator and read/verify only matching candidate metadata/body before returning exact duplicate. For the body-only crash window, first probe the unindexed candidate implied by next_id and only perform a wider recovery scan when index/filesystem inconsistency is actually detected. Allocate from next_id on the fast path and probe/increment on real collisions instead of globbing all historical namespaces every capture.
GUARDRAIL: Index data remains a locator/projection, not sufficient proof of content identity. A digest candidate must still have its authoritative metadata/body verified. The documented crash-after-body-before-index retry must continue adopting the same receipt. Stale/corrupt index state must enter bounded recovery/refusal, never silently duplicate or reuse IDs.
VERIFY: Capture 100/300/1000/3000 unique sources while counting metadata/body reads. Normal unique capture should perform approximately O(1) lookup work per source. Exact duplicate capture still returns the original receipt. Inject body-only crash, stale next_id and path collision cases and prove safe idempotent recovery without duplicate receipt IDs.

[P1] [PERF-003] PROVEN BOTTLENECK tools/run_scenarios.py scenario source-fixture execution + tools/validate.py conformance side effects + tools/freshness.py::_is_saipen_path
EVIDENCE: run_scenarios executes the canonical validator directly against checked-in source fixtures under tests/scenarios. validate.py emits a conformance receipt on PASS or FAIL, so every run mutates those fixture trees. A copied stale-state-reconciliation fixture contained 52 conformance receipt files before one validator invocation and 53 afterward. The supplied archive already contains 1,991 files under `tests/scenarios/*/.saipen/recovery/conformance/`, about 1.30MB, accumulated as untracked runtime output. freshness._is_saipen_path excludes only root `.saipen`, so nested scenario `.saipen/recovery/...` files are treated as ordinary project source. Validator/source-identity profiling consequently performed thousands of evidence reads over test-generated receipt data. In an isolated comparison, excluding only nested runtime recovery/lock/cache artifacts reduced a representative identity capture from roughly 282ms to roughly 119ms while leaving the Git subprocess structure unchanged.
ISSUE: The scenario suite permanently increases its own future workload and changes project source identity. Test-generated runtime evidence accumulates inside source fixtures, becomes fingerprint input, causes package/evidence staleness and makes successive validation/audit runs progressively slower.
OPTIMIZE: Validate each source scenario from a disposable fixture copy and discard its generated runtime evidence after assertion, following the pattern already used by hostile/fault probes. Remove accumulated generated conformance artifacts from checked-in fixture trees. Exclude only contractually generated nested `.saipen/recovery`, `.saipen/locks` and `.saipen/cache` subtrees from source identity where they occur inside test fixtures.
GUARDRAIL: Do not exclude entire nested `.saipen` directories. Tracked fixture STATE.md, BOARD.md, LOG.md, schemas/contracts and deliberate corrupt checkpoint inputs must remain source identity. Scenario assertions must still prove that conformance receipts are generated in the disposable copy.
VERIFY: Run the complete scenario suite twice. `git status --porcelain`, fixture hashes and project source identity must remain identical before/after when implementation source is unchanged; nested conformance file count must not grow. Profiling must show scenario-generated runtime receipt files no longer contribute source-fingerprint evidence reads.

[P2] [PERF-004] PROVEN BOTTLENECK tools/saipen_engine/journal.py::{semantic_receipt_snapshot,_receipt_namespace_entries,_scan_receipt_namespace,_recovery_identity_binding} + tools/saipen_engine/subs.py read consumers
EVIDENCE: The supplied project contains 1,549 terminal semantic operation receipts. On that real baseline, semantic_receipt_digest over exact receipt bytes measured ~51.5-69.9ms with ~52.5ms median, while semantic_receipt_snapshot decoding/validating the same namespace measured ~286.2-342.0ms with ~304.9ms median and returned all 1,549 records. Under tracemalloc the full snapshot reached ~24.9MB peak Python allocations. Synthetic minimal receipt histories scale linearly: ~8.4ms at 100, ~42.4ms at 500, ~90.2ms at 1000 and ~242.4ms at 3000. The implementation first retains raw operation/progress bytes for the complete namespace, then allocates decoded records, so lifetime-sized raw and structured forms overlap. Recovery lineage binding was also recomputed roughly once per applicable receipt instead of once per snapshot. sub_list already shares one decoded snapshot between roles, but each role's _collect_review_state calls _collect_linkage over the full record tuple again and reparses Core BOARD through _terminal_tickets; current `python -S tools/saipen.py sub list --json` is approximately 0.69s on this baseline.
ISSUE: Fresh semantic-receipt consumers pay O(lifetime history) decoding/allocation on every invocation, and some consumers then repeatedly rescan that already-decoded lifetime set. Settled receipts are immutable and monotonically accumulate, so read-path cost and memory rise forever even when no relevant recent evidence changes.
OPTIMIZE: First eliminate intra-scan duplication: compute live project-lineage identity once per semantic snapshot, stream raw receipt entries into validation/decode instead of holding the complete raw namespace simultaneously, and build command-scoped derived indices such as package-identity->collect linkage once for SubSaipen rather than once per role. Capture Core terminal-ticket state once per sub-list command. For cross-process repeated reads, use the existing exact semantic_receipt_digest as a validation token for a compact non-authoritative decoded snapshot/index; an exact token match may reuse derived data, otherwise perform the current full strict scan and refresh the projection.
GUARDRAIL: Any cache/index is derived only and must obey W2 ownership/no-follow requirements. Exact receipt-byte change, namespace change, duplicate op_id, malformed JSON, lineage mismatch, unsafe node or cache corruption must invalidate/bypass reuse and surface the same fail-closed error as canonical scanning. Never convert corrupt authority into an empty receipt universe.
VERIFY: Compare optimized and canonical records/errors/digest across valid, corrupt, duplicate, moved-lineage and changing-receipt fixtures. At 1k/3k/10k settled receipts, unchanged repeated consumers should avoid full JSON decode and repeated per-role lifetime iteration. Changed/corrupt history must force full validation. Peak allocations should no longer retain the entire raw namespace alongside the entire decoded namespace.

[P2] [PERF-005] PROVEN BOTTLENECK tools/saipen_engine/log.py::read_history_snapshot
EVIDENCE: read_history_snapshot parses complete sealed+active history while retaining normalized full text, a dict for every event, every raw legal event line, illegal-line diagnostics and max-ticket information simultaneously. Valid synthetic canonical histories measured without allocation tracing: 10,000 events / ~0.9MB -> median ~27.3ms; 50,000 / ~4.9MB -> ~192.9ms; 100,000 / ~9.8MB -> ~387.9ms. Under tracemalloc the 100,000-event case reached ~95.6MB peak Python allocation, almost ten times raw ledger size; 50,000 peaked ~47.8MB and 10,000 ~9.5MB. The supplied active LOG is small and its tracked sealed history is absent from this archive, so the finding is a verified lifetime bound rather than a current 400ms CLI delay.
ISSUE: Full history capture materializes multiple lifetime-sized representations regardless of what a caller actually needs. Long-running projects with large sealed immutable ledgers will eventually pay hundreds of milliseconds and tens/hundreds of MB for commands that may need only structured events, a tail or hashes.
OPTIMIZE: Make history retention purpose-specific. Generalize the existing retain_text seam so callers request only required representations: structured events, combined text, bounded raw tail, event lines, etc. For immutable sealed segments, allow derived validated per-segment summaries keyed by the canonical framed segment identity so unchanged sealed segments need not be fully reparsed on every command; always parse the active segment fresh.
GUARDRAIL: Ledger correctness is primary. Consumers requiring complete event semantics must still observe every legal/illegal event. Any sealed segment byte/path/order/framing change invalidates a derived summary. max_ticket_id, event/parent ordering, contract errors and canonical framed history digest must remain identical.
VERIFY: Build 10k/50k/100k-event histories across sealed and active segments and compare all requested semantic fields/hashes with the current reader. Purpose-limited consumers must demonstrate lower peak memory. Mutating, renaming or resegmenting a supposedly immutable segment must invalidate reuse and reproduce current corruption/hash behavior.

[P2] [PERF-006] STRONGLY EVIDENCED WASTE tools/perf_wave_regressions.py::run_t1022 subprocess lifecycle
EVIDENCE: run_t1022 launches `[python, tools/run_scenarios.py]` with subprocess.run(timeout=900) but without an owned process group/session or parent-cancellation cleanup. An earlier bounded perf-wave invocation exceeded its outer execution window and left run_scenarios.py alive after the parent runner was terminated. A controlled reproduction then launched only the T-1022 parent path, observed its run_scenarios.py child, terminated the parent and one second later observed the child still running reparented to PID 1. The audit had to terminate that child explicitly. Final sanity confirmed no such child remained.
ISSUE: Cancellation of the performance runner leaks its most expensive descendant. Automated timeout/retry systems can stack independent scenario suites for many minutes, consuming CPU, memory and filesystem bandwidth and corrupting the validity of subsequent performance measurements.
OPTIMIZE: Launch heavy nested probes in an invocation-owned process group/session and centralize process-tree shutdown. On outer timeout, SIGTERM, KeyboardInterrupt or runner teardown, terminate and reap the complete owned descendant group, escalating only that owned group if needed. Implement equivalent POSIX and Windows process-tree ownership.
GUARDRAIL: Never find/kill processes by command text or broad Python matching. Cleanup may affect only descendants explicitly created by this invocation. Normal return code/stdout/stderr handling and T-1022's 900-second successful-run allowance remain unchanged unless separately justified.
VERIFY: Cancel the parent before child bootstrap, mid-scenario and near completion; no owned descendant may survive a bounded cleanup interval. Exercise subprocess timeout and KeyboardInterrupt. Repeated cancelled invocations must not accumulate processes. A normal T-1022 run must still reach its expected summary and live-state byte-identity assertions.

[P2] [PERF-007] STRONGLY EVIDENCED WASTE tools/run_scenarios.py raw tempfile.mkdtemp fixture lifecycle
EVIDENCE: run_scenarios.py contains 16 direct tempfile.mkdtemp sites in addition to 62 context-managed TemporaryDirectory sites. run_improve_probes defines project_fixture with raw mkdtemp and contains 41 textual calls to that helper. Multiple other major probe families allocate raw roots such as saipen-nitro-*, saipen-m2-*, saipen-m3-*, saipen-integrity-*, saipen-logtail-* and hostile-journal/release/convergence/authority/state roots without a common finally cleanup boundary. A direct isolated execution of run_log_tail_probes passed all 7 checks, created exactly one `/tmp/saipen-logtail-*` directory, returned normally, and that directory still existed after return; the audit removed only that directory afterward. The machine also contained multiple older `saipen-*` temporary directories at the end of the campaign, although those pre-existing residues were not used as sole defect evidence because their exact originating invocation cannot be proven.
ISSUE: Successful as well as interrupted scenario probes can leak complete temporary project trees. Repeated nightly/super-audit campaigns therefore accumulate disk usage, inode pressure and copied repository data in the system temp directory. Large probe roots can also preserve stale lock/recovery artifacts that complicate later debugging and environmental measurements.
OPTIMIZE: Replace raw mkdtemp ownership in scenario probes with TemporaryDirectory/context-managed fixture objects, or register every raw root in one suite-level cleanup stack that is unwound in `finally` on success, assertion failure, exception and cancellation. Nested helper-created fixture roots must register with the same owner automatically. Combine with PERF-006 process-tree cancellation so cleanup still executes on controlled termination where possible.
GUARDRAIL: Cleanup must only remove directories created by the current suite invocation and must never delete a user/project checkout or externally supplied path. Failed tests may optionally preserve a fixture only behind an explicit diagnostic flag; default CI/audit behavior must clean it. Existing probe semantics and failure evidence must remain available in captured output before cleanup.
VERIFY: Instrument tempfile creation during every run_scenarios probe family. After successful, failing and exception paths, the set of suite-owned temp roots must be empty. Specifically, run_log_tail_probes must still pass 7/7 while leaving no saipen-logtail-* directory. Run repeated scenario campaigns and prove temp disk/inode usage returns to baseline after each invocation. Exercise cancellation together with PERF-006 and verify both child processes and suite-owned temp trees are reclaimed.

PERFORMANCE_DONE_WHEN: PERF-001 through PERF-007 are repaired at their root causes; intake validation/release and source capture perform approximately linear work with one command-scoped authority snapshot; scenario execution leaves checked-in fixtures, source identity and system temporary storage stable across repeated runs; semantic receipt and LOG history consumers avoid unnecessary lifetime-sized decode/allocation while preserving exact fail-closed corruption semantics; heavy nested performance probes own and reap their full process trees; all new scaling, memory, temp-lifecycle and cancellation regressions pass together with existing focused performance checks; and after CORE-003 is repaired, the canonical complete test harness terminates with a trustworthy full-suite result.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.
# SAIPEN SEMANTIC COMPRESSION — WAVE 4

## LOAD TOPOLOGY CLOSURE + CONFORMANCE CUTOVER

ROLE

Continue the semantic-compression architecture established by T-1210.

The first major vertical slice succeeded.

Verified current shape:

* `CORE.md`: ~163 KB -> ~23 KB
* `CORE §1.10`: ~36 KB -> ~1.9 KB
* `COMMANDS.md`: ~4 KB
* `REGISTRY.json`: ~11.7 KB
* `EXECUTION.md`: ~1.8 KB
* main human protocol Markdown: ~632 KB -> ~494 KB
* command execution no longer reconstructs shortcuts from Markdown
* Cyrillic confusables, phase DFA, error codes, STATE closed facts increasingly derive from REGISTRY
* semantic golden artifact exists
* protocol budget tool exists
* CONFORMANCE machine corpus exists

Do NOT perform another broad CORE rewrite.

CORE is no longer the dominant context problem.

Wave 4 targets actual runtime load topology:

1. reproducible closure;
2. BOOT;
3. INDEX;
4. budget truth;
5. CONFORMANCE cutover.

Phase compression comes AFTER this wave.

---

# W4.0 — SEAL THE PREVIOUS WAVE BEFORE STARTING NEW SEMANTICS

The supplied tree currently does not reproduce the previously logged green core gate.

Current core validation reports at least:

* dangling LOG parent involving `E-4482`;
* historical CONFIRMED Improve findings lacking canonical ticket linkage;
* `_AUDAPACK_MANIFEST.json` outside the closed repository-root set.

Resolve these through their actual owners.

Do NOT weaken validator rules.

In particular:

* repair/reconcile LOG ancestry canonically;
* give historical Improve findings truthful terminal/canonical linkage;
* treat `_AUDAPACK_MANIFEST.json` as packaging/transport metadata, not a new protocol-root file unless there is a separately ratified reason for making it part of the repository.

Also verify that the Wave 3 changes are actually represented by a reproducible commit/clean checkout.

T-1210 is recorded DONE, but the supplied archive still carries a substantial staged/unstaged delta relative to HEAD.

A protocol-compression milestone is not sealed until:

fresh checkout
→ install/runtime artifacts present
→ validator green
→ golden semantics green.

Do this before opening another large Work wave.

---

# W4.1 — MAKE BOOT WHAT IT CLAIMS TO BE

`BOOT.md` currently says:

> No rule is defined here.

Make that literally true.

BOOT must be an execution router, not a second protocol specification.

Current BOOT size:

~13 KB.

Target:

~6–7.5 KB.

BOOT should contain only:

1. load STYLE before user-visible output;
2. bind project root and protocol home;
3. read/validate STATE;
4. invoke recovery/reconciliation when required;
5. activate source/user/runtime context when applicable;
6. read BOARD + active LOG tail;
7. resolve the CURRENT user input;
8. route to the owning document/mechanical engine;
9. load one phase document;
10. checkpoint through the canonical operation.

Everything else should be a Rule ID or owner reference.

---

# W4.2 — REMOVE STALE COMMAND ROUTING FROM BOOT

BOOT currently still instructs agents to:

`Open CORE.md §1.10 and read the row.`

That is obsolete.

The new authority model is:

`REGISTRY.json`
= closed executable command facts.

`COMMANDS.md`
= human command semantics.

`CORE.md`
= global command priority/invariants only.

Therefore BOOT command routing should become conceptually:

If deterministic engine is available:
→ resolve mechanically.

If a human-readable command rule is needed:
→ load `COMMANDS.md`.

Never load CORE merely to resolve a command.

Remove stale descriptions of:

* shortcut table ownership;
* command rows in CORE;
* compound semantics owned elsewhere;
* Cyrillic mapping details already owned by COMMANDS/REGISTRY.

BOOT may state one invariant:

> Current explicit user input is resolved before persisted continuation.

Then point to `CMD-*`.

---

# W4.3 — REMOVE OTHER RULE DUPLICATION FROM BOOT

Audit BOOT line by line.

For every paragraph ask:

Does BOOT need this information to decide WHAT TO LOAD NEXT?

If NO:
remove it and point to its owner.

Examples of content that should normally move/be referenced rather than repeated:

* detailed source receipt semantics -> `SOURCES.md`;
* USERPERSON precedence details -> its owner;
* runtime capability semantics -> `RUNTIME.md`;
* complete compound-command contract -> `COMMANDS.md`;
* complete Pick Rule explanation -> CORE Rule ID;
* exact next_action vocabulary -> REGISTRY/CORE owner;
* repair mechanics -> reconciliation/OPS owner;
* full reply-language rules -> STYLE;
* HUSH/output policy -> EXECUTION;
* rebind-home detailed procedure -> mechanical operation owner.

Keep only the routing trigger.

---

# W4.4 — FIX INDEX TO MATCH THE NEW OWNERSHIP GRAPH

`INDEX.md` still says CORE owns commands and that BOOT step 7 sends agents to CORE §1.10.

That is stale.

Correct INDEX so it is purely a lazy-load routing table.

Required entries now include:

`CORE.md`
global invariants/state semantics only.

`COMMANDS.md`
command semantics.

`EXECUTION.md`
output/execution policy including HUSH.

`MAINTENANCE.md`
goal/autonomous maintenance semantics.

`IMPROVE.md`
Improve lifecycle.

`OPS.md`
mechanical transaction/reconciliation semantics.

`SOURCES.md`
receipt/source authority.

`RUNTIME.md`
runtime capability semantics.

`phases/*.md`
one active phase delta.

INDEX must not summarize those contracts extensively.

Target:

~3 KB.

---

# W4.5 — MAKE THE COLD BUDGET HONEST

Current budget code says the intended cold target is <=20 KB but actually permits 28 KB.

Current measured cold profile:

~26,216 bytes.

Therefore today's `BUDGET PASS` is not proof of the originally declared cold target.

Restore the intended target after BOOT compression:

`cold <= 20 * 1024`

Do not change the written target upward merely because current files exceed it.

Expected after BOOT/INDEX reduction:

STYLE ~8.6 KB
BOOT ~6–7 KB
INDEX ~3 KB

cold:
~17–19 KB.

That is the desired range.

---

# W4.6 — MAKE EVERY DECLARED BUDGET ACTUALLY MEASURED

`protocol_budget.py` currently declares:

* cold;
* ordinary_continue;
* command_resolution;
* ordinary_phase;
* ship_improve;
* single_doc.

But only part of these profiles are actually constructed/enforced.

That is not enough.

Implement real load-profile accounting for every named budget.

For each profile define the exact files/registry surfaces required by the routing contract.

Example conceptual profiles:

COLD
`BOOT + STYLE + INDEX`

COMMAND
`BOOT-minimum + REGISTRY + COMMANDS`

ORDINARY CONTINUE
cold routing delta

* CORE kernel when required
* current phase only
* relevant Work/source pointer surface

ORDINARY PHASE
CORE kernel

* active phase
* only explicitly required owner documents

SHIP
CORE kernel

* ship phase
* OPS
* release-specific owner docs actually required

IMPROVE
CORE kernel

* IMPROVE
* relevant seat/meta contracts

Do not estimate by summing the whole protocol.

Measure the declared load graph.

---

# W4.7 — ADD A LOAD GRAPH, NOT ONLY BYTE COUNTERS

Add a small machine-readable routing/load description.

This can live in REGISTRY if it remains closed data, or in a dedicated tiny file.

Concept:

profile
→ mandatory documents
→ conditional documents
→ excluded documents.

Example:

`cold`
must:
BOOT
STYLE
INDEX

exclude:
CONFORMANCE
CHANGELOG
all inactive phases

`command`
must:
REGISTRY
COMMANDS

conditional:
CONTROLS
EXECUTION
IMPROVE

The budget tool should consume this graph.

Do not duplicate profile membership in Python and prose.

---

# W4.8 — COMPLETE CONFORMANCE CORPUS BEFORE CUTOVER

Current state:

`CONFORMANCE.md`
contains 256 canonical scenario rows.

`tests/conformance_cases.jsonl`
contains only 251.

Missing corpus IDs:

250
251
252
253
254

This must be repaired before CONFORMANCE.md is compressed.

Add all five missing cases faithfully.

Then require exact identity:

# Markdown/canonical scenario set

machine corpus scenario set

during migration.

No row may silently disappear.

---

# W4.9 — FIX THE MACHINE CORPUS SCHEMA QUALITY

Some current corpus `rule_id` values are not stable Rule IDs and appear to be parsed historical labels such as:

`(T-505`
`(T-539`
`re-T-542`

Do not treat those strings as proper Rule IDs.

Separate fields:

`id`
scenario number

`rule_ids`
stable protocol Rule IDs where available

`history_refs`
T-### / incident references

`concept`
short human label

`setup`
input/precondition

`expected`
observable invariant

`coverage`
fixture/test/validator owner

Avoid blindly parsing parenthetical historical text into `rule_id`.

Unknown Rule ID is better represented as null than malformed authority metadata.

---

# W4.10 — MAKE CONFORMANCE CORPUS THE PROOF DATA OWNER

After 256/256 parity is proven:

`tests/conformance_cases.jsonl`
becomes the machine scenario corpus.

Do NOT make execution agents load it.

`CONFORMANCE.md` becomes a compact human-facing contract/index.

Suggested contents:

* what conformance means;
* TEST-001 cold-continuation principle;
* the three validation vectors;
* corpus location/schema;
* how to run conformance;
* how a new scenario is added;
* compact summary/count by category;
* debugging instructions.

Do not retain the 256-row essay table in the routinely maintained human document.

Target:

`CONFORMANCE.md <= 10–15 KB`.

---

# W4.11 — MAKE CONFORMANCE HUMAN VIEW GENERATED OR VERIFIED

Prevent the old duplication from returning.

Prefer:

machine corpus
→ generated compact index/statistics

or:

machine corpus
↔ deterministic parity validator.

Adding scenario 257 must require exactly one semantic data addition, not manual maintenance of two giant independent tables.

No second 200 KB mirror.

---

# W4.12 — EXPECT A LARGE SAFE SIZE WIN

Current human protocol Markdown:

~494 KB.

CONFORMANCE alone:

~208 KB.

Replacing it with a ~10–15 KB human contract should reduce the human protocol to approximately:

~295–305 KB

before any phase compression.

Combined with BOOT/INDEX reduction:

approximately:

~285–300 KB.

That reaches the original first-stage human-protocol target without touching phase semantics.

This is unusually high-value because the reduction is largely structural rather than semantic.

Take it.

---

# W4.13 — CLEAN STALE CROSS-DOCUMENT REFERENCES

After command ownership moved, stale references remain.

Examples currently include references in:

* BOOT;
* INDEX;
* IMPROVE;
* RFC compatibility redirect;
* phase docs;
* CONTROLS;

that still describe command semantics as living in CORE §1.10.

Audit all references.

Do NOT mechanically replace every `§1.10` string.

Classify each one:

A. genuinely points to a surviving CORE invariant;
B. now belongs to COMMANDS;
C. historical citation that should remain historical;
D. obsolete wording.

Update only B/D.

The resulting ownership graph must be truthful.

---

# W4.14 — FINISH REGISTRY AUTHORITY FOR CLOSED COMMAND RESULTS

Most important closed facts now derive correctly from REGISTRY.

One remaining cleanup area is command chain/result vocabulary.

Avoid leaving:

REGISTRY chain/disposition vocabulary
+
independent hand-maintained Python string constants

as two authorities.

Where practical derive:

chain policies
disposition vocabulary
failure disposition set

from REGISTRY.

Do not overengineer this.

This is closure cleanup, not a new abstraction.

---

# W4.15 — KEEP HUSH SPEC, DELAY FULL HUSH EXECUTION UNTIL LOAD TOPOLOGY IS CLEAN

`EXECUTION.md` now exists and is correctly separated from STYLE/Core.

That is good.

REGISTRY honestly marks HUSH:

`status: planned`

Keep that truthful during this wave.

Do not claim HUSH implementation because EXECUTION.md exists.

However W4 must prepare the routing boundary:

`hush <task>`
→ execution-policy modifier
→ normal command/objective resolver
→ unchanged semantic task
→ output suppression policy.

After BOOT/INDEX/load graph are clean, real HUSH should be the NEXT small feature wave.

It will then serve as a practical stress test that the new modular ownership actually works.

---

# W4.16 — DO NOT COMPRESS PHASE DOCS YET

Phase docs remain ~109 KB total.

This is now the next large semantic surface, but lazy loading means only one phase is normally active.

Therefore they are lower priority than BOOT.

Do not touch them during W4 except stale-reference corrections.

The next compression campaign after W4 should target phase deltas.

Likely order by value:

SHIP ~17 KB
TRANSLATE ~13.7 KB
MARKHUNT ~10.9 KB
CLEAN ~10.7 KB
VERIFY ~10.2 KB

Target total later:

~60–70 KB.

But not yet.

---

# W4 ACCEPTANCE BAR

Wave 4 is complete only when:

1. Current tree core validation is reproducibly green.
2. Fresh checkout core validation is green.
3. T-1210 implementation is durably represented, not merely dirty/staged Work.
4. BOOT no longer sends command resolution to CORE §1.10.
5. BOOT defines routing only.
6. BOOT <= ~7.5 KB.
7. INDEX reflects the actual ownership graph.
8. INDEX <= ~3–3.5 KB where practical.
9. Cold profile <=20 KB.
10. protocol_budget measures every budget it declares.
11. load-profile membership has one machine owner.
12. CONFORMANCE corpus contains exactly 256/256 current scenarios.
13. malformed pseudo-rule IDs are separated from historical ticket refs.
14. machine corpus is the scenario-data owner.
15. CONFORMANCE.md <=15 KB.
16. routine execution never loads CONFORMANCE.
17. stale CORE §1.10 references are classified and corrected.
18. human protocol Markdown falls to approximately <=300 KB.
19. phase semantics are unchanged.
20. HUSH remains honestly marked planned until its real execution path exists.

NEXT WAVE

After W4:

### Wave 5 — Phase Delta Compression

Compress the ~109 KB phase surface to ~60–70 KB using:

universal invariant references

* phase-specific deltas only.

Then:

### Wave 6 — HUSH Runtime

Implement the actual:

`hush <task>`

execution modifier against the cleaned routing/load architecture.

That order gives both:

smaller persistent protocol context
and
smaller live execution narration.

CORE PRINCIPLE

The biggest remaining win is no longer shortening rules.

It is making sure the agent loads only the rules that matter.

Compression has moved from document size to execution topology.

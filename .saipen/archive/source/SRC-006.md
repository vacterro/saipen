# SAIPEN — Audit Handoff

RUN_ID: acb-mtdl5wli-c622ecfe2ea647b9872e
GENERATED_AT: 2026-08-29T02:59:47.212967
PROJECT_NAME: SAIPEN
TARGET: SAIPEN_28.08.26-T07-33-45(4).zip -> unpacked SAIPEN v7.231.9 project; ACB_CHAIN_RECEIPT=startcore-mtdl59wp-2ef6abce03aa
BASELINE: archive SHA-256 32263edd2de0ceacc11da1fd7160b9af2dcf2083178b8d499823b238c2c74730; VERSION 7.231.9
GIT_CONTEXT: PRESENT main@c7ea5b1bb5f8e953c07140cda4f636a382c08310 - supplied worktree is dirty (123 porcelain entries); the actual archive contents, including its uncommitted implementation changes, were audited as authoritative
SAIPEN_CONTEXT: STALE - STATE is VERIFY/T-1173 and BOARD shows the audit-fix wave in progress; the supplied archive also omits Git-tracked sealed LOG history needed by current canonical validation, so .saipen history was used only as orientation
TOTAL_TICKETS: 12
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 4 tickets (baseline: archive SHA-256 32263edd2de0ceacc11da1fd7160b9af2dcf2083178b8d499823b238c2c74730; VERSION 7.231.9)
- Wave 2 (Second Wave): 4 tickets (baseline: archive SHA-256 32263edd2de0ceacc11da1fd7160b9af2dcf2083178b8d499823b238c2c74730; VERSION 7.231.9; Git main@c7ea5b1bb5f8e953c07140cda4f636a382c08310; supplied worktree contains substantial post-HEAD implementation and .saipen changes)
- Wave 3 (Performance): 4 tickets (baseline: archive SHA-256 32263edd2de0ceacc11da1fd7160b9af2dcf2083178b8d499823b238c2c74730; VERSION 7.231.9; Git main@c7ea5b1bb5f8e953c07140cda4f636a382c08310; supplied dirty worktree is authoritative)

---
## 01 — AUDIT CORE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-29T02:31:00+03:00
WAVE: AUDIT CORE
TARGET: SAIPEN_28.08.26-T07-33-45(4).zip -> unpacked SAIPEN v7.231.9 project; ACB_CHAIN_RECEIPT=startcore-mtdl59wp-2ef6abce03aa
BASELINE: archive SHA-256 32263edd2de0ceacc11da1fd7160b9af2dcf2083178b8d499823b238c2c74730; VERSION 7.231.9
GIT_CONTEXT: PRESENT main@c7ea5b1bb5f8e953c07140cda4f636a382c08310 - supplied worktree is dirty (123 porcelain entries); the actual archive contents, including its uncommitted implementation changes, were audited as authoritative
SAIPEN_CONTEXT: STALE - STATE is VERIFY/T-1173 and BOARD shows the audit-fix wave in progress; the supplied archive also omits Git-tracked sealed LOG history needed by current canonical validation, so .saipen history was used only as orientation
AUDIT_SCOPE: entry/state orientation; source identity/freshness; source intake, Contract/coverage/archive/recovery; release planning/preflight/staging; conformance receipt generation/index/read path; journal/undo; controls/focus; liveness; producer READY lifecycle; validator/audit harness and focused regressions; audit method/quality contract :contentReference[oaicite:0]{index=0}
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: python -m ruff --version could not run because the sandbox has no ruff module; full `PYTHONPATH=tools python -m unittest discover -s tools -p 'test_*.py' -q` exceeded the 200-second execution window. Separately, `PYTHONPATH=tools python tools/validate.py --gate core` actually ran and failed with 3 supplied-snapshot problems (dangling LOG parent from absent sealed history, sweep-ticket links consequently unresolved, and `_AUDAPACK_MANIFEST.json` outside the validator root-file closed set); `PYTHONPATH=tools python tools/audit_checks.py` consequently failed its synthetic release-ledger probe on the same dangling LOG history.
VERIFIED_INSTEAD: `python -m compileall -q tools tests` PASS; `PYTHONPATH=tools python -m unittest tools.test_audit_2026_08_27_core -v` 10/10 PASS; `PYTHONPATH=tools python -m unittest tools.test_audit_2026_08_27_w2 -q` 12/12 PASS; `PYTHONPATH=tools python -m unittest tools.test_audit_2026_08_28_all3 -q` 12/12 PASS; `git diff --check` rc=0 apart from CRLF conversion warnings; four focused hostile reproductions below were executed against temporary fixtures without modifying the audited project
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT

[P0] [CORE-001] tools/saipen_engine/intake.py::_looks_sensitive/capture + tools/saipen_engine/release_contract.py::source_authority_paths + tools/saipen_engine/release.py::_stage_release_content
EVIDENCE: `capture(root, "api_key = <redacted> source_kind="external_audit", work="T-7")` stores the original source bytes verbatim while metadata records `sensitive: true`. `source_authority_paths()` then includes `.saipen/intake/active/SRC-001.md` unconditionally and `_stage_release_content()` passes that path to `git add`. Hostile reproduction proved `SENSITIVE_FLAG True`, `SENSITIVE_IN_RELEASE_PATHS True`, and the Git index contained the exact line `api_key = <redacted> The project security contract explicitly treats an agent-caused secret entering committed content as a security failure, while `saipen/CORE.md` requires redaction at discovery.
DEFECT: the sensitivity detector is informational only. A source already recognized by SAIPEN as containing a credential is persisted verbatim and then promoted into the clone-stable release authority surface with no intake or release gate consuming `sensitive`. A normal release can therefore commit and publish a credential that SAIPEN itself detected.
REPAIR: make credential containment authoritative at the source-intake boundary, reusing the project's existing secret-redaction/credential-containment policy rather than merely setting metadata. Never persist newly captured known credential bytes verbatim. Also make release preflight fail closed on legacy active/archive source authority marked sensitive unless its stored carrier is demonstrably sanitized; this is required so pre-fix receipts cannot bypass the repaired capture path. Preserve source provenance using sanitized-body digest/redaction metadata rather than silently dropping the receipt from clone-stable authority.
VERIFY: add a regression capturing representative API-key/token/password credentials and prove the literal credential never appears in active body, metadata, recovery, Git index, committed blob, or fresh clone; legacy `sensitive:true` unsanitized authority must make ship refuse before commit/push. A normal non-sensitive source receipt must still stage, commit, clone, and validate exactly as the existing clone-stability regression requires.

[P0] [CORE-002] tools/saipen_engine/conformance.py::generate_conformance_receipt/_update_receipt_index/latest_receipt
EVIDENCE: canonical generation snapshots `out_dir.stat().st_mtime_ns` before writing the new receipt and `_update_receipt_index()` treats equality between that old value and `.lineage.json["receipt_dir_mtime_ns"]` as sufficient proof for its incremental branch. That branch validates only the newly appended receipt and skips `_iter_receipts()`. Hostile reproduction generated one valid PASS, injected malformed `corrupt-race.json` after the pre-append mtime snapshot but immediately before the second canonical receipt write, and generated a second PASS. A strict `_iter_receipts()` then raised `ReceiptDiscoveryError: receipt corrupt-race.json is not readable canonical JSON`, while `conformance_status(root, "core")` nevertheless returned `CURRENT_PASS` and `latest_receipt(root, "core")` returned the second green receipt. The writer had advanced both locator and lineage proof over corrupt canonical history.
DEFECT: the optimized append proof has a TOCTOU hole. Directory mtime captured before the append cannot prove that the writer was the only mutation between that observation and publication. A foreign/corrupt sibling inserted in that interval is absorbed into the writer's final directory mtime while the lineage count/xor is advanced as if exactly one receipt appeared. Subsequent O(1) reads therefore trust a green locator and bypass the strict scan, violating the fail-closed conformance invariant.
REPAIR: do not advance lineage/index state solely from `prior_receipt_dir_mtime_ns`. Before publishing the new trusted locator, mechanically prove that live receipt-directory membership equals the previously trusted inventory plus exactly the new canonical receipt. The smallest correctness-first change is to recompute the cheap names/stat inventory after every canonical append and require `(previous_count + 1, previous_xor ^ new_token)`; mismatch must enter the strict full-validation path and corrupt history must leave the locator stale/INVALID. Any later performance optimization must preserve the same atomic membership proof rather than reintroducing an mtime-only TOCTOU assumption.
VERIFY: add the exact race regression by injecting malformed and separately valid out-of-band siblings between pre-append observation and the canonical receipt write. In both cases the incremental proof must not advance as a one-receipt append; malformed history must make `latest_receipt`/`conformance_status` fail closed rather than return CURRENT_PASS. Retain clean sequential-append and relocated/corrupt-index regressions.

[P0] [CORE-003] tools/saipen_engine/release.py::ReleasePlan/plan_release/_preflight_plan/_stage_release_content + tools/saipen_engine/release_contract.py::source_authority_paths + tools/freshness.py::_is_saipen_path
EVIDENCE: `plan_release()` snapshots `metadata_paths=tuple(_metadata_paths(root))`, and `ReleasePlan.release_paths` later stages only that frozen name set. All `.saipen/**` paths are deliberately excluded by `freshness._is_saipen_path()`, so adding or changing source authority does not change `source_head`/`source_tree_fingerprint`. `_preflight_plan()` rechecks STATE/BOARD/LOG hashes, `release_gate()`, source identity, index and reviewed source scope, but never recomputes or hashes the source-authority inventory against the plan. Reproduction took the metadata snapshot before capture, then captured an unlinked source, added R001 and terminally REJECTED it with evidence. `release_gate()` returned `SOURCE_RELEASE_COVERAGE_COMPLETE`, source identity remained byte-identical, while live `release_metadata_paths()` gained the receipt body/meta/Contract/coverage/index authority absent from the old plan; staging the old planned surface omitted the late receipt. Existing source-authority files have the inverse problem: their names may already be in `metadata_paths`, but their post-PLAN bytes are not bound, so APPLY can stage changed authority that the immutable plan never reviewed.
DEFECT: ReleasePlan binds source code and three canonical state carriers but does not bind the membership or content of the clone-stable source-authority surface it publishes. Because that surface lives under `.saipen` and is excluded from source identity, PLAN->APPLY can silently omit newly created authority or publish changed authority. This breaks the claimed immutable-release-decision invariant and can recreate fresh-clone source loss/provenance drift.
REPAIR: add a canonical source-authority manifest to ReleasePlan consisting of every release-required intake/archive path plus a content hash (and include it in `canonical()`). At preflight, recompute the exact owned source-authority manifest and require byte-for-byte/path-for-path equality before any staging or external side effect. Do not merely recompute `metadata_paths` at APPLY and silently widen the release: a changed authority set means the plan is stale and must be rebuilt.
VERIFY: PLAN a release, then before APPLY (a) add a fully covered unlinked receipt, (b) archive/close a receipt, (c) mutate an existing authority carrier, and (d) remove one. Every case must fail PREFLIGHT before staging. An unchanged plan must stage exactly its hashed authority manifest, and a fresh clone must retain and validate all planned source receipts.

[P0] [CORE-004] tools/saipen_engine/intake.py::_settle_archive_commit
EVIDENCE: `_settle_archive_commit()` claims to verify archived Contract/coverage against the still-indexed projection, but in implementation it checks the archived Contract only for matching `source_sha256`; for coverage it checks only that the JSON and `requirements` are dictionaries, counts entries/actionable flags, and then constructs a tombstone with hard-coded `unresolved: 0`. It never proves Contract clause IDs/content equal coverage requirements and never applies the normal terminal/evidence/verification completeness gate. Reproduction created SRC-001 with one actionable requirement `SRC-001:R001`, set it to `REJECTED` with evidence, confirmed `coverage_complete == True`, and simulated a crash after `_archive_closed_locked()` but before tombstone/index settlement. The archived Contract still contained R001; the archived coverage was then corrupted to `requirements: {}`. Retrying `close_receipt()` returned `SOURCE_CLOSED`, removed SRC-001 from `index.active`, and wrote a tombstone with `requirements: 0, actionable: 0, unresolved: 0` while the archived Contract still contained the lost requirement.
DEFECT: crash recovery treats structurally parseable archived coverage as proof of completed source disposition and manufactures `unresolved: 0` instead of re-establishing the same closure invariant required before the original archive move. An interrupted close can therefore convert missing/tampered requirement evidence into a successful CLOSED tombstone, permanently hiding source obligations and destroying provenance.
REPAIR: before any destructive settlement, run an in-memory archived equivalent of the normal Contract/coverage integrity and closure gates: source digest must match; Contract clause set and immutable clause fields must correspond exactly to coverage; every actionable clause must have a terminal disposition and required evidence/verification; and the derived summary must itself be closure-complete. Construct tombstone requirement/actionable/unresolved fields only from that validated summary, never from permissive counts plus a hard-coded zero. Any mismatch must return a stable corruption/contract-drift refusal with zero writes and leave the pending archive state recoverable.
VERIFY: extend interrupted-close regressions with archived coverage that (1) drops a Contract requirement, (2) adds an unknown requirement, (3) downgrades a terminal disposition, (4) removes evidence, (5) removes verification from IMPLEMENTED/VERIFIED, and (6) changes class/actionable/text relative to Contract. Every case must refuse repeatedly with byte-identical pending evidence. An untampered interrupted close must still settle idempotently to the exact tombstone derived from its validated pre-crash coverage.

CORE_DONE_WHEN: CORE-001 prevents detected credential bytes from entering persistent/release authority and blocks unsafe legacy receipts; CORE-002 cannot advance a green conformance locator across any out-of-band receipt mutation in the append race; CORE-003 binds exact source-authority membership+content into ReleasePlan and refuses PLAN/APPLY drift before writes; CORE-004 re-proves exact Contract/coverage terminal closure before interrupted archive settlement. Add hostile regressions for all four root causes, keep the existing 34 focused audit regressions green, then run the complete unittest suite, `python -m ruff check tools/ tests/`, `python tools/audit_checks.py`, and `python tools/validate.py --gate core` on a faithful checkout/snapshot containing the required canonical LOG history until all intended verification passes.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-29T02:47:35+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: UNKNOWN_FROM_SUPPLIED_INPUT
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: second
WAVE_INDEX: 2
WAVE_COUNT: 3
WAVE: AUDIT SECOND WAVE
TARGET: SAIPEN_28.08.26-T07-33-45(4).zip -> fully unpacked SAIPEN v7.231.9 project
BASELINE: archive SHA-256 32263edd2de0ceacc11da1fd7160b9af2dcf2083178b8d499823b238c2c74730; VERSION 7.231.9; Git main@c7ea5b1bb5f8e953c07140cda4f636a382c08310; supplied worktree contains substantial post-HEAD implementation and .saipen changes
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@c7ea5b1bb5f8e953c07140cda4f636a382c08310 - embedded Git metadata is readable; archive worktree is dirty, so the actual supplied archive bytes rather than HEAD alone were audited
SAIPEN_CONTEXT: STALE - STATE is VERIFY/T-1173 and active intake contains earlier audit/repair history, but the supplied snapshot also lacks tracked sealed LOG-001..LOG-015 and contains extensive runtime/kitchen material; used only as orientation while live implementation remained authoritative
AUDIT_SCOPE: lifecycle/boundary/repeatability/error-drift attack across producer epoch claiming, staging begin/package binding, READY publication/reuse/parser symmetry, restart/takeover recovery, package identity, READY retirement/SETTLED transitions, same-role serialization, journal/retry fixes, source-close recovery, release boundary changes, liveness locking, and relevant regression contracts
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: NONE
VERIFIED_INSTEAD: `PYTHONPATH=tools python -m unittest tools.test_v7_producer_parallelism tools.test_audit_2026_08_27_w2 tools.test_audit_2026_08_28_all3 -q` passed 41/41 existing focused checks; `python -m compileall -q tools/saipen_engine tools/saipen.py` passed; `git diff --check -- tools/saipen_engine tools/saipen.py tests tools/test_*.py` returned 0 apart from CRLF conversion warnings. A separate five-case hostile lifecycle unittest then failed 5/5 against current project behavior: stale-generation epoch rebinding published READY, cross-role package publication succeeded, malformed READY was falsely REUSED, staging recovery inverted marker/manifest authority, and source-distinct packages produced the same package identity. Additional deterministic reproductions proved stale retirement can move a newer READY artifact to SETTLED and an existing SETTLED identity can cause a newer READY artifact to be deleted while old settled evidence survives.
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/saipen_engine/producer.py::{package_identity,ProducerPackage.__post_init__,ProducerPackage.from_dict,ProducerEpoch._decode,current,current_owner,owns,claim,StagingGeneration.begin,set_package,_verify,_publish_under_lock,publish,ready_package,scan_ready,recover,_recover_under_lock,_generation_epoch,_retire_ready_package,integrate_packages_core}; KNOWLEDGE/ADR-0001-v7-producer-parallelism.md §§3-6; tools/saipen_engine/{journal,controls,intake,release,release_contract,liveness,lock,paths}.py changed lifecycle surfaces; tools/test_v7_producer_parallelism.py; tools/test_audit_2026_08_27_w2.py; tools/test_audit_2026_08_28_all3.py
COVERAGE_DEFERRED: performance/throughput and scaling belong to wave 3; remote Git/service side effects were not contacted; native Windows junction/reparse and msvcrt behavior was not runtime-exercised in this Linux sandbox; complete repository unittest discovery was not repeated because this wave had complete focused lifecycle coverage plus explicit failing hostile checks
CROSS_WAVE_REFERENCES: current AUDIT CORE reported CORE-001 detected secrets can enter persistent/release source authority; CORE-002 conformance incremental indexing has an append TOCTOU that can advance green authority across corrupt raced history; CORE-003 ReleasePlan does not bind source-authority membership/content across PLAN->APPLY; CORE-004 interrupted intake settlement can close against Contract/coverage drift. Those remain independent and are not duplicated below.
RESIDUAL_UNCERTAINTY: the current campaign input supplies no explicit CAMPAIGN_RUN_ID and no byte-serialized previous-wave handoff artifact from which an exact SHA-256 can be mechanically established, so CAMPAIGN_RUN_ID is not invented and PREVIOUS_WAVE_SHA256 remains NONE; additional producer concurrency interleavings may exist beyond the deterministic stale-handle cases reproduced here

[P0] [W2-001] tools/saipen_engine/producer.py::{StagingGeneration.begin,set_package,_publish_under_lock} generation authority binding
EVIDENCE: `begin()` reads the namespace's current ProducerEpoch and persists it into `staging.manifest.json`, and the class contract says a mutable preparation generation carries the epoch it claimed. Publication does not re-read or bind that persisted generation epoch. Instead `_publish_under_lock()` calls `ProducerEpoch.owns(self.namespace, self.package.epoch)`, so the mutable package supplied later through unrestricted `set_package()` becomes the authority. Deterministic reproduction: claim epoch 1; call `begin()` and write payload under epoch 1; claim epoch 2 as takeover; construct a new package declaring epoch 2; attach it to the old epoch-1 generation; `publish()` returns `PUBLISHED` and creates READY. The existing stale-worker test passes only because it keeps the original epoch-1 package. Producer role is equally unbound: a `ProducerPackage(producer="saiwiki", epoch=<current saitranslate epoch>)` attached to a `StagingGeneration(..., "saitranslate")` returned `PUBLISHED` inside the saitranslate READY directory; `scan_ready()` immediately rejected that artifact as `INVALID_READY` because producer `saiwiki` does not match namespace `saitranslate`.
DEFECT: ownership/fencing authority is self-asserted by replaceable package metadata instead of being immutably bound to the generation that actually produced the payload. A stale generation can therefore relabel itself with the takeover epoch and publish old work after ownership changed, defeating the fundamental stale-worker fence. The same missing binding permits one producer role to publish an artifact into another role's namespace and report success even though the canonical READY reader rejects it.
REPAIR: make generation authority immutable at `begin()`. Persist and retain the canonical producer plus claimed epoch as generation metadata, strictly re-read that owned manifest at publication, and require manifest generation_id, namespace producer, package.producer and package.epoch to agree exactly. The live namespace epoch must still equal that originally claimed generation epoch. `set_package()` should reject producer/epoch disagreement immediately, while `_publish_under_lock()` must independently repeat the persisted checks so direct state manipulation or restart cannot bypass them. Never permit a package to upgrade an already-created generation to a newer epoch.
VERIFY: add hostile regressions for (1) begin at E1 -> takeover E2 -> attach E2 package to old generation, (2) attach a different producer role at the same numeric epoch, (3) tamper staging manifest epoch/generation_id before publish, and (4) normal E1 generation/package publication. Cases 1-3 must refuse with zero READY creation and preserve recoverable staging evidence; case 4 must remain PUBLISHED. Existing same-package stale-worker and parallel producer tests must remain green.

[P1] [W2-002] tools/saipen_engine/producer.py::_publish_under_lock + ProducerPackage.from_dict READY reuse validation
EVIDENCE: the normal READY readers call `ProducerPackage.from_dict(..., expected_producer=..., expected_identity=..., ready_path=...)`, which activates the closed READY schema, requires `status == "ready"` and verifies authenticated payload fields. The idempotent reuse branch does not: at `_publish_under_lock()` it reads an existing `READY/<identity>.json` and calls `ProducerPackage.from_dict(json.loads(...))` with no `ready_path`, expected producer or expected identity. A hostile fixture placed `package.to_dict()` with `status="staging"` and no `payload_hashes`/`payload_bytes` at the exact READY filename, then prepared a completely valid generation for the same package. `publish()` deleted the valid staging generation and returned `{"ok": true, "code": "REUSED"}`. Immediately afterward `scan_ready()` returned zero valid packages and `INVALID_READY: READY schema mismatch (missing=['payload_bytes', 'payload_hashes'], extra=[])`.
DEFECT: publication and consumption use different persistence contracts for the exact same READY carrier. The reuse path treats a partially shaped STAGING object as valid terminal READY evidence, discards the newly prepared valid payload and reports success, leaving the producer with no consumable READY package. This violates both atomic READY visibility and idempotent retry semantics.
REPAIR: the reuse path must decode the existing artifact with the exact strict persisted READY contract used by `ready_package()`/`scan_ready()`: pass `expected_producer=self.producer`, `expected_identity=rid` and `ready_path=target`, and require the resulting object to be a fully authenticated READY package before returning REUSED. Invalid existing authority must never cause staging deletion. Prefer a structured `INVALID_READY`/corruption refusal that preserves both the existing evidence and valid staging generation for explicit recovery rather than silently overwriting corruption.
VERIFY: pre-place at the canonical READY filename: STAGING-shaped JSON, READY missing payload hashes, READY with wrong producer, READY with mismatched filename identity, malformed JSON and a legitimate strict READY artifact. Only the final legitimate artifact may return REUSED and remove duplicate staging. Every malformed/partial case must return a stable refusal, keep the valid staging generation recoverable, and make no false READY success claim.

[P1] [W2-003] tools/saipen_engine/producer.py::{StagingGeneration.begin,recover,_recover_under_lock,_generation_epoch} partial-start recovery authority
EVIDENCE: `begin()` creates the staging/payload directories, then exclusively creates `.in-flight`, and only afterward creates `staging.manifest.json` containing the generation epoch. A process death between those two writes is therefore a normal implementation-generated state: marker present, manifest absent. Reproduction simulated exactly that cut after epoch 1, advanced ownership to epoch 2, then called `recover()`. It returned `{"removed_staging": [], "false_ready": false, "busy": false}` and left the stale marker-only generation permanently present because `_generation_epoch()` returned None and `_recover_under_lock()` skips every `gen_epoch is None`. The inverse is also contradictory: the method's own contract states deletion requires an in-flight marker AND a superseded/invalid generation epoch, but recovery never checks `.in-flight`. A manifest-only epoch-1 generation with no marker was deleted after takeover to epoch 2; the report listed `manifest-only` in `removed_staging`.
DEFECT: generation lifecycle truth is split across two separately created authority carriers, but startup ordering and recovery do not implement the same state machine. One normal crash cut produces an unclassifiable orphan that recovery will never clean, while the opposite incomplete state is destructively deleted without the marker the documented safety proof requires. Repeated crashes/retries can therefore accumulate permanent staging debris or delete evidence not mechanically proven in-flight.
REPAIR: replace the split marker/manifest authority with one atomically published generation-state record carrying generation_id, producer, epoch and lifecycle state, or otherwise make the first durable carrier sufficient by itself to classify recovery safely. If two carriers must remain, include epoch/generation identity in the first one, define an explicit creation/commit order and require both carriers to agree before destructive cleanup. Mismatch/partial evidence should produce a structured corrupt/incomplete-staging report rather than being silently preserved forever or deleted contrary to the stated invariant.
VERIFY: fault-inject restart after every `begin()` filesystem step: generation-dir creation, payload-dir creation, first authority write and manifest/state completion. After a later takeover, every implementation-reachable partial state must deterministically converge or return an explicit recoverable corruption state; no ordinary crash cut may become a permanent invisible orphan. Separately create manifest-only, marker-only, mismatched-epoch and mismatched-generation-id evidence and prove recovery deletes only states carrying the exact mechanical abandonment proof. Existing restart-recovery test M must remain green.

[P1] [W2-004] tools/saipen_engine/producer.py::{package_identity,ProducerPackage.__post_init__,_publish_under_lock,_retire_ready_package} logical identity versus artifact-generation identity
EVIDENCE: the live producer ADR §6 defines `package_identity` over `base_source_head + base_source_tree_fingerprint + role_revision + read_set + write_set + requested_scope`, but `package_identity()` actually hashes only producer, role_revision, dependency fingerprint and requested scope. Two packages with identical dependencies/scope but different source bindings (`A.../tree:A` versus `B.../tree:B`) therefore produced the exact same SHA-256 package identity. `_publish_under_lock()` explicitly recognizes this collision and overwrites the same READY filename when source binding differs. That makes lifecycle operations unsafe because `_retire_ready_package()` treats the identity-derived filename as immutable artifact identity. Deterministic stale-handle reproduction: publish source-A payload OLD; `scan_ready()` and retain the A package object; publish source-B payload NEW under the same identity, replacing READY; invoke `_retire_ready_package()` on the previously captured A object. The function moved the source-B/NEW artifact into SETTLED and left READY empty, so fresh producer work was terminally retired by a stale handle without integration. A second reproduction exposed permanent terminal-evidence loss: retire A normally so `SETTLED/<id>.json` exists; publish B under the same id; retire B. Because the destination already exists, `_retire_ready_package()` simply unlinks the current READY artifact. SETTLED remained the old A/OLD record and all persisted B/NEW producer evidence disappeared.
DEFECT: one identifier is being used both as a logical repeat-work key and as the unique identity of a concrete source-bound READY generation even though it does not bind the source generation. Publication, stale handles and SETTLED collision handling therefore alias distinct artifacts. A newer package can be retired by an older package object, and repeated integration across source generations can silently discard the newer terminal artifact while retaining obsolete settled evidence.
REPAIR: restore the documented source-bound identity contract by incorporating canonical base_source_head and base_source_tree_fingerprint into package identity (and include discovery_model if it is part of the project's canonical source identity), or introduce a separate immutable artifact/generation identity and stop using the source-agnostic work key as a READY/SETTLED filename. Independently make retirement compare-and-swap safe: before moving or unlinking READY, strictly decode the current carrier and prove it is the exact artifact that was integrated, including source binding and authenticated payload identity. An existing SETTLED destination may be treated idempotently only when it is byte/content-equivalent to the artifact being retired; otherwise retain both generations under distinct artifact identities or return an explicit collision. Legacy identity migration must fail closed rather than reinterpret two different source generations as one package.
VERIFY: packages with identical role/dependencies/scope but distinct source bindings must no longer alias the same concrete artifact identity. Add deterministic interleavings: scan A -> publish B -> retire stale A; retire A -> publish B -> retire B; publish B concurrently between integration and A cleanup; and an exact duplicate A retry. A stale A retirement must never move/delete B, B must remain READY until B itself is integrated, terminal evidence for A and B must remain distinguishable, and only a byte/source-equivalent duplicate may take the idempotent reuse/settlement path.

SECOND_WAVE_DONE_WHEN: W2-001 binds every staging generation immutably to its original producer+epoch so stale/cross-role metadata cannot forge publication authority; W2-002 uses one strict READY decoder for both reuse and consumption and never discards valid staging on malformed existing authority; W2-003 gives every begin/crash cut a mechanically classifiable recovery state and deletes only proven in-flight superseded generations; W2-004 gives concrete source generations non-aliasing artifact identity and makes READY retirement exact-artifact/CAS safe. The five hostile failing lifecycle checks must turn green, add stale-retirement/SETTLED-collision regressions, retain the existing 41/41 focused producer/W2/all3 tests, and rerun compileall, diff checks and the canonical verification harness without weakening the four prior CORE findings.

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-29T02:58:40+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: UNKNOWN_FROM_SUPPLIED_INPUT
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: performance
WAVE_INDEX: 3
WAVE_COUNT: 3
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: SAIPEN_28.08.26-T07-33-45(4).zip -> fully unpacked SAIPEN v7.231.9 project
BASELINE: archive SHA-256 32263edd2de0ceacc11da1fd7160b9af2dcf2083178b8d499823b238c2c74730; VERSION 7.231.9; Git main@c7ea5b1bb5f8e953c07140cda4f636a382c08310; supplied dirty worktree is authoritative
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@c7ea5b1bb5f8e953c07140cda4f636a382c08310 - embedded Git metadata readable; 123 porcelain entries in supplied snapshot, including implementation changes and missing sealed LOG segments
SAIPEN_CONTEXT: STALE - STATE is VERIFY/T-1173 and BOARD records the ongoing audit wave; supplied snapshot omits tracked LOG-001..LOG-015 and contains substantial runtime/recovery history, so .saipen was orientation only
AUDIT_SCOPE: canonical validator profiling; source-freshness interaction; scenario harness fixture lifecycle; conformance receipt write/index hot path; semantic operation-receipt snapshot memory/I/O; canonical test-runner subprocess output buffering; focused existing performance regressions and bounded hostile microbenchmarks
TEST_STATUS: TEST_PARTIAL
TEST_LIMITATION: `PYTHONUNBUFFERED=1 PYTHONPATH=tools python tools/perf_wave_regressions.py` exceeded the 210-second execution limit after printing 38 PASS checks through `AUDIT PERF-004 convergence/release reuse one canonical receipt decode`; T-1022 nested third-wave completion was therefore not observed. No missing external prerequisite caused this cutoff.
VERIFIED_INSTEAD: `PYTHONPATH=tools python -m unittest tools.test_audit_2026_08_28_all3.PerformanceAuditTests -v` PASS 5/5 in 2.220s; `python -m compileall -q tools/saipen_engine tools/test_audit_2026_08_28_all3.py tools/perf_wave_regressions.py` PASS; `git diff --check` on inspected implementation/test surfaces rc=0 apart from CRLF conversion warnings; cProfile of canonical validator, direct receipt/freshness timing, tracemalloc, fixture-copy reproductions and bounded subprocess-output probes executed without modifying the audited project
STATUS: PERFORMANCE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/run_scenarios.py full fixture-validation loop; tools/freshness.py::{compute_source_identity,_git_identity,revalidate_source_identity,_git_delta_listing,_parse_git_delta_evidence,_stream_digest}; tools/saipen_engine/conformance.py::{generate_conformance_receipt,_validate_conformance_containment,_update_receipt_index,latest_receipt}; tools/saipen_engine/journal.py::{_receipt_namespace_entries,_scan_receipt_namespace,semantic_receipt_snapshot,semantic_receipt_digest}; tools/saipen_engine/test_runner.py::{_run_family,run_canonical_suite}; tools/test_audit_2026_08_28_all3.py::PerformanceAuditTests; tools/perf_wave_regressions.py T-1019..T-1023 paths
COVERAGE_DEFERRED: full T-1022 third-wave nested probe completion; full canonical suite; Windows-native filesystem/process memory measurements; network/remote Git performance; performance of external producer implementations
CROSS_WAVE_REFERENCES: CORE-001..CORE-004 and W2-001..W2-004 remain independent correctness/lifecycle findings. PERF-001 below materially amplifies freshness cost but does not replace CORE source-identity or W2 producer fixes; PERF-003 optimization must preserve CORE-002 conformance fail-closed race repair.
RESIDUAL_UNCERTAINTY: no explicit campaign run-id or byte-serialized second-wave handoff artifact was supplied, so CAMPAIGN_RUN_ID and PREVIOUS_WAVE_SHA256 are not invented; performance measurements are sandbox-local absolute timings but the operation/file counts and asymptotic behaviors are deterministic

[P1] [PERF-001] PROVEN BOTTLENECK tools/run_scenarios.py::<full fixture-validation loop> + tools/freshness.py source discovery
EVIDENCE: the full scenario loop invokes the canonical validator directly against each checked directory under `tests/scenarios/` (`--project-root <fixture>`), not against a disposable copy. The validator contract writes a conformance receipt on every run. A copied `active-task-not-claimed` fixture had 50 receipt files before one validator invocation and 51 afterward, proving one test execution permanently grows fixture state. The supplied repository currently has 38 runnable `.saipen` scenario fixtures, 2,029 direct conformance receipts plus 76 index JSON files under their nested `.saipen/recovery/conformance` trees. Git reports those generated carriers as untracked because the root `.gitignore` rule `.saipen/recovery/` does not cover nested fixture paths. `freshness._git_delta_listing()` consequently exposed 2,106 non-root-.saipen untracked source paths, 2,105 of them under `tests/`; filtering only the nested generated conformance carriers reduced one `_parse_git_delta_evidence + _stream_digest` pass from 2,120 evidence files / 0.1125s to 15 files / 0.0029s on the same tree. Canonical validator cProfile showed `compute_source_identity` at 0.511s and receipt-time `revalidate_source_identity` at 0.404s, together more than half of its 1.726s profiled execution. Every ordinary full scenario run adds another 38 receipt files, so this cost grows monotonically because the test harness itself dirties its own source tree.
ISSUE: canonical scenario verification is not hermetic. Read-only verification continuously manufactures nested runtime evidence inside source fixtures; that debris is then interpreted as source by Git freshness, permanently increasing file count, Git untracked enumeration, fingerprint stat/read work, sandbox copies and archive size. The test suite therefore makes subsequent tests and normal validation progressively slower simply by being run.
OPTIMIZE: never execute a receipt-emitting validator against the repository-owned fixture directory. For each runnable behavioral fixture, make a disposable temporary copy of the fixture and run `validate.py --project-root` against that copy, exactly as other scenario probes already do. Keep the source fixture byte-identical. Do not solve this merely by adding an ignore pattern: ignoring the files would hide source-identity pollution while still allowing unbounded disk accumulation and fixture-state drift. After the harness is hermetic, remove the already-generated nested recovery/conformance debris from the fixture source trees as regenerable test output.
GUARDRAIL: preserve the validator's unconditional receipt-emission contract; preserve every fixture's exact declared PASS/FAIL and `expect_fail_contains` behavior; the temporary copy must include the complete fixture `.saipen` state and any fixture-local files so tests do not become weaker; source fixtures must be byte-identical before/after the scenario suite.
VERIFY: hash or byte-snapshot every repository-owned `tests/scenarios/**` file before a full scenario run and assert exact identity afterward; run the suite twice and prove `git status --porcelain -- tests/scenarios` and nested conformance file counts are unchanged after both runs. Add a direct red control showing that a validator run against a disposable fixture still creates its expected receipt inside the temporary copy. Finally delete the current generated fixture receipts and prove the same canonical validator/source-identity operation no longer enumerates thousands of self-generated test artifacts while all scenario expectations remain green.

[P2] [PERF-002] LOW-RISK SIMPLIFICATION tools/saipen_engine/journal.py::_receipt_namespace_entries -> semantic_receipt_snapshot
EVIDENCE: `_receipt_namespace_entries()` first builds a complete `captured: list[_ReceiptEntry]` containing every `operation.json` and optional `progress.json` byte string for the entire namespace; only after the full list exists does `_scan_receipt_namespace()` iterate it, frame each entry into the digest and decode it. The supplied project has 1,563 settled operation directories. Their `operation.json` carriers total 8,370,411 bytes before JSON objects, dictionary/list overhead and the result tuple are counted. Direct uninstrumented `semantic_receipt_snapshot(root)` runs measured 0.286-0.340s for 1,563 records. A tracemalloc run measured 26,260,441 bytes peak Python allocation while producing the same 1,563-record snapshot. `_scan_receipt_namespace()` already consumes entries strictly one at a time and does not require the whole raw-byte list after each entry has been framed and decoded.
ISSUE: the receipt scanner unnecessarily materializes the entire raw lifetime ledger before beginning semantic consumption. Memory therefore scales with both total raw receipt bytes and decoded result objects at the same time. The settled ledger is intentionally long-lived, so this avoidable duplicate residency grows for the lifetime of the project even though exact-byte digesting and strict decoding are naturally streamable.
OPTIMIZE: convert `_receipt_namespace_entries()` from a raw-byte list builder into a deterministic iterator/generator. Capture only the sorted directory membership needed for deterministic traversal, then yield each `_ReceiptEntry` immediately after its ownership/stat/read proof. `_scan_receipt_namespace()` and `semantic_receipt_digest()` already consume the function as an iterable and can frame/decode one entry before its raw bytes become collectible. Namespace-level structural failures should yield one deterministic sentinel entry then stop, preserving today's digest/error semantics.
GUARDRAIL: do not weaken the exact-byte semantic digest, structural sentinels, symlink/reparse rejection, duplicate-op detection, progress sidecar handling, ordering, project-lineage checks or corruption fail-closed behavior. Membership ordering must still be captured deterministically enough that a racing add/remove produces the same visible corruption/stability semantics as today; this is memory simplification, not an authority-model change.
VERIFY: run the existing PERF-005 digest-parity and receipt-corruption regressions unchanged; add a 1,500+ receipt fixture asserting byte-identical `SemanticReceiptSnapshot.records/errors/corrupt_op_ids/digest` before versus after the refactor. Instrument peak memory and require it not to retain the aggregate raw manifest corpus while decoding; on a fixture comparable to the supplied 1,563-receipt ledger, peak allocation should fall materially while decoded records and digest remain exactly identical.

[P2] [PERF-003] STRONGLY EVIDENCED WASTE tools/saipen_engine/conformance.py::{generate_conformance_receipt,_validate_conformance_containment,_update_receipt_index}
EVIDENCE: `_validate_conformance_containment()` validates the four storage-chain components and then glob/lstat-checks every existing `*.json` receipt sibling. `generate_conformance_receipt()` calls this full function before writing the new receipt, and `_update_receipt_index()` calls the same full function again before writing the locator/lineage index. The supplied project has 478 direct conformance receipts; one containment call caused 974 `os.lstat` invocations and took 0.0085s, so one canonical append performs roughly 1,948 legacy-receipt lstats solely from these two checks. A synthetic directory with 5,000 receipts required 10,020 lstats and 0.1044s for one containment call, approximately 20,040 lstats per canonical append because the write path calls it twice. This is independent of the separate receipt-index lookup optimization and therefore makes a nominally incremental append O(lifetime receipt count).
ISSUE: write containment and historical receipt-set validation are conflated. To prove that a newly written receipt/index cannot escape the project, the writer needs to prove the owned directory chain and its concrete write target; rescanning every immutable sibling twice contributes no additional containment proof for those writes. Because receipts intentionally only grow, validator append cost grows forever despite the O(1) locator/index design.
OPTIMIZE: split the current helper into (a) constant-size storage-chain/write-target containment validation for canonical writes and (b) strict historical receipt-candidate validation used by full discovery/fallback/read verification. `generate_conformance_receipt()` and index publication should use the constant-size write proof. Existing sibling corruption must still invalidate authority through strict readers/fallback/index-staleness logic. When implementing CORE-002's append-race repair, use the required membership proof exactly once; do not accidentally replace this duplicate O(N) scan with multiple new O(N) scans.
GUARDRAIL: preserve zero-write refusal for symlink/junction/reparse-point `.saipen`, `recovery`, `conformance`, and `index` components; preserve strict rejection of symlink/reparse receipt candidates whenever historical receipts are consumed as evidence; preserve malformed/out-of-band receipt fail-closed behavior and the CORE-002 race repair. Optimization may remove irrelevant sibling scans from the write-containment proof, never from evidence validation.
VERIFY: seed 5,000 valid historical receipt files, instrument `os.lstat`, append one canonical receipt and assert write-containment work remains constant with respect to historical count apart from the single membership proof required by CORE-002. Inject a symlink/reparse storage-chain component and prove zero writes. Inject a malformed/symlinked historical sibling and prove subsequent conformance reads/status still fail closed. Existing index bootstrap, append, corruption-fallback and race regressions must remain green.

[P2] [PERF-004] PROVEN BOTTLENECK tools/saipen_engine/test_runner.py::_run_family
EVIDENCE: `_run_family()` executes every family with `subprocess.run(..., capture_output=True, text=True)` and only calls `_tail(..., limit=8000)` after the child has completely exited. Therefore stdout and stderr are fully accumulated and decoded in RAM even though at most 8,000 characters from each are ever returned. A bounded hostile probe ran `_run_family()` with a child emitting 32 MiB of stdout. The returned report correctly retained only 8,000 characters, but parent-process `RUSAGE_SELF.ru_maxrss` increased from 110,020 KiB before the run to 208,452 KiB afterward. The same mechanism wraps verbose unit discovery, validator, audit-checks, scenario and parity families, so a noisy failure or runaway diagnostic can multiply the test orchestrator's memory footprint before truncation gets a chance to run.
ISSUE: the advertised output bound is post-hoc, not a memory bound. A child can force the canonical test runner to buffer arbitrarily large stdout/stderr, potentially causing memory pressure or OOM during precisely the failing/debug-heavy executions where output tends to explode.
OPTIMIZE: redirect each child's stdout and stderr to owned temporary files (or another bounded streaming sink) instead of PIPE-backed `capture_output=True`. After normal exit or timeout, seek/read only the last configured number of bytes and decode those tails with replacement semantics. Keep stdout and stderr separate. Temporary carriers must be deleted after each family. This preserves useful diagnostics while making parent RAM independent of child output volume.
GUARDRAIL: preserve family exit code, timeout behavior, PASS/FAIL classification, stdout/stderr tail separation, UTF-8 replacement behavior and the existing 8,000-character reporting contract. Do not pipe without continuously draining both descriptors, which would trade memory growth for deadlock. Timeout handling must still terminate/wait for the child and return the captured tail rather than losing diagnostics.
VERIFY: add a family emitting at least 32 MiB on stdout and another emitting large stderr concurrently; assert returned strings remain bounded to the configured tail, execution does not deadlock, timeout still works and parent peak memory stays approximately constant instead of scaling with emitted bytes. Run ordinary PASS, FAIL and TIMEOUT family regressions plus `PerformanceAuditTests` afterward.

PERFORMANCE_DONE_WHEN: the scenario harness validates only disposable fixture copies and repeated runs leave `tests/scenarios` byte-identical with no generated receipt growth; semantic receipt scanning streams raw carriers instead of retaining the entire lifetime raw ledger while preserving exact snapshot/digest semantics; conformance append containment is constant-size apart from the single correctness-required membership proof and all CORE fail-closed guarantees remain intact; canonical test-family output capture is genuinely memory-bounded before truncation. Re-run the 5/5 current PerformanceAuditTests, add the four targeted regressions above, complete `tools/perf_wave_regressions.py` including T-1022, then run the canonical verification harness without weakening any CORE or SECOND-WAVE invariant.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.
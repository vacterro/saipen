# SAIPEN — Audit Handoff

RUN_ID: acb-mtbzp578-5f74f9b297494738884c
GENERATED_AT: 2026-08-28T00:32:39.233982
PROJECT_NAME: SAIPEN
TARGET: SAIPEN_27.08.26-T23-36-48.zip (ACB_CHAIN_RECEIPT: startcore-mtbzouez-ae35d046ccf9)
BASELINE: archive SHA-256 fa3fe76ec160ca80803b86bb2c3e2290e1a38007ed844e7134ccb769b9195f58; Git HEAD dbbfad94688515d604b54e2c97dc92aa5fa7eab1; VERSION/tag v7.231.9
GIT_CONTEXT: PRESENT main@dbbfad94688515d604b54e2c97dc92aa5fa7eab1 - tag v7.231.9 and origin/main resolve to the same commit; tracked implementation files match HEAD, while supplied runtime/project-state material contains post-HEAD changes
SAIPEN_CONTEXT: STALE - .saipen is present, but the supplied worktree contains post-HEAD T-1171 closure state, untracked source-authority material, deleted sealed-log material and local validator drift; it was used only as orientation and live tracked implementation remained authoritative
TOTAL_TICKETS: 10
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 3 tickets (baseline: archive SHA-256 fa3fe76ec160ca80803b86bb2c3e2290e1a38007ed844e7134ccb769b9195f58; Git HEAD dbbfad94688515d604b54e2c97dc92aa5fa7eab1; VERSION/tag v7.231.9)
- Wave 2 (Second Wave): 3 tickets (baseline: archive SHA-256 fa3fe76ec160ca80803b86bb2c3e2290e1a38007ed844e7134ccb769b9195f58; Git HEAD dbbfad94688515d604b54e2c97dc92aa5fa7eab1; branch main; tag v7.231.9)
- Wave 3 (Performance): 4 tickets (baseline: archive SHA-256 fa3fe76ec160ca80803b86bb2c3e2290e1a38007ed844e7134ccb769b9195f58; Git main@dbbfad94688515d604b54e2c97dc92aa5fa7eab1; exact tag v7.231.9)

---
## 01 — AUDIT CORE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-27T23:58:15+03:00
WAVE: AUDIT CORE
TARGET: SAIPEN_27.08.26-T23-36-48.zip (ACB_CHAIN_RECEIPT: startcore-mtbzouez-ae35d046ccf9)
BASELINE: archive SHA-256 fa3fe76ec160ca80803b86bb2c3e2290e1a38007ed844e7134ccb769b9195f58; Git HEAD dbbfad94688515d604b54e2c97dc92aa5fa7eab1; VERSION/tag v7.231.9
GIT_CONTEXT: PRESENT main@dbbfad94688515d604b54e2c97dc92aa5fa7eab1 - tag v7.231.9 and origin/main resolve to the same commit; tracked implementation files match HEAD, while supplied runtime/project-state material contains post-HEAD changes
SAIPEN_CONTEXT: STALE - .saipen is present, but the supplied worktree contains post-HEAD T-1171 closure state, untracked source-authority material, deleted sealed-log material and local validator drift; it was used only as orientation and live tracked implementation remained authoritative
AUDIT_SCOPE: source intake/body/metadata/contracts/coverage/index lifecycle; close/archive/recovery state transitions; release plan/preflight/gate/staging/closure/fresh-clone invariants; path/ownership and recent recovery-hardening surfaces; validators, focused tests and relevant contracts; audit performed read-only as required by the attached command :contentReference[oaicite:0]{index=0}
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: ruff unavailable: "python -m ruff check tools/ tests/" -> "/opt/pyvenv/bin/python: No module named ruff"; PowerShell audit-floor half unavailable because neither pwsh nor powershell exists; full unittest discovery did not finish within the 420-second execution window; release-executor probe initially hit HOME_REQUIRED from the archive's inaccessible configured Windows saipen_home and a scratch-local rebound run did not finish within 480 seconds
VERIFIED_INSTEAD: fresh-clone validator A/B reproduced v7.231.8 PASS versus v7.231.9 FAIL on missing SRC-003; 62 focused tests passed (35 source-receipt, 12 W2 race/symlink, 10 CORE, 5 containment); shell audit-floor 27/27 passed; audit-order 46 tools passed; audit-tags checked 345 comparable tags with no new mismatch; compileall and git diff --check passed; focused disposable-project reproductions verified archive-recovery and body-size defects
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 3
HANDOFF: IMPLEMENTATION_AGENT

[P0] [CORE-001] tools/saipen_engine/release.py::ReleasePlan.release_paths/_stage_release_content + tools/saipen_engine/intake.py::release_gate + tools/saipen_engine/release_contract.py::release_metadata_paths
EVIDENCE: HEAD BOARD for T-1171 references source_receipts=SRC-003, but "git ls-files .saipen/intake" at dbbfad94688515d604b54e2c97dc92aa5fa7eab1 returns zero tracked intake carriers and SRC-003 exists only in the supplied untracked worktree state. A real "git clone --no-local" of this repository lands exactly on dbbfad94688515d604b54e2c97dc92aa5fa7eab1/v7.231.9 and "python -B tools/validate.py" fails with "BOARD Work T-1171 references missing source receipt SRC-003"; a clean checkout of the preceding closure commit v7.231.8 validates successfully. The release path is structurally capable of producing this state: intake.release_gate() validates linked source authority from the current filesystem, ReleasePlan.release_paths is built from reviewed scope + release_metadata_paths + release-scope record, release_metadata_paths does not include source receipt authority, and _stage_release_content() stages only plan.release_paths. The project already enforces an analogous clone-stability invariant for .saipen/IDENTITY.md via git tracking checks, but source authority has no equivalent protection. close_receipt() also refuses closure while linked Work is not DONE, so an active source receipt must legitimately survive the SHIP boundary rather than being closable before release.
DEFECT: the release gate proves source authority only in the publisher's working tree while the release staging contract does not guarantee that the same authority is committed. A release can therefore publish a BOARD reference to receipt evidence that remains local/untracked. Fresh clones lose the original source authority, fail canonical validation and cannot reliably resume/close the linked Work. This is a release-time provenance/data-loss defect, not merely stale metadata in the supplied archive.
REPAIR: define one canonical clone-visible source-authority path inventory in the intake/release contract and make ReleasePlan include every carrier required to preserve the linked receipt invariant across a fresh clone: index plus the applicable body/metadata/current contract/coverage and any revision/tombstone/archive carriers required by intake validation for the receipt's current lifecycle state. Stage that mechanically owned source surface during release rather than relying on reviewed implementation scope. Add a ship-gated tracking/staging assertion analogous to the existing IDENTITY clone-stability check so an untracked receipt cannot satisfy release_gate(). Keep ordinary source capture usable before release; the tracking requirement belongs at the release boundary, not at receipt creation.
VERIFY: create a source receipt linked to an in-flight Work item, exercise the canonical release path, clone the resulting release into a new directory with no original worktree files, and assert that the receipt's required body/metadata/contract/coverage/index authority exists and "python -B tools/validate.py" passes there. Add a negative regression where the receipt exists locally but is untracked and prove SHIP refuses before publishing. Preserve the existing source-receipt and release tests.

[P0] [CORE-002] tools/saipen_engine/intake.py::_settle_archive_commit/_archive_closed_locked
EVIDENCE: _settle_archive_commit() reads archived metadata and then calls _archive_closed_locked() before proving that the archived metadata/body/contract/coverage still match the receipt/index authority. _archive_closed_locked() performs destructive convergence including removal of active metadata and movement of coverage/contract/revision carriers. Only afterward does _settle_archive_commit() compare archived source_sha256/body/contract evidence and potentially return SOURCE_CORRUPTION. A focused disposable-project reproduction created a legitimate partial-close crash state with archive body already moved but active metadata, coverage and contract still intact, then corrupted only the archived metadata digest. Immediately before retry the carriers were active_meta=True, active_cov=True, active_contract=True, archive_body=True. close_receipt() correctly returned SOURCE_CORRUPTION ("archived metadata digest SRC-001 drift"), but after that refusal active_meta=False, active_cov=False and active_contract=False while archive_cov/archive_contract had been moved. A second retry still returned SOURCE_CORRUPTION. Thus the supposedly fail-closed retry destroyed the remaining good active recovery evidence before refusing the corrupted archive state.
DEFECT: archive recovery validates integrity after destructive settlement. A corrupted or inconsistent partial archive can therefore cause the recovery path itself to delete/move the last known-good active authority and leave the receipt in a strictly worse, non-recoverable state. The corruption refusal is not transactionally fail-closed.
REPAIR: split archive settlement into a non-mutating verification phase followed by mutation. While the index/active authority is still intact, inspect every permissible active/archive crash position and validate already-present metadata, source digest/body, contract, coverage and revision evidence against the expected receipt authority. If duplicate active/archive copies exist, verify they are mutually consistent. On any mismatch return the stable corruption result with zero writes/unlinks/moves. Only after the complete preflight succeeds may _archive_closed_locked() converge carriers and finalize tombstone/index state. Keep the final mutation idempotent so valid partial-close retries still settle successfully.
VERIFY: add crash-window regressions covering corruption of archived metadata, body, contract and coverage at each supported partial-settlement position. Snapshot hashes/existence of all active evidence before retry and assert every corruption refusal leaves it byte-for-byte unchanged across repeated retries. Separately prove each valid partial crash state converges to the same closed/tombstoned result under one retry and repeated retries.

[P1] [CORE-003] tools/saipen_engine/intake.py::capture/_write_body/_is_archive_commit_pending
EVIDENCE: the source-body size contract is applied inconsistently. _BODY_MAX is 64 MiB and _META_MAX is 1 MiB, but _is_archive_commit_pending() probes both body and metadata candidates with _META_MAX. A focused 2 MiB receipt reproduction, which is valid under _BODY_MAX, was placed into a normal partial-close state with archive metadata/body present and active metadata still present; _is_archive_commit_pending() returned False and close_receipt() returned INVALID "receipt body missing" even though the 2,097,152-byte archived body existed. The opposite boundary is also broken during capture: capture() writes the supplied body before enforcing the reader's _BODY_MAX. With a 64 MiB + 1 byte source, the first capture returned SOURCE_CORRUPTION but left a 67,108,865-byte SRC-001.md; an exact retry again returned SOURCE_CORRUPTION and created a second 67,108,865-byte SRC-002.md, with no intake index committed. Thus an input the subsystem cannot subsequently read is written before rejection and repeated retries multiply orphan authority files.
DEFECT: there is no single admission/recovery invariant for source-body size. The writer accepts data larger than its own readable contract, while the pending-archive detector incorrectly applies the metadata limit to valid bodies. This causes both retry-amplified orphan writes for oversized capture and a permanent close-recovery dead end for otherwise valid 1-64 MiB bodies.
REPAIR: enforce the UTF-8 encoded body byte length against _BODY_MAX before allocating a receipt ID or writing any authority carrier; oversized capture must return a stable input/size refusal with zero filesystem mutation. In _is_archive_commit_pending(), apply _BODY_MAX to source body carriers and _META_MAX only to metadata carriers. Prefer a small shared typed-carrier limit helper so body/meta limits cannot silently diverge again, without changing the existing accepted 64 MiB body contract.
VERIFY: add a valid body larger than _META_MAX (for example 2 MiB), simulate the archive-commit-pending crash window and prove pending detection plus close recovery succeeds idempotently. Add boundary tests at _BODY_MAX and _BODY_MAX+1: the limit-sized body must retain current valid behavior; the oversized body must be refused before any SRC body/meta/index creation, and an exact retry must leave the filesystem unchanged. If test cost matters, use an injectable test limit for the oversized behavioral case plus one direct assertion that production body probes are wired to _BODY_MAX.

CORE_DONE_WHEN: all three root defects are repaired; a canonical release containing a linked active source receipt validates from a genuinely fresh clone; corrupted partial archive settlement is provably zero-write fail-closed while valid partial settlement remains idempotently recoverable; valid 1-64 MiB bodies survive pending-close recovery and >64 MiB capture is rejected before writing; focused source/release/recovery regressions, canonical validator, compile checks and the available broader test harness pass without weakening existing invariants.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-28T00:10:32+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: UNKNOWN_FROM_SUPPLIED_INPUT
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: second
WAVE_INDEX: 2
WAVE_COUNT: 3
WAVE: AUDIT SECOND WAVE
TARGET: SAIPEN_27.08.26-T23-36-48.zip
BASELINE: archive SHA-256 fa3fe76ec160ca80803b86bb2c3e2290e1a38007ed844e7134ccb769b9195f58; Git HEAD dbbfad94688515d604b54e2c97dc92aa5fa7eab1; branch main; tag v7.231.9
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@dbbfad94688515d604b54e2c97dc92aa5fa7eab1 - tag v7.231.9; tracked implementation matches HEAD while supplied .saipen/runtime material contains post-HEAD changes and untracked evidence
SAIPEN_CONTEXT: STALE - usable for work-state orientation, but current archive contains post-HEAD BOARD/STATE/LOG edits, deleted sealed-log files and extensive untracked runtime/recovery material; live tracked implementation remained authoritative
AUDIT_SCOPE: mutation journal startup/retry/recovery; corrupt-evidence handling; operation idempotence; deterministic undo repeatability; project writer/producer locking; producer staging/recovery; liveness serialization; OperationPlan/APPLY boundary; relevant existing second-wave regression contracts
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: NONE
VERIFIED_INSTEAD: existing focused second-wave suites passed 101/101 checks (56 regression_second_wave_audit_fixes + 33 regression_second_wave_receipt_fixes + 12 test_audit_2026_08_27_w2); three additional lifecycle/repeatability invariant checks all reproduced project defects and exited 1; python compileall tools/saipen_engine passed; git diff --check passed
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 3
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/saipen_engine/journal.py::{scan_pending,recovery_preflight,Journal.start,Journal.read,run_mutation,recover}; tools/saipen_engine/controls.py::{_undo_op_id,undo_confirm}; tools/saipen_engine/plan.py::apply_plan; tools/saipen_engine/lock.py; tools/saipen_engine/producer.py::{ProducerEpoch,StagingGeneration}; tools/saipen_engine/liveness.py; existing W2 regression suites and related receipt/recovery tests
COVERAGE_DEFERRED: performance/cost characteristics of canonical test sandbox copying and other throughput surfaces belong to wave 3; real remote Git publish/tag/push effects were not exercised; previously reported CORE source-receipt defects were not duplicated
CROSS_WAVE_REFERENCES: prior AUDIT CORE reported CORE-001 release can omit active source authority from the committed clone; CORE-002 archive-settlement recovery mutates before validating corrupted archive evidence; CORE-003 source-body size admission/recovery limits disagree. Those remain separate root causes and are not renumbered here.
RESIDUAL_UNCERTAINTY: the supplied second-wave command did not provide a campaign run-id or serialized SHA-256 of the prior handoff, so CAMPAIGN_RUN_ID is explicitly non-invented and PREVIOUS_WAVE_SHA256 is NONE; external remote-side-effect recovery was intentionally not exercised

[P0] [W2-001] tools/saipen_engine/journal.py::recovery_preflight + Journal.start
EVIDENCE: scan_pending() explicitly classifies an operation directory with no operation.json as CORRUPT_JOURNAL because it may contain interrupted pre-manifest staged evidence and says that evidence "must be resolved explicitly". recovery_preflight(), however, removes corrupt entries whose op_id equals exclude_op_id. run_mutation() passes its requested op_id as exclude_op_id, so retrying that same id bypasses the corrupt-evidence gate. Journal.start() then calls self.dir.mkdir(..., exist_ok=True), treats the pre-existing orphan directory as its own, and its exception handler unconditionally executes shutil.rmtree(self.dir, ignore_errors=True). Focused reproduction created .saipen/recovery/ops/op-test/0_8a6dcb9c9836d0a0.staged containing b"OLD-EVIDENCE" but no manifest, then called run_mutation() with op_id="op-test" and a target whose staged filename collided. Current code raised ValueError "journal staged evidence ... already exists" and afterwards op_exists=False: the entire pre-existing corrupt/recovery-evidence directory had been deleted. The focused invariant check printed "CHECK1 FAIL ... op_exists=False".
DEFECT: the same-op retry exemption contradicts the journal's own corrupt-evidence contract. A caller can retry an op_id whose pre-manifest recovery evidence is already present; instead of returning CORRUPT_JOURNAL with zero writes, the start path can throw an unstructured exception and delete the evidence it was required to preserve. This is recovery-data loss at an error boundary.
REPAIR: never exclude CORRUPT_JOURNAL evidence merely because its name equals the requested op_id. Existing valid manifest-bearing same-id operations are already handled by run_mutation() before recovery_preflight, so only malformed/pre-manifest evidence reaches this gate and must remain blocking. Additionally make Journal.start() distinguish a directory created by this invocation from one that pre-existed: cleanup may remove only files/directories conclusively created by the failed start attempt. A pre-existing op directory must never be recursively removed by generic exception cleanup. Prefer exclusive creation/refusal of a fresh journal namespace so an orphan namespace returns a structured CORRUPT_JOURNAL/RECOVERY_REQUIRED result.
VERIFY: create an op directory containing one or more staged files but no operation.json, snapshot every byte, then retry run_mutation() with the identical op_id both with a colliding staged target and a non-colliding target. Both cases must return a stable structured corruption/recovery refusal, emit no traceback, create no manifest/new staged files, and leave the orphan directory byte-for-byte unchanged. Also prove an ordinary genuinely absent op_id still starts and commits normally and a valid existing COMMITTED same-id receipt retains idempotent handling.

[P1] [W2-002] tools/saipen_engine/controls.py::_undo_op_id/undo_confirm
EVIDENCE: _undo_op_id() is deterministic from canonical project identity + target checkpoint + reason. undo_confirm() constructs Journal(root, op_id) and, before undo_preview(), current milestone validation, source-binding validation, project-receipt binding or semantic comparison, performs journal.read(); any record whose status string is COMMITTED causes immediate Result(ok=True, code="ALREADY_APPLIED"). Focused reproduction created only a settled operation.json under the computed undo id with operation="totally-different", status="COMMITTED", zero targets and unrelated semantic fields. No milestone state was created at all. undo_confirm(root, "agent", "CP-1", "same reason") nevertheless returned True / ALREADY_APPLIED / {"target":"CP-1","reason":"same reason"}. The focused invariant check printed "CHECK2 FAIL True ALREADY_APPLIED ...". A legitimate historical undo receipt has the same structural problem: after later milestones/work modify the project, invoking the same target+reason reuses the same op_id and can report the old undo as already applied without proving its restored after-state is still live.
DEFECT: deterministic undo idempotence is decided solely by op_id plus a mutable status field, not by the current project state or the receipt's semantic identity. An unrelated/colliding COMMITTED journal can falsely satisfy undo, and a previously valid undo can remain a permanent ALREADY_APPLIED oracle after subsequent project evolution, causing a requested current undo to become a silent no-op.
REPAIR: remove the unconditional pre-preview COMMITTED shortcut. A committed deterministic undo receipt may return ALREADY_APPLIED only after strict operation-record decoding, project-lineage binding, operation == "milestone_restore", semantic binding to the requested target/reason, and proof that every receipt target still equals its recorded after_hash, including the milestone current pointer/Core evidence. If the old receipt is semantically different, return an op-id collision/corruption refusal. If it is the same historical undo but its after-state no longer holds, continue normal current undo planning or allocate an execution identity that includes the current source/current milestone generation rather than falsely reporting completion. Do not weaken append-only LOG/Git-history invariants.
VERIFY: add four regressions: (1) unrelated COMMITTED receipt under the deterministic undo id must never return ALREADY_APPLIED; (2) exact immediate retry after a successful undo with all after-state intact returns ALREADY_APPLIED; (3) perform a successful undo, then advance/create a later milestone and invoke the same target+reason again: it must operate on the current state or return an explicit stale/collision result, never the historical ALREADY_APPLIED shortcut; (4) corrupted/malformed receipt under that id must return structured corruption with zero project writes.

[P1] [W2-003] tools/saipen_engine/journal.py::run_mutation existing-receipt semantic fingerprint
EVIDENCE: run_mutation() documents that an existing op_id must satisfy exact semantic idempotence, but the retry fingerprint contains only operation, semantic_payload_hash, verification_policy, target path/role/action/after_hash, and a selected receipt_metadata subset. It omits the request's canonical preconditions and read_preconditions. The persisted fingerprint likewise omits record.preconditions, record.read_preconditions and each target's before_hash. Focused reproduction committed op_id="same-op" while read_preconditions bound absent y.txt, then created y.txt with new content and retried the same op_id, operation, payload and x.txt target using the new y.txt dependency hash. The first result was COMMITTED. The causally different second request returned {"ok": True, "code": "ALREADY_APPLIED"} instead of identifying an op-id semantic collision. The focused invariant check printed "CHECK3 FAIL" with those two results.
DEFECT: the idempotence key describes desired output but not the causal snapshot that authorized that output. Two operations with the same op_id and after-bytes but different read/write precondition authority collapse into one receipt. Retry/collision handling can therefore claim that a materially different request was already applied, defeating the stale-plan/read-dependency guarantees that normal first execution enforces.
REPAIR: make the existing-receipt semantic comparison include the canonical request authority that affects whether the operation was authorized: canonical preconditions and canonical read_preconditions, including expected absence/tree/source-identity tokens, with deterministic normalization identical on write and retry. Where write preconditions are represented both as preconditions and target before_hash, choose one canonical representation and use it consistently rather than maintaining two drifting fingerprints. Preserve intentional exclusions only for metadata derived after APPLY (for example resulting_* fields), and document those exclusions explicitly.
VERIFY: commit an operation with one read dependency/precondition set, then retry the same op_id with identical operation/payload/target after-bytes but: (a) a changed read dependency hash, (b) a changed write precondition/expected-before state, and (c) an added/removed dependency. Every case must return a stable op-id semantic-collision refusal and write nothing. An exact retry with byte-for-byte equivalent canonical dependencies must still return ALREADY_APPLIED. Repeat the tests with equivalent path spellings that canonicalize to the same owned identity to prove normalization does not manufacture false collisions.

SECOND_WAVE_DONE_WHEN: W2-001 preserves all pre-existing malformed/pre-manifest journal evidence and returns structured zero-write refusal on same-op retries; W2-002 proves deterministic undo completion against strict receipt semantics plus the current live after-state instead of status alone; W2-003 binds idempotence to canonical causal preconditions/read dependencies as well as outputs; all three focused failing checks turn green, the existing 101/101 second-wave regressions remain green, compileall/diff checks pass, and the prior CORE-001..CORE-003 repairs remain independently verifiable.

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-28T00:28:19+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: UNKNOWN_FROM_SUPPLIED_INPUT
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: performance
WAVE_INDEX: 3
WAVE_COUNT: 3
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: SAIPEN_27.08.26-T23-36-48.zip -> fully unpacked SAIPEN project
BASELINE: archive SHA-256 fa3fe76ec160ca80803b86bb2c3e2290e1a38007ed844e7134ccb769b9195f58; Git main@dbbfad94688515d604b54e2c97dc92aa5fa7eab1; exact tag v7.231.9
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@dbbfad94688515d604b54e2c97dc92aa5fa7eab1 - exact tag v7.231.9; tracked implementation matches HEAD while supplied operational .saipen state contains post-HEAD modifications/untracked runtime material
SAIPEN_CONTEXT: STALE - BOARD/STATE/LOG/intake/recovery/producer/cache material contains post-HEAD state and tracked sealed LOG segments are deleted in the supplied worktree; used only for orientation, with tracked live implementation authoritative
AUDIT_SCOPE: liveness cache contention and lock semantics; focus/cut source scanning and memory bounds; producer staging/READY serialization, publication and restart visibility; conformance receipt/index scaling; repeated receipt/freshness scans; existing performance regression contracts; scenario/test orchestration; compile/diff sanity
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: NONE
VERIFIED_INSTEAD: 37 existing focused performance checks passed (T-1019: 15, T-1020: 8, T-1021: 2, T-1023: 12); producer-parallelism 17 tests passed; crew-liveness 14 tests passed; control-primitives 34 tests passed; CCC/FF control 3 tests passed; conformance-closure 29/29 checks passed; focused cross-process lock, large-file RSS, oversized READY and conformance-scaling reproductions verified the findings below; compileall for tools/saipen_engine and git diff --check passed
STATUS: PERFORMANCE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/saipen_engine/liveness.py::{record_actionable,clear}; tools/saipen_engine/lock.py::{file_writer_lock,_os_lock}; tools/saipen_engine/controls.py::{focus_projection,_read_text_lossy,_project_tree_fingerprint,_source_binding,cut_preview}; tools/saipen_engine/producer.py::{_read_owned_descendant,StagingGeneration.add_payload,StagingGeneration._publish_under_lock,StagingGeneration.scan_ready,StagingGeneration.ready_package}; tools/saipen_engine/conformance.py::{generate_conformance_receipt,_update_receipt_index,_iter_receipts,latest_receipt}; related freshness/journal snapshot paths; tools/perf_wave_regressions.py; producer/liveness/control/conformance regression suites
COVERAGE_DEFERRED: external remote/network publication effects; native Windows lock/reparse behavior could not be runtime-exercised in this Linux sandbox; full tools/perf_wave_regressions.py did not finish inside the 300-second audit execution window; exact SAIPEN_THIRD_WAVE_PROBES_ONLY=1 tools/run_scenarios.py invocation did not finish inside 180 seconds although its individual third-wave probe functions completed when invoked directly, so that orchestration-level delay remains unisolated rather than ticketed
CROSS_WAVE_REFERENCES: CORE-001 release can omit linked active source authority from the committed clone; CORE-002 archive-settlement recovery mutates before validating corrupt archive evidence; CORE-003 source-body writer/reader size contracts disagree. W2-001 same-op journal retry can destroy malformed pre-manifest recovery evidence; W2-002 deterministic undo can falsely return ALREADY_APPLIED without proving current live state; W2-003 journal idempotence omits causal preconditions/read dependencies. None are duplicated below.
RESIDUAL_UNCERTAINTY: CAMPAIGN_RUN_ID and a serialized previous-wave SHA-256 were not supplied and are intentionally not inferred; full performance/scenario runners exceeded the bounded audit windows for an unisolated reason; Windows-specific locking behavior remains statically rather than natively verified

[P1] [PERF-001] PROVEN BOTTLENECK tools/saipen_engine/liveness.py::{record_actionable,clear} + tools/saipen_engine/lock.py::file_writer_lock
EVIDENCE: liveness.py defines the liveness record as a rebuildable best-effort projection that must not become a new failure surface, but both record_actionable() and clear() acquire the liveness writer lock with blocking=True. file_writer_lock reaches _os_lock(), whose POSIX path uses a blocking flock and whose Windows path uses the corresponding blocking msvcrt lock, with no timeout. A focused cross-process reproduction held exactly liveness.LOCK_REL from one process for five seconds, then invoked record_actionable() in a second process under a one-second process timeout. The holder reported "holder-acquired"; the caller was still blocked when killed at 1.00 seconds and exited 124. Normal crew command handling calls these liveness writers after computing user-visible crew state, so contention in an auxiliary cache can delay the primary command for the full duration of another holder.
ISSUE: a best-effort cache is wired as a mandatory unbounded synchronization point. Parallel agents or a stalled lock holder can turn crew/liveness bookkeeping into arbitrary command latency or an apparent CLI hang even though skipping one cache update is safe and the projection is explicitly rebuildable.
OPTIMIZE: retain cross-process locking, but acquire the liveness cache writer lock non-blockingly. On FileLockBusy, fail soft for this projection: return the already-computed liveness observation without modifying the cache, or report a non-fatal cache-skip result to the internal caller. Apply the same policy to clear(). Do not spin or add polling retries to this hot path.
GUARDRAIL: preserve the existing serialized read-modify-write behavior whenever the lock is immediately obtainable; preserve repeat/stalled counting semantics for ordinary sequential calls; malformed cache data must retain its current fail-safe treatment; read-only and dry-run paths must remain zero-write; this optimization must not weaken authoritative project writer locking elsewhere.
VERIFY: hold the liveness lock from another process and prove record_actionable(), clear(), and the public crew path return promptly rather than waiting for the holder. Confirm no partial/malformed cache file is produced. Then run sequential duplicate observations and prove repeats/stalled progression is unchanged. Add concurrent-process coverage showing callers either perform one valid locked update or skip the optional cache update, with no indefinite wait.

[P1] [PERF-002] PROVEN BOTTLENECK tools/saipen_engine/controls.py::{_read_text_lossy,focus_projection,_project_tree_fingerprint,_source_binding}
EVIDENCE: _read_text_lossy(path, limit=256_000) calls path.read_bytes() first and only afterward checks len(raw) > limit, so its advertised limit does not bound I/O or allocation. focus_projection() may call this helper across up to 1200 candidate source files. Separately, _project_tree_fingerprint() also calls path.read_bytes() for every included source file before hashing it, and _source_binding()/cut paths depend on that fingerprint. A focused reproduction added one 134,217,728-byte sparse file to an otherwise small scenario project. focus_projection(root, "definitely_absent_token") returned successfully with zero matches, but /usr/bin/time measured approximately 242,572 KiB peak RSS. Direct _read_text_lossy() and _project_tree_fingerprint() reproductions against the same 128 MiB file reached approximately 242,776 KiB and 242,632 KiB RSS respectively. The process therefore materializes a file hundreds of times larger than the text-search limit merely to discover that it should be skipped, while tree fingerprinting scales process memory directly with the largest source file.
ISSUE: nominal per-file bounds are post-allocation checks rather than admission bounds, and hashing is whole-file rather than streaming. Large media, generated assets or accidental repository blobs can produce large transient allocations, paging, or OOM in focus/cut workflows. focus_projection additionally retains a theoretical scan cost of up to roughly 1200 × 256 KiB of accepted textual content even after the single-file defect is fixed.
OPTIMIZE: make _read_text_lossy reject oversized regular files before reading their bodies, preferably through the project's bounded/no-follow read primitive or an equivalent stat/fstat-verified bounded read. Change _project_tree_fingerprint() to hash included regular files incrementally in fixed-size chunks while preserving the exact current digest framing, including path ordering and encoded byte-length contribution. Where race resistance matters, verify descriptor identity/size around the streamed read rather than reverting to read_bytes(). Keep the current file-count and match-count focus bounds; a total text-byte budget may be added only if it preserves deterministic focus semantics.
GUARDRAIL: do not change which project paths participate in focus or source binding; do not follow symlinks or weaken existing containment/ownership checks; fingerprints produced for stable ordinary files must remain byte-for-byte identical to the current algorithm so existing plans/bindings are not invalidated merely by the optimization; files at or below the configured text limit must remain searchable.
VERIFY: add a 128 MiB or suitably scaled sparse-file regression and assert focus rejects it without reading the full body and with peak memory not proportional to file size. Prove streamed _project_tree_fingerprint() equals the pre-change implementation on a fixture containing empty, binary and normal text files. Exercise a large included file and assert peak RSS stays bounded while the digest remains stable across repeated runs. Retain existing focus/cut stale-binding regressions.

[P1] [PERF-003] PROVEN BOTTLENECK tools/saipen_engine/producer.py::{StagingGeneration.add_payload,StagingGeneration._publish_under_lock,StagingGeneration.scan_ready,StagingGeneration.ready_package,_read_owned_descendant}
EVIDENCE: producer READY readers use _read_owned_descendant() with a default 32 MiB authority limit. The writer has no corresponding publication bound. add_payload() keeps every raw payload in self._payloads in addition to writing it to staging; _publish_under_lock() then base64-encodes the raw payloads into additional strings and serializes the complete self-contained JSON package before safe_atomic_write_bytes(). A focused 25 MiB payload was accepted by add_payload() and publish() returned {"ok": True, "code": "PUBLISHED", ...}. The resulting READY JSON was 34,953,109 bytes, already larger than the reader's 32 MiB maximum. An immediate StagingGeneration.scan_ready() of the same namespace returned zero valid packages and a bounded-reader refusal for that just-published READY authority. The publication process reached approximately 238,580 KiB peak RSS in the reproduction because raw payload, base64 expansion and serialized JSON coexist transiently.
ISSUE: the producer can successfully publish an artifact that its own discovery/restart path categorically refuses to read. Base64's approximately 4/3 expansion plus duplicate in-memory representations also makes peak memory several times the raw payload size. This is both a stability/effectiveness failure and an avoidable resource amplifier on a primary producer boundary.
OPTIMIZE: establish one canonical READY serialized-size contract shared by publisher and reader. Before committing READY, compute or conservatively bound the final canonical serialized package size and refuse publication if it cannot fit the reader bound. Prefer enforcing a cumulative raw-payload budget early enough that base64 expansion plus metadata is guaranteed to remain representable; if exact serialization is still needed, serialize once and compare the exact byte length before the atomic READY write. Avoid retaining unnecessary simultaneous copies of raw payloads, base64 strings and final JSON where the current self-contained format permits streaming or staged encoding. Do not "fix" this by making the reader effectively unbounded.
GUARDRAIL: every PUBLISHED READY package must be reopenable by ready_package()/scan_ready() after process restart; atomic publication and payload SHA-256 verification must remain intact; an oversized refusal must not create a READY authority node or claim PUBLISHED; staging evidence may remain available for correction/retry according to the existing producer lifecycle; keep the bounded-reader security/stability invariant.
VERIFY: test serialized READY boundaries immediately below, at, and above the canonical maximum, including multiple payloads whose total expansion crosses the limit. Every successful publish must survive a fresh StagingGeneration.scan_ready()/ready_package() read. Oversized publication must return a stable non-PUBLISHED result with no READY file, and exact retries must not accumulate READY artifacts. Add a large-payload memory regression demonstrating bounded/controlled peak memory rather than raw + base64 + JSON duplication without limit.

[P2] [PERF-004] PROVEN BOTTLENECK tools/saipen_engine/conformance.py::{generate_conformance_receipt,_update_receipt_index,_iter_receipts}
EVIDENCE: every generated conformance receipt calls _update_receipt_index(). Before advancing the latest-receipt locator, _update_receipt_index() invokes _iter_receipts(root) specifically to ensure a corrupt sibling cannot be hidden. _iter_receipts() glob-sorts, stats, reads and JSON-parses every historical receipt. Consequently receipt append N performs O(N) historical work and a sequence of N appends performs O(N^2) parsing/I/O. Focused measurements with valid small synthetic receipts showed one index update growing approximately linearly with history size: 100 receipts 2.66 ms, 500 10.98 ms, 1000 22.62 ms, 2000 49.53 ms. More importantly, cumulative append measurements were 100 receipts 0.136 s, 200 0.479 s, 400 1.676 s, and 800 6.615 s. Increasing history 8× increased total publication time about 48.6×, demonstrating the quadratic accumulation. latest_receipt() already contains an indexed fast path, so the writer-side full rescan defeats the intended asymptotic benefit during continuous receipt creation.
ISSUE: conformance publication reparses all immutable historical receipts for every append. Long-running projects therefore pay ever-increasing latency and filesystem churn merely to advance a latest locator; cumulative work grows quadratically even when all history is healthy and append-only.
OPTIMIZE: preserve the existing "never hide a corrupt sibling" invariant while making healthy append incremental. Extend the trusted index state with a cumulative validated-lineage/generation proof sufficient to establish that all receipts through the previous generation were already validated; on a normal append, verify that trusted prior state plus the newly created receipt and advance the proof/index without rescanning old receipts. If the index/proof is absent, stale, inconsistent with directory membership, or any out-of-band mutation is detected, fall back to one full _iter_receipts() scan and rebuild the trusted state. The exact representation can stay project-local and simple; the required property is O(1) or append-proportional healthy updates with a full-scan fallback on trust loss.
GUARDRAIL: do not turn the latest index into an authority that can mask corrupt or injected historical siblings. Any out-of-band receipt addition/removal/corruption must invalidate the incremental trust proof and force the existing fail-closed scan behavior. Receipt JSON format, ordering semantics, gate filtering and latest_receipt() correctness must remain unchanged.
VERIFY: retain hostile tests proving an older corrupt sibling prevents a false green/latest result, including corruption after index creation and out-of-band directory changes. Add instrumentation or a spy proving a healthy sequence does at most one bootstrap full-history scan and does not call _iter_receipts() for every append. Re-run 100/200/400/800 publication scaling and require near-linear cumulative growth rather than the measured 0.136/0.479/1.676/6.615-second quadratic curve. Confirm indexed and forced-full-scan latest receipt identities are identical.

PERFORMANCE_DONE_WHEN: liveness bookkeeping can never block a primary crew command on an optional cache lock; focus and source-binding scans have real pre-read/streaming memory bounds while preserving fingerprints and containment semantics; every producer READY object accepted as PUBLISHED is guaranteed readable under the same bounded contract after restart and oversized payloads are refused before READY publication; healthy conformance receipt append no longer rescans the entire historical corpus while corrupt/out-of-band history remains fail-closed; focused defect reproductions turn green, the existing 37 performance regressions plus producer/liveness/control/conformance suites remain green, compile/diff checks pass, and CORE-001..003 plus W2-001..003 invariants remain intact.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.
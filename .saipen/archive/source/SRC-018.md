# SAIPEN — Audit Handoff

RUN_ID: acb-mtliklnp-607566f6639c434b9512
GENERATED_AT: 2026-09-03T16:37:27.509791
PROJECT_NAME: SAIPEN
TARGET: SAIPEN_03.09.26-T15-40-44.zip -> SAIPEN repository snapshot, VERSION 7.249.0
BASELINE: archive sha256=f9b73b98cb891c3415d67dac3512f32715992928513c7374dda11c4b1e1adbcb; AUDAPACK manifest created_at=2026-09-03T15:40:56.653317; recorded main@02fd4892; dirty=true; changed_files=17
GIT_CONTEXT: UNREADABLE - .git/HEAD and refs preserve main@02fd489277227ffe65971f7cf06febb355233892, but the archive omits .git/objects; standard git status/log/diff/rev-parse cannot operate
SAIPEN_CONTEXT: PRESENT - STATE is BUILD/T-1276, last_event=5578, mode=full; BOARD/active LOG/KNOWLEDGE are readable, but tracked sealed history .saipen/logs/LOG-001.md..LOG-016.md is absent from the archive
TOTAL_TICKETS: 12
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 5 tickets (baseline: archive sha256=f9b73b98cb891c3415d67dac3512f32715992928513c7374dda11c4b1e1adbcb; AUDAPACK manifest created_at=2026-09-03T15:40:56.653317; recorded main@02fd4892; dirty=true; changed_files=17)
- Wave 2 (Second Wave): 4 tickets (baseline: archive sha256=f9b73b98cb891c3415d67dac3512f32715992928513c7374dda11c4b1e1adbcb; AUDAPACK manifest created_at=2026-09-03T15:40:56.653317; recorded main@02fd4892; dirty=true; changed_files=17)
- Wave 3 (Performance): 3 tickets (baseline: archive sha256=f9b73b98cb891c3415d67dac3512f32715992928513c7374dda11c4b1e1adbcb; AUDAPACK manifest created_at=2026-09-03T15:40:56.653317; recorded main@02fd4892; dirty=true; changed_files=17)

---
## 01 — AUDIT CORE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-09-03T15:41:00+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtliklnp-607566f6639c434b9512
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
WAVE_ID: core
WAVE_INDEX: 1
WAVE_COUNT: 3
WAVE: AUDIT CORE
TARGET: SAIPEN_03.09.26-T15-40-44.zip -> SAIPEN repository snapshot, VERSION 7.249.0
BASELINE: archive sha256=f9b73b98cb891c3415d67dac3512f32715992928513c7374dda11c4b1e1adbcb; AUDAPACK manifest created_at=2026-09-03T15:40:56.653317; recorded main@02fd4892; dirty=true; changed_files=17
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: UNREADABLE - .git/HEAD and refs preserve main@02fd489277227ffe65971f7cf06febb355233892, but the archive omits .git/objects; standard git status/log/diff/rev-parse cannot operate
SAIPEN_CONTEXT: PRESENT - STATE is BUILD/T-1276, last_event=5578, mode=full; BOARD/active LOG/KNOWLEDGE are readable, but tracked sealed history .saipen/logs/LOG-001.md..LOG-016.md is absent from the archive
AUDIT_SCOPE: system map from tools/saipen.py -> routing/capability -> operations/transitions -> STATE/BOARD/LOG ownership -> verification/acceptance -> journal/persistence -> validation/registry/conformance -> phase contracts -> focused regression tests; active T-1276 oracle-integrity implementation inspected in depth
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: archive omits .git/objects and tracked sealed LOG-001..016, so Git-dependent freshness/history checks cannot be fully verified; one broader focused pytest batch timed out at 120s after 44 tests progressed
VERIFIED_INSTEAD: python tools/conformance_corpus.py --check reproduced 3 intrinsic corpus failures; python tools/validate.py --gate core reproduced 7 failures, with corpus failures intrinsic and Git/history/root-manifest failures attributable to archive packaging; pytest tools/test_verify_oracle.py tools/test_verification_grammar.py -q => 32 passed; pytest tools/test_protocol_registry.py -q => 16 passed; compileall tools succeeded; direct hostile-function reproductions performed
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 5
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: VERSION; _AUDAPACK_MANIFEST.json; .saipen/{STATE,BOARD,LOG,IDENTITY,KNOWLEDGE}; saipen/{CORE,COMMANDS,OPS,SOURCES,REGISTRY,MANIFEST,CONFORMANCE,phases/verify.md,phases/review.md}; tools/saipen.py; tools/validate.py; tools/saipen_engine/{router,operations,log,oracle,capability,acceptance,corpus,registry,state,snapshot,journal,intent,effects}; conformance corpus and focused verification/registry tests
COVERAGE_DEFERRED: performance/scale profiling to later quick3 wave; translation/subSaipen kitchen payload internals; external host/service behavior; complete Git-history/diff and sealed-LOG-history validation unavailable from supplied archive
CROSS_WAVE_REFERENCES: NONE - first wave; active project ticket T-1276 directly overlaps CORE-001 and CORE-002
RESIDUAL_UNCERTAINTY: snapshot is mid-BUILD T-1276 rather than a claimed finished implementation; omitted Git objects and sealed LOG segments prevent proving full historical integrity, but do not affect the direct code-path reproductions below
ACB_CHAIN_RECEIPT: startcore-mtlikabr-4babe14ec207

[P1] [CORE-001] tools/saipen_engine/{oracle.py::regression_pair_verdict,log.py::verification_evidence,operations.py::_plan_transition/_plan_finish}
EVIDENCE:
- oracle.py:25-27 explicitly makes the new oracle logic pure and unaware of tickets/state.
- Repository search finds verifier_identity/regression_pair_verdict production definitions but no production caller; practical uses are confined to tools/test_verify_oracle.py.
- operations.py:1321-1335 gates VERIFY -> REVIEW solely through verification_evidence(ticket, history).
- operations.py:2020-2034 uses the same verification_evidence() gate before ticket finish.
- log.py:648-696 decides success from free-form current-cycle RUN text containing PASS + conf: high or MANUAL-VERIFY; it never calls regression_pair_verdict and never compares oracle/subject identities.
- Direct repro: after a VERIFY boundary, `PASS conf: high oracle:22222222 subject:aaaaaaaa` returns `(True, ...)`; changing the oracle token or subject token does not affect that verdict.
DEFECT:
VERIFY-ORACLE-01 is currently documentation plus a detached helper/test, not an authoritative lifecycle invariant. A bug may remain untouched, its test/oracle may be weakened, a normal `PASS conf: high` may be logged, and the canonical VERIFY -> REVIEW and finish gates still accept it. This is exactly T-1276's CONTROL_DISARMED escape.
REPAIR:
Create one machine-owned regression-evidence path that records the pre-fix FAIL and post-fix PASS with verifier identity, subject identity, command/oracle scope and current verification-cycle provenance. Establish a structured way to mark when bug-fix/regression evidence is required; do not infer it from narrative prose. Make the authoritative transition and finish gates consume regression_pair_verdict for those tickets. REVIEW/reconcile/status must consume the same stored verdict rather than independently treating generic green text as equivalent evidence. A verifier change must spend the previous pair and require a new pre-fix FAIL under the new verifier.
VERIFY:
- Red control: buggy subject + original oracle FAIL; weaken only oracle until green; actual transition VERIFY -> REVIEW must return INCOMPLETE_TICKET/ORACLE_CHANGED with zero canonical writes.
- Green control: original oracle unchanged, pre-fix subject FAIL, post-fix subject PASS => transition admitted.
- Change both subject and oracle => refused until FAIL is re-established with the new oracle.
- Finish gate must independently refuse an inadmissible/missing pair.
- Tests must exercise real transition/finish APIs, not regression_pair_verdict in isolation.

[P1] [CORE-002] tools/saipen_engine/oracle.py::regression_pair_verdict/parse_identity
EVIDENCE:
- regression_pair_verdict lines 127-135 fails closed only when verifier identity is absent.
- Subject equality is checked only under `if before_s and after_s and before_s == after_s` at lines 150-156; missing subject identities fall through to ADMISSIBLE at lines 157-160.
- Direct repro: FAIL `{verifier:v1}` -> PASS `{verifier:v1}` returns `ADMISSIBLE`; FAIL `{verifier:v1,subject:s1}` -> PASS `{verifier:v1}` also returns `ADMISSIBLE`.
- tools/test_verify_oracle.py:264-269 covers missing verifier identity but has no equivalent missing-subject rejection.
- parse_identity() returns keys `oracle` and `subject`, while regression_pair_verdict() consumes `verifier` and `subject`; the parser's record is therefore not the verdict function's declared record shape without undocumented remapping.
DEFECT:
The new evidence primitive is fail-open on the identity of the implementation whose change supposedly caused the green result. An absent subject is treated as proof that the subject changed. Its parse/compare record vocabulary is also internally inconsistent, making future production wiring easy to get wrong.
REPAIR:
Require non-empty identities on BOTH sides for verifier and subject before any ADMISSIBLE result. Missing subject identity must be explicitly inadmissible. Define one canonical machine record/key vocabulary for serialized evidence and comparison (`verifier` vs `oracle`, not both implicitly), with one parser producing exactly the shape consumed by regression_pair_verdict.
VERIFY:
Add red tests for missing-before-subject, missing-after-subject, both-subjects-missing, missing verifier on either side, and parser -> verdict round-trip. Every incomplete identity record must be inadmissible. Preserve honest same-verifier/different-subject ADMISSIBLE and same-subject SUBJECT_UNCHANGED cases.

[P1] [CORE-003] tools/saipen_engine/log.py::verification_evidence + tools/saipen.py::transition/checkpoint + saipen/phases/verify.md
EVIDENCE:
- verify.md:9-12 says manual-verify must not auto-transition and may proceed only from the user's reported result.
- verify.md:39-42 says unavailable GUI/environment checks record MANUAL-VERIFY STEPS + EXPECTED and explicitly says never fabricate PASS.
- log.py:688-689 treats the mere presence of the exact MANUAL-VERIFY token as successful verification evidence.
- Direct repro with only `transition to VERIFY` followed by `MANUAL-VERIFY steps recorded; 0 FAIL` returns `(True, ...)`.
- tools/test_verification_grammar.py:100-102 explicitly locks this unsafe behavior in by asserting that `MANUAL-VERIFY steps recorded; 0 FAIL` counts as success.
- tools/saipen.py:5079-5086 only denies transition when live capability equals read-only; manual-verify is not blocked, and transition_phase() has no live-capability parameter.
- checkpoint accepts arbitrary RUN description text, providing a canonical route for such a token.
- The same classifier is reused by the finish gate at operations.py:2028.
DEFECT:
A procedural/manual verification instruction is indistinguishable from a successful human verdict. In ordinary full mode, a RUN line merely mentioning MANUAL-VERIFY can satisfy VERIFY and finish. In actual manual-verify mode, the engine also fails to enforce the documented prohibition on automatic transition. Human confirmation has become a magic substring rather than provenance-bearing evidence.
REPAIR:
Separate manual verification REQUEST/STEPS from manual verification RESULT. Only a canonical human-reported PASS record may satisfy the gate. Carry the current negotiated capability into transition policy so manual-verify cannot automatically leave VERIFY/REVIEW before such a result exists. Generic MANUAL-VERIFY prose or steps must never classify as PASS, regardless of current mode. Keep one classifier shared by transition, finish, reconcile and status.
VERIFY:
- Full/no-publish session + `MANUAL-VERIFY steps recorded` => verification false and VERIFY -> REVIEW refused.
- manual-verify session + steps/request only => refused.
- manual-verify session + canonical human PASS result => admitted.
- canonical human FAIL => refused.
- Arbitrary RUN prose containing MANUAL-VERIFY must remain non-authoritative.
- Add end-to-end transition/finish red controls, not only string-classifier tests.

[P1] [CORE-004] tools/saipen_engine/capability.py::negotiate_capability + tools/saipen.py::_negotiate_capability
EVIDENCE:
- capability.py:73-83 maps BOTH an absent declaration and any unrecognized declaration to DEFAULT_CAPABILITY=`full`.
- capability.py:86-101 separately states that unknown capability values must fail CLOSED rather than silently behave like full.
- Direct repro: `negotiate_capability({"SAIPEN_CAPABILITY":"nonsense"})` returns `full`; capability_error() then returns None.
- tools/run_scenarios.py:17473-17475 currently asserts this fail-open behavior as expected.
- Public CLI authorization consumes the normalized value, so the fact that the host supplied an invalid restrictive declaration is irretrievably lost before write/publish gates see it.
DEFECT:
A typo, corrupted value, unsupported spelling or bad host integration in the live authorization declaration silently grants the strongest capability. The closed-set validation exists downstream but can never fire because negotiation launders invalid input into `full`.
REPAIR:
Distinguish ABSENT from PRESENT-BUT-INVALID. Preserve default `full` only for an actually absent declaration if backward compatibility requires it. Any non-empty value outside CAPABILITIES must produce a closed failure propagated through the public command boundary before journal creation, canonical mutation, routing with effects, release or publish. Remove the scenario that treats nonsense as full and replace it with a zero-write refusal control.
VERIFY:
- Env absent => documented default behavior.
- Each four valid capabilities => exact capability.
- `nonsense`, whitespace-wrapped invalid strings and likely typo variants => CAPABILITY_DENIED/VALIDATION_FAILED before any write.
- Hash canonical files/journals before and after a mutating command under invalid capability; bytes and recovery directory must be unchanged.
- Publish/release path must likewise refuse invalid declaration.

[P1] [CORE-005] tools/saipen_engine/corpus.py + tests/conformance_cases.jsonl:257 + tools/validate.py conformance gate
EVIDENCE:
- REGISTRY.json already declares VERIFY-ORACLE-01 as a semantic fact and rule owner.
- tests/conformance_cases.jsonl deliberately adds scenario id 257 referencing VERIFY-ORACLE-01.
- corpus.py:21 fixes EXPECTED_IDS to 1..256.
- corpus.py:22-25 RULE_ID_RE omits the VERIFY prefix.
- corpus.py:157-159 independently hard-codes human text `IDs 1..256`.
- Row 257 coverage is not sorted (`tools/test_verify_oracle.py` precedes `saipen/phases/verify.md`).
- `python tools/conformance_corpus.py --check` reproducibly fails:
  1. row 257 rule_ids invalid,
  2. row 257 coverage not sorted,
  3. extra scenario id 257 outside exactly 1..256.
- `python tools/validate.py --gate core` reproduces the same three intrinsic failures, so the current active change cannot pass the canonical core gate even though `tools/test_protocol_registry.py` reports 16 passed.
DEFECT:
The machine registry and conformance corpus disagree about the newly introduced invariant. The corpus validator still encodes the previous closed world, and the generated prose duplicates its old upper bound. This makes legitimate T-1276 evidence structurally impossible to ship in the current snapshot.
REPAIR:
Deliberately advance the closed corpus contract to include scenario 257; admit VERIFY-ORACLE-01 using the registry-backed Rule-ID authority or, minimally, extend the explicit accepted prefix set consistently; sort row 257 coverage; derive generated display text from the authoritative expected-ID definition rather than a second literal `256`; regenerate saipen/CONFORMANCE.md and any required mirror. Preserve strict detection of gaps, duplicates and unauthorized future rows.
VERIFY:
- `python tools/conformance_corpus.py --check` PASS with exactly 1..257.
- `python tools/conformance_corpus.py --write` followed by `--check` is stable/no-diff.
- VERIFY-ORACLE-01 is accepted because it is a registered Rule ID; an unknown fake rule remains rejected.
- Missing/duplicate IDs remain rejected.
- On a complete checkout, `python tools/validate.py --gate core` has no conformance-corpus or generated-view failure.

CORE_DONE_WHEN: On a complete checkout with full Git objects and sealed LOG history, CORE-001..005 are repaired; real transition/finish red controls reject weakened/missing oracle evidence; missing subject/verifier identity never returns ADMISSIBLE; MANUAL-VERIFY steps cannot masquerade as a human PASS and manual-verify mode cannot auto-transition; invalid SAIPEN_CAPABILITY fails closed with zero writes; conformance scenario 257 and VERIFY-ORACLE-01 validate canonically; focused unit/regression tests, conformance_corpus --check, and tools/validate.py --gate core all PASS without relying on archive-induced exceptions.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-09-03T16:09:23+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtliklnp-607566f6639c434b9512
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
WAVE_ID: second
WAVE_INDEX: 2
WAVE_COUNT: 3
WAVE: AUDIT SECOND WAVE
TARGET: SAIPEN_03.09.26-T15-40-44.zip -> SAIPEN repository snapshot, VERSION 7.249.0
BASELINE: archive sha256=f9b73b98cb891c3415d67dac3512f32715992928513c7374dda11c4b1e1adbcb; AUDAPACK manifest created_at=2026-09-03T15:40:56.653317; recorded main@02fd4892; dirty=true; changed_files=17
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: UNREADABLE - .git/HEAD and refs identify main@02fd489277227ffe65971f7cf06febb355233892, but .git/objects is absent; `git status --short --branch` returns `fatal: not a git repository (or any of the parent directories): .git`
SAIPEN_CONTEXT: PRESENT - STATE is BUILD/T-1276, last_event=5578, mode=full; BOARD/STATE/LOG/KNOWLEDGE are readable; sealed .saipen/logs/LOG-001.md..LOG-016.md are absent from the supplied archive
AUDIT_SCOPE: lifecycle/recovery and hostile-boundary pass over audit producer enqueue -> allocator/lock -> temp placement -> retry/idempotence -> audit inbox classification/capture -> Source receipt -> BOARD Work creation -> binding/routing; existing crash/retry/hostile regression suites exercised
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: full Git/history-dependent recovery verification unavailable because archive omits .git/objects and sealed LOG-001..016; Windows-specific reparse/hardlink execution was not available in the Linux sandbox
VERIFIED_INSTEAD: focused existing suite `python -m pytest -q tools/test_audit_enqueue.py tools/test_repeated_cc.py tools/test_audit_inbox.py tools/test_audit_transport_loop.py tools/regression_second_wave.py tools/regression_second_wave_receipt_fixes.py tools/second_wave_regressions.py tools/test_hostile_wave_regressions.py` => 125 passed, 4 subtests passed in 11.37s; four additional isolated fault/crash injections reproduced invariant failures with no source-tree mutation
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: VERSION; _AUDAPACK_MANIFEST.json; .saipen/{STATE,BOARD,LOG,KNOWLEDGE}; tools/saipen.py::_audit; tools/saipen_engine/{audit_enqueue,audit_inbox,intake,paths,journal,lock}; audit enqueue/inbox/transport/repeated-cc/hostile second-wave regression tests
COVERAGE_DEFERRED: remote release/publish endpoint behavior; performance/scale profiling for wave 3; complete Git-object/sealed-history recovery paths unavailable from supplied archive
CROSS_WAVE_REFERENCES: CORE-001..CORE-005 remain valid on this same baseline and are not duplicated here; W2 findings are new lifecycle/boundary failures. CORE-004 capability fail-open also affects authorization around audit ingest, but is a separate root cause from W2-004 recovery split-brain
RESIDUAL_UNCERTAINTY: snapshot is dirty and mid-BUILD T-1276; Git objects and sealed historical logs are missing. W2-001 hostile temp-node reproductions were executed on Linux; equivalent Windows NTFS reparse/hardlink behavior was not executed, although the vulnerable implementation uses portable `os.open` without exclusive/no-follow protection
ACB_CHAIN_RECEIPT: second-mtlj53r4-c12476e750fc

[P0] [W2-001] tools/saipen_engine/audit_enqueue.py::_place
EVIDENCE:
- The module contract says producer enqueue provides "no path escape"; tools/test_audit_enqueue.py likewise names no-path-escape as an acceptance requirement.
- `_place` lines 307-320 uses the deterministic temporary path `audit/.enqueue-<layer>.tmp`.
- It opens that path with `O_WRONLY | O_CREAT | O_TRUNC`; there is no `O_EXCL`, `O_NOFOLLOW`, exclusive regular-file witness, or inode/link validation.
- The parent directory is proved owned, but the final temporary node itself is not.
- Direct symlink repro: precreate `audit/.enqueue-1.tmp -> /tmp/.../outside.txt`, with outside bytes `ORIGINAL`; `_place(root, 1, b"PAYLOAD")` returns `None`, changes the outside file to `PAYLOAD`, and leaves canonical `audit/1.md` as a symlink to the outside file.
- Direct hardlink repro: precreate `.enqueue-1.tmp` as a hardlink to an outside sentinel; `_place` returns `None`, overwrites the outside inode with `PAYLOAD-HARDLINK`, and canonical `1.md` remains linked to that same outside inode.
- The repository already contains hardened primitives in paths.py: `_open_exclusive_regular` uses O_EXCL/O_NOFOLLOW plus fstat/lstat identity checks, and `_write_all` performs complete writes. `_place` bypasses them.
DEFECT:
The supposedly constrained audit producer can be turned into an arbitrary same-permission file truncation/write primitive by planting the predictable temporary node. The escape happens before canonical-layer validation can reject the resulting symlink. This is a real filesystem-boundary violation and potential data loss outside the project.
REPAIR:
Replace the bespoke predictable-temp sequence with an owned, exclusive, no-follow, uniquely named atomic-create/no-overwrite primitive. Reuse the hardened path machinery instead of duplicating lower-level `os.open`. Preserve the enqueue invariant that an existing canonical layer is NEVER overwritten: do not merely call a generic replace helper whose normal semantics permit replacing a preexisting target. The install step must atomically fail when the destination appears, and temporary-node identity must be witnessed as a newly created regular file through the whole write/install sequence. Include hardlink/link-count considerations in the hostile topology check rather than fixing only symlinks.
VERIFY:
- Preexisting temp symlink to an outside file => refusal; outside bytes unchanged; no canonical layer created.
- Preexisting temp hardlink to an outside file => refusal; outside bytes unchanged.
- Preexisting regular temp node => refusal or safely ignored via a different exclusive random temp; its bytes unchanged.
- Destination created between planning and install => no overwrite.
- Successful enqueue still exposes only complete canonical bytes and leaves no temp residue.
- Run the controls on POSIX and Windows/reparse-capable CI where available.

[P1] [W2-002] tools/saipen_engine/audit_enqueue.py::_place/enqueue retry promotion
EVIDENCE:
- Module lines 23-27 explicitly promise that a crash after placement but before allocator COMMIT is recovered by seeing the file, verifying its digest, then promoting the record.
- `_place` line 316 calls `os.write(handle, body)` exactly once and ignores the returned byte count.
- Direct short-write injection made the underlying write accept only 8 of 16 bytes. `_place` returned `None`; canonical `audit/1.md` contained only `b'01234567'`.
- There is no post-placement readback/hash check before allocator state becomes COMMITTED.
- Retry lines 366-371 use `target.is_file()` and, when a RESERVED record's target exists, immediately set `state=COMMITTED, sha256=digest`; the target bytes are never compared with the recorded digest.
- `Path.is_file()` follows symlinks, so even final-node topology is not an authoritative witness on that path.
- Direct crash-state repro: reserve layer 1 for body `EXPECTED\n` with expected SHA-256 `fe6c38465f34c6c6cfdb0411f8bf0a604da841f46cea8b4de7238957ca0152c1`, place it, replace its bytes with `FOREIGN\n`, then retry the same producer operation. `enqueue` returned `ok=True`, `idempotent=True`, state COMMITTED and the EXPECTED digest while the actual layer digest was `861bbb9fd984a70f43ae176609ecb9ba7f9d35d2db54ea8af5bafb51d67812e2`.
DEFECT:
The allocator can certify bytes that were never durably written. Both normal placement and crash recovery lack the content-identity witness their contract claims: a legal short write produces truncated canonical data, and a RESERVED retry can promote foreign or changed bytes as the producer's committed audit.
REPAIR:
Use the existing complete-write primitive or equivalent loop until every byte is written, fsync the descriptor, and treat zero/negative/failed progress as failure. Before any RESERVED -> COMMITTED promotion, safely no-follow reopen/witness the canonical target and compute its full SHA-256; require exact equality with the operation record/body digest. Perform the same identity check immediately after a fresh installation before recording COMMITTED. On mismatch or unsafe final-node topology, return CONFLICT/RECOVERY_REQUIRED and retain truthful recovery state; never rewrite the expected digest onto unverified bytes.
VERIFY:
- Inject one or several legal short writes: final bytes must equal body exactly or enqueue must fail without COMMITTED.
- Crash after placement, mutate one byte, retry => refusal; allocator stays non-COMMITTED; returned receipt never claims expected SHA as present.
- RESERVED target replaced by symlink => refusal.
- Exact intact crash-left target => same layer promoted idempotently.
- Fresh and retry paths must use the same content-identity verifier.
- Add these controls to tools/test_audit_enqueue.py rather than relying only on direct helper tests.

[P1] [W2-003] tools/saipen_engine/audit_enqueue.py::read_allocator/_reconcile/enqueue
EVIDENCE:
- `read_allocator` lines 163-189 states that absent/corrupt allocator data reads as an empty allocator and says the "worst" loss is idempotency memory.
- JSON decode failure, wrong document type, missing operations map, invalid next_id and broad OSError are all collapsed to `{next_id:1, operations:{}}`.
- `_reconcile` can reconstruct only the numeric allocation floor from current layers, allocator records and inbox bindings. It cannot reconstruct `producer + producer_operation_id -> layer`.
- The producer operation mapping is the sole idempotence authority used at enqueue lines 353-382.
- Direct recovery repro: persist RESERVED `audapack/op-1 -> layer 1`, successfully place its bytes, simulate crash-before-COMMITTED, corrupt `audit_allocator.json` to `{broken`, then retry exactly `audapack/op-1`.
- Retry returned `AUDIT_ENQUEUED`, `layer=2`, `idempotent=False`; both `audit/1.md` and `audit/2.md` existed with identical `AUDIT ONE\n` bytes.
DEFECT:
Allocator corruption is treated as benign rebuildable state even though the essential producer-operation identity is not rebuildable from the data `_reconcile` reads. A crash plus allocator damage therefore converts an idempotent retry into duplicate dispatch, creating duplicate audit work while returning success. The documentation's "worst cost" statement is false.
REPAIR:
Distinguish ABSENT from PRESENT-BUT-CORRUPT/UNREADABLE. If operation identity cannot be reconstructed, fail closed with an explicit allocator corruption/recovery-required result rather than starting with an empty operation map. For automatic recovery, persist enough independent durable provenance to rebuild `{producer, operation_id, layer, digest, state}` without trusting audit prose; a machine-owned sidecar/journal is appropriate. Preserve the monotonic numeric floor, but do not confuse reconstructing `next_id` with reconstructing idempotence. Status must surface corrupt allocator state rather than reporting a synthetic empty allocator.
VERIFY:
- Missing allocator on a genuinely fresh project follows the documented initialization path.
- Corrupt/truncated allocator with existing/reserved layers => explicit recovery refusal and zero new layer writes.
- Crash after placement before COMMITTED + allocator corruption + same-operation retry => exactly one layer; either recover its original allocation or fail closed.
- After an explicit successful repair, a new distinct producer operation allocates above every historically consumed/current layer.
- Duplicate producer-operation retries remain single-allocation after process restart and recovery.

[P1] [W2-004] tools/saipen.py::_audit + tools/saipen_engine/audit_inbox.py::classify/projection/reconcile_bootstrap
EVIDENCE:
- `_audit ingest` first captures the external layer at lines 2330-2340.
- If it has no linked Work, lines 2346-2364 call `ticket_add`, which independently commits a new BOARD ticket.
- Only after that commit, lines 2365-2368 call `capture_layer(..., work=work)` to attach the Source receipt to the newly created Work.
- Only after source linkage does lines 2370-2383 write the audit inbox binding. These are separate durable mutations with no shared resumable transaction marker.
- `classify` lines 440-462 discovers the exact receipt and classifies it ACTIVE while exposing its `linked_work`; after a crash between ticket creation and source relink this is ACTIVE with `linked_work=None`.
- `projection` lines 539-551 explicitly routes ACTIVE/no-Work back to `saipen audit ingest`.
- But `_audit` line 2288 selects only a `NEW` layer. When none exists, lines 2289-2309 return `AUDIT_INBOX_STATUS` success without repairing the ACTIVE/no-Work state.
- Direct crash injection wrapped the real `ticket_add`, allowed it to commit, then raised `SIMULATED_CRASH_AFTER_TICKET_ADD`.
- Crash-left state contained exactly one BOARD ticket `Execute external audit inbox layer audit/1.md (SRC-001)`, while Source `SRC-001` was ACTIVE with `linked_work=None`.
- Retrying the exact projected action `saipen audit ingest` returned rc=0/status only. Source remained ACTIVE with no linked Work, BOARD still had one created ticket, and `projection()` continued to prescribe `saipen audit ingest`.
DEFECT:
Audit ingestion has a split transaction with a permanent crash wedge. A process death after BOARD Work commits but before Source linkage leaves real Work that the router cannot reach from the audit receipt. The router instructs the operator/agent to run a command whose implementation refuses to resume that state, producing an infinite no-progress loop. Naively treating the receipt as NEW would instead risk duplicate Work, so this requires explicit recovery semantics rather than broader selection.
REPAIR:
Make `(layer generation, receipt, Work, binding)` ingestion a resumable idempotent operation. ACTIVE/no-Work must be an explicit recovery state. Persist a stable ingest operation identity before or with Work creation, or move BOARD creation + Source linkage + inbox binding under one recoverable journal/plan so a retry can determine exactly which committed Work belongs to the receipt. On retry after ticket commit, discover the already-created Work through machine-owned identity, attach that exact Work to the receipt, write/reconcile the binding, and continue; never manufacture a second ticket from title-text heuristics. Align `projection()` with an action that actually consumes every state it emits.
VERIFY:
Inject crashes at each durable boundary:
1. after Source capture, before Work creation;
2. after Work commit, before Source linkage;
3. after Source linkage, before inbox binding;
4. after inbox binding, before command return.
For every state, repeating `saipen audit ingest` must converge to exactly one Source receipt, exactly one BOARD Work item, exactly one receipt->Work link, exactly one ACTIVE binding, and the normal `PHASE SCOUT <work>` continuation. Further repeats must be byte/idempotence stable. The router must never repeatedly prescribe an ingest action that returns success without changing a recoverable partial state.

SECOND_WAVE_DONE_WHEN: W2-001..W2-004 are repaired on a complete checkout; producer placement cannot write through symlink/hardlink/predictable temp carriers or overwrite a raced destination; every committed enqueue is backed by exact safely witnessed bytes; allocator corruption cannot duplicate a producer operation; every audit-ingest crash boundary converges idempotently to one receipt + one Work + one binding; the four adversarial controls turn green; the existing 125-test/4-subtest focused lifecycle suite remains green; and complete-checkout validation/recovery tests introduce no new Git/history failures.

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-09-03T16:30:43+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtliklnp-607566f6639c434b9512
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
WAVE_ID: performance
WAVE_INDEX: 3
WAVE_COUNT: 3
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: SAIPEN_03.09.26-T15-40-44.zip -> SAIPEN repository snapshot, VERSION 7.249.0
BASELINE: archive sha256=f9b73b98cb891c3415d67dac3512f32715992928513c7374dda11c4b1e1adbcb; AUDAPACK manifest created_at=2026-09-03T15:40:56.653317; recorded main@02fd4892; dirty=true; changed_files=17
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: UNREADABLE - partial .git metadata identifies main@02fd489277227ffe65971f7cf06febb355233892, but .git/objects is absent; `git status --short --branch` returns `fatal: not a git repository (or any of the parent directories): .git`
SAIPEN_CONTEXT: PRESENT - STATE is BUILD/T-1276, last_event=5578, agent=workbuddy, mode=full; BOARD/STATE/LOG/KNOWLEDGE are readable; sealed LOG-001.md..LOG-016.md are absent from the supplied archive
AUDIT_SCOPE: hot-path and lifetime-growth inspection of semantic operation receipts, settled-receipt indexing, operation-specific receipt lookups, SubSaipen projections, conformance latest-receipt lookup/index maintenance, performance regression oracles, and third-wave producer-gate test execution
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: complete Git/history-dependent profiling is unavailable because the archive omits .git/objects and sealed LOG-001..016; `python -u tools/perf_wave_regressions.py` completed rc=1 with `perf wave: 39/41 checks passed, 2 failed`, both through nested T-1022 third-wave runner state (`third_wave: 93/95 checks passed, 2 failed`), including `saipen next --json` returning VALIDATION_FAILED / checkpoint-invalid
VERIFIED_INSTEAD: direct read-only complexity instrumentation on the supplied 2,526-receipt/1,028-conformance-receipt snapshot; tracemalloc and wall-clock measurements; content-hash call counting; valid settled-index behavior measured on a disposable copy; `run_producer_gate_probes()` executed independently with 14/14 checks passing
STATUS: PERFORMANCE: COMPLETE
TICKETS: 3
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: VERSION; _AUDAPACK_MANIFEST.json; .saipen/{STATE,BOARD,LOG,recovery/settled,recovery/conformance}; tools/saipen_engine/{journal,conformance,subs,convergence,crew,intent,release,operations}; tools/perf_wave_regressions.py; tools/run_scenarios.py::run_producer_gate_probes; semantic-receipt and conformance-index call sites
COVERAGE_DEFERRED: remote/network release verification; Windows-specific filesystem performance; complete Git-object/sealed-history flows; broad UI/DOM surfaces not present in this Python-centric target
CROSS_WAVE_REFERENCES: CORE-001..CORE-005 and W2-001..W2-004 remain valid on this baseline and are not duplicated here. PERF-001 concerns cost/scaling of the same receipt authority used by several lifecycle paths but is distinct from W2 enqueue/ingest correctness defects
RESIDUAL_UNCERTAINTY: absolute timings depend on cache and sandbox conditions, but deterministic complexity evidence is independent of timing: 2,526 settled records are traversed for operation-specific queries, 1,028 conformance receipt contents are hashed by a supposedly constant-I/O lookup, and producer-gate probes launch 13 complete validator processes
ACB_CHAIN_RECEIPT: performance-mtljng1q-56ac442d9e4b

[P2] [PERF-001] PROVEN BOTTLENECK tools/saipen_engine/journal.py::_read_settled_index / semantic_receipt_snapshot / semantic_receipt_digest / semantic_receipts_for_operation* / _settle_journal
EVIDENCE:
- The supplied archive contains 2,526 terminal receipt directories under `.saipen/recovery/settled`, occupying approximately 46 MiB.
- The existing `.receipt-index.json` is 7,809,985 bytes but contains only 1,567 entries while 2,526 settled directories exist, so it is already stale on the supplied baseline.
- `_read_settled_index()` does not provide bounded lookup even when the index is valid:
  - lines 4234-4238 reconstruct and compare `_settled_stat_inventory(root)`, which walks every settled receipt and stats its directory, operation.json and progress.json;
  - lines 4239-4252 then read and SHA-256 every operation.json and every present progress.json before accepting the index.
- The index duplicates each fully decoded operation record (`"record": record` at lines 4271-4282), so its memory/storage cost also grows with lifetime receipt volume.
- A disposable rebuild of the same 2,526-receipt history produced a valid settled index of 20,738,998 bytes. A single `_read_settled_index()` on that valid index took 1.078 s under tracemalloc and peaked at 76.9 MiB.
- The stale-index path is also material: direct `semantic_receipts_for_operation_safe(root, "ticket_transition")` on the supplied snapshot returned ZERO matching records but still took 3.394 s and peaked at 40.2 MiB because it constructed the complete 2,526-record semantic snapshot before filtering.
- A direct `sub_list(root)` call took 3.727 s and peaked at 40.4 MiB. `subs.py` obtains the same full semantic receipt snapshot even though the command returns only the current SubSaipen projection.
- Warm-page-cache runs reduce the constants but not the work: zero-match operation lookup still took ~0.62 s and `sub_list` ~0.70 s while traversing the same lifetime history.
- `_settle_journal()` lines 529-543 attempts `_extend_settled_index()` only when the prior index is already valid. If an index exists but is stale/corrupt, the bootstrap branch is skipped because it runs only when the index file does not exist. Once this cache falls stale, ordinary future settlements therefore leave it stale and every consumer remains on the lifetime full-scan path.
ISSUE:
A routine command is coupled to the entire historical journal. Both the fallback and the nominal cache are O(N) in settled-receipt count; the nominal cache additionally materializes a large duplicate JSON representation and re-hashes every authority file. Operation-specific APIs then sort/materialize all history before discarding unrelated records. The supplied project has already crossed into multi-second, ~40-77 MiB read-only calls at only 2,526 receipts, and the stale index cannot self-heal through normal operation. This is an unbounded latency/memory curve in long-running agent loops, not merely cosmetic file size.
OPTIMIZE:
- Keep immutable exact-byte receipt evidence authoritative, but stop representing all lifetime receipts as one giant decoded JSON cache.
- Compact old settled receipts into bounded immutable sealed segments, with each segment carrying a content hash plus minimal indexed metadata (`op_id`, operation, status, created_at, lineage, segment/offset). Keep only a small active tail as individual directories.
- Make operation-specific queries use the operation index/segment offsets directly instead of constructing `semantic_receipt_snapshot()` for every unrelated historical record.
- Make the common digest/snapshot path verify a bounded number of sealed segment authorities plus the active tail. Reserve a deep leaf-by-leaf historical audit for the explicit validation/recovery path.
- Preserve detection of same-size/mtime-restored replacement by making a changed sealed segment fail its owned-node/content witness; do not trade correctness for a blind memoization cache.
- Make stale-index recovery explicit and crash-safe. If a stale existing projection is detected, rebuild/replace it once from canonical evidence or persist a repair-required state; do not silently remain in permanent full-scan fallback simply because the stale file exists.
- Avoid embedding complete decoded records in the hot index when minimal query metadata plus immutable segment offsets suffice.
GUARDRAIL:
- Settled operation.json/progress.json bytes remain authoritative until safely sealed.
- Duplicate op_id detection across active and settled history must remain fail-closed.
- Foreign lineage, malformed receipt, symlink/reparse topology, structural corruption and exact-byte digest mismatches must retain current refusal semantics.
- Crash during settlement/segment sealing must leave either the old valid generation or the new valid generation, never an index that claims evidence not durably present.
- An invalid/stale accelerator must never manufacture positive semantic evidence.
VERIFY:
- Build equivalent fixtures with 100, 1,000, 5,000 and 20,000 historical receipts and instrument file opens/stats/bytes decoded.
- An operation query with zero matches must not decode/sort all N historical records; work should scale with active tail + matching index/segments.
- Current 2,526-receipt fixture: `semantic_receipts_for_operation_safe(..., "ticket_transition")` and `sub_list()` must avoid opening all 2,526 operation manifests and must remain under a documented bounded I/O budget.
- A healthy settled projection must not require loading a 20+ MiB duplicate-record document merely to answer one operation query.
- Deliberately stale the projection, then perform one normal settlement/recovery cycle; subsequent reads must converge back to the healthy indexed path rather than permanent full scan.
- Modify an old sealed receipt/segment without changing its nominal logical metadata; positive evidence must be refused.
- Preserve existing duplicate-id, foreign-lineage, malformed-record, crash-recovery and digest regression controls.

[P2] [PERF-002] PROVEN BOTTLENECK tools/saipen_engine/conformance.py::latest_receipt / _receipt_inventory / _receipt_content_inventory / _receipt_member_inventory / _update_receipt_index + tools/perf_wave_regressions.py PERF-004 control
EVIDENCE:
- The supplied project has 1,028 top-level conformance receipt JSON files under `.saipen/recovery/conformance`; `.lineage.json` is 134,934 bytes and its `members` array grows with the receipt population.
- Source comments at lines 497 and 711-717 describe the per-gate index as an O(1)/constant-I/O latest-receipt mechanism.
- `latest_receipt()` lines 958-965 reads the direct locator, then immediately calls BOTH `_receipt_inventory(root)` and `_receipt_content_inventory(root)` before trusting it.
- `_receipt_inventory()` lines 502-529 scans every receipt and lstats every file.
- `_receipt_content_inventory()` lines 590-610 scans every receipt again and `_receipt_content_token()` opens and SHA-256 hashes the exact bytes of every file.
- Direct instrumentation of ONE `latest_receipt(root, "core")` call on this snapshot counted exactly 1,028 `_receipt_content_token()` invocations. The call took 0.186 s and peaked at 3.2 MiB under tracemalloc. The direct locator therefore does not provide constant I/O.
- The append path has the same scaling defect: `_update_receipt_index()` lines 727-738 calls `_receipt_member_inventory(root)` whenever a prior lineage proof exists; `_receipt_member_inventory()` lines 627-645 opens/hashes every receipt and sorts the complete member list.
- After that O(N) scan, `_update_receipt_index()` rewrites a lineage JSON document containing the entire members list at lines 822-837. Repeating that on every append gives O(N^2) cumulative lifetime work and ever-growing metadata writes.
- The existing performance regression is a false oracle. `tools/perf_wave_regressions.py` lines 950-973 counts only calls to `_iter_receipts` and `_find_receipt_by_id`, then reports `PERF-004 index lookup is constant-I/O (no full scan)` when both counters remain zero. It does not instrument `_receipt_inventory`, `_receipt_content_inventory`, `_receipt_member_inventory`, `_receipt_content_token`, `os.scandir`, or receipt opens.
- Consequently the current performance suite prints PASS for the constant-I/O claim while the same call demonstrably hashes all 1,028 receipts.
ISSUE:
The conformance index optimizes only the final record selection, not the actual I/O. Every indexed read performs a complete sibling metadata scan plus a complete sibling content-hash scan, and every indexed append re-hashes the full historical membership and rewrites the growing membership list. The implemented complexity contradicts the documented contract and its regression test is incapable of detecting the real work.
OPTIMIZE:
- Store immutable receipts in bounded sealed segments instead of one independently validated file per lifetime event.
- Maintain a small append-active segment plus immutable segment hashes and a compact top-level lineage root. Sealing a segment should cost O(segment_size); appending inside the active segment must not re-hash all historical segments.
- Keep the per-gate latest locator as a direct locator into a receipt/segment, but validate it against the current compact lineage generation instead of hashing every historical receipt on each lookup.
- A strict deep conformance-history verifier may scan every historical segment when explicitly requested; ordinary `latest_receipt(gate)` must not secretly perform that deep audit.
- Remove the lifetime `members` rewrite from every append. If explicit membership proof is needed, maintain a segmented/Merkle-style root whose update is O(1) or O(log segments), not O(receipts).
- Repair the PERF-004 regression so its I/O counter observes all receipt enumeration/hash paths, not only the two helpers the implementation happens not to call.
GUARDRAIL:
- The latest receipt must still be selected by canonical validated completion order.
- Missing/corrupt locator, malformed receipt, unsafe path topology or inconsistent lineage must fail closed or fall back to an explicitly correct strict path.
- An unindexed newer receipt must never be silently hidden as current PASS.
- Modification/replacement of historical conformance evidence must remain detectable at the appropriate authority boundary.
- PASS receipt source/checkpoint bindings and content-hash semantics must remain unchanged.
VERIFY:
- Replace the existing PERF-004 oracle with instrumentation of receipt opens/content hashes/scandir operations.
- On fixtures containing 100, 1,000 and 10,000 historical receipts, `latest_receipt(root, "core")` must touch a bounded number of receipt/segment authorities rather than N individual receipt files.
- Append one new receipt to 1,000- and 10,000-receipt histories; the number of historical content hashes must remain bounded rather than growing linearly with N.
- Assert the current implementation fails that new regression by observing N `_receipt_content_token` calls.
- Corrupt the locator, active segment, old sealed segment and lineage root independently; each must either fail closed or take the documented strict fallback without returning false PASS.
- Add an unindexed newer receipt and verify it cannot be hidden by the fast path.
- Existing conformance ordering, future-time, source-identity, malformed-receipt and hostile-path controls must remain green.

[P2] [PERF-003] STRONGLY EVIDENCED WASTE tools/run_scenarios.py::run_producer_gate_probes / third-wave performance regression execution
EVIDENCE:
- `run_producer_gate_probes()` explicitly states at lines 7357-7365 that the defect under test is the mapping from GATE to producer-severity, not parsing itself.
- Despite that narrow subject, its local `validate()` helper lines 7377-7384 launches the complete `tools/validate.py` executable in a fresh Python subprocess.
- Static and runtime instrumentation show 13 full validator subprocesses are launched to produce 14 producer-gate assertions.
- A direct isolated execution completed all 14 assertions successfully but took 10.882 s; the instrumentation counted exactly 13 `validate.py` subprocesses.
- The complete `python -u tools/perf_wave_regressions.py` run took 34.29 s with max RSS reported as 135,920 KiB and finished 39/41. The producer-gate probe alone therefore consumes roughly one-third of this performance-wave wall time while testing primarily a small severity matrix.
- Multiple calls intentionally evaluate the same fixture under default, ship, collect:saiwiki, collect:saitranslate and converge gates; each process pays Python startup, module import, full validator initialization and unrelated project checks again.
ISSUE:
A narrow policy regression is implemented mostly as repeated whole-program validation. This inflates performance-suite turnaround, obscures which layer actually failed, and makes a performance/stability suite itself expensive enough to become a recurring audit cost. Full CLI wiring deserves coverage, but thirteen end-to-end validator launches are not required to prove a gate-to-severity truth table.
OPTIMIZE:
- Move or expose the producer gate/severity decision as one side-effect-free policy helper with a closed gate/producer input contract.
- Test the complete gate x producer x finding matrix directly in-process against that helper.
- Retain a small end-to-end set proving that `validate.py` actually wires the policy correctly: ordinary/default soft behavior, one producer-specific collect hard case, converge hard case, and unknown/misspelled gate refusal are sufficient integration sentinels.
- Reuse one prepared fixture where independence does not require reconstruction; keep subprocess tests only where process/CLI behavior itself is the subject.
- Preserve hostile malformed/stale OUTBOX integration checks rather than converting the entire suite into mocks.
GUARDRAIL:
- Do not weaken coverage of unknown/misspelled gates, malformed OUTBOX parsing, stale producer identity or producer-specific collect routing.
- The pure policy test and CLI integration test must share the same production helper; do not duplicate the truth table into test-only code.
- Tests that depend on isolated filesystem/Git state must remain isolated.
- The optimization must not hide validator initialization regressions: retain at least one normal validator subprocess smoke path in the performance campaign.
VERIFY:
- Instrument subprocess execution in `run_producer_gate_probes`; reduce validator launches from 13 to a small documented integration budget while preserving all logical matrix assertions.
- The direct producer-gate group must retain 14/14 semantic assertions or equivalent expanded matrix coverage.
- Compare pre/post wall time on the same fixture; the group should no longer consume ~10.9 s predominantly in validator subprocess repetition.
- Run the full performance wave and confirm the producer-gate optimization does not alter expected PASS/FAIL text or gate exit semantics.
- Inject one wrong severity mapping and prove the in-process policy matrix fails immediately.
- Inject one CLI wiring error while leaving the policy helper correct and prove the retained end-to-end sentinel still fails.

PERFORMANCE_DONE_WHEN: PERF-001..PERF-003 are addressed without weakening receipt/conformance integrity: operation-specific and SubSaipen reads no longer materialize lifetime settled history; stale settled projections recover rather than remaining permanently slow; conformance latest lookup and append no longer hash every historical receipt; performance tests measure actual file/hash work instead of helper names; producer gate policy coverage no longer requires 13 whole-validator launches; scale controls at 100/1,000/5,000+ receipts demonstrate bounded or segmented growth; hostile tamper, lineage, duplicate-id, crash-recovery and fail-closed invariants remain green; and the complete performance suite passes on a full checkout.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.
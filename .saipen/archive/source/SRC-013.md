# SAIPEN SEMANTIC COMPRESSION — WAVE 5

## PHASE DELTA COMPRESSION + EXECUTION-POLICY READINESS

ROLE

Continue the semantic-compression campaign after Wave 4.

Wave 4 targets:

* BOOT reduction;
* INDEX correction;
* honest load budgets;
* CONFORMANCE machine-corpus cutover;
* reproducible green closure.

Wave 5 targets the next meaningful semantic surface:

`saipen/phases/*.md`

Current phase-doc surface is approximately:

~109 KB total.

Do NOT merge phases.

Do NOT redesign the state machine.

Do NOT change phase names.

Do NOT change legal transition semantics unless a separately proven defect requires it.

The objective is:

> Make every phase document contain only the semantic delta unique to that phase.

---

# PRIMARY DESIGN RULE

A phase document should answer:

> What is different while SAIPEN is in THIS phase?

It should NOT re-explain:

* global state semantics;
* checkpoint mechanics;
* root binding;
* source authority;
* destructive confirmation;
* generic Work ownership;
* generic Attempt ownership;
* universal failure handling;
* command routing;
* HUSH;
* STYLE;
* general recovery;
* general next_action grammar.

Those facts already have owners.

Reference them.

Do not clone them.

---

# W5.1 — BUILD A PHASE DUPLICATION MAP FIRST

Before editing phase prose, inventory repeated semantic families across:

`scout.md`
`build.md`
`verify.md`
`review.md`
`ship.md`
`maintain.md`
`audit.md`
`fix.md`
`plan.md`
`add.md`
`hunt.md`
`markhunt.md`
`clean.md`
`translate.md`
and the remaining phase documents.

Classify repeated content into:

A. universal Core invariant;

B. mechanical OPS behavior;

C. phase-specific rule;

D. source/runtime/style/execution-policy rule;

E. historical rationale/example.

Do not delete any repeated paragraph until its canonical owner is confirmed.

---

# W5.2 — STANDARDIZE PHASE DOCUMENT SHAPE

Move all phase documents toward one compact schema:

`Purpose`

`Entry`

`Required reads`

`Procedure`

`Exit`

`Failure / blocked behavior`

`Rule references`

Optional:

`Phase-specific evidence`

Avoid custom prose architectures per phase.

A cold agent should be able to scan any phase document using the same visual grammar.

---

# W5.3 — DEFINE A UNIVERSAL PHASE CONTRACT

Create one compact global phase contract in CORE or an existing appropriate owner.

It should cover only universal semantics such as:

* exactly one canonical phase is active;
* phase legality comes from REGISTRY DFA;
* phase-specific rules come from the active phase document;
* phase transitions must be checkpointed;
* phase completion does not imply Work completion unless the lifecycle says so;
* required safety/recovery rules outrank phase procedure.

Then remove those explanations from individual phases.

Do not create a second giant phase framework.

The global contract should remain small.

---

# W5.4 — START WITH THE LARGEST PHASES

Current highest-value targets are approximately:

`SHIP` ~17 KB

`TRANSLATE` ~13.7 KB

`MARKHUNT` ~10.9 KB

`CLEAN` ~10.7 KB

`VERIFY` ~10.2 KB

Do these first.

The reason is simple:

largest semantic duplication
→ largest proof of compression.

Do not start with a 2 KB phase just because it is emotionally less frightening.

---

# W5.5 — SHIP DELTA COMPRESSION

SHIP is likely the most valuable phase target.

Keep SHIP-specific semantics only:

* release eligibility;
* first-publish behavior;
* existing-origin behavior;
* version/release evidence requirements;
* publish ordering;
* rollback/refusal points unique to SHIP;
* terminal transition semantics.

Remove/refer out:

* generic destructive confirmation;
* generic checkpoint contract;
* generic source capture;
* generic command behavior;
* global retry semantics;
* broad recovery explanation;
* generic filesystem transaction guarantees.

Expected target:

`ship.md <= 7–9 KB`

without changing release behavior.

---

# W5.6 — TRANSLATE DELTA COMPRESSION

Keep only translation-specific behavior:

* translation target/source determination;
* preservation requirements;
* validation specific to translated artifacts;
* language/source consistency;
* phase exit conditions.

Do not restate:

* source authority;
* general file writes;
* generic VERIFY;
* generic Work evidence;
* runtime model identity;
* STYLE policy unless translation semantics explicitly require it.

Expected target:

`translate.md <= 6–8 KB`.

---

# W5.7 — VERIFY DELTA COMPRESSION

VERIFY is safety-sensitive.

Compress conservatively.

Keep explicitly:

* verification objective;
* verification attempt semantics;
* retry/cap semantics unique to VERIFY;
* evidence acceptance;
* failure transition;
* manual verification handling;
* distinction between unavailable verification and failed verification.

Do NOT compress away:

* attempt limits;
* retry termination;
* false-green prevention;
* required evidence.

Move only globally duplicated mechanics.

Expected target:

`verify.md <= 6–7 KB`.

---

# W5.8 — REVIEW DELTA COMPRESSION

Keep:

* review entry conditions;
* what review evaluates that VERIFY does not;
* review acceptance/refusal;
* review retry/cap behavior;
* transition decisions.

Remove generic test/checkpoint/source explanations.

VERIFY and REVIEW must remain semantically distinct.

Do NOT combine them because they appear similar.

---

# W5.9 — MARKHUNT / HUNT / CLEAN OWNERSHIP CLEANUP

These maintenance-oriented phases are especially vulnerable to duplicated lifecycle prose.

Clarify boundaries:

`HUNT`
discovers/evaluates candidates.

`MARKHUNT`
records/commits accepted hunt findings and brakes.

`CLEAN`
performs cleanup/pruning according to already-owned policy.

`MAINTENANCE.md`
owns the autonomous maintenance lifecycle around them.

Individual phases should not each restate the entire maintenance system.

This should yield a substantial reduction.

---

# W5.10 — IMPROVE MUST NOT LEAK INTO EVERY PHASE

The new:

`continue -> improve`

behavior must remain owned by COMMANDS/IMPROVE/MAINTENANCE routing.

Do not add "if nothing remains, run Improve" paragraphs into every phase.

Phase exit returns to canonical routing.

Canonical routing decides whether Improve becomes the next action.

One owner.

---

# W5.11 — RULE IDS INSTEAD OF RESTATED PARAGRAPHS

Where a phase depends on a global rule:

reference it.

Example:

Instead of:

> Before destructive effects the agent must ask...

use:

`Destructive effects remain governed by OPS-DESTRUCTIVE-001.`

Instead of repeating checkpoint order:

`Checkpoint per STATE-CHECKPOINT-001.`

Instead of repeating source capture:

`Source authority: SOURCE-RECEIPT-001.`

References must remain understandable.

Do not create unreadable documents consisting only of cryptic IDs.

One short local explanation plus stable Rule ID is ideal.

---

# W5.12 — MOVE PHASE HISTORY OUT OF THE HOT PATH

Historical regression narratives are valuable.

They should not sit inside routinely loaded phase prose.

Move durable lessons into one of:

* test case metadata;
* KNOWLEDGE;
* ADR;
* incident notes;
* CHANGELOG.

Keep only the surviving invariant in the phase contract.

Example:

Historical incident:
agent published before verification.

Hot-path phase rule:

`SHIP MUST NOT publish before required verification/review gates are satisfied.`

The novel about the incident belongs elsewhere.

---

# W5.13 — PHASE-SPECIFIC SEMANTIC GOLDENS

Before compressing each high-risk phase, add or confirm structured golden behavior.

At minimum:

SHIP:

* first publish;
* existing origin;
* publish refusal;
* failed push;
* post-publish evidence.

VERIFY:

* pass;
* fail;
* retry;
* cap reached;
* unavailable verifier;
* manual verification.

REVIEW:

* accept;
* reject;
* retry/cap.

CLEAN:

* valid prune;
* protected artifact refusal.

MARKHUNT:

* accepted finding;
* brake behavior;
* no-finding path.

TRANSLATE:

* valid translation;
* source ambiguity;
* validation failure.

Do not use prose snapshots as the main proof.

Compare structured semantics.

---

# W5.14 — ADD PHASE SIZE / LOAD BUDGETS

Extend `protocol_budget.py`.

Track:

* bytes per phase;
* median phase size;
* maximum phase size;
* total phase corpus;
* active phase load profile.

Targets:

ordinary phase:
<= 5 KB preferred.

complex phase:
<= 8–10 KB.

total phase corpus:
<= 60–70 KB.

active-phase runtime profile:
must remain within the previously ratified ordinary-phase context budget.

Warn first.

Fail CI once the migration baseline is stable.

---

# W5.15 — NO PHASE COLLAPSE

Explicitly preserve the 16-phase architecture.

Do NOT introduce:

`EXECUTE`
to replace BUILD/FIX/CLEAN/etc.

Do NOT merge:

VERIFY + REVIEW

HUNT + MARKHUNT

BUILD + FIX

PLAN + SCOUT

The current phase separation supports:

* lazy loading;
* deterministic state;
* recovery;
* auditability.

Compression should strengthen those benefits.

---

# W5.16 — PREPARE HUSH ROUTING DURING PHASE CLEANUP

Do not fully implement HUSH yet.

But make sure phase documents do not assume narration.

Forbidden dependencies include semantics such as:

> announce entry into VERIFY

> tell the user that REVIEW begins

> explain each tool operation

Phase behavior must work with zero discretionary narration.

That makes the protocol naturally compatible with HUSH.

Required model:

phase semantics
≠
phase narration.

---

# W5.17 — DEFINE EXECUTION EVENTS VS USER OUTPUT

While compressing phases, distinguish:

A. semantic event

from:

B. user-visible narration.

Examples:

`VERIFY started`
may need semantic persistence/evidence.

It does NOT require assistant prose:

"Now entering VERIFY."

`SHIP failed`
may require LOG/state evidence.

It does NOT automatically require a mid-task explanation under HUSH.

This separation is essential for future silent execution.

---

# W5.18 — HUSH READINESS TEST

Add one protocol-level readiness test before implementing HUSH.

Run representative phase transitions with assistant narration removed from the expected path.

Prove that:

* phase transitions still happen;
* checkpoints still happen;
* LOG evidence still exists;
* failures remain detectable;
* required operator interaction still occurs;
* no semantic rule depends on narration text.

This is not a HUSH implementation test.

Call it something truthful such as:

`test_phase_lifecycle_does_not_depend_on_narration`.

---

# W5.19 — KEEP MANDATORY USER INTERACTION EXPLICIT

Silent-compatible does not mean interaction-free.

Phase docs must still clearly identify cases requiring operator input:

* destructive authorization;
* unresolved ambiguity;
* external credential/input requirement;
* irrecoverable BLOCKED state;
* explicit manual verification requirement.

These are semantic interactions.

HUSH will not suppress them.

Ensure compression does not hide those boundaries behind generic "handle errors appropriately" language.

---

# W5.20 — COMPRESS MAINTENANCE CROSS-REFERENCES AFTER PHASES

Once HUNT/MARKHUNT/CLEAN ownership is clear, return to MAINTENANCE and remove duplicated phase procedures.

MAINTENANCE should own:

* when maintenance starts;
* candidate ordering;
* autonomous lifecycle;
* safety valves;
* continuation conditions.

Phase docs own:

* how their individual phase executes.

This two-way cleanup may reduce MAINTENANCE further without touching its autonomy semantics.

---

# W5.21 — DO NOT RE-EXPAND CORE

During phase compression, resist the temptation to move every shared sentence back into CORE.

CORE is now ~23 KB.

Keep it there.

If a rule is shared by several phases ask:

1. is it truly global?
2. does it already belong to OPS/SOURCES/RUNTIME/MAINTENANCE?
3. can a Rule ID reference solve it?

Only genuinely global invariants belong in CORE.

No new 10 KB "Shared Phase Rules" appendix.

---

# W5.22 — CROSS-REFERENCE INTEGRITY

After phase compression run a reference audit.

Detect:

* stale section-number references;
* Rule IDs with no owner;
* phase docs referencing deleted paragraphs;
* CORE references to removed phase procedures;
* INDEX routing to obsolete ownership;
* conformance cases tied only to removed wording.

Prefer Rule IDs over fragile heading/section-number references where possible.

---

# W5.23 — PHASE LOAD SIMULATION

Do not measure only file sizes.

Simulate realistic loading.

Examples:

BUILD task:
BOOT/STYLE/INDEX

* CORE
* BUILD
* COMMANDS only when relevant
* source/runtime docs only when activated.

VERIFY:
CORE

* VERIFY
* relevant runtime/ops contracts.

SHIP:
CORE

* SHIP
* OPS
* only required release/source contracts.

Report actual context bytes.

The objective is not:

"all phase docs are small."

The objective is:

"one phase execution loads a small, predictable law surface."

---

# W5.24 — TARGET OUTCOME

Expected after Wave 4:

human normative Markdown:
~285–300 KB.

Wave 5 target:

phase corpus:
~109 KB -> ~60–70 KB.

Human normative Markdown after W5:

approximately:
~235–260 KB.

More important:

ordinary execution profile:
~20–40 KB depending on phase.

This reaches the original semantic-compression objective without removing any state-machine capability.

---

# W5 ACCEPTANCE BAR

Wave 5 completes only when:

1. all 16 phases remain;
2. legal DFA is unchanged unless separately ratified;
3. each phase has one compact, consistent structure;
4. phase-specific semantics remain owned by phase docs;
5. universal rules are not duplicated;
6. SHIP/VERIFY/REVIEW behavior is regression-proven;
7. maintenance-oriented phase boundaries are explicit;
8. phase corpus <= ~70 KB or a justified variance is recorded;
9. no ordinary phase exceeds ~10 KB without justification;
10. phase lifecycle does not depend on user-visible narration;
11. mandatory operator interaction remains explicit;
12. CORE does not materially regrow;
13. MAINTENANCE loses duplicated phase procedure;
14. stale cross-references are removed;
15. active-phase load profiles are measured;
16. validator/scenarios/goldens remain green;
17. a clean checkout reproduces the same result.

---

# NEXT WAVE — REAL HUSH RUNTIME

After phase semantics are narration-independent, implement the actual execution modifier.

Target:

`hush <task>`

or:

`/hush <task>`

Execution model:

parse HUSH modifier
→ activate task-local execution policy
→ strip modifier
→ route normal task through SAIPEN
→ preserve all semantic state/evidence
→ suppress discretionary assistant narration
→ permit mandatory interaction
→ produce bounded final report
→ eject HUSH policy.

Do NOT implement HUSH by hiding text after it was generated.

Suppress narration generation at the execution-policy boundary when the harness permits it.

---

# W5 PRINCIPLE

Wave 4 makes SAIPEN load less protocol.

Wave 5 makes each loaded phase contain less duplication.

The next HUSH wave will make the agent produce less execution noise.

Together:

small startup
+
small active law surface
+
silent execution
================

the intended SAIPEN operating model.

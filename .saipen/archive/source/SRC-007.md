# SAIPEN — Audit Handoff

RUN_ID: acb-mteufe43-90e94e5caf7148c7a6b6
GENERATED_AT: 2026-08-30T00:12:33.040973
PROJECT_NAME: SAIPEN
TARGET: SAIPEN_29.08.26-T23-37-05.zip -> fully unpacked SAIPEN v7.231.9 project tree
BASELINE: SAIPEN_29.08.26-T23-37-05.zip sha256:6966de14b6a2b465516e31f196a9f41827d037af2d3417980cfc29db44b861c6; live working tree based on main@c7ea5b1bb5f8e953c07140cda4f636a382c08310
GIT_CONTEXT: PRESENT main@c7ea5b1bb5f8e953c07140cda4f636a382c08310 - supplied archive contains extensive modified/deleted/untracked working-tree state; live files, not HEAD, were audited as current behavior
SAIPEN_CONTEXT: PRESENT - active BUILD/T-1181; BOARD records T-1182/T-1183 matching independently reproduced intake-transaction and dry-run defects; historical claims were treated only as context, not evidence
TOTAL_TICKETS: 14
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 4 tickets (baseline: SAIPEN_29.08.26-T23-37-05.zip sha256:6966de14b6a2b465516e31f196a9f41827d037af2d3417980cfc29db44b861c6; live working tree based on main@c7ea5b1bb5f8e953c07140cda4f636a382c08310)
- Wave 2 (Second Wave): 4 tickets (baseline: SAIPEN_29.08.26-T23-37-05.zip sha256:6966de14b6a2b465516e31f196a9f41827d037af2d3417980cfc29db44b861c6; live working tree based on main@c7ea5b1bb5f8e953c07140cda4f636a382c08310)
- Wave 3 (Performance): 6 tickets (baseline: SAIPEN_29.08.26-T23-37-05.zip sha256:6966de14b6a2b465516e31f196a9f41827d037af2d3417980cfc29db44b861c6; live working tree based on main@c7ea5b1bb5f8e953c07140cda4f636a382c08310)

---
## 01 — AUDIT CORE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-29T23:38:00+03:00
WAVE: AUDIT CORE
TARGET: SAIPEN_29.08.26-T23-37-05.zip -> fully unpacked SAIPEN v7.231.9 project tree
BASELINE: SAIPEN_29.08.26-T23-37-05.zip sha256:6966de14b6a2b465516e31f196a9f41827d037af2d3417980cfc29db44b861c6; live working tree based on main@c7ea5b1bb5f8e953c07140cda4f636a382c08310
GIT_CONTEXT: PRESENT main@c7ea5b1bb5f8e953c07140cda4f636a382c08310 - supplied archive contains extensive modified/deleted/untracked working-tree state; live files, not HEAD, were audited as current behavior
SAIPEN_CONTEXT: PRESENT - active BUILD/T-1181; BOARD records T-1182/T-1183 matching independently reproduced intake-transaction and dry-run defects; historical claims were treated only as context, not evidence
AUDIT_SCOPE: CLI routing and dry-run behavior; recover/continue reconciliation and recovery classification; source receipt Contract/coverage/revision mutation; journal recovery preflight; protocol-state reconciliation; source/release/archive CORE regressions; validator/test harness. Read-only auditor mandate: :contentReference[oaicite:0]{index=0}
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: Full `python -B tools/test_runner.py --family unit --json` did not complete before the audit shell's 45-second execution cap; ruff could not start because `/opt/pyvenv/bin/python: No module named ruff`
VERIFIED_INSTEAD: `SourceReceiptTests` 35/35 PASS; six focused existing CORE classes 27/27 PASS; `Core002ReconciliationTests` runs 8 PASS + 4 ERROR from a test-module NameError, and all 12/12 PASS when only the missing `re` global is injected at runtime; `python -B -m compileall -q tools tests` PASS; `python -B tools/validate.py --gate core` completed and FAILED with 5 project-state problems/13 warnings; focused black-box/fault-injection reproductions confirmed CORE-001..003 below
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT

[P1] [CORE-001] tools/saipen_engine/intake.py::add_requirement / Contract+revision+coverage persistence
EVIDENCE: `add_requirement` validates only receipt-body integrity at lines 1009-1011, mutates the coverage ledger in memory, reads the current Contract, increments `interpretation_revision`, then performs three independent writes at lines 1045-1047: `_write_contract`, `_write_contract_revision`, `_write_coverage`. Those helpers each perform their own `_atomic_write`; no OperationPlan/journal spans the three targets. `_contract_integrity` already exists at lines 621-647 and detects Contract/coverage identity drift, but `add_requirement` does not call it. Fault injection raising `OSError("injected-after-contract")` from `_write_contract_revision` returned `VALIDATION_FAILED` after the live Contract had already advanced to revision 1 and gained `SRC-001:R001` while coverage remained empty; `verify_integrity` still returned `SOURCE_INTEGRITY_OK`. Retrying then returned `REQUIREMENT_ADDED`, advanced the current Contract to revision 2, and left only `SRC-001.json` plus `SRC-001.r002.json`, permanently skipping immutable revision r001. Independently, seeding Contract with `SRC-001:R999` while coverage lacked it made `_contract_integrity` return `CONTRACT_DRIFT`; calling `add_requirement(...R001...)` nevertheless returned `REQUIREMENT_ADDED`, and `_contract_integrity` remained red afterward. OPS §2 states every mutation is PLAN/APPLY over one immutable OperationPlan, while SOURCES requires Contract/coverage integrity as a closure/reread invariant.
DEFECT: Source requirement normalization has competing independently durable writes instead of one recoverable transaction and does not refuse an already split Contract/coverage state. A crash can expose a mixed authoritative state, and ordinary retry can silently skip an immutable interpretation revision. An already corrupt source can also receive a successful `REQUIREMENT_ADDED` response without repairing or refusing the corruption.
REPAIR: Before mutation, run the complete receipt + Contract/coverage integrity gate, refusing `CONTRACT_DRIFT` with zero writes. Compute the new current Contract, immutable revision file, and coverage ledger bytes together, bind them into one OperationPlan with their read/CAS dependencies, and APPLY through the existing journal/recovery machinery under the canonical writer lock. Remove the direct three-write success path. Recovery/retry must converge idempotently to either the complete old set or complete new set and must never synthesize a missing revision number by simply incrementing again.
VERIFY: Add crash injection before and after each Contract/current-revision/coverage target application. At every boundary, ordinary recovery must yield either the complete old set or complete new set, never mixed; repeated recovery must be idempotent. Inject failure after current Contract advancement and prove retry preserves a contiguous revision history. Seed Contract/coverage clause drift and prove `source req` returns `CONTRACT_DRIFT`, writes zero bytes, and never reports `REQUIREMENT_ADDED`.

[P1] [CORE-002] tools/saipen.py::_source/_recover/_continue/_improve -- global `--dry-run` semantic planning
EVIDENCE: `_source` lines 1659-1670 returns `{ok:true, code:"SOURCE_DRY_RUN"}` for every mutating source action before parsing or semantically validating its arguments. Black-box reproduction: `python -B tools/saipen.py --project-root <fixture> --dry-run --json source req BAD R001 requirement x` returned rc=0, `ok:true`, `SOURCE_DRY_RUN`; the identical non-dry command returned rc=1, `INVALID_ID`, proving dry-run bypasses the real request semantics. `_recover` lines 1113-1123 returns `DRY_RUN_UNSUPPORTED` when an interrupted operation is pending; `_continue` lines 1499-1511 returns `RECOVERY_REQUIRED` instead of projecting recovery and the post-recovery route; `_improve` lines 2768-2786 rejects submit/complete/cycle-complete/abort before their semantic planning. OPS lines 48-59 require PLAN to validate the semantic request, compute all final bytes and return an OperationPlan with zero writes, explicitly stating "`--dry-run` calls PLAN and renders it. Nothing else." OPS lines 198-202 further require every mutation to support validated zero-write dry-run with affected files/before-after. Existing `test_src27_dry_run_creates_no_intake_or_lock` passes but protects only the zero-write property, not semantic equivalence or a real plan.
DEFECT: The CLI implements dry-run as command-family short-circuiting rather than the same semantic PLAN path used by the real mutation. It can certify invalid input as successful, cannot expose concrete target changes, and cannot project recovery/continuation through proposed state. This makes dry-run observationally different from the operation it claims to preview.
REPAIR: Remove `SOURCE_DRY_RUN` and mutation-level `DRY_RUN_UNSUPPORTED` early exits. Give source/recover/continue/improve mutators one semantic PLAN path that performs normal parsing, validation, integrity/precondition checks and plan construction without acquiring mutation ownership or writing canonical bytes. Render that plan when `dry_run=True`; APPLY the same plan otherwise. Pending recovery dry-run must inspect and validate the recoverable journal and expose its replay plan. `continue --dry-run` must route against the proposed recovered/reconciled state rather than rereading unchanged on-disk state between preview and routing.
VERIFY: For each mutating source/recover/continue/improve action, pair invalid dry-run/non-dry invocations and assert the same refusal class. For valid requests assert concrete planned target paths/structural deltas and byte-identical project trees before/after dry-run. With one PREPARED journal, `recover --dry-run` must describe replay without settling it; `continue --dry-run` must expose the route that would exist after that recovery/reconciliation, including safety-valve decisions derived from proposed goal counters.

[P1] [CORE-003] tools/saipen.py::_recover -- corrupt/conflict recovery precedence
EVIDENCE: `_scan_full` returns `(pending, conflicts, corrupt)` from one manifest snapshot. Bare `_recover` at lines 1066-1083 checks `conflicts` first and immediately emits `code:"CONFLICT"`. It does not examine `_corrupt` until lines 1099-1107. A direct execution of `_recover` with the one-scan result `pending=["op-corrupt"]`, `conflicts=["op-conflict"]`, `corrupt=[{"op_id":"op-corrupt","corrupt":true,...}]` deterministically returned rc=1 with `code:"CONFLICT"` and only `op-conflict`, hiding the corrupt receipt. The shared `journal.recovery_preflight` does the opposite at lines 1266-1293: corrupt evidence is checked first and emits `CORRUPT_JOURNAL`, then unresolved conflicts emit `RECOVERY_CONFLICT`. `_continue` also checks corrupt before conflict at lines 1483-1498. OPS lines 112-116 state undecodable/unsafe evidence is `CORRUPT_JOURNAL`, not CONFLICT, and lines 149-157 explicitly require corrupt evidence to refuse before unresolved conflicts.
DEFECT: Bare `recover` duplicates recovery classification instead of following the shared corrupt-first invariant. When corrupt and conflicting evidence coexist, the user is told only about the conflict and the stronger corruption condition is hidden; the command also emits the generic `CONFLICT` code where the recovery preflight contract uses `RECOVERY_CONFLICT`. Writes remain blocked, but recovery diagnosis and ordering are inconsistent across entry points.
REPAIR: Make bare `_recover` consume one shared recovery-preflight/classification result, or at minimum evaluate `_corrupt` immediately after `_scan_full` and before `conflicts`, returning the shared `_corrupt_refusal`. Align unresolved-conflict classification with the canonical recovery contract instead of maintaining a second bespoke status vocabulary. Preserve the current single manifest traversal and zero-write refusal behavior.
VERIFY: Build one fixture containing a valid unresolved CONFLICT journal plus separate corrupt journal evidence. `recover`, `continue`, status/next recovery projection, and mutation preflight must all surface `CORRUPT_JOURNAL` first with the corrupt op identity and write zero bytes. Remove only the corrupt evidence and prove the same fixture then surfaces `RECOVERY_CONFLICT` for the remaining conflict. Add a regression preventing branch-order reversal.

[P2] [CORE-004] tools/test_audit_2026_08_29_all3_impl.py::Core002ReconciliationTests._append_events
EVIDENCE: The regression module imports `json`, `os`, `subprocess`, `sys`, `unittest`, and `Path` at lines 9-14 but never imports `re`; `_append_events` line 60 calls `re.search`. Running `python -B -m unittest -v tools.test_audit_2026_08_29_all3_impl.Core002ReconciliationTests` executed 12 tests: 8 PASS and 4 ERROR with `NameError: name 're' is not defined` (`test_a_lost_bump_survives_a_sealed_marker`, `test_goal_counters_behind_the_log_are_repaired_upward`, `test_reauthorization_clears_the_rebuild_window`, `test_tripped_valve_counter_is_never_tidied_down`). Without modifying the file, injecting only `module.re = re` and rerunning the exact class produced 12/12 PASS in 0.930s, isolating the failure to the test harness rather than reconciliation implementation.
DEFECT: Four load-bearing CORE-002 goal-counter/recovery regressions do not execute their assertions in the checked-in live test source. The test suite therefore cannot currently prove sealed-marker rebuild, upward counter repair, reauthorization reset, or tripped-valve preservation even though the implementation passes those assertions once the harness defect is bypassed.
REPAIR: Add the missing standard-library `import re` to the regression module. Do not weaken, skip, rewrite, or mark the four tests optional.
VERIFY: Run the class normally, without monkeypatching globals: all 12 tests must execute and pass. Also run `python -B -m compileall -q tools tests`, the focused existing CORE classes, and the canonical unit family; confirm no test is skipped because of the repair.

CORE_DONE_WHEN: CORE-001 commits requirement Contract/current-revision/coverage as one integrity-checked recoverable transaction; CORE-002 makes every audited mutation dry-run execute its real semantic PLAN with zero writes; CORE-003 gives corrupt recovery evidence canonical precedence and classification across entry points; CORE-004 restores execution of all reconciliation regressions; the new hostile/fault regressions, SourceReceiptTests, focused CORE suites and all 12 reconciliation tests pass, the complete unit family finishes without errors in an unrestricted test window, compileall remains clean, and ruff/validator are rerun in an environment providing ruff with any unrelated pre-existing snapshot metadata failures kept explicitly separate from these repairs.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-29T23:38:00+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: second-mteuzuif-29b5800cb9e9
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: second
WAVE_INDEX: 2
WAVE_COUNT: 3
WAVE: AUDIT SECOND WAVE
TARGET: SAIPEN_29.08.26-T23-37-05.zip -> fully unpacked SAIPEN v7.231.9 live project tree
BASELINE: SAIPEN_29.08.26-T23-37-05.zip sha256:6966de14b6a2b465516e31f196a9f41827d037af2d3417980cfc29db44b861c6; live working tree based on main@c7ea5b1bb5f8e953c07140cda4f636a382c08310
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@c7ea5b1bb5f8e953c07140cda4f636a382c08310 - supplied archive contains extensive modified/deleted/untracked state; live files were treated as authoritative
SAIPEN_CONTEXT: PRESENT - BUILD/T-1181 active; BOARD contains T-1184..T-1187 hypotheses corresponding to this wave, but they were treated only as orientation and independently reproduced before acceptance
AUDIT_SCOPE: journal settlement/cleanup lifecycle; cleanup-debt publication and compaction; semantic receipt opening/closing snapshots and crew CAS stability; ops/settled boundary races; source purge crash/retry behavior; source Contract revision continuity and archive settlement; focused lock/ownership regressions and existing second-wave tests
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: NONE
VERIFIED_INSTEAD: tools.test_audit_2026_08_27_w2 ran 15/15 PASS; tools/regression_second_wave_receipt_fixes.py ran 33/33 PASS; SourceReceiptTests.test_src17_purge_keeps_honest_tombstone PASS; tests.test_external_audit_2026_08_21 ran 21 tests with 20 PASS/1 FAIL caused by live implementation behavior in test_perf_003_semantic_snapshot_reads_each_manifest_once (expected operation/progress reads 14/2, observed 15/3); python -B -m compileall -q tools tests PASS; deterministic symlink-race, I/O-failure, receipt-membership-race, purge-failure and revision-gap probes reproduced W2-001..004
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/saipen_engine/journal.py::{safe_op_dir,_settle_journal,_drop_settled_staged,_compact_drop_staged,_compact_migrate_ops,_compaction_queue_preflight,compact_committed,_receipt_namespace_entries,_read_settled_index,_extend_settled_index,_bootstrap_settled_index,_scan_receipt_namespace,semantic_receipt_snapshot,semantic_receipt_digest}; tools/saipen_engine/crew.py::{_capture_operation_receipts,_capture_receipt_digest,crew_snapshot}; tools/saipen_engine/intake.py::{_contract_integrity,_closed_archive_bundle,_settle_archive_commit,_finish_archive_bundle_locked,_archive_closed_locked,close_receipt,purge_receipt,validate_project}; tools/saipen_engine/paths.py ownership-safe create/write/unlink primitives; saipen/OPS.md transaction/recovery contract; saipen/SOURCES.md source lifecycle contract; relevant W2/source/external-audit regressions
COVERAGE_DEFERRED: wave-3 performance/memory scaling beyond defects directly coupled to receipt closing proof; external network/repository mutation paths; Windows-only junction/reparse behavior beyond statically inspected guards; complete unit family was not rerun during this wave
CROSS_WAVE_REFERENCES: CORE-001 previously identified non-transactional source requirement persistence; W2-003 is a distinct destructive source lifecycle transaction defect. CORE-002 dry-run planning remains separate. CORE-003 recovery classification remains separate. CORE-004 test-harness import defect remains separate.
RESIDUAL_UNCERTAINTY: process-death boundaries were reproduced with deterministic fault injection rather than OS-level kill; Linux symlinks reproduced W2-001 directly, while Windows junction-specific variants were not executable here; no evidence found that these uncertainties weaken the four reported root defects

[P0] [W2-001] tools/saipen_engine/journal.py::_drop_settled_staged / cleanup-debt marker ownership
EVIDENCE: `_drop_settled_staged` deletes settled `.staged` files best-effort, sets `debt=True` on failure, then lines 1786-1797 suppress both `OSError` and `InvalidIdError`, creates `.saipen/recovery/settled/.cleanup-needed`, resolves the marker through `safe_op_dir`, and finally publishes it with plain `marker.write_text("")`. `safe_op_dir` lines 370-390 proves containment and rejects an already-present symlink, but returns an ordinary pathname; there is a check-to-use window before `Path.write_text`, which follows a symlink if the final node is swapped after the proof. A deterministic race injected immediately after the real `safe_op_dir` proof replaced `.cleanup-needed/op-race` with a symlink to an outside sentinel. `_drop_settled_staged` then followed it and changed the outside file from `"SENTINEL"` to `""`; the cleanup marker remained a symlink. Independently, injecting staged-unlink failure followed by cleanup-marker `write_text` failure left the `.staged` payload durable but no marker. A subsequent unmodified `compact_committed(root)` returned `{"ok": True, "compacted": [], "skipped": []}` and left the staged payload in place. This is structurally expected: `compact_committed` processes settled cleanup only from validated queue markers at lines 4695+, while `_compact_migrate_ops` scans the bounded `ops` namespace rather than rediscovering unmarked staged debt already resident in `settled`.
DEFECT: The post-COMMITTED cleanup path crosses an ownership boundary with a follow-symlink write after a separate pathname proof, allowing a race to overwrite/truncate an arbitrary writable file outside the project. The same path suppresses failure to durably publish cleanup debt, so an interrupted cleanup can become permanently undiscoverable while future compaction falsely reports clean success. Semantic COMMITTED status need not be revoked for cleanup failure, but ownership safety and truthful durable cleanup state are mandatory.
REPAIR: Replace raw cleanup-marker `Path.write_text` with an ownership-safe no-follow publication primitive such as `safe_atomic_write_bytes` or an equivalent exclusive regular-file primitive under the project root, with an existing regular empty marker treated idempotently. Do not silently collapse marker-publication failure into successful invisible cleanup. Preserve the queue as the fast path, but give explicit maintenance compaction a correctness fallback that can rediscover settled `.staged` leftovers when their marker could not be published, without following unsafe descendants. Return/surface `cleanup_pending` or equivalent post-commit maintenance truth to the caller when staged deletion or debt publication fails; COMMITTED operation semantics may remain successful, but cleanup success must not be claimed.
VERIFY: Inject a symlink swap after the final containment proof and before cleanup-marker publication; the outside sentinel must remain byte-identical and the operation must report a safe cleanup refusal/pending condition. Repeat with a pre-existing regular marker and prove idempotency. Inject staged unlink failure plus marker publication failure, restart the process, run normal maintenance compaction, and prove the leftover staged payload is rediscovered and either removed safely or truthfully remains pending. Unsafe queue/marker nodes must produce CORRUPT_JOURNAL with zero outside writes.

[P1] [W2-002] tools/saipen_engine/journal.py::semantic_receipt_snapshot/semantic_receipt_digest + tools/saipen_engine/crew.py::_capture_receipt_digest
EVIDENCE: `semantic_receipt_snapshot` scans `ops` first at lines 4461-4464, and when that scan reports empty it may accept `_read_settled_index` at lines 4465-4476. `_read_settled_index` authenticates settled inventory/content but does not bind the contemporaneous `ops` membership. A deterministic race injected a valid PREPARED operation immediately after the empty `ops` scan and before `_read_settled_index`; `semantic_receipt_snapshot` returned `errors=()` and `records=[]`, while an immediate `pending_ops(root)` listed `late-snapshot-op`. `semantic_receipt_digest` has the analogous race at lines 4544-4549: it probes `ops` using `next(_receipt_namespace_entries(...), None)` and, if empty at that instant, may return the settled-index digest. Injecting `late-digest-op` while `_read_settled_index` executed produced exactly the old digest `ops-receipt-sha256:0eb03c...`, while an immediate fresh digest was `ops-receipt-sha256:b25748...`. Crew then defeats its own closing-CAS intent more directly: `_capture_receipt_digest(root, snapshot)` lines 501-507 returns `snapshot.digest` unchanged whenever the opening snapshot was clean. Mutating a settled `operation.json` after opening capture produced opening digest `ad6bbe...`, reused "closing" digest `ad6bbe...`, but the actual live closing digest was `afb5a1...`; the change was therefore mathematically impossible for the crew comparison at `stability_before == stability_after` to detect. Existing regression `tests.test_external_audit_2026_08_21.test_perf_003_semantic_snapshot_reads_each_manifest_once` also currently fails: expected 14 operation/2 progress reads, observed 15/3, because `semantic_receipt_digest` consumes the first `ops` entry once for the emptiness probe and then reads it again during the real traversal.
DEFECT: Semantic receipt authority is not captured as one race-detecting generation. Opening capture has an empty-ops/index handoff window, the nominal closing digest has the same handoff window, and crew commonly does not perform a closing capture at all, instead comparing the opening digest with itself. A newly PREPARED, changed, removed, or corrupt receipt can therefore appear during planning without staling the crew snapshot, allowing a decision to proceed using receipt authority that was never simultaneously true.
REPAIR: Make semantic receipt capture bind both namespaces as one generation. A snapshot/index fast path must capture/recheck `ops` membership around settled-index validation so an operation appearing after the initial empty observation invalidates the capture. Implement the closing proof as a fresh live authority sample, not `return snapshot.digest`; crew must compare the opening token to that independently captured closing token. Avoid the current `next(generator)` plus second traversal: capture namespace membership once and read each manifest/progress authority once per opening or closing pass. If membership/content changes while either pass is being assembled, return stale/corrupt evidence rather than a clean digest. Preserve the authenticated settled index as a performance optimization only when its use is also bound to stable live `ops` authority.
VERIFY: Reproduce the empty-ops race by publishing a PREPARED op between ops membership capture and settled-index acceptance; both `semantic_receipt_snapshot` and `semantic_receipt_digest` must refuse/stale or include the op, never return the old clean token. Mutate/add/remove/corrupt a receipt after crew opening capture and prove `crew_snapshot.stable` becomes false. Stable fixtures must retain identical opening/closing digests. Run `test_perf_003_semantic_snapshot_reads_each_manifest_once` and require exactly 14/2 reads for its seven-manifest fixture, with no first-entry double read.

[P1] [W2-003] tools/saipen_engine/intake.py::purge_receipt
EVIDENCE: `purge_receipt` lines 1947-1968 takes the writer lock but performs the destructive operation directly: it sequentially unlinks archived body, metadata, coverage, Contract and all discovered revision files, then only afterward sets `tomb["purged"]`, writes the tombstone and writes the intake index. There is no OperationPlan or recovery journal before the first irreversible delete despite OPS requiring every mutation to PLAN/APPLY through durable progress evidence. Fault injection on the second `safe_unlink_owned` call produced `{"ok": False, "code": "VALIDATION_FAILED", "detail": "injected purge failure after first delete"}` after the archived body was already deleted while archive metadata remained. Both persisted tombstone and index still had no `purged` flag. Public projections then contradicted each other: `status(root,rid)` returned `SOURCE_CORRUPTION` because the supposedly non-purged archive bundle had lost its body, while `read_body(root,rid)` returned `SOURCE_PURGED` merely from the missing body. No pending journal existed from which ordinary recovery could determine that this was an authorized interrupted purge rather than corruption.
DEFECT: Confirmed purge is a multi-file destructive mutation with no durable transaction state. Any crash or I/O failure after the first delete can leave an unrecoverable half-purged receipt: forensic bytes are already gone, canonical tombstone/index still claim non-purged closure, and readers disagree about lifecycle state. A retry may continue deleting, but there is no durable proof of the intended delete set or crash frontier, so recovery cannot distinguish authorized partial purge from unrelated archive damage.
REPAIR: PLAN the exact confirmed purge before deleting anything. Bind the proven closed archive/tombstone/index state and exact expected revision set into one immutable journal operation whose ordered targets contain every `delete_file` plus the final purged tombstone and intake-index bytes. APPLY it under the existing writer lock through generic journal progress/recovery. The PREPARED receipt must be durable before the first archive deletion. Ordinary recovery should roll forward only when each surviving/deleted target matches the planned before/after state; unexpected changes become CONFLICT. Public `status`/`read_body` should classify a journaled interrupted purge consistently as recovery pending rather than guessing `SOURCE_PURGED` from body absence alone.
VERIFY: For a confirmed purge containing body, metadata, coverage, Contract and multiple revision files, crash/fail before and after every delete and before/after tombstone/index publication. After each boundary, restart and run ordinary recovery; it must converge to one complete purged state with tombstone/index agreeing, or preserve a conflict without further deletion. Before recovery, status/read paths must surface truthful pending/conflict state rather than contradictory PURGED/CORRUPTION results. Repeating purge/recovery after completion must be idempotent and never allocate a second equivalent operation.

[P2] [W2-004] tools/saipen_engine/intake.py::_contract_integrity / validate_project / _closed_archive_bundle / _archive_closed_locked
EVIDENCE: Contract interpretation is explicitly revisioned, but active validation only parses the current Contract and, at lines 2324-2333, checks that the single file matching the current `interpretation_revision` exists and is not a link. It does not require r001..r(current) to be contiguous or authenticate historical revision content. `_contract_integrity` lines 621-647 validates only current Contract versus coverage. `_closed_archive_bundle` lines 1632-1642 likewise proves current archived Contract/coverage but does not validate archived revision history. Closure/archive code lines 1794-1801 simply globs whatever `SRC-NNN.r*.json` files happen to exist and moves that subset. Reproduction created r001 and r002, deleted r001 while leaving the current Contract at revision 2 and r002 intact, then ran `validate_project`: no problem mentioning missing r001 was produced. `close_receipt` subsequently returned successful `SOURCE_CLOSED`; archive contained `SRC-001.r002.json` and no r001. The system therefore sealed an incomplete interpretation history as valid closed authority.
DEFECT: `interpretation_revision` is treated as a latest-file existence hint rather than a chain invariant. Intermediate revision deletion/tampering is invisible to integrity, closure and archive settlement as long as the current Contract/coverage remain valid. Closure can permanently institutionalize an incomplete historical Contract chain, defeating the stated revisioned provenance and making later forensic reconstruction depend on whichever revision files happened to survive.
REPAIR: Centralize one Contract revision-chain validator used by active integrity, project validation, source closure and archived-bundle verification. For current revision N, require exactly the owned regular contiguous set r001..rNNN, parse each revision safely, require matching receipt/source digest/schema/revision number, and require rNNN to equal the current Contract representation expected for that revision. Reject missing, linked/reparse, malformed, duplicate/out-of-range or tampered revisions before any close/archive mutation. Archive settlement must move and then verify exactly this expected revision set rather than an unconstrained glob of survivors.
VERIFY: With current revision >=3, independently delete r001, an intermediate revision and the current revision; symlink each position; alter historical revision identity/source digest/revision number/content. `validate_project`, `close_receipt` and archive settlement must all refuse with zero settlement. Positive multi-revision closure must archive exactly r001..rNNN and archived validation must prove the same contiguous chain. Retry of an interrupted valid close must remain idempotent and must not silently drop a revision.

SECOND_WAVE_DONE_WHEN: W2-001 publishes cleanup debt only through no-follow ownership-safe writes and no staged cleanup can become permanently undiscoverable; W2-002 opening and closing semantic-receipt tokens each represent a live race-detecting generation and crew performs a real closing capture with the 14/2 regression green; W2-003 confirmed purge is fully journaled before its first delete and every injected crash converges through ordinary recovery without contradictory source state; W2-004 enforces and archives the complete authenticated Contract revision chain; all focused hostile/fault regressions, the 15-test W2 ownership suite, 33/33 receipt regressions, source happy-path purge test and external audit suite pass with no new failures.

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: SAIPEN
DATE_TIME: 2026-08-30T00:02:52+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: performance-mtev9igu-34d8f4683f32
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: performance
WAVE_INDEX: 3
WAVE_COUNT: 3
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: SAIPEN_29.08.26-T23-37-05.zip -> fully unpacked SAIPEN v7.231.9 live project tree
BASELINE: SAIPEN_29.08.26-T23-37-05.zip sha256:6966de14b6a2b465516e31f196a9f41827d037af2d3417980cfc29db44b861c6; live working tree based on main@c7ea5b1bb5f8e953c07140cda4f636a382c08310
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@c7ea5b1bb5f8e953c07140cda4f636a382c08310 - archive contains extensive modified/deleted/untracked working-tree state; live implementation files were authoritative
SAIPEN_CONTEXT: PRESENT - BOARD already contains T-1188..T-1193 performance hypotheses corresponding to this wave; each reported defect below was independently inspected and instrumented rather than accepted from BOARD metadata
AUDIT_SCOPE: complete-history capture and mutation precondition generation; producer READY scanning/decoding and targeted selection; fast checkpoint validation; source-intake validation scaling; read-only ProjectSnapshot retention used by status/next/explain; release source-authority inventory construction; focused performance/stability regressions
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: `python -B tools/perf_wave_regressions.py` exceeded the 45-second execution cap before producing a result; this timeout was not classified as a project failure
VERIFIED_INSTEAD: tools.test_audit_2026_08_28_all3.PerformanceAuditTests 13/13 PASS; six focused tests from tests.test_external_audit_2026_08_21 ran with 5 PASS/1 FAIL, the existing W2 semantic-receipt read-budget regression failing at observed operation/progress reads 15/3 versus expected 14/2; `python -B -m compileall -q tools tests` PASS; direct instrumentation reproduced sealed-history double reads and mixed-generation tokens, READY memory growth, two full fast-validator history captures, BOARD/coverage source-validation amplification, ProjectSnapshot retained-memory growth, and three source-authority inventories per full release PLAN
STATUS: PERFORMANCE: COMPLETE
TICKETS: 6
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/saipen_engine/log.py::{read_history_snapshot,read_history_snapshot_and_logs_digest,snapshot_contract_errors,history_contract_errors}; tools/saipen_engine/operations.py checkpoint/history PLAN loading; tools/saipen_engine/producer.py::{ProducerPackage.from_dict,StagingGeneration.scan_ready,ready_package}; tools/saipen_engine/crew.py::_producer_ready_health; tools/saipen_engine/intent.py::{_integrate_producer,collect_and_ship_producer}; tools/saipen_engine/fast_check.py::{validate_project,validate_texts}; tools/saipen_engine/intake.py::validate_project and coverage helpers; tools/saipen_engine/snapshot.py::ProjectSnapshot.capture; tools/saipen.py::{_status,_next_action,_explain_next}; tools/saipen_engine/release.py::{plan_release,_plan_crew_release,_metadata_paths,_source_authority_manifest}; tools/saipen_engine/release_contract.py::{source_authority_paths,release_metadata_paths}
COVERAGE_DEFERRED: complete unit family; full completion of tools/perf_wave_regressions.py beyond the execution cap; OS-level RSS measurement beyond Python allocation tracing; Windows-specific filesystem timing; remote release I/O was not contacted and release planning was exercised with local deterministic seams only
CROSS_WAVE_REFERENCES: W2-002 already owns semantic-receipt membership/closing-snapshot instability and the currently red external 15/3 read-count regression; it is not duplicated as a performance ticket here. PERF-001 below concerns the separate LOG-history/read-precondition generation. CORE-001/W2-003 source transaction correctness remains separate from source-validator read amplification in PERF-004.
RESIDUAL_UNCERTAINTY: tracemalloc measurements describe Python allocation peak rather than complete process RSS, but the retained decoded READY payload bytes and history strings were directly counted and scale linearly with accumulated input; release PLAN call-count reproduction used patched remote/classification seams to isolate local inventory behavior while retaining the real source-authority enumerator

[P1] [PERF-001] PROVEN BOTTLENECK tools/saipen_engine/log.py::read_history_snapshot_and_logs_digest
EVIDENCE: The function first reads every validated sealed LOG segment while constructing HistorySnapshot, then calls `hash_tree_dependency(logs_dir, read_file=_read_sealed)`, where `_read_sealed` executes `Path(candidate).read_bytes()` again. On a fixture with LOG-001, LOG-002 and active LOG.md, instrumented read counts were LOG-001=2, LOG-002=2, LOG.md=1; the returned history hash and dependency digest still matched independently computed legacy values. This is not only duplicate I/O: injecting a replacement of LOG-001 immediately before its second read returned a HistorySnapshot hash matching the OLD history while `_logs_digest` matched the NEW sealed tree. Concrete reproduction: old snapshot `52b0a92acd83f157`, mixed snapshot `52b0a92acd83f157`, old dependency `delete-tree-sha256:76f3...`, mixed/new dependency `delete-tree-sha256:fc26...`. `operations.py` then uses the snapshot events/tail as PLAN semantics while storing `_logs_digest` as the APPLY stale-state precondition.
ISSUE: The advertised one-pass history capture actually performs two content reads of every sealed segment. Cost is 2x sealed-history I/O on every mutation PLAN, and the second traversal creates a generation split: a PLAN can reason from old LOG bytes while binding its stale-state guard to newer bytes. If the tree remains at that newer generation until APPLY, the dependency check can pass even though the semantic PLAN was derived from an older history.
OPTIMIZE: Compute both identities from the exact raw bytes obtained during the first sealed traversal. Factor the delete-tree framing into a byte-feeding helper or parallel hasher so each sealed file contributes path/stat/sentinel framing and the already-read content without reopening it. Do not retain all raw segment bytes merely to avoid rereads; update both digests incrementally per file.
GUARDRAIL: Preserve byte-for-byte `HistorySnapshot.hash` and `hash_tree_dependency(.saipen/logs)` compatibility, ownership/lstat checks, active-LOG encoding enforcement, missing-tree sentinels and APPLY's fresh under-lock dependency recomputation. This optimization must strengthen generation coherence, not cache dependency state across commands.
VERIFY: On fixtures with 0, 1 and many sealed segments, instrument `Path.read_bytes` and require exactly one content read per sealed segment plus one active LOG read. Compare both returned digests against the current independent implementations on stable fixtures. Inject rename/content replacement between former capture stages and require either one coherent old generation or a stale/refusal result, never `{old semantic snapshot, new dependency token}`. Run mutation PLAN/APPLY race tests proving a post-PLAN sealed-history change still returns STALE_STATE.

[P1] [PERF-002] PROVEN BOTTLENECK tools/saipen_engine/producer.py::StagingGeneration.scan_ready / ProducerPackage.from_dict
EVIDENCE: `scan_ready` iterates every READY JSON, and `ProducerPackage.from_dict(...ready_path=...)` base64-decodes every declared payload, verifies its hash, stores every decoded byte string in `ProducerPackage.payloads`, and `scan_ready` appends all resulting packages to one list. READY artifacts permit up to 32 MiB each and there is no package-count bound. Tracemalloc on valid 128 KiB-payload packages showed accumulated growth: 5 packages = 655,360 decoded payload bytes / 1,252,620-byte peak; 20 = 2,621,440 payload / 3,257,787 peak; 50 = 6,553,600 payload / 7,275,149 peak. `crew._producer_ready_health` then uses only identity/provenance metadata, while `intent.collect_and_ship_producer` filters the complete decoded list and selects exactly one package with `max(current, key=(epoch, package_identity))`. Existing corrupt-READY regression remains green, confirming strict validation is load-bearing.
ISSUE: READY discovery retains O(sum of every READY payload) decoded bytes even for callers that need only metadata or ultimately choose one package. With the per-artifact 32 MiB limit but no count bound, a backlog of otherwise valid READY records can exhaust memory long before selection. Base64 JSON already temporarily amplifies each artifact; retaining every decoded payload makes that transient cost cumulative.
OPTIMIZE: Split strict READY validation from payload retention. Scan each artifact one at a time, validating the complete closed schema, identity, payload key set, base64 validity and payload hashes, but retain only compact package metadata for discovery/health/selection. After the deterministic winner is known, reopen exactly that identity through `ready_package` to materialize its authenticated payload. Callers intentionally integrating multiple packages should stream/materialize one package at a time or explicitly request payload retention rather than inheriting the discovery behavior.
GUARDRAIL: Do not turn stale or unselected READY files into unchecked evidence. Every artifact, including stale/non-winning records, must still be fully integrity-validated so a corrupted READY package continues to make the namespace fail closed exactly as today. Preserve package identities, diagnostics, ordering, epoch selection and authenticated bytes delivered to integration.
VERIFY: Build 100 mixed current/stale valid READY artifacts with large payloads plus corruption cases. Peak retained memory after scanning must remain near metadata plus at most one artifact's decode working set rather than sum(payloads). Selected package identity and integration bytes must exactly match the current implementation. Corrupt stale and corrupt current packages must both still return INVALID_READY/READY corruption rather than being skipped. Existing `test_corrupt_ready_is_preserved_instead_of_replaced` and READY size-contract tests must remain green.

[P2] [PERF-003] STRONGLY EVIDENCED WASTE tools/saipen_engine/fast_check.py::validate_project
EVIDENCE: `validate_project` reads STATE/BOARD/LOG, then calls `read_history_snapshot(root)` into `_history` and passes that frozen snapshot to `validate_texts(... sealed_events=_history)`. Immediately afterward it calls `history_contract_errors(root)`, whose implementation is `snapshot_contract_errors(read_history_snapshot(root))`, reopening and reparsing the complete sealed+active history. Instrumenting `log.read_history_snapshot` on the supplied live project produced exactly `FAST_HISTORY_READS 2` for one `fast_check.validate_project` invocation.
ISSUE: A single fast validation performs two O(history) ownership traversals, content reads, decodes and event parses. The second result contains no information unavailable from `_history`; it exists solely because the convenience wrapper insists on capturing its own snapshot.
OPTIMIZE: Feed the existing `_history` directly to `snapshot_contract_errors` after `validate_texts`, or add an explicit snapshot parameter to the wrapper while retaining the existing public fresh-capture behavior for independent callers.
GUARDRAIL: Preserve the current ordering and wording of validation diagnostics, active LOG checks, complete sealed-history syntax/parent/order validation and the rule that independent validator calls do not cache history across commands.
VERIFY: Instrument `read_history_snapshot` and require exactly one complete capture per `fast_check.validate_project`. Compare the complete ordered error list before/after on a valid fixture and on illegal line, duplicate E-ID, dangling parent, out-of-order event and malformed checkpoint fixtures. Add a scale case with many sealed segments demonstrating one traversal rather than two.

[P2] [PERF-004] PROVEN BOTTLENECK tools/saipen_engine/intake.py::validate_project
EVIDENCE: The validator begins with `_board_source_links(root)`, which reads and parses BOARD once. For every active receipt carrying `linked_work`, it then imports `parse_board`, rereads `.saipen/BOARD.md`, reparses the complete board and looks up the same linked ticket. Inside the same receipt iteration `_validate_contract_coverage(..., _read_coverage(...))` reads coverage once and the subsequent `coverage_ids = set(_read_coverage(...))` reads it again; a DONE linked Work additionally calls `coverage_complete`, causing a third ledger read. Instrumented valid DOING fixtures produced: N=1 -> parse_board=2, coverage=2; N=10 -> parse_board=11, coverage=20; N=30 -> parse_board=31, coverage=60, all with zero validator errors. A 10-receipt DONE fixture produced parse_board=11 and coverage=30. As BOARD commonly grows with Work count, reparsing it once per receipt drives the validation path toward O(receipts * BOARD-size), effectively quadratic when both grow together.
ISSUE: Source validation repeatedly reconstructs two pieces of authority already available in the same call: the parsed BOARD and each receipt's coverage ledger. Large source-intake projects therefore pay repeated file I/O, UTF-8 decode, BOARD parsing and JSON parsing on a gate expected to run frequently during mutations/releases.
OPTIMIZE: Capture BOARD bytes and its parsed ticket map once at validator entry and reuse it for reverse projection plus every linked-work lookup. Read each active receipt's coverage ledger once, validate Contract/coverage against that object, derive `coverage_ids` and any completion summary from the same parsed ledger, and pass it to helpers instead of reopening the file.
GUARDRAIL: Preserve exact fail-closed behavior for unreadable/malformed BOARD and coverage, linkage drift, Contract drift and DONE-with-unresolved cases. Reuse must be call-scoped only; do not introduce a cross-command cache that hides filesystem changes.
VERIFY: Instrument a 120-receipt fixture and require exactly one BOARD parse/read for the validator plus one coverage read per receipt. Compare ordered error sets against the current implementation for missing Work, absent BOARD projection, malformed coverage, Contract/coverage clause mismatch, digest corruption and DONE-with-unresolved coverage. Runtime should scale near O(BOARD + receipts), not O(receipts * BOARD).

[P2] [PERF-005] STRONGLY EVIDENCED WASTE tools/saipen_engine/snapshot.py::ProjectSnapshot.capture + tools/saipen.py::{_status,_next_action,_explain_next}
EVIDENCE: `ProjectSnapshot.capture` always calls full `read_history_snapshot`, which retains `HistorySnapshot.text`, parsed `events`, and an additional tuple containing every accepted raw `event_line`. The read-only status/next/explain paths consume state_text, board_text, history hash/tail and parsed history events; routing's `validate_checkpoint_surface` consumes `snap.history.events/tail` plus `snapshot_contract_errors(snap.history)`. These paths do not consume `snap.history.text` or `event_lines`. Synthetic captures showed linear retained-memory amplification: 2,000 events -> peak 1,881,879 bytes, combined text 217,786 bytes, event_lines 215,786; 8,000 -> peak 7,036,150, text 877,786, event_lines 869,786; 20,000 -> peak 17,685,701, text 2,217,788, event_lines 2,197,788. Thus more than 4.4 MB of duplicate textual history is deliberately retained at 20k events even before accounting for parsed event dictionaries.
ISSUE: Frequent read-only routing/status commands materialize and retain two O(history-text) representations they never use. Parsed events themselves are necessary for validation/routing, but full joined text and raw event-line copies are context-rendering concerns, not semantic snapshot requirements for these commands.
OPTIMIZE: Add an explicit lean ProjectSnapshot/history-capture mode that preserves hash, tail, parsed events, illegal-line diagnostics and max-ticket metadata while omitting combined `text` and `event_lines`. Use lean capture in status, next and explain-next. Keep full capture for context/audit renderers that actually consume verbatim event lines or complete history text.
GUARDRAIL: Do not remove parsed events or immutable-ledger diagnostics from lean snapshots and do not change hash framing/read count. Full capture must remain available with byte-identical text/event_lines for context output. Avoid making mode selection implicit from global state.
VERIFY: Run status, next and explain-next against representative valid, blocked, recovery and malformed-ledger fixtures in full and lean modes and require byte-identical user-visible JSON/text output. Instrument one history content read per segment in both modes. On a large-history fixture, assert lean retention excludes combined text/event-line storage while hash/tail/events and all routing decisions remain identical.

[P2] [PERF-006] STRONGLY EVIDENCED WASTE tools/saipen_engine/release.py::plan_release/_plan_crew_release + release_contract.source_authority_paths
EVIDENCE: A deterministic full-release PLAN fixture with real `source_authority_paths` and local remote/classification seams invoked `source_authority_paths(root)` exactly 3 times before returning one ReleasePlan. The first invocation comes from `_metadata_paths(root)` while building the allowed pre-staged set; the second comes from `_metadata_paths(root)` again when filling `ReleasePlan.metadata_paths`; the third comes from `_source_authority_manifest(root)`. Each `source_authority_paths` invocation independently executes a bounded `git ls-files -z -- .saipen/intake .saipen/archive/source` query and recursively walks both current authority trees with `rglob("*")`. No-publish/crew branches similarly reconstruct overlapping inventories rather than deriving their views from a call-scoped inventory.
ISSUE: One immutable PLAN repeatedly asks the filesystem and Git for the same authority membership. Work scales as multiple complete inventories of the source-intake/archive surface, and because those captures occur at different instants they also make the PLAN harder to reason about as one generation even when no correctness failure is triggered.
OPTIMIZE: Capture the source-authority path inventory once during PLAN. Derive the metadata path list, staging `allowed` set and source manifest from that exact path tuple, hashing each required current file once when building the manifest. Thread the call-scoped inventory through normal/no-publish/crew planners rather than hiding repeated discovery behind `_metadata_paths`.
GUARDRAIL: Do not reuse the PLAN inventory for APPLY-time freshness verification. `_check_source_authority_manifest` must still perform a fresh live inventory/hash under the existing preflight semantics so additions, deletions, active->archive moves or content changes after PLAN remain detectable. Preserve tracked-deletion discovery via Git and no-Git behavior.
VERIFY: Instrument `source_authority_paths` and require one invocation per release PLAN across ordinary full release, no-publish and crew closure. Compare `allowed`, `metadata_paths`, canonical plan serialization and `source_manifest` byte-for-byte against current behavior on active additions, tracked deletions, closed-source moves, archived sources and no-Git fixtures. Mutate source authority after PLAN and prove APPLY/preflight still refuses from a fresh inventory.

PERFORMANCE_DONE_WHEN: PERF-001 reads every history segment once while producing generation-coherent semantic/history-dependency tokens; PERF-002 validates arbitrary READY backlogs without retaining cumulative decoded payloads and materializes only packages actually consumed; PERF-003 performs one complete history capture per fast validation; PERF-004 parses BOARD once and each source coverage ledger once per source-validation pass; PERF-005 gives status/next/explain a lean semantic snapshot with byte-identical behavior and bounded retained history text; PERF-006 performs one source-authority inventory per release PLAN while APPLY retains an independent fresh drift check; all new instrumentation/scale/race tests pass, PerformanceAuditTests remain 13/13 green, the W2 semantic-receipt regression is green after its owning W2-002 repair, compileall stays clean, and the complete performance regression runner finishes successfully in an unrestricted execution window.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.
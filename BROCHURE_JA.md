# SAIPEN

## エージェントは忘れる。プロジェクトは覚えている。

**SAIPEN — AIエージェントのためのファイルベース継続プロトコル。**

プロジェクトの記憶はチャットでも、サービス系の曖昧な「長期記憶」でも、特定モデルの頭の中でもなく、コードのすぐ隣、プレーンなMarkdownファイルに保存される。

今日はClaudeが動く。
明日はGPT。
明後日はGemini、Qwen、あるいは他の何かの電子シャーマン。

モデルは変わる。コンテキストは消える。プロジェクトは仕事を続ける。

**Agent dies. Project remembers.**
**Agent kaob. Projekt mäletab.**
**エージェントは消える。プロジェクトは覚える。**
*[Ējento wa kieru. Purojekuto wa oboeru. エージェントは消える。プロジェクトは覚える。]*

---

## 問題

普通のコーディングエージェントは、ターミナルへのアクセスを持った金魚のようなものだ。

二十分かけてプロジェクトを調べ、アーキテクチャを理解し、バグを見つけ、修正を始める — そしてリミットにぶつかるか、閉じられる。

新しいエージェントがやってくる:

> 何をしている?
> どのファイルが重要?
> 何がもう確認済み?
> 何に触れてはいけない?
> 続ける?

人間はまたプロジェクトの歴史を語り直す。もう一度。それから巨大な `CLAUDE.md` を書く。そしてそのファイルは、古い決定、でたらめな禁止事項、モデルがかつて誤解したフレーズの神聖なゴミ捨て場になる。

SAIPENは言う:

> プロジェクトを会話の中に置くのはやめろ。
> 状態をプロジェクトの隣に置け。
> 新しいエージェントが読んで続ける。

---

# SAIPENにできること

## 1. チャット履歴なしで仕事を続ける

ユーザーが書く:

```text
saipen continue
```

または短く:

```text
cc
```

コールドエージェントが読む:

```text
STATE.md
BOARD.md
LOG.mdの末尾
human_note
```

そして記録された `next_action` を実行する。

インタビューはしない。
アーキテクチャの説明を求めない。
古いプランの上に新しいプランをでっち上げない。
最初の二十行を読んだだけで「コンテキストは把握した」ふりもしない。

具体的な場所から仕事を続ける。

---

## 2. プロジェクトの記憶をプレーンなファイルに保存する

SAIPENは記憶を目的別に分ける。

### `STATE.md`

質問に答える:

> 今すぐ何をする?

現在のフェーズ、アクティブなチケット、作業モード、正確な次のアクション、重要な制約がここにある。

### `BOARD.md`

答える:

> どんな仕事が存在する?

タスクは分けられる:

```text
DOING
TODO
BLOCKED
DONE
```

どのタスクにも優先度、依存関係、検証条件、ブロック理由がある。

### `LOG.md`

答える:

> 実際に何が起きて、なぜここに辿り着いた?

「これから確認する」ではない。
「たぶん修正された」ではない。
具体的なイベント、コマンド、結果、そして以前の決定との関連。

### `KNOWLEDGE/`

答える:

> プロジェクトの永続的な真実は何?

アーキテクチャ上の決定、制約、慣習、ADRは何キロにもなる一時ログに埋もれない。

### `kitchen/`

作業台。

下書き、中間資料、準備済みパッケージ、未完了の作業はここに残る。セッションが死んでも、三十分の作業が灰と哲学にならないように。

---

## 3. エージェントに正確な次のアクションを一つだけ与える

SAIPENの心臓はフィールドだ:

```yaml
next_action:
```

エージェントはセッションごとに「何をするか」を再決定する必要がない。

プロジェクトは次のステップをすでに知っている:

```text
PHASE SCOUT T-399
RUN validate.py
VERIFY T-501
WAIT: destructive-op
SHIP release
```

ひとつの状態。
ひとつのコマンド。
ひとつの現実。

エージェントがプロセス管理で即興を減らすほど、実際のタスクに使う頭が増える。官僚主義が仕事を妨げず、ニューラルネットがドライバーを手に森へ逃げるのを防ぐ、珍しいケースだ。

---

## 4. 厳密な状態機械で仕事を進める

仕事はフェーズで進む:

```text
INIT
→ PLAN
→ SCOUT
→ BUILD
→ VERIFY
→ REVIEW
→ SHIP
→ DONE
```

特別なフェーズもある:

```text
BLOCKED
HUNT
MARKHUNT
ADD
CLEAN
TRANSLATE
PREPARE
VALIDATE
```

各フェーズは知っている:

* エージェントが何を読まなければならないか;
* 何を変更してよいか;
* 何が完了とみなされるか;
* 次にどこへ進むか;
* いつ停止しなければならないか;
* どんな証拠を残すか。

最初のグリーンテストが出た直後にタスクを完了と宣言することはできない。最初にBUILD、次にVERIFY、その次にREVIEW、最後にSHIP。

「一度は自分のマシンで動いた」はエンジニアリング標準ではない。民間信仰だ。

---

## 5. さまざまなモデルとツールで動く

SAIPENは一社のものではない。

対応想定:

* Claude Code;
* OpenAI Codex;
* Gemini;
* OpenCode;
* Aider;
* Antigravity;
* DeepSeek;
* Qwen;
* ファイルを読み、コマンドを実行できるその他のエージェント。

プラットフォームにはアダプターとインジェクターがあるが、土台は同じだ:

```text
プレーンなファイル
プレーンなGit
プレーンなMarkdown
```

独自メモリなし。
必須データベースなし。
明日アップデートして「君のプロジェクトはもうクラウドの鉢に置くべきだ」と決める常駐デーモンもなし。

---

## 6. 長い目標を自律的に実行する

コマンド:

```text
saipen goal <objective>
```

でゴールモードが始まる。

SAIPENは:

1. 目標を記録する;
2. キューを作るか再構築する;
3. 次の作業可能なチケットを選ぶ;
4. 実行する;
5. 検証する;
6. レビューする;
7. 次へ進む;
8. ステップごとに「続ける?」と聞かない。

ウェーブ数とチケット数にはセーフティリミットがある。自律性は月明かりの下でリポジトリを永遠に彷徨うことを意味しない。

ゴールモードは誠実な結末のいずれかまで動く:

```text
目標達成;
作業がブロック;
セーフティバルブ発動;
利用可能な作業量を使い切った。
```

---

## 7. 次の有益な仕事を自分で見つける

通常のバックログが尽きると、メンテナンス層はループに入れる:

```text
HUNT
→ ADD
→ HUNT
```

### HUNT

探すもの:

* 実際の欠陥;
* 矛盾;
* 壊れた検証;
* デッドコード;
* アーキテクチャのドリフト;
* 保護されていない必須ルール;
* ドキュメントと挙動の乖離。

### ADD

明らかな欠陥がなくなると、既存アーキテクチャを継ぐ次の自然な機能を提案する。

創作フェスティバルは開かない。

SAIPENは進化的に発展する:

```text
既存パターン
→ 明白なギャップ
→ 最小限の拡張
→ 検証
```

こんなふうではない:

```text
エージェントが退屈した
→ プロジェクトの半分をRustで書き直した
→ それをモダンアーキテクチャと呼んだ
```

---

## 8. 監査と修理を区別する

SAIPENは、モデルが一つの鍋に放り込みたがる複数の動作を分離する。

### `markhunt`

広範な監査を行い、発見を記録する。

何も修正しない。

これが重要だ: まず事実を確立し、それからプロジェクトを変える。

### `hunt`

確認済みの発見を整理し、正式なタスクに変える。

### `validate`

SAIPEN状態自体の構造的整合性を確認し、許可された損傷を修復する。

### `test`

宣言されたテストを実行し、PASSかFAILを報告する。

検査中にテストをこっそり直したりしない。

### `clean`

ゴミを削除し、ボードを掃除し、構造を整える。ただしファイルを移動・削除する前にはリンクと依存関係を必ず確認する。

なぜなら、ファイルは午前三時にプログラムが呼び出すまで孤児に見えることがあるからだ。

---

## 9. きれいな報告書ではなく証拠を求める

SAIPENは典型的なLLMの癖に対抗して作られている:

* 読んでもいないファイルを「読んだ」と主張する;
* 実行もせずに「テストは通った」と言う;
* もっともらしい経路を捏造する;
* 最初のグリーン結果で止まる;
* 実行せずにタスクを言い直す;
* ユーザーの最後の発言をプロジェクトの新しい真実にする;
* 一時的な推測を恒久ルールにする。

だから仕事は痕跡を残す:

```text
ファイル変更;
LOGイベント;
BOARD変更;
STATE変更;
コマンドとその結果;
verification evidence。
```

エージェントが何もしていなければ、そのまま言わなければならない。壮大な内面準備についての報告書を作るのではなく。

SAIPENでは、「完了」という言葉だけでは、商品名のないレシートほどの重みしかない。

---

## 10. プロトコル自身を検証する

SAIPENには独自のコンプライアンス層がある。

チェックするもの:

* 状態の正しさ;
* 有効なフェーズ遷移;
* チケットの形式;
* 依存関係;
* イベントの順序;
* ドキュメント間リンク;
* スキーマ準拠;
* ルールのチェックによるカバレッジ;
* PowerShellとshellでの動作;
* 破損と復旧のシナリオ;
* ミューテーション赤コントロール。

ルールが壊れたとき、チェックは赤くなれなければならない。

いつも緑のテストはテストではない。観葉植物だ。

---

## 11. 中断とエージェント交代を乗り越える

SAIPENはチェックポイントを使う。

危険な遷移の前に、エージェントは保存する:

* 現在のフェーズ;
* アクティブなチケット;
* 完了した部分;
* 検証結果;
* 次のステップ;
* 作業ディレクトリの状態。

エージェントが死に、フリーズし、リミットに当たり、閉じられても、次のエージェントはチャットで考古学発掘を始めない。

チェックポイントを読んで続ける。

**継続 — keizoku — 継続。**

英雄的な記憶回復ではない。普通の定常作業だ。

---

## 12. 他人の未完了の仕事を壊さない

汚れたGitツリーは、SAIPENにとってプロジェクトの正常な状態だ。

エージェントは判別しなければならない:

* どの変更が自分のチケットに属するか;
* どの変更をユーザーが残したか;
* どの変更が他の未完了の仕事に属するか;
* 何を今回のリリースに含められるか;
* 何に触れてはいけないか。

SAIPENは禁止する:

* 予期しない `reset`;
* 他人のファイルの巻き戻し;
* コミット前の作業の消去;
* 検証なしの一括上書き;
* 理解できないものの削除による偽りの「掃除」。

原理は単純だ:

> 誰のものか分からないなら — 手を出すな。

---

## 13. 正直に止まる

仕事が本当に続けられないとき、SAIPENは明示的な `WAIT:` を使うか、チケットを `BLOCKED` に移す。

理由は具体的でなければならない:

```text
手動確認が必要;
破壊的操作に許可が必要;
capability不足;
必須の決定が不明;
最初のpublishが必要;
外部のblockerが存在;
セーフティバルブ発動。
```

エージェントは推測してはいけない。

しかし自分の怠惰でブロックしてもいけない。

情報不足 — 停止。
ファイルを見たくない — 停止ではない。

---

## 14. プロジェクトの実際の状態を示す

コマンド:

```text
saipen status
```

は読み取り専用だ。

表示されるもの:

* 現在のフェーズ;
* アクティブなタスク;
* 次の仕事;
* ユーザーを待っているもの;
* 主張されているが証明されていない結果;
* 最後にコンプライアンスチェックが走った日時;
* 現在の状態がどのくらい古いか;
* ブロックされているタスク。

SAIPENは自分のプロジェクトを「健全」とか「本番準備完了」とは宣言しない。

事実を示す。結論は人間が出す。

自分で自分にメダルを授けたエージェントは、やはり自分で自分にメダルを授けたエージェントだ。

---

## 15. 専門化された工場の間で仕事を渡す

SAIPENは分離されたproducerプロセスをサポートする。

彼らはメインツリーから離れて働き、結果を `OUTBOX.md` 経由で渡す。

例:

### `saihunt`

欠陥と怪しい場所を探す。

センサーだ。修理屋ではない。

### `saitest`

疑念を再現可能な事実にするか、誤った仮説を殺す。

adversarialシナリオを作る:

* 誤った入力;
* 境界値;
* 順序の破壊;
* 繰り返し呼び出し;
* 破損した状態;
* resource pressure;
* hostile environment。

### `saipython`

小さなPython欠陥を取り、分離ワークスペースでコピーを修正し、検証して、完成したpatchを渡す。

メインツリー自身は編集しない。

### `saitranslate`

Coreから離れて多言語パッケージを準備する。

### `saiwiki`

ソースの特定の状態に紐づいたドキュメントとwikiパッケージを準備する。

Coreは新鮮で検証済みの完全なパッケージだけを受け入れる。

次のようなものは禁止:

> なんかそこで作業したんだよね、隣のチャットを見て。

payload、instructions、source commit、evidenceだけ。

---

## 16. 産業パイプラインで仕事を進める

Crew Circuitコマンドはプロジェクトを一連の工程に通す:

```text
sense
→ reproduce
→ intake
→ build
→ verify
→ review
→ translate
→ document
→ publish
```

各工程は約束ではなく、次へ渡す:

```text
再現;
判定;
チケット;
パッケージ;
証拠。
```

工程が空なら、それが結果として記録される。

工程がブロックされれば、パイプラインは止まる。

部分的な結果を「あっちはだいたい大丈夫」というメモ付きで先へ流すことはない。

現在のCrew Circuitは順次実行だ。アトミックなclaims、epochs、worktrees、release captainを備えた本格的な並行Crew Modeは別途設計中であり、解決済みの問題として提示されてはいない。

---

## 17. コンテキストを節約する

SAIPENは遅延ロードを使う。

エージェントはまず小さなBOOTカーネルとINDEXを読む。

その後、次のものだけを読み込む:

* アクティブなフェーズのドキュメント;
* 必要なCoreルール;
* 現在の作業用の専門ドキュメント;
* 関連する永続的なプロジェクト記憶。

毎セッション、憲法全体、国家の歴史、バリデータの系譜を読み直す必要はない。

これが減らすもの:

* token cost;
* 気が散る確率;
* 古い指示との衝突;
* 無関係なルールの影響;
* cold-start時間。

**Vähem müra, rohkem tööd.**
ノイズを減らし、仕事を増やす。

---

## 18. 必須インフラなしで動く

SAIPENの土台:

```text
Markdown
Git
ファイルシステム
フルバリデータ用のstdlib Python
```

Pythonがなくても、ポータブルなshell/PowerShell検証フロアは残る。

プロジェクトはObsidian vaultとして開ける。

`KNOWLEDGE/` はグラフに見える。
普通のリンクが機能する。
状態は人間が読める。
Gitは変更履歴の全体を示す。

隠れた魔法はない。

SAIPENが壊れても、メモ帳で開いて何が起きたか理解できる。

それがシステムと呼ばれるものだ。それ以外は時々ダッシュボードと呼ばれる。

---

# 主なコマンド

```text
saipen set                 プロジェクトを受け入れる
saipen continue            仕事を続ける
saipen plan                プランとチケットを作る
saipen goal <text>         目標を自律実行する
saipen status              状態を表示、何も変更しない
saipen stop                チェックポイントを保存して停止
saipen hunt                欠陥を探す
saipen markhunt            広範な監査を行う
saipen test                テストを実行する
saipen validate            構造を確認する
saipen clean               プロジェクトを掃除する
saipen prepare             handoffを準備する
saipen collect             完成パッケージを受け取る
saipen translate           翻訳を準備する
saipen ship                リリースを出す
saipen crew                全生産サイクルを回す
```

頻繁な操作には短いキーがある:

```text
cc    続ける
sss   状態を表示
ss    チェックポイントを保存して停止
```

コマンドはメッセージ全体だ。四段落の呪文はない。

---

# SAIPENがやらないこと

SAIPENは新しいLLMではない。

弱いモデルを天才にはしない。

エージェントが絶対に間違えないと保証もしない。

百台のマシンのための分散コンセンサスアルゴリズムでもない。

テスト、Git、エンジニア的思考の代わりにはならない。

必要もなく秘密やユーザーデータを保存すべきでもない。

彼がやることは別だ:

> 誤りが可視になる。
> 仕事が継続可能になる。
> 状態が検証可能になる。
> エージェントが交換可能になる。

---

# 誰のためのものか

SAIPENは次の場合に役立つ:

* 複数のAIコーディングエージェントと仕事をしている;
* セッションリミットに常にぶつかっている;
* 価格、可用性、タスクに応じてモデルを切り替えている;
* 一回の会話より長くプロジェクトを続けている;
* 同じ説明をエージェントに繰り返すのに疲れている;
* ステップごとの手動 `continue` なしのタスクキューが欲しい;
* 「やった」と「証明された」の違いを見たい;
* チャットの魔法の記憶を信じていない;
* 閉じたインフラより開かれたファイルを好む;
* 特定のエージェントが死んでもプロジェクトが生き残るようにしたい。

---

# 一言で

**SAIPENは、忘れっぽい対話者だったAIを、工場に来てプロジェクトの状態を読み、次のタスクを取り、証拠を残し、工場の人生全部を語り直すことを求めない、交換可能な労働者に変える。**

```text
チャットは消えた。
モデルは変わった。
リミットは尽きた。
プロジェクトは忘れなかった。
```

**SAIPEN. One command. Zero dependencies. Zero amnesia.**

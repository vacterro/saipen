# SAIPEN Conformance

Implementations MUST pass self-check across three vectors:
1. **Repo Validation**: `STATE.md`, `BOARD.md`, `LOG.md`, and `KNOWLEDGE/` MUST conform to the shapes RFC.md § 1.2 defines, enforced by `tools/validate.py` (canonical -- validates STATE.md against `extensions/schemas/state.schema.json` directly, plus graph checks: E-### uniqueness/monotonicity, parent resolution), with `tests/validate.sh` / `validate.ps1` as the frozen portable floor for hosts without Python. `board.schema.json`/`log.schema.json` remain descriptive reference -- see `extensions/schemas/README.md`.
2. **Session Validation**: `BOARD.md`'s `needs:` graph MUST be acyclic AND every reference MUST resolve to a ticket that actually exists on the board -- a dangling reference is worse than a cycle, since nothing else catches it. `LOG.md` graph parent-child links MUST resolve.
3. **Phase Contract Validation**: Stated `mode` MUST legally permit the current `phase` (RFC.md § 1.3). `phase` MUST be one of the sixteen enum values RFC.md § 1.6 defines, and any phase-to-phase transition MUST be legal per § 1.6's transition table.

## TEST-001: The Continuation Test
Any release of this protocol MUST pass the gold standard test:
1. Cold agent (zero chat history).
2. Execute `saipen continue` (or equivalent bootstrap command).
3. Agent MUST: read `next_action` and execute it instantly WITHOUT asking for context.
If the agent asks "What should I do?", the protocol has failed. A `next_action: WAIT: <specific question>` (RFC.md § 1.2) does NOT fail this test -- asking one exact, pre-determined question instantly is the executable action; the failure mode is vague context-seeking, not a specific authorization gate. This includes the bootstrap `WAIT:` `INIT` produces on a brand-new project (RFC § 1.2's fifth `WAIT:` category, `phases/init.md`) -- asking for the first project goal or raw backlog is the same kind of specific question, not vague context-seeking, and doesn't fail this test either: nothing in `STATE`/`BOARD`/`LOG` could have answered it instead, since none of them have any history yet.

## Scenario Coverage
`tests/scenarios/` holds one fixture directory per concept below. Behavioral
ones are README-only where the assertion is about agent decision-making, not
file shape -- there is nothing to validate mechanically. "Covered by" names
the actual fixture; entries with no fixture state why, not silently.

Note: row IDs were renumbered after the duplicate 43 incident in v7.86.0.
Current IDs are canonical; old changelog references remain historical.

| # | Concept | Covered by |
|---|---------|------------|
| 1 | Cold continuation | This section (TEST-001 above) + this repo's own live `.saipen/` -- no separate fixture adds anything a dedicated one wouldn't just duplicate. |
| 2 | Corrupt STATE recovery | `resume-after-crash`, `stale-state-reconciliation` |
| 3 | Dependency cycle | `dependency-cycle` |
| 4 | Dangling `needs:` reference | `dangling-needs-reference` |
| 5 | Stale claim forfeiture | `multi-agent-claim-conflict` |
| 6 | Goal counter crash recovery | `goal-counter-recovery` |
| 7 | Manual-verify gate | `manual-verify-fallback` |
| 8 | No-publish restriction -- git-dependent *steps* are skipped (no commit/tag/push, never a false "pushed"), but `SHIP` itself stays reachable and still closes the ticket to `DONE` (RFC § 1.3, `phases/ship.md`'s no-publish branch, v7.66.0) | `no-git-ship-denial` -- behavioral, README-only (folder name predates v7.66.0, when this asserted the opposite) |
| 9 | Read-only restriction | `read-only-restriction` |
| 10 | Board-empty maintenance transition | `board-empty-maintenance-transition` -- behavioral, no fixture |
| 11 | Goal objective exit | N/A -- `goal_exit` was evaluated and rejected three times (recorded in `.saipen/KNOWLEDGE/decisions.md` and CHANGELOG v7.13.1/v7.19.1); `goal_mode` never exits on board-empty, so there is no "objective exit" behavior to test |
| 12 | Extension absence does not block | `extension-absence` |
| 13 | Unresolved LOG parent | Enforced by `tools/validate.py` since v7.24.0 (parent must resolve to an earlier `E-###`; IDs unique + monotonic). The old excuse -- historical numbering resets a naive resolver would misflag -- died with v7.24.0's user-approved ledger repair; this repo's own LOG.md now passes the check it once couldn't. |
| 14 | Invalid phase transition | `invalid-phase-transition` -- automated via `transition_from` in STATE.md; `tools/validate.py` checks every non-self transition against RFC § 1.6's table (v7.82.0) |
| 15 | Mode-phase restrictions -- `read-only` MUST NOT enter `INIT`/`PLAN`/`ADD`/`BUILD`/`SHIP`/`CLEAN`/`TRANSLATE` (RFC § 1.3 -- every phase whose work product is a file write; `INIT`/`PLAN` added v7.93.0, `ADD` v7.94.0, whose entire output is tickets), the only such ban still live since v7.66.0 freed `no-publish` + `SHIP` | `read-only-restriction` (fails as intended); `invalid-mode-phase-combination` is now the inverse regression guard -- it asserts `no-publish` + `SHIP` stays legal, since re-adding that ban already shipped once |
| 16 | Ticket-level BLOCKED (non-cycle failure), work continues | `blocked-ticket` |
| 17 | Fresh INIT bootstrap from `templates/` | `fresh-init` -- behavioral, no `.saipen/` yet by definition (that's what INIT creates) |
| 18 | Evolutionary ADD feature symmetry (§ 2.2) | `add-feature-symmetry` -- behavioral, no fixture |
| 19 | Unclaimed DOING adoption (RFC § 1.4, v7.28.0) | `unclaimed-doing-adoption` -- behavioral, README-only |
| 20 | Clean tree after ticket-level BLOCKED (`phases/verify.md`, v7.27.0) | `blocked-ticket-clean-tree` -- behavioral, README-only |
| 21 | Dirty tree on continuation (RFC § 1.5, v7.29.0) | `dirty-tree-continuation` -- behavioral, README-only |
| 22 | Parallel TRANSLATE doesn't stomp shared STATE (RFC § 2.1, `phases/translate.md`, v7.34.0) | `parallel-translate-isolation` -- behavioral, README-only |
| 23 | Dual-location extension conflict resolved, never merged/guessed (RFC § 1.9, v7.36.0) | `extension-dual-location-conflict` -- behavioral, README-only |
| 24 | Dual-location TRANSLATE conflict resolved, never merged/guessed (RFC § 2.1, v7.37.0) | `translate-dual-location-conflict` -- behavioral, README-only |
| 25 | `saipen sub spawn` / parallel TRANSLATE both refuse on a project with no `.saipen/` yet, instead of silently triggering INIT (RFC § 1.9 v7.36.0, § 2.1 v7.37.0) | `spawn-requires-init` -- behavioral, README-only |
| 26 | HUNT skip requires an exact HEAD-hash match; mtime/diff-timing is never a substitute for actually running the six categories (`phases/hunt.md`, v7.41.0) | `hunt-skip-requires-exact-hash-match` -- behavioral, README-only |
| 27 | MARKHUNT records findings without fixing/capping, unlike HUNT (`phases/markhunt.md`) | `markhunt-dry-record-only` -- behavioral, README-only |
| 28 | VERIFY hysteresis: a ticket blocked a second time escalates to session-level `BLOCKED` instead of spending a fresh retry budget (`phases/verify.md`) | `verify-hysteresis-second-block` -- behavioral, README-only |
| 29 | SubSaipen `STATE.md` shape + policy (schema-valid, `mode: read-only`, no write-requiring `phase`) checked by `tools/validate.py`, not just the main project's own `STATE.md` (`extensions/subs/PROTOCOL.md` § 1/§ 8, v7.49.0) | Enforced directly by `tools/validate.py` -- no fixture; the repo's four live instances exercise the pass case every run. The walk also covers the shipped library at `extensions/subs/`, which holds only `TEMPLATE/` since v7.84.0, so that half now guards against instance state reappearing there -- the intended end state, not a dead check |
| 30 | `read-only` reaches `MARKHUNT`/`PREPARE`/`VALIDATE` only in report-only form -- findings/handoff/corruption are reported in chat, never written (no `## BLOCKED` tickets, no `kitchen/`, no structural repair) (RFC § 1.3) | `read-only-audit-phases` -- behavioral, README-only |
| 31 | `stop` preserves the goal counters; `saipen continue`/bare `saipen` resume them unchanged, but bare `saipen goal` deliberately resets `goal_waves`/`goal_tickets` to `0` for a fresh valve budget (RFC § 1.10 / § 2.4) | `stop-goal-counter-semantics` -- behavioral, README-only |
| 32 | `goal_waves` is not double-counted when `ADD` RETURNs to `PLAN` -- the cycle's wave is counted once at ADD's RETURN, and the follow-on `PLAN` skips its own increment for that same cycle (RFC § 2.4, `phases/plan.md`/`add.md`) | `goal-waves-no-double-count` -- behavioral, README-only |
| 33 | A fixer-type subSaipen (saipython) emits a tested patch through OUTBOX, having cloned targets into its own `kitchen/pen/` and verified there in phase `VERIFY` -- it still never writes the main tree and never enters `BUILD`/`SHIP` (`mode: read-only`, enforced by `tools/validate.py`); the main agent applies the patch through Core gates (`extensions/subs/PROTOCOL.md` § 9) | `fixer-subsaipen-patch-outbox` -- behavioral, README-only |
| 34 | Cold continuation via `saipen/BOOT.md` -- the compact kernel reads STATE -> BOARD -> active LOG tail, executes `next_action`, and loads the phase doc on demand without pulling the full RFC; TEST-001 passes on BOOT + STATE + BOARD + tail alone (`saipen/BOOT.md`, RFC § 1.1) | `boot-cold-start-kernel` -- behavioral, README-only |
| 35 | `human_note:` optional STATE field -- a one-line human nudge read on continue (`BOOT.md`'s `human_note` step) and cleared once applied; never a ticket or goal, and one that implies work becomes a real `TODO` instead of living in `STATE.md` (RFC § 1.2) | `human-note-nudge` -- behavioral, README-only |
| 36 | Human digest -- `saipen ship` and `saipen stop` (over)write `.saipen/kitchen/digest.md` (done/remaining/awaiting, three lines) so the human reads one snapshot instead of scrolling `LOG.md` (`phases/ship.md`, RFC § 1.10) | `human-digest-on-exit` -- behavioral, README-only |
| 37 | MARKHUNT closure self-test -- completion is verified against its manifest (`vectors:` 5/5, `cursor: done`, `head_end` == current HEAD, `findings:` == the `[MARKHUNT]` tickets written) before it may reach `DONE`; the enriched completion line (`N findings, V/5 vectors, @head`) keeps coverage auditable after `kitchen/` is swept -- the checkable closure HUNT gets from its hash line, which MARKHUNT previously lacked (`phases/markhunt.md`, closes T-136) | `markhunt-closure-self-test` -- behavioral, README-only |
| 38 | LOG segmentation -- `E-###` stays globally unique and monotonic, and `[parent: E-###]` resolves, across sealed `.saipen/logs/LOG-<NNN>.md` segments plus the active `LOG.md` treated as one sequence, not just within whichever file is currently open (RFC § 1.2, v7.53.0) | Enforced directly by `tools/validate.py` -- no separate fixture, this repo's own live `.saipen/LOG.md` plus every sealed `.saipen/logs/LOG-*.md` segment already exercise the pass case every run |
| 39 | Shipped-library integrity -- `tools/validate.py` checks the subSaipen states in `extensions/subs/` (what `saipen sub spawn` copies and the injector distributes), not only this project's live `.saipen/extensions/subs/` instances; a concrete `saipen_home` in a shipped template is a FAIL, since that file reaches every user (v7.72.0) | Enforced directly by `tools/validate.py` -- the repo's own three library examples plus its live instance exercise it every run |
| 40 | Board soft cap -- `BOARD.md` is read on every cold start (RFC § 1.1, `BOOT.md` step 2), so growth past ~16 KB WARNs and points at `phases/clean.md`'s board scrub. No sealing machinery, unlike `LOG.md`: the board is prunable rather than append-only, and closed tickets already live permanently in `LOG.md`/`CHANGELOG.md` (RFC § 1.2, v7.72.0) | Enforced directly by `tools/validate.py` -- caught this repo's own 28 KB board on the run that introduced it |
| 41 | `saipen status` answers the question actually being asked -- alongside phase/ticket/queue it reports what is waiting on a human, what is claimed but unproven, the last recorded conformance result, and staleness; and it states facts without pronouncing the project healthy or ready (RFC § 1.10, v7.82.0) | `status-answers-the-real-question` -- behavioral, README-only |
| 42 | LOG timestamp UTC sanity -- two faults, two severities (RFC § 1.2): **>3h in the future** is a FAIL (a wrong clock, inherited by every later event); **>5min earlier than the previous event** is a WARN unless a later `DEC` documents the drift. The check that collapsed both into one WARN via `abs()` had never fired at all -- its regex did not match a LOG line (removed v7.93.0) | Enforced directly by `tools/validate.py` |
| 43 | Reserved -- superseded by row 54 in v7.93.0. This row described the same state at the old WARN severity and named only one legal `WAIT:`, so the table asserted two different verdicts about one board. Row 54 is the live rule: FAIL, and both § 1.2 fixed wordings allowed | N/A -- see row 54 |



| 44 | Checkpoint self-confirmation -- after writing `STATE.md` the agent re-reads it and confirms **every field in RFC § 1.2's required set** survived. This row deliberately does not restate the set (it said "all eight" while the set was nine). A checkpoint missing `next_action` fails TEST-001 outright and is not a checkpoint (RFC § 1.5) | `checkpoint-self-confirmation` -- behavioral, README-only |
| 45 | Returning agent distrusts its own memory -- before acting on anything it believes it already knows, it compares `STATE.md`'s `agent:` and `updated:` against itself; either mismatch means recollection is stale and the files win (RFC § 1.1, `BOOT.md`, v7.85.0) | `returning-agent-stale-memory` -- behavioral, README-only |
| 46 | A tripped safety valve is a pause, not an exit -- `goal_mode` stays `true`, the at/over-cap counters ARE the tripped state, a resuming agent re-states the stop instead of continuing, and bare `saipen goal` resetting them to `0` is the re-authorization (RFC § 2.4, v7.86.0) | `safety-valve-is-a-pause` -- behavioral, README-only |
| 47 | Determinism invariants (RFC § 1.11) -- fixed action priority RECOVER > UNBLOCK > FINISH > START > MAINTAIN; at most one `## DOING` ticket in total in Core (§ 1.11; "per agent" until v7.93.0, which invited the reading "that ticket is not mine, so I may claim a second"); every session leaves a LOG/BOARD/STATE/project-file trace or says it did nothing; insufficient information is a `WAIT:`, never a guess (v7.86.0) | `determinism-invariants` -- behavioral, README-only |
| 48 | A subSaipen short of a fact writes `status: blocked` with the missing fact in `details`, never a guessed `ready` -- RFC § 1.11's stop-don't-guess rule translated to the one channel a sub actually has, since it has no `WAIT:` a human reads (`extensions/subs/PROTOCOL.md` § 2, v7.88.0) | `subsaipen-blocked-not-guessed` -- behavioral, README-only |
| 49 | `next_action` vocabulary, not just shape -- a value starting `saipen ` must name a command RFC § 1.10 actually defines; `saipen hunt` passes every shape check while naming a phase reached only autonomously (§ 2.1), so a cold agent must decline it and TEST-001 fails on a state that looked valid (`tools/validate.py`, v7.89.0) | Enforced directly by `tools/validate.py` -- caught this repo's own live `STATE.md` on the run that introduced it |
| 50 | One ticket at a time -- `tools/validate.py` FAILs a `BOARD.md` carrying more than one `## DOING` ticket. RFC § 1.11 shipped this as prose in v7.86.0 with nothing enforcing it, which is precisely the ticket-hopping it exists to stop (v7.90.0) | Enforced directly by `tools/validate.py` |
| 51 | Goal counters leave a countable trace -- a non-zero `goal_waves`/`goal_tickets` under `goal_mode: true` with no matching `DEC: goal_<counter> N->M` line in any LOG segment WARNs, since § 1.5 Recovery rebuilds those counters by counting exactly those lines (RFC § 2.4, v7.90.0) | Enforced directly by `tools/validate.py` -- WARN not FAIL, since pre-v7.87.0 states legitimately lack the lines |
| 52 | MARKHUNT findings carry their evidence -- a `[MARKHUNT]` ticket whose `| blocker:` says only "unvetted audit" with no file:line or command output FAILs. "No cite, no ticket" (`phases/markhunt.md`) is what stops a dry audit becoming a generator of confident vibes; unenforced until v7.91.0 | Enforced directly by `tools/validate.py` |
| 53 | subSaipen OUTBOX entries are well-formed -- a recognized `status:`, and on `ready` a `summary:` and `critical:`; an entry carrying a `patch:` additionally needs `base_head:` and `verified:` (§ 9), since a diff with no provenance cannot be re-checked before it is applied. OUTBOX is the only channel out of a subSaipen and nothing validated it until v7.91.0 | Enforced directly by `tools/validate.py` |
| 54 | Deadlocked board FAILs instead of warning -- `phase: DONE` + empty `## TODO` + a `WAIT:` that is neither the § 2.4 safety valve nor `WAIT: user brake -- <reason>` is non-conformant: § 2.1 requires that exact state to auto-transition to `HUNT`. Both legal WAITs carry fixed wording precisely so a machine can tell a real pause from drift (RFC § 1.2, § 1.11) | Enforced directly by `tools/validate.py` |
| 55 | Tripped safety valve has one legal shape -- `goal_mode: true` with `goal_waves >= 3` or `goal_tickets >= 20` MUST carry the § 1.2 safety-valve `WAIT:` in `next_action`, and MUST NOT set `phase: BLOCKED` (a § 2.4 Exit condition, which would clear `goal_mode` and make the bare `saipen goal` the WAIT line prescribes illegal under § 1.10 -- the valve destroying its own continuation path). Prose since v7.86.0, unchecked until v7.92.0 (RFC § 2.4) | Enforced directly by `tools/validate.py` |
| 56 | BOARD section headings appear exactly once each -- duplicate `## DOING`/`## TODO`/`## DONE`/`## BLOCKED` splits the work surface even when every ticket ID stays unique, and the Pick Rule then reads only whichever copy it reached first (RFC § 1.2). Checked since v7.86.0, never listed here until v7.93.0 | Enforced directly by `tools/validate.py` |
| 57 | `WAIT:` carries a category token from a closed set of seven (`manual-verify`, `destructive-op`, `first-publish`, `user brake`, `blocked`, `safety valve`, `init`). A vague `WAIT: need more context` is shape-identical to a real gate; the token is what separates them mechanically, and tells the human what kind of answer unblocks it (RFC § 1.2) | Enforced directly by `tools/validate.py` -- two vague fixtures FAIL, all seven categories pass |
| 58 | Cross-document drift is detected, not found by hand -- `tools/validate.py` parses six sets out of RFC.md (required fields, phase enum, from-any-phase transition set, read-only bans, `next_action` prefixes, `WAIT` categories) and FAILs when any other copy disagrees, or when BOOT/CONFORMANCE re-enumerate the field set. A moved RFC anchor is itself a FAIL: a drift check that stops checking still prints PASS (RFC § 1.1) | Enforced directly by `tools/validate.py` -- seven desync fixtures, one per check |
| 59 | Checkbox agrees with section -- `[x]` only under `## DONE`, `[/]` only under `## DOING`, `[ ]` only under `## TODO`/`## BLOCKED`. The section is the status; a board where the two disagree answers "is this done" differently depending on which the reader trusts (RFC § 1.2). WARN on two of the three cases until v7.93.0, the third unchecked | Enforced directly by `tools/validate.py` |
| 60 | A write is confirmed by reading it back -- all three checkpoint files, not just `STATE.md`. A pattern substitution whose anchor missed left `BOARD.md` unchanged through eleven tickets while the validator stayed green, since an empty `## TODO` is perfectly conformant (RFC § 1.5, v7.93.0) | `write-confirmed-by-readback` -- behavioral, README-only; the failure is an agent trusting its own success message, which no file shape can reveal |
| 61 | A recognized command is not a legal transition -- `saipen ship` is accepted from any phase (§ 1.10), but `phase: SHIP` is enterable only from `REVIEW`, which is the transition table's single route into it. The from-any-phase *transition* set is six phases; `SHIP` sat in it from v7.83.0 (validator) and v7.92.0 (RFC text) until v7.94.0, so one half of the protocol said "run the gates" while the other made the jump legal (RFC § 1.6, § 1.10) | Enforced directly by `tools/validate.py` -- INIT->SHIP FAILs, REVIEW->SHIP passes, `next_action: saipen ship` stays legal vocabulary |
| 62 | A rule reaches the docs that emit it -- every `WAIT:` a shipped doc instructs an agent to write must carry a § 1.2 category token. v7.93.0 made the category mandatory and enforced it on `STATE.md` while leaving four shipped docs prescribing category-less WAITs, so an agent obeying its own phase doc produced a state this validator then FAILed (RFC § 1.2, v7.94.0) | Enforced directly by `tools/validate.py` -- walks `phases/` and `extensions/` for prescribed `next_action: WAIT:` values |
| 63 | Human-facing guides teach the current shape -- `tools/validate.py` WARNs when a `guides/GUIDE_*.md` still shows `WAIT: <question>` without § 1.2's category token. WARN, not FAIL: guides are not in the injector manifest and no agent boots from them, but 33 of them went on teaching the pre-v7.93.0 form through two releases that changed it, because nothing looked there. Core owns en/ru/et/ded; the rest are subSaipen translation work (v7.95.0) | Enforced directly by `tools/validate.py` |
| 64 | The portable floor is not more permissive than the canonical validator -- `tools/validate.py` parses RFC § 1.2's required set and § 1.3's read-only bans and asserts `tests/validate.sh` and `tests/validate.ps1` still probe every one. Until v7.96.0 they required 7 of 9 fields and knew none of the bans added in v7.93.0/v7.94.0, so a host without Python got PASS on a state this validator FAILs. The ban half is parsed out of each script's own message, not matched by bare word presence -- the first draft looked for `INIT` anywhere in the file and could never go red, because the phase enum lists it (RFC § 1.2/§ 1.3, v7.96.0) | Enforced directly by `tools/validate.py` -- four desync fixtures, two per script |
| 65 | The two halves of the portable floor agree with each other -- `validate.ps1` parsed `needs: (.*)` to end of line while `validate.sh` used `[^|]*`, so a conformant `\| needs: T-1 \| verify: x` line made the PowerShell script report a dangling reference and FAIL a legal board. Found by running it: this repo's own board tripped it (v7.96.0) | Covered by running both scripts on this repo by hand. Nothing ran `validate.ps1` automatically until v7.97.0 added it to `validate.yml` -- and that workflow fires on `pull_request` only, which this repo never produces, so the pre-commit hook remains the real gate |
| 66 | The CI workflow's trigger is stated honestly next to what it claims to enforce -- `validate.yml` fires on `pull_request`/`workflow_dispatch` only (`c022677` removed `push` deliberately), and this repo has never opened a pull request, so it does not run on the way anything actually ships here. Its header said the opposite ("nothing actually enforced conformance on a push or a PR. This does.") while five releases went out on 2026-07-28 with zero runs. The real gate is the opt-in, bypassable pre-commit hook, and the file now says so (v7.97.0) | Behavioral -- the defect was a comment asserting coverage the trigger cannot provide; no fixture can catch a false claim about a trigger |
| 67 | Hunt mode expands to surfaces outside the drift detector -- subs README claimed "Not yet field-tested" with 4 live subs running; sub `next_action` fields lacked RFC SS 1.2 prefixes; self-transitions skipped all validation; adapter `saipen/` paths were never checked. All fixed in v7.98.0 | Enforced by `tools/validate.py` -- sub next_action checked by prefix/WAIT-category rules; self-transitions validated against known phases; adapter paths checked on save |
| 68 | saiwiki-outbox-cycle scenario -- behavioral fixture demonstrating full subSaipen lifecycle: drift detection -> OUTBOX -> collect -> apply. Validated against real repo history. 43rd fixture (9 executable + 34 behavioral) | Enforced by tools/run_scenarios.py -- all 43 fixtures match declared outcome |
| 69 | A push claim is adjudicated by git -- `STATE.next_action` mentioning a push while commits sit local-only FAILs. v7.98.0 shipped with next_action reading "shipped, committed, pushed. CI green" while three commits, two of them changes to CI itself, had never left the machine; nothing could contradict it because nothing looked. Degrades to WARN with no git or no upstream (RFC § 1.11, v7.99.0) | Enforced directly by `tools/validate.py` |
| 70 | The project's own Python is linted -- `tools/validate.py` had never been, at 1418 lines. The first run found nine cp1251-mangled section signs inside its own FAIL messages: valid UTF-8, so its own U+FFFD corruption check structurally could not see them. The doc text lint now catches that byte sequence too, and CI runs `ruff` (v7.99.0) | Enforced by the `Lint (ruff)` CI step + `ruff.toml`, every disabled rule carrying its reason |
| 71 | subSaipen liveness is visible without opening each board -- a sub with open tickets, zero done and an empty OUTBOX WARNs as spawned-but-never-run (saipython sat that way for a day, identical to a healthy one in MANIFEST.md), and `status: ready` OUTBOX entries WARN as findings waiting on `saipen sub collect` (v7.99.0) | Enforced directly by `tools/validate.py` -- both halves red-tested |
| 72 | A release cannot contradict its own tag -- `release.yml` refuses to publish when `VERSION` or the top `CHANGELOG.md` entry disagrees with the tag name. v7.99.0 was published from a commit still reading 7.98.0, carrying v7.98.0's notes and a VERSION asset saying 7.98.0, because a shell line ran `git tag` after the commit before it had already failed and nothing compared the two (v7.99.0) | Enforced by the `release` workflow; both guards simulated against the offending commit and the correct one |
| 73 | The descriptive schemas are held to the RFC too -- `log.schema.json` must require `event_id` and must not cap `content` length, and `board.schema.json` must describe every ticket field RFC § 1.2 recognises. Both had drifted precisely because they are documented as "unread by any agent": log's schema made [E-###] optional where § 1.2 makes it a MUST, and capped text at 120 characters, which declared 54 of this repo's own 72 live LOG lines invalid. A schema nobody reads is a lie waiting for the first tool built on it (v7.100.0) | Enforced directly by `tools/validate.py` -- three desync fixtures |
| 74 | The OUTBOX status vocabulary agrees three ways -- `extensions/subs/PROTOCOL.md` § 2's table, `outbox.schema.json`'s enum, and `tools/validate.py`'s accepted tuple. All three disagreed until v7.100.0: the table listed four while § 4 and § 9 of the same document told agents to write a fifth (`stale`), and both implementations already accepted it -- the validator even cited "PROTOCOL.md § 2" in its error text while enforcing a superset of what § 2 said. The table is normative for the extension (RFC § 1.9); the other two follow it | Enforced directly by `tools/validate.py` -- desync fixture verified red |
| 75 | The validator works in the layout users actually run -- the injector flattens `RFC.md` to `tools/`'s sibling, and nothing had ever executed that copy. It was broken twice: an unguarded `VERSION` read crashed with a traceback in any consuming project that had run `saipen translate` (VERSION belongs to the SAIPEN home, never to a consuming project), and every cross-document check FAILed with "SAIPEN home clone incomplete" because sibling paths were derived from the development layout alone. CI now installs into a sandbox HOME and runs the copy (v7.100.0) | Enforced by the `Installed layout` CI step |
| 76 | The injector writes paths the reading agent can actually open -- under git bash / MSYS / Cygwin, `pwd` yields `/v/proj/saipen`, which only that shell resolves. The agents that later read `CLAUDE.md` are Windows programs and cannot open it, so a Windows user who ran the `.sh` injector got a block full of dead paths and no error at all. `inject.sh` now converts via `cygpath -w` on those platforms and is untouched elsewhere (v7.100.0) | Verified by installing into a sandbox HOME and confirming the written path opens; both injectors also produce identical 49-file skill copies |
| 77 | `next_action` shape is a FAIL, not a warning -- RFC § 1.2 makes the five-prefix rule a MUST, and the identical check on a subSaipen's STATE had always FAILed, so the protocol was stricter about a read-only worker's state than about the one a cold agent boots from. The vague-phrase regex beside it is a blacklist and evadable by construction: `fix the thing`, `ship it` and `look at the board` all validated clean at exit 0 until v7.100.0, each a state TEST-001 cannot execute | Enforced directly by `tools/validate.py` -- three invalid values FAIL, both legal forms pass |
| 78 | **Known floor gap, stated rather than silent**: `tests/validate.sh`/`.ps1` check only that `next_action:` is present, never that it is executable. A host without Python therefore certifies a state that fails TEST-001. Adding the shape check would be a NEW check, which the floor is frozen against (unlike v7.96.0's corrections, which fixed stale data inside checks that already existed) -- so the limitation is recorded here instead of being closed unilaterally (v7.100.0) | Documented gap; `tools/validate.py` covers it on any host with Python |
| 79 | A fail-fixture proves the failure it names -- `expect: fail` MAY be pinned with `expect_fail_contains: <substring>`, and `tools/run_scenarios.py` reports a fixture that fails for any other reason. Three fixtures (`dependency-cycle`, `dangling-needs-reference`, `read-only-restriction`) carried a control character where `saipen_version: 7` belonged, so every run died on unparseable frontmatter long before reaching the cycle, the dangling reference or the mode ban they exist to prove. The suite stayed green the whole time, because it compared only pass/fail and never why (v7.100.0) | Enforced directly by `tools/run_scenarios.py` -- red-tested with a wrong pin and by reinstating the original corruption |
| 80 | Fixture `next_action` values are executable -- six of the nine executable fixtures shipped a value that is not one of RFC § 1.2's five forms (`plan`, `check verify`, `push release`, `implement feature`, and two free-text sentences). They validated clean only because the prefix rule was a WARN; promoting it to FAIL surfaced all six at once. Each now matches its own fixture's `phase`/`task` (v7.100.0) | Enforced directly by `tools/validate.py` via row above; the suite exercises it every run |
| 81 | No document states a COUNT of RFC § 1.2's required fields, only § 1.2 itself -- `tests/scenarios/checkpoint-self-confirmation/README.md` named a count one short of the real set for eight releases, and the existing no-re-enumeration check could not see it because a count enumerates nothing. The check now covers BOOT, CONFORMANCE and every scenario README, and rejects a *correct* count too: the number belongs to § 1.2, and today's right copy is tomorrow's wrong one (v7.100.0) | Enforced directly by `tools/validate.py` -- red-tested with both a stale and a currently-correct count |
| 82 | Document-level checks walk a document inventory, not a directory. The root `GUIDE.md` is a separate file from `guides/GUIDE_EN.md` and sat outside the `guides/` glob entirely, so it went on teaching the pre-v7.93.0 `WAIT: <question>` shape straight through v7.95.0's sweep of all 33 locale guides. Same shape as the scenario READMEs escaping the no-re-enumeration check: a glob is not an inventory (v7.100.0) | Enforced directly by `tools/validate.py` -- regressing the root guide WARNs |
| 83 | Every shipped document is accounted for -- each one either matches a pattern some check reads, or is listed exempt with a stated reason. Every other check answers "do these two agree?"; none answered "is anything being looked at?", and twice running that was the actual defect (scenario READMEs, then the root `GUIDE.md`, both invisible because a directory glob only sees what is inside it). The first run found 29 unclassified documents, one of them a genuine orphan: a fixture README written to the phantom path `saipen/tests/scenarios/` in `00c7557`, tracked ever since, read by nothing, and carrying an older wording of the MARKHUNT rule than the real fixture at root (v7.100.0) | Enforced directly by `tools/validate.py` -- a new unclassified document FAILs |
| 84 | The checks guarding the protocol's core promises have standing fixtures, not just a hand test from the day they shipped -- `next-action-not-executable`, `checkpoint-missing-required-field`, `done-wait-deadlock`, `tripped-valve-without-wait`, `two-doing-tickets`. `tools/validate.py` has 93 failure paths and three had a fixture behind them; the rest were verified once by hand at best, which is exactly how the LOG timestamp check stayed dead from `feae149` and how the first draft of the portable-floor check could never go red. A hand test proves a check works once; a fixture proves it still works (v7.100.0) | Enforced by `tools/run_scenarios.py` -- each pins its reason, so it cannot pass by failing at something unrelated |
| 85 | `DATE` is mandatory on new LOG entries -- `LOG_RE` had always accepted a dateless line while RFC § 1.2 lists DATE as a mandatory slot, so such a line passed the skeleton check and contributed no timestamp at all. FAIL in the active log, where the writer can still fix it; WARN, collapsed to one finding, for the 125 dateless entries sealed in `LOG-001`, which append-only forbids rewriting (v7.100.0) | Enforced directly by `tools/validate.py` -- red-tested with a dateless entry appended to the active log |
| 86 | The timestamp harvest is never silently empty -- LOG entries with zero parseable timestamps FAIL, because the inversion and future-timestamp checks below both iterate over that harvest and would otherwise pass by looping over nothing. This is the precise shape of the check that lay dead from `feae149` to v7.99.0, generalised into a guard (v7.100.0) | Enforced directly by `tools/validate.py` -- reachability proved with a scratch project whose entire history is sealed and dateless |
| 87 | Every check in the portable floor is proved still able to go red -- `tools/audit_floor.py` builds a known-good scratch project, breaks it one way per check, and asserts `tests/validate.sh` names that specific failure. All 20 pass. The floor's checks had been hand-verified when they shipped and nothing kept them verified, which is exactly how the LOG timestamp check lay dead in `validate.py` from `feae149` to v7.99.0. The PowerShell half is out of scope and stated so: CI proves it runs clean on a conformant project, not that its individual checks still fire (v7.100.0) | Enforced by the `Portable floor still goes red` CI step -- red-tested by rewording one floor message |
| 88 | Both halves of the portable floor are audited, not just the shell one -- `tools/audit_floor.py` runs all 20 mutations against `validate.sh` and `validate.ps1`. Auditing the PowerShell half found a divergence on its first run: the two reported the same defect in different words (`cyclic needs: dependencies involving: ...` versus `cyclic dependencies`), so a human comparing platforms had to know which half produced the line. Aligned. The previous release had stated this gap as out of scope; it is now closed rather than documented (v7.100.0) | Enforced by the `Portable floor still goes red` CI step; SKIP, never PASS, where an interpreter is absent |
| 89 | Citations resolve -- every `§ N.N` and `phases/<name>.md` reference in a shipped document must name something that exists. Sections get renumbered and phase docs get renamed, and neither leaves a mark on the citing document: the pointer rots into a reference to a rule that is gone while the sentence around it still reads correctly. 103 documents scanned, zero dangling at the time this shipped, which is the point -- it stays that way now (v7.101.0) | Enforced directly by `tools/validate.py` -- red-tested with a dead section, a dead phase doc, and a renumber of § 1.11 that orphaned 7 real citations |
| 90 | `.saipen/KNOWLEDGE/` is checked, not blanketed as project data -- RFC § 1.2 makes it durable truth an agent reads before planning, so it is agent-facing text and belongs under the citation, field-count and coverage checks. The v7.100.0 coverage inventory excluded all of `.saipen/` in one stroke, and that exemption had been hiding stale rules: `traps.md` and `decisions.md` both taught that a `WAIT:` at DONE is legal only when a `[MARKHUNT]` ticket blocks the auto-proceed, superseded nine releases earlier by § 1.2's two fixed forms, and `traps.md` described the LOG drift check as a WARN when it is a FAIL and its WARN half had been deleted as dead (v7.101.0) | Enforced directly by `tools/validate.py` -- three red tests against `traps.md` |

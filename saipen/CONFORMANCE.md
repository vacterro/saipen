# SAIPEN Conformance

Implementations MUST pass self-check across three vectors:
1. **Repo Validation**: `STATE.md`, `BOARD.md`, `LOG.md`, and `KNOWLEDGE/` MUST conform to the shapes RFC.md § 1.2 defines, enforced by `tools/validate.py` (canonical -- validates STATE.md against `extensions/schemas/state.schema.json` directly, plus graph checks: E-### uniqueness/monotonicity, parent resolution), with `tests/validate.sh` / `validate.ps1` as the frozen portable floor for hosts without Python. `board.schema.json`/`log.schema.json` remain descriptive reference -- see `extensions/schemas/README.md`.
2. **Session Validation**: `BOARD.md`'s `needs:` graph MUST be acyclic AND every reference MUST resolve to a ticket that actually exists on the board -- a dangling reference is worse than a cycle, since nothing else catches it. `LOG.md` graph parent-child links MUST resolve.
3. **Phase Contract Validation**: Stated `mode` MUST legally permit the current `phase` (RFC.md § 1.3). `phase` MUST be one of the sixteen enum values RFC.md § 1.6 defines, and any phase-to-phase transition MUST be legal per § 1.6's transition table.

## TEST-001: The Continuation Test
Any release of this protocol MUST pass the gold standard test:
1. Cold agent (zero chat history).
2. Execute `saipen continue` (or equivalent bootstrap command).
3. Agent MUST: read `next_action` and execute it instantly WITHOUT asking for context.
If the agent asks "What should I do?", the protocol has failed. A `next_action: WAIT: <specific question>` (RFC.md § 1.2) does NOT fail this test -- asking one exact, pre-determined question instantly is the executable action; the failure mode is vague context-seeking, not a specific authorization gate. This includes the bootstrap `WAIT:` `INIT` produces on a brand-new project (RFC § 1.2's fifth `WAIT:` category, `phases/init.md`) -- asking for the first project goal or raw backlog is the same kind of specific question, not vague context-seeking, and doesn't fail this test either: nothing in `STATE`/`BOARD`/`LOG` could have answered it instead, since none of them have any history yet.

## Scenario Coverage
`tests/scenarios/` holds one fixture directory per concept below. Behavioral
ones are README-only where the assertion is about agent decision-making, not
file shape -- there is nothing to validate mechanically. "Covered by" names
the actual fixture; entries with no fixture state why, not silently.

Note: row IDs were renumbered after the duplicate 43 incident in v7.86.0.
Current IDs are canonical; old changelog references remain historical.

| # | Concept | Covered by |
|---|---------|------------|
| 1 | Cold continuation | This section (TEST-001 above) + this repo's own live `.saipen/` -- no separate fixture adds anything a dedicated one wouldn't just duplicate. |
| 2 | Corrupt STATE recovery | `resume-after-crash`, `stale-state-reconciliation` |
| 3 | Dependency cycle | `dependency-cycle` |
| 4 | Dangling `needs:` reference | `dangling-needs-reference` |
| 5 | Stale claim forfeiture | `multi-agent-claim-conflict` |
| 6 | Goal counter crash recovery | `goal-counter-recovery` |
| 7 | Manual-verify gate | `manual-verify-fallback` |
| 8 | No-publish restriction -- git-dependent *steps* are skipped (no commit/tag/push, never a false "pushed"), but `SHIP` itself stays reachable and still closes the ticket to `DONE` (RFC § 1.3, `phases/ship.md`'s no-publish branch, v7.66.0) | `no-git-ship-denial` -- behavioral, README-only (folder name predates v7.66.0, when this asserted the opposite) |
| 9 | Read-only restriction | `read-only-restriction` |
| 10 | Board-empty maintenance transition | `board-empty-maintenance-transition` -- behavioral, no fixture |
| 11 | Goal objective exit | N/A -- `goal_exit` was evaluated and rejected three times (recorded in `.saipen/KNOWLEDGE/decisions.md` and CHANGELOG v7.13.1/v7.19.1); `goal_mode` never exits on board-empty, so there is no "objective exit" behavior to test |
| 12 | Extension absence does not block | `extension-absence` |
| 13 | Unresolved LOG parent | Enforced by `tools/validate.py` since v7.24.0 (parent must resolve to an earlier `E-###`; IDs unique + monotonic). The old excuse -- historical numbering resets a naive resolver would misflag -- died with v7.24.0's user-approved ledger repair; this repo's own LOG.md now passes the check it once couldn't. |
| 14 | Invalid phase transition | `invalid-phase-transition` -- automated via `transition_from` in STATE.md; `tools/validate.py` checks every non-self transition against RFC § 1.6's table (v7.82.0) |
| 15 | Mode-phase restrictions -- `read-only` MUST NOT enter `INIT`/`PLAN`/`ADD`/`BUILD`/`SHIP`/`CLEAN`/`TRANSLATE` (RFC § 1.3 -- every phase whose work product is a file write; `INIT`/`PLAN` added v7.93.0, `ADD` v7.94.0, whose entire output is tickets), the only such ban still live since v7.66.0 freed `no-publish` + `SHIP` | `read-only-restriction` (fails as intended); `invalid-mode-phase-combination` is now the inverse regression guard -- it asserts `no-publish` + `SHIP` stays legal, since re-adding that ban already shipped once |
| 16 | Ticket-level BLOCKED (non-cycle failure), work continues | `blocked-ticket` |
| 17 | Fresh INIT bootstrap from `templates/` | `fresh-init` -- behavioral, no `.saipen/` yet by definition (that's what INIT creates) |
| 18 | Evolutionary ADD feature symmetry (§ 2.2) | `add-feature-symmetry` -- behavioral, no fixture |
| 19 | Unclaimed DOING adoption (RFC § 1.4, v7.28.0) | `unclaimed-doing-adoption` -- behavioral, README-only |
| 20 | Clean tree after ticket-level BLOCKED (`phases/verify.md`, v7.27.0) | `blocked-ticket-clean-tree` -- behavioral, README-only |
| 21 | Dirty tree on continuation (RFC § 1.5, v7.29.0) | `dirty-tree-continuation` -- behavioral, README-only |
| 22 | Parallel TRANSLATE doesn't stomp shared STATE (RFC § 2.1, `phases/translate.md`, v7.34.0) | `parallel-translate-isolation` -- behavioral, README-only |
| 23 | Dual-location extension conflict resolved, never merged/guessed (RFC § 1.9, v7.36.0) | `extension-dual-location-conflict` -- behavioral, README-only |
| 24 | Dual-location TRANSLATE conflict resolved, never merged/guessed (RFC § 2.1, v7.37.0) | `translate-dual-location-conflict` -- behavioral, README-only |
| 25 | `saipen sub spawn` / parallel TRANSLATE both refuse on a project with no `.saipen/` yet, instead of silently triggering INIT (RFC § 1.9 v7.36.0, § 2.1 v7.37.0) | `spawn-requires-init` -- behavioral, README-only |
| 26 | HUNT skip requires an exact HEAD-hash match; mtime/diff-timing is never a substitute for actually running the six categories (`phases/hunt.md`, v7.41.0) | `hunt-skip-requires-exact-hash-match` -- behavioral, README-only |
| 27 | MARKHUNT records findings without fixing/capping, unlike HUNT (`phases/markhunt.md`) | `markhunt-dry-record-only` -- behavioral, README-only |
| 28 | VERIFY hysteresis: a ticket blocked a second time escalates to session-level `BLOCKED` instead of spending a fresh retry budget (`phases/verify.md`) | `verify-hysteresis-second-block` -- behavioral, README-only |
| 29 | SubSaipen `STATE.md` shape + policy (schema-valid, `mode: read-only`, no write-requiring `phase`) checked by `tools/validate.py`, not just the main project's own `STATE.md` (`extensions/subs/PROTOCOL.md` § 1/§ 8, v7.49.0) | Enforced directly by `tools/validate.py` -- no fixture; the repo's four live instances exercise the pass case every run. The walk also covers the shipped library at `extensions/subs/`, which holds only `TEMPLATE/` since v7.84.0, so that half now guards against instance state reappearing there -- the intended end state, not a dead check |
| 30 | `read-only` reaches `MARKHUNT`/`PREPARE`/`VALIDATE` only in report-only form -- findings/handoff/corruption are reported in chat, never written (no `## BLOCKED` tickets, no `kitchen/`, no structural repair) (RFC § 1.3) | `read-only-audit-phases` -- behavioral, README-only |
| 31 | `stop` preserves the goal counters; `saipen continue`/bare `saipen` resume them unchanged, but bare `saipen goal` deliberately resets `goal_waves`/`goal_tickets` to `0` for a fresh valve budget (RFC § 1.10 / § 2.4) | `stop-goal-counter-semantics` -- behavioral, README-only |
| 32 | `goal_waves` is not double-counted when `ADD` RETURNs to `PLAN` -- the cycle's wave is counted once at ADD's RETURN, and the follow-on `PLAN` skips its own increment for that same cycle (RFC § 2.4, `phases/plan.md`/`add.md`) | `goal-waves-no-double-count` -- behavioral, README-only |
| 33 | A fixer-type subSaipen (saipython) emits a tested patch through OUTBOX, having cloned targets into its own `kitchen/pen/` and verified there in phase `VERIFY` -- it still never writes the main tree and never enters `BUILD`/`SHIP` (`mode: read-only`, enforced by `tools/validate.py`); the main agent applies the patch through Core gates (`extensions/subs/PROTOCOL.md` § 9) | `fixer-subsaipen-patch-outbox` -- behavioral, README-only |
| 34 | Cold continuation via `saipen/BOOT.md` -- the compact kernel reads STATE -> BOARD -> active LOG tail, executes `next_action`, and loads the phase doc on demand without pulling the full RFC; TEST-001 passes on BOOT + STATE + BOARD + tail alone (`saipen/BOOT.md`, RFC § 1.1) | `boot-cold-start-kernel` -- behavioral, README-only |
| 35 | `human_note:` optional STATE field -- a one-line human nudge read on continue (`BOOT.md`'s `human_note` step) and cleared once applied; never a ticket or goal, and one that implies work becomes a real `TODO` instead of living in `STATE.md` (RFC § 1.2) | `human-note-nudge` -- behavioral, README-only |
| 36 | Human digest -- `saipen ship` and `saipen stop` (over)write `.saipen/kitchen/digest.md` (done/remaining/awaiting, three lines) so the human reads one snapshot instead of scrolling `LOG.md` (`phases/ship.md`, RFC § 1.10) | `human-digest-on-exit` -- behavioral, README-only |
| 37 | MARKHUNT closure self-test -- completion is verified against its manifest (`vectors:` 5/5, `cursor: done`, `head_end` == current HEAD, `findings:` == the `[MARKHUNT]` tickets written) before it may reach `DONE`; the enriched completion line (`N findings, V/5 vectors, @head`) keeps coverage auditable after `kitchen/` is swept -- the checkable closure HUNT gets from its hash line, which MARKHUNT previously lacked (`phases/markhunt.md`, closes T-136) | `markhunt-closure-self-test` -- behavioral, README-only |
| 38 | LOG segmentation -- `E-###` stays globally unique and monotonic, and `[parent: E-###]` resolves, across sealed `.saipen/logs/LOG-<NNN>.md` segments plus the active `LOG.md` treated as one sequence, not just within whichever file is currently open (RFC § 1.2, v7.53.0) | Enforced directly by `tools/validate.py` -- no separate fixture, this repo's own live `.saipen/LOG.md` plus every sealed `.saipen/logs/LOG-*.md` segment already exercise the pass case every run |
| 39 | Shipped-library integrity -- `tools/validate.py` checks the subSaipen states in `extensions/subs/` (what `saipen sub spawn` copies and the injector distributes), not only this project's live `.saipen/extensions/subs/` instances; a concrete `saipen_home` in a shipped template is a FAIL, since that file reaches every user (v7.72.0) | Enforced directly by `tools/validate.py` -- the repo's own three library examples plus its live instance exercise it every run |
| 40 | Board soft cap -- `BOARD.md` is read on every cold start (RFC § 1.1, `BOOT.md` step 2), so growth past ~16 KB WARNs and points at `phases/clean.md`'s board scrub. No sealing machinery, unlike `LOG.md`: the board is prunable rather than append-only, and closed tickets already live permanently in `LOG.md`/`CHANGELOG.md` (RFC § 1.2, v7.72.0) | Enforced directly by `tools/validate.py` -- caught this repo's own 28 KB board on the run that introduced it |
| 41 | `saipen status` answers the question actually being asked -- alongside phase/ticket/queue it reports what is waiting on a human, what is claimed but unproven, the last recorded conformance result, and staleness; and it states facts without pronouncing the project healthy or ready (RFC § 1.10, v7.82.0) | `status-answers-the-real-question` -- behavioral, README-only |
| 42 | LOG timestamp UTC sanity -- two faults, two severities (RFC § 1.2): **>3h in the future** is a FAIL (a wrong clock, inherited by every later event); **>5min earlier than the previous event** is a WARN unless a later `DEC` documents the drift. The check that collapsed both into one WARN via `abs()` had never fired at all -- its regex did not match a LOG line (removed v7.93.0) | Enforced directly by `tools/validate.py` |
| 43 | Reserved -- superseded by row 54 in v7.93.0. This row described the same state at the old WARN severity and named only one legal `WAIT:`, so the table asserted two different verdicts about one board. Row 54 is the live rule: FAIL, and both § 1.2 fixed wordings allowed | N/A -- see row 54 |



| 44 | Checkpoint self-confirmation -- after writing `STATE.md` the agent re-reads it and confirms **every field in RFC § 1.2's required set** survived. This row deliberately does not restate the set (it said "all eight" while the set was nine). A checkpoint missing `next_action` fails TEST-001 outright and is not a checkpoint (RFC § 1.5) | `checkpoint-self-confirmation` -- behavioral, README-only |
| 45 | Returning agent distrusts its own memory -- before acting on anything it believes it already knows, it compares `STATE.md`'s `agent:` and `updated:` against itself; either mismatch means recollection is stale and the files win (RFC § 1.1, `BOOT.md`, v7.85.0) | `returning-agent-stale-memory` -- behavioral, README-only |
| 46 | A tripped safety valve is a pause, not an exit -- `goal_mode` stays `true`, the at/over-cap counters ARE the tripped state, a resuming agent re-states the stop instead of continuing, and bare `saipen goal` resetting them to `0` is the re-authorization (RFC § 2.4, v7.86.0) | `safety-valve-is-a-pause` -- behavioral, README-only |
| 47 | Determinism invariants (RFC § 1.11) -- fixed action priority RECOVER > UNBLOCK > FINISH > START > MAINTAIN; at most one `## DOING` ticket in total in Core (§ 1.11; "per agent" until v7.93.0, which invited the reading "that ticket is not mine, so I may claim a second"); every session leaves a LOG/BOARD/STATE/project-file trace or says it did nothing; insufficient information is a `WAIT:`, never a guess (v7.86.0) | `determinism-invariants` -- behavioral, README-only |
| 48 | A subSaipen short of a fact writes `status: blocked` with the missing fact in `details`, never a guessed `ready` -- RFC § 1.11's stop-don't-guess rule translated to the one channel a sub actually has, since it has no `WAIT:` a human reads (`extensions/subs/PROTOCOL.md` § 2, v7.88.0) | `subsaipen-blocked-not-guessed` -- behavioral, README-only |
| 49 | `next_action` vocabulary, not just shape -- a value starting `saipen ` must name a command RFC § 1.10 actually defines; `saipen hunt` passes every shape check while naming a phase reached only autonomously (§ 2.1), so a cold agent must decline it and TEST-001 fails on a state that looked valid (`tools/validate.py`, v7.89.0) | Enforced directly by `tools/validate.py` -- caught this repo's own live `STATE.md` on the run that introduced it |
| 50 | One ticket at a time -- `tools/validate.py` FAILs a `BOARD.md` carrying more than one `## DOING` ticket. RFC § 1.11 shipped this as prose in v7.86.0 with nothing enforcing it, which is precisely the ticket-hopping it exists to stop (v7.90.0) | Enforced directly by `tools/validate.py` |
| 51 | Goal counters leave a countable trace -- a non-zero `goal_waves`/`goal_tickets` under `goal_mode: true` with no matching `DEC: goal_<counter> N->M` line in any LOG segment WARNs, since § 1.5 Recovery rebuilds those counters by counting exactly those lines (RFC § 2.4, v7.90.0) | Enforced directly by `tools/validate.py` -- WARN not FAIL, since pre-v7.87.0 states legitimately lack the lines |
| 52 | MARKHUNT findings carry their evidence -- a `[MARKHUNT]` ticket whose `| blocker:` says only "unvetted audit" with no file:line or command output FAILs. "No cite, no ticket" (`phases/markhunt.md`) is what stops a dry audit becoming a generator of confident vibes; unenforced until v7.91.0 | Enforced directly by `tools/validate.py` |
| 53 | subSaipen OUTBOX entries are well-formed -- a recognized `status:`, and on `ready` a `summary:` and `critical:`; an entry carrying a `patch:` additionally needs `base_head:` and `verified:` (§ 9), since a diff with no provenance cannot be re-checked before it is applied. OUTBOX is the only channel out of a subSaipen and nothing validated it until v7.91.0 | Enforced directly by `tools/validate.py` |
| 54 | Deadlocked board FAILs instead of warning -- `phase: DONE` + empty `## TODO` + a `WAIT:` that is neither the § 2.4 safety valve nor `WAIT: user brake -- <reason>` is non-conformant: § 2.1 requires that exact state to auto-transition to `HUNT`. Both legal WAITs carry fixed wording precisely so a machine can tell a real pause from drift (RFC § 1.2, § 1.11) | Enforced directly by `tools/validate.py` |
| 55 | Tripped safety valve has one legal shape -- `goal_mode: true` with `goal_waves >= 3` or `goal_tickets >= 20` MUST carry the § 1.2 safety-valve `WAIT:` in `next_action`, and MUST NOT set `phase: BLOCKED` (a § 2.4 Exit condition, which would clear `goal_mode` and make the bare `saipen goal` the WAIT line prescribes illegal under § 1.10 -- the valve destroying its own continuation path). Prose since v7.86.0, unchecked until v7.92.0 (RFC § 2.4) | Enforced directly by `tools/validate.py` |
| 56 | BOARD section headings appear exactly once each -- duplicate `## DOING`/`## TODO`/`## DONE`/`## BLOCKED` splits the work surface even when every ticket ID stays unique, and the Pick Rule then reads only whichever copy it reached first (RFC § 1.2). Checked since v7.86.0, never listed here until v7.93.0 | Enforced directly by `tools/validate.py` |
| 57 | `WAIT:` carries a category token from a closed set of seven (`manual-verify`, `destructive-op`, `first-publish`, `user brake`, `blocked`, `safety valve`, `init`). A vague `WAIT: need more context` is shape-identical to a real gate; the token is what separates them mechanically, and tells the human what kind of answer unblocks it (RFC § 1.2) | Enforced directly by `tools/validate.py` -- two vague fixtures FAIL, all seven categories pass |
| 58 | Cross-document drift is detected, not found by hand -- `tools/validate.py` parses the core copied sets out of RFC.md (required fields, phase enum, from-any-phase transition set, read-only bans, `next_action` prefixes, `WAIT` categories) and later rows extend that same pattern to command and ticket-field vocabularies. It FAILs when any other copy disagrees, or when BOOT/CONFORMANCE/scenario prose re-enumerates a moving rule where only RFC should own it. A moved RFC anchor is itself a FAIL: a drift check that stops checking still prints PASS (RFC § 1.1) | Enforced directly by `tools/validate.py` -- desync fixtures plus live doc sweeps for stale re-enumerations |
| 59 | Checkbox agrees with section -- `[x]` only under `## DONE`, `[/]` only under `## DOING`, `[ ]` only under `## TODO`/`## BLOCKED`. The section is the status; a board where the two disagree answers "is this done" differently depending on which the reader trusts (RFC § 1.2). WARN on two of the three cases until v7.93.0, the third unchecked | Enforced directly by `tools/validate.py` |
| 60 | A write is confirmed by reading it back -- all three checkpoint files, not just `STATE.md`. A pattern substitution whose anchor missed left `BOARD.md` unchanged through eleven tickets while the validator stayed green, since an empty `## TODO` is perfectly conformant (RFC § 1.5, v7.93.0) | `write-confirmed-by-readback` -- behavioral, README-only; the failure is an agent trusting its own success message, which no file shape can reveal |
| 61 | A recognized command is not a legal transition -- `saipen ship` is accepted from any phase (§ 1.10), but `phase: SHIP` is enterable only from `REVIEW`, which is the transition table's single route into it. The from-any-phase *transition* set is six phases; `SHIP` sat in it from v7.83.0 (validator) and v7.92.0 (RFC text) until v7.94.0, so one half of the protocol said "run the gates" while the other made the jump legal (RFC § 1.6, § 1.10) | Enforced directly by `tools/validate.py` -- INIT->SHIP FAILs, REVIEW->SHIP passes, `next_action: saipen ship` stays legal vocabulary |
| 62 | A rule reaches the docs that emit it -- every `WAIT:` a shipped doc instructs an agent to write must carry a § 1.2 category token. v7.93.0 made the category mandatory and enforced it on `STATE.md` while leaving four shipped docs prescribing category-less WAITs, so an agent obeying its own phase doc produced a state this validator then FAILed (RFC § 1.2, v7.94.0) | Enforced directly by `tools/validate.py` -- walks `phases/` and `extensions/` for prescribed `next_action: WAIT:` values |
| 63 | Human-facing guides teach the current shape -- `tools/validate.py` WARNs when a `guides/GUIDE_*.md` still shows `WAIT: <question>` without § 1.2's category token. WARN, not FAIL: guides are not in the injector manifest and no agent boots from them, but 33 of them went on teaching the pre-v7.93.0 form through two releases that changed it, because nothing looked there. Core owns en/ru/et/ded; the rest are subSaipen translation work (v7.95.0) | Enforced directly by `tools/validate.py` |
| 64 | The portable floor is not more permissive than the canonical validator on RFC § 1.2's required fields or § 1.3's read-only bans. Until v7.96.0 it required 7 of 9 fields and knew none of the later bans, so a host without Python got PASS on a state the canonical validator rejected. The first guard searched script text and even its first draft could never go red because it matched phase names in an unrelated enum. The audit now derives both closed sets from RFC.md, removes each required field and enters every banned phase in turn, and executes both floor implementations | Enforced behaviorally by `tools/audit_floor.py`; source formatting and error-message layout are not evidence |
| 65 | The two halves of the portable floor agree with each other -- `validate.ps1` parsed `needs: (.*)` to end of line while `validate.sh` used `[^|]*`, so a conformant `\| needs: T-1 \| verify: x` line made the PowerShell script report a dangling reference and FAIL a legal board. Found by running it: this repo's own board tripped it (v7.96.0) | Enforced by `tools/audit_floor.py` executing every mutation against both halves and by CI running both scripts on push and pull request |
| 66 | The CI workflow's trigger is stated honestly next to what it claims to enforce -- `validate.yml` fires on `pull_request`/`workflow_dispatch` only (`c022677` removed `push` deliberately), and this repo has never opened a pull request, so it does not run on the way anything actually ships here. Its header said the opposite ("nothing actually enforced conformance on a push or a PR. This does.") while five releases went out on 2026-07-28 with zero runs. The real gate is the opt-in, bypassable pre-commit hook, and the file now says so (v7.97.0) | Behavioral -- the defect was a comment asserting coverage the trigger cannot provide; no fixture can catch a false claim about a trigger |
| 67 | Hunt mode expands to surfaces outside the drift detector -- subs README claimed "Not yet field-tested" with 4 live subs running; sub `next_action` fields lacked RFC SS 1.2 prefixes; self-transitions skipped all validation; adapter `saipen/` paths were never checked. All fixed in v7.98.0 | Enforced by `tools/validate.py` -- sub next_action checked by prefix/WAIT-category rules; self-transitions validated against known phases; adapter paths checked on save |
| 68 | saiwiki-outbox-cycle scenario -- behavioral fixture demonstrating full subSaipen lifecycle: drift detection -> OUTBOX -> collect -> apply. Validated against real repo history. 43rd fixture (9 executable + 34 behavioral) | Enforced by tools/run_scenarios.py -- all 43 fixtures match declared outcome |
| 69 | A push claim is adjudicated by git -- `STATE.next_action` mentioning a push while commits sit local-only FAILs. v7.98.0 shipped with next_action reading "shipped, committed, pushed. CI green" while three commits, two of them changes to CI itself, had never left the machine; nothing could contradict it because nothing looked. Degrades to WARN with no git or no upstream (RFC § 1.11, v7.99.0) | Enforced directly by `tools/validate.py` |
| 70 | The project's own Python is linted -- `tools/validate.py` had never been, at 1418 lines. The first run found nine cp1251-mangled section signs inside its own FAIL messages: valid UTF-8, so its own U+FFFD corruption check structurally could not see them. The doc text lint now catches that byte sequence too, and CI runs `ruff` (v7.99.0) | Enforced by the `Lint (ruff)` CI step + `ruff.toml`, every disabled rule carrying its reason |
| 71 | subSaipen liveness is visible without opening each board -- a sub with open tickets, zero done and an empty OUTBOX WARNs as spawned-but-never-run (saipython sat that way for a day, identical to a healthy one in MANIFEST.md), and `status: ready` OUTBOX entries WARN as findings waiting on `saipen sub collect` (v7.99.0) | Enforced directly by `tools/validate.py` -- both halves red-tested |
| 72 | A release cannot contradict its own tag -- `release.yml` refuses to publish when `VERSION` or the top `CHANGELOG.md` entry disagrees with the tag name. v7.99.0 was published from a commit still reading 7.98.0, carrying v7.98.0's notes and a VERSION asset saying 7.98.0, because a shell line ran `git tag` after the commit before it had already failed and nothing compared the two (v7.99.0) | Enforced by the `release` workflow; both guards simulated against the offending commit and the correct one |
| 73 | The descriptive schemas are held to the RFC too -- `log.schema.json` must require `event_id` and must not cap `content` length, and `board.schema.json` must describe every ticket field RFC § 1.2 recognises. Both had drifted precisely because they are documented as "unread by any agent": log's schema made [E-###] optional where § 1.2 makes it a MUST, and capped text at 120 characters, which declared 54 of this repo's own 72 live LOG lines invalid. A schema nobody reads is a lie waiting for the first tool built on it (v7.101.0) | Enforced directly by `tools/validate.py` -- three desync fixtures |
| 74 | The OUTBOX status vocabulary agrees three ways -- `extensions/subs/PROTOCOL.md` § 2's table, `outbox.schema.json`'s enum, and `tools/validate.py`'s accepted tuple. All three disagreed until v7.101.0: the table listed four while § 4 and § 9 of the same document told agents to write a fifth (`stale`), and both implementations already accepted it -- the validator even cited "PROTOCOL.md § 2" in its error text while enforcing a superset of what § 2 said. The table is normative for the extension (RFC § 1.9); the other two follow it | Enforced directly by `tools/validate.py` -- desync fixture verified red |
| 75 | The validator works in the layout users actually run -- the injector flattens `RFC.md` to `tools/`'s sibling, and nothing had ever executed that copy. It was broken twice: an unguarded `VERSION` read crashed with a traceback in any consuming project that had run `saipen translate` (VERSION belongs to the SAIPEN home, never to a consuming project), and every cross-document check FAILed with "SAIPEN home clone incomplete" because sibling paths were derived from the development layout alone. CI now installs into a sandbox HOME and runs the copy (v7.101.0) | Enforced by the `Installed layout` CI step |
| 76 | The injector writes paths the reading agent can actually open -- under git bash / MSYS / Cygwin, `pwd` yields `/v/proj/saipen`, which only that shell resolves. The agents that later read `CLAUDE.md` are Windows programs and cannot open it, so a Windows user who ran the `.sh` injector got a block full of dead paths and no error at all. `inject.sh` now converts via `cygpath -w` on those platforms and is untouched elsewhere (v7.101.0) | Verified by installing into a sandbox HOME and confirming the written path opens; both injectors also produce identical 49-file skill copies |
| 77 | `next_action` shape is a FAIL, not a warning -- RFC § 1.2 makes the five-prefix rule a MUST, and the identical check on a subSaipen's STATE had always FAILed, so the protocol was stricter about a read-only worker's state than about the one a cold agent boots from. The vague-phrase regex beside it is a blacklist and evadable by construction: `fix the thing`, `ship it` and `look at the board` all validated clean at exit 0 until v7.101.0, each a state TEST-001 cannot execute | Enforced directly by `tools/validate.py` -- three invalid values FAIL, both legal forms pass |
| 78 | **The floor's coverage is measured, not guessed.** This row used to name ONE known gap -- `next_action` presence without executability. Applying `tools/audit_checks.py`'s mutation table to both tools put a number on it: the portable floor catches **11 of 41** defects `tools/validate.py` catches. The gap is by design (the floor is frozen against new checks, and a `grep` cannot parse a transition table), but stating one omission where there are 28 told a Python-less host it was nearly as protected as it was not. `tools/audit_parity.py` keeps the number honest and FAILs if the floor drops BELOW its baseline -- a floor getting quietly weaker is the failure worth guarding, not the gap (v7.120.0) | Enforced by the `Floor parity` CI step -- red-tested by disabling one floor check |
| 79 | A fail-fixture proves the failure it names -- `expect: fail` MAY be pinned with `expect_fail_contains: <substring>`, and `tools/run_scenarios.py` reports a fixture that fails for any other reason. Three fixtures (`dependency-cycle`, `dangling-needs-reference`, `read-only-restriction`) carried a control character where `saipen_version: 7` belonged, so every run died on unparseable frontmatter long before reaching the cycle, the dangling reference or the mode ban they exist to prove. The suite stayed green the whole time, because it compared only pass/fail and never why (v7.101.0) | Enforced directly by `tools/run_scenarios.py` -- red-tested with a wrong pin and by reinstating the original corruption |
| 80 | Fixture `next_action` values are executable -- six of the nine executable fixtures shipped a value that is not one of RFC § 1.2's five forms (`plan`, `check verify`, `push release`, `implement feature`, and two free-text sentences). They validated clean only because the prefix rule was a WARN; promoting it to FAIL surfaced all six at once. Each now matches its own fixture's `phase`/`task` (v7.101.0) | Enforced directly by `tools/validate.py` via row above; the suite exercises it every run |
| 81 | No document states a COUNT of RFC § 1.2's required fields, only § 1.2 itself -- `tests/scenarios/checkpoint-self-confirmation/README.md` named a count one short of the real set for eight releases, and the existing no-re-enumeration check could not see it because a count enumerates nothing. The check now covers BOOT, CONFORMANCE and every scenario README, and rejects a *correct* count too: the number belongs to § 1.2, and today's right copy is tomorrow's wrong one (v7.101.0) | Enforced directly by `tools/validate.py` -- red-tested with both a stale and a currently-correct count |
| 82 | Document-level checks walk a document inventory, not a directory. The root `GUIDE.md` is a separate file from `guides/GUIDE_EN.md` and sat outside the `guides/` glob entirely, so it went on teaching the pre-v7.93.0 `WAIT: <question>` shape straight through v7.95.0's sweep of all 33 locale guides. Same shape as the scenario READMEs escaping the no-re-enumeration check: a glob is not an inventory (v7.101.0) | Enforced directly by `tools/validate.py` -- regressing the root guide WARNs |
| 83 | Every shipped document is accounted for -- each one either matches a pattern some check reads, or is listed exempt with a stated reason. Every other check answers "do these two agree?"; none answered "is anything being looked at?", and twice running that was the actual defect (scenario READMEs, then the root `GUIDE.md`, both invisible because a directory glob only sees what is inside it). The first run found 29 unclassified documents, one of them a genuine orphan: a fixture README written to the phantom path `saipen/tests/scenarios/` in `00c7557`, tracked ever since, read by nothing, and carrying an older wording of the MARKHUNT rule than the real fixture at root (v7.101.0) | Enforced directly by `tools/validate.py` -- a new unclassified document FAILs |
| 84 | The checks guarding the protocol's core promises have standing fixtures, not just a hand test from the day they shipped -- `next-action-not-executable`, `checkpoint-missing-required-field`, `done-wait-deadlock`, `tripped-valve-without-wait`, `two-doing-tickets`. `tools/validate.py` has 93 failure paths and three had a fixture behind them; the rest were verified once by hand at best, which is exactly how the LOG timestamp check stayed dead from `feae149` and how the first draft of the portable-floor check could never go red. A hand test proves a check works once; a fixture proves it still works (v7.101.0) | Enforced by `tools/run_scenarios.py` -- each pins its reason, so it cannot pass by failing at something unrelated |
| 85 | `DATE` is mandatory on new LOG entries -- `LOG_RE` had always accepted a dateless line while RFC § 1.2 lists DATE as a mandatory slot, so such a line passed the skeleton check and contributed no timestamp at all. FAIL in the active log, where the writer can still fix it; WARN, collapsed to one finding, for the 125 dateless entries sealed in `LOG-001`, which append-only forbids rewriting (v7.101.0) | Enforced directly by `tools/validate.py` -- red-tested with a dateless entry appended to the active log |
| 86 | The timestamp harvest is never silently empty -- LOG entries with zero parseable timestamps FAIL, because the inversion and future-timestamp checks below both iterate over that harvest and would otherwise pass by looping over nothing. This is the precise shape of the check that lay dead from `feae149` to v7.99.0, generalised into a guard (v7.101.0) | Enforced directly by `tools/validate.py` -- reachability proved with a scratch project whose entire history is sealed and dateless |
| 87 | Every check in the portable floor is proved still able to go red -- `tools/audit_floor.py` builds a known-good scratch project, breaks it one way per check, and asserts `tests/validate.sh` names that specific failure. All 20 pass. The floor's checks had been hand-verified when they shipped and nothing kept them verified, which is exactly how the LOG timestamp check lay dead in `validate.py` from `feae149` to v7.99.0. The PowerShell half is out of scope and stated so: CI proves it runs clean on a conformant project, not that its individual checks still fire (v7.101.0) | Enforced by the `Portable floor still goes red` CI step -- red-tested by rewording one floor message |
| 88 | Both halves of the portable floor are audited, not just the shell one -- `tools/audit_floor.py` runs every mutation, including the RFC-derived required-field and read-only-phase matrices, against `validate.sh` and `validate.ps1`. Auditing the PowerShell half found a divergence on its first run: the two reported the same defect in different words (`cyclic needs: dependencies involving: ...` versus `cyclic dependencies`), so a human comparing platforms had to know which half produced the line. Aligned. The previous release had stated this gap as out of scope; it is now closed rather than documented (v7.101.0) | Enforced by the `Portable floor still goes red` CI step; each half must first pass a known-good control; SKIP, never PASS, where an interpreter is absent |
| 89 | Citations resolve -- every `§ N.N` and `phases/<name>.md` reference in a shipped document must name something that exists. Sections get renumbered and phase docs get renamed, and neither leaves a mark on the citing document: the pointer rots into a reference to a rule that is gone while the sentence around it still reads correctly. 103 documents scanned, zero dangling at the time this shipped, which is the point -- it stays that way now (v7.101.0) | Enforced directly by `tools/validate.py` -- red-tested with a dead section, a dead phase doc, and a renumber of § 1.11 that orphaned 7 real citations |
| 90 | `.saipen/KNOWLEDGE/` is checked, not blanketed as project data -- RFC § 1.2 makes it durable truth an agent reads before planning, so it is agent-facing text and belongs under the citation, field-count and coverage checks. The v7.101.0 coverage inventory excluded all of `.saipen/` in one stroke, and that exemption had been hiding stale rules: `traps.md` and `decisions.md` both taught that a `WAIT:` at DONE is legal only when a `[MARKHUNT]` ticket blocks the auto-proceed, superseded nine releases earlier by § 1.2's two fixed forms, and `traps.md` described the LOG drift check as a WARN when it is a FAIL and its WARN half had been deleted as dead (v7.101.0) | Enforced directly by `tools/validate.py` -- three red tests against `traps.md` |
| 91 | "Exempt" means no rule-CONTENT check applies, never "nothing looks at it" -- citations are verified across every shipped document, exempt or not, because a pointer at a file or section that no longer exists is wrong wherever it sits. Five of the seven exemptions in fact name protocol files and three are shipped into every install; `SKILL.md` is the entry point telling a skill platform which file to read first, and its exemption reason claimed it held no protocol content. Citation resolution now covers the six named protocol files plus `extensions/subs/PROTOCOL.md`; an arbitrary `Foo.md` is deliberately out of scope and said so (v7.101.0) | Enforced directly by `tools/validate.py` -- removing STYLE.md orphans 12 citations, removing subs/PROTOCOL.md orphans 5 |
| 92 | Warn categories are reachable, not decoration -- a warning nobody can see is indistinguishable from a check that is not there. Auditing all 18 found one unreachable: `unknown-field` sat behind a branch on `additionalProperties`, and both call sites pass `state.schema.json`, which sets it `false`. The warn arm could never execute in any run ever produced. Collapsed to an unconditional FAIL, 18 categories down to 17 (v7.101.0) | Enforced by the audit method itself: probing a category requires a control mutation to fire first, so an instrument that measures nothing cannot report a clean bill |
| 93 | A gate stuck red lies as loudly as one stuck green -- `phases/verify.md` guarded only the green pole ('a gate that cannot fail is not a gate'). A run reporting that *everything* failed is a claim about the instrument first: a real defect is almost never total, a misconfigured harness almost always is. Before reporting a negative, an agent runs a control whose result is already known; control silent means report nothing and fix the instrument. Three harnesses in one session claimed total failure and were each wrong, and each would have shipped a confident false finding (v7.101.0) | Behavioral -- `phases/verify.md`; the two standing tools were separately proven to fail safe when their subject breaks |
| 94 | The shipped subSaipen TEMPLATE is validated like any instance -- it was explicitly skipped by the walk (`if p.parent.name != "TEMPLATE"`), and that is precisely where a defect lived: its `next_action` read "read the main project, plan the first tickets on BOARD.md", no legal prefix, so every subSaipen `saipen sub spawn` ever produced was born failing RFC § 1.2. The one file exempted from the check was the file the check existed to protect (v7.101.0) | Enforced directly by `tools/validate.py` -- 5 subSaipen states checked, regressing the template FAILs |
| 95 | Phase docs do not prescribe a `next_action` the validator rejects -- `phases/done.md` endorsed `next_action: wait for user command` for `goal_mode: false`, which has no legal prefix and which TEST-001 cannot execute. Replaced with § 1.2's `WAIT: user brake -- <reason>`. A phase doc that instructs an agent to write a state the protocol rejects is worse than one that says nothing, because the agent obeys it (v7.101.0) | Enforced by the prescribed-`next_action` sweep across `phases/` and `extensions/` |
| 96 | No document cites a version that has not shipped -- `tools/validate.py` bounds every `vX.Y.Z` in a shipped doc by `VERSION` itself. Writing "fixed in" plus a version number that has not shipped is easy and reads as fact; if the release slips or never happens, every such line is a promise the repository cannot keep. This release had to obey the rule before it could be written: 11 citations to v7.101.0/v7.101.0 stood above a VERSION of 7.99.0 (v7.101.0) | Enforced directly by `tools/validate.py` -- it FAILed on this repo until the release it named actually shipped |
| 97 | Every platform adapter names the cold-start kernel -- an adapter that sends a cold agent straight at `RFC.md` inverts the 2-tier design, since the constitution is ~100 KB while `BOOT.md` is under 4 and is all a bare `saipen continue` needs. Seven of the nine adapters never mentioned `BOOT.md` at all: T-204 corrected two and nobody checked the rest (v7.102.0) | Enforced directly by `tools/validate.py` -- stripping the reference from one adapter FAILs |
| 98 | Every prescribed `next_action` in a shipped doc is checked, not only the `WAIT:` ones -- restricting it to WAITs is how `phases/done.md` came to endorse a prefix-less parking value and how the subSaipen TEMPLATE shipped one, both found by a one-off sweep and fixed by hand. A sweep is not a guard. The check now covers `phases/`, `extensions/` and `.saipen/KNOWLEDGE/`, and caught `done.md` again on its first run: the hand-fix had rewritten the endorsing sentence while leaving the offending literal in the prose | Enforced directly by `tools/validate.py` -- three red fixtures across two doc roots |
| 99 | The text lint walks the shipped surface, not a curated four, and knows every cp1251 mojibake shape rather than one. `KNOWLEDGE/`, `extensions/` and the fixture READMEs sat outside its target list, and it recognised only the mangled section sign -- so a mangled arrow lived in `traps.md`, the very file documenting that corruption, invisible to the check whose subject it describes. Five sequences now, across every shipped doc (v7.103.0) | Enforced directly by `tools/validate.py` -- it FAILed on `traps.md` the moment the target list widened |
| 100 | A cited version must exist in the release ledger, not merely sit below `VERSION`. The future-version bound assumed the sequence is dense; it is not. One number was skipped entirely -- no tag, no CHANGELOG entry, no commit whose `VERSION` ever said it -- while 42 lines across `CONFORMANCE.md`, `extensions/subs/PROTOCOL.md`, both JSON schemas, `tools/validate.py`, the PowerShell floor and sixteen scenario READMEs named it as the release they shipped in. Every one was below `VERSION`, so the existing bound certified all of them. The ledger is git tags plus CHANGELOG entries; below the older of the two floors the repo has no memory and the check stays silent, which is honest -- silence above it was the defect. The scan also had to widen past markdown: the schemas and the validator itself carried the phantom number (v7.104.0) | Enforced directly by `tools/validate.py` -- red-tested by reinstating one citation |
| 101 | The release ledger's two halves are compared without repeating known history forever. Tagged 7.81.0/7.82.0 releases now have truthful backfilled CHANGELOG entries; the nine 7.84.0-7.91.0 release commits whose tags were never published live in `tools/release_ledger_baseline.json` with commit evidence instead of a permanent warning. A new tag-only or CHANGELOG-only release still WARNs separately, while an exception whose divergence disappears FAILs as stale so the baseline cannot become a blind spot (introduced v7.104.0; explicit baseline v7.134.0) | Enforced behaviorally by `tools/audit_checks.py`: a synthetic Git history proves clean, new tag-only, new CHANGELOG-only, and stale-baseline paths by executing `tools/validate.py`; no remote ref is created or changed |
| 102 | "Latest" on GitHub means highest version, not most recently pushed. `release.yml` left `make_latest` at its default, so re-pushing an old tag -- exactly what correcting a mistagged release requires -- marked that old release Latest and buried the current one. Observed live while fixing `v7.101.0`, whose tag had been pushed at a commit two releases behind. The workflow now decides from `git tag -l | sort -V`, not from the clock (v7.104.0) | Enforced by the `Is this the highest tag?` step in `release.yml` |
| 103 | A check that reads external state refuses to run on a PARTIAL view of it. The release-ledger check treats git tags and CHANGELOG entries as two halves of one record; `actions/checkout` clones shallow and fetches no tags, so in CI the ledger arrived with only its CHANGELOG half and two legitimately-tagged releases read as never having happened. It FAILed a correct repository on its first CI run -- the instrument broken, not the subject, and only visible because it had never been run anywhere but a full local clone. Both halves present or the check is skipped with a WARN naming why; both workflows now check out with `fetch-depth: 0` (v7.105.0) | Enforced directly by `tools/validate.py` -- red-tested against a working-tree copy with no git at all, and confirmed still able to go red with tags present |
| 104 | The UI palette has one name and every document uses it. `UI.md` names **Vintage Golden** as the default palette -- not a theme, not one option among several -- and the eighteen tokens under it are the reference an implementation is measured against. The name it superseded lived in 46 files: two shipped root docs and 44 locale copies, all of which would have gone on naming a palette the defining document no longer knows. Same shape as the 33 guides teaching a superseded `WAIT:` form and the seven adapters pointing a cold agent at the constitution: a change that lands where it was written and nowhere else. The superseded literal is assembled in code rather than written into this row, because this file is itself scanned (v7.106.0) | Enforced directly by `tools/validate.py` -- red-tested in both directions: reinstating the old name in a shipped doc, and renaming the palette away in `UI.md` |
| 105 | The palette-name guard holds a LIST of superseded names, not one. The first name it enforced was itself wrong -- a one-letter slip in the name shipped one release earlier -- so the correction had to travel the same 49 files the rename did, and the guard had to learn the name it had been created to enforce. A constant would have made the second rename a code change; a list makes it a one-line append, which is the difference between a rule that survives being wrong and one that gets deleted. `CHANGELOG.md` is exempt: it records what the name WAS, and a rule that forces a rewrite of the past is one that gets disabled the first time it is inconvenient (v7.107.0) | Enforced directly by `tools/validate.py` -- red-tested with both superseded names, and confirmed not to trip on the CHANGELOG entry that legitimately carries one |
| 106 | Workspace hygiene is checked, not merely asserted -- RFC § 1.7 forbids `saipen set` from copying phases or scripts into a project, because a copy goes stale the moment `saipen_home` moves and nothing in the project would ever say so (`KNOWLEDGE/traps.md` records exactly that failure for the skill-directory copies, which must be re-injected after every pull). `.saipen/` carrying `phases/`, `tools/`, `tests/`, `schemas/`, `adapters/`, `templates/` or any core doc now FAILs. `extensions/subs/` is deliberately not in that list: those are the project's own subSaipen instances, not a copy of the home (v7.108.0) | Enforced directly by `tools/validate.py` -- red-tested by creating `.saipen/phases/` in a scratch project |
| 107 | **Behavioral, not machine-checkable, and stated rather than silent**: RFC § 1.8's no-rush rule -- a raw backlog MUST NOT be implemented in one pass, and a trailing "etc." MUST NOT be extrapolated into invented work. No artifact in `.saipen/` witnesses it: a board with twenty tickets from one prompt is shape-identical to a board with twenty tickets from twenty prompts, and the invented item reads exactly like a real one. Evidence is the scenario fixtures and review, never the validator. Recorded here because a MUST with no row at all is indistinguishable from a MUST nobody remembered (v7.108.0) | Documented limit; `tests/scenarios/` carries the behavioral cases |
| 108 | **Behavioral, not machine-checkable, and stated rather than silent**: RFC § 2.3's completion rule -- the agent MUST finish the minimal coherent set for a requested workflow and MUST NOT expand into adjacent epics. Both halves are judgement about scope, and scope leaves no trace a file can carry: the same finished board satisfies or violates it depending on what was asked. Evidence is review. The rule stays normative; what this row records is that no automated gate stands behind it (v7.108.0) | Documented limit; enforced by review, not by any tool in this repo |
| 109 | Every RFC section that states a MUST is claimed by a CONFORMANCE row. The doc-coverage check answers "is any check looking at this FILE?"; nothing asked the same question one level up, about RULES, and three sections turned out to state nine MUSTs between them with no row claiming any -- not disputed, not exempted, simply unaccounted for, and invisible to every check in the repo because they all compare things that ARE written. A behavioral rule no validator can test still gets a row saying so: that row is how the protocol admits a limit instead of leaving a silent hole (v7.108.0) | Enforced directly by `tools/validate.py` -- it named all three on its first run, and dropping a citation from any row turns it red again |
| 110 | Every release tag is swept against its own `VERSION` file, not just the one being pushed. `release.yml`'s guard is forward-only by construction -- it exists BECAUSE a tag had already reached origin pointing two releases behind, and it can say nothing about the tags that were there before it. Nothing ever swept them, so the sweep found four mismatches nobody knew about on top of the one that caused the guard: a tag one release behind, one a release ahead, and two whose commit carries the right work with an unbumped `VERSION`. Pre-existing ones are recorded with a per-entry reason rather than rewritten -- moving a published tag re-runs `release.yml` from the TAG's commit, which for these is a workflow with no guard at all -- and a NEW mismatch FAILs (v7.109.0) | Enforced by the `Every tag agrees with its VERSION` CI step -- red-tested with a fresh mismatched tag, and with a stale exemption entry, which also FAILs |
| 111 | An exemption list is rechecked, not trusted. `tools/audit_tags.py` FAILs if a tag listed as a known mismatch has since come to agree with its `VERSION`: an entry that no longer describes a real defect is how a check quietly stops covering what it claims to, and this session had already found that exemptions are where coverage rots (v7.109.0) | Enforced by the same CI step -- red-tested by adding a healthy tag to the list |
| 112 | A mangled `.saipen/` file is diagnosed, not fatal. `STATE.md` is the first thing this validator reads, and it read it as `utf-8-sig` -- so a UTF-16 file (PowerShell 5.1's default, already recorded in `KNOWLEDGE/traps.md`) raised `UnicodeDecodeError` and killed the entire run: a Python traceback, zero FAILs, and not one other check performed. From a pre-commit hook. The same three shapes fail differently in every other tool, which is what hides the cause: the grep-based portable floor reports missing fields, and a BOM alone breaks the `^---` match so the frontmatter parses as silently empty. All three checkpoint files are now encoding-checked up front, by name, and every remaining check still runs (v7.110.0) | Enforced directly by `tools/validate.py` -- red-tested with UTF-16 (with and without a BOM) and a BOM-carrying UTF-8 file; each gives one named FAIL and seven surviving PASSes where the traceback had given none |
| 113 | A state written by a NEWER schema than the validator understands no longer passes silently. The invariant read `schema_version >= CURRENT`, which is the wrong end: a newer state may carry required fields this validator has never heard of, or the same field names with changed meaning, and every PASS is then a claim with nothing behind it. `schema_version: 99` validated clean at exit 0. Same defect class as the release-ledger check running on half a ledger -- a check reporting on data it cannot evaluate. WARN, not FAIL: a FAIL would block every commit in a project the moment the protocol bumps its schema, including during the bump (v7.110.0) | Enforced directly by `tools/validate.py` -- red-tested with a future schema_version |
| 114 | `mode: read-only` means two different things and both documents now say so. Core's is a **capability** lock -- filesystem write is unavailable, so RFC § 1.3 bans all seven phases whose work product is a file write, `PLAN` and `ADD` among them. A subSaipen's is a **scope** lock: it writes its own `STATE`/`BOARD`/`LOG`/`kitchen` freely and is barred only from the shared tree, so its ban is the four phases whose product lands outside that folder. `extensions/subs/PROTOCOL.md` § 1 had claimed the contract was "identical" while `tools/validate.py` enforced four -- so a reader obeying the document would never `PLAN`, and every real subSaipen did, including the shipped TEMPLATE. Drift in the rarer direction: the document was STRICTER than the tool (v7.111.0) | Enforced directly by `tools/validate.py` -- both bans are named constants and the drift detector parses PROTOCOL.md's own sentence, failing on a missing anchor as well as a changed list |
| 115 | `HUNT -> DONE` is legal for a subSaipen and only for one. RFC § 1.6 routes `HUNT` to `ADD`/`PLAN`/`SCOUT`/`BLOCKED` because for Core a clean sweep still has to decide what work it creates; a reporting subSaipen's deliverable is its OUTBOX, and the add step happens in the main project during `collect` (PROTOCOL.md § 4). `saihunt` had been sitting in exactly that state, truthfully, since its first sweep -- no check had ever looked at a subSaipen's transitions at all (v7.111.0) | Enforced directly by `tools/validate.py` -- red-tested; the same transition from any other phase still FAILs |
| 116 | A subSaipen's `STATE.md` is held to the same rules as Core's. PROTOCOL.md § 1 says "same `STATE.md`/`BOARD.md`/`LOG.md` shape", and the validator printed `subSaipen STATE.md shape valid` while checking a fraction of it: the ninth required field (`transition_from`), transition legality, the ISO-8601 UTC form of `updated`, § 1.10's command vocabulary and § 2.4's goal counters were all Core-only. The message claimed a shape it had not checked -- the mirror of the v7.101.0 inversion, where the prefix rule was stricter for a read-only worker than for the state a cold agent boots from (v7.111.0) | Enforced directly by `tools/validate.py` -- red-tested with six separate mutations of a live subSaipen state, each producing its own FAIL |
| 117 | A fixture whose validator CRASHED is reported as a crash, not as a failure for the wrong reason. Both exit non-zero, and the softer wording sent the reader at the fixture when the defect was in the tool: a `NameError` on a constant declared after its first use, on a branch this repository's own `STATE.md` never enters, so only a fixture could reach it (v7.111.0) | Enforced directly by `tools/run_scenarios.py` -- red-tested by injecting a crash into the path two fail-fixtures take |
| 118 | No tool reads a module-level name before the line that assigns it. `tools/validate.py` is a 2300-line straight-line script whose checks execute in file order, so a constant placed below its first use is a `NameError` waiting for the one input that reaches that branch -- and three landed in a single day, every one invisible locally because the branch reading it only runs on a state this repository does not have. `SAIPEN_COMMANDS` was caught by a fixture; `IS_SAIPEN_HOME` and a name that never existed at all were caught while being written. Ruff cannot see these: the name IS bound, just later, and `F821` reports only names never bound anywhere (v7.112.0) | Enforced by the `No tool reads a name before assigning it` CI step -- red-tested by reproducing the exact v7.111.0 defect |
| 119 | `requires:` is held to RFC § 1.3's capability vocabulary. The field was type-checked as an array of strings and its VALUES compared to nothing, so `requires: [pyhton]` read as an unknown capability -- and § 1.3 says an unmapped entry "is not a licence to ignore it", the agent MUST degrade to the mode describing what is lost. It cannot do that for a capability nobody defines, so a typo silently REMOVED a requirement instead of tightening it. WARN, not FAIL: the vocabulary is explicitly open to new entries (v7.112.0) | Enforced directly by `tools/validate.py` |
| 120 | `saipen_version` is compared to the protocol generation actually installed. It was type-checked as an integer and compared to nothing at all, so a project declaring 6 while running against a v7 home had every v7 rule applied to a v6 state with no signal anywhere (v7.112.0) | Enforced directly by `tools/validate.py` -- WARN, since a project may legitimately lag its home |
| 121 | A warning that states a count lists that many, or says how many it hid. The release-ledger warning printed "10 release(s)" and then listed eight, silently truncated. Small, and exactly the kind of thing that teaches a reader to stop trusting the numbers in a message (v7.112.0) | Enforced directly by `tools/validate.py` -- the truncation now appends "and N more" |
| 122 | The installed pre-commit hook is not from an older generation. In a consuming project the hook is the ONLY thing gating a commit, and its text is baked into `.git/hooks/pre-commit` at install time -- it never updates itself, so a hook installed twenty releases ago goes on running whatever logic it was born with, silently. That is the failure `KNOWLEDGE/traps.md` already records for the injector's skill copies, which need a re-inject after every pull; the hook had no equivalent signal at all. It carries a generation stamp now, and the validator compares it against the number the installer ships -- parsed out of `install_hook.py`, not imported, since that module does its work at import time. This repository's own hook was stamp-less, which is how the check introduced itself (v7.113.0) | Enforced directly by `tools/validate.py` -- red-tested with an older stamp and with no stamp; WARN, since a stale hook still validates something |
| 123 | The hook says so when it validates NOTHING. Reaching its final `exit 0` means neither `validate.py` nor the portable floor was found -- usually a moved `saipen_home`, and if `STATE.md` is UTF-16 the `sed` fallback recovers nothing either. The commit was allowed through, correctly, and in complete silence: an unvalidated commit that LOOKS validated is the same silent PASS this protocol has spent ten releases digging out. It stays fail-open -- blocking every commit on a broken install is worse -- and now prints why (v7.113.0) | Verified by reading the generated hook; the message names `saipen_home` and the command that repairs it |
| 124 | The reply-language rule lives in the kernel, not behind an escalation. `STYLE.md` has carried it since v7.23.0 -- answer in the language the user themselves typed, English when a first message carries no prose -- but `BOOT.md`, the only file a bare `saipen continue` reads, listed `STYLE.md` solely under "rule questions the phase doc doesn't answer". An agent that booted and simply worked never opened it, so the rule governing every response from its first token sat behind an escalation nobody escalates to. `BOOT.md` states it directly now -- the one thing it repeats rather than points at, because a pointer is too late by construction (v7.114.0) | Enforced directly by `tools/validate.py` -- red-tested by removing the rule from BOOT and by removing the English default from it |
| 125 | The ban on ambient language signals names the repository itself. `STYLE.md` forbade inferring the user's language from the OS/IDE locale, the platform UI, or unrelated prior context -- three sources, and it missed the one this project is full of. SAIPEN ships 33 translated guides and 32 locale directories, so an agent working inside it is surrounded by Ukrainian, Japanese and Estonian prose that neither it nor the user wrote. Second real incident of the class, after the German one that produced the original rule: a session answered a Russian-speaking user in Ukrainian, out of a repository that merely CONTAINS Ukrainian. Files are content to produce, never a cue for which language to speak (v7.114.0) | Enforced directly by `tools/validate.py` -- red-tested by dropping the clause |
| 126 | Every translated locale has a guide and every guide has a locale. The two sides name Estonian differently -- `et` in `.saipen/saitranslate/kitchen/` (ISO 639-1, a language) and `EE` in `guides/` (ISO 3166, a country, chosen to sit beside the flag in a human-facing badge). Both are defensible in their own role; what was missing was any statement of which governs where, so the two sets could diverge in silence and the first tool to join them would drop Estonian without a word. The alias is written down and the join is checked both ways, with English exempt by name as the source language (v7.114.0) | Enforced directly by `tools/validate.py` -- red-tested three ways: a locale with no guide, a guide with no locale, and the alias target renamed |
| 127 | RFC § 1.2's STATE freshness marker is enforced. `last_event` exists to catch a `STATE.md` that has drifted from its own `LOG.md`: lower than the LOG tail is stale, higher is corrupt or from an incompatible branch. `tools/validate.py` originally checked only a present value; schema-v2 migration later made presence itself enforceable without rejecting readable legacy state (v7.115.0) | Enforced directly by `tools/validate.py` -- canonical mutations cover missing, lower, and higher values; the executable migration probe confirms exact values stay silent |
| 128 | **Stated limit of the rule-coverage check**: it works at SECTION granularity. Every RFC section stating a MUST must be cited by a CONFORMANCE row (v7.108.0), and § 1.2 is cited by dozens of them -- so one unenforced MUST sitting inside a heavily-cited section is invisible to it, which is precisely how the `last_event` clause survived. Recorded rather than silently narrowed: the check is worth keeping and its blind spot is worth knowing (v7.115.0) | Documented limit; per-MUST granularity would need each clause to carry an id, which the RFC does not have |
| 129 | TEMPLATE's placeholders cannot escape into a live subSaipen. The shipped template carries `agent: <name>`, an empty `saipen_home` and a fixed `updated:` that `saipen sub spawn` replaces (PROTOCOL.md § 6). A check already stopped a concrete path leaking INTO the template; nothing stopped a placeholder surviving OUT of it. It matters concretely: RFC § 1.4 decides concurrency by comparing `agent:` against itself, so a spawned worker still called `<name>` makes every liveness comparison meaningless (v7.115.0) | Enforced directly by `tools/validate.py` -- red-tested with both placeholders escaping into a live subSaipen |
| 130 | RFC § 1.4's claim fields are checked, not merely spelled correctly. `claim_time` was a recognised field NAME whose value nothing ever looked at, while § 1.4 decides whether a ticket is live or forfeitable by comparing it against a 15-minute window -- and a stamp with no zone marker is not comparable across agents at all, the identical argument § 1.2 already makes for `updated`, which IS checked. A shipped fixture had carried a zone-less `claim_time` for releases. An `owner` without a `claim_time` or the reverse is half a claim and warns: liveness is read from the pair, so one alone cannot be judged (v7.116.0) | Enforced directly by `tools/validate.py` -- red-tested with a zone-less stamp and with each half of the pair alone; the well-formed pair is silent |
| 131 | Warnings print their category. It appeared only in the "... and N more" roll-up, so every individual warning was anonymous and anything grepping for the key found nothing. That is a trap `KNOWLEDGE/traps.md` has recorded since a warn-coverage audit scored 8 of 8 categories unreachable -- and it was walked into five more times after being written down, twice while writing checks in this session. A trap that keeps catching people after it is documented is a missing affordance, not a discipline problem (v7.116.0) | Verified by reading the output: every WARN line now carries `[category]` |
| 132 | `review_passes` enforces the two-pass cap mechanically, which is the reason RFC § 1.2 says the field exists: "to enforce its two-pass cap mechanically instead of from memory". The field NAME was recognised by the board grammar and the number was never read, leaving the cap exactly where the RFC says it must not be -- in memory (v7.117.0) | Enforced directly by `tools/validate.py` -- red-tested with a ticket at four passes |
| 133 | The human digest is the shape `phases/ship.md` promises, and is not from another era. That doc says "(over)write ... exactly three short lines" -- `done:`/`remaining:`/`awaiting:` -- "overwrite every time", and `saipen stop` writes the same file. Neither half was checked, and the live digest was found naming a release 33 versions old: every ship since had silently skipped the write, including fourteen in the session that added this check. A snapshot nobody refreshes is worse than no snapshot, because it reads as current (v7.117.0) | Enforced directly by `tools/validate.py` -- red-tested with two lines, with the three in the wrong order, and with a foreign version named in the text |
| 134 | MARKHUNT's closure manifest is read. `phases/markhunt.md` specifies `.saipen/kitchen/markhunt_progress.md` in full -- `vectors:` (which of scope categories 1-5 are done), `surface:`, `findings:`, `cursor: partial | done`, `head_start:`/`head_end:` -- and states plainly why it exists: the file IS MARKHUNT's closure check, "the thing HUNT gets from its exact hash-match skip and MARKHUNT historically lacked, leaving completeness pure self-report". No tool had ever opened it, so completeness was back to pure self-report by a different route. The shape, the `cursor` vocabulary, the vector-completeness rule for `cursor: done`, and the contradiction of a `partial` manifest under a phase that has moved on are all checked (v7.118.0) | Enforced directly by `tools/validate.py` -- red-tested across nine cases: five that must fail, four that must stay silent including no manifest at all |
| 135 | A `no-git` head pair must be a PAIR. `markhunt.md` permits the literal `no-git` in both `head_start` and `head_end`, and says the head-equality closure test is then "satisfied automatically". A mixed pair -- one real hash, one `no-git` -- is undefined by that wording and would skip the equality test on the strength of half a reason. Found by reading the spec closely enough to enforce it, which is the only way that class of gap surfaces (v7.118.0) | Enforced directly by `tools/validate.py` -- red-tested with a mixed pair, and with both fields `no-git` confirmed silent |
| 136 | The canonical validator's checks are proved still able to go red, not just the portable floor's. `tools/validate.py` carries about 160 failure paths, while ordinary valid inputs exercise only a fraction. Every other check once rested on a hand test from the day it was written, which is exactly how one lay dead from `feae149` to v7.99.0. `tools/audit_checks.py` breaks a known-good copy one condition at a time and asserts the validator names each specific failure; `tools/audit_floor.py` does the same for both portable halves | Enforced by the `Validator checks still go red` and `Portable floor still goes red` CI steps; each harness rejects expectations already present in its unmodified control |
| 137 | Every case in that audit must be evidence. A case whose expected text already appears in the UNMODIFIED repository proves nothing, and five did on the first run -- "at most one", "cyclic" and "dangling needs" all occur in the PASS lines announcing those very checks succeeded. The harness compares only FAIL/WARN lines now and fails loudly on any case whose expectation is present before the mutation (v7.119.0) | Enforced by `tools/audit_checks.py` itself -- the control run is a precondition, not a formality |
| 138 | The portable floor no longer claims conformance in the canonical validator's words. Both halves ended with `Validation complete. Agent is conformant.` -- the exact sentence `tools/validate.py` prints -- while catching 11 of 41 defects it catches. A host without Python read a far stronger claim than the file can make. They now say `Portable floor complete: no structural break found` and name themselves a SUBSET. Correcting a claim is not a new check, so the freeze permits it, on the same footing as v7.96.0's corrections (v7.120.0) | Verified by running both halves; `tools/audit_floor.py` proves every current mutation goes red, unaffected by the wording |
| 139 | A tool that shells out picks a real `bash`, never `sh`. `tools/audit_parity.py` fell into both halves of this trap in one release: on Windows a bare `bash` resolves from Python to the WSL stub, which without a distro prints a UTF-16 error and runs nothing (already recorded in `KNOWLEDGE/traps.md`), and its fallback to `sh` meant Ubuntu handed it **dash**, which `tests/validate.sh` -- shebanged `#!/bin/bash` -- exits 2 on immediately. It died in CI in 0.4 seconds, which is by itself the diagnosis: neither tool could have run in that time (v7.121.0) | Enforced by the tool's own shell selection; reproduced locally with `dash tests/validate.sh`, exit 2 |
| 140 | A precondition names what it saw. That same failure printed "one of the two tools rejects an UNMODIFIED copy" and stopped -- true, useless, and it cost a CI round trip to learn nothing at all. It now names which tool, its exit code, and the FAIL lines it produced. This repository has spent twenty releases finding checks that report without diagnosing; writing another one is the same defect wearing a new hat (v7.121.0) | Verified by reading the tool's control-failure branch |
| 141 | The closed ticket-field list is stated in the document that owns it. `tools/validate.py` has FAILed any field outside `needs/owner/claim_time/blocker/verify/review_passes` from the beginning, with a message citing "RFC § 1.2's field list" -- and § 1.2 named neither the list nor `verify:`, which 72 of this repository's own tickets carry. `phases/plan.md` cited § 1.2 for it as well. Three places pointed at a clause that did not exist. § 1.2 now states the list, each field's job, and the `\|` escape (v7.122.0) | Enforced directly by `tools/validate.py` -- the tool's set and the document's are compared, red-tested by renaming a field in the RFC and by removing the paragraph |
| 142 | § 1.10's command surface is compared against the tool's copy -- the seventh such set, after required fields, phase enum, from-any-phase, read-only bans, next_action prefixes and WAIT categories. It agreed on the day it was checked, which is the point: a copied vocabulary with no comparison is a bet that nobody edits either side (v7.122.0) | Enforced directly by `tools/validate.py` -- red-tested by renaming a command in the RFC |
| 143 | **Stated limit of the citation checker**: it proves a cited section EXISTS, never that the section says the thing being cited. That is how `verify:` stayed undefined while two documents and one tool cited § 1.2 for it -- every one of those citations resolved, because § 1.2 is real. Closing this in general needs each clause to carry an id, which the RFC does not have; the closed sets are covered one at a time instead, and this row records why that is a series rather than a single check (v7.122.0) | Documented limit; eight closed vocabularies are compared individually |
| 144 | No gitlink is committed inside `.saipen/`. A subSaipen's kitchen is a sandbox and may legitimately hold a clone -- `saiwiki` keeps the GitHub wiki there. `git add -A` turns a nested repository into a mode-160000 entry: a pointer to a commit no clone of this repository can fetch, carrying none of the content. Git prints a hint about it, which scrolls past inside a wall of CRLF warnings. It shipped in v7.122.0 exactly that way -- in the very commit that added two new drift checks (v7.123.0) | Enforced directly by `tools/validate.py` -- it FAILed the pushed v7.122.0 tree on its first run, which is how the defect was found |
| 145 | A row cannot keep claiming an enforcement that is gone. This table only ever grew -- 144 rows, not one retirement -- and nothing made a rule loud when the tool, CI step or fixture behind it disappeared. A row naming something deleted reads exactly like a row that is enforced, which is the whole difference between a guarantee and a decoration; row 78 sat wrong for releases and was corrected only because somebody measured it by hand. This is the mechanical half of retiring a rule: it cannot decide that a rule is obsolete, but it refuses to let one claim an enforcement that no longer exists, so the choice -- restore it or retire the row -- has to be made out loud (v7.124.0) | Enforced directly by `tools/validate.py` -- red-tested three ways: a named tool deleted, a named CI step renamed, a named fixture removed |
| 146 | **Behavioral, not machine-checkable, and stated rather than silent**: RFC § 1.6's retry rule -- a repeated attempt MUST be able to name what changed since the last one, and if the honest answer is "nothing" the retry is forbidden. Counters do not carry this: `phases/verify.md`'s 3-hypothesis/2-fix-cycle cap and `phases/ship.md`'s retry-once both bound how MANY times an attempt repeats and neither asks whether the second could go differently. `verify.md` had said "never re-test without new evidence" of debugging hypotheses for a long time -- true of every repeated attempt and written for one of them. No artifact witnesses whether the agent really had new information, so the LOG line naming the delta is the mechanism: it removes the excuse rather than the possibility (v7.124.0) | Documented limit; the rule is normative in § 1.6 and the two phase docs that repeat attempts now inherit it by name |
| 147 | BUILD looks for existing code before writing new code: this project's own, then the standard library, then a dependency the project ALREADY has -- adding one is a ticket, not a build step -- and only then a fresh implementation. One pass down that ladder, deliberately, not a research project: the target is the third private copy of the same helper, not turning every edit into an audit. A search that costs more than writing it ends in an honest LOG line, which beats a silent duplicate (v7.124.0) | Documented in `phases/build.md`; behavioral, like the rest of that document |
| 148 | `agent:` has a definition, so § 1.4's comparison has two sides. Every concurrency rule in that section compares `agent:` against "itself" and nothing said where an agent got its own value -- so each session invented one. This repository's history carries six for two or three actual actors, and one LOG line reached disk reading `[agent: id]`. A comparison with one side undefined fires both ways: a model upgrade renames the seat and the next session sees a stranger, while two genuinely different actors sharing a generic value are indistinguishable. The field names the SEAT and is inherited from `STATE.md`; changing it requires a `DEC` naming both values, and a model version change is not a different actor (v7.125.0) | Behavioral for the stability half -- nothing can tell a genuine handover from a renamed seat -- but placeholders are mechanical and FAIL: red-tested with `id`, `<name>`, `AgentID`, `unknown` |
| 149 | Installed skill copies carry `VERSION`. RFC § 1.2 says `<saipen_home>/VERSION` is the single source of truth for the version guard, but both injectors copied `RFC.md` without copying `VERSION`, so a fresh skill could only degrade to read-only with "VERSION missing" before doing useful work. A source-token check claimed this was enforced without installing anything | Enforced behaviorally by `tools/run_scenarios.py`, which executes both injectors in isolated homes and compares the installed VERSION to the source |
| 150 | Skill-copy refreshes are replacements, not overlays. `Copy-Item -Recurse -Force` and `cp -r` do not remove files deleted from the source, so installed copies became hybrids of old and new layouts. Cleanup must precede recreation: the first shell implementation created `tests/`, deleted it, then failed to copy the floor scripts while a literal-order source check called that enforced | Enforced behaviorally by `tools/run_scenarios.py`: both injectors start over seeded stale managed directories, must remove every sentinel, and must land both floor scripts; a deleted-`tests/` red-control must fail |
| 151 | Installed validators do not mix their own install tree with the project's release ledger. When `C:\Users\...\skills\saipen\tools\validate.py` was run from the SAIPEN home, `IS_SAIPEN_HOME` correctly saw the project root, but the phantom-version check read `CHANGELOG.md` beside the installed skill while reading git tags from the project. That is a partial ledger made out of two different roots, so changelog-only releases were reported as phantom even though the source validator passed. The release ledger now comes from the project root whenever the project root is the SAIPEN home | Enforced directly by `tools/validate.py` -- reproduced by running the Codex-installed validator against this repository |
| 152 | SHIP preflight has a repair loop. SHIP requires 100% green, but its DFA row allowed only `DONE`/`BLOCKED`; when v7.126.0 preflight found a fixable delete-after-create bug in the injector, returning to BUILD was illegal and BLOCKED would have falsely ended goal mode. A failure found before commit/tag/push may return the current ticket `SHIP -> BUILD`, then MUST repeat VERIFY/REVIEW/SHIP. Publish failures and already-pushed work do not use this edge | Enforced by `tools/validate.py`, `phases/ship.md`, and executable scenario `ship-preflight-build-return`; transition red-test now pins an always-illegal INIT->SHIP pair instead of depending on the live STATE phase |
| 153 | The release ledger observes git tags once and reuses that snapshot for both phantom-version and ledger-divergence checks. It queried `git tag -l` twice; the first failure emitted a WARN, but a transient failure of only the second query disappeared behind `except: pass` and silently skipped the comparison after the same run had proved tags available | Enforced behaviorally by `tools/audit_checks.py`: Git Trace2 observes one real child process; an AST-located duplicate executable call is the red-control and must produce two |
| 154 | SHIP publishes the intended release tag by exact ref, never by selecting all annotated tags from ambient local state. A one-off inspection fetch preserved a prior `v7.101.0` object under local ref `remote-v7101`; later `git push --follow-tags` published that unrelated ref beside v7.126.0. The creator was not a live script, but the broad publication path was still prescribed as "push tags" (v7.130.0) | Command contract in `phases/ship.md`; re-measured against a temporary bare origin with both intended and stray annotated tags present -- the exact refspec published only the intended tag |
| 155 | A guard about script behavior executes the script. The injector distribution/order block in `tools/validate.py` searched for tokens and pinned one exact `mkdir` layout; formatting alone could kill it while a delete-after-create injector once passed its predecessor. The same sweep found portable-floor behavior inferred from message text and a tag-query invariant inferred by counting source literals. All three now execute. Retained source reads are explicitly syntax contracts: document vocabularies/citations, declarative hook generation, workflow step identity, or static Python name-order analysis (v7.131.0) | Enforced by `tools/run_scenarios.py`, `tools/audit_floor.py`, `tools/audit_checks.py`, and `tools/audit_order.py`; injector and floor source formatting is outside their observation path |
| 156 | Checkpoint paths are bound to one dynamically resolved project root, never ambient cwd. A relative `.saipen/` write after `cd` could land in another project without an error; persisting an absolute root in STATE would merely trade that for false corruption after a move or clone. BOOT binds once, Git identity is the worktree/common-dir pair, linked worktrees use the main worktree's gitignored memory, non-Git projects use the nearest ancestor with `.saipen/`, and explicit `--project-root` deliberately overrides cwd (v7.132.0) | Enforced behaviorally by `tools/run_scenarios.py`: correct Git root, nested Git cwd, foreign Git cwd rejection, explicit override, nested non-Git cwd, and linked-worktree ownership all execute the canonical validator; every non-owner path is asserted to remain free of a second `.saipen/` |
| 157 | A canonical mutation never disappears because normal protocol maintenance changed the storage layout. LOG sealing left active `LOG.md` without two events, so the backwards-ID mutation skipped; `audit_checks.py` still exited 0 after saying 40/41, and parity silently changed its denominator from 41 to 40. The logical LOG target now resolves to active or the newest sealed segment with an event pair, both runners save and restore that same physical file, and any unavailable mutation is fatal (v7.133.0) | Enforced by `tools/audit_checks.py` and `tools/audit_parity.py`; an emptied active LOG with sealed history still applies 41/41, while removing every event-bearing LOG makes both tools exit nonzero naming the skipped mutation |
| 158 | `last_event` has a migration boundary instead of becoming required overnight. A missing marker in schema v1 or an unversioned state WARNs and remains readable; the next checkpoint writes schema v2 plus the numeric ID of the real LOG tail. Schema v2 with an event-bearing LOG and no marker FAILs. A fresh empty bootstrap omits it honestly, and Recovery derives the same value from sealed plus active LOG so repeated recovery is idempotent (v7.135.0) | Enforced behaviorally by `tools/run_scenarios.py`: six executions cover legacy, missing-v2, exact, stale-after-LOG-advance, recovered-exact, and above-tail states; `tools/audit_checks.py` keeps all four marker and schema-metadata failure paths red |
| 159 | A tag audit that has enumerated tags fails closed if its `git cat-file --batch` process exits nonzero or returns malformed, truncated, or surplus protocol bytes. A legal `<spec> missing` record remains a warning for historical tags without VERSION; losing or misparsing the batch evidence is not the same fact and cannot produce PASS (v7.136.0) | Enforced behaviorally by `tools/audit_checks.py`: executable Git shims make the batch process exit 9, return an empty-OID malformed header, truncate a blob, and append surplus bytes; all four controls must exit nonzero with focused FAIL text and no PASS, while the normal tag audit remains green |
| 160 | Bootstrap install and uninstall reports preserve process truth and user-owned config bytes. A failed read, backup, transform, copy, removal, or write exits nonzero and cannot reach completion text; a successful install/uninstall pair removes only the managed block and its own separator, never surrounding spaces or blank lines (v7.137.0) | Enforced behaviorally by `tools/run_scenarios.py`: real Git Bash and PowerShell installs replace stale skill trees, run their installed validators, uninstall, and reproduce seeded CRLF config bytes exactly; four executable shell-write, shell-transform, PowerShell caught-copy, and PowerShell read failure controls must exit nonzero without `Done.` |
| 161 | Export archives belong to the project that owns `.saipen/`, never to ambient cwd. Git invocation resolves the worktree/common-dir identity so a linked worktree uses the main owner's memory; only a common directory named `.git` may nominate its parent, so an unrelated external git-dir cannot become the owner. Non-Git invocation selects the nearest owning ancestor; a foreign Git repository fails unless a non-empty explicit project root is supplied. The archive is written beside that owner and contains its `.saipen/`, so changing cwd cannot silently export another project's state (v7.138.0) | Enforced behaviorally by `tools/run_scenarios.py`: shell and PowerShell exporters each execute nested, foreign, explicit-root, empty-explicit, linked-worktree, and separate-git-dir paths; all twelve inspect the real process result, successful paths open the real tar/zip output and compare an owner marker, and rejected/non-owner paths are asserted archive-free |
| 162 | The Unix crew launcher reports accepted terminal launches, not merely installed launcher names or background process creation. A launcher that exits nonzero during the observation window permits the next available launcher to be tried; exhausting available launchers fails the command and cannot print `Done.`. Three accepted launches are required before the three-window completion claim; the no-emulator command-printing fallback names zero launched windows instead (v7.139.0) | Enforced behaviorally by `tools/run_scenarios.py`: controlled `gnome-terminal`/`konsole`/`xterm` shims make every attempted process exit 9, then make the first launcher exit 0; the broken run must exit nonzero with focused failure and no `Done.`, while the working run must record exactly three accepted calls and the truthful three-window completion text |
| 163 | Generated Python bytecode is neither source nor a distributable skill artifact. Repository ignore rules cover `__pycache__/` and `*.py[cod]`; tracked cache blobs are forbidden. Because ignore rules do not affect recursive filesystem copies, both injectors remove cache directories and loose `.pyc`/`.pyo` files from the installed tools tree before reporting copy success (v7.140.0) | Enforced behaviorally by `tools/run_scenarios.py`: each real injector runs while a source-side cache sentinel exists, then its installed layout must contain no cache directory or bytecode before the installed validator executes; an installed-bytecode red-control proves that assertion can fail |
| 164 | Shell bootstrap content predicates distinguish match, absence, and process/read failure. `grep` status 1 may select an absence path; status greater than 1 fails the operation and prevents completion. Uninstall removes a managed skill path whether it is a directory, symlink, or regular file instead of declaring an existing non-directory clean (v7.141.0) | Enforced behaviorally by `tools/run_scenarios.py`: executable grep shims return 2 during install and uninstall, which must both exit nonzero without `Done.` and preserve the config bytes; a regular-file skill destination must be removed under a normal uninstall |
| 165 | The historical tag audit distinguishes an absent Git executable from a launched enumeration process that fails. Missing Git may SKIP loudly because no audit is possible on that host; a nonzero `git tag -l v*` result is observed evidence loss and must exit nonzero with a focused failure, never PASS or SKIP (v7.142.0) | Enforced behaviorally by `tools/audit_checks.py`: an empty-PATH control must produce the sole allowed missing-Git SKIP; the existing executable Git shim then returns 9 with a diagnostic for the initial tag enumeration, and the audit must propagate a nonzero result containing that diagnostic without printing any success/skip verdict |
| 166 | The shell portable floor's LOG filter treats an empty set of malformed lines as success while propagating failures from every pipeline process. A read/filter error exits nonzero with a focused diagnostic before either the LOG-format PASS or portable-floor completion can print (v7.143.0) | Enforced behaviorally by `tools/audit_floor.py`: a targeted executable `sed` shim delegates every earlier call but returns 7 for the LOG BOM-strip invocation; the real floor must exit nonzero with `FAIL: LOG.md read/filter failed` and no later success text, while all normal floor mutation controls remain green |
| 167 | The installed POSIX pre-commit hook may itself run under `/bin/sh`, but its no-Python portable floor is Bash code and must be invoked through a resolved `bash`, never through ambient `sh`. If the floor exists but Bash does not, the hook fails nonzero with a focused dependency diagnostic instead of rejecting a healthy project on shell syntax or silently skipping validation (v7.144.0) | Enforced behaviorally by `tools/run_scenarios.py`: a copied real installer creates hooks against a controlled fake SAIPEN home whose floor uses a Bash here-string; real dash executes the hook with only Bash reachable and must print the floor marker, then executes it with Bash absent and must fail focused without that marker |
| 168 | A re-authorization is countable, or it expires at the next crash. Bare `saipen goal` resets `goal_waves`/`goal_tickets` to `0`, and nothing required the reset to leave a line. The bumps it cancels stay in the LOG, so § 1.5's rebuild -- which counted from the `saipen goal <text>` pivot -- restored the pre-reset totals, re-tripping a valve the human had just cleared while § 2.4 forbids tidying those counters back down. Not one-shot either: every later Recovery revoked it again from a count that only grows. The bare path is now required to write `DEC: goal reauthorized -- goal_waves N->0, goal_tickets M->0`, § 1.5 counts from the NEWEST marker rather than the first, and only increments count so a reset line's own `N->0` is not miscounted as work (v7.146.0) | Enforced directly by `tools/validate.py` -- the check REPLAYS the rebuild and compares it against `STATE.md` rather than grepping for the line, since a marker that exists but does not explain the counters is the same defect; red control is the fixture pair `goal-reauthorization-trace` / `goal-reauthorization-untraced`, identical but for the evidence |
| 169 | The chat voice & compression rule lives in the kernel, not behind an escalation. `STYLE.md` defines `caveman-дед` (structural compression, zero preambles/postambles, zero corporate apologies), but `BOOT.md` listed `STYLE.md` solely under "rule questions the phase doc doesn't answer". An agent booting on `saipen continue` skipped `STYLE.md` entirely. `BOOT.md` and `RFC.md` § 1.1 state it directly now -- mandatory before any output | Enforced directly by `tools/validate.py` -- cross-doc check `13j` asserts `BOOT.md` mandates `STYLE.md` before output and fails if omitted |
| 170 | Files the protocol APPENDS to end on a line boundary. An append to a file that stops mid-line does not add a line, it extends the last one, and no structural check can see the result because the bytes never become a second line. `.saipen/LOG.md` reached that state and every mutation `tools/audit_checks.py` appends landed inside the final entry: two of its 45 red controls stopped being evidence while the suite printed PASS. Three more live files were mid-line when this shipped -- a subSaipen `BOARD.md` ending on `## BLOCKED`, where one appended ticket costs the heading and the ticket together, and a subSaipen `STATE.md` ending on its own closing `---`. Reading the last byte is the one thing no other check here did (v7.147.0) | Enforced directly by `tools/validate.py` across all 21 append targets; red control strips the final newline from a fixture `BOARD.md`, so the mutation is bytes rather than wording |
| 171 | A shortcut resolves to a command the command surface actually defines, and the length of the shortcut says what it costs. The table's right-hand column went unread by any check: `hh` routed to `HUNT`, a PHASE with no command behind it, and `cc` routed to a 'Full pipeline' that was not a command either and quietly put commit+push behind the most-typed key, while the same paragraph still cited the doubled-first-letter rule `cc` no longer followed. `saipen hunt` now exists, `cc` is plain `saipen continue`, and the pushing chain is `ccc` -- doubled is safe, tripled reaches a remote (v7.148.0) | Enforced directly by `tools/validate.py`, which parses the table itself rather than prose about it; red-tested by pointing `hh` back at a phase name |
| 172 | An empty board at `DONE` goes to `HUNT`, and only one document decides it. RFC § 1.11 step 5 and § 2.1 both mandated the auto-transition 'with no human asked' while `phases/done.md` step 2 mandated parking with a manufactured `WAIT: user brake` under `goal_mode: false`. Same state, two mandates, so whether a plain `saipen continue` kept maintaining or stopped depended on which document the agent read. § 1.11 wins and the phase doc defers to it by name; a brake the user actually asked for is still legal to sit at (v7.148.0) | Behavioral -- the executable scenario `board-empty-maintenance-transition` covers the transition; what cannot be checked mechanically is an agent choosing the losing document, which is why the losing text now points at the winner instead of restating a rule |
| 173 | Bare `saipen goal` resets the safety-valve counters ONLY against a tripped valve. Resetting unconditionally meant the most convenient key silently granted a fresh 3-wave/20-ticket budget to anyone who typed it out of habit mid-run -- §1.10 already named that failure for the `goal_mode: false` case, and it happened here (E-1468 cleared 0 waves and 3 tickets against a valve that had never tripped). The re-authorization line names the counters it cleared, so the claim checks itself (v7.148.0) | Enforced by `tools/validate.py` -- WARN, not FAIL, because sealed history is append-only and cannot be rewritten; it fires on this repository's own E-1468 |
| 174 | A resume names what is stuck. Every `## BLOCKED` ticket, any untriaged `[MARKHUNT]` finding, and a live `WAIT:` go into the reply -- report only, no new field, nothing queued. `.saipen/kitchen/digest.md` carried that view and is written at `saipen stop` and `SHIP` only, so a blocked ticket could sit unmentioned across an unbounded run of sessions, each one truthfully reporting the work it did do (v7.148.0) | Behavioral, README-only: the duty is to say it in chat, and no artifact witnesses what an agent said |
| 175 | A shortcut's stated rationale describes the table it governs. v7.148.0 grounded the scheme's length in cost -- the tripled form was 'the one that reaches a remote', true only while `ccc` was the sole three-letter row. `sss` for `saipen status` pushes nothing, so the row and the rule changed together: length is collision order (doubled to one claimant on a shared first letter, tripled to the next), and `ccc` states its remote reach as a fact on its own row instead of leaving it to be inferred from counting keys. Third instance of this class in two days, after the E-1415 citation and the `cc` row (v7.150.0) | Behavioral for the rationale; the mechanical half is `tools/validate.py`'s shortcut-resolution check, red-tested by pointing a row at a bare command-less name |

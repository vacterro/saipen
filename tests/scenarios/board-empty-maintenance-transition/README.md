Test: `BOARD.md` has no tickets in `## DOING`/`## TODO`/`## BLOCKED` and the user runs bare `saipen`. Per RFC § 2.1 ZERO-PROMPT AUTO-TRANSITION, the agent MUST NOT ask "should I hunt or add?" -- it silently transitions to `HUNT`, and to `ADD` immediately if `HUNT` comes back clean. This is a behavioral test (agent decision-making), not a structural one -- no `.saipen/` fixture to validate against; the assertion is entirely in the agent's response to the empty-board condition.

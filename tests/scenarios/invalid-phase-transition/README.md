Test: `phase: INIT` transitioning directly to `phase: SHIP` violates RFC § 1.6's transition table (`INIT -> PLAN | BLOCKED` only). Automated since v7.83.0 via `transition_from` in `STATE.md`: `tools/validate.py` reads `transition_from` and asserts the transition against the table. The fixture ships no `.saipen/` -- the assertion is that the validator catching e.g. `transition_from: INIT, phase: SHIP` and FAILing is the correct behavior. Manual grep fallback still works: `grep -n "^INIT " saipen/RFC.md` confirms `SHIP` does not appear in that line.

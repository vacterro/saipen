Test: `phase: INIT` transitioning directly to `phase: SHIP` violates RFC § 1.6's transition table (`INIT -> PLAN | BLOCKED` only). This is documented as a **conceptual** test, not an automated one: `STATE.md` records only the current phase, never the previous one, so a stateless per-run validator cannot detect an illegal transition after the fact -- it would need phase-history tracking, which is new scope beyond what shipped with the transition table itself (see `.saipen/kitchen/SAIPEN_GAP_MATRIX.md`). Verify manually: `grep -n "^INIT " saipen/RFC.md` and confirm `SHIP` does not appear in that line.

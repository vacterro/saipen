Test: A project already supports Bold and Underline but not Italic; `BOARD.md` is empty and `HUNT` finds no bugs. Per `phases/add.md`'s Evolutionary ADD selection order, the agent MUST propose the feature-symmetric gap (Italic) over a speculative, unrelated feature (e.g. "add AI chat", "add cloud sync"). Behavioral test -- no `.saipen/` fixture, the assertion is about which ticket the agent proposes, not file shape.

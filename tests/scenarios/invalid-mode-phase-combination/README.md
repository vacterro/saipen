Test: `mode: no-publish` (missing git) combined with `phase: SHIP` -- RFC § 1.3 explicitly forbids this ("Missing Git: mode: no-publish. Agent MUST NOT transition to SHIP"). The validator MUST catch this specific, already-normative combination. Not the full mode x phase matrix (rejected elsewhere as unnecessary bulk) -- just the restrictions that already have explicit teeth in RFC.md.
